module.exports = [
  {
    "version": "8.30",
    "title": "Audit log of who locked/unlocked the site and when",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** The one-click site-lockdown toggle records only the *current* 'ON since … by …' state — every flip overwrote the previous one, so there was no way to answer 'who took the site dark at 2am?'. For a switch that can blank the public site, a historical trail matters.",
      "**Audit trail.** New `lockdown_audit_log` table (actor, action lock/unlock, optional reason, created_at). The lockdown PUT handler now appends a row on every genuine state change (no-op re-saves are skipped so the trail stays clean). When the `FULL_SITE_LOCKDOWN` env var forces the gate ON, a single deduped boot-time row is recorded too, so env-driven locks show up in the same history.",
      "**Admin visibility.** The 🔒 Site lockdown card in the AdminPanel Overview tab gains a collapsible 'History' section that lazy-loads the last ~20 changes (newest first) via a new superuser-only `GET /api/admin/lockdown-audit` — each row shows the lock/unlock action, timestamp, actor, and reason.",
      "**Scope.** Full edition only — community edition has no lockdown gate and is untouched."
    ],
    "author": "System"
  },
  {
    "version": "8.29",
    "title": "Lock down the smurf detector's fingerprint overlap math with tests",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** The fingerprint signal depends on a three-piece round-trip — session middleware stamps `sess.ip`/`sess.ua`, the scorer's index reads them back, and the overlap math turns a shared machine into points. Nothing tested that chain, so a regression in any piece would silently revert the signal to 'no data' with no visible failure.",
      "**Coverage.** New `tests/smurfFingerprint.test.js` asserts: two accounts with a shared hashed IP produce a >0 contribution that names the partner; the no-overlap case returns 0 with the 'no overlapping fingerprints' detail; an empty index degrades to 'no fingerprint data available'; `stampSession` is a no-op for anonymous sessions and a write for authenticated ones (and a no-op re-stamp inside the refresh window); and an end-to-end round-trip where middleware-written prints flow through the index into the scorer.",
      "**Scope.** Tests only — no runtime, schema, payment, or community-edition changes."
    ],
    "author": "System"
  },
  {
    "version": "8.28",
    "title": "Smurf Watch: see which accounts share a fingerprint with the suspect",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** The smurf scorer's fingerprint signal already detects when two accounts sign in from the same machine, but the admin Smurf Watch detail card only showed the aggregate score and a plaintext 'shares fingerprint with account X' line. The single most useful thing for a reviewing admin — *which* other account(s) — wasn't clickable.",
      "**Partner chips.** The fingerprint signal now returns a structured `partners` list (every overlapping account, capped at 10, with overlap counts), and the Smurf Watch signal breakdown renders them as clickable chips that link straight through to each player's profile. Partner nicknames are resolved server-side so chips show names instead of bare IDs where available.",
      "**Scope.** Full edition only — community edition has no Smurf Watch. Advisory-only, no automatic action; stored scores pick up the partner list on the next recompute."
    ],
    "author": "System"
  },
  {
    "version": "8.27",
    "title": "See who tried to reach the site while the lockdown gate was on",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** When the owner-only lockdown gate is on, it's useful to see *which* URLs people are trying to reach (and from what UA family / IP) — a human hitting a deep link from a leaked share matters more than yet-another crawler. This is separate from the AI-agent traffic card.",
      "**Access log.** Every request the lockdown gate blocks is now recorded into a small in-memory ring buffer (`src/security/lockdownLog.js`, ~1000 cap, no schema) capturing timestamp, IP, path, a coarse UA family (known bots labelled via the shared classifier, otherwise a browser family like chrome/firefox/edge/safari), method, and which response it got — `html-gate` (browser navigation shown the sign-in page) or `401-empty` (API/asset request denied).",
      "**Owner visibility.** New superuser-only `GET /api/admin/lockdown-attempts` aggregates the last N days grouped by UA family with per-family decision counts, unique IPs, unique paths, and a recent-requests feed. Surfaced in a new \"🚪 Lockdown access log\" card in the AdminPanel Overview tab, sitting next to the AI-agent traffic card and hidden entirely while the gate is off.",
      "**Scope.** Full edition only — community edition has no lockdown gate and is untouched. Buffer is in-process only and resets on reboot."
    ],
    "author": "System"
  },
  {
    "version": "8.26",
    "title": "Fix dev preview: games (and all /api) failing to load",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** In the Replit dev preview every `/api` call (the mini-games' \"Failed to load daily puzzle\" being the most visible symptom) was failing. Both the Vite frontend dev server and the backend (`src/index.js`) were configured for port 5000, so the backend never bound a port and Vite's `/api` proxy had nothing to reach.",
      "**Fix.** The backend dev workflow now runs on port 3000 (the port already reserved for it in `.replit`), and `web/vite.config.js` proxies `/api`, `/auth`, and `/scouting` to `http://127.0.0.1:3000`. The frontend stays on port 5000.",
      "**Scope.** Local dev/preview environment only — production serves the built frontend from the backend on a single port and was never affected. No schema, payment, runtime, or community-edition changes."
    ],
    "author": "System"
  },
  {
    "version": "8.25",
    "title": "Block hotlinking of our logo & branded assets (Task #491)",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** A clone site has been mimicking OCE Inhouse, and AI builders (or the humans prompting them) commonly hotlink a target site's logo / favicon / imagery straight off its domain. Distinctive brand assets now refuse to load when requested from another site.",
      "**Hotlink gate.** New middleware (`src/security/assetHotlink.js`, mounted just before the static handler) inspects the `Referer` on requests for our distinctive assets — `/oa-logo.png`, `/favicon.png`, `/favicon.ico`, `/badges/*`, `/voice-packs/*`, `/sounds/*`, and scoreboard renders (`*/recap-card.png`, `/overlay/scoreboard/*`). Empty/unparseable referer, same-origin, our own domains, and known social unfurlers (Discord/Twitter/Slack/Telegram/etc.) are allowed; anything else gets a 403. Generic bundled assets (JS/CSS/fonts, the Dota minimap, `sw.js`, `robots.txt`) are deliberately NOT gated.",
      "**Configurable allow-list.** `BRAND_ASSET_REFERER_ALLOWLIST` (comma-separated host suffixes) extends the built-in default so partner domains can embed our imagery without code changes.",
      "**Owner visibility.** Every decision (allowed/blocked) lands in an in-memory ring buffer (5000 cap, no schema, no new dependency). The superuser-only `GET /api/admin/asset-hotlink-report` aggregates the last N days grouped by referer host, surfaced in a new \"🖼️ Brand-asset hotlinks\" card in the AdminPanel Overview tab so the owner can see whether the clone (or anything else) has been pulling assets off oceinhouse.gg.",
      "**Scope.** Full edition only — community edition is untouched. No watermarking, signed URLs, or takedown automation."
    ],
    "author": "System"
  },
  {
    "version": "8.24",
    "title": "Browser-smoke: fix match/tournament not-found false-failures (Task #489)",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** Three smoke journeys were false-failing with `expected selector not found`. The `match_detail` (`/match/:id`) and `tournament_detail` (`/tournaments/:id`) journeys default their sample id to `1`, which doesn't exist on prod — so both pages render their terminal `.error-state` (\"Match not found\" / \"Tournament not found.\"), and the old selectors (`.match, table, h1, h2` / `.tournament, h1, h2`) only matched the *loaded* state, never the not-found one.",
      "**Selector fix.** `match_detail` now accepts `.match-detail-header, .error-state, table, h1, h2` and `tournament_detail` accepts `.tournament-layout, .page-title, .error-state, h1, h2`, so both pass whether the sample record exists (loaded view) or not (deterministic not-found view) — and the perceptual diff still catches regressions against whichever state the baseline was captured from.",
      "**Inhouse cleanup.** Dropped the dead `.inhouse` class from the anon `/inhouse` journey selector (no element ever carried it); it now leads with `h1, main`, which always render once the SPA mounts.",
      "**Note for prod.** For a stronger canary, set `SMOKE_MATCH_ID` / `SMOKE_TOURNAMENT_ID` to ids that exist on prod so these journeys exercise the real loaded view, then re-approve their baselines.",
      "**Scope.** Full edition smoke harness only; no schema, payment, runtime, or community-edition changes."
    ],
    "author": "System"
  },
  {
    "version": "8.23",
    "title": "Unit tests for the browser-smoke diff + failure paths (Task #489)",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** `src/smoke/runner.js` had no automated coverage — the pixel-diff threshold maths, the viewport-size-mismatch branch, the missing-baseline branch, the optional-deps SKIPPED branch, the in-flight guard, and the owner-alert path were only ever exercised by the live Sunday-3am cron, so a regression would surface in production rather than in CI.",
      "**Testable refactor.** The inline perceptual-diff decision in `runSmoke()` was factored out into a pure `_diffAgainstBaseline()` helper (injectable `fs` / `pixelmatch` / `PNG`) so the threshold, viewport-mismatch, and pixelmatch-error branches can be unit-tested without launching a real browser. Runtime behavior is unchanged.",
      "**New coverage.** Added `tests/smokeRunner.test.js`: baseline-absent → ok, under-threshold → ok, over-threshold → failed, viewport mismatch → failed, pixelmatch error → failed, missing optional deps → SKIPPED bootstrap row, concurrent call → in-flight skip, and `_alertOwner` no-op when the Discord bot is unavailable (plus the happy-path DM). Wired into the existing `node --test` suite automatically.",
      "**Scope.** Test + smoke-harness only; no schema, payment, runtime, or community-edition changes."
    ],
    "author": "System"
  },
  {
    "version": "8.22",
    "title": "Browser-smoke: deeper signed-in journeys + session reuse (Task #488)",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** The smoke suite's authenticated tail only covered the signed-in `/profile` and `/inhouse` pages. The highest-value regressions hide on surfaces an anonymous visitor never sees, so the suite now drives more of the signed-in app through the existing synthetic Steam test-login.",
      "**New signed-in journeys.** Added the coach earnings dashboard (`/coach/earnings`), the Draft & Assistant page (`/draft`), and the Pro upsell / checkout-intent page (`/pro`) to `src/smoke/journeys.js`, all gated behind the synthetic login. There is no standalone \"captain draft\" route — captain draft is an in-lobby phase of `/inhouse` that needs a live 10-player lobby, which a smoke run can't deterministically reach — so the draft assistant is the closest stable draft surface.",
      "**Session reuse via storageState.** The runner now persists the authenticated Playwright `storageState` to `tests/smoke/.auth/storage-state.json` after a successful test-login and reuses it on subsequent runs (validated against `/api/auth/me`, refreshed once older than 6 days or when the cookie no longer authenticates), so it stops round-tripping the login endpoint every run. The state file holds a live session cookie and is `.gitignore`-blocked so it can never be committed.",
      "**Scope.** Full edition smoke harness only; no schema, payment, or community-edition changes. Authenticated journeys still record as `skipped` with a clear reason when `SMOKE_TEST_LOGIN_TOKEN` / `SMOKE_TEST_ACCOUNT_IDS` aren't configured."
    ],
    "author": "System"
  },
  {
    "version": "8.21",
    "title": "Browser-smoke: fix the inhouse false-failure + easier access (Task #487)",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** Capturing the first browser-smoke baselines on prod surfaced a deterministic false failure on the anonymous `/inhouse` journey (`expected selector not found: \".inhouse, h1\"`). The page renders an `<h1>` only after a brief `Loading…` state and has no `.inhouse` class at all, so the runner's fallback — which waited on only the *first* listed selector — sat waiting for a class that never exists and timed out.",
      "**Selector fix.** The inhouse journeys now lead with selectors that actually render (`h1, main` / `h1, button`) instead of the non-existent `.inhouse` class.",
      "**Runner robustness.** When the initial check misses (it races SPA hydration), the runner now waits up to 5s for *any* of a journey's listed selectors, not just the first — so selector order can no longer turn a slow-rendering page into a false failure.",
      "**Modal suppression.** The runner starts from a fresh browser context, so the site-wide WelcomeModal (\"what's new\") was overlaying every screenshot. The runner now reports the version-stamped `welcome_modal_dismissed_v<n>` flag as already-dismissed (and pre-seeds the onboarding nudge) before each navigation, so baselines capture the actual page, not the popup.",
      "**Easier access.** Added a \"📸 Browser smoke\" link to the Admin Panel Quick Links so the suite no longer has to be reached by typing the URL.",
      "**Scope.** Full edition smoke harness + admin UI only; no schema, payment, or community-edition changes."
    ],
    "author": "System"
  },
  {
    "version": "8.20",
    "title": "Billable API rate-limit quota tiers (Task #463)",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** Heavy API consumers were capped at the free/Pro per-key defaults (60 / 600 req/min) with no paid path to more throughput. Task #463 plumbs API usage into Stripe Billing so a key can buy a higher rate-limit ceiling, and adds the per-key monthly usage visibility that was missing.",
      "**Paid quota tiers.** Two monthly subscription tiers raise a single key's ceiling: **2,000 req/min** (AUD $29/mo) and **10,000 req/min** (AUD $99/mo). Bought per key from Settings → API & webhooks via Stripe Checkout; fulfilment is webhook-driven (the bump only applies once payment is confirmed) and a key can hold one bump at a time. Optionally wire dashboard-managed Prices via `STRIPE_API_QUOTA_2K_PRICE_ID` / `STRIPE_API_QUOTA_10K_PRICE_ID`; otherwise an inline AUD recurring price is used.",
      "**Monthly usage counter.** Per-key request counts now roll up from the in-process rate-limiter into a durable `api_key_usage_monthly` table via a 60s flusher (swap-and-upsert; un-written deltas are re-queued on DB failure so nothing is lost across restarts).",
      "**Usage tile.** Settings → API & webhooks shows a \"Usage this month\" summary plus per-key this-month request count, the effective per-minute cap, and the active quota tier — with inline buy / cancel controls.",
      "**Auto-degrade on lapse.** A cancelled or lapsed subscription degrades the key back to its tier default through three layers: the Stripe `customer.subscription.deleted`/`updated` webhook, an immediate local degrade on user-initiated cancel, and an hourly safety-net sweep (`degradeLapsedApiQuotas`) for missed events. A per-request grace window keeps `past_due` keys serving briefly while Stripe retries the card.",
      "**Scope.** Full edition only — the public API is full-edition-only, so the community edition is untouched."
    ],
    "author": "System"
  },
  {
    "version": "8.19",
    "title": "Official client libraries for the public API (Task #462)",
    "published_at": "2026-05-30",
    "notes": [
      "**Why.** Task #415 shipped the v2 public API but explicitly excluded SDKs, so integrators had to hand-roll HTTP calls and — riskier — re-implement webhook signature verification by hand. Two official thin clients remove that friction and the signature-verification footguns around the new `match.finalized` event.",
      "**TypeScript / Node** (`@oce-inhouse/sdk`, `packages/sdk-js`). Typed method per `/v1` endpoint (matches, leaderboard, profile, teams, tournaments, coaches, inhouse status, plus `webhooks.list/create/delete`), bearer auth, and automatic retry on `429` honouring `Retry-After` / `retry_after_seconds`. Uses the global `fetch` (Node 18+). Throws `OceInhouseApiError` carrying `status` + the machine-readable `code`.",
      "**Python** (`oce-inhouse-sdk`, `packages/sdk-python`). Same surface, zero runtime dependencies (stdlib `urllib` only), `snake_case` methods, same 429 retry behaviour, raises `OceInhouseApiError`.",
      "**Signed-webhook verifier** in both. `constructWebhookEvent` / `construct_webhook_event` (and boolean `verifyWebhookSignature` / `verify_webhook_signature`) replicate the dispatcher's `HMAC-SHA256` over `<timestamp>.<raw_body>` and enforce the 5-minute replay window — verified against the server signer in each SDK's test suite.",
      "**Docs.** Every endpoint card on `/developers` now renders a copy-paste *Use the SDK* sample (TypeScript + Python), plus a new *Client libraries* install section. `tests/developerSdkSamples.test.js` is a drift guard that fails if a new endpoint lands without an SDK sample or if either SDK is missing a sampled method.",
      "**Scope.** Full edition only — the public API is full-edition-only, so the community edition is untouched. Publishing to npm / PyPI is a release-time step (the packages are publish-ready with `prepublishOnly` build + metadata); no registry credentials are committed."
    ],
    "author": "System"
  },
  {
    "version": "8.18",
    "title": "Regression tests for the mobile action endpoints + deep-link router (Task #461)",
    "published_at": "2026-05-29",
    "notes": [
      "**Why.** The two Task #414 mobile write endpoints (`POST /api/matches/:id/mvp-vote`, `POST /api/bookings/:id/reminder-ack`) and the deep-link router in `mobile/app/_layout.tsx` shipped without automated coverage. A refactor of session auth, the coaching feature gate, or the push-data shape could silently break the mobile happy path and we'd only learn from user reports.",
      "**Endpoint tests.** `tests/mobileActionEndpoints.test.js` exercises the MVP-vote route (sign-in gate, invalid/missing ids, self-vote guard, match-not-found, the 24h voting window, voter/candidate participation, the same-team rule, happy path, and re-vote idempotency) and the booking-reminder-ack route (coaching-disabled 404, sign-in gate, invalid id, not-yours/not-ackable 404, happy path bound to the session account, and COALESCE idempotency) — all via the shared `node --test` server harness, no real DB/Stripe/Discord touched.",
      "**Deep-link helper.** Extracted the pure routing decisions out of `_layout.tsx` into `mobile/lib/deepLink.js` (`parseDeepLink` + `resolvePushRoute`). `tests/mobileDeepLink.test.js` covers the Steam OpenID `?t=token` hand-off, `oceinhouse:///action/<kind>/<id>?slot=…` action links (query-encoding + empty values), token-wins precedence, and the push `data.url` shapes.",
      "**Scope.** Tests + a no-behaviour-change refactor of the mobile deep-link handler. No server, schema, or route changes; full edition mobile companion only."
    ],
    "author": "System"
  },
  {
    "version": "8.17",
    "title": "Mobile: don't lose an action when the network drops mid-tap (Task #460)",
    "published_at": "2026-05-29",
    "notes": [
      "**Why.** Task #414 explicitly left offline queueing out of scope. On flaky transit Wi-Fi, tapping Accept on a ready-check (or any mobile write action) just failed with a red error — the intent was lost and the user had to re-open a push that may already have timed out server-side. That meant missed lobby seats.",
      "**Queue.** New `mobile/lib/offlineQueue.ts` is an AsyncStorage-backed queue that persists the `{ method, path, body }` triple whenever a write fails at the network level (no HTTP response). De-dupes identical pending intents so a double-tap doesn't stack.",
      "**Retry loop.** A foreground drainer wired to `NetInfo` + `AppState` (started once in `mobile/app/_layout.tsx`) replays the queue the moment connectivity returns or the app is foregrounded — single-flight and in tap order, so \"accept then decline\" replays correctly.",
      "**TTLs.** Per-kind expiries (`ready-check` 2 min, `mvp-vote` / `booking-reminder` 1 h, `scrim` 6 h, `roster-transfer` 1 day) drop stale intents so a queued ready-check accept never fires after the accept window closes. Any HTTP response on replay (even a 4xx) counts as delivered and the intent is dropped; only true network failures keep it queued.",
      "**UX.** Each action screen now shows a calm amber \"Queued — will retry when online\" affordance (via `actionErrorState` in `ActionShell.tsx`) instead of the red error. Coach booking / VOD review are deliberately NOT queued — they need an interactive Stripe Checkout hand-off.",
      "**Scope.** Full edition mobile companion only. Adds `@react-native-async-storage/async-storage` + `@react-native-community/netinfo`; no server or schema change — replays hit the existing Task #414 endpoints."
    ],
    "author": "System"
  },
  {
    "version": "8.16",
    "title": "Mobile Inbox — every pending action in one place (Task #459)",
    "published_at": "2026-05-29",
    "notes": [
      "**Why.** Task #414 shipped six deep-linked mobile action screens, but they only opened from a push or a shared link. Dismiss the push and there was no in-app way back to a pending ready-check, scrim/transfer proposal, open MVP vote, or coaching reminder. The new Inbox closes that loop.",
      "**New screen.** `mobile/app/inbox.tsx` lists every item currently awaiting the signed-in account — pending ready-checks, scrim invites, roster-transfer approvals, open MVP-vote windows, and un-ack'd coaching booking reminders — each row deep-linking to the existing `/action/<kind>/<id>` screen. Re-fetches on focus so resolved items drop off when you return.",
      "**One fetch.** New `GET /api/me/pending-actions` aggregates all five kinds via `db.getPendingActionsForAccount` — five independent, individually-guarded queries fanned out in parallel so a partial outage degrades one kind to empty rather than blanking the inbox. Anonymous callers get an empty list.",
      "**Badge.** The home screen now shows an Inbox card with a live count badge matching the list length, refreshed on focus alongside the rest of the home data.",
      "**Scope.** Full edition mobile companion + server/DB layer; no schema change. Read-only aggregation — the actual mutations still run through the Task #414 action endpoints."
    ],
    "author": "System"
  },
  {
    "version": "8.15",
    "title": "Notification defaults trimmed to opt-in (Task #455)",
    "published_at": "2026-05-29",
    "notes": [
      "**Why.** Most notification categories defaulted to ON, so new and existing players got a stream of DMs they never asked for. Defaults are now reserved for the genuinely transactional messages and two celebratory milestones — everything else is opt-in.",
      "**Default-ON now.** MVP / teammate-attitude vote prompts, all coaching messages (booking confirmed, session reminder, review request, VOD delivered, group-session reminder), and the two milestones — anniversary shout-out and Season Wrapped recap (Discord only). Pro billing/dunning DMs stay on too, deliberately: silently muting \"your payment failed\" would let memberships lapse without warning.",
      "**Default-OFF now (opt-in).** Post-match reports, hot-streak shoutouts, game-schedule reminders, weekly recap/summary, inhouse pick warnings, lobby invite / match-imminent push, tournament check-in, league/scrim accepted, season rollover, coach-of-the-month, quest completion, and match-prediction grading.",
      "**No surprise opt-outs.** Anyone who has explicitly set a preference keeps it — only players with no saved row inherit the new defaults via the catalog fallback in `isEventEnabled` / `eventDefaultEnabled`.",
      "**Settings UX.** `/me/notifications` now shows a \"(default on)\" / \"(default off)\" hint next to each toggle and a dismissable two-week banner explaining the change. A snapshot test (`tests/notificationDefaults.test.js`) locks the default matrix so future drift is caught in CI.",
      "**Scope.** Full edition only. Catalog defaults flipped in both the v1 category list and the v2 per-channel event catalog in `src/db/index.js`; no schema change and no migration."
    ],
    "author": "System"
  },
  {
    "version": "8.14",
    "title": "Swiss engine regression tests + first-place payout remainder (Task #454)",
    "published_at": "2026-05-29",
    "notes": [
      "**Why.** The Swiss pairing engine (`src/tournaments/swissEngine.js`) drives live tournament pairings but had thin automated coverage — a regression in no-repeat pairing, bye selection, tiebreak math, or payout rounding could silently corrupt a real event.",
      "**Tests.** `tests/swissEngine.test.js` now covers `pairRound1` (even + odd fields), `pairNextRound` (no rematches across rounds, single bye per player, score-group integrity + single float-down), `computeStandings` (a constructed field where Buchholz and Sonneborn–Berger rank tied players in *opposite* order), `computePayouts` (remainder placement + guards), and the `recommendedSwissRounds` boundary table — all via the existing `node --test` harness.",
      "**Payout fix.** `computePayouts` now routes the cents-rounding remainder to **first place** (was previously absorbed by the lowest paid place), so the top finisher gets the leftover and the pool still reconciles exactly to the cent.",
      "**Scope.** Pure-function tests + one engine behaviour change; no schema or route changes. Output shape and signature unchanged, so the DB caller in `src/db/index.js` is unaffected."
    ],
    "author": "System"
  },
  {
    "version": "8.13",
    "title": "Automatic tournament prize payouts via Stripe Connect (Task #453)",
    "published_at": "2026-05-29",
    "notes": [
      "**Why.** Tournament payouts (Task #412) only snapshotted who-won-what into `tournament_payouts` — actually sending the prize money was a manual chore. Winners now get paid automatically via Stripe Connect Express the moment they have a connected account, with no manual transfer step.",
      "**Per-row transfer tracking.** `tournament_payouts` gains `stripe_transfer_id`, `transfer_status` (`pending` / `paid` / `failed`), `transfer_error`, `transferred_at`, and `currency` (default `aud`). Re-running `finalizeTournamentPayouts` now preserves any in-flight / settled transfer for an unchanged winner instead of wiping it, so the snapshot stays idempotent without ever double-paying.",
      "**Connect accounts for any winner.** A new `payout_accounts` table mirrors the coaching onboarding flow so non-coach winners can connect a Stripe Express account; existing coaches reuse their Connect account automatically (`resolvePayoutDestination` prefers a player's own account, then an active coach account, skipping synthetic test ids).",
      "**Settlement paths.** Transfers fire (1) immediately when payouts are finalized, (2) on a per-tournament \"Pay winners\" admin button, and (3) from a background sweep (2 min after boot, then every 10 min) that pays winners who connect *after* the snapshot — also kicked by the `account.updated` webhook once KYC completes. Each transfer carries a `transfer_group` of `tournament_<id>` and an idempotency key to prevent accidental double-sends.",
      "**Admin + winner UX.** The tournament payouts panel shows a status badge per row, a superuser \"Pay winners\" / \"Retry failed\" control, and a per-row Retry on failures. Winners with a pending, unconnected prize see a \"Connect payout account\" CTA that launches Stripe onboarding. The AdminPanel tournament section surfaces a cross-tournament \"Failed prize payouts\" table with one-click retry (hidden when nothing failed).",
      "**Scope.** Full edition only — community edition stays paywall-free and untouched. Best-effort throughout: when Stripe isn't configured, rows simply stay `pending`."
    ],
    "author": "System"
  },
  {
    "version": "8.12",
    "title": "Tournament check-in window notifications (Task #452)",
    "published_at": "2026-05-29",
    "notes": [
      "**Why.** Task #412 added a structured check-in window with auto-DQ for no-shows, but players only saw it if they happened to load the tournament page in time. Registered players now get a Discord DM + web/Expo push the moment check-in opens, plus a final reminder before it closes — dramatically cutting accidental DQs.",
      "**Open + reminder signals.** A new per-minute cron (`__t452_checkin_notify`, alongside the existing `__t412_checkin_sweep`) calls `db.claimTournamentCheckinNotifications`: it fires a **check-in open** notice to every registered participant once `(starts_at - checkin_offset_min)` is reached, and a **5-minute-before-close** reminder to anyone who still hasn't checked in. Each tournament's two signals are one-shot — claimed atomically via `UPDATE ... WHERE <ts> IS NULL RETURNING` against new `tournaments.checkin_open_notified_at` / `checkin_reminder_notified_at` columns, so the sweep never double-DMs and survives restarts.",
      "**Per-user opt-out.** Both notifications go through the `notify()` send-site gated by a new `tournament_checkin` event in the notification preference centre (defaults: Discord on, push on), so players can mute them from their notification settings.",
      "**Scope.** Full edition only — server + DB layer; no community-edition wiring. Best-effort fan-out: per-recipient failures are logged but never abort the tick."
    ],
    "author": "System"
  },
  {
    "version": "8.11",
    "title": "Daily Dota mini-games suite (Task #451)",
    "published_at": "2026-05-29",
    "notes": [
      "**Why.** A new `/games` hub of bite-sized daily Dota puzzles to pull players back every day. Each game has a **Daily** mode (the same puzzle for everyone, AEST-seeded, resets at Sydney midnight, with a shareable Wordle-style result string) plus an endless **Endless** mode, and a per-game leaderboard (today's solvers + all-time average guesses).",
      "**Games shipped.** *Heroguessr* (guess the hero from progressive hint reveals — primary attribute, attack type, roles, legs, ability icons, first letter), *Item-zoom* (identify an item from an 800%-zoomed icon that zooms out with each wrong guess), *Statline* (name the hero from a real inhouse scoreboard line pulled from `player_stats`), and *Talent guesser* (name the hero from its talent tree). *Voiceline daily* ships as a **\"coming soon\"** placeholder — we don't yet host the audio clips.",
      "**Determinism + secrecy.** New `src/games/` module: `seed.js` (Australia/Sydney date, FNV-1a + mulberry32 PRNG seeded from `game:YYYY-MM-DD`, puzzle numbering, and an HMAC token signer keyed off `SESSION_SECRET`), `heroData.js` / `itemData.js` (metadata sourced from the `dotaconstants` package — talents, abilities, attributes, a ~125-item shortlist), and `puzzles.js` (per-game payload builders, guess-checking, share strings). Answers never leave the server until the player finishes; CDN icon URLs are proxied through `/api/games/image?t=<HMAC token>` so the answer slug never appears in a client-readable URL.",
      "**Backend.** New `game_daily_puzzles` (deterministic puzzle cache, statline snapshots the DB once per day) and `game_results` (one row per player per daily puzzle, unique — powers streaks + leaderboards) tables with helpers in `src/db/index.js`: `getDailyPuzzleRow` / `upsertDailyPuzzle` / `recordGameResult` / `getGameDailyResult` / `getGameStreak` / `getGameLeaderboard` / `getStatlineCandidates` / `getGameStats`. Routes live in `src/games/routes.js` (mounted into `createApiRouter`): hub, daily, endless, guess, leaderboard, image proxy. An hourly `node-cron` job (Australia/Sydney) pre-generates today's + tomorrow's puzzles, and a one-shot pregeneration runs at startup.",
      "**Frontend.** New `/games` hub (`Games.jsx`) listing each game with streak + record, a shared play surface (`GamePlay.jsx`) with daily/endless toggle, keyboard-navigable guess autocomplete, per-game clue renderers, share-result copy, and an inline leaderboard, plus a `GamesProfileWidget` on your own profile (self-hides until you've played). API helpers added to `web/src/api.js`; `Daily Mini-Games` link added to the Tools nav.",
      "**XP hookup.** Best-effort no-op against the quests system (Task #440) — wired to call `db.awardQuestXp` when present, silently skipped while that task isn't merged.",
      "**A11y.** Every clickable is a real `<button>`; mode toggle uses `aria-pressed`; the guess box is a labelled `role=\"combobox\"` with arrow-key navigation; icon emojis are `aria-hidden`; no `<div onClick>`, no hover-only reveals.",
      "**Scope.** Full edition only — no `community-edition/` wiring. No cash/coin rewards, no player-authored puzzles, no multiplayer."
    ],
    "author": "System"
  },
  {
    "version": "8.10",
    "title": "AI agent owner alerts now report hit counts + timestamps",
    "published_at": "2026-05-29",
    "notes": [
      "**Why.** With `BLOCK_AI_AGENTS=1` now live on prod, the owner DM only said \"first-seen\" — it never told you how many times an agent visited or when. This upgrades the alert into a rolling 24h digest that reports a hit count and a timestamp range.",
      "**What changed (`src/security/agentClassifier.js`).** `_maybeAlertOwner` now accumulates every blockable hit per agent family in a `_pending` map (`count`, `firstTs`, `lastTs`, sample path/UA). The first sighting still fires an **immediate** DM (count 1); after that the family is suppressed for 24h while hits keep counting, then the next hit past the window flushes a digest: `N hits between <first> and <last> (Sydney time)`. Timestamps render via `toLocaleString('en-AU', { timeZone: 'Australia/Sydney' })`.",
      "**Notifications still fire when blocking is on.** The DM is dispatched *before* the 403 block return, so turning on `BLOCK_AI_AGENTS=1` never silences alerts — blocked agents are still counted and reported.",
      "**Unchanged.** Only `ai-crawler` / `app-builder` families are DM'd (`unknown-bot` stays observability-only via the admin card). The `/admin/agent-traffic-report` card already shows full per-family counts, unique IPs/paths, and recent-request timestamps.",
      "**Scope.** Applied identically to both editions (full + community)."
    ],
    "author": "System"
  },
  {
    "version": "8.09",
    "title": "Inhouse coin betting — full markets (Task #450)",
    "published_at": "2026-05-29",
    "notes": [
      "**Why.** Builds on last patch's no-stakes prediction game with a full coin-wagering layer over live inhouse matches. Players bet existing in-app coins (v6.79 currency, **no real money**) across five pari-mutuel markets per match — Winner, First Blood team, MVP, Duration bucket, Total kills bucket — keyed by `match_id` and mirroring the prediction window lifecycle (open on draft, lock on launch/first-blood, settle in `recordMatch`).",
      "**Pari-mutuel engine (`src/betting/markets.js`).** New pure, DB-free, unit-testable module: `MARKET_TYPES` registry (each with an outcomes generator, `lockTrigger`, and `settle(ctx)` grader), duration + total-kills bucket definitions, anti-abuse constants (min stake 10, per-market cap 500, per-match cap 1000, daily-loss cap 2000), and `computePayoutPlan` / `payoutForBet` — classic pari-mutuel: losing pool redistributed across winning stakes pro-rata, with full refund when a market has no winning bets. `TRIGGERS_ON_MATCH_START` / `TRIGGERS_ON_FIRST_BLOOD` drive the lock fan-out.",
      "**DB layer (`src/db/index.js`).** New `bet_markets` / `bet_market_outcomes` / `bets` tables. Helpers: `openMarketsForMatch` (idempotent, generates the five standard markets from the captured lobby roster), `lockMarketsForMatch(matchId, triggers)`, `getMarketsForMatch` (pools + bettor counts + viewer's own bets), `placeBet` (atomic, per-account advisory lock serialised against coin grants/purchases — enforces kill-switch, market-open, valid outcome, self-bet block, min/max stake, per-match cap, daily-loss cap, one-bet-per-market, sufficient balance), `settleMarketsForMatch` (auto-graded standard markets), `settleMarketToOutcome` (manual settle for custom markets), `voidMarket` (refunds every stake), `createCustomMarket`, `getBettingStats`, and `getBettingPaused` / `setBettingPaused` kill-switch. Settlement hooked into `recordMatch()` post-commit next to the existing coin-wager resolver.",
      "**Coin-ledger fix.** `bet_payout` / `bet_refund` credit rows write `ref_match_id = NULL` to avoid colliding with the partial unique index `uq_coin_tx_match_grant` when one account wins multiple markets on the same match; `bet_stake` debits keep `ref_match_id` (delta<0 is exempt). `getBettingStats` reads the `bets` table directly so the NULL ref is harmless.",
      "**Lifecycle wiring (`src/discord/bot.js`).** `matchIdCaptured` → `openMarketsForMatch` with the captured roster; `matchStarted` → `lockMarketsForMatch(matchId, TRIGGERS_ON_MATCH_START)`; the existing first-blood lock closure → `lockMarketsForMatch(matchId, TRIGGERS_ON_FIRST_BLOOD)`. All best-effort try/catch — never blocks the existing prediction-window or voice-move side-effects.",
      "**API surface (`src/web/server.js`).** `GET /api/inhouse/:matchId/markets` (markets + pools + my bets + lock state + balance + limits), `POST /api/inhouse/markets/:marketId/bet` (auth-required, `publicWriteLimiter`, structured error codes → HTTP statuses), `GET /api/me/betting-stats`, `GET /api/player/:id/betting-stats` (public, honours `isAccountHidden`). Superuser-only: `POST /api/admin/betting/pause` (kill-switch), `/api/admin/betting/markets/:marketId/void`, `/api/admin/betting/markets/:marketId/settle`, `/api/admin/inhouse/:matchId/markets/custom`.",
      "**Frontend.** New `web/src/pages/InhouseMarkets.jsx` at `/inhouse/:matchId/markets` — live pari-mutuel pools, projected-payout preview, stake entry via the shared `<Dialog>` primitive, and balance/limit guards. A Markets link on the `/inhouse` lobby page, a public coin-betting widget on `PlayerProfile.jsx` (bets placed / won / win-rate / staked / net), and a `BettingControlsCard` on the AdminPanel overview (kill-switch `role='switch'` toggle, per-match market inspector with void/manual-settle, custom-market creator).",
      "**A11y.** Every clickable is a real `<button>` with `aria-label`; the kill-switch uses `role='switch'`+`aria-checked`; the bet dialog uses the shared `<Dialog>` primitive; status/error regions carry `role='status'`/`role='alert'`. No `<div onClick>`, no hover-only reveals, no icon-only buttons without labels.",
      "**Scope.** Full edition only — no `community-edition/` wiring (the paywall-free edition stays untouched). No real money at any point; this is purely the existing in-app coin currency."
    ],
    "author": "System"
  },
  {
    "version": "8.08",
    "title": "Fix deploy-blocking server boot crash",
    "published_at": "2026-05-29",
    "notes": [
      "**What.** The full edition refused to boot — `createServer()` threw `ReferenceError: requireSuperuser is not defined` at `src/web/server.js` the moment the admin Discord Rich Presence route was registered. Because the boot crash also tripped the test-suite gate, it blocked both editions' deploys.",
      "**Root cause.** The `GET /api/admin/discord-rich-presence` route (added with the Discord Rich Presence work) was registered directly on `app` inside `createServer()`, but the `requireSuperuser` middleware it referenced is declared inside `createApiRouter()` — a separate function scope — so it was never in scope at the registration site.",
      "**Fix.** Added a scope-local `requireSuperuserApp` guard in `createServer()` that mirrors `requireSuperuser` exactly (session-based superuser OR the `x-superuser-key` header; the lower-privilege `UPLOAD_KEY` still never satisfies a superuser check, per the threat model) and pointed the route at it. No behaviour change to the endpoint's auth contract.",
      "**Scope.** Full edition only — community edition was unaffected."
    ],
    "author": "System"
  },
  {
    "version": "8.07",
    "title": "Match Prediction Game (Task #449)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** Friction-free engagement loop: anyone signed in with Steam can predict the winner of an in-progress inhouse match between lobby-lock and first-blood. No coins, no buy-in — just bragging rights via three leaderboards (all-time accuracy, current streak, this-season accuracy) plus an opt-in Discord DM telling you whether your call was right when the match grades.",
      "**Window engine.** New `match_prediction_windows` table (match_id PK + radiant/dire account_id arrays + opened_at/locked_at/graded_at/winner_team). Window opens when the lobby manager fires `matchIdCaptured` (post-draft) with the 10 player account_ids captured up front so the anti-cheat path doesn't depend on `player_stats` having rows yet. Lock is **first-blood**: when `matchStarted` fires we schedule a poll of OpenDota `/matches/<id>` every 30s and lock the window the first tick `first_blood_time` comes back non-zero. Hard 15-minute fallback locks the window if OpenDota never indexes the match. Grades inside `recordMatch()` once the winner is known.",
      "**DB helpers in `src/db/index.js`.** `openMatchPredictionWindow` / `lockMatchPredictionWindow` / `getMatchPredictionWindow` / `listOpenMatchPredictionWindows` / `listRecentGradedPredictionWindows` / `gradeMatchPredictionWindow` (fan-out resolves every `match_predictions` row then returns the affected predictors for the notify loop) / `getPlayerPredictionStreak` (total + correct + accuracy + current/best streak via chronological walk) / `getPredictionAccuracyLeaderboard` (min 25 graded picks) / `getPredictionStreakLeaderboard` (SQL window-function tail-run) / `getPredictionSeasonLeaderboard` (joined to `matches.season_id`) / `getPlayerPredictionHistory`. New partial index `idx_mpw_open` on opened_at where `graded_at IS NULL`, plus `idx_match_predictions_account` for the per-player history page.",
      "**API surface in `src/web/server.js`.** `GET /api/predictions/open` now returns `{ open: [...] }` with per-window radiant/dire pick counts, my-pick echo, and an `in_match` flag (legacy `prediction` shape preserved for old clients). `POST /api/predictions` (auth-required, `publicWriteLimiter`-rate-limited, rejects in-match players + locked/graded windows). `GET /api/predictions/me` returns lifetime stats + last 50 history rows. `GET /api/predictions/leaderboard?type=accuracy|streak|season` powers the three tabs. The legacy `POST /api/match-predictions/:matchId` is now auth+anti-cheat hardened (was previously unauthenticated and accepted client-supplied predictor names).",
      "**Lobby hook in `src/discord/bot.js`.** `matchIdCaptured` now also captures the 10 lobby players' account_ids (steam64 → 32-bit account_id conversion when GC didn't include the 32-bit form) and opens the window. `matchStarted` kicks off `_schedulePredictionLockOnFirstBlood(matchId)` — a single-flight 30s polling loop against OpenDota `/matches/<id>` that locks the window the first time `first_blood_time` comes back non-zero (with a 15-minute hard fallback so it can't loop forever). Timers are unref'd so they never block process exit. Both wrapped in try/catch — never blocks the existing voice-move / channel-notify side-effects.",
      "**Grader hook in `recordMatch()`.** When `db.gradeMatchPredictionWindow()` returns affected predictors, each gets a DM via `notify(accountId, 'prediction_graded', …)` with a green-or-red embed showing their pick vs the winner plus an aggregate '`X/Y predictors were right`' field. New `prediction_graded` event in `NOTIFICATION_EVENTS` (defaults to **off** on both channels — opt-in per spec so we don't DM-spam people who never visit /predictions).",
      "**UI on `web/src/pages/Predictions.jsx`.** Two new tabs (default: **🎯 Match Picks**, plus **🏆 Leaderboards**) sit alongside the existing season Top-5 tabs (now relabelled to keep them clearly distinct). Match Picks polls every 15s, shows live radiant/dire pick split as a coloured bar, big Radiant/Dire `aria-pressed` toggle buttons with `aria-label`, and your last 15 graded predictions underneath. Leaderboards uses `role='radiogroup'` + `role='radio'` + `aria-checked` per house-rule #5.",
      "**Profile widget upgrade on `web/src/pages/PlayerProfile.jsx`.** Existing 🎯 Match Predictions card now also surfaces Current Streak (amber when ≥3) and Best Streak (brass) cards — added conditionally so older payloads without those fields still render the original three. Server-side `/players/:id/predictions` swapped to `getPlayerPredictionStreak()` so the widget gets the streak fields automatically. Card stays public (the route was already open).",
      "**A11y.** Every button is real `<button type='button'>` with `aria-label`; toggles carry `aria-pressed`; leaderboard tab strip is a `role='radiogroup'`; coloured pick-split bar carries an `aria-label` describing the percentages so screen-reader users get the same signal sighted users do. No `<div onClick>`, no hover-only reveals, no icon-only buttons without labels.",
      "**Scope.** Full edition only — no `community-edition/` wiring (the paywall-free edition stays untouched). Out of scope by design: coin / Stripe / cosmetic rewards, non-winner markets (first-blood, total kills, etc.), tournament-match prediction surface."
    ],
    "author": "System"
  },
  {
    "version": "8.06",
    "title": "Birthday & anniversary flair (Task #448)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** People who've been inhousing for years should get the small visible nod when their anniversary lands. Task #448 adds it across all three of the surfaces it should hit: a profile ribbon on the day, a daily 09:00 Sydney combined Discord shout-out, and permanent milestone badges (1y/3y/5y/10y) in the existing achievement strip. Per-user opt-out lives in the standard notification preferences centre. Steam join_date isn't accessible, so the signal is purely 'first recorded OCE inhouse match' — which is what the community actually cares about anyway.",
      "**New DB helpers in `src/db/index.js`.** `getPlayerAnniversary(accountId)` returns `{ first_seen_at, years, isToday, isMilestone }` from `MIN(matches.date)` joined via `player_stats`, with all month/day/year comparisons done at `AT TIME ZONE 'Australia/Sydney'`. `getAnniversariesToday()` returns one row per account whose first-recorded match's Sydney-local month+day matches today, sorted years DESC. `grantAnniversaryAchievement(accountId, years)` idempotently INSERTs `anniv_<years>y` into the existing `achievements` table (ON CONFLICT DO NOTHING).",
      "**Four new milestone entries in `src/data/achievements.js`.** `anniv_1y` / `anniv_3y` / `anniv_5y` / `anniv_10y` live in the Milestones group with `check: () => false` so the per-match grant pipeline never tries to award them — the daily cron is the only source of truth, matching the existing pattern used by `season_*` awards.",
      "**Daily 09:00 Australia/Sydney cron in `src/discord/bot.js`.** New `_sendDailyAnniversaryShoutout()` pulls today's anniversaries, mints any milestone badges (always, regardless of opt-out), filters the channel post by the new `anniversary_shoutout` notification event (fail-open on errors), and posts one combined message to stats channels grouped by years DESC with `<@discordId>` mentions when the player has linked Discord. Heartbeats via `recordCronHeartbeat({ name: 'anniversary_shoutout' })` so /admin/feature-health can spot a silently-broken cron.",
      "**New `anniversary_shoutout` notification event** in `NOTIFICATION_EVENTS` — defaults to `discord: true, push: false`. Renders automatically in `/me/notifications` (v2 prefs centre, server-supplied labels). Opting out suppresses the channel call-out only — the milestone badge is still minted.",
      "**New `GET /player/:id/anniversary` API + `web/src/api.js` helper.** Public read, honours `isAccountHidden`. Returns `{ anniversary: { first_seen_at, years, isToday, isMilestone } | null }`.",
      "**Profile ribbon on `web/src/pages/PlayerProfile.jsx`.** New `AnniversaryRibbon` component renders above the achievement strip only when `isToday` (Sydney) is true. Milestone years get the gold-gradient variant + 'milestone unlocked' subtitle; non-milestone years get the parchment-on-brass variant.",
      "**A11y gate green.** Ribbon is a `<section>` with `aria-label`, decorative emoji marked `aria-hidden`, no clickable surface. No `<div onClick>`, no hover-only reveals.",
      "**Scope.** Full edition only — community edition (`dota.stats.corvidaeinc.com`) is untouched."
    ],
    "author": "System"
  },
  {
    "version": "8.05",
    "title": "Embeddable player stat cards (Task #447)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** People already share screenshots of their OCE Inhouse profile in Discord and on streams — Task #447 makes that a first-class surface so anyone can drop a live, themable card into a Discord channel, blog post, or stream overlay without spinning up a screenshot every time. Two iframe size variants + a matching PNG endpoint, both honouring a per-player privacy toggle.",
      "**New `GET /embed/player/:steamId` HTML route in `src/web/server.js`.** Self-contained HTML (no SPA, no React) so it loads instantly inside an iframe. Two sizes via `?size=tall|wide` — `tall` is 240×320 (default), `wide` is 480×120. Theme via `?theme=light|dark` (defaults to dark). Always returns 200; the body content reflects privacy / hidden / invalid-id state so embedders get a predictable surface. `X-Frame-Options` cleared + `Content-Security-Policy: frame-ancestors *` so the iframe works on any host.",
      "**New `GET /og/player/:steamId.png` PNG route + `src/services/playerStatCard.js`.** Canvas-based PNG generator built on `@napi-rs/canvas`, matching the two size variants exactly. Pulls signature hero via the existing `_resolveOgProfileHero` so the embed picks the same hero the OG card does (pinned → most-played fallback). Privacy / hidden / invalid-id paths redirect to `/oa-logo.png` so the image slot never breaks.",
      "**5-minute in-memory cache.** Two layers: `_embedAssembleCache` in `server.js` keyed by `accountId` caches the DB roll-up (nickname + rating + signature hero + last-10 + customization extras) shared by both the HTML and PNG routes; `services/playerStatCard.js` keeps a buffer cache keyed by `(accountId, variant, theme)` plus a content signature so re-renders only happen when the underlying stats change.",
      "**Per-player privacy toggle.** New `embed_enabled` boolean on `extras` JSONB (default `true`) wired through `validateExtras()` in `src/profileCosmetics.js` + mirrored on `web/src/profileCosmetics.js`. Settings → Profile gets a new checkbox under the share-card section — *Allow public embeds of my stats (iframe & image)*. When false, both routes return a generic 'embed disabled' placeholder (HTML) / redirect to the logo (PNG) instead of leaking any rating data.",
      "**New owner-only Share / Embed panel on `web/src/pages/PlayerProfile.jsx`.** Rendered right after `ShareCardPreviewTile` (own-profile only). Two size segmented controls (`role='radio'` + `aria-checked`), two theme controls, copy-iframe-snippet + copy-image-URL buttons, and a live `<iframe>` preview keyed by `(variant, theme)` so toggling instantly re-renders against the same URL the embedder will use.",
      "**A11y.** Size + theme segmented controls use `role='radiogroup'` + `role='radio'` + `aria-checked` (per house-rule #5). Copy buttons are real `<button type='button'>` with `aria-label`. No `<div onClick>`, no hover-only reveals, no icon-only buttons without labels. The embed HTML itself wraps in a single `<a>` so the whole card is keyboard-reachable.",
      "**Scope.** Full edition only — no `community-edition/` wiring (the paywall-free edition stays untouched). Out of scope by design: team / match / leaderboard cards, animated or video embeds — this ships the player-card primitives first and leaves the other surfaces as a follow-up."
    ],
    "author": "System"
  },
  {
    "version": "8.04",
    "title": "Discord Rich Presence — admin-gated rollout (Task #446)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** Players who already linked Discord get nothing on their Discord profile when they're queueing, in a draft, or mid-match on OCE Inhouse. Task #446 adds an opt-in Rich Presence connection that mirrors site presence — queue / lobby / in-match — onto the user's Discord profile, behind both a per-user opt-in AND a global admin feature flag so the rollout is fully under operator control.",
      "**New `/auth/discord-rpc` OAuth flow in `src/web/server.js`.** Separate from the existing link flow — uses its own session stash key (`discordRpcOAuth`) and requests `identify rpc.activities.write` with `prompt=consent`. The callback refuses unless the requesting Steam account already has Discord linked AND the OAuth identity matches the linked Discord ID (no broadcasting to a different person's client), then upserts the token bundle into the new `discord_rpc_connections` table. Always returns to `/settings/account?tab=rpc` with a `rpc_connect=success|error&reason=…` querystring.",
      "**New `discord_rpc_connections` table + helpers in `src/db/index.js`.** One row per account: discord_id, OAuth tokens, scope, `opted_in` (default true), and pusher bookkeeping fields (`last_state`, `last_published_at`, `last_error`, `publish_count`). Helpers: `upsertDiscordRpcConnection`, `getDiscordRpcConnection`, `setDiscordRpcOptedIn`, `deleteDiscordRpcConnection`, `listOptedInDiscordRpcConnections`, `recordDiscordRpcPublish`, `getDiscordRpcConnectionStats`, `listDiscordRpcConnectionsForAdmin`. Also seeds the new feature flag `discord_rich_presence_enabled` at `state='off'` for the staged rollout.",
      "**New presence pusher worker `src/services/discordRichPresencePusher.js`.** Walks opted-in connections on a 30 s tick AND subscribes to a new `change` event on `presenceService` (debounced 2 s) so 'just entered a lobby' updates land in seconds, not minutes. Dedups via an in-memory `last published state` map so static presences don't spam Discord every tick. When the admin flag is anything other than `on`, the worker still updates `last_state` for the admin table but never calls Discord — keeping the backend 'built and ready' for a flip-on rollout. Per-user failures are caught and persisted to `last_error`; nothing throws out of the tick.",
      "**`presenceService.js` now emits change events.** Added an `EventEmitter` (`events`) that fires on every `setDiscordPresence` / `setSteamPresence`. Same backwards-compatible API — existing call sites just keep working.",
      "**User-facing card on `/settings/account`.** New 'Discord Rich Presence' card with a Connect button (linking to `/auth/discord-rpc`), a `role=switch` opt-in toggle (pause publishing without revoking the OAuth grant), a Disconnect button, and a status line showing the last state the pusher saw and the current flag posture so the user can tell whether they're live yet. Card hides itself behind 'Link Discord first' if the user hasn't linked yet.",
      "**Admin control on `/admin` → Config tab.** New `DiscordRichPresenceCard` with the standard three-state flag toggle (on / preview / off — `aria-pressed` per option), live stats (total connected, opted-in, published in last 24 h, total publishes, worker status + tick interval + DISCORD_CLIENT_ID config warning), and a sortable status table using the shared `SortableTh` component (player, discord id, opted-in, last state, last published, publish count, connected at, last error). New `GET /api/admin/discord-rich-presence` (superuser-gated) backs it.",
      "**A11y gate green.** Opt-in switch uses `role='switch'` + `aria-checked` + `aria-label`. Connect / Disconnect / flag-state buttons are real `<button type='button'>` (or `<a class='btn'>` for the navigational connect link) with `aria-label` / `aria-pressed`. Admin table uses shared `SortableTh`. No `<div onClick>`, no hover-only reveals, no icon-only buttons without labels.",
      "**Scope.** Full edition only — no `community-edition/` wiring (the paywall-free edition stays untouched). Default OFF: the flag ships as `off`, so production behaviour is unchanged until an operator flips it on the AdminPanel. Note: `rpc.activities.write` is an application-whitelisted Discord scope; for non-whitelisted apps the worker records the 403 in `last_error` and keeps running — the connect UI + opt-in + admin table all stay functional regardless."
    ],
    "author": "System"
  },
  {
    "version": "8.03",
    "title": "Live pick advisor (opt-in) for inhouse drafts (Task #445)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** During an inhouse draft the captain's call is canonical, but the player on the team still has to volunteer a hero they're comfortable on into the role they're being slotted into. Task #445 adds a tiny, non-blocking advisor that whispers — only if the player asked for it — *'your team needs a 4 and you're on a 87% heater on Hoodwink, want to try that?'* It's a personal nudge, not a draft tool — captain-pick suggestions and counter-pick scoring are deliberately out of scope.",
      "**New profile toggle.** Added `pick_advisor_optin: false` to `DEFAULT_EXTRAS` in `web/src/profileCosmetics.js` and a matching checkbox in Settings → Profile (`web/src/pages/SettingsProfile.jsx`, next to the streak-badge toggle). Off by default — anyone who doesn't flip the switch never sees the panel and never pays the network cost.",
      "**New `GET /api/inhouse/:id/pick-advisor` in `src/web/server.js`.** Authorization: must be a signed-in member of the inhouse session AND drafted onto a team (team > 0); spectators / un-team'd pool get an empty result, not a 403. Computes the team's *missing positions* by subtracting teammates' single-position preferences from {1..5} (ambiguous multi-position prefs don't claim a role). Pulls the requester's last 25 matches grouped by hero with the community's primary position for each hero stamped on via a `pos_mode` window CTE, filters out heroes currently picked/banned in the live GC lobby (best-effort — degrades to no-exclusion when lobbyManager isn't holding lobby data), then returns up to 3 candidates sorted by WR then sample size. `Cache-Control: no-store`.",
      "**New `PickAdvisorPanel` in `web/src/pages/Inhouse.jsx`.** Renders inside the lobby card, above the Connect button, only when (a) the viewer has opted in, (b) is on a team, and (c) status is `drafting` or `in_progress`. Polls the endpoint every 8s, shows the spec's sample line — *'Your team needs a 4. You're 87% WR on Hoodwink (last 10).'* — plus an alternatives chip row when there's more than one suggestion. Dismiss state is keyed per `session.id` in component-local state, so closing the panel hides it for THIS draft only; joining the next inhouse brings it back without touching profile settings.",
      "**Viewer opt-in fetch.** A small one-shot `useEffect` reads `/api/me/profile` and stores `customization.extras.pick_advisor_optin` so the panel is only mounted (and only polls) when the toggle is on. No traffic for the default-off case.",
      "**A11y.** The dismiss control is a `<button type=\"button\">` with `aria-label=\"Dismiss pick suggestions for this draft\"`. The panel wraps in `<aside aria-label=\"Hero pick suggestions for this draft\">`. No `<div onClick>`, no hover-only reveals, no icon-only buttons.",
      "**Scope.** Full edition only — no `community-edition/` wiring (the paywall-free edition stays untouched). Out of scope by design: captain-side draft suggestions, counter-pick scoring against the enemy lineup, and any kind of forced or modal recommendation flow. Soft signal, easy to ignore, easy to switch off."
    ],
    "author": "System"
  },
  {
    "version": "8.02",
    "title": "Pre-match mood & form widget (Task #444)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** Before someone queues an inhouse the most useful thing the site can tell them isn't more numbers — it's a one-glance read on their own recent form so they know whether tonight feels like a hot streak to ride or a cold patch to break. Task #444 adds a soft, non-judgemental 'pre-match vibe' card that summarises the last 7/14 days, surfaces a weekday-WR pattern if there is one, and calls out any hero on a heater over the most recent 10 games.",
      "**New `db.getPlayerFormSummary(accountId)` in `src/db/index.js`.** Single query pulls the last 60 non-legacy matches (`date`, `hero_id`, derived `won` flag), then computes: last-7d and last-14d W-L tallies, per-weekday WR keyed off `Intl.DateTimeFormat` in `Australia/Sydney` (DST-correct), the weekday with the largest lift versus the player's overall WR over the same sample (≥3 games required before bragging), and the 'hot hero' — any hero in the last 10 matches with ≥3 games and ≥60% WR, highest WR wins ties. Returns `{ sample: 0, … }` for new accounts so the widget self-suppresses with no empty-state clutter.",
      "**New `GET /api/player/:id/form-summary` in `src/web/server.js`.** Open endpoint — same posture as the other public profile reads (e.g. win-rate-history). The only thing gating render is the viewer's `mood_widget` notification pref, which is honoured client-side.",
      "**New `web/src/components/MoodFormWidget.jsx`.** Self-fetching display-only card. Renders an italic templated vibe line ('Hot week. Ride it.', 'Mondays are your day — that's today.', 'Your Pudge is on fire.', or 'Steady. Anything could happen tonight.' — no LLM, no MMR forecast) above a row of chips: last 7d W-L, last 14d W-L (only when distinct from 7d), best-weekday WR with delta, and the hot hero. Colour cues are gentle: brass for the section header, green for ≥60% WR / strong week / hot hero, brass-muted for in-between, muted text for cold signals — never red. No buttons, no interactive elements; the whole card is one `<section aria-label>`.",
      "**Wiring.** Rendered on `web/src/pages/PlayerProfile.jsx` (own profile only, gated by `isOwnProfile`, immediately below the Weekly Rivals card) and on `web/src/pages/Inhouse.jsx` (above the position picker when the viewer is signed in, so the vibe line lands the moment they're about to queue). The widget itself fetches `/api/me/notifications` once and hides if `mood_widget` is toggled off; the new category is registered in `NOTIFICATION_CATEGORIES` so the existing Settings → Notifications page picks up the toggle automatically — no settings UI change required.",
      "**A11y.** Display-only card — no `<div onClick>`, no hover-only reveals, no toggles, no icon-only buttons. Wrapped in a `<section aria-label=\"Pre-match mood and form\">` so screen readers announce the surface.",
      "**Scope.** Full edition only — no `community-edition/` wiring. By design out of scope: any LLM-generated text (the vibe line is a small template over the same data the chips show), MMR forecasting, and any kind of 'should you queue right now?' prediction. Soft signal, nothing more."
    ],
    "author": "System"
  },
  {
    "version": "8.00",
    "title": "Head-to-Head page — drill-down, OG card, compare-vs pickers (Task #442)",
    "published_at": "2026-05-28",
    "major": true,
    "notes": [
      "**Why.** The existing `/head-to-head` tab inside PlayerTools answered 'what's the score?' for two players, but every other question — *which heroes did they pick into each other, who won lane when they faced off, how does the score swing by side, what's the longest streak either way* — required clicking into individual matches. Task #442 promotes head-to-head from an in-app tab to its own first-class, deep-linkable, shareable page so two players can settle a beef in a single URL.",
      "**New `/h2h/:playerA/:playerB` route** with a `/me/h2h/:other` shortcut that resolves the viewer's account id from session and redirects to the canonical URL. Same Pro paywall (`requirePro('head_to_head')`) as the in-app tab so the gating story stays consistent across both surfaces. Lazy-loaded React page (`web/src/pages/H2H.jsx`) with a header scoreboard (record, win-rate, all-time PERF Δ, longest streak each), four tabs (Hero matchups grid, Lane matchups, Side breakdown, Timeline with running score), and a Share button that copies the canonical link.",
      "**New `db.getH2HDetailed(a, b, seasonId)` in `src/db/index.js`.** Single query joins `player_stats a` to `player_stats b` on opposing teams for the same match, returns a header bundle (totals + a/b longest streak walked chronologically + average PERF + perf delta), the aggregated hero-matchup grid, derived lane-matchup rows (lane bucketing matches `computeMatchLaneOutcomes` so 'A safe vs B off' / 'A mid vs B mid' / 'A off vs B safe' detection is consistent with the rest of the site; per-row W/~/L outcome uses the existing >2000 / >500 / -500 / -2000 net-worth thresholds when both players have `laning_nw` populated), side breakdown (A's record on Radiant vs Dire), and the full chronological timeline.",
      "**Shareable OG card.** New `src/services/h2hOgCard.js` patterned on `profileOgCard.js` — 1200×630 PNG via `@napi-rs/canvas`, in-memory cache (10-min TTL, 200-entry cap), each player's top hero as a faded backdrop slab on their side of a brass divider, scoreline rendered in 220px numerals with the leader coloured green / loser red / ties parchment. Endpoint at `GET /og/h2h/:a/:b.png`. Companion crawler unfurl at `GET /h2h/:a/:b` sniffs the social-bot UA list (Discord/Twitter/Slack/Facebook/LinkedIn/Telegram/WhatsApp/Reddit/Skype/Embedly/Iframely/Googlebot) and responds with OG meta-tag HTML; real browsers fall through to the SPA so React still mounts normally.",
      "**'Compare vs…' pickers.** New `H2HComparePicker` component on every Player Profile (own + others) — a single `<select>` of all players that navigates to `/h2h/<thisProfile>/<picked>` on change, plus a one-click 'vs me →' button when the signed-in viewer is looking at someone else's profile so they don't have to scroll the dropdown. Self-fetches the roster once via `getAllPlayers()`. RivalCard (Task #441) gains a 'Detailed H2H →' link pointing at `/me/h2h/:rivalAccountId` so the weekly rivalry pipes straight into the new drill-down page.",
      "**A11y.** All buttons are `<button type=\"button\">` with `aria-label` on icon-only actions (Swap, Share, vs me, change-left/right selects); the tab strip uses `role=\"tablist\"` + `role=\"tab\"` + `aria-selected` + `aria-controls`; the panel below is `role=\"tabpanel\"` + `aria-labelledby`; the picker `<select>` has a real `<label>` wrapping it; no `<div onClick>` and no hover-only reveals.",
      "**Scope.** Full edition only (mirrors the rest of the Pro-paywalled surface — community edition is paywall-free by policy). Out of scope by design: 3-way comparisons, time-window slicing (use the existing season selector for the season-scoped slice). The lane-outcome column is intentionally blank ('—') when either player's `laning_nw` is missing rather than guessing from the match result, so the column either tells you the truth or stays silent."
    ],
    "author": "System"
  },
  {
    "version": "7.99",
    "title": "Weekly Rivals auto-pairing (Task #441)",
    "published_at": "2026-05-28",
    "major": true,
    "notes": [
      "**Why.** Quests and community challenges (v7.98) give every player a fresh thing to chase each day/week — but the chase is solo. Weekly Rivals adds a single, social, person-shaped reason to queue: every Monday, every active player gets auto-paired with a similar-tier opponent and a head-to-head score that runs for the week. Beating your rival doesn't pay XP/coins/cosmetics by design — it pays bragging rights, a recap DM on Sunday, and a fresh pair on Monday.",
      "**New `src/rivals/pairing.js`.** Pure pairing engine — no DB, no IO. Greedy MMR-sorted match-up with a ±150 MMR window. Tie-break score blends hero-pool Jaccard overlap (`HERO_OVERLAP_WEIGHT=1.0`), side-balance complement (`SIDE_BALANCE_WEIGHT=0.5` — punishes a pair where both players always pick the same side), and an MMR-distance penalty (`MMR_PENALTY=0.25 * Δ/window`). Canonical `a<b` ordering on every pair so the admin inspector + recap maths stay stable across runs. `currentWeekStart()` helper buckets to the LOCAL Monday in `Australia/Sydney` using `Intl.DateTimeFormat` (DST-correct: handles AEDT/AEST flips without drifting onto the wrong week).",
      "**All-time H2H + 'both in queue' indicators.** `db.getAllTimeRivalH2H(a, b)` counts every recorded match where both rivals appeared on opposite teams across all history; `db.areBothInInhouseQueue(a, b)` joins `inhouse_queue` for the live 'both queued right now — go!' signal. Both are exposed on `GET /api/me/rival`, rendered on the RivalCard (a quiet all-time line under the weekly score, and a green status-role 'both in queue' badge when applicable), and folded into the post-match DM (explicit WIN/LOSS title + color + week + all-time fields) and the Sunday recap DM (week and all-time side-by-side).",
      "**DB additions in `src/db/index.js`.** Three new tables: `weekly_rivals` (one row per `(week_start, account_id_a)`, mirror-written for both sides so 'my rival this week' is a single PK hit, carries `wins_a` / `wins_b` / `mmr_a` / `mmr_b` / `score`), `rival_exemptions` (admin-managed opt-out set), and `weekly_rival_match_log` (idempotency log keyed `(week_start, account_id_a, match_id)` so re-records can never double-credit a H2H win). New helpers: `generateWeeklyRivals({force})`, `getCurrentRival`, `listAllWeeklyRivalPairings`, `recordRivalMatchResult`, `setRivalExemption` / `listRivalExemptions`, `repairRivalForAccount`, `getWeeklyRivalRecap`.",
      "**Post-match hook in `src/web/server.js`.** After every `recordMatch()` commit, the rivals engine walks the 10 players, bumps `wins_a` (and the mirror row's `wins_b`) for any rival pair on opposing teams, and for each touched account fires `notify(aid, 'rival_match_played', …)` so the per-user opt-out catalogue still gates the Discord embed + web push. Same-team matches are ignored — only true H2H counts. Wraps the whole thing in a try/catch so a rivals failure can never break the upload pipeline.",
      "**Two new crons in `src/discord/bot.js`.** Monday 00:05 Australia/Sydney → `generateWeeklyRivals(null, {force:false})` rebuilds the week's pairings; idempotent on `(week_start)` so a missed cron + manual repair is safe. Sunday 20:00 Australia/Sydney → end-of-week recap: walks every pair, DMs each side via `notify(aid, 'rival_weekly_recap', …)` when there was at least one H2H match that week (no spammy '0–0' recaps). Both crons emit `recordCronHeartbeat` rows so `/admin/feature-health` sees them.",
      "**Frontend.** New self-fetching `web/src/components/RivalCard.jsx` renders on the Home page alongside the QuestTracker / CommunityChallengeTile row, and on the viewer's own `/player/:id` profile (gated by `isOwnProfile`). Renders nothing when the viewer is signed out, exempt, or unpaired — no empty-state clutter for new accounts. Live H2H scoreline coloured by who's leading, links to the rival's profile, and shows the week-of date.",
      "**API.** Public: `GET /api/me/rival` (session-scoped, returns the caller's current rival or `null`). Superuser: `GET /api/admin/rivals` (current week's pairings + exemption list), `POST /api/admin/rivals/regenerate` (force wipe + re-pair), `POST /api/admin/rivals/repair` (drop a single account's pair and re-pair just them against the unpaired pool — used when someone's rival goes inactive mid-week), `POST /api/admin/rivals/exempt` (toggle exemption set, takes effect on the next Monday cron).",
      "**Admin.** New 'Engagement → ⚔️ Weekly Rivals' tab in AdminPanel + matching SEARCH_INDEX entry. `WeeklyRivalsPanel` lists this week's pairings with MMR Δ, live H2H, and the tie-break score; per-row 'Re-pair A' + 'Exempt A' buttons; a 'Force re-pair week' button at the top (confirmation-gated because it nukes mid-week H2H scores); and an exemption add/remove form below the pairings table.",
      "**A11y.** All buttons are `<button type=\"button\">`, all icon-only / per-row actions carry `aria-label`, the error banner uses `role=\"alert\"`, the exemption input has a visually-hidden `<label htmlFor>`, the RivalCard is `aria-labelledby` its title heading, no `<div onClick>`, no hover-only reveals.",
      "**Scope.** Full edition only — no `community-edition/` wiring. By design: no XP / coin / cosmetic / cash rewards on this batch (the rivalry itself is the reward); the post-match DM and the Sunday recap DM ride existing `notify()` plumbing so the per-user opt-out catalogue gates them like every other event."
    ],
    "author": "System"
  },
  {
    "version": "7.98",
    "title": "Daily / weekly quests + community challenges (Task #440)",
    "published_at": "2026-05-28",
    "major": true,
    "notes": [
      "**Why.** Inhouse activity flatlines mid-week between scheduled lobbies, and the only ongoing 'something to chase' was the season pass itself — which moves slowly. Quests give every player a fresh, individual reason to queue any time (3 daily + 2 weekly, auto-rolled), and community challenges layer a shared, time-boxed leaderboard on top so a single eye-catching event (e.g. 'most kills as Pos 1 this week') can rally the whole community for 7 days.",
      "**New `src/data/questCatalogue.js`.** 8 daily + 7 weekly quest definitions, each a pure predicate over a `player_stats` row (`win`, `kills>=X`, `kda>=X`, position-specific support quests, parser-derived wards/healing/dewarding/last-hits/ranged hero stuns, etc.). Daily/weekly assignments use a deterministic shuffle keyed on `(accountId, periodStart)` so a player gets a stable mix per day/week and quests don't reshuffle on refresh.",
      "**DB additions in `src/db/index.js`.** Five new tables: `player_quests` (assigned quests + progress + completion ts + idempotent XP grant key), `player_quest_hero_log` (per-match contribution audit, drives 'no double counting' on re-records), `community_challenges` (admin-authored, JSON scoring DSL), `community_challenge_scores` (rolled-up per-account leaderboard), `community_challenge_match_log` (idempotent per-match contribution log). New helpers: `getOrAssignActiveQuests`, `applyMatchToPlayerQuests`, `bumpMvpQuestProgress`, `list/get/create/update/deleteCommunityChallenge`, `getChallengeLeaderboard`, `getChallengeRankForAccount`, `applyMatchToCommunityChallenges`.",
      "**Post-match hook in `recordMatch()`.** Every recorded match walks the 10 players, applies each one's active quests + every active community challenge, and on any quest transition from in-progress to completed: fires `awardSeasonPassXp` (sourced as `quest:<id>:<periodStart>` so re-records can never double-grant), and fires `notify({ event: 'quest_completed' })` so the per-user opt-out catalogue gates both the Discord DM (rich embed with XP) and web push.",
      "**Notification catalogue.** New `quest_completed` event registered in `NOTIFICATION_EVENTS` with defaults `{ discord: true, push: true }`. Surfaces in the existing `/me/notifications` settings page like every other event.",
      "**Scoring DSL.** Community challenges score from a small JSON DSL: `{ metric, agg, filter? }`. Metric ∈ `kills | deaths | assists | gpm | xpm | last_hits | hero_damage | tower_damage | hero_healing | wins | matches | perf | kda`. Agg ∈ `sum | max | count`. Optional filter narrows to `team` (radiant/dire), `position` (any subset of 1–5), or `won` (true/false). Examples: `{metric:'kills', agg:'sum', filter:{position:[1]}}` = 'most kills as Pos 1 this week', `{metric:'wins', agg:'count', filter:{position:[4,5]}}` = 'most wins as a support'.",
      "**Frontend.** New `QuestTracker` widget on `/` (signed-in users) and on your own `/player/:id` profile — grouped Daily / Weekly with progress bars, in-page completion toast with XP delta when a quest flips to done between renders, soft 60s poll. New `CommunityChallengeTile` on the Home page showing the current challenge's top 3 + the viewer's rank-of-N if they're scored. New `/challenges/:id` route with the full leaderboard and a highlighted 'you' row.",
      "**Admin.** New 'Engagement → 🎯 Community Challenges' tab in AdminPanel. Lists every challenge with active pill + window dates, plus an inline editor for title/description/prize/window/active + JSON scoring DSL with validation. Routes: `GET/POST/PATCH/DELETE /api/admin/community-challenges`, all superuser-gated via `_isSu`. Public reads: `GET /api/me/quests`, `GET /api/community-challenges/active` (top 3 + viewer rank), `GET /api/community-challenges/:id` (full leaderboard).",
      "**A11y.** All buttons are `<button type=\"button\">` with `aria-label` on icon-only / per-row destructive actions, completion toast uses `role=\"status\"` + `aria-live=\"polite\"`, the Home challenge tile is `aria-labelledby` its title heading, no `<div onClick>` and no hover-only reveals.",
      "**Scope.** Full edition only — none of this is wired into `community-edition/`. Out of scope by design: no cosmetic, coin, or Stripe rewards on this batch — XP only. Those can layer on later without touching this surface."
    ],
    "author": "System"
  },
  {
    "version": "7.97",
    "title": "Match Insights v2 — admin preview, ward-heatmap PNG, backfill (Task #439)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** We already store a lot of rich per-match signal (laning_nw, killed_by, item_first_times, ward_placements, match_fights, game_timeline, player_abilities), but the only place most of it surfaces is the raw match-detail page. Task #439 wraps ten distinct insights into a single admin-only preview surface so we can iterate on each one in isolation and ship them to the public via per-insight feature flags rather than a single big-bang release.",
      "**New `src/insights/matchInsights.js`.** Pure derivation module — all ten insights are pure functions of `db.getMatch()` + `db.getMatchFights()`, with no DB or HTTP access of their own. Same code path runs the admin endpoint, the backfill persistence layer, and the snapshot tests in `tests/matchInsights.test.js`. The insights: Lane Grading (stomp / win / even / lose / feed buckets from laning_nw vs the opposing physical lane), Vision Report (ward placement summary), Item-Timing Cohort (percentile rank vs supplied same-hero distributions), Fight Participation (per-player presence + arrival-time approximation from NW deltas), Net-Worth Swings (top-K biggest team-gold-lead deltas with attribution), Skill Build vs Top-WR Build (longest-common-prefix similarity vs supplied cohort), Death Context (solo / ganked / teamfight classifier from killed_by + tfp), Roshan/Aegis Timeline (events from `game_timeline.events`), Saves & Stuns (parser-derived save events when available), and Parser Field Status (transparency on which of the new columns the Java parser has caught up on).",
      "**New `src/insights/wardHeatmap.js`.** Server-side @napi-rs/canvas renderer that paints obs (cyan) + sentry (purple) heat over `web/public/minimap.jpg` and falls back to a dark gradient when the asset is missing. Exposed as `GET /api/admin/match-insights/:matchId/ward-heatmap.png` (superuser-gated, accepts the existing `?superuser_key=` query param so `<img>` can load it without custom headers).",
      "**New `src/insights/backfill.js`.** Idempotent runner that walks matches with a parsed `game_timeline` and back-fills the three surface-derivable columns (`lane_outcome`, `death_context`, `fight_arrival_time`) on `player_stats`. Wraps every UPDATE in `COALESCE(col, $)` so re-runs never clobber parser-populated values. Module-scope in-flight flag plus state object the admin UI polls every 5 s for live progress.",
      "**DB additions in `src/db/index.js`.** Seven new nullable columns on `player_stats`: `lane_outcome` / `death_context` / `fight_arrival_time` (back-fillable from existing data) and `items_sold_gold` / `end_inventory_gold` / `time_spent_dead` / `save_events` (parser-pending stubs that stay NULL until the Java parser surfaces them; the admin UI shows them as '—' / 'parser field status' so it's obvious what hasn't shipped yet).",
      "**Admin surface.** New lazy route `/admin/match-insights` (and `/admin/match-insights/:matchId`) mounts `web/src/pages/AdminMatchInsights.jsx` — match-id input, per-insight expand / collapse / raw-JSON toggle, flag-state pill (on/off/preview/unknown), inline ward-heatmap PNG inside the Vision Report card, and a Backfill 200 button that streams live progress from `GET /api/admin/match-insights/backfill/status`.",
      "**Per-insight feature flags.** Every insight carries a stable `match_insights_*` key. The admin endpoint resolves the caller's flag state via the existing `db.getResolvedFeatureFlags({ isSuperuser: true })` helper and tags each insight with `flag_state` so we can flip individual insights to `on` / `preview` / `off` from the Admin Panel without redeploying. Public surfacing of any insight is intentionally not part of this patch — that's the next batch.",
      "**Discord owner command.** `!insights-backfill [limit]` (alias `!insightsbackfill`) in `src/discord/bot.js`, owner-gated via `OWNER_DISCORD_ID`. Defaults to 200, capped at 5000. Re-firing while a sweep is in flight returns a 'already running' notice rather than starting a second pass.",
      "**Tests.** `tests/matchInsights.test.js` covers all eleven derivation behaviours (lane bucketing by physical lane, item-percentile maths, empty-cohort fallback, NW-swing detection, fight-participation %, death classifier, roshan/aegis ordering, the canonical insight set + shape, persistable-field extraction, and graceful no-timeline handling). 11/11 green on `node --test tests/matchInsights.test.js`. A11y gate (`npm run check:a11y`) and community paywall gate both clean."
    ],
    "author": "System"
  },
  {
    "version": "7.96",
    "title": "One-click site-lockdown toggle in the admin panel (Task #497)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** The owner-only lockdown gate from v7.90 was env-var-only (`FULL_SITE_LOCKDOWN=1`), which meant SSH + pm2 restart to flip. New AdminPanel card now toggles it live — useful at launch, and when temporarily sharing a preview link to a tester.",
      "**Where.** AdminPanel → Overview tab, top of the page (above the AI agent traffic card). Card shows the current state with a LOCKED/OPEN status pill, an 'ON since …' timestamp, the actor who flipped it, and a single switch.",
      "**Precedence.** The env var still wins when set — if `FULL_SITE_LOCKDOWN=1` is forcing the gate, the toggle goes read-only and the card surfaces the override clearly. Unset the env var to let the DB toggle take effect.",
      "**Backend.** New `site_settings.site_lockdown` row stores `{enabled, since, actor}`; cached in memory and refreshed only on PUT so the middleware stays O(1). New routes: `GET /api/admin/lockdown`, `PUT /api/admin/lockdown` (both superuser-only). Boot-time DB read fails open (gate disabled) so a brief DB hiccup at startup can never lock the public site by surprise.",
      "**A11y.** Toggle uses `role=\"switch\"` + `aria-checked` per the house rule for custom toggle shapes, and the confirm dialog asks before any flip so a stray click can't accidentally lock the site."
    ],
    "author": "System"
  },
  {
    "version": "7.95",
    "title": "Cache marketplace next-available so popular filters don't re-walk every coach (Task #438)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** `db.listActiveCoaches` (the engine behind `GET /api/coaches` and the `/coaches` browse page) computes each coach's `next_available_at` and the `instant_booking` badge in JS by walking up to 14 days × N weekly slots per coach on every request. The work is pure CPU and grows linearly with roster size, but the underlying availability rows only change when a coach edits their schedule — so any given request usually recomputes a value that was already correct seconds ago.",
      "**Small in-process memo in `src/db/index.js`.** New `_cachedNextAvailableForSlots(accountId, slots, now)` wraps the existing `_nextAvailableForSlots` helper. Cache key is the coach's account id; entry stores the computed `nextMs`, a 5-minute `expiresAt`, and a canonical hash of the slot set (day + start + end + timezone, sorted) so a mid-cache schedule change is detected even if the explicit invalidation path were ever bypassed. Stale entries whose cached `nextMs` has fallen into the past are also recomputed automatically, so the weekly roll-forward heals itself without waiting for the TTL.",
      "**Explicit bust on edit.** `db.setCoachAvailability` calls `_bustCoachAvailabilityCache(coachAccountId)` right after the transaction commits, so the very next browse request recomputes against the freshly-written slots — no eventual-consistency window for a coach who just hit Save.",
      "**No API shape change.** `next_available_at` / `instant_booking` are returned exactly as before; only the per-request CPU drops. Snippets, ratings, sorting, and the `availableThisWeek` filter all keep working unchanged."
    ],
    "author": "System"
  },
  {
    "version": "7.94",
    "title": "Coach of the Month congratulations DM (Task #437)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** Task #410 ships the auto-picked 'Coach of the Month' tile on /coaches, but the winner only finds out by visiting the page. A monthly DM closes the loop, gives coaches something to share, and reinforces the marketplace ranking signal.",
      "**New cron in `src/discord/bot.js`.** Scheduled `0 9 1 * *` UTC (1st of each month at 09:00 UTC) alongside the existing pro-winback cron. Resolves `db.getCoachOfTheMonth({ tenantId: 'all' })` to pick the overall winner and DMs them a single congratulatory embed with their monthly completed-sessions count and average review rating. Delivery routes through the existing `notify()` helper so the per-user `coach_of_the_month` toggle gates it.",
      "**New event in the notification catalogue.** `coach_of_the_month` added to `NOTIFICATION_EVENTS` in `src/db/index.js` with defaults `{ discord: true, push: false }` — push stays off because spotlight wins are rare and not time-sensitive. Users can opt out from /me/notifications like every other event.",
      "**Operability.** Heartbeat written via `db.recordCronHeartbeat({ name: 'coach_of_the_month_dm' })` on every tick (ok / skipped / partial / error), so `/admin/ops` overdue check doesn't mistake quiet months for breakage. No-winner months still heartbeat ok with a `'no winner this month'` summary."
    ],
    "author": "System"
  },
  {
    "version": "7.93",
    "title": "Coach marketplace — hero specialty filter (Task #436)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** The Task #410 marketplace refresh added Position, Language, Price, Rating and Available-this-week filters, but the `hero` query param — which the `/api/coaches` endpoint and `db.listActiveCoaches` have always supported via ILIKE against the coach's `taught_heroes` free-text field — wasn't surfaced anywhere in the UI. Students who wanted 'only Invoker coaches' had to know to hand-edit the URL.",
      "**New control in `web/src/pages/Coaches.jsx`.** A Hero specialty input now sits in the filter sidebar between Language and Price range. It's a normal text input backed by a `<datalist>` typeahead populated from `/api/heroes` (mapped through `formatHeroName` so the suggestions read 'Invoker', 'Pudge', 'Nature's Prophet'). Selecting or typing a hero sets the `hero` query param; clearing it (or clicking Reset filters) drops it.",
      "**Server / DB untouched.** No backend changes — `GET /api/coaches` and `db.listActiveCoaches` already wire the param through. The input still accepts arbitrary substrings, so a coach whose `taught_heroes` reads 'Carry heroes, Anti-Mage, Spectre' matches both 'Anti-Mage' and 'Carry'."
    ],
    "author": "System"
  },
  {
    "version": "7.92",
    "title": "Smurf detector — fingerprint signal now actually fires (Task #431)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** The smurf score has a `fingerprint` signal worth 10 / 100 points whose job is to catch the clearest-cut smurf case: two accounts signing in from the same machine. The scorer (`src/smurf/smurfScorer.js`) already knew how to read `sess.ip` / `sess.ua` off the `user_sessions.sess` JSONB and overlap them across accounts — but nothing was writing those keys, so the signal always degraded to `\"no fingerprint data available\"` in production. This patch wires up the missing writer.",
      "**New `src/web/sessionFingerprint.js`.** Tiny middleware that, on every authenticated request, stamps `sess.ip` + `sess.ua` onto the express-session row. Mounted in `src/web/server.js` directly after `app.use(session(...))`, plus an immediate stamp inside the `/api/auth/complete` and `/auth/steam/test-login` handlers so the freshly-regenerated session has prints before the user makes a second request.",
      "**Privacy posture.** Raw IPs and UAs never hit the DB. Both values are salted-SHA-256-hashed and truncated to 16 hex chars before storage — enough entropy for overlap detection, no recoverability of the source string. Salt is `SESSION_FINGERPRINT_SALT` if set, else falls back to `SESSION_SECRET` (same trust boundary as the cookie signature). Rotating either env var effectively forgets all prior fingerprints on next login.",
      "**Retention.** Bounded by the existing 7-day cookie `maxAge` plus connect-pg-simple's session pruner — no separate long-lived audit log of IPs / UAs is created. Only authenticated sessions are stamped; anonymous visitors leave nothing behind. The middleware re-writes at most once every 15 minutes per session, OR immediately when the hashed IP/UA changes (the case the scorer cares about, e.g. same account signing in from a different machine).",
      "**Documented** in `replit.md` → \"Environment variables (security-relevant)\" under `SESSION_FINGERPRINT_SALT`. Community edition (which has no smurf detector) is untouched."
    ],
    "author": "System"
  },
  {
    "version": "7.91",
    "title": "Browser smoke suite + release ritual (Task #426)",
    "published_at": "2026-05-27",
    "notes": [
      "**Why.** Feature-health probes (Task #425) prove the backend can answer questions; they can't prove the React frontend actually rendered something, that the captain-draft page didn't go blank, or that the coaches grid still lays out the way it did yesterday. This task adds a real-browser Playwright smoke pass that loads ~12 top user journeys, screenshots each one, and perceptual-diffs against a stored baseline — the missing layer between 'the API responded' and 'the site is alive'.",
      "**Three triggers.** Weekly cron at `0 3 * * 0` in `Australia/Sydney` registered from `src/index.js` (node-cron handles DST). Admin button on the new `/admin/browser-smoke` page (▶ Run smoke now). And `scripts/trigger-major-smoke.js` invoked from `scripts/post-merge.sh` after every push — POSTs to the internal trigger route only when the most-recently-merged patch note carries `major: true`, so day-to-day batches don't fire the suite but a 6.x → 7.x style release does.",
      "**Programmatic runner (`src/smoke/runner.js`).** Uses the `playwright` package directly (Chromium-only, headless, 1280x800 viewport). For each journey: navigate, wait for an expected selector, screenshot the viewport, perceptual-diff via `pixelmatch` against `tests/smoke/baselines/<key>.png` if present, record pass/fail to `browser_smoke_steps`. A single in-flight flag prevents overlapping runs. Optional deps (`playwright`, `pixelmatch`, `pngjs`, `@playwright/test`) are detected at runtime — when missing, the runner writes a single SKIPPED step with a clear `npx playwright install` hint instead of throwing.",
      "**Journey registry (`src/smoke/journeys.js`).** 15 journeys cover the anonymous public surface (home, leaderboard, match list + sample match detail, sample player profile, hero stats, coaches index + sample coach profile, tournaments index + sample tournament detail, inhouse, patch notes, `/api/health` JSON), and an authenticated tail (signed-in `/profile`, signed-in `/inhouse`) that runs through the new synthetic Steam test-login. Sample IDs are configurable per deploy via `SMOKE_MATCH_ID` / `SMOKE_PLAYER_ID` / `SMOKE_COACH_ID` / `SMOKE_TOURNAMENT_ID`. Adding a new journey is a one-line append.",
      "**Synthetic Steam test-login.** New `POST /auth/steam/test-login` endpoint accepts a shared bearer token (`SMOKE_TEST_LOGIN_TOKEN`) plus an env-allow-listed accountId (`SMOKE_TEST_ACCOUNT_IDS`, comma-separated), sets `req.session.accountId` exactly the way the real OpenID return handler does, and returns 200. Disabled by default — fails closed with 404 when either env var is unset. Even with the token, only explicit account IDs can log in this way. The runner POSTs to this endpoint once per run before iterating journeys; auth journeys are silently skipped (recorded as `skipped` with a clear reason) when the login isn't configured rather than failing the run.",
      "**Owner alerting with failing screenshots attached.** On any step failure the runner DMs `OWNER_DISCORD_ID` via `getDiscordBot()._dmOwner()` with a per-step failure summary (capped at 10 lines + a 'and N more' tail), a link to `/admin/browser-smoke/<runId>`, AND up to 4 failing screenshots attached (diff PNG preferred, falls back to current). `_dmOwner` grew a backwards-compatible `{ files: [paths] }` option that wraps each path in a `discord.js` AttachmentBuilder. Alert is best-effort — Discord-unavailable never breaks the runner.",
      "**Baseline approval UI.** New `/admin/browser-smoke` page lists the latest 30 runs with status pills, trigger source, and per-row pass/fail counts. Clicking through to `/admin/browser-smoke/:id` shows each step's Current / Baseline / Diff PNGs side-by-side, the diff ratio, and an *Approve new baseline* button that copies the current screenshot over `tests/smoke/baselines/<key>.png`. The PNG ships with the next merge so prod picks it up on deploy. A compact summary panel is also embedded directly on `/admin/feature-health` (▶ Run smoke now button + latest 5 runs + link through) so the operator's existing 'is everything OK' page is the one-stop shop.",
      "**New schema.** `browser_smoke_runs (id, trigger, status, started_at, finished_at, total_steps, passed_steps, failed_steps, notes)` + `browser_smoke_steps (id, run_id, step_key, label, status, reason, duration_ms, screenshot_path, baseline_path, diff_path, diff_pixels, diff_ratio)` added inline in `src/db/index.js` init() with `(started_at DESC)` and `(run_id, step_key)` indexes. Helpers: `createBrowserSmokeRun`, `finishBrowserSmokeRun`, `recordBrowserSmokeStep`, `listBrowserSmokeRuns`, `getBrowserSmokeRun`, `getBrowserSmokeSteps`, `getBrowserSmokeStep`, `updateBrowserSmokeStepBaseline`.",
      "**Routes.** `GET /api/admin/smoke/runs` (list), `GET /api/admin/smoke/runs/:id` (detail + steps), `POST /api/admin/smoke/run` (queues an admin-triggered run), `POST /api/admin/smoke/runs/:id/steps/:stepKey/approve-baseline` (promotes screenshot to baseline), `GET /api/admin/smoke/image?path=…` (serves screenshot/baseline/diff PNGs with path-traversal protection scoped to `tests/smoke/`). All five superuser-gated. Plus `POST /api/internal/smoke/trigger` (shared-secret `SMOKE_INTERNAL_TOKEN` bearer) for the post-merge hook — fails closed with 404 when the env var is unset.",
      "**Release ritual section in `replit.md`.** 6-step 5-minute manual checklist (Steam sign-in, inhouse smoke, coaching surface, feature-health, browser-smoke, Discord presence) plus a full reference for the automated suite. Distinct from the existing manual `/admin/smoke-test` checklist tool (Task #479) which is the per-release verification runner."
    ],
    "author": "System"
  },
  {
    "version": "7.90",
    "title": "Owner-only login gate for oceinhouse.gg (Task #493)",
    "published_at": "2026-05-28",
    "notes": [
      "**Why.** While the full edition is still being built out, scrapers (and over-eager link-preview bots) shouldn't get to see in-progress pages. A single env flag now hides the entire site behind a minimal password gate until launch.",
      "**Toggle.** Set `FULL_SITE_LOCKDOWN=1` on the production server to enable the gate. Unset it at launch to remove the gate cleanly — no code change required. Default off, so nothing changes for anyone who doesn't opt in. Documented in `replit.md` under 'Environment variables (security-relevant)'.",
      "**Behavior when on.** Unauthenticated browser navigations (any URL) get a minimal inline HTML sign-in page — no app HTML, no meta description, no OG tags, `X-Robots-Tag: noindex, nofollow, noai, noimageai`. Unauthenticated non-HTML requests (API calls, JSON, images, JS bundles) get a 401 with an empty body. Scrapers and link-preview bots get nothing useful.",
      "**Allowlist.** `/api/stripe/webhook` (server-to-server), `/api/admin/superuser-login` (the gate's own form), `/auth/steam` + `/auth/steam/return` + `/api/auth/complete` (Steam OpenID round-trip), `/robots.txt`, `/favicon.ico`. Everything else is blocked until the visitor authenticates as superuser via session or `x-superuser-key` header.",
      "**Unlock flow.** The inline page POSTs to the existing `/api/admin/superuser-login` (no new endpoint), then reloads to the original URL so deep links survive. After unlock, the real site (Steam login, admin panel, etc.) behaves exactly as before.",
      "**Scope.** Full edition only. Community edition (`dota.stats.corvidaeinc.com`) is untouched — no gate, no env var, no behavior change."
    ],
    "author": "System"
  },
  {
    "version": "7.89",
    "title": "AI scraper / clone-builder hardening — deterrent + observability (Task #492)",
    "published_at": "2026-05-28",
    "notes": [
      "**Problem.** A near-pixel-perfect clone of `oceinhouse.gg` recently appeared in the wild, almost certainly produced by pointing an AI app-builder agent at the live site. We can't stop a human screenshotting, but we can raise the cost: declare the policy in `robots.txt` + meta tags, identify and throttle known AI/agent user-agents, and emit a clear server-side signal when one is detected so a repeat is visible immediately.",
      "**Single source of truth (`src/security/agentUaList.js`).** Lists 21 AI-crawler families (GPTBot, ChatGPT-User, OAI-SearchBot, ClaudeBot, Claude-Web, anthropic-ai, Google-Extended, CCBot, PerplexityBot, Perplexity-User, cohere-ai, Bytespider, Amazonbot, Diffbot, FacebookBot, meta-externalagent, Omgilibot, Applebot-Extended, YouBot, AI2Bot, ImagesiftBot) and 7 app-builder agent families (Replit-Agent, Lovable, v0, Cursor-Agent, Emergent, Devin, Bolt). Each entry carries the documented source URL so the list survives drift. `web/public/robots.txt` and `community-edition/web/public/robots.txt` are generated from this same list — both editions explicitly disallow every family above.",
      "**Runtime classifier (`src/security/agentClassifier.js`).** Mounted as the first middleware in `src/web/server.js` so it runs before everything except `helmet()`. On every request: (a) sets `X-Robots-Tag: noai, noimageai`, (b) classifies the UA as `human` / `ai-crawler` / `app-builder` / `unknown-bot`, (c) records classified hits into an in-process ring buffer (capped at 5000 entries — sustained scrape cannot OOM the process), (d) applies a stricter `express-rate-limit` bucket keyed on `(ip, ua-family)` so one family can't blow up the quota for others sharing an IP. Wrapped end-to-end in try/catch — a bad regex or Discord outage can never break request handling.",
      "**Opt-in hard block via `BLOCK_AI_AGENTS=1`.** Default off (observability first). When set, ai-crawler / app-builder UAs get a 403 with a short policy message; `unknown-bot` is never hard-blocked (observability only, so we can spot new patterns before deciding to add them to the list).",
      "**Owner DM on first-seen-per-24h.** Reuses the dedupe pattern from `src/featureHealth/runner.js` — in-process `Map<family, lastAlertAt>`, 24h window — so a new AI family appearing on the site pings the owner once, not every page-view. `unknown-bot` does not DM.",
      "**`<meta name=\"robots\" content=\"noai, noimageai\">` on both editions.** Added to `web/index.html` and `community-edition/web/index.html`. Per the task scope, community edition gets robots.txt + meta only — none of the runtime middleware, classifier, hard-block, DM, or admin card touches it (community is paywall-free; the surface stays minimal).",
      "**Three-state decision tracking (blocked / throttled / logged).** The agent rate-limiter has a custom `handler` that flips the ring-buffer entry's decision to `throttled` before returning the 429, so the report can distinguish *rejected-by-quota* from *merely-recorded* traffic. The 'AI agent traffic' admin card surfaces all three columns.",
      "**New admin card.** `GET /api/admin/agent-traffic-report?days=N` (superuser; N clamped to 1–30) returns `{ families: [{ family, kind, hits, blocked, throttled, logged, unique_ips, unique_paths, first_seen, last_seen, sample_ua }], recent: [...last 200 raw entries...], ringBufferSize, blockOn }`. Rendered as a new 'AI agent traffic' card in the Overview tab of the full-edition AdminPanel — sortable table using the shared `SortableTh` component, native `<button type=\"button\">` controls with explicit `aria-label`s, expandable recent-requests panel with `aria-expanded`/`aria-controls`. All six a11y rules satisfied.",
      "**Generator script + drift gate.** `scripts/build-robots-txt.js` regenerates both robots.txt files from `agentUaList.js`; `npm run check:robots` (and `... --check`) fails CI if either file drifts. Both editions' robots.txt now carry a 'DO NOT EDIT BY HAND' header.",
      "**Gates.** `npm run check:a11y`, `npm run check:patch-notes`, `npm run check:parser`, `npm run check:robots`, and `scripts/check-community-paywall.sh` all pass. Patch notes get this grouped entry; `replit.md` 'Environment variables (security-relevant)' section documents `BLOCK_AI_AGENTS`."
    ],
    "author": "System"
  },
  {
    "version": "7.88",
    "title": "Feature health dashboard — green/yellow/red status per subsystem (Task #425)",
    "published_at": "2026-05-27",
    "notes": [
      "**Problem.** With 24+ subsystems (Steam auth, parser, OpenDota fallback, inhouse provisioner, captains draft, tournaments, prize pools, three coaching surfaces, Stripe checkout/webhook, web push, leaderboard, hero meta, replay viewer, public v1/mobile APIs, season state, notification prefs, …) there was no single place to ask 'is everything still working?'. `/admin/ops` shows live counters and history but not 'does feature X still actually function end-to-end'. The answer used to be 'try it in the UI and see' — for every feature, every release.",
      "**New `/admin/feature-health` page (superuser-only).** Single-pane table listing every probe with a green/yellow/red status pill, last-run timestamp (relative + absolute), last-success timestamp, and a 'Run now' button per row. A 'Run all' button at the top kicks every probe in series and refreshes the snapshot in place. Reason text shown inline beneath the probe label on failure. Empty/never-run state shows a neutral 'NEVER RUN' badge instead of a misleading green.",
      "**Probe registry in one file (`src/featureHealth/probes.js`).** Adding a new feature to the dashboard is a one-line edit: append `{ key, label, run: async () => ({ ok, reason }) }` to the PROBES array. 24 probes ship in this release covering every major subsystem. Each probe is defensive — wrapped in try/catch + a 10s timeout — so a hung subsystem never blocks the dashboard or other probes.",
      "**Scheduler.** `src/featureHealth/runner.js` runs all probes on a cron tick (default every 30 min, override via `FEATURE_HEALTH_INTERVAL_MS`), with an initial run 2 minutes after boot so Discord/Steam/parser have time to come up. Single-flight guard prevents overlapping ticks. Both timers are `.unref()`'d so tests don't hang.",
      "**Persistence.** New `feature_health_probes` table (id, key, status, reason, duration_ms, ran_at) with `(key, ran_at DESC)` index. Snapshot read uses `DISTINCT ON (key)` for the latest result per probe plus a separate `MAX(ran_at) WHERE status='ok'` for the last-success column. Insert is fire-and-forget — a DB error during recording never breaks the probe loop.",
      "**Owner alerting with 24h dedupe.** When a probe flips from ok → red, the runner DMs the bot owner via `getDiscordBot()._dmOwner()` with the probe key, label, and failure reason. Per-key in-process Map suppresses re-alerts for 24h so a flapping subsystem doesn't spam the owner. Recovery (red → ok) clears the suppression window for that key — the next failure alerts immediately.",
      "**New routes** (both superuser-gated via the existing `requireSuperuser` middleware): `GET /api/admin/feature-health` returns the snapshot; `POST /api/admin/feature-health/run` re-runs either every probe or just the one named in `{ key }` and returns the fresh snapshot inline so the UI updates without a second fetch."
    ],
    "author": "System"
  },
  {
    "version": "7.87",
    "title": "/admin/ops now keeps a rolling 7-day history with per-tile sparklines (Task #423)",
    "published_at": "2026-05-27",
    "notes": [
      "**Problem.** The server-side ops dashboard introduced in Task #406 was process-local — restart the bot and 'was last night's parser slowness new?' was unanswerable. Tiles only showed the current value and a 60-minute 5xx window.",
      "**New `ops_metrics` table.** One row per ~1-minute snapshot, lazily created by `src/web/opsState.js` (`ensureHistoryTable` runs once per process). Columns mirror the live tiles: `http_5xx_60m`, `parser_queue_depth`, `parser_last_duration_ms`, `parser_ready`, `stripe_max_lag_ms`, `provisioner_in_flight`, cumulative `provisioner_success_total` / `provisioner_failure_total`, `discord_gateway_latency_ms`, `push_subscription_count`. `ts DESC` index for the read-side range scan.",
      "**Snapshot writer.** New `captureHistorySnapshot(db)` reads from the in-memory opsState, looks up the live push-subscription count, inserts a row, and prunes anything older than `HISTORY_RETAIN_DAYS=7` in the same call. `src/web/server.js` schedules an initial write 30s after boot (so db.init() + first parser/Discord/Steam reports have landed) and then every 60s. Both timers are `.unref()`'d so tests don't hang. Snapshot failures are best-effort — they log to the ops ring buffer but never throw.",
      "**Provisioner counters.** `reportProvisioner` now increments `successTotal` / `failureTotal` cumulative counters alongside the existing `lastSuccessAt` / `lastFailureAt` timestamps so the frontend can compute per-minute failure deltas from consecutive samples.",
      "**New `/admin/ops/history?hours=N` route.** Superuser-only. Returns `{ retainDays, hours, samples: [...] }` where `hours` is clamped to the retention window. Samples are returned ascending by ts so the frontend can render them directly.",
      "**Frontend sparklines (`web/src/pages/AdminOps.jsx`).** New `<Sparkline>` component renders an inline SVG mini-chart inside each tile: parser duration, Discord gateway latency, push subscriptions, 5xx count (amber/red), and provisioner failures-per-minute (computed client-side as the delta of the cumulative counter, with a guard so a process restart doesn't show as a negative spike). A trend-window selector in the page header (1h / 6h / 24h / 3d / 7d) re-fetches via the new endpoint and refreshes every 60s to match the server-side cadence. 'Collecting…' placeholder shows on tiles with <2 datapoints (e.g. immediately after the schema is first created). A11y gate green — the select has an aria-label and the SVG is marked aria-hidden while the wrapper carries the descriptive label.",
      "**Coverage.** New `tests/opsHistory.test.js` (4 tests) covers the schema-ensure call, insert param order, the best-effort failure path (pool throws → returns false, no exception), the hours-clamping behaviour of `readHistory`, and that provisioner success/failure counters increment correctly across calls."
    ],
    "author": "System"
  },
  {
    "version": "7.86",
    "title": "Live smoke-test checklist tool (Task #479)",
    "published_at": "2026-05-27",
    "notes": [
      "**New `/admin/smoke-test` admin page (superuser, full edition only).** Replaces the manual `docs/site-smoke-test.md` paste-and-tick workflow with a stateful per-release verification runner. Each run snapshots a base checklist of 23 sections (~140 items) covering nav/auth, profile, leaderboards, matches, replays, inhouse lobbies, draft/captain flow, tournaments, prize pools, coaching marketplace, Pro/cosmetics/shop, Stripe webhooks, admin panel, push/Discord notifications, and the a11y house rules — then auto-injects one extra `🆕 Release: vX.YZ` section per patch note shipped since the most recent submitted run, with one verification item per top-level bullet plus a per-section free-form notes slot.",
      "**Stateful and resumable.** New `smoke_test_runs` table (id, started_by_account_id, started_at, submitted_at, base_release_version, template JSONB, state JSONB, summary JSONB). Per-item status pills are pending / ok / flag (radiogroup, keyboard-operable, screen-reader-labelled) and per-item notes auto-save on blur. Closing the tab mid-run loses nothing — the run stays open until explicitly submitted, at which point it freezes read-only and the next new run uses ITS `base_release_version` as the cutoff for which patch notes to auto-inject.",
      "**Markdown export.** Every run exposes `GET /api/admin/smoke-test/runs/:id/export.md` (also linked from the editor + list view as `↗ Export .md`), rendering the snapshotted template + state + summary counts as paste-back markdown that's a drop-in replacement for the old `docs/site-smoke-test.md` workflow.",
      "**Wired into Quick Links.** Admin Panel → Overview tab → Quick Links has a new `🧪 Smoke-test runs` tile next to Patch Notes and Match List. List view shows summary counts (`ok / flag / pending of total`), submission timestamps, and the release version stamped at start.",
      "**Module + helpers.** New `src/data/smokeTestTemplate.js` exports `BASE_SECTIONS`, `buildTemplateForRun`, `buildInitialState`, `summariseState`, `exportRunAsMarkdown`. New `db` helpers: `createSmokeTestRun`, `getSmokeTestRun`, `listSmokeTestRuns`, `getLatestSubmittedSmokeTestRun`, `updateSmokeTestRunItem`, `updateSmokeTestRunOverallNotes`, `submitSmokeTestRun`. The 'since' cutoff is resolved by version-index lookup in `src/data/patchNotes` (newest-first) so the auto-injection survives version renames by safely defaulting to the newest few rather than the whole history."
    ]
  },
  {
    "version": "7.85",
    "title": "Refund-fee reconciliation on coach earnings (Task #421)",
    "published_at": "2026-05-27",
    "notes": [
      "**New `stripe_refund_fee_ledger` table.** Sibling of the existing `stripe_fee_ledger` introduced in Task #405, but keyed on `refund_id UNIQUE` (idempotent on Stripe webhook retries). Stores the refund's own BalanceTransaction: refunded gross (negative), the slice of the original processing fee Stripe is returning (≤ 0, often `0` in AU/NZ where Stripe keeps the fee), the resulting net, plus a `source_kind` / `source_id` pointer back at the originating booking / group seat / VOD review so the earnings query can join straight through without re-walking the PI.",
      "**New `db.upsertStripeRefundsFromCharge(stripe, { paymentIntent | chargeId, sourceKind?, sourceId? })`.** Retrieves the charge with `refunds.data.balance_transaction` expanded, iterates every refund, and upserts one row per refund — partial refunds and multiple refunds against the same charge each get their own row. When called without an explicit source it resolves one from the parent `stripe_fee_ledger` row first (cheap), then falls back to a direct PI → coaching-table scan. Best-effort: failures (missing PI, BalanceTransaction not yet settled) are logged and swallowed so the next webhook redelivery / reconciliation sweep can complete the row.",
      "**Hooked into `charge.refunded` webhook.** `src/web/server.js`'s existing `charge.refunded` branch now calls `db.upsertStripeRefundsFromCharge(stripe, { paymentIntent, chargeId })` after the per-table `markBooking/Seat/VodRefundedByIntent` calls. Covers auto no-show, admin dispute release, and manual Stripe-dashboard refunds.",
      "**`getCoachEarningsMonth` now surfaces refunded rows.** Previously refunded bookings vanished from the completed/delivered filters and the coach had no surface for \"what did the refund actually cost me?\". New refund rows appear bucketed by `refunded_at`. Each row uses the refund's OWN BalanceTransaction amounts (so a partial $30 refund on a $90 booking shows −$30 gross, not −$90), with the platform-fee credit and stripe-fee-share computed proportionally via `ratio = |refund_gross| / orig_charge_gross`. The original charge's processing fee is LEFT-JOINed from `stripe_fee_ledger` and falls back to the AU domestic-card estimate when the parent ledger row is missing.",
      "**Critical math fix — `net_cents` is now the COACH'S real balance delta.** Refund rows carry an explicit `net_cents = -stripe_fee_kept`. The previous algebraic `amount − platform − stripe` was overstating coach losses by the full gross-minus-platform — because the booking, group, and VOD refund paths all use `reverse_transfer:true` + `refund_application_fee:true`, the gross transfer to the coach is cleanly reversed and the platform fee is refunded back, leaving the slice of the Stripe processing fee Stripe did NOT return as the only money the coach truly loses. The frontend + totals trust this explicit `net_cents` for refund rows and skip the recompute.",
      "**Totals are now bucketed.** `totals.gross / platform_fee / stripe_fee` reflect completed-row revenue ONLY (refund reversals don't distort the month's commission + fee figures). New refund-specific totals: `refunded_rows`, `refunded_gross` (sum of negative refund amounts), `refund_net_loss` (sum of `net_cents` across refund rows = the real cost), `stripe_fee_kept_on_refunds`, `stripe_fee_returned_on_refunds`. `totals.net` adds refund `net_cents` so the month-net tile reflects the true bottom line.",
      "**Frontend (`web/src/pages/CoachEarnings.jsx`):** amber summary line above the table (\"3 refunded rows this month — refunded gross −$120 AUD, Stripe kept $5.40 of the original processing fee (your real refund cost)\"). Refund rows render with a `↺ Refunded` amber pill, subtle amber row tint, signed currency formatting (negative gross + credit-style platform/stripe returns read cleanly), and the Net column uses the explicit `net_cents` when present so it shows the actual coach loss. The Stripe-column tooltip on refund rows spells out the fee-kept amount per row.",
      "**CSV export** grew `net_cents`, `stripe_fee_kept_cents`, `refunded`, and `refund_id` columns; TOTAL row carries `stripe_fee_kept_on_refunds` so a downstream sheet can sum the real refund cost directly.",
      "**Coverage:** existing `tests/stripeWebhookRefundsKyc.test.js` continues to pass — the new `db.upsertStripeRefundsFromCharge` call routes through the Proxy fallback in the test's db stub (returns null), so the assertions on `markProRefunded` / `markBookingRefundedByIntent` are unchanged. A11y gate untouched (no new clickable shapes; only span pills + signed-currency cells)."
    ],
    "author": "System"
  },
  {
    "version": "7.84",
    "title": "Season auto-rollover (Task #419)",
    "published_at": "2026-05-27",
    "notes": [
      "**Per-minute cron.** New tick in `src/web/server.js` (90 s after boot, then every 60 s, `.unref()`-ed) finds any active season whose `end_date` (already `TIMESTAMPTZ`, so minute-granularity) has passed and that has no `season_archives` row, then runs the transactional rollover. A single in-flight flag prevents overlapping ticks; the DB rollover is also idempotent so a missed tick or overlapping admin click can never double-archive.",
      "**All-or-nothing transactional rollover.** New `db.rolloverSeason(id)` in `src/db/index.js` runs every required DB step inside a single `BEGIN…COMMIT`: snapshot leaderboard + summary + resolved payouts (gathered before `BEGIN` to keep the txn tight) → `SELECT … FOR UPDATE` the season row → archive (`active=false, season_status='archived'`, matches → `is_legacy=true`) → open next season (prefer explicit `pending`, else create from the new `seasons.next_season_template` JSONB) → persist resolved percent-of-pool payouts back onto the live category rows → stamp `paid_out_at = NOW()` on every payout with a winner (the explicit \"trigger payouts\" step) → mint the six season-end achievements → insert the `season_archives` row last as the idempotency anchor (`UNIQUE(season_id)`). Any failure in any step rolls back the whole thing; no partial state is persisted.",
      "**Season-end achievements (in-txn).** Six new catalog entries — `season_champion` / `season_silver` / `season_bronze` for the final top 3, `season_biggest_climb` for the most-improved player, `season_iron_butt` for most matches that season with ≥5 games, `season_mvp_machine` for most MVP votes received. Minted via the same transactional client as the rest of the rollover so an achievement-insert failure aborts archival + next-season activation along with it. `ON CONFLICT DO NOTHING` keeps retries safe. The missing `mvp_votes` table on community-edition is detected via `information_schema.tables` and the MVP grant is cleanly skipped rather than aborting the txn.",
      "**Discord announce is inside the transaction.** `rolloverSeason()` accepts an `onBeforeCommit` callback that runs while the txn is still open; `bot._closeSeasonAndAnnounce` passes the embed-post step into it. If every announce channel fails (or no channels resolve) the callback throws and the whole rollover ROLLs BACK — DB archival, payout finalization, achievement grants, next-season activation are all undone, and the next per-minute cron tick retries from a clean slate. The follow-up \"next season is open\" message is best-effort post-commit since the main embed is already guaranteed.",
      "**Bot-independent owner alert + no db-only fallback.** New `_alertOwnerOfRolloverFailure` in `src/web/server.js` tries `bot._dmOwner` first (DMs `OWNER_DISCORD_ID`), then a configurable `OWNER_ALERT_WEBHOOK_URL` (Discord/Slack-compatible incoming-webhook POST), and finally a loud `[ROLLOVER FAILURE]` stderr line that always lands in PM2 logs. The cron, the manual `POST /seasons/:id/close` and `POST /seasons/:id/rollover` routes now all REFUSE to roll over when the Discord bot is unavailable — there is no db-only fallback path that could archive a season without announcing it. The routes return HTTP 503 with a retry-when-bot-is-back message; the cron throws into the same owner-alert path and the next per-minute tick retries.",
      "**Admin UI.** Admin Panel → Season Lifecycle now uses a `datetime-local` input for End Date & Time (matches the per-minute cron granularity), and the existing close button always routes through `_closeSeasonAndAnnounce` → `db.rolloverSeason`. Bot-unavailable fallback on `POST /seasons/:id/close` now also calls `db.rolloverSeason` instead of the legacy archive-only path, so the manual close button can never skip the rollover flow. New **↩️ Undo Rollover** button on archived seasons (next to Repost Announcement) calls `POST /seasons/:id/undo-rollover` → `db.undoLastRollover()`, which restores the season to active, un-flags its matches, deactivates the next season if the rollover had opened it, and deletes the archive row — but refuses if the next season already has matches recorded so the operator must reconcile manually.",
      "**New routes.** `POST /api/seasons/:id/rollover` (explicit rollover-now alias), `POST /api/seasons/:id/undo-rollover`, `PUT /api/seasons/:id/next-template` (sets the JSONB blob honoured by the rollover when no explicit pending season exists), and `GET /api/seasons/:id/archive` for fetching a snapshot. All mutating routes are `requireSuperuser`.",
      "**Schema.** Migrations in `src/db/index.js` add `seasons.next_season_template JSONB NULL`, `season_payout_categories.paid_out_at TIMESTAMPTZ`, and a new `season_archives (season_id INT UNIQUE REFERENCES seasons(id), leaderboard JSONB, payouts JSONB, summary JSONB, next_season_id INT, snapshotted_at TIMESTAMPTZ DEFAULT NOW())` table."
    ],
    "author": "System"
  },
  {
    "version": "7.83",
    "title": "Patch-notes dedup + new build-time uniqueness gate (Task #418)",
    "published_at": "2026-05-27",
    "content": "Bot boot was warning on every start about five duplicate version strings in `src/data/patchNotes.js` (v6.80, v6.81, v6.82, v6.83, v6.84). The duplicates landed when a later batch reused version numbers that had already been claimed by older entries — `db.seedPatchNotes()` deduped them at runtime and dropped the second occurrence, but the warning was easy to miss in PM2 logs and the silently-skipped entries never reached the in-app patch-notes feed.\n\n**Cleanup.** Deleted the five duplicate entries from `src/data/patchNotes.js`, preserving the first occurrence of each version as the source of truth (per task spec). The five second-occurrence entries described later cosmetic-shop UX work that was already covered by adjacent entries, so no published content is lost.\n\n**New gate.** Added `scripts/check-patch-notes.js` — loads `src/data/patchNotes.js`, asserts every entry has a unique string `version` field, and exits non-zero with a per-duplicate pointer (`v<x> — first at entry #N, duplicate at entry #M`) on collision. Exposed as `npm run check:patch-notes`. Wired into all three pre-deploy/push gate scripts (`scripts/post-merge.sh`, `deploy.sh`, `community-edition/deploy.sh`) alongside the existing parser-jar / a11y / paywall / wrong-edition gates. A future duplicate now fails the GitHub push or the prod deploy before PM2 is touched, instead of warning silently in production on every boot. `replit.md`'s gate list is updated to gate 5.",
    "author": "System"
  },
  {
    "version": "7.82",
    "title": "Observability — OpenTelemetry → Grafana Cloud (Task #417)",
    "published_at": "2026-05-27",
    "notes": [
      "**OTel SDK bootstrap.** New `src/observability/otel.js` initialises `@opentelemetry/sdk-node` with HTTP + Express auto-instrumentation and OTLP/HTTP trace + metric exporters. Required as the very first side-effect in `src/index.js` (after dotenv) so HTTP/Express/pg are auto-hooked before they're loaded. Gated on `OTEL_EXPORTER_OTLP_ENDPOINT` — when unset, the SDK never starts and there is zero perf cost in dev. Companion env vars: `OTEL_EXPORTER_OTLP_HEADERS` (e.g. Grafana Cloud `Authorization=Basic <base64(instanceID:token)>`), `OTEL_SERVICE_NAME` (defaults to `oi-bot`), `OTEL_METRICS_EXPORT_INTERVAL_MS` (defaults to 30s). Fs/dns/net auto-instrumentations are explicitly disabled to keep trace volume on the free tier sane.",
      "**Custom spans.** `withSpan()` helper in `src/observability/tracing.js` wraps: replay parse round-trip (`replay.parse`), HTTP replay download (`replay.download`), dedicated-server provisioner (`inhouse.provision`), Discord channel sends (`discord.send`), and every Stripe SDK call (`stripe.<resource>.<method>`). Stripe instrumentation is transparent — a single cached `getStripe()` returns a recursive `Proxy` so all 19 `require('stripe')(STRIPE_SECRET_KEY)` call sites in `src/web/server.js` now flow through the wrapper without per-call boilerplate.",
      "**Metrics.** New `src/observability/metrics.js` exposes HTTP request count/duration (by method/route/status), parser parse-duration histogram + queue-depth gauge, Stripe call latency + outcome counter, Stripe webhook processing-lag histogram, Discord send count/duration, provisioner run count/duration (by trigger), replay-download duration/size, and Expo push delivery success/removed/failure counters. Wired into the existing Express finish-listener (next to the 5xx ops counter) and the existing ops-state reporters so nothing is double-counted and the ops dashboard semantics are preserved.",
      "**Grafana dashboard.** `ops/grafana/dashboard.json` ships an importable Service Overview with ten panels covering HTTP throughput + p95 by route, parser queue depth + p50/p95, Stripe call latency by op, webhook lag by event type, push delivery rates, provisioner success/failure, Discord send outcomes, and replay-download p95. Re-importable from Grafana → Dashboards → New → Import.",
      "**Docs.** `replit.md` Environment variables section documents `OTEL_EXPORTER_OTLP_ENDPOINT` + companion vars and points at the committed dashboard JSON.",
      "**Out of scope.** Log aggregation (PM2 logs stay as-is), frontend RUM, and Grafana alert rules — operators can layer those on top via the Grafana UI without code changes."
    ],
    "author": "System"
  },
  {
    "version": "7.81",
    "title": "Money-path test coverage (Task #416)",
    "published_at": "2026-05-27",
    "notes": [
      "**New test file** `tests/moneyPathsCoverage.test.js` adds 21 integration tests that close the coverage gaps in the Stripe-touching surface — every test runs against the in-process Express app with a stubbed Stripe SDK + a Proxy-backed `db` stub, so the whole file completes in under three seconds with no live network and no real DB.",
      "**Webhook coverage.** Extends the existing `stripeWebhookAsyncPayment` / `stripeWebhookRefundsKyc` files with the two coaching purposes they missed: `coaching_group_seat` and `coaching_vod_review`. For each purpose we now assert `checkout.session.completed` (paid AND unpaid), `checkout.session.async_payment_succeeded`, `checkout.session.async_payment_failed`, `checkout.session.expired`, `payment_intent.succeeded` (group-seat capture), and `payment_intent.canceled` — plus a `charge.refunded` test that proves a single refund event flips both the group-seat row AND the VOD row by intent id (the webhook calls every `markXRefundedByIntent` helper, not just `markBookingRefundedByIntent`).",
      "**Refund fail-closed.** Three new tests prove the route-level Stripe wrappers never flip a DB row to `refunded`/`cancelled` when the Stripe API throws: `/bookings/:id/no-show-refund` (1:1 booking → must return 502 with no `markBookingRefunded` call), `/me/coach/group-sessions/:id/cancel` (group session → must return 502 with no `setGroupSessionStatus('cancelled')` and no `markGroupSeatRefundedByIntent`), and `/vod-reviews/:id/refund` (VOD review → must return 502 with no `setVodStatus` and no `markVodRefundedByIntent`). Each path is paired with a happy-path counterpart so the happy path keeps working.",
      "**Race coverage.** New concurrency test fires 5 simultaneous `POST /api/group-sessions/:id/join` calls against a capacity-3 session. `db.reserveGroupSeat` is mocked with a serialised counter that mirrors the SQL `FOR UPDATE` + capacity-check pattern in `db.reserveGroupSeat`. Asserts exactly 3 reservations succeed, 2 are rejected with HTTP 400 \"Session full\", and Stripe checkout creation is invoked exactly 3 times — i.e. no over-booking past capacity. The inhouse server-provisioner single-flight race is already covered by `tests/inhouseServerProvisioner.test.js` and was left as-is.",
      "**Test infrastructure.** The stubbing pattern matches the existing money-path tests (`stubModule()` cache poisoning + a Proxy `db` stub + `setInterval` unref so the bot's background tickers don't keep node alive). Two small additions: a `_findRouterRoute` helper that walks the api router mounted at `/api` (the existing helpers only reached `app`-level routes like the webhook), and a `_runRoute` helper that walks every layer on a router route so `express.json()` middleware is a no-op over a pre-parsed body.",
      "**Deploy gate.** New `npm run check:money-paths` script runs only the money-path test files (`moneyPathsCoverage` + `stripeWebhookAsyncPayment` + `stripeWebhookRefundsKyc` + `inhouseServerProvisioner`) — 71 tests in ~3.4s with no live network. Wired into `scripts/post-merge.sh` (aborts the GitHub push) AND `deploy.sh` (aborts the full-edition prod deploy BEFORE PM2 is touched), so a Stripe / refund / race regression can no longer reach origin/main or replace a working PM2 process. Community-edition `deploy.sh` is intentionally NOT wired — community edition is paywall-free and ships none of the Stripe surface.",
      "**Out of scope.** Pre-existing failures in `pickem`, `adminRouteManifest`, and a couple of source-guard tests are untouched by this work — they were already red before this batch and are unrelated to the money paths."
    ],
    "author": "System"
  },
  {
    "version": "7.80",
    "title": "Public API v2 + developer portal (Task #415)",
    "published_at": "2026-05-27",
    "notes": [
      "**Scoped API keys.** Every key now carries a `scopes` array drawn from `read`, `read:matches`, `read:players`, `read:leaderboard`, `read:teams`, and `write:webhooks`. The legacy `read` scope from Task #371 keeps acting as a catch-all wildcard for all `read:*` endpoints, so every key issued before today keeps working unchanged. New keys default to a least-privilege selection (`read:matches` + `read:leaderboard`) which the user can tighten or expand at creation, and a new **Edit** action on each row lets owners change scopes after the fact without re-issuing.",
      "**Per-key rate limits.** New `api_keys.rate_per_min INTEGER` column overrides the tier default when set (hard-capped at 10,000/min so a misconfigured key can't shadow infra limits). Defaults move to 60 req/min for anonymous IP traffic, 60 req/min for keyed free-tier keys, and 600 req/min for keyed Pro keys (was 30/120). Daily quotas (1,000/50,000) and all `X-RateLimit-*` response headers continue to work; the limit header now reflects the per-key value when one is set.",
      "**`/developers` portal with live runner.** New manifest-driven docs page at `/developers` (`web/src/pages/Developers.jsx`). One `ENDPOINTS` array drives both the human-readable reference table AND a per-endpoint **Try it** runner — paste your key once (stored in `localStorage` so it survives reloads), fill in path/query/body fields, and the runner fires the real `fetch` against this deployment, prints status / latency / rate headers / pretty-printed JSON. Endpoint cards are real `<button aria-expanded>` toggles so keyboard users can navigate them like a screen-reader-friendly accordion. The old `/api-docs` page is kept as a thin reference for back-compat.",
      "**`match.finalized` webhook.** New outbound event fired alongside `match.ended` once the OpenDota Java parser has produced full stats. Payload is explicitly versioned (`version: 1`) and stable: match + radiant_win + duration + season + patch + recorded_at + a per-player array with hero/team/slot/k-d-a, last hits, denies, GPM, XPM, hero/tower/healing damage, net worth, level, and items. The looser legacy `match.ended` payload stays as-is so any existing subscriber keeps working.",
      "**Two new `/v1` endpoints.** `GET /v1/teams` and `GET /v1/teams/:id` (require `read:teams`) list active teams with member counts and return per-team rosters. `GET /v1/webhooks`, `POST /v1/webhooks`, and `DELETE /v1/webhooks/:id` (require `write:webhooks`) let a key programmatically manage subscriptions owned by the key's account, reusing the same `validateWebhookUrlSync` + `assertSafeAtDispatch` SSRF guards as the browser-session flow.",
      "**Server API.** `POST /api/me/api-keys` accepts optional `scopes` + `rate_per_min` at creation; new `PATCH /api/me/api-keys/:id` edits scopes / rate / label on a live key. The list response now includes `scopes`, `rate_per_min`, and the full `known_scopes` set so the UI can render checkboxes without hardcoding. `/v1/status` reports `product_version: 'v2'`, the full known event set (including `match.finalized`), the known scope set, and both free + pro keyed defaults — keeping the URL on `/v1` so subscribers don't have to repoint. The anonymous IP-burst limiter (60/min) is scoped to `/v1/status` only so it never shadows a Pro key's 600/min budget from a single integration host. The `/v1/webhooks` management endpoints additionally gate on Pro tier (returning `403 pro_required` otherwise), matching the existing session-based webhook policy in `server.js`.",
      "**A11y.** Every new clickable surface — endpoint accordion buttons, scope checkboxes, edit/save/cancel/revoke key buttons, copy buttons, webhook toggles — is a real `<button type=\"button\">` (or `<input>` with paired `<label>`) carrying a descriptive `aria-label`. The Try-it result panel uses `role=\"status\" aria-live=\"polite\"` so screen readers hear the response without stealing focus. Accordion toggles set `aria-expanded`. No hover-only reveals, no new `<div onClick>` patterns. A11y gate stays green.",
      "**Out of scope.** No OAuth, no SDKs, no billing — by design. Community edition stays paywall-free and ships none of this surface."
    ],
    "author": "System"
  },
  {
    "version": "7.79",
    "title": "Mobile companion v2 — write actions (Task #414)",
    "published_at": "2026-05-27",
    "notes": [
      "**Six write actions on mobile.** The Expo companion app (`mobile/`) graduates from read-only to read+act. Users can now (1) accept/decline a lobby ready-check, (2) cast their post-match MVP vote, (3) accept/decline a scrim proposal, (4) approve/decline a roster-transfer, (5) book a coach for either a 1:1 session or a VOD review — including plan redemption — and (6) acknowledge a coaching booking reminder. Each action is a dedicated deep-link screen under `mobile/app/action/<kind>/[id].tsx` with one-tap confirm, busy/ok/error states, and a shared `<ActionShell>` chrome so the UX is identical across kinds.",
      "**Push → deep-link routing.** Existing fan-out pushes already encode `data.url` (e.g. `/action/ready-check/123`). `app/_layout.tsx` now listens for `Notifications.addNotificationResponseReceivedListener` and forwards the URL to Expo Router, so tapping a push lands directly on the right confirm screen. The same handler also accepts custom-scheme deep links of the form `oceinhouse:///action/<kind>/<id>?…` for share/test entry points.",
      "**Shared session, no new auth.** All write actions reuse the existing Steam OpenID → `oi.sid` session cookie that the v0.1 read-only app already manages via `expo-secure-store`. No new auth path. A 401 from any write call routes through the new `setOnUnauthorized` hook in `mobile/lib/api.ts` and raises a single, coalesced app-wide **Session expired** modal in `_layout.tsx` — “Sign in again” clears the cookie and routes to `/sign-in`, “Not now” dismisses.",
      "**Two new server endpoints.** `POST /api/matches/:matchId/mvp-vote` (validates the voter and candidate played on the same team in this match and the vote is inside the 24h window, then writes via `saveMatchRating`) and `POST /api/bookings/:id/reminder-ack` (student-only, gated by the existing `_coachingOn` flag, writes `coaching_bookings.reminder_acked_at`). All four other write paths reuse the existing routes — `/api/inhouse/:id/accept|decline`, `/api/scrims/:id/respond`, `/api/roster-transfers/:id/respond`, `/api/coaches/:id/book` and `/api/coaches/:id/vod-review` — which already accept the mobile session cookie unchanged.",
      "**Schema.** One additive column: `coaching_bookings.reminder_acked_at TIMESTAMPTZ` (`ALTER … ADD COLUMN IF NOT EXISTS`, no backfill needed). Paired with `db.ackBookingReminder(id, accountId)` which only writes if the row's `student_account_id` matches the caller and the column is still NULL — idempotent and authorization-safe.",
      "**A11y.** Every clickable on every new screen is a real `<Pressable accessibilityRole=\"button\">` with descriptive `accessibilityLabel`; MVP teammate cards use `accessibilityRole=\"radio\"` + `accessibilityState.selected`; reauth modal carries `accessibilityViewIsModal` + `role=\"alert\"`; status messages use `accessibilityLiveRegion=\"polite\"`. No hover-only reveals. The web `check:a11y` gate is unchanged — it scans `web/src/` only, mobile RN code is out of its scope.",
      "**Out of scope.** No new write actions beyond the six above, no offline queueing of pending actions, no app-store submission — this still ships via EAS dev builds / Expo Go.",
      "**Edition scope.** Full edition only. The community edition stays paywall-free by policy and its mobile companion remains intentionally unwired."
    ],
    "author": "System"
  },
  {
    "version": "7.78",
    "title": "Coaching v3 — recurring student plans (Task #413)",
    "published_at": "2026-05-27",
    "notes": [
      "**Recurring plans.** Coaches can now sell monthly Stripe Billing subscriptions to repeat students. A plan bundles a quota of 1:1 sessions, group-session seats, and async VOD reviews per billing cycle (any mix, at least one quota field > 0). Pricing is set per-plan in AUD; existing per-session and group/VOD prices are unaffected — plans are purely additive.",
      "**Schema.** Three new tables: `coach_plans` (catalog: name, description, price_cents, quotas, status draft/active/archived, stripe_product_id + stripe_price_id), `coach_plan_subscriptions` (per-student subscription with used_* counters, period_anchor, cancel_at_period_end, partial unique index blocking double-live-subscriptions per (plan, student)), and `coach_plan_invoices` (idempotent on stripe_invoice_id, mirrors every paid renewal). Booking tables get a `plan_subscription_id` column so redemption rows can be distinguished from cash bookings in earnings + audits.",
      "**Stripe Billing wiring.** Publish a draft plan → server creates a Stripe Product + recurring monthly Price under the coach's umbrella and stores both ids back on the row. Student subscribes via a Checkout session in `mode=subscription` with destination charges (`application_fee_percent` set from `resolveCommissionBpsForCoach`) so the platform commission rides on every renewal automatically — no manual capture step required. Both `customer.subscription.*` and `invoice.payment_succeeded` are now webhook-handled: subscription events flip status + reset quota counters when `current_period_start` rolls forward; invoice events record paid amounts into `coach_plan_invoices` keyed off `stripe_subscription_id` (no metadata trust required, since Stripe doesn't always echo it onto invoice objects).",
      "**Booking redemption.** The three booking routes (`/coaches/:id/book`, `/group-sessions/:id/join`, `/coaches/:id/vod-review`) accept an opt-in `use_plan: true` flag. Path resolves the student's active sub on this coach, atomically debits one slot of the matching quota kind via `consumeCoachPlanQuota` (single `UPDATE … WHERE used < quota RETURNING` so two concurrent redemptions can't oversell), inserts the booking/seat/review at amount=0/fee=0 with status='paid' and `plan_subscription_id` stamped, and short-circuits Stripe entirely. Any insert failure refunds the quota via `refundCoachPlanQuota` (bounded by GREATEST(used-1, 0) so a double-call can't underflow). No-active-sub or quota-exhausted returns a 400/409 rather than silently falling through to a cash charge — the UI shows two distinct CTAs.",
      "**Earnings dashboard tiles.** `getCoachEarningsMonth` now joins paid plan invoices into the same line-item list (kind=`plan_invoice`, reconciled=true since Stripe Billing carries the real fee). Three new MRR-style tiles render above the existing gross/net tiles: **MRR (active plans)** = sum of `coach_plans.price_cents` over every subscription in active/past_due right now; **Active subscribers** = count of same; **Retained this month** = distinct subscribers with a paid invoice this month AND at least one earlier paid invoice (i.e. renewals, not first-time signups).",
      "**API surface.** New routes: `POST/GET /coach/plans`, `PATCH /coach/plans/:id`, `POST /coach/plans/:id/publish`, `GET /coaches/:id/plans` (public, active-only), `POST /coaches/:id/plans/:planId/subscribe`, `GET /me/coaching/plan-subscriptions`, `GET /me/coach/plan-subscribers`, `POST /me/coaching/plan-subscriptions/:id/cancel` (cancel-at-period-end; either party may call). Client helpers added to `web/src/api.js` including `bookCoachWithPlan`, `joinGroupSessionWithPlan`, `requestVodReviewWithPlan` convenience overloads.",
      "**UI.** Coach editor gets a new `📦 Recurring student plans` card — draft → publish → archive lifecycle, inline subscriber roster with per-quota usage counters. Public coach profile gets a `📦 Monthly plans` section above the booking form listing every active plan with a Subscribe CTA (Stripe Checkout). Out-of-scope per the task: tiered plans, gifting, mid-cycle pro-rating.",
      "**A11y.** All new interactive surfaces are real `<button type=\"button\">` with descriptive `aria-label`s; status messages use `role=\"status\"`; sections use `aria-labelledby`. No hover-only reveals or new `<div onClick>` patterns. A11y gate stays green.",
      "**Edition scope.** Full edition only — community edition is paywall-free by policy and ships no plan surface."
    ],
    "author": "System"
  },
  {
    "version": "7.77",
    "title": "Tournament v2 — Swiss, check-in window & per-place payouts (Task #412)",
    "published_at": "2026-05-26",
    "notes": [
      "**Swiss format.** New tournament format alongside single/double elimination. A pure pairing engine in `src/tournaments/swissEngine.js` handles round 1 (top-half-vs-bottom-half by seed), subsequent rounds (group by score, greedy no-repeat pairing within each score group, bottom seed gets the bye and at most one bye per player), and final standings with **Buchholz** (default) or **Sonneborn–Berger** tiebreaks. Recommended round count is auto-derived from participant count (≤4 → 2 rounds, ≤8 → 3, ≤16 → 4, ≤32 → 5, ≤64 → 6, else 7) when the admin leaves the field blank.",
      "**Schema.** `tournaments` gets `swiss_rounds`, `tie_break_method` (default `buchholz`), `checkin_offset_min` (default 30), `checkin_dq_done`, `payouts_finalized_at`, `starts_at`. Three new tables: `tournament_checkins` (PK on tournament_id + account_id), `tournament_prize_splits` (place → percent, replaces the legacy JSONB column for new tournaments while still mirroring back to it for backward-compat), and `tournament_payouts` (snapshot of place + percent + amount_cents per account).",
      "**Structured check-in window.** Each tournament can specify `starts_at` and a `checkin_offset_min` (default 30). A new `📋 Check-in window` panel on the tournament page shows opens/closes timestamps and a live participant count. Players who are registered (or have a paid `tournament_entries` row) hit `POST /tournaments/:id/checkin` to confirm — server-side window check rejects early/late attempts. The button shows `Check in` → `…` → `✓ Checked in` and disables outside the window.",
      "**Auto-DQ sweep.** A 60s cron (`__t412_checkin_sweep`) calls `db.sweepTournamentCheckInDqs` against every `upcoming` tournament whose `starts_at` has passed and whose `checkin_dq_done` is still false. Anyone who didn't check in is removed from `tournament_participants` in a single `DELETE … NOT IN` and `checkin_dq_done` is flipped to TRUE so the sweep is idempotent across restarts. Bracket generation honours the check-in set: when ≥1 player has checked in, only checked-in participants enter the bracket; with zero check-ins it falls back to all signed-up players so admin-skipped events still work.",
      "**Per-place prize splits & auto-payout.** A new admin **💰 Per-place prize splits** editor accepts either positional (`50,30,20`) or explicit (`1:50, 2:30, 3:20`) notation, validates that percentages sum to 100, and writes both the normalized `tournament_prize_splits` table and the legacy `tournaments.prize_split` JSONB so older consumers stay in sync. When a Swiss tournament's final round resolves, `_maybeCompleteSwissTournament` flips status to `completed` and calls `finalizeTournamentPayouts` — pure idempotent snapshot of standings × splits × prize pool into `tournament_payouts`. Single/double-elim brackets can be finalised on demand by superusers via the same Snapshot payouts button. A new **🏆 Payouts** panel renders the resulting place/player/percent/amount table.",
      "**API surface.** `GET /tournaments/:id/standings`, `GET/POST /tournaments/:id/checkins`, `POST /tournaments/:id/checkin` (Steam session required), `POST /tournaments/:id/advance-swiss-round` (superuser), `GET /tournaments/:id/prize-splits` + `PUT /tournaments/:id/prize-splits` (superuser), `GET /tournaments/:id/payouts`, `POST /tournaments/:id/finalize-payouts` (superuser). `POST /tournaments` now accepts `swissRounds`, `checkinOffsetMin`, `tieBreakMethod`, `startsAt`. Client helpers added to `web/src/api.js`.",
      "**Bracket plumbing.** `generateTournamentBracket` short-circuits to `generateSwissBracket` when `format === 'swiss'`. `setTournamentMatchWinner` on a Swiss match no longer routes to the next bracket slot — Swiss is round-based, so it just records the winner and calls `_maybeCompleteSwissTournament` to detect end-of-event. Swiss matches use `bracket = 'S'`; bye matches insert with `p2_id = NULL` and `winner_id` pre-set so they count immediately in standings.",
      "**A11y.** All new clickables (`Check in`, `Pair round N`, `Save`, `Snapshot payouts`) are real `<button type=\"button\">` with descriptive `aria-label`s. The prize-splits input has a paired `<label htmlFor>`. Status messages use `role=\"status\"`. No hover-only reveals; no new `<div onClick>` patterns. A11y gate stays green.",
      "**Edition scope.** Full edition only — community edition is paywall-free and ships no tournament v2 surface."
    ],
    "author": "System"
  },
  {
    "version": "7.76",
    "title": "Replay viewer v3 — fights, item hover & share clips (Task #411)",
    "published_at": "2026-05-26",
    "notes": [
      "**Team-gold delta sparkline.** A new compact strip sits above the scrub bar showing radiant-minus-dire net worth across the match as a single signed curve (green = Radiant ahead, red = Dire ahead) with a zero baseline and a synced amber scrub cursor. The delta at the cursor reads in the strip's header (`Rad +6.2k` / `Dire +1.8k`), so a glance reads momentum + lead at the same time as the minimap below. Click anywhere on the strip to scrub to that minute.",
      "**Hover-to-inspect items.** Mousing over a hero marker on the minimap pops a small tooltip anchored next to the hero showing slot/name, **Inventory** (up to 6 items purchased on or before the scrub time), and **Backpack** (the previous 3 when more than 6 are owned). Tooltip border colour matches the team; positioning auto-flips so it never falls off the canvas. Best-effort — the parser stores purchase times not slot occupancy, so the snapshot is approximate.",
      "**Auto-detected team fights.** A new `match_fights` table (cascade-deleted with the match, `UNIQUE(match_id, start_s)`) persists fights detected by `src/replay/fightDetector.js` — a pure shared helper that clusters hero kills within a 10s temporal gap **and** ~1500-unit spatial radius, requiring ≥3 unique heroes / ≥2 deaths. Stored per fight: `start_s`, `end_s`, `heroes[]`, `winner`, `radiant_deaths`, `dire_deaths`. Detection runs inline in `replayParser._aggregateStats` and persists post-commit in `recordMatch` so a failure can't roll back the match record.",
      "**Fight chips strip.** Each detected fight renders as a coloured pill along the scrub bar (proportional width, green/red/grey by winner), with the live scrub cursor overlaid. Every chip is a real `<button type=\"button\">` with `aria-label` describing time / hero count / winner / deaths, so keyboard users can tab through and Enter-to-jump exactly like sighted clickers. Empty state surfaces an italic *No auto-detected team fights for this match* line rather than leaving a blank rail.",
      "**Share-clip button + URL params.** A new 🔗 **Share clip** button copies a deep link of the form `/replay/:id?t=START&end=END&focus=heroId` (default window = 15s from the current cursor; `focus` is the currently hovered hero's id, or the inbound `?focus=` if no hover). When a recipient opens the link, the viewer autoplays from `t`, pauses cleanly at `end`, highlights the focused hero with a gold ring on the minimap, and shades the clip window across the sparkline. Falls back to surfacing the raw URL inline when the browser blocks `navigator.clipboard`.",
      "**Discord/Slack rich previews.** New `GET /replay/:matchId` social-unfurler handler (UA-gated by `_isSocialUnfurler`, real browsers fall through to the SPA) returns Open Graph + Twitter card meta tags. When `?t=` is present the title reads `Replay clip @ 12:34–12:49 · Match #… · OCE Inhouse`; description includes the winner + score from `_resolveOgMatch` and the focused hero id when supplied. Reuses the existing `/og/match/:id.png` card image so unfurls land with a proper hero thumb instead of a blank link.",
      "**Admin backfill.** New `POST /api/admin/replays/backfill-fights` (superuser-only) walks every match that has a stored `game_timeline` but no `match_fights` rows yet, replays detection over the persisted timeline JSON, and writes the result. Runs in the background with a polling status endpoint; the **Admin → Stored Replays** panel gets a new ⚔️ **Backfill fights** button showing `done/total · failed · detected · remaining`. No .dem files needed — pure in-DB pass.",
      "**API surface.** `GET /api/matches/:id/replay-timeline` now includes a `fights: []` field on the response, preferring persisted `match_fights` rows and falling back to the embedded `timeline.fights` blob (which the parser also writes on first parse, so freshly-parsed matches Just Work even before the backfill runs). Client helpers `backfillReplayFights` + `getReplayFightsBackfillStatus` added to `web/src/api.js`.",
      "**A11y.** All new clickable surfaces — share button, fight chips, backfill button — are `<button type=\"button\">` with `aria-label`; the sparkline SVG carries `role=\"img\"` + descriptive `aria-label`; the share-confirmation message uses `role=\"status\"`; tooltip uses `role=\"tooltip\"` with `pointer-events: none` so it never traps focus. No hover-only reveals (the tooltip mirrors info already available in the side panel). A11y gate stays green.",
      "**Edition scope.** Full edition only — the community edition stays paywall-free and pro-feature-free per policy and ships none of these viewer surfaces."
    ],
    "author": "System"
  },
  {
    "version": "7.75",
    "title": "Coaching marketplace discovery upgrade (Task #410)",
    "published_at": "2026-05-26",
    "notes": [
      "**`/coaches` rebuilt as a real marketplace.** Two-column layout: a sticky filter sidebar (Position 1–5, Language, Max $/hr, Minimum rating ★3/★4/★4.5, **Available this week** toggle) and a results column with a results-count strip and a **Sort by** dropdown — Relevance (default), Price: low→high, Price: high→low, Top rated, Next available, Most booked. Coach Premium subscribers always float to the top of every sort mode (paid perk preserved); the chosen sort breaks ties within each premium tier. Reset-filters button clears everything in one tap.",
      "**Instant Booking badge.** Cards whose next weekly availability slot falls within the next 48h get a green **⚡ Instant** pill alongside the existing ★ Premium pill. `next_available_at` is computed in JS by walking the next 14 days of each coach's recurring availability slots in the slot's stored IANA timezone (default `Australia/Sydney`), using `Intl.DateTimeFormat` to convert wall-clock day-of-week + HH:MM back to absolute UTC. Same instant is shown as a friendly relative label on the card (`In 6h`, `In 2d`, …).",
      "**Coach of the Month spotlight tile.** Auto-picked once per page render by `db.getCoachOfTheMonth({tenantId})` — ranks active coaches by `(deliveredBookingsThisMonth × avgRating)` with ties broken by total delivered bookings, then `created_at`. Coaches with zero monthly bookings or no ratings are excluded so the tile never falls back to a freshly-onboarded profile. Renders a brass-bordered hero card above the grid with name, roles, rating, monthly session count, bio preview, and a deep-link to the profile.",
      "**Consent-gated annotation snippets.** New `coaches.review_consent_quotes BOOLEAN DEFAULT FALSE` column plus a `role=\"switch\"` toggle in `/coach/edit` (\"Share anonymised review snippets\"). When ON, up to 3 anonymised excerpts from delivered VOD-review annotations (`coach_vod_review_notes` joined on `coach_vod_reviews.status = 'delivered'`) appear on both the public marketplace card (`/coaches`) and the coach detail page (`/coaches/:id`, rendered as a dedicated **Sample annotations** section above Recent reviews, up to 3 quotes). Default OFF — coaches must explicitly opt in.",
      "**Snippet anonymisation pipeline.** All quotes pass through `_anonymiseSnippetText` before leaving the API. It redacts URLs (`http(s)://…`, `www.…`), email addresses, Discord `<@id>` pings, `@handle` mentions, `username#1234` discriminators, and any digit run of 5+ characters (Steam IDs, account IDs, match IDs). A leading greeting + name (`Hi Alex,`, `Thanks Sam` …) is stripped of the trailing name. Snippets whose redacted markers cover >40% of the visible text are dropped to `null` rather than shipped as nonsense, and the surviving text is whitespace-collapsed and hard-capped at 180 chars. Coverage locked in by `tests/coachSnippetAnonymise.test.js`.",
      "**API surface.** `GET /api/coaches` now accepts `min_rating`, `available_this_week`, and `sort` (whitelisted to one of six values, anything else falls back to `relevance`). Response shape extended to `{ coaches, coach_of_the_month }`; each coach row carries `next_available_at` (ISO or `null`), `instant_booking` (bool), `delivered_bookings_count`, `monthly_bookings`, and a `snippets` array (consent-gated, omitted when off). `GET /api/coaches/:id` likewise returns `snippets: []` so the detail page has the same shape regardless of opt-in state.",
      "**DB helpers.** New `getCoachOfTheMonth`, `getCoachSnippets`, plus a rewritten `listActiveCoaches` that pulls availability for the result set in a single batched `ANY($1::bigint[])` query (no N+1), then computes derived fields in JS. Min-rating filter is applied post-fetch so coaches with `NULL` avg can still pass when the filter is unset. Snippet fetch is best-effort — a query failure leaves the listing intact, just without quotes.",
      "**A11y.** Sort dropdown is a real `<select>` with `aria-label`; filter inputs are labelled via paired `<label htmlFor>`; the *Available this week* and *Share anonymised review snippets* toggles use `role=\"switch\"` + `aria-checked`; the spotlight CTA + Reset filters are real `<button type=\"button\">` / `<Link>` elements with `aria-label`; no hover-only reveals on any new surface. The a11y gate stays green.",
      "**Out of scope** per task brief: paid promotion / sponsored-coach slots, coach-to-coach messaging, group-session filters (group sessions still surface through the existing `/coaching/group-sessions` page).",
      "**Edition scope.** Full edition only — community edition is paywall-free and ships no coaching marketplace surface."
    ],
    "author": "System"
  },
  {
    "version": "7.74",
    "title": "Hero meta v3 — patch diff + draft trainer (Task #409)",
    "published_at": "2026-05-26",
    "notes": [
      "**New `/heroes` › Patch Diff tab.** Side-by-side comparison of two patches with per-hero deltas in win rate, pick rate, ban rate, and position distribution. Top 5 movers (by |Δ| of the chosen metric) float to the top with a gold accent; the picker defaults to the two newest patches present in the database. A min-games filter (default 3) hides noisy single-game outliers; the sort key toggles between |Δ WR| / |Δ pick rate| / |Δ ban rate|. Powered by a new `GET /api/heroes/patch-diff?from=&to=&season=` endpoint that aggregates picks (and bans from `match_draft` when present) with a single `_getHeroPatchAggregates` helper run in parallel for each side.",
      "**New `/heroes/draft-trainer` page (lazy-loaded).** Captain-mode simulator — pick your side (A or B), the engine picks/bans for the other side using the existing `getHeroCounterScores` counter-pick engine. The CM sequence is 7-4-4-4-3-2 (24 actions). Finishing the draft scores a predicted advantage by comparing the counter-WR of your actual line-up against the best-available counter set (clamped to [-1, +1]), with a one-line verdict (`strong favourite` → `underdog`) and explanation. Engine turns auto-advance; user picks are committed via a searchable hero grid where every tile is a real `<button type=\"button\">` with `aria-label`.",
      "**Trainer accuracy persistence.** Completed runs by signed-in users are saved to a new `draft_trainer_runs` table (`account_id, side, picks_a, picks_b, bans, predicted_advantage, matched_match_id, matched_outcome, evaluated_at, created_at`). `evaluateUnmatchedDraftRuns` looks for any real non-legacy match whose 5+5 picks (sorted, set-equal) match the recorded draft on either side; when found, it records the outcome from the user's POV and marks `sign(predicted_advantage) === outcome` as correct. The hook is fired best-effort right after each save so accuracy starts climbing immediately when a match is already in the DB.",
      "**Profile widget.** Players with ≥ 1 saved run get a new `🎯 Draft Trainer accuracy` section on their profile (`/player/:id`) showing rolling accuracy (correct / matched), total runs, and a deep-link back to the trainer. Hidden when there are no runs so unused profiles don't grow a dead section. Fed by `GET /api/player/:id/draft-trainer-accuracy`.",
      "**Engine reuse, not a new scorer.** Both surfaces piggyback on the existing v2 infrastructure: `getHeroCounterScores` / `_computeHeroCounterMap` for trainer picks, `getHeroPatchWinRates` aggregation patterns for the diff. No new ratings/Elo and no ML — the trainer is a teaching aid that uses the same numbers `/heroes/counter-pick` already shows.",
      "**API surface (all public, read-only except the save path).** `GET /api/heroes/patch-diff`, `POST /api/heroes/draft-trainer/simulate-pick` (anonymous-OK so users can play before signing in), `POST /api/heroes/draft-trainer/runs` (requires `req.session.accountId`; returns 401 otherwise), `GET /api/player/:id/draft-trainer-accuracy`. Save responses kick off an async accuracy evaluation pass scoped to that account.",
      "**A11y.** Hero-grid tiles, reset, and side-picker radios are real form controls with labels; the search input has `aria-label`; tab strip continues to use the existing `role=\"tablist\"` infrastructure on `/heroes`. No new modals — the verdict card inlines under the draft."
    ],
    "author": "System"
  },
  {
    "version": "7.73",
    "title": "Smurf detector (advisory) (Task #408)",
    "published_at": "2026-05-26",
    "notes": [
      "**New `/admin/smurf-watch` page (superuser-only).** A heuristic scorer flags accounts that look like smurfs based on five signals — shared-lobby graph (30 pts), hero-pool entropy (20 pts), early-game PERF outliers vs claimed TrueSkill bracket peers (20 pts), account age vs MMR climb (20 pts), and best-effort IP/UA fingerprint overlap (10 pts) — summing to a 0–100 score. The page shows flagged accounts above the threshold, with a per-row expandable signal breakdown (value, contribution, detail), a link to each player profile, and an **Acknowledge / Dismiss** action with optional note. Acknowledged accounts are hidden from the default view until a new recompute produces a more recent score; a `Show acknowledged accounts` toggle reveals them anyway.",
      "**Advisory only — no automated player-facing action.** A flagged account is not auto-banned, not blocked from inhouse, and not hidden from the leaderboard. The list exists to point operators at suspicious accounts; the decision is theirs.",
      "**Admin-editable threshold** (default 60, stored in `site_settings.smurf_threshold`, validated 0..100). Saving the threshold updates the persisted default; the page also accepts a transient `?threshold=` override for one-off triage.",
      "**Schema.** `smurf_scores (account_id BIGINT PRIMARY KEY, score SMALLINT, signals JSONB, computed_at TIMESTAMPTZ)` keeps the latest score per account; `smurf_acknowledgements (id, account_id, operator_account_id, operator_label, note, ack_at)` keeps every ack so a dismissed account can resurface if a later recompute produces a higher-confidence score. Indexes on `(score DESC)` and `(account_id, ack_at DESC)`.",
      "**Scorer** lives in `src/smurf/smurfScorer.js`. `scoreAccount(pool, accountId)` returns `{ score, signals }`. The perf-outlier signal compares first-10-game average PERF against the **peer-cohort median** in the same 250-MMR bracket (via `_buildBracketBaselines`, which `PERCENTILE_CONT(0.5)`s the bracket once per recompute pass), so an above-average score for a 500-MMR player is treated very differently from the same score for a 3000-MMR player. Unrated accounts and sparse brackets degrade the signal to `value: null, weight: 0` rather than guessing. `recomputeAll(pool)` walks every account with ≥ 5 non-legacy matches, builds the session-fingerprint index and the bracket-baselines map once per run, and upserts the result. Per-signal failures are caught locally so one bad row never halts the sweep. The fingerprint signal degrades gracefully (`value: null`, weight 0) when no `ip`/`ua` is present on the connect-pg-simple session payload, since production sessions don't currently stash either.",
      "**Cron + on-demand.** Nightly recompute on a 24h interval (first run 5 min post-boot to avoid stacking with other startup work), heartbeat reported via `db.recordCronHeartbeat('smurf_recompute', …)` so `/admin/ops` picks it up. `POST /api/admin/smurf-watch/recompute` kicks off an on-demand pass from the page; a module-scope in-flight flag prevents the on-demand path from racing the cron.",
      "**API.** `GET /admin/smurf-watch[?threshold=&include_acknowledged=1]` returns `{ threshold, effective_threshold, flagged[] }` with nicknames joined and per-account `last_ack_at` / `last_ack_note` / `last_ack_operator`. `POST /admin/smurf-watch/threshold` (validated 0..100), `POST /admin/smurf-watch/recompute`, `POST /admin/smurf-watch/:accountId/acknowledge` (note ≤ 500 chars, operator label captured from session), `GET /admin/smurf-watch/:accountId` for per-account history. All routes go through `requireSuperuser`.",
      "**A11y.** Acknowledge / Recompute / Save-threshold are real `<button type=\"button\">`s; the per-row Details toggle carries `aria-expanded` + `aria-label`. No new modal surface — the breakdown panel inlines under the row instead. The a11y gate stays green.",
      "**Out of scope per task brief.** No leaderboard hiding, no inhouse-lobby restriction, no Discord ping, no cross-platform fingerprint correlation, no ML model — strictly the five heuristic signals above plus an operator-only advisory surface.",
      "**Edition scope.** Full edition only — community edition has no admin panel surface for this and isn't wired."
    ]
  },
  {
    "version": "7.72",
    "title": "Notification preference centre v2 (Task #407)",
    "published_at": "2026-05-26",
    "notes": [
      "**New `/me/notifications` matrix page.** One row per catalogued notification event (12 in this cut: match_result, mvp_vote, hot_streak, vod_delivered, group_session_reminder, lobby_invite, achievement_unlocked, prize_pool_change, coach_booking_confirmed, coach_booking_reminder, league_scrim_accepted, season_rollover) with two independent toggles per row — **Discord DM** and **Web Push**. Each cell renders as a `role=\"switch\"` with `aria-checked` for the a11y gate. Existing `/settings/notifications` (presence chip, push-device setup, legacy per-category toggles) stays where it is and links across to the new matrix.",
      "**Schema.** New `user_notification_prefs (account_id BIGINT, event_key TEXT, channel TEXT, enabled BOOLEAN, updated_at TIMESTAMPTZ)` with composite PK `(account_id, event_key, channel)` — no SERIAL surrogate, idempotent on conflict. A missing row means \"use the per-event channel default\", which is how we preserve the pre-v2 behaviour for everyone who hasn't visited the page yet.",
      "**Resolution order in `db.isEventEnabled(accountId, eventKey, channel)`:** v2 row → legacy `notification_prefs.category` row (Discord channel only, per the `legacy:` field on each catalogued event) → per-event default. So a player who already opted out of `post_match_dm` in v1 still has `match_result/discord` muted in v2 without re-flipping anything.",
      "**Single send-site helper.** New `src/notify.js` module exports `notify(accountId, eventKey, { discord, push })` — every channel-bearing notification now flows through this one helper. `discord` can be a `{ embed }` / `{ content }` payload (helper fetches the user and sends) or a callback `({ user, bot }) => Promise` for sites that need to construct collectors or interaction patterns. `push` carries `{ title, body, url, data }` and fans out across web-push + Expo push. Lives in its own module to avoid the `bot.js ↔ server.js` require cycle; the Expo fan-out is injected via `setExpoFanOut()` at server startup.",
      "**One-tap unsubscribe.** Every web-push payload now embeds a signed `unsubscribeUrl` of the shape `${SITE_URL}/unsubscribe?t=<token>`. Tokens are HMAC-SHA256 over `{aid, ev, ch, ts}` with `SESSION_SECRET` as the key, base64url-encoded, scope-limited to exactly one (event, channel) tuple, and TTL'd to 30 days. The service worker (`web/public/sw.js`) surfaces an **Unsubscribe** action on every notification — tapping it fires the GET in the background and opens the confirmation page in a tab. The endpoint also works as a plain link (forwardable in chat / email).",
      "**Migrated call sites.** Two flavours: **(a) fully ported to `notify(...)`** — inhouse imminent-push (`/internal/inhouse/:id/imminent-push`) now sends via `notify(aid, 'lobby_invite', { push })` so the unsubscribe action is embedded in every web push; coaching booking confirmed DM and the hourly coaching reminder cron both call `notify(account_id, ev, { discord: { embed | content } })`, letting the helper own the pref check, Discord user fetch, and the eventual unsubscribe link on the push channel. **(b) gate-only migration** (legacy `user.send(...)` left in place, pref check swapped to `require('../notify').isEventEnabled(...)`) — `_sendReportCardDMs` → `match_result/discord`; rating session DM → `mvp_vote/discord`; attitude step → `mvp_vote/discord` AND legacy `attitude_vote` as a secondary gate so per-step opt-outs are preserved; tier-change announce → `hot_streak/discord`; schedule reminder cron → `group_session_reminder/discord`. Sites without a v2 event in scope (pro billing dunning, coaching review request) kept on legacy `isNotificationEnabled` unchanged.",
      "**Security: fail-closed token signing.** `SESSION_SECRET` is now required (>=16 chars) to sign or verify unsubscribe tokens — the previous fallback constant has been removed. Missing-secret environments produce `null` tokens that the `/unsubscribe` route rejects with 400 rather than forgeable links.",
      "**API.** `GET /api/me/notification-events` returns the full per-account matrix (every catalogued event, each channel resolved to `{ enabled, source: 'user'|'legacy'|'default', default }`) so the UI can render the grid even on first visit. `POST /api/me/notification-events` accepts `{ updates: [{ event_key, channel, enabled }, ...] }` — single-cell writes for snappy optimistic UI. Validation rejects unknown event_keys or channels with 400.",
      "**Defaults.** High-signal events default ON for both channels (lobby_invite, vod_delivered, group_session_reminder, coach_booking_*, league_scrim_accepted, hot_streak). Match-result defaults Discord-on / push-off (matches what existing users get today). Season-rollover defaults Discord-on / push-off (rare event, push would be overkill). Low-signal events default OFF for both (achievement_unlocked, prize_pool_change) — opt-in only.",
      "**Out of scope** per task brief: email channel, per-server scoping, quiet hours.",
      "**Edition scope.** Full edition only — community edition has the legacy `notification_prefs` stub but no inhouse/coaching/web-push surfaces to gate."
    ]
  },
  {
    "version": "7.71",
    "title": "Server-side ops dashboard (Task #406)",
    "published_at": "2026-05-26",
    "notes": [
      "**New `/admin/ops` page (superuser-only).** A single live view of every moving part — parser readiness + queue depth + last parse latency, Steam connection + last event, Discord bot connection + gateway latency, Stripe webhook last-received-per-type + processing lag, dedicated-server provisioner in-flight set + last failure, web-push readiness + subscription count + last delivery error, and a rolling 5xx count for the last 60 minutes. Each tile is colour-coded (green/amber/red) and the page auto-refreshes every 10 seconds. A 100-entry ring-buffered log pane sits underneath, filterable by source.",
      "**Telemetry collector** (`src/web/opsState.js`) — tiny in-memory module with bounded memory (100-entry log ring, 50-event-type Stripe map, 60-min timestamp window for 5xx). Process-local by design: a restart wipes it, which is the right tradeoff for a live operational view (not history). All reporters are `try { require } catch (_) {}`-wrapped at the callsites so a missing module can never crash a subsystem.",
      "**Subsystems wired** — `replayParser.parseReplayFull` reports per-parse duration + last error; `steamClient` reports loggedOn / error / disconnected; `discord/bot.js` reports gateway connection on `ready` and re-samples `ws.ping` every 30s; `inhouse/serverProvisioner.js` reports in-flight session ids + last success / failure sessionId + reason; the Stripe webhook handler reports event-type + lag the moment `constructEvent` returns; `_sendExpoPush` reports delivery success / batch error. A response-finish middleware mounted before the API router increments the 5xx counter and drops a log line per 5xx response.",
      "**API.** `GET /api/admin/ops/state` returns a single snapshot (superuser-only, `Cache-Control: no-store`) with derived live status refreshed at request time (parser ready, Discord ws status + ping, Steam isLoggedIn) and `web_push_subscriptions` count via cheap `COUNT(*)`. Companion `GET /api/admin/ops/logs?source=…` returns just the (optionally filtered) ring buffer for callers that want higher-cadence log polling.",
      "**Out of scope** (per task brief). No APM / distributed tracing (that's the OpenTelemetry task), no alerting / paging integrations, no historical charts beyond the last 60 minutes — those would each be tasks of their own.",
      "**Edition scope.** Full edition only — the community edition has a separate server file and doesn't carry the Stripe / push / inhouse-provisioner surfaces this dashboard exists to monitor."
    ]
  },
  {
    "version": "7.70",
    "title": "Stripe earnings reconciliation (Task #405)",
    "published_at": "2026-05-26",
    "notes": [
      "**Real Stripe fees on `/coach/earnings`.** The dashboard previously showed an *estimated* Stripe fee (1.75% + 30c AU domestic-card). It now reads the actual `fee` + `net` off each charge's `BalanceTransaction` and labels reconciled rows with a green ✓ **Reconciled** pill. Unreconciled rows fall back to the estimate so partially-reconciled months still tally cleanly — no row disappears just because the BalanceTransaction hasn't been ingested yet.",
      "**New `stripe_fee_ledger` table** — one row per Stripe charge (`payment_intent`, `charge_id` UNIQUE, `balance_tx_id`, `gross_cents`, `fee_cents`, `net_cents`, `currency`, `source_kind`, `source_id`, `captured_at`). Idempotent on `charge_id` via `ON CONFLICT DO UPDATE`, indexed on `(source_kind, source_id)` so the earnings LEFT JOIN is cheap, and on `payment_intent` for late-reconciliation lookups.",
      "**Ingestor** — `db.upsertStripeFeeFromCharge(stripe, { paymentIntent | chargeId, sourceKind, sourceId })` retrieves the charge with `expand:['balance_transaction']` (or pulls it via `paymentIntents.retrieve` → `latest_charge.balance_transaction`) and upserts the row. Wired into the Stripe webhook switch on a new **`charge.succeeded`** branch (resolves `source_kind`/`source_id` from the PaymentIntent across `coaching_bookings` / `coach_group_session_seats` / `coach_vod_reviews`) AND fired best-effort from each of the three coaching `checkout.session.completed` success paths so the ledger is usually populated within seconds of payment. Every ingestor call is wrapped in `.catch(() => {})` — webhook fulfilment must never fail because Stripe rate-limited a BalanceTransaction fetch.",
      "**Earnings query rewrite.** `db.getCoachEarningsMonth()` now LEFT JOINs `stripe_fee_ledger` per row, prefers `sfl.fee_cents` when present, and tags each row `reconciled: true|false`. Totals expose `unreconciled_rows` + `fully_reconciled` so the dashboard can switch between the all-clean copy and the partial-reconciliation copy.",
      "**CSV export.** `GET /api/me/coach/earnings.csv` gained a final `reconciled` column (`true`/`false` per row, plus a TOTAL row tagged with the month's `fully_reconciled` boolean) so the accountant hand-off can tell at a glance which line items are Stripe-grade and which are still estimated.",
      "**Out of scope.** Stripe Connect transfer reconciliation lives in a separate ledger (not touched here). Refund-fee reconciliation goes only as far as what the BalanceTransaction already returns. Historical backfill of pre-feature months can be done later with a small one-shot script; not shipped in this cut.",
      "**Edition scope.** Full edition only — community edition is paywall-free, has no coach earnings dashboard, and isn't wired."
    ]
  },
  {
    "version": "7.69",
    "title": "Coaching v2 — Group sessions, async VOD review, coach earnings (Task #384)",
    "published_at": "2026-05-25",
    "notes": [
      "**Group coaching sessions.** Coaches can now schedule one-host / many-student sessions (2–8 seats). New tables `coach_group_sessions` + `coach_group_session_seats` with a partial unique index that prevents one student from holding two live seats on the same session. New `/group-sessions` page lists open upcoming sessions; students click **Join** and pay one seat via Stripe Checkout. Same destination-charge / manual-capture (escrow) pattern as 1:1 bookings — funds sit on Stripe until the coach marks the session complete, at which point all paid seats capture in a single sweep. Cancelling a session refunds every paid seat by cancelling the uncaptured PaymentIntent (no Stripe fees, no money movement). Coaches manage their schedule from the new `/coach/group-sessions` page (create / complete / cancel with confirm dialogs).",
      "**Async VOD review.** New tables `coach_vod_reviews` + `coach_vod_review_notes`. Student requests a review on `/coaches/:id/vod-review` — provides **either** a recorded match ID (opens in the 2D replay viewer with annotations) **or** a hosted replay URL (`.dem` link, Dotabuff/Stratz), writes a question (≥10 chars), sets an offer bounded by `$10 ≤ price ≤ coach.hourly_rate`. Funds escrow until the coach delivers. Annotation happens inline on the existing 2D replay viewer (`/replay/:matchId?vodReview=ID`): coach adds a note at the current scrub time, notes appear as clickable amber markers on a timeline strip + a side panel, and the student can jump to any moment. First note auto-flips `paid` → `in_progress`. Coach hits **Deliver** to capture the PaymentIntent; either party can **Refund** before delivery to cancel the PI cleanly. Standalone `/vod-reviews/:id` page remains as a metadata + actions surface. Orphan-row cleanup if Stripe Checkout creation fails between row insert and session attach.",
      "**Coach monthly earnings dashboard.** New `/coach/earnings` page shows gross / platform-take / Stripe-fees (estimated at AU domestic-card pricing — 1.75% + 30c) / net for any of the last 12 months, rolled up across all three revenue streams (1:1 bookings, group session seats, VOD reviews). One-click CSV export at `GET /api/me/coach/earnings.csv?ym=YYYY-MM` for accountant hand-off. Aggregation lives in `db.getCoachEarningsMonth()` so any future stream (e.g. tournament coaching, sponsorship payouts) plugs in by appending to one helper.",
      "**Stripe webhook switch.** Three new purposes wired (`coaching_group_seat`, `coaching_vod_review`) into every relevant event: `checkout.session.completed`, `checkout.session.async_payment_succeeded`/`failed`, `checkout.session.expired`, `charge.refunded`, `payment_intent.succeeded` (safety-net capture for group seats), `payment_intent.canceled`. Group seats expose a `seats_taken >= capacity` auto-flip to `status='full'` on the success path so the listing UI doesn't keep showing a full session as joinable.",
      "**A11y.** All new clickable shapes use `<button type=\"button\">` with `aria-label`s on icon-only / context-only buttons. Month selectors carry explicit labels; CSV export is a regular `<a download>`. The a11y gate stays green (no new dialog usage — request forms are full-page surfaces, not modals).",
      "**Edition scope.** Full edition only — community edition routes / paywall gate untouched. Webhook + DB helpers added to `src/db/index.js` + `src/web/server.js`; the community edition's separate copies are deliberately not patched.",
      "**Round 4 fixes (post-review):** when a coach hits **Deliver** the student now gets a Discord DM (via `bot.sendDmToAccount`) plus a best-effort web-push notification — both are wrapped so a notification failure can never unwind the Stripe capture / status flip. Students can now upload a raw `.dem` replay on the review detail page (`POST /api/vod-reviews/:id/upload-replay`, multer disk storage, 300 MB cap, `.dem` extension/octet-stream filter, student-only, status-gated to pending/paid/in_progress). Files land in `UPLOAD_DIR` with randomised names; the on-disk path is stored as `file://…` in `coach_vod_reviews.replay_url` and the GET endpoint masks that to an auth-gated `/api/vod-reviews/:id/replay-download` URL (party-or-superuser only, path-traversal guarded against `UPLOAD_DIR`). New `db.setVodReplayUrl` helper + `uploadVodReplay` API client + upload widget on `VodReviewDetail`."
    ]
  },
  {
    "version": "7.68",
    "title": "Team v2 — Roster history, scrim scheduler, leagues + bracket (Task #383)",
    "published_at": "2026-05-25",
    "notes": [
      "**Team profile page rebuilt as a tabbed interface** (`Roster` / `Schedule` / `Recent matches` / `Manage`). The tab strip is a proper `role=\"tablist\"` with per-tab `aria-selected` + `aria-controls` and matching `role=\"tabpanel\"` regions, so screen readers and keyboard users move through the team surface the same way they do on `/heroes`. The old single-page layout is gone — every existing surface (record, roster, invites, upkeep, leave) is still reachable, just under the right tab.",
      "**Roster history is now tracked permanently.** A new `team_roster_history` table records every join / role-change / leave with a `[joined_at, left_at)` range, populated by an `AFTER INSERT/UPDATE/DELETE` trigger on `team_members` so the history can never drift from the live roster. The Roster tab shows current members at the top and a Historical members table underneath with joined / left dates per stint. Existing teams are backfilled on boot (idempotent — only inserts missing open intervals).",
      "**Captain-to-captain scrim scheduler.** New `team_scrims` table + four endpoints (`POST /api/teams/:id/scrims`, `POST /api/scrims/:id/respond`, `POST /api/scrims/:id/cancel`, `GET /api/teams/:id/schedule`). A captain or owner of team A proposes a scrim against team B at a specific datetime; the opposing captain accepts or declines, and either captain can cancel a pending request. The Schedule tab on each team profile renders upcoming + recent scrims and surfaces Accept/Decline buttons inline when the viewer is the opposing captain.",
      "**Roster transfers now require two-captain confirm.** New `team_roster_transfers` table tracks `from_approved` / `to_approved` flags; only when **both** captains have approved does the system atomically move the `team_members` row (the trigger above keeps `team_roster_history` in sync, closing the old interval and opening a new one). Free-agent pulls auto-approve on the source side. Captains see incoming/outgoing transfer requests for their team inside the Manage tab with Approve / Decline buttons.",
      "**Captain edit form** in the Manage tab — bio, primary colour, and logo URL — all routed through the existing `POST /api/teams/:id/edit` which already enforces the owner-or-captain check at the DB layer.",
      "**Recent team matches** tab — derived from the same `≥3 members on one side` definition `getTeamStats()` has always used. Each row links to the underlying `/matches/:id` page.",
      "**New `/leagues` + `/leagues/:id` pages.** Operator-created (superuser-gated) tournaments with three format options: round-robin, single-elimination, double-elimination. Schema: `leagues` (name, slug, format, status, description, starts_at), `league_teams` (league_id + team_id + optional seed, PK on the pair), `league_matches` (bracket W/L/RR, round, slot, team_a / team_b, winner, optional `match_id` reference). Leagues page lists all leagues with team count + status; League profile page shows the standings table (wins / losses computed live from `league_matches.winner_team_id`) and a horizontally-scrolling bracket view grouped by round.",
      "**Single-elim bracket auto-generation** — `POST /api/leagues/:id/generate-bracket` takes the current `league_teams` ordered by seed, rounds up to the next power of two with BYE slots, applies the standard 1v8 / 4v5 / 2v7 / 3v6 seed pairing (recursive seed-list generator), and inserts a fresh round-1 set of slots (deletes any previous unresolved W-bracket rows first so it's safe to re-run before the league starts). Marking a winner in a round-1 slot via `POST /api/league-matches/:id/winner` **auto-promotes** the winner into the next round's slot (`slot >> 1`, A-side for even slots / B-side for odd) — the bracket cell either updates the existing next-round row or inserts a new one. Round-robin and double-elim leagues store the format string but defer auto-generation; operators record winners manually for those.",
      "**Matches table gained an optional `league_id`.** `ALTER TABLE matches ADD COLUMN IF NOT EXISTS league_id INTEGER REFERENCES leagues(id) ON DELETE SET NULL` plus a partial index. A new `POST /api/matches/:matchId/attach-league` (superuser) tags an existing recorded match against a league so the standings + the bracket cell's `view match →` link can hop straight into the match detail page. The lobby team-builder code path is untouched — `team_id`-aware logic continues to operate without seeing the new column.",
      "**Frontend wiring + a11y.** New API client functions in `web/src/api.js` (`getTeamRosterHistory`, `getTeamRecentMatches`, `getTeamSchedule`, `proposeScrim`, `respondScrim`, `cancelScrim`, `proposeRosterTransfer`, `respondRosterTransfer`, `getMyRosterTransfers`, `listLeagues`, `getLeague`, `createLeague`, `addLeagueTeam`, `removeLeagueTeam`, `generateLeagueBracket`, `setLeagueMatchWinner`). Tools dropdown gains a `Leagues` link. Every clickable shape is a real `<button type=\"button\">` with explicit Accept/Decline/Approve/Win text, satisfying the icon-only-button + sortable-th + dialog house rules; the a11y gate stays green.",
      "**Edition scope.** Full edition only — community edition is deliberately not wired (out of scope for this task and the source-scan paywall gate stays green). No prize-pool wiring, no Discord commands, no Stripe league fees in this cut.",
      "**Round 2 fixes (post-review):** captain edit form now also accepts the team **name** (3–60 chars, case-insensitive uniqueness check at the DB layer). Team profile header gained **current win/loss streak**, **average roster MMR** (V3 conservative), **hero pool top-5** (games + win-rate), and **signature drafts top-3** (linked to the underlying match). Match detail page got a superuser **League match** dropdown that calls `POST /api/matches/:matchId/attach-league` to tag a recorded match against a league — bracket cells light up the moment a winner is set. Broken match-detail links on the team and league pages corrected to the canonical `/match/:matchId` route.",
      "**Round 3 fixes (post-review):** lobby-creation now accepts an optional **`leagueId`** (`inhouse_sessions.league_id` column added, `POST /api/inhouse` reads it, `/inhouse/:id/complete` propagates it onto `matches.league_id` automatically — the captain/operator tag is recorded once at lobby creation and never has to be re-entered). The Match Detail **League match** control now also exposes a **Bracket slot** + **Winner** picker so the operator can attach the match AND advance the bracket in a single round-trip; the backend's `attach-league` route accepts `league_match_id` + `winner_team_id` and pipes them through `setLeagueMatchWinner` (auto-promotes the winner into the next round, respects the final-round guard). `/teams/:id/edit` error mapping fixed — name-validation now returns `400`, name-conflict returns `409` (was returning `500` for both)."
    ]
  },
  {
    "version": "7.67",
    "title": "Hero meta v2 — Synergy matrix, Counter-pick scorer, Patch trends (Task #382)",
    "published_at": "2026-05-25",
    "notes": [
      "**`/heroes` now ships three new tabs** alongside the existing Hero Stats / Tier List / Matchups / Pro tabs. Tab nav is now a proper `role=\"tablist\"` with per-tab `aria-selected` + `aria-controls` and matching `role=\"tabpanel\"` regions so screen readers and keyboard users can navigate the page correctly.",
      "**🤝 Synergy tab** — pair and trio heat-map of which heroes win most often when picked together, click any row to drill straight into the underlying matches (`/matches?heroes=a,b[,c]`). Sortable by games / wins / win-rate via the shared `SortableTh` primitive. Pairs are unordered (deduped via `LEAST/GREATEST`), trios are unordered (sorted ASC), HAVING ≥2 shared games to keep the matrix tractable for inhouse-scale data.",
      "**🎯 Counter-pick tab** — pick up to 5 enemy heroes + an optional position, get a ranked list of the strongest heroes to pick into that lineup. Scoring blends counter win-rate (heavier) with the candidate's base win-rate, with a sample-size shrinkage factor (`games / (games + 4)`) so a 1-game 100% pick doesn't crown the list. Backed by a new shared `_computeHeroCounterMap()` helper that both the new endpoint **and** the existing Draft Assistant call into, so the two surfaces can never disagree.",
      "**📈 Patch Trends tab** — pick a hero, see pick rate + win rate per patch for up to the last 8 patches with data, oldest→newest. Bars are pick-rate-relative so movement is easy to read at a glance. Empty-state nudges admins to run the new backfill if older uploads pre-date the `matches.patch` column.",
      "**New patch picker at the top of `/heroes`** (visible on the Tier / Synergy / Counter-pick tabs). **Defaults to the current (latest) patch** so the page is patch-relevant out of the box; 'All patches' is still a one-click option for the old all-time view. The selected patch is passed through to `/heroes/tier-list?patch=`, `/heroes/synergy-matrix?patch=`, and `/heroes/counter-scores?patch=` so every cell on the page is scoped to the same patch window. A new `GET /api/heroes/patches` powers the dropdown.",
      "**Draft Assistant now consumes the same counter-pick scorer endpoint.** When enemies are present, `DraftAssistant.jsx` fires a parallel `GET /heroes/counter-scores` alongside its existing `/draft-assistant` call and overlays the returned `counter_wr` onto the 'vs Enemies' column. The two surfaces are now wired to a single counter source of truth — both at the route layer (`/heroes/counter-scores`) and the DB layer (shared `_computeHeroCounterMap()`).",
      "**Tier List now shows patch-vs-previous movement arrows.** Each tier card gets a ▲/▼ delta next to the win rate when the hero has ≥2 games on the comparison patch. With a patch selected the comparison is **current-patch WR vs the immediately-preceding patch's WR** (apples-to-apples, both patch-scoped via the new `getHeroPatchWinRates()` + extended `getHeroPrevPatchWinRates(seasonId, currentPatch)`); with 'All patches' the comparison falls back to all-time WR vs the second-newest patch. Hidden when there's only one patch of data, or when the delta is <1pp (noise floor).",
      "**Four new backend surfaces in `src/db/index.js`** — `getHeroSynergyMatrix()`, `getHeroCounterScores()`, `getHeroPatchTrends()`, `getHeroPrevPatchWinRates()` — exposed via three public read-only API routes (`GET /api/heroes/synergy-matrix`, `GET /api/heroes/counter-scores`, `GET /api/heroes/:id/patch-trends`) and one superuser-only `POST /api/admin/heroes/backfill-patch` that retro-populates `matches.patch` for older uploads using the new newest-first `src/data/dotaPatchReleases.js` release-date table.",
      "**`getDraftSuggestions()` refactored to share the counter map.** The old inline counter-bonus query block (which used `EXISTS` against any enemy hero) is replaced by a call to the new `_computeHeroCounterMap()`, which uses an intersected count so picks are scored against the full enemy lineup the way captains actually draft. `DraftAssistant.jsx` UI is unchanged — only the underlying numbers improve.",
      "**Edition scope + paywall.** Full edition only — community edition is deliberately not wired (out of scope for this task and the source-scan paywall gate stays green). The three new tabs are free; the existing `★ Position Meta` and `★ Hero Breakdown` tabs remain Pro-gated, gold-tinted, and behind `PaywallBlur` exactly as before."
    ]
  },
  {
    "version": "7.66",
    "title": "Mobile companion app (Expo, read-only first cut) (Task #381)",
    "published_at": "2026-05-25",
    "notes": [
      "**New top-level `mobile/` Expo Router app** \u2014 a read-only iOS/Android companion for oceinhouse.gg. Five screens: Home (live stats + nav), Leaderboard (top 100 MMR), Recent Matches (last 50 games), Match Detail (per-team K/D/A breakdown), and Settings (notification prefs + sign-in). Full edition only \u2014 community edition is deliberately not wired.",
      "**Steam OpenID sign-in via in-app browser.** Reuses the existing `/auth/steam` \u2192 `/auth/steam/return` \u2192 `/api/auth/complete` token-exchange that the website already runs. The mobile app launches `expo-web-browser` against `/auth/steam?mobile_redirect=oceinhouse://` and the server now honours that param to bounce back into the app's URL scheme with the short-lived token attached. Scheme is strictly allow-listed to the published `oceinhouse://` scheme only (no `exp:`, no http(s)) at both write *and* consume time, so the single-use Steam auth token can never be redirected to an attacker-controlled Expo experience or arbitrary URL. Session cookie is held encrypted-at-rest in `expo-secure-store` and re-attached as `Cookie:` on every request.",
      "**Expo push notifications** \u2014 new `expo_push_tokens` table (one row per device, unique on token, mirrors `web_push_subscriptions`) + three new endpoints (`POST /api/me/expo-push/register`, `POST /api/me/expo-push/unregister`, `POST /api/me/expo-push/test`). The existing imminent-push dispatcher (`/internal/inhouse/:id/imminent-push`) now fans out to Expo tokens in parallel with VAPID web-push, sharing the same per-account `match_imminent_push` preference gate so toggling a category in either surface is honoured everywhere. Dead Expo tokens (`DeviceNotRegistered` / `InvalidCredentials`) are pruned automatically when the push service rejects them.",
      "**Settings parity with web push prefs.** The mobile Settings screen reads/writes the same `notification_prefs` rows as `/settings/notifications` on the website \u2014 toggles are shared, not duplicated. Includes a 'Send test push to this device' button that registers an Expo token on demand if the user previously denied permission.",
      "**Out of scope (intentionally).** No write/mutation flows (no queue join, captain draft, accept phase, hero signup, bookings, payments), no inhouse lobby UI \u2014 the website stays the source of truth. No app-store submission \u2014 first cut is Expo Go + EAS internal dev builds only. No community-edition wiring. Documented in `mobile/README.md`.",
      "**Pre-deploy gates left untouched.** The a11y gate, community-paywall gate, and parser-freshness gate all scope to `web/src/`, `community-edition/`, and `odota-parser/` respectively \u2014 `mobile/` is exempt by design, so no gate changes were needed."
    ]
  },
  {
    "version": "7.65",
    "title": "Twitch extension companion (Task #380)",
    "published_at": "2026-05-25",
    "notes": [
      "**New `twitch-extension/` package** \u2014 a Twitch panel + video-overlay companion that surfaces an OCE Inhouse player's rank, current W/L streak, and last 5 matches to viewers without leaving Twitch. Three surfaces (`config/`, `panel/`, `video_overlay/`), all vanilla JS (no React, no bundler) so the uploaded zip stays well under Twitch's 10 MB ceiling. Panel rows expand to KDA/GPM/XPM on hover, focus, click, or Enter/Space \u2014 keyboard-reachable per the frontend a11y rules.",
      "**Read-only, public endpoints only.** The extension hits `/api/overlay/ticker/<aid>` (rank/streak/W-L), a new `/api/players/<aid>/recent-matches?limit=5` (last 5 matches with KDA/GPM/XPM and result), and `/api/overlay/live/current?for=<aid>` (presence dot). No auth, no secrets in the bundle, polls every 30 s per Twitch's rate-limit policy. Recent-matches is capped at limit=10 to prevent bulk scraping.",
      "**CORS allowlist extended for `*.ext-twitch.tv`.** The strict allowlist still rejects unknown origins, but the Twitch extension iframe hostname suffix is now accepted so the extension can `fetch()` the JSON directly. Safe because these endpoints don't accept session cookies \u2014 the original credential-CSRF gap stays closed.",
      "**Local smoke-test harness at `twitch-extension/test/index.html`** stubs `window.Twitch.ext` and hosts all three surface iframes side-by-side, with a base-URL input + Refresh button so the extension can be developed against the local dev server without ever packaging it.",
      "**Aggregated read alias `GET /api/me/preferences`** \u2014 single endpoint returning both stream-privacy prefs and notification categories so other settings consumers have one place to fetch them. PUTs still go to the specialised endpoints.",
      "**`/settings/profile` Twitch extension tile** \u2014 copyable account id + config-page URL, the two values the streamer needs to fill in once after installing the extension on their channel.",
      "**Edition scope.** Full edition only \u2014 community edition has no overlay endpoints, no Twitch extension, and no matching settings tile."
    ]
  },
  {
    "version": "7.64",
    "title": "Streamer mode + OBS overlays (Task #379)",
    "published_at": "2026-05-25",
    "notes": [
      "**Three new public overlay routes built for OBS browser sources.** `/overlay/live/:lobbyId` renders the current inhouse lobby's Radiant/Dire rosters as a top-left card; `/overlay/scoreboard/:matchId` renders a full K/D/A · LH/DN · GPM/XPM · Net Worth table for both teams as a bottom card; `/overlay/ticker/:accountId` renders a single-player MMR / W-L / win-rate strip. All three are unauthenticated (OBS can load them directly), set `Cache-Control: no-store`, and refresh on a 5-30s poll so streams stay live.",
      "**Chrome stripper for any other page on the site.** Append `?streamer=1` to any URL and the navbar, footer, side banners, login bars, and onboarding modals all disappear, and `body` flips to `background: transparent` so OBS can composite the page over gameplay without window-coloured fringe. The new `AppShell` + `useStreamerMode()` hook in `App.jsx` does the conditional render; the overlay routes get the chrome stripper for free since their path starts with `/overlay/`. Overlay routes additionally render inside a fixed 1920×1080 `.overlay-main` viewport so OBS canvases line up.",
      "**Stream privacy prefs persisted on `player_profiles`.** Three new columns (`stream_hide_mmr`, `stream_hide_region`, `stream_alias`) wired through a new `GET/PUT /api/me/stream-prefs`. Every overlay endpoint accepts `?for=<accountId>` and applies that account's privacy server-side — when the streamer ticks 'Hide my MMR & tier' their MMR is stripped from every overlay (not just the ticker), and `stream_alias` (max 32 chars) replaces their Steam name. The owner check is strict per-row: only the streamer's own row gets re-aliased / MMR-hidden in the live + scoreboard overlays so opponent stats stay readable.",
      "**Settings UI in `/settings/profile`.** A new 'Streamer setup' section at the bottom of the profile editor exposes the three privacy toggles + a 32-char alias input, copy-URL buttons for all three overlay URLs (pre-filled with the signed-in player's account id), and a live preview iframe with Ticker / Live lobby / Off tabs. Copy buttons carry an `aria-label` per the icon-only-button rule; the iframe carries a `title`; the preview tab buttons use `aria-pressed`. A11y gate green.",
      "**Edition scope.** Full edition only — community edition has no streamer mode (no overlay routes, no settings section, no DB columns added on its side)."
    ]
  },
  {
    "version": "7.63",
    "title": "Pro Replay Browser — cached OpenDota pro matches with deep-link to Draft Assistant (Task #378)",
    "published_at": "2026-05-25",
    "notes": [
      "**New page `/pro-replays`** (Tools → Pro Replay Browser). Filter recent pro/league matches by league, hero, position (lane role 1–5), patch, team name, or free-text search across any field. Each row shows the date, league, both team names, full radiant + dire picks (hero portraits), match length, and the winning side. Sort by date, duration, or **league prestige** (premium → professional → amateur, OpenDota's league_tier vocabulary).",
      "**'Analyze in Draft' button on every row** deep-links into `/draft-assistant?radiant=h1,h2,...&dire=h3,h4,...`. DraftAssistant now parses those query params on mount and pre-populates allies (radiant by default) + enemies (dire) so the suggestion engine fires immediately against the real pro draft. Pass `?side=dire` to flip which side you're analyzing from.",
      "**Backed by a new `pro_matches` table** synced every ~6h from OpenDota's `/proMatches` feed (`src/api/proMatchSyncer.js`), with a per-match details drain that pulls picks/bans/players via `/matches/:id` at the existing 1 req/sec rate-limit. Picks are stored as JSONB and queried via GIN-indexed `@>` containment so the hero filter stays fast. Position filtering uses the `players` JSONB (with `lane_role` per pick) so it composes correctly with hero filters. The frontend reads our cache, never the upstream API.",
      "**Public by default**, gated by the new `pro_replay_browser` feature flag (seeded `state='on'`). State `preview` restricts the surface to superusers (dogfood mode); state `off` returns 404 on every route so the surface is fully invisible and the sync job is also skipped, sparing the OpenDota quota.",
      "**Replay deep-link** — rows with `has_replay=true` (i.e. OpenDota has a `replay_url` for the match) get a 'Replay' link straight into the existing `/replay/:matchId` viewer.",
      "**Edition scope.** Full edition only — community edition has no Tools dropdown surface and no syncer wiring."
    ]
  },
  {
    "version": "7.62",
    "title": "Parser new ideas: first-blood chain, comeback factor, throne DPM, item benchmarks (Task #377)",
    "published_at": "2026-05-25",
    "notes": [
      "**`<FirstBloodChainPanel>` on every match page.** Shows the first 4 kills of the match (FB + next 3) with killer/victim team-coloured, alongside a snowball-score pill — *what % of kills in the 5 minutes after FB were scored by the team that drew FB*. ≥70% renders green (Snowballed), 45–69% yellow (Traded out), <45% red (Got punished). Both fields are computed once inside `replayParser._aggregateStats` from the already-sorted `gameEvents` kill list — no extra parser work, no DB join. New `matches.first_blood_chain JSONB` + `matches.snowball_score SMALLINT` columns persist them; legacy matches stay NULL and the panel auto-hides.",
      "**`<ThroneDpmPanel>` — per-team damage to the Ancient.** New parser counters (`throneDamage` + `throneDamageWindow`) split building damage by `victimName.includes('fort')` and bucket by attacker team, alongside the existing per-slot `tower_damage`. The panel surfaces DPM (damage / match minutes), total damage, and the throne-attack window (first → last throne hit) per side. Long, clutch defences show low DPM over a long window; clean closes show high DPM in a short burst. Stored on `matches.throne_dpm JSONB`.",
      "**Comeback factor: 0–100 score on the existing Comeback Metric panel + the Records → Greatest Comebacks tab.** Walks per-slot `timelineSamples` once to find the *eventual winner's* max gold deficit, then maps linearly (20,000g = 100). Stored on `matches.comeback_factor SMALLINT`. `<ComebackMetricPanel>` now shows a colour-graded pill above the NW-rank table (≥70 red 'Massive comeback', 40–69 yellow 'Real comeback', 15–39 blue 'Notable'). Records tab re-sorts by factor (then deficit as tiebreaker) and shows the per-row factor pill — replaces the old size-only sort.",
      "**`<ItemBenchmarkPanel>` — first-purchase timeline for major items.** Per-player parser now emits `itemFirstTimes` ({ itemName: seconds }) for the ~60-item major-cost set (Blink, BKB, Aghs/Shard, Radiance, Octarine, Refresher, the carry ladder, key support items, every boots tier, etc.). Persisted on the new `player_stats.item_first_times JSONB` column and rendered as a compact MM:SS table sorted by earliest-purchase column. Hidden when no player carries the data.",
      "**`getComebackMatches` prefers `comeback_factor` when present, falls back to the legacy deficit/200 derivation** for un-reparsed matches — keeps the Records tab populated during the backfill window without forcing a re-parse first.",
      "**Backfill script for legacy matches: `scripts/backfill-derived-metrics.js`.** Walks every match with `game_timeline` and back-fills `first_blood_chain`, `snowball_score`, `comeback_factor` *without* needing a replay re-parse — all three are derivable from the timeline blob the parser already wrote. `throne_dpm` and `item_first_times` still need a replay re-parse (combat-log data isn't in the timeline blob), and the script logs how many matches are eligible for re-parse so we can target them via the existing 'Re-aggregate from stored replay' superuser button. Usage: `node scripts/backfill-derived-metrics.js [--limit=N] [--dry-run]`.",
      "**Edition scope.** Full edition only — community edition is deprecated for new work."
    ]
  },
  {
    "version": "7.61",
    "title": "Parser big-M bundle: runes, couriers, lane outcomes, ward deaths (Task #376)",
    "published_at": "2026-05-25",
    "notes": [
      "**Three new match-page panels surfacing previously-ignored parser events.** `<RuneControlPanel>` shows per-team bounty/power/wisdom/shield/etc pickups with a per-pickup timeline. `<CourierKillsPanel>` shows who killed whose courier, when, and the gold swing. `<LaneOutcomesPanel>` shows the net worth / XP / kill differential at 10 minutes per lane with a canonical winner badge (Radiant / Dire / Even). All three auto-hide when the underlying data isn't present.",
      "**Ward-death overlay on `<WardMapPanel>`.** Toggle (✕ Deaths on/off, `role=switch` + `aria-checked` for the a11y gate) draws an X over every dewarded position, coloured by ward-owner team. Natural 6-min expiries are excluded — only kills the parser attributed to an attacker are shown. Respects the existing observer/sentry and per-player chip filters.",
      "**Parser pipeline now captures `runes_log` events.** The Java parser (`CreateParsedDataBlob.handleRunePickup`) was already emitting `{type:'runes_log', time, slot, key=runeTypeId}` for every `CHAT_MESSAGE_RUNE_PICKUP`, but `replayParser._aggregateStats` only read the integer `rune_pickups` count and dropped the per-event detail. Now collected into a sorted `timeline.runePickups` list with `{t, slot, team, runeType}`.",
      "**Lane-outcome summary pre-computed in the aggregator.** New `_computeLaneOutcomesSummary(players, laningNwAt10, laningXpAt10, laningKillsAt10, detectedPositions)` groups by lane (bottom/mid/top), sums per-team NW/XP/kills, and tags a winner using a ±500g threshold for 'even'. Shipped on `timeline.laneOutcomesSummary` so the UI doesn't have to re-derive it from per-player scoreboard columns. Independent of the existing `match.lane_outcomes` Stratz-formula score (which the team tables still consume).",
      "**Ward deaths and courier-kill events ride the timeline blob too.** `timeline.wardDeaths` (already built locally for the support report) is now persisted alongside `events`/`players`. Courier kills were already in `timeline.events` (`type:'courier'`) via the `npcNameToSlot` map — the new panel just reads them. No new DB columns, no migration — everything reuses the existing `matches.game_timeline` JSONB column.",
      "**Superuser: 'Re-aggregate from stored replay' button** on `/match/:id` (only when `match.has_replay` is true). Calls the existing `POST /api/admin/reparse-replay/:matchId` endpoint (`requireSuperuser`, runs `replayParser.parseReplayFull` on the cached .dem and writes the new `matchStats` blob via `db.reparseMatchFromStats`). Lets us backfill the new panels onto historical matches one at a time without a bulk migration.",
      "**Edition scope.** Full edition only — community edition is deprecated for new work."
    ]
  },
  {
    "version": "7.60",
    "title": "Engagement bundle: Rivals leaderboard + /this-week AI recap page (Task #364 follow-up)",
    "published_at": "2026-05-25",
    "notes": [
      "**Rivals leaderboard on every profile.** Existing head-to-head was a Pro-gated *pair picker* (`/head-to-head`, pick two players manually). The new `Rivals` panel on `/player/:accountId` shows every opponent this player has faced ranked by total games, with W–L records both against (`games_against`/`wins_against`) and alongside (`games_with`/`wins_with`), plus winrate against. Public — no Pro, no auth — mirroring `/players/:accountId/nemesis` in scope.",
      "**New `db.getPlayerRivals(accountId, seasonId, minTotal=2)` and `/api/players/:accountId/rivals`.** Single self-join on `player_stats` with `COUNT FILTER` for the four buckets, `LEFT JOIN LATERAL` for nickname resolution, season-gated on `m.is_legacy = false` when no season is passed (same convention as `getHeadToHead`). Filters out one-off opponents (default `min_total=2`) and caps results at 50.",
      "**New `/this-week` page.** AI-generated weekly recap surfaced on the site. Reads from the existing `/api/latest-recap` endpoint which serves the cached row written by the Monday Discord cron (`generateWeeklyRecapBlurb` + `db.saveWeeklyRecap` in `src/discord/bot.js`) — no new generation path, no new cron, no admin button. Renders blurb + top performers (linked to profiles) + fun highlights. Linked from the top nav next to **Matches**.",
      "**Why no new weekly cron.** The Discord weekly recap already runs every Monday, generates the AI blurb via Groq, and persists `weekly_recaps` rows. The site was the only surface that hadn't been wired up — fixed.",
      "**Edition scope.** Full edition only — community edition is deprecated for new work."
    ]
  },
  {
    "version": "7.59",
    "title": "Parser quick-wins: buyback timeline panel (Task #364 follow-up)",
    "published_at": "2026-05-25",
    "notes": [
      "**Buyback events now surfaced on every match page.** The Java parser was already emitting `buyback_log` (blob mode) and `DOTA_COMBATLOG_BUYBACK` (streaming mode) entries, and `replayParser._aggregateStats` was already capturing them into a per-slot `buybackTimes` map — but the timestamps were only used internally for dieback detection and never reached the UI. They're now included in the timeline player record alongside `smokeTimes`/`abilityLog`, so they ride the existing `matches.game_timeline` JSONB column with no new migration.",
      "**New `<BuybackPanel>` on `/match/:id`.** Renders directly below the smoke-usage panel. Each row shows the player (team-coloured), total buybacks, dieback count (red when > 0), and an inline event log: timestamp + gold cost + a red ⚠ marker for each buyback that ended in a dieback. Auto-hidden when no player bought back in the match.",
      "**Per-buyback dieback flag is parser-canonical.** The dieback flag (`{t, dieback}`) is computed once inside `replayParser._aggregateStats` next to the existing `diebackCount` column using the same 120s post-buyback window, then shipped on `timeline.players[].buybackEvents`. The UI is pure rendering — no chance of the panel total drifting from the per-player `dieback_count` column even when buybacks and deaths cluster inside the same window.",
      "**Live cost approximation.** Cost shown per buyback is `200 + level*9 + time/0.6` (the live Dota 2 formula), approximating level from the player's final level scaled linearly across the real `match.duration` (passed into the panel as a prop; the parser timeline payload doesn't carry duration). Not exact, but a tight tooltip — the timestamp + dieback flag are the authoritative bits.",
      "**Audit pass on the rest of the quick-wins shortlist.** Smoke timeline (`<SmokePerPlayerPanel>` at MatchDetail:2052), ability-leveling log (`<AbilityRow>`/`<TalentBuild>` at MatchDetail:1873), smoke-kills (`smoke_kills` column), buyback count (`buybacks` column), and ward-killer attribution (via `attackername` → `npcNameToSlot` at replayParser:911) are all already shipping. Buyback timeline was the only real gap.",
      "**Edition scope.** Full edition only — community edition is deprecated for new work."
    ]
  },
  {
    "version": "7.58",
    "title": "Public /v1 API + outbound webhooks (Task #371)",
    "published_at": "2026-05-25",
    "notes": [
      "**Versioned read API at `/v1`.** New `src/web/publicApiRouter.js` exposes 11 read endpoints: `/status`, `/me`, `/matches`, `/matches/:id`, `/leaderboard`, `/profile/:accountId`, `/inhouse/status`, `/tournaments`, `/tournaments/:id`, `/coaches`, `/coaches/:id/availability`. All require an API key via `Authorization: Bearer` or `X-API-Key`. Internal/operator-only fields are stripped before serialisation.",
      "**Per-key rate limits + tiers.** Free keys get 30 req/min · 1,000 req/day; Pro keys get 120 req/min · 50,000 req/day. Limits are enforced with an in-process minute window plus an IP burst gate via the existing `express-rate-limit` middleware. Every response carries `X-RateLimit-Limit/Remaining/Tier`; over-limit calls return HTTP 429 with `{\"error\":\"rate_limited\"}`.",
      "**API key management UI.** New `web/src/pages/SettingsApi.jsx` under Settings → API & webhooks (`SectionLink` icon 🔌). Create labelled keys (cap 5 free / 10 Pro), copy the raw token once at creation (we only store SHA-256 hashes), see prefix + usage count + last-used + status, revoke instantly. Backed by `/api/me/api-keys` GET/POST/DELETE on the authenticated session.",
      "**Outbound webhooks (Pro-only).** New `src/web/webhookDispatcher.js` worker (`startWorker()` tick every 15s) signs each delivery with HMAC-SHA256 over `<timestamp>.<rawBody>` and emits `X-OI-Signature: t=…,v1=…`, `X-OI-Timestamp`, `X-OI-Event`, `X-OI-Delivery`. Six-step exponential backoff: 0s, 30s, 2m, 10m, 1h, 6h (~7h max). Per-subscription delivery log is surfaced in the settings UI. Events: `match.ended`, `lobby.full`, `tournament.round_started`, `coaching.booked`.",
      "**`match.ended` wired into the replay pipeline.** After `db.recordMatch` in the web replay-job path, `dispatchEvent('match.ended', …)` enqueues a delivery row per active subscription — fire-and-forget so a slow subscriber never blocks parsing. Postgres `FOR UPDATE SKIP LOCKED` claim makes the worker safe across multiple processes.",
      "**Schema additions.** `api_keys` (token_hash, prefix, label, tier, usage_count, last_used_at, revoked_at, owner_was_superuser), `webhook_subscriptions` (url, events JSONB, secret, active, last_delivery_*), `webhook_deliveries` (event, payload, status pending/succeeded/failed, attempts, next_attempt_at, last_status_code, last_error). All `CREATE IF NOT EXISTS` so the migration is boot-safe.",
      "**`/api-docs` page.** New `web/src/pages/ApiDocs.jsx` documents auth, rate-limit tiers, every endpoint, the four webhook events, a Node verification snippet for HMAC signatures, and the error code table. Linked from the Settings API page.",
      "**Feature flag `public_api` (preview by default).** Seeded in `src/db/index.js`. In `preview` the `/v1` router only accepts keys created while the owner was a superuser (`owner_was_superuser=true`) — everyone else can already mint keys ready for the public flip. Flip to `on` in the admin Config tab to launch.",
      "**Edition scope.** Full edition only — the community edition has no Pro tier and no Stripe surface to gate against."
    ]
  },
  {
    "version": "7.57",
    "title": "Replay parser shortlist + chat log capture end-to-end (Task #363)",
    "published_at": "2026-05-24",
    "notes": [
      "**New `docs/parser-upgrade-options.md` shortlist.** 12 candidate parser upgrades scored S/M/L with current status, data unlocked, product feature, and risk: per-tick position trail, teamfight cards, damage-attribution graph, buyback economy, rune control, vision uptime, ward-kill attribution, sells/swaps timeline, ability+talent build, smoke/TP timeline, chat log, plus the already-shipped lane-outcome metric. Backfill cost estimate (~17k matches × ~95s = ~450 parser-hours, ~12 weeks at 200/night) included with a proposed nightly cron rather than a one-shot.",
      "**Candidate #1 — Chat + chat-wheel log shipped end-to-end.** The Java parser already emits `chat` / `chatwheel` entries (Parse.onAllChatMessage + onChatWheel + S1/S2 SayText2) but `replayParser._aggregateStats` was discarding them. Now captured into `matchStats.chatLog` (cap: 500 lines/match, 512 chars/line) and persisted to a new `matches.chat_log JSONB` column via `recordMatch`. No Java change, no Maven dependency, no jar rebuild — pure Node + DB.",
      "**Feature flag `chat_log_visible` (preview by default).** New flag seeded at `preview` so admins/superusers see the panel on every newly-parsed match while everyone else waits for the public flip. Server enforces the gate inside `GET /api/matches/:matchId` (strips `chat_log` from the response when state=off, or when state=preview and the viewer isn't staff) so the column is never leaked. Flip `chat_log_visible` → `on` in the admin Config tab to make it public.",
      "**`<ChatLogPanel>` on `/match/:id`.** Renders below the team-abilities panel: tabular-nums timestamp, team-coloured player name (radiant green / dire red / `-1` slot system grey), chat-wheel ids decoded to their English equivalent via a built-in 30-entry table (full list falls through as `#id`). Auto-hidden when the column is empty or the server stripped the field — old matches show nothing rather than a placeholder.",
      "**Coverage in `tests/parserChatLog.test.js`.** Exercises `_aggregateStats` against a synthetic event stream: chat + chatwheel lines are pulled out of the main switch, sorted by time, slot mapping preserved, empty/null `key`s skipped, slot `< 0` collapsed to `-1`, individual lines truncated to 512 chars, and the 500-line hard cap honoured. Three test cases; no Java jar / network dependency.",
      "**Candidate #2 — per-tick position trail / heatmap — deferred** to follow-up Task #366 (needs a real Maven build environment + UI polish pass; sized as standalone work). Backfill plan filed as follow-up Task #367.",
      "**Gates green:** a11y, community-paywall, parser-jar freshness; the new ALTER COLUMN is idempotent so the migration is safe to re-run on every boot.",
      "**Edition scope.** Full edition only — community edition is deprecated for new work."
    ]
  },
  {
    "version": "7.56",
    "title": "Security remediation batch — rotated GCP key, Vite CVE, SQL whitelist, env-ify owner ID (Task #362)",
    "published_at": "2026-05-24",
    "notes": [
      "**Leaked Google service-account JSON removed from the working tree.** `attached_assets/dota-stats-487707-68694b22a462_1771315018239.json` contained a real `private_key` for `dota-bot@dota-stats-487707.iam.gserviceaccount.com` and was deleted. **The key itself must be rotated in the Google Cloud console** (Service Accounts → dota-bot → Keys → delete `68694b22a4622b48c8b2729e9108c99c38ee38c7`, generate a new one, redeploy Google Sheets integration with the new credentials) — file deletion alone does not invalidate the key. `.gitignore` now blocks `attached_assets/*.json`, `*.pem`, `*.key` so an upload can't reintroduce this class of leak.",
      "**Vite CVE-2025-30208 patched in both editions.** `web/package.json` and `community-edition/web/package.json` bumped from `vite ^5.0.0` to `^5.4.15` (resolved to 5.4.21 on install). Both `npm run build`s are clean — no React 18 code depended on vite-5.x-only API surface, so the upgrade is a no-op for the bundles.",
      "**Community + full edition backup-restore SQL injection hardened.** `community-edition/src/web/server.js` (restore + delete-backup routes ~1505/1535) and `src/web/server.js` (~4863/4893) previously accepted any `^[a-z0-9_]+$` suffix into a template-literal `CREATE/INSERT/DROP TABLE …_bak_<x>` SQL statement. Tightened to `^([a-z0-9_]+_)?[0-9]{14}$` so the suffix must match the actual `createDbBackup` output format (optional `label_` prefix + a 14-digit `YYYYMMDDHHMMSS` timestamp). Even though both routes are superuser-only, the old regex would have accepted constructed names like `evil_bak_x_attack_payload`; the new whitelist refuses anything that isn't a real backup slug.",
      "**`OWNER_DISCORD_ID` env-ified in both bots.** `src/discord/bot.js:30` and `community-edition/src/discord/bot.js:11` now read `process.env.OWNER_DISCORD_ID || '<historical default>'` with a comment, instead of hardcoding the literal. Documented under a new \"Environment variables (security-relevant)\" section in `replit.md`. Production behaviour is unchanged when the env var is absent — but a handoff or a SAST false-positive flag can now be addressed by setting the env var rather than patching the source.",
      "**Gates green:** a11y, community paywall, parser-jar freshness; both edition builds clean on the new Vite. **Action item for ops:** rotate the GCP service-account key in the cloud console — this commit only stops the bleeding from the repo side.",
      "**Edition scope.** Both editions — security fix."
    ]
  },
  {
    "version": "7.55",
    "title": "Ops stability + telemetry batch — push hook, regressions, cron heartbeat, bot filter (Task #361)",
    "published_at": "2026-05-24",
    "notes": [
      "**Post-merge GitHub push self-heal is no longer aborting on legitimate merges.** `scripts/post-merge.sh` now adds a third accepted divergence class to the self-heal gate: if the remote SHA is an ancestor of HEAD (i.e. our local tree is strictly ahead — the textbook safe `force-with-lease` scenario), we push without scanning the file-level diff. The old gate only allowed `artifacts/mockup-sandbox/src/.generated/` and `attached_assets/` and was therefore failing on every merge that brought along non-trivial product changes, even though the underlying push was perfectly safe. The auto-generated allow-list is still consulted as the fallback for the genuine \"trees differ AND remote is a sibling, not an ancestor\" case.",
      "**`db.initSchema is not a function` regression fixed.** The `scripts/recompute-position-baselines.js` script (and the perf-backfill / pick-source-backfill scripts) called `db.initSchema()` but the export was renamed to `init()` long ago. Re-exported `initSchema: init` from `src/db/index.js` so every existing caller — including the Baselines startup recompute fired from `bot.js` 60 s after boot — works again without paper-over try/catches.",
      "**Patch-note seeder no longer aborts on duplicates.** A duplicated v6.79 entry in `src/data/patchNotes.js` (one at line 637, one at line 2248) was making `seedPatchNotes` throw on every bot startup, which previously surfaced as `[Startup] Patch note seed failed: duplicate version`. Fix: renamed the second copy to v6.79.1, and softened the seeder guard to collapse duplicates to the first occurrence with a single `console.warn` instead of throwing. The upsert is keyed on `version` so the second copy would have silently stomped the first anyway — the warn keeps the hazard visible without blocking the seed.",
      "**Sponsorship beacons now skip crawler / headless / scripted clients.** New `src/web/botUserAgents.js` block-list (Googlebot, Bingbot, AhrefsBot, Facebookexternalhit, TwitterBot, Discordbot, HeadlessChrome, Puppeteer, Playwright, curl, wget, python-requests, etc., plus empty UAs) is consulted by `POST /api/sponsorships/track/impression` and `POST /api/sponsorships/track/click` before the DB write. Blocked requests still return `{ok:true}` (so the block-list isn't probe-able by diffing response shapes) but no longer inflate the per-day trend chart. Coverage in `tests/sponsorshipBotUserAgents.test.js` locks the classifier against real-browser regressions.",
      "**Coach Premium first-week view-lift card no longer shows an empty placeholder.** `_computeCoachPremiumFirstWeekViewLift` in `src/db/index.js` drops `MIN_COHORT` from 3 to 1 so a freshly-launched Premium tier with a single cohort member still publishes a real ratio; the cards on `web/src/pages/CoachPremium.jsx` and `web/src/pages/CoachEdit.jsx` now disclose the actual cohort sizes (`based on N Premium coach vs M non-Premium`) plus an italic `(early data)` caveat whenever the premium cohort is under 3. The card stops being an empty box from day one of Premium.",
      "**Pro winback DM cron now has a heartbeat.** New `cron_heartbeats(name PRIMARY KEY, last_ran_at, last_status, last_message)` table written at the end of every `_sendProWinbackDms()` tick (status: `ok` / `partial` / `skipped` / `error`, message: `sent=N/M` plus any errors). New `GET /api/admin/system/heartbeats` (superuser-only) returns each row plus an `age_ms` and an `overdue` flag derived from a per-cron expected window (36 h for the daily winback cron). New `CronHeartbeatsPanel` in the admin Config tab renders a tiny table with the cron name, last-ran timestamp + relative age, colour-coded status, and an `OVERDUE` flag in dire-red when the last tick is past the expected window. Operators can spot a silently-broken cron without trawling logs.",
      "**Edition scope.** Full edition only — community edition deprecation makes mirror changes unnecessary."
    ]
  },
  {
    "version": "7.54",
    "title": "Sponsorship impressions and clicks now show a 30-day trend chart (Task #349)",
    "published_at": "2026-05-24",
    "notes": [
      "**New `sponsorship_telemetry_daily` table.** Composite PK on `(order_id, day)` with a secondary index on `day`. Written by `recordSponsorshipImpression` and `recordSponsorshipClick` in `src/db/index.js` via `INSERT … ON CONFLICT (order_id, day) DO UPDATE SET impressions = impressions + 1` (and the click equivalent), so concurrent beacons just increment the same row instead of contending. Both UPSERTs are wrapped in a best-effort try/catch — a daily-table outage falls back to the existing lifetime-counter update without breaking the visible UI.",
      "**Two new aggregation helpers + wired into existing endpoints.** `getSponsorshipSlotTrends({ days: 30 })` returns one row per `(slot_id, day)` summed across every order ever attached to the slot, ordered chronologically. `getSponsorshipOrderTrendsForBuyer({ accountId, buyerEmail, days: 30 })` is the buyer-scoped equivalent keyed by `order_id`. Both are bolted onto the existing routes — `GET /api/admin/sponsorships` now returns a `slot_trends` array alongside `slot_analytics`, and `GET /api/me/sponsorship-orders` returns an `order_trends` array alongside `orders` — so no new routes were needed.",
      "**`SponsorshipTrendChart` SVG component (dependency-free).** New `web/src/components/SponsorshipTrendChart.jsx` renders two polylines (brass = impressions, amber = clicks) sharing a y-axis scaled to whichever metric is larger. Pads the row list out to a continuous 30-day window ending today so gaps render as zero rather than collapsing the x-axis, and hides itself entirely (showing a muted \"no activity\" label) when every day in the window is zero. Carries a `role=\"img\"` plus `<title>` summary (`Slug X: 30-day trend, 2026-04-24 to 2026-05-24. 1,234 impressions, 56 clicks.`) so screen readers get the same takeaway sighted users get from the line shape.",
      "**Wired into Admin Panel → Sponsorships and `/sponsorships/inbox`.** Admin Panel's slot-performance table gets a new `30d trend` column with a per-slot sparkline; the inbox's `My sponsorship orders` table gets a per-order sparkline. Both reuse the shared component via a small `trendRowsFor(allRows, key, value)` helper that filters the flat trend response down to a single row's series at render time.",
      "**Edition scope.** Full edition only — community edition has no sponsorship system."
    ]
  },
  {
    "version": "7.53",
    "title": "Limited-drop cards now show when the drop started (Task #347)",
    "published_at": "2026-05-24",
    "notes": [
      "**\"Started\" line on every limited-drop card.** `web/src/pages/CosmeticsShop.jsx` now renders a muted `Started <local time> · Xh ago` line beside the existing `Ends …` line on each card, so players can tell at a glance whether they're early or late into the window (and how long the original window was). Uses the same `formatAbsoluteEndTime` helper as the end-time line so the timezone/format match exactly, plus a new `formatTimeSince` helper for the compact relative tag (days → hours → minutes; \"just now\" for sub-minute deltas).",
      "**No API change needed.** `GET /api/limited-drops/active` already returns `available_from` (the DB helper does `SELECT *`), so the client just had to start reading the field. Older rows without `available_from` populated fall through gracefully — the started line simply isn't rendered.",
      "**Edition scope.** Full edition only — community edition has no limited-drop panel."
    ]
  },
  {
    "version": "7.52",
    "title": "'Watch live' badge now visible from every page (Task #346)",
    "published_at": "2026-05-24",
    "notes": [
      "**Site-wide spectator hook.** The `WatchLiveBadge` component from `web/src/components/HomeWidgets.jsx` is now mounted inside the top-level `<Nav />` in `web/src/App.jsx` with a new `variant=\"nav\"` prop, so visitors on `/heroes`, `/leaderboard`, a player profile, etc. can jump straight into `/spectate/:matchId` without bouncing back to `/` or `/inhouse`. Reuses the existing `/api/inhouse/live-spectate` poll (10s interval, auto-hides when nothing is live) — no new endpoint, no new poller.",
      "**Compact `variant=\"nav\"` render.** Tightens padding (4px 10px) and font-size (11px) and drops the outer `marginBottom: 16` / flex-end wrapper so the pill fits the navbar row. The original home-widget render (`variant=\"home\"`, the default) is unchanged.",
      "**Edition scope.** Full edition only — the community edition has no inhouse lobby / live spectator stream."
    ]
  },
  {
    "version": "7.51",
    "title": "Coach Premium upsell now shows your real would-have-saved figure (Task #344)",
    "published_at": "2026-05-24",
    "notes": [
      "**Personalised savings on the Premium card.** New `GET /api/coach/premium/lift` aggregates the signed-in coach's last 30 days of paid/completed bookings (`coaching_bookings` rows in `status IN ('paid','completed')`) and computes `(current_bps − 700) × gross / 10000` for non-Premium coaches (\"you would have saved $X\") or `(default_bps − 700) × gross / 10000` for active Premium coaches (\"you have saved $X vs the standard rate\"). Rendered as a new `PremiumLift` line inside the existing `⭐ Coach Premium` card in `web/src/pages/CoachEdit.jsx`, and on the hero of `web/src/pages/CoachPremium.jsx` when signed in as a coach. Both surfaces fall back to a muted \"figure will appear here once you've taken your first paid booking\" hint when `enough_history` is false.",
      "**New `coach_profile_views` table for top-of-funnel lift.** Lightweight per-day counter throttled by a unique partial index on `(coach_account_id, viewer_key, viewed_on)` so a single visitor refreshing can't inflate the count. Viewer key is the signed-in `account_id` (`a:<id>`) when available, otherwise a `SESSION_SECRET`-salted SHA-256 hash of the request IP (`h:<24-char-hex>`) — raw IPs are never stored. The write is best-effort inside `_coachDetail` in `src/web/server.js` (wrapped in try/catch and skipped when the viewer is the coach themselves), so a telemetry hiccup can never break the public detail page.",
      "**Aggregate first-week profile-view lift on the public pitch page.** Same `/api/coach/premium/lift` endpoint exposes an aggregate cohort comparison — `COUNT(*) FROM coach_profile_views` in the first 7 days after Premium activation (`MIN(coach_premium_subscriptions.created_at)`) vs the first 7 days after `coaches.created_at` for non-Premium coaches. The pitch hero renders \"**Nx more first-week profile views** than non-Premium coaches\" once both cohorts have ≥ 3 coaches and the ratio is > 1.05; hidden entirely otherwise so we never publish a misleading or under-sampled number. CoachEdit's `PremiumLift` shows the same line at the same threshold.",
      "**Public/private split on a single endpoint.** Signed-in coaches get the full payload (personal + aggregate). Unauthenticated callers — the public pitch page hero — get an aggregate-only response with `personal: null` computed by a small dedicated helper `db._computeCoachPremiumFirstWeekViewLift()`, so the conversion-trust line still renders without leaking per-account data.",
      "**Edition scope.** Full edition only — the community edition has no coaching marketplace by policy."
    ]
  },
  {
    "version": "7.50",
    "title": "Sponsorship banners on leaderboard, match, and profile pages (Task #343)",
    "published_at": "2026-05-24",
    "notes": [
      "**Three new sponsorship slots wired into high-traffic surfaces.** `web/src/pages/Leaderboard.jsx` now renders `<SponsorshipBanner slug=\"leaderboard_top\" />` directly under the page title; `web/src/pages/MatchDetail.jsx` renders `<SponsorshipBanner slug=\"match_sidebar\" />` just above the match header; `web/src/pages/PlayerProfile.jsx` renders `<SponsorshipBanner slug=\"profile_sidebar\" />` at the top of the magazine layout. Each slot fetches independently via the existing `/api/sponsorships/active/:slug` endpoint and silently renders nothing when no active sponsor is assigned, so the surfaces are unchanged until an admin lights them up.",
      "**No new component logic.** Reuses the existing `SponsorshipBanner` component (which already handles impression/click beacons, `javascript:` URL hardening, FTC-style 'Sponsored by' disclosure, and `rel=\"noopener sponsored nofollow\"`). Admins can create matching slot rows in the existing Sponsorships admin panel using the new slugs — no schema or backend changes required.",
      "**Edition scope.** Full edition only — community edition is paywall- and monetisation-free by policy."
    ]
  },
  {
    "version": "7.49",
    "title": "Sponsorship slot performance reporting for admins and sponsors (Task #342)",
    "published_at": "2026-05-24",
    "notes": [
      "**Per-slot performance table in Admin → Sponsorships.** New `getSponsorshipSlotAnalytics()` in `src/db/index.js` rolls up `SUM(impressions)`, `SUM(clicks)`, and active-vs-total order counts grouped by slot (LEFT JOIN so a slot with zero orders still appears) and is now returned alongside `slots` + `orders` from `GET /api/admin/sponsorships`. `web/src/pages/AdminPanel.jsx` (`SponsorshipsAdminPanel`) renders a 'Slot performance' table sorted by lifetime impressions, with CTR computed in JS so zero-impression slots show '—' instead of a misleading 0.00%.",
      "**Per-order impressions/clicks/CTR in the Recent orders table.** Same admin panel — the existing recent-orders table now carries `Impr.`, `Clicks`, `CTR` columns so admins can see how individual sponsor placements are converting, not just aggregate slot performance.",
      "**Sponsor-side telemetry on `/sponsorships/inbox`.** New `GET /api/me/sponsorship-orders` endpoint (session-gated) returns the signed-in buyer's orders with impressions/clicks/CTR via `listSponsorshipOrdersForBuyer({ accountId })`. `web/src/pages/SponsorshipInbox.jsx` — which the checkout `success_url` already lands on — now shows a 'My sponsorship orders' section underneath the existing inbox so sponsors can see how their paid slots are performing. The section is hidden when there are no buyer orders, so non-buyer visitors are unaffected.",
      "**No new tracking writes.** The frontend beacons + `recordSponsorshipImpression/Click` already write into `sponsorship_orders.impressions` / `.clicks`; this task is read-only reporting on top of the existing counters. A time-series chart was the optional stretch and is intentionally deferred since the schema only stores aggregate counts.",
      "**Edition scope.** Full edition only — the community edition has no sponsorship system."
    ]
  },
  {
    "version": "7.48",
    "title": "Limited-drop countdown shows your local end time (Task #338)",
    "published_at": "2026-05-24",
    "notes": [
      "**Absolute end time, in your timezone.** Each card on the 'Available now — limited drop' panel (`web/src/pages/CosmeticsShop.jsx`) now leads with `Ends <localized date/time>` using `toLocaleString` with the visitor's own locale + short timezone name (e.g. 'Ends Sun, 24 May, 8:00 pm AEST'). The previous live `Ends in Xd Yh Zm Ws` figure stays as a secondary, muted line so urgency is still legible at a glance. The exact ISO string is exposed as a `title` tooltip for anyone who wants to copy/paste it.",
      "**Adaptive ticker.** The `nowMs` `setInterval` now switches cadence based on the soonest drop's remaining window — 1 minute when > 1h remains (the relative line only changes at that scale anyway), 1 second only in the final 60 minutes when the seconds digit is actually visible. Drives off `React.useMemo`-derived `soonestEndsAtMs` so the effect re-runs cleanly the moment any card crosses the 1h threshold.",
      "**Edition scope.** Full edition only — the community edition does not surface a limited-drop shop."
    ]
  },
  {
    "version": "7.47",
    "title": "Home-page 'Watch live' badge surfaces active spectator stream (Task #336)",
    "published_at": "2026-05-24",
    "notes": [
      "**Watch live badge on the home page.** New `WatchLiveBadge` widget in `web/src/components/HomeWidgets.jsx` polls the existing `GET /api/inhouse/live-spectate` probe every 10s (same cadence the inhouse hub uses) and renders a small red 'Watch live' pill linking to `/spectate/:matchId` whenever the lobbyManager reports an active match. Auto-hides when no match is live, so the layout is unchanged on quiet days. Wired into `web/src/pages/Home.jsx` above `LiveInhousePulse` for both signed-in and signed-out branches.",
      "**Why this matters.** Task #325 added the 'Watch live' pill to `/inhouse`, but new visitors landing on the home page had no signal a live spectator stream was available — they had to click through to the inhouse hub first to discover it.",
      "**A11y.** Badge carries an `aria-label` and the decorative pulsing dot is `aria-hidden`. `npm run check:a11y` passes.",
      "**Edition scope.** Full edition only — the surfaced endpoint is part of the full-edition inhouse system."
    ]
  },
  {
    "version": "7.46",
    "title": "Promote Coach Premium so coaches discover the upgrade (Task #335)",
    "published_at": "2026-05-24",
    "notes": [
      "**Public pitch page at `/coach/premium`.** New `web/src/pages/CoachPremium.jsx` explains the three perks (featured placement, 7% commission vs the 10% site default, priority support), shows the maths on a $50/hr session (break-even at 7 bookings/month), and surfaces a `Subscribe — $9.99/mo` CTA that calls the existing `POST /api/coach/premium/checkout` endpoint and redirects to Stripe. Already-active subscribers see a `✓ You're already Premium` confirmation and a `Manage in coach editor` link instead. Visitors who aren't signed in or don't yet have a coach row are bounced to `/coach/onboarding` first. Lazy-loaded route wired in `web/src/App.jsx`.",
      "**Premium badge on `/coaches` directory cards.** `listActiveCoaches` in `src/db/index.js` now SELECTs `(c.is_premium IS TRUE AND (c.premium_until IS NULL OR c.premium_until > NOW())) AS is_premium` so the public marketplace JSON exposes entitlement state. `web/src/pages/Coaches.jsx` reads that field and renders a brass `★ Premium` pill on each card plus a brass border + subtle amber inner-glow, making the featured-first ordering visually legible (previously premium coaches were silently ordered first with no visual differentiation). The directory header gains a `★ Coach Premium` link to the new pitch page.",
      "**Onboarding flow surfaces Premium as the natural post-KYC step.** `CoachOnboarding.jsx` now ends with a brass-bordered \"After KYC: consider Coach Premium\" card linking to `/coach/premium`. The `CoachEdit.jsx` page already had the subscribe card from Task #320 and is unchanged.",
      "**No new backend routes.** Reuses the existing checkout / status / cancel endpoints and the existing `coach_premium` Stripe webhook lifecycle — entitlement flips in one place (`db.applyCoachPremiumStripeEvent`) and propagates to the marketplace listing automatically via the SQL change above.",
      "**Edition scope.** Full edition only — community edition is paywall-free by policy."
    ]
  },
  {
    "version": "7.45",
    "title": "Public sponsorship storefront + home banner placement (Task #334)",
    "published_at": "2026-05-24",
    "notes": [
      "**`/sponsorships` storefront.** New `web/src/pages/Sponsorships.jsx` lists every active slot from `GET /api/sponsorships/slots` (label, slug, description, monthly price formatted via `Intl.NumberFormat`) and surfaces a `Sponsor this slot` button on each card. Clicking opens a modal that collects sponsor name (required), click-through URL, logo URL, contact email, and months (1–12), then POSTs to `/api/sponsorships/:slug/checkout` and redirects to the returned Stripe Checkout URL. Anonymous visitors can buy via the `buyer_email` fallback already accepted by the existing checkout route — no new server endpoints were added.",
      "**Modal uses the shared `<Dialog>` primitive.** Focus capture/restore, Escape-to-close, body-scroll lock, and ARIA labelling all come from the existing primitive, so the new page passes `npm run check:a11y` without any allowlist entry.",
      "**Routing + nav.** New lazy route `/sponsorships` wired in `web/src/App.jsx` next to the existing `/sponsorships/inbox`, plus a `Sponsor a Slot` entry in the Tools dropdown so prospective sponsors have a discoverable entry point.",
      "**Home banner already wired.** `SponsorshipBanner` was already rendering on the home page with slug `home_top`; this task verifies the public surface exists and adds the storefront required to actually sell those slots.",
      "**Edition scope.** Full edition only — the community edition is paywall-free by policy and does not surface monetisation pages."
    ]
  },
  {
    "version": "7.44",
    "title": "Per-row tenant isolation across matches, tournaments, coaches, inhouse sessions (Task #333)",
    "published_at": "2026-05-24",
    "notes": [
      "**Row-level tenant scoping is now wired through the four big user-facing tables.** `matches`, `tournaments`, `coaches`, and `inhouse_sessions` each gain a nullable `tenant_id INTEGER` column + `idx_<table>_tenant` index via idempotent `ALTER TABLE … ADD COLUMN IF NOT EXISTS` in `src/db/index.js`. `NULL` means \"the default tenant\" (the legacy OCE Inhouse experience) — same convention already used by `sponsorship_slots`, so backfill is a no-op and existing rows keep working exactly as before. New rows inserted through `createTournament`, `createInhouseSession`, `getOrCreateOpenInhouseSession`, and `createCoachRow` accept an optional `tenantId` so a white-label sub-brand records under its own tenant.",
      "**Public list endpoints filter by `req.tenant.id`.** `GET /api/matches`, `/api/tournaments`, `/api/coaches`, `/api/inhouse`, and `/api/inhouse/active` now route through a new `_resolveScopeTenantId(req)` helper. Priority: the Host-resolved `req.tenant.id` wins; otherwise a superuser-only `?tenant_id=<id|all>` query param can override scope (admin tooling); otherwise the request resolves to the default tenant (`tenant_id IS NULL`). New `_tc(tenantId, params, alias)` SQL clause builder in `src/db/index.js` keeps the scope logic in one place — pass `'all'` to opt out entirely (admin cross-tenant view).",
      "**Detail-route IDOR guard.** `GET /api/matches/:matchId`, `/api/tournaments/:id`, `/api/inhouse/:id`, and `/api/coaches/:id` now run the fetched row through a new `_visibleInScope(row, scopeTenantId)` check and return 404 (not 403 — preserves opacity) when a tenant tries to fetch another tenant's record by direct id. Closes the cross-tenant read leak the list-only scope didn't cover.",
      "**Create-path tenant stamping.** `POST /api/tournaments`, `POST /api/inhouse`, `POST /api/inhouse/join` (auto-create), the leftover-rollover create inside the inhouse complete flow, and both coach-creation paths (`/api/coach/onboard` + the superuser test-promote shortcut) now pass `tenantId: req.tenant?.id || null` so new rows are stamped onto the correct tenant from the moment they're inserted — sub-brand-hosted writes can no longer silently land in the default tenant.",
      "**Single-tenant invariant for inhouse auto-create.** `getOrCreateOpenInhouseSession` now uses a tenant-scoped advisory lock key (`inhouse_session_create_v603:t<id>`) and a tenant-scoped \"already-open?\" lookup, so each tenant gets at most one auto-open session in flight without blocking other tenants. The default-tenant lock key is unchanged for backwards compatibility.",
      "**Admin Panel — tenant data-scope preview.** New `<TenantDataScopeFilter/>` widget at the top of the 🏢 White-label tenants panel lets superusers pick \"Default tenant / All tenants / a specific tenant\" and see live counts for matches, tournaments, coaches, and inhouse sessions under that scope. Drives the `?tenant_id=` override so operators can verify isolation before deploying a sub-brand, without having to flip their Host header.",
      "**Integration test for two-tenant isolation.** New `tests/task333TenantRowIsolation.test.js` seeds three scopes (default + tenant 1 + tenant 2) into an in-memory pg.Pool stub, then asserts that `getMatches` / `getMatchCount` / `getTournaments` / `listActiveCoaches` / `listInhouseSessions` / `getActiveInhouseSession` each return only the rows belonging to the requested tenant, that the row-id sets are pairwise disjoint, and that the `'all'`-scope admin path sees the full union. Runs without a live Postgres.",
      "**Edition scope.** Full edition only — every change is under `src/` / `web/src/`. No paywall, Stripe, or tenant code touches `community-edition/`."
    ]
  },
  {
    "version": "7.43",
    "title": "Limited-drop cosmetics surfaced in the shop (Task #330)",
    "published_at": "2026-05-24",
    "notes": [
      "**\"Available now — limited drop\" panel on `/shop`.** `web/src/pages/CosmeticsShop.jsx` now calls `getActiveLimitedDrops()` on mount and renders a top-of-page panel whenever `/api/limited-drops/active` returns rows. Each card shows the drop's label, kind, a live ticking countdown to `available_until`, and (when `quantity_cap` is set) an `X / cap sold` line that flips the card to a `🔒 LOCKED` `Sold out` state once `quantity_sold >= quantity_cap`.",
      "**Buy paths reuse existing checkouts.** Coin button calls the existing `/api/coins/spend` endpoint via `spendCoinsOnSku(drop.sku)`. Stripe button routes by `drop.kind` to the already-wired checkout for that family (`frame` → `purchaseFrameCheckout`, `founder_ring` → `buyFounderRingCheckout`); deliberately no new server-side purchase route was added — limited drops are a curated, time-boxed surface over cosmetics that already have a purchase flow. A drop with only a `coin_price` simply renders the coin button alone.",
      "**Ticker is scoped.** The 1s `setInterval` only mounts when there's at least one active drop with a future `available_until`, and clears itself when the list drains, so visitors who don't see a drop pay no render cost. Ended drops are filtered out of the panel rather than rendering as `Ended` placeholders.",
      "**A11y.** Every button is a real `<button type=\"button\">` with visible text (no icon-only controls) and a `title=` hint for the disabled/sold-out/signed-out states. A11y gate (`npm run check:a11y`) passes."
    ]
  },
  {
    "version": "7.42",
    "title": "Live spectator link on the inhouse hub (Task #325)",
    "published_at": "2026-05-24",
    "notes": [
      "**\"Watch live\" affordance on the inhouse hub.** Whenever the bot's `lobbyManager` reports an active matchId, a red `🔴 Watch live` pill renders next to the session-status pill on `/inhouse` and links straight to the existing `/spectate/:matchId` SSE page. The pill is a real `<Link>` with an `aria-label`, so it's keyboard-reachable and screen-reader-friendly out of the box.",
      "**Lightweight live-probe endpoint.** New `GET /api/inhouse/live-spectate` returns `{ matchId, lobbyName, state }` from `lobbyManager.currentLobby` (or `{ matchId: null }` when nothing is in flight). Public read — same trust boundary as the underlying `/api/spectate/:matchId` SSE. The inhouse page polls it every 10s so the button appears/disappears as the bot's lobby transitions in/out of an active match, without forcing every visitor to open an SSE connection.",
      "**Edition scope.** Full edition only — community edition has no spectator page, so no community surface is touched."
    ]
  },
  {
    "version": "7.41",
    "title": "Coaching commission controls + Coach Premium + sponsorships + white-label tenants (Task #320)",
    "published_at": "2026-05-24",
    "notes": [
      "**Configurable coaching commission + auto-tier system.** Site default platform fee moves from a hard-coded 10% to a `site_settings` row (`coaching_commission_bps`, basis points). Coaches are auto-tiered into **Rookie (site default)**, **Established (18%)**, or **Elite (12%)** by completed sessions + avg rating (`evaluateAndAutoTierCoach` recomputes on every booking-completed transition). Rookie deliberately inherits the admin-set global rate so the slider is materially active for the majority of coaches; established/elite are explicit promotional discounts on top. Booking flow calls `db.resolveCommissionBpsForCoach(coach)` — resolution order is manual override (`commission_override_bps`) → Coach Premium (7%) → established/elite discount → site default. `coaches` gains `commission_override_bps`, `commission_tier`, `is_premium`, `premium_until`. New superuser routes: `GET /api/admin/coaching/commission` (returns effective rate per coach + tier ladder), `POST /api/admin/coaching/commission/default`, `POST /api/admin/coaching/commission/coach`. Admin Panel → Marketplace gains a **💸 Coaching commission** panel with editable default, per-coach override + tier picker, and a **live earnings preview** (\"coach earns $X on a $50 session\") that recomputes as you type.",
      "**Sponsor URL hardening.** `sponsor_url` and `sponsor_image_url` are now validated against an `http(s)`-only allowlist at write time in `POST /api/sponsorships/:slug/checkout` (rejects `javascript:`, `data:`, `file:` etc. with HTTP 400) and re-checked at render time inside `<SponsorshipBanner/>` so a legacy or hand-edited unsafe URL can never become a clickable XSS vector on a public surface. Both layers normalise via `new URL()` so the persisted value is canonical.",
      "**Coach Premium subscription ($9.99/mo).** New `coach_premium_subscriptions` table (Stripe-driven, idempotent on `stripe_subscription_id`). Routes: `POST /api/coach/premium/checkout` (Stripe Checkout in `mode='subscription'` with `metadata.purpose='coach_premium'`, override price via `STRIPE_COACH_PREMIUM_PRICE_ID` or `COACH_PREMIUM_PRICE_CENTS`), `GET /api/coach/premium/status`, `POST /api/coach/premium/cancel` (sets `cancel_at_period_end`). Webhook lifecycle: `customer.subscription.*` events with `purpose=coach_premium` flow through `db.applyCoachPremiumStripeEvent`, which flips `coaches.is_premium` + `premium_until` and triggers the 7% commission tier automatically. `CoachEdit.jsx` surfaces a brass-bordered Premium card with subscribe/cancel CTAs and renewal date. **Entitlement enforcement**: `listActiveCoaches` now orders premium-active coaches first in the public marketplace listing, so the paid plan actually buys featured placement (other premium perks — extra session-slot caps, video analysis tools, priority support — remain on the roadmap and are tracked via the existing follow-up tasks).",
      "**Sponsorship slots + telemetry + public render.** New `sponsorship_slots` table (admin-defined placements, tenant-scopable via `tenant_id`) + `sponsorship_orders` (Stripe-backed, 30-day window, plus `impressions` and `clicks` counters). Public: `GET /api/sponsorships/active/:slug` (tenant-scoped — a tenant-specific slot wins over a global one with the same slug), `GET /api/sponsorships/slots`, `POST /api/sponsorships/:slug/checkout` (`purpose='sponsorship'`), and best-effort beacon endpoints `POST /api/sponsorships/track/impression` + `/track/click`. New `<SponsorshipBanner slug=…/>` React component renders the active sponsor with the required **\"Sponsored by\"** disclosure label, fires an impression on mount, fires a click when the sponsor link is followed, and silently renders nothing when no sponsor is active so it can be dropped into any layout. Webhook branch in `fulfillCompletedSession` writes orders on confirmation. Superuser CRUD + recent-orders audit in Admin Panel → Marketplace → **📣 Sponsorships**.",
      "**Tenant-owner self-service + runtime branding.** New routes let tenant owners/admins manage their own white-label without needing the superuser key: `GET /api/tenants/mine` (tenants the signed-in user can manage), `PUT /api/tenants/:id` (display_name + branding, gated by `isTenantMember(...,'admin')`), `POST /api/tenants/:id/sponsorships/slots` and `GET /api/tenants/:id/sponsorships/slots`. Subdomain, custom hostname, plan, and status remain superuser-only. New public `GET /api/tenant/theme` resolves the current `req.tenant` from the Host header and returns a whitelisted set of CSS custom properties (`--bg-primary`, `--accent`, `--gold`, `--brass`, `--amber`, `--ink-navy`) plus logo/favicon URLs so the frontend can theme per-tenant at runtime. `<SponsorshipBanner slug=\"home_top\"/>` is now wired into the Home page (both signed-in and signed-out variants) so the public 'Sponsored by' disclosure renders end-to-end.",
      "**Site default seeded to 25% (Rookie published rate).** The `coaching_commission_bps` seed is now 2500 (25%) so a fresh deploy matches the published Rookie/Established/Elite ladder (25 / 18 / 12) without manual setup. Existing installs keep their admin-configured value (`ON CONFLICT DO NOTHING`). Rookie still resolves through the site default by design — operators changing the default rate immediately re-prices their entire Rookie cohort.",
      "**White-label tenants (Model A) — registry, host routing, and first scoped table.** New `tenants` table (slug, display_name, subdomain, custom_hostname, branding JSONB, plan, Stripe IDs, status) + `tenant_members` (owner/admin/editor). Express middleware after `app.use(express.json())` resolves `req.tenant` from the `Host` header (subdomain of `TENANT_PARENT_DOMAIN` or full `custom_hostname` match) and swallows DB hiccups. First scoped surface: **sponsorship slots + orders are tenant-scoped** end-to-end (tenant-specific slot beats global; default-tenant context cannot see tenant-only slots). Schema enforces this with two partial unique indexes — `(tenant_id, slug)` where `tenant_id IS NOT NULL` and `(slug)` where `tenant_id IS NULL` — replacing the legacy single-column `UNIQUE(slug)` so a global and a tenant-specific slot of the same slug can coexist. Admin slot create/edit accepts `tenant_id`. Routes: `GET /api/tenant/current`, superuser `GET/POST/PUT/DELETE /api/admin/tenants[/:id]` + `POST /api/admin/tenants/:id/members`, `POST /api/tenants/:id/billing/checkout` (Stripe subscription, plans `starter` $29 / `pro` $99). Webhook handlers for `purpose='tenant_subscription'` flip status active/past_due/archived. Admin Panel → Marketplace gets a **🏢 White-label tenants** panel.",
      "**Tenant + commission isolation tests.** New `tests/task320Isolation.test.js` exercises the tenant scoping invariants (tenant-only sponsorship slots can NOT leak into the default tenant; tenant-scoped slug beats global) and the full commission resolution ladder (override > premium > tier rookie/established/elite), with a stubbed pool so the tests run without a live Postgres.",
      "**Scope honesty.** Per-row tenant scoping is rolled out for sponsorships in this batch. Other tables (matches, coaches, tournaments) still operate on the default tenant — those columns will land incrementally as each surface migrates. The middleware + admin panel copy say so explicitly so operators aren't misled.",
      "**Edition scope.** Full edition only — every new route, table, and panel is under `src/` / `web/src/`. No paywall, Stripe, or tenant code touches `community-edition/`, and the paywall gate's allow-list is unchanged. Community edition stays paywall-free by policy."
    ]
  },
  {
    "version": "7.40",
    "title": "Season Pass v2, Teams/clans, weekly challenges, limited drops & extended gifting (Task #319)",
    "published_at": "2026-05-24",
    "notes": [
      "**Season Pass v2 ($14.99 AUD, 50 tiers).** New `/season-pass` page surfaces the 50-tier track, current tier + XP, and a Stripe buy CTA. `POST /api/season-pass/checkout` opens a Stripe Checkout (`purpose='season_pass_self'`), records a pending `season_pass_purchases` row, and the webhook flips it to `active` + calls `grantSeasonPassActivation`. Existing gift flow (`gift_season_pass`) is untouched. Season passes confer season-exclusive cosmetics (new `season_cosmetics` catalog), +20% coin earnings, prediction multiplier, and a season trophy.",
      "**Teams / clans.** New tables `teams`, `team_members` (`UNIQUE(account_id)` — one team per player), `team_invites`, `team_upkeep_payments`. `$10 AUD` one-time fee via Stripe (`purpose='team_creation'`) registers a team with a unique name + tag; the webhook flips `is_active=TRUE` and inserts the owner as the first `team_members` row with `role='owner'`. Optional `$5 AUD / 30 days` upkeep via `team_upkeep_payments` (`purpose='team_upkeep'`) extends `teams.upkeep_paid_until`. New routes: `GET /api/teams`, `GET /api/teams/:id` (team + roster + W/L), `GET /api/me/team` (current team + pending invites), `POST /api/teams/checkout`, `POST /api/teams/:id/upkeep/checkout`, `POST /api/teams/:id/edit` (owner/captain only — bio, colors, logo, frame), `POST /api/teams/:id/invite`, `POST /api/team-invites/:id/respond`, `POST /api/teams/:id/leave` (owner cannot leave). New pages: `/teams` (browse + my invites), `/teams/new` (create form), `/teams/:id` (profile + roster + management).",
      "**Weekly challenges.** New tables `weekly_challenges` (admin-defined: title, description, metric, target, XP + coin reward, week window) and `weekly_challenge_progress` (per-account counter, `completed_at`, `claimed_at`). Surfaced on `/season-pass`. Routes: `GET /api/weekly-challenges/active`, `GET /api/me/weekly-challenges`, `POST /api/weekly-challenges/:id/claim` (idempotent via `claimed_at` — issues XP + coin grants on success), `POST /api/admin/weekly-challenges` (superuser). `bumpWeeklyChallengeProgress({metric, delta})` helper lets matchrecording and other event sources tick progress safely.",
      "**Limited-drop cosmetics with admin rotation.** New `limited_drops` table with `(sku, label, kind, price_cents | coin_price, quantity_cap, season_number, available_from, available_until)`. Public `GET /api/limited-drops/active` powers a future shop panel; superuser-gated `GET/POST /api/admin/limited-drops` and `POST /api/admin/limited-drops/:id/deactivate` rotate the window.",
      "**Extended gifting — coins + anonymous.** New `POST /api/gift/coins` opens a Stripe checkout (`purpose='gift_coins'`) using the existing `COIN_PACKS` catalog; the webhook calls `db.grantCoins({ delta: pack.coins, reason: 'gift_coins:<sid>' })` on confirmation. `gift_purchases` gains `anonymous BOOLEAN` and `message TEXT`; `createAnonymousGift` helper writes both. Existing `gift_pro` and `gift_season_pass` flows are unchanged.",
      "**Stripe webhook dispatcher.** Five new branches added to `fulfillCompletedSession` in `src/web/server.js`: `team_creation`, `team_upkeep`, `season_pass_self`, `gift_coins`. All are idempotent on the Stripe session id and propagate errors so Stripe retries on transient DB failures.",
      "**Edition scope.** Full edition only — every new surface is behind `src/`/`web/src/` and the existing payments stack. Community edition stays paywall-free per policy (`community-edition/SETUP.md`).",
      "**A11y.** Every new clickable is a real `<button type=\"button\">` or a `<Link>`. The icon-free claim/invite/leave/pay-upkeep controls all carry visible labels; the season-pass progress bars are decorative + the textual `X / target` summary stays the screen-reader source of truth."
    ]
  },
  {
    "version": "7.39",
    "title": "Pro subscriptions (Task #318): monthly Stripe subscription tier · grandfathered Founders · winback flow · lapsed-subscriber DMs",
    "published_at": "2026-05-24",
    "notes": [
      "**Monthly subscription tier.** Pro is now primarily a recurring monthly Stripe subscription (default $6 AUD / month, override via `PRO_MONTHLY_PRICE_CENTS` and/or `STRIPE_PRO_MONTHLY_PRICE_ID`). `POST /api/pro/checkout` now accepts `{ plan: 'monthly' | 'lifetime' }` — monthly opens a Stripe Checkout in `mode: 'subscription'` with `subscription_data.metadata.purpose='pro_monthly'` so every downstream subscription/invoice webhook can be safely scoped to Pro. The `/pro` page renders two plan cards side-by-side: Monthly (default, gold) and Lifetime (Founders, brass).",
      "**Founders grandfathering.** Existing lifetime purchasers are flipped to `status='lifetime' + is_founder=true` via in-place backfill in the schema migration, get the new `♛ Founder` `ProBadge` variant, and unlock an exclusive `founder` cosmetic frame (brass double-outline, listed in `FOUNDER_FRAMES` in `src/profileCosmetics.js` and `web/src/profileCosmetics.js`). Frame validation in `PUT /api/me/profile-card` gates the founder frame on `db.isFounder(accountId)` — non-founders get a 403 even if they craft the request manually. Lifetime SKU is bumped to $50 AUD by default (override via `PRO_LIFETIME_PRICE_CENTS`) so the one-time price reflects its Founders-only status.",
      "**Subscription state model.** `pro_subscriptions` gains `stripe_customer_id`, `stripe_subscription_id`, `current_period_end`, `cancel_at_period_end`, `cancelled_at`, `is_founder`, `dunning_attempts`, `last_payment_failed_at`. Status `CHECK` expanded to `active | past_due | cancelled | canceled | lifetime | refunded | pending`. New helpers `applyStripeSubscriptionEvent`, `recordProInvoicePaid`, `recordProInvoiceFailed`, `attachStripeIdsToProRow`, `isFounder`, `listLapsedSubscribersForDm`, `markWinbackDmSent`, `recordCancellationReason`, `listCancellationReasons`. `isProMember` now treats `active`/`lifetime`/`past_due` as Pro (so a single failed renewal doesn't immediately strip access — Stripe retries during dunning).",
      "**Webhook lifecycle.** New branches in `src/web/server.js` for `customer.subscription.created|updated|deleted` (gated by `metadata.purpose='pro_monthly'`), `invoice.payment_succeeded` (skips `subscription_create` to avoid a double-write race with the subscription event), and `invoice.payment_failed` (records dunning, flips to `past_due`). The existing `checkout.session.completed` branch now handles both `pro_lifetime` and `pro_monthly` — for monthly it also stashes the Stripe customer + subscription ids on the row so the post-checkout subscription event can find it. Idempotent on `stripe_subscription_id`.",
      "**Settings → Billing UI.** `SettingsBilling.jsx` now shows plan, status pill (Active / Lifetime / Payment failed / Cancelled), next renewal date, and amount. Monthly subscribers get **Manage billing** (Stripe Customer Portal, via `POST /api/pro/portal`), **Cancel subscription**, and **Resume subscription** buttons. Past-due renewals surface an explicit warning banner with a CTA into the portal to update the card. Lifetime / Founder rows are read-only with a thank-you copy block.",
      "**Winback flow.** Cancelling opens a single-shot `Dialog`-backed modal: step 1 = reason picker (too expensive / not using / missing features / break / switching / other) + optional free-text comment; step 2 = 50%-off-3-months offer for price-sensitive reasons (`POST /api/pro/winback`, applies `STRIPE_WINBACK_COUPON_ID` coupon to the live subscription); step 3 = confirmation that cancel-at-period-end is set and the user keeps Pro until the period ends. Every reason — and whether the discount was offered/accepted — is logged to the new `pro_cancellation_reasons` table; admins can read the last 500 via `GET /api/admin/pro/cancellation-reasons`.",
      "**Lapsed-subscriber Discord DMs.** New daily cron in `src/discord/bot.js` (09:00 Australia/Sydney) calls `_sendProWinbackDms()`, which fans out 7-day and 30-day winback DMs to accounts whose `current_period_end` falls in the matching window. `pro_winback_dms` (`UNIQUE(account_id, stage)`) gives per-stage idempotency so re-runs are safe; missing Discord link → silently marked sent so we don't requery forever. 7-day DM is a soft nudge, 30-day DM mentions the 50%-off-3-months offer.",
      "**Edition scope.** Full edition only — community edition stays Pro-free by policy (`community-edition/SETUP.md`). No paywall, Stripe, or subscription surface is added to `community-edition/`."
    ]
  },
  {
    "version": "7.38",
    "title": "Site polish (Task #317): unified /settings hub · two-stage account deletion (30-day grace) · 5-step onboarding tour with resumable nudge",
    "published_at": "2026-05-24",
    "notes": [
      "**Unified `/settings` hub (`web/src/pages/Settings.jsx`).** New parent route with a sticky left-nav (Profile · Notifications · Linked accounts · Subscription & billing · Coaching when applicable · Danger zone) and an index landing page showing a card grid + the Replay-tour control. All existing deep-links keep working — `/settings/profile`, `/settings/notifications`, `/settings/billing` are now nested children of the same hub so muscle memory and external links are preserved. The top-right user dropdown now points the gear icon at `/settings` (the hub) and adds a direct Billing entry. Coaching shows in the side-nav only when `/api/coach/me` confirms the signed-in account already holds a coach row.",
      "**Linked accounts panel (`web/src/pages/SettingsAccount.jsx`).** Consolidates Steam and Discord connection state in one place. Steam is shown as already-linked (the sign-in identity); Discord renders connect / reconnect via the existing `/auth/discord?return=settings` flow and shows `discord_link=success|error` status from the URL after the round-trip.",
      "**Two-stage account deletion (Task #317).** New columns on `player_profiles` — `pending_deletion_at`, `anonymized_at`, `anonymized_label` — plus DB helpers `requestAccountDeletion`, `cancelAccountDeletion`, `anonymizeAccount`, `getAccountDeletionStatus`, `listAccountsReadyForAnonymization`. Flow: user types `DELETE` to confirm → `POST /api/me/account/request-deletion` stamps `pending_deletion_at` and **destroys the server-side session** (`req.session.destroy()` — authoritative cut so a stolen cookie can't be replayed) → 30-day grace window during which any Steam sign-in calls `cancelAccountDeletion` from `/api/auth/complete` and restores the account (only if `anonymized_at IS NULL`). After the window elapses a periodic sweep (boot + every 6 h, `_runAccountDeletionSweep` in `src/web/server.js`) anonymises each due account: `persona_name` becomes `Deleted Player #N` on every `player_stats` row, **every external identity link is severed** (rows in `nicknames`, `players`, `discord_auto_join_failures`, `discord_link_pending`, `web_auth_sessions` deleted; `discord_id` blanked on `player_stats`/`ratings`), customisations (bio, custom title, frames, theme, voice pack, cover fx, vanity slug, pinned hero/match) are wiped, and `notification_prefs` + `push_subscriptions` are deleted. The `player_stats.account_id` reference is intentionally preserved so other players' historical scoreboards stay correct, but every PII linkage to the original human is irreversibly removed. New `GET /api/me/account-status` returns `{ pending_deletion_at, anonymized_at, scheduled_purge_at, grace_days }` for the UI. Surfaced in `web/src/pages/SettingsDangerZone.jsx`.",
      "**5-step onboarding tour (`web/src/components/OnboardingWizard.jsx`).** Replaces the previous single-screen welcome with a guided 5-step flow (Welcome → Link accounts → Customize profile → How queueing works → You\u2019re ready). Each step is persisted server-side via the new `POST /api/me/onboarding/step` endpoint backed by the new `onboarding_step_index` column. The wizard is skippable at any time (close × or backdrop click — the wizard uses the shared `<Dialog>` primitive so focus capture, body-scroll lock, and Escape-to-close are all handled), and the persisted step lets it resume mid-flow on the user\u2019s next visit. The Link-Accounts step live-checks `/api/auth/me` so the Discord row flips from \u201cNot linked yet\u201d → \u201c\u2713 Linked\u201d without a refresh. Onboarding completion now also stamps an `onboarding_completed_at` timestamp on `player_profiles` so Settings can show \u201cCompleted on \u2026\u201d.",
      "**Replay-anywhere + non-blocking nudge banner.** Settings landing page has a \u201cReplay tour\u201d button that calls `POST /api/me/onboarding/reset` and re-opens the wizard at step 1. While onboarding is incomplete, a small `OnboardingNudge` banner appears across the site (hidden on `/settings` and `/inhouse` to avoid stacking with their own controls) with Resume / Settings / Dismiss actions; the dismiss is `localStorage`-scoped so it doesn\u2019t silence the banner forever across devices, and \u201cResume tour\u201d reopens the wizard at the persisted step.",
      "**Edition scope.** Full edition only — the Settings hub, Billing tab, Coaching tab, and the onboarding wizard\u2019s Discord-link / Pro-cosmetics copy are full-edition surfaces. The two-stage deletion flow and the linked-accounts panel are general account-management features and not paywalled.",
      "**A11y.** Every new clickable is a real `<button type=\"button\">` or a `<Link>`; the wizard and the delete-confirm dialog both use the shared `<Dialog>` primitive (which handles `role=\"dialog\"` + `aria-modal`, focus capture/restore/trap, and Escape-to-close). The icon-only dismiss controls on the wizard and the nudge banner carry explicit `aria-label`s. The settings side-nav is wrapped in `<nav aria-label=\"Settings sections\">` with each entry as a `<NavLink>` so the current section is announced. `npm run check:a11y` continues to pass."
    ]
  },
  {
    "version": "7.37",
    "title": "Engagement loop (Task #316): live queue widget · weekly summary DMs · Bronze→Diamond hero mastery (with cosmetic unlock) · match-imminent push · pool-split coin wagers · prop bets v2 (first tower + MVP + comeback + first Rosh) · Stripe coin top-ups",
    "published_at": "2026-05-24",
    "notes": [
      "**Live inhouse queue widget — now with queued players.** `LiveQueueWidget` (mounted on the homepage AND at the top of `/inhouse`) subscribes to `GET /api/inhouse/queue/stream` SSE; the SSE payload now includes a `queued[]` array per session (`account_id`, `nickname`, `rating`, `preferred_positions`, `wait_seconds`) plus a `needed` count so the widget renders \"X more needed\" and a player-chip strip (nickname · rating · pos · wait). When there is no live queue, the widget falls back to recent matches (`emptyMode=\"recent\"` → `/api/matches?limit=5`) so the slot is never empty. SSE still auto-closes on `req.on('close')` and the rollup query is single-statement.",
      "**Weekly summary DMs (Sunday 19:00 AEDT) — explicit opt-in + enriched payload.** Per reviewer, the `weekly_summary` and `match_imminent_push` notification categories now default **OFF** and `listWeeklySummaryRecipients()` requires an explicit `notification_prefs.enabled = true` row — no implicit broadcast. The DM payload itself is now `getWeeklyPlayerSummary()`-driven and includes the prior 7 days' W/L · K/D/A · avg PERF **plus** `MMR before → after (Δ ▲▼)`, top hero (`{hero} ({wins}/{games})`), achievements unlocked, and a `View the full week:` deep-link to the player's week page. Best-effort web-push branch (new `_sendWebPushToAccount` helper on the bot — lazily loads `web-push`, honours `VAPID_*` env, prunes 404/410 endpoints inline) fires the same digest as a push when configured; silent no-op otherwise. DiscordAPIError 50007 (DMs disabled) is swallowed quietly.",
      "**Per-hero per-position mastery — Bronze → Silver → Gold → Platinum → Diamond.** New `hero_mastery` table (`UNIQUE(account_id, hero_id, position)`); `recordMatch` post-commit upserts one row per scored player with the freshly-persisted PERF value. Tier is derived on read from games **AND** win-rate **AND** average PERF (per spec) so threshold tweaks never require a backfill: e.g. Diamond = ≥30 games + ≥55% WR + ≥7.5 avg PERF. New API: `GET /api/hero-mastery/me`, `/player/:accountId`, `/leaderboard?hero_id&position&limit` — all use the rich tiering. New `HeroMasterySection` rendered on the player profile. **Cosmetic unlock hook:** every new tier-up at Gold / Platinum / Diamond inserts a `cosmetic:mastery_<tier>:<hero_id>` row into `user_one_off_perks` (`ON CONFLICT DO NOTHING` so re-crosses are no-ops).",
      "**Match-imminent web push (~5 min before lobby boot).** `autoStartTicker` fires a superuser-keyed internal POST to `/api/internal/inhouse/:id/imminent-push` the moment a session flips from `open → accepting`. The handler walks every participant's subscribed push endpoints, filters by the now-opt-in `match_imminent_push` category, and sends a single \"Inhouse match starting!\" payload that deep-links to `/inhouse`. Expired endpoints (HTTP 404/410) self-prune.",
      "**Match prediction mini-game — true pool-split coin wagering.** New `match_wagers` table (`UNIQUE(match_ref, account_id)`, stake CHECK enforces 25/50/100 🪙). Stake debits via `player_profiles.coin_balance` under a `pg_advisory_xact_lock(account_id)`. `resolveCoinWagers` (called from `recordMatch` post-commit) now does a real pool split per reviewer: each winner gets their stake back **plus** a proportional share of the losing pool (`payout = stake + stake/winningPool * losingPool`); one-sided pools refund stakes 1:1. New routes `POST/GET /api/predictions/:matchRef/wager`. Pickem entry is FREE for all signed-in users (Pro gate removed); correct winner picks grant 10 🪙 via the daily-cap path. Frontend wager panel on `/pickem` reflects the pool-split economics in copy.",
      "**Prop bets v2 — first tower, MVP team, comeback >10k, first Roshan.** Four new picked/actual/points column triples on `pickem_picks` (via additive `ALTER TABLE IF NOT EXISTS` chain). `submitPickemPick` / `resolvePickemMatch` / `autoResolvePickemForMatch` / `POST /pickem/pick` all accept the new dims; `Pickem.jsx` adds three new selects (MVP side · Comeback yes/no · First Rosh radiant/dire/none) alongside the existing First Tower select. Resolver scoring tolerates null `actual_*` (replay didn't surface that dim → 0 pts) so the rest of the pick still scores.",
      "**Real-money coin top-ups via Stripe.** New `coin_pack_purchases` table (`UNIQUE stripe_session_id`) + four packs in the server-authoritative `COIN_PACKS` catalog: Starter (500🪙/$4.99), Standard (1,200🪙/$9.99), Premium (2,800🪙/$19.99), Whale (7,500🪙/$49.99). New `POST /api/coins/buy` creates a Stripe checkout session with `metadata.purpose='coin_pack'`; the existing webhook's `fulfillCompletedSession` has a `coin_pack` branch that idempotently calls `markCoinPackCompleted` + `grantCoins({applyDailyCap: false})`. New `/coins/buy` page; the CosmeticsShop's `INSUFFICIENT_FUNDS` path links straight to it.",
      "**Edition scope.** Full edition only — every new surface here (Stripe, inhouse-lobby wiring, Pro adjacency) is full-edition by design. Community edition stays paywall-free per `community-edition/SETUP.md`."
    ]
  },
  {
    "version": "7.36",
    "title": "Replay experience (Task #315): player-upload fallback · dedicated-server auto-archive hook · Pro 2D minimap viewer · live spectator SSE",
    "published_at": "2026-05-24",
    "notes": [
      "**Player-upload fallback.** Any signed-in match participant can now submit their own `.dem` copy from the match-detail page (new green ⬆ Submit Replay button next to the existing Remote-replay download). The server enforces match-participation via `_resolvePlayerUploader` and rejects mismatched files inside `processReplayJob` via the new `opts.expectedMatchId` guard — a player can't sneak an unrelated replay through their own match page. Chunked upload reuses the existing 2 MB chunk infrastructure but with Steam-session auth instead of `UPLOAD_KEY`. Provenance is recorded as `player_uploaded`.",
      "**Dedicated-server auto-archive hook.** New `POST /api/replay-hooks/dedicated-server` accepts `{ remotePath, matchId? }` authenticated by `REPLAY_HOOK_SECRET` (header `x-replay-hook-secret`). Only the basename of `remotePath` is honoured — the remote directory is fixed by `dota.dedicatedServer.ssh.replayDir` so a leaked secret can't traverse outside the configured replay dir. Pulls the file via the existing `fetchReplayByName` SSH helper, then runs through `processReplayInternal` with provenance `dedicated_server` and the bound match id (when supplied) for parsed-match-id verification.",
      "**Provenance column.** New `replay_provenance` text column on `matches` (added via the same `ALTER TABLE IF NOT EXISTS` chain that bootstraps the other replay columns). Threaded through `recordMatch(... , replayProvenance)` and exposed on the match GET payload so the UI can badge \"Player-uploaded\" / \"From dedicated server\".",
      "**Pro 2D minimap replay viewer (`/replay/:matchId`).** New `getReplayTimeline` endpoint slim-serves the parser's `gameTimeline.players[].positions` array (sampled every 10 s — newly added to the parser output) plus per-player metadata and the key game events. The new `ReplayViewer` page renders the standard minimap.jpg with interpolated hero dots, scrubbable timeline, play/pause, and 1×/2×/4×/8× speed controls (a11y-compliant `role=\"radiogroup\"`). Gated by `_isProAccount`; non-Pro callers get HTTP 402 with `code: 'pro_required'`.",
      "**Live spectator SSE (`/spectate/:matchId`).** New SSE endpoint streams a `snapshot` event every 3 s built from the in-process `lobbyManager.currentLobby` GC state — team/slot/heroId per seat — and an `end` event when the lobby is no longer active. New public `Spectate` page renders Radiant/Dire seats with a live indicator and reconnects via the browser's `EventSource` auto-retry.",
      "**Edition scope.** Full edition only — the inhouse league surface and Pro paywall both live there. Community edition stays untouched (zero new paywall surface)."
    ]
  },
  {
    "version": "7.35",
    "title": "Scoreboard & match QOL (Task #314): per-match MMR deltas, PERF \"why you scored X\" breakdown, nemesis spotlight, shareable recap cards",
    "published_at": "2026-05-24",
    "notes": [
      "**ΔMMR column** added to every match's team table — shows the actual rating change for the match (before → after) with a green/red tint and a first-ranked-match badge. Backed by a new `getMatchMmrDeltas` lookup that joins against the previous `rating_history` row per player.",
      "**Tier-up banner** surfaces above the team tables whenever a player crossed a tier boundary in the match.",
      "**PERF breakdown panel** explains *why* each player's PERF score came out where it did — expands a per-player factor list (kill involvement, survival, damage, vision, etc.) sourced from the persisted `perf_breakdown` JSON.",
      "**Nemesis spotlight** highlights dominance runs, loss streaks, streak-breakers, and lifetime milestones for the signed-in viewer. New `GET /matches/:matchId/nemesis-spotlight` endpoint (returns `{spotlight: null}` for unauthenticated callers).",
      "**Recap card generator** — `@napi-rs/canvas`-rendered 1200×630 (OG) and 1080×1920 (story) PNGs with three variants: *classic*, *magazine*, *tournament*. Available at `GET /matches/:matchId/recap-card.png?size=&variant=&download=`. The match page has a live preview + style/size picker + download button.",
      "**Discord auto-post** — every match summary now also posts the recap card (variant chosen via `MATCH_RECAP_VARIANT` env, defaults to *classic*). Full edition gains a `POST /matches/:matchId/share-recap-card` endpoint + a \"Post to Discord\" button that targets the highlights channel.",
      "**Community edition** receives all the non-paywall pieces: ΔMMR column, PERF breakdown, nemesis spotlight, recap card download + auto-post to its scoreboard channel. No \"share to Discord\" button (no shared highlights channel surface)."
    ]
  },
  {
    "version": "7.34",
    "title": "Founders Rings shop catalog: 10 new individually-sold cover rings + Inscribed (bundled with Founders Pack)",
    "published_at": "2026-05-24",
    "content": "Task #314 graduates the 11 ring designs from the Founders Ring picker mockup (`artifacts/mockup-sandbox/src/components/mockups/shop-redesign/FoundersRingPicker.tsx`) into real, purchasable shop SKUs. **Inscribed** stays bundled with the Founders Pack (the existing `founders_pass_ring` entitlement, still capped at 200 — no path change for existing buyers). The other 10 (**Classic Brass**, **Laurel Wreath**, **Beveled Edge**, **Phoenix**, **Twin Halo**, **Astrolabe**, **Eclipse**, **Forge**, **Storm**, **Constellation**) are individually-purchasable, uncapped cosmetics tiered by complexity: static rings (Classic, Laurel) at **$4.99 / 1200 🪙**, animated rings (everything else) at **$7.99 / 2000 🪙**. Coin prices stay deliberately above the Stripe equivalent so the coin path remains the *alternative* route, not the cheap one.\n\n**Component port (`web/src/components/founderRings/FounderRing.jsx`).** All 11 ring SVGs ported from TSX → JSX in one file, plus the `AvatarDisc` helper (5 disc treatments: monogram, steam, hero, tier, emblem) and the `useUid` hook that namespaces SVG gradient/clip/mask ids per render. The default export `<FounderRing sku=\"…\" size={N} disc=\"emblem\"/>` switches on slug and falls back to Inscribed for unknown values rather than throwing. Animation drivers (`fp-sweep`, `fp-sweep-rev`, `fp-breathe`, `fp-pulse`, `fp-pulse-bright`, `fp-twinkle`, `fp-strike`) appended to `web/src/styles.css`.\n\n**Catalog single-source-of-truth.** `FOUNDER_RING_SLUGS` / `FOUNDER_RING_TIER` / `FOUNDER_RING_LABEL` / `FOUNDER_RING_USD_CENTS` / `FOUNDER_RING_COIN_PRICE` plus `founderRingSku()`, `isValidFounderRingSlug()`, `isPurchasableFounderRingSlug()` mirrored into BOTH `src/profileCosmetics.js` and `web/src/profileCosmetics.js`. Server-side `COIN_PRICES` in `src/web/server.js` extended with all 10 `founder_ring:*` SKUs so the existing `/api/coins/spend` allow-list automatically accepts them — no separate spend endpoint needed.\n\n**Schema + DB helpers (`src/db/index.js`).** New nullable `equipped_founder_ring TEXT` column on `player_profiles` (NULL = render the legacy `.v3-founders-ring` CSS halo when the account holds the Founders Pack, else nothing). New helpers: `listOwnedFounderRings(accountId)` unions ownership across (a) the legacy `founders_pass_ring` entitlement → `'inscribed'`, (b) `founder_ring:<slug>` entitlements (Stripe-bought, granted in the webhook with `cap=null`), and (c) `coin_owned_cosmetics` rows with `kind='founder_ring'` (coin-bought). `setEquippedFounderRing(accountId, slug)` validates the slug against `FOUNDER_RING_SLUGS` and verifies the caller actually owns it before persisting — throws `{ code: 'NOT_OWNED' }` / `{ code: 'BAD_SLUG' }` which the route maps to 409 / 400. `getPlayerProfileCard()` now surfaces `equipped_founder_ring` alongside `owned_entitlements` so the profile renderer can pick the right SVG without a second round trip.\n\n**Server endpoints (`src/web/server.js`).** `POST /api/shop/founders-ring/checkout` extended to accept `{ slug }`: passing `'inscribed'` (or omitting `slug`) falls through to the unchanged capped Founders Pack flow — same `metadata.purpose='founders_ring'`, same cap re-check + auto-refund branch in the webhook handler. Passing any other slug triggers the new individual-ring path with `purpose='founder_ring_individual'`, no cap, and a fresh duplicate-buy guard that blocks Stripe checkout if the account already coin-owns the ring. The webhook handler grants the matching `founder_ring:<slug>` SKU via the existing `grantEntitlementWithCap({ cap: null, ... })` — idempotent on `(account_id, sku)` via the entitlements table. New `GET /api/me/founder-rings` returns the full catalog (slugs, tier, labels, usd, coin prices) plus `{ owned, equipped }` so the shop can render every card with correct ownership state in one call. New `POST /api/me/equipped-ring { sku }` sets/clears the equipped slug.\n\n**Shop UI (`web/src/pages/CosmeticsShop.jsx`).** New \"Founders Rings\" section inserted after Founders Pass, before Vanity URL. One `<CosmeticCard>` per slug with a 140 px `<FounderRing>` preview, tier label (\"Static\" / \"Animated\" / \"Bundled with Founders Pack\"), and per-state CTAs: signed-out → sign-in link; owned + unequipped → \"Equip\" button (`aria-pressed=false`); owned + equipped → \"✓ Equipped (click to unequip)\" (`aria-pressed=true`); unowned + non-Inscribed → \"Buy $X.XX\" (Stripe) plus a smaller \"or N 🪙\" alt-buy; Inscribed unowned → CTA defers up to the Founders Pass section. The `ringBusy` slug-keyed lock disables only one card's buttons at a time so the rest of the catalog stays responsive during a slow checkout open.\n\n**Profile rendering (`MagazineCover.jsx` + `PlayerProfile.jsx`).** `MagazineCover` now accepts an `equippedFounderRing` prop (slug string, default null). When set, it renders the matching `<FounderRing size={72}/>` as a top-right corner badge over the cover (`pointer-events: none`, `zIndex: 3`, drop-shadow for legibility against any hero backdrop). The existing `.v3-founders-ring` CSS halo stays intact and unchanged when `foundersRing===true`, so original Founders Pack owners who haven't picked a specific slug yet still see the brass→amber border halo they paid for. `PlayerProfile.jsx` plumbs `profileCard.equipped_founder_ring` through (gated on `showProfileCustomization` like the existing `foundersRing` / `coverFx` props).\n\n**No backfill required** — nobody currently owns the Founders Pack ring (the `founders_pass_ring` entitlement table is empty in production), so there are no existing accounts whose default needs migrating, and the new `equipped_founder_ring` column starting at NULL is the correct default for everyone.",
    "author": "System"
  },
  {
    "version": "7.33",
    "title": "Foundation overhaul (Task #313, batch 1): bulk match inserts, admin role tiers + audit log, structured logger, GC reliability watchdog, GitHub Actions CI, migrations scaffolding",
    "published_at": "2026-05-24",
    "content": "Task #313 is the umbrella overhaul of the bot's foundations. The full scope (split the 11k-line `src/web/server.js`, 6.5k-line `src/discord/bot.js`, 15k-line `src/db/index.js` monoliths; extract a `packages/core` to retire the community-edition fork; stand up a persistent Java parser service; wire Sentry; convert the entire `init()` schema into individual migration files) is realistically multiple weeks of careful work and is being landed across multiple batches. This is batch 1 — six focused, low-risk pieces that don't touch any production code path's contract, plus the scaffolding the later batches will build on.\n\n**(1) Bulk match insert in `recordMatch` (src/db/index.js).** The pre-refactor function issued ~270 sequential `await client.query(...)` round-trips inside its transaction for a typical 10-player inhouse: 10 `player_stats` inserts, ~60 `player_items` inserts, ~200 `player_abilities` inserts, plus N draft rows. Each round-trip is a full pg request/response across the DB connection, and they're serialised inside a `BEGIN ... COMMIT` so the transaction stays open the entire time, holding row locks against `matches` / `player_stats`. The refactor collapses every per-player loop into a single multi-row `INSERT ... VALUES ($1,...),($N,...)` statement per table — four statements total instead of ~270. Identical columns, identical column order, identical JSON serialisation for the `killed_by` / `ward_placements` / `hook_cast_*` payloads, identical `ON CONFLICT` semantics. The optional `damage_physical/magical/pure` UPDATE pass remains per-row (it only fires for rows that actually carry the breakdown, and pg's bulk-UPDATE-from-VALUES syntax doesn't compose as cleanly), but the row count is at most 10 so it's now negligible relative to the previous shape. The post-commit best-effort blocks (PERF, season pass XP, achievements, coin grants) are untouched — they were always outside the transaction.\n\n**(2) Admin role tiers + audit log (`src/auth/adminRoles.js` + schema in `init()`).** The legacy admin model is two opaque shared secrets — `UPLOAD_KEY` (admin-ish, e.g. for replay uploads) and `SUPERUSER_PASSWORD` (full god-mode). There's no way to grant a person 'admin' powers without handing over the password. Batch 1 adds a tiered, per-Steam-account role model on top without removing the secrets. Tiers `none | moderator | admin | superuser` (low → high); helpers `getAdminRole`, `setAdminRole`, `hasRole`, `recordAuditEvent`, `ensureBootstrapAdmin`, and a `createRoleGate(db, needed)` express-middleware factory that resolves the caller's role from `req.session.accountId` AND honours the legacy `SUPERUSER_PASSWORD` header as a break-glass `superuser` short-circuit (the role gate returns 401 when no role is found, 403 when the resolved role is below `needed`). Storage is a small `admin_users` table keyed by 32-bit account id + an append-only `admin_audit_log` with `(actor_account_id, actor_role, action, target_account_id, meta jsonb, created_at)`. `setAdminRole` writes the role change and the audit row in a single transaction so they can't drift. `ensureBootstrapAdmin()` is called at startup; if `BOOTSTRAP_ADMIN_STEAM_ID` is set in env and the target account has no existing role, it inserts as `superuser` so operators can land the first admin without manually editing the DB. Hooking the gate into the existing `src/web/server.js` route stack is deliberately deferred to batch 2 — the helpers and schema exist now so future PRs can adopt them per-route without a big-bang switch.\n\n**(3) Structured logging shim (`src/logger.js`).** Centralised `logger.info/warn/error/debug` with a `pino`-compatible surface that falls back to a console-shaped object when `pino` isn't installed. Env knobs: `LOG_LEVEL` (default `info`), `LOG_PRETTY=1` (honoured when `pino-pretty` is also present). The shim's `child(bindings)` returns a sub-logger with bound context so future modules can `const log = logger.child({ mod: 'lobby' })` and have every line tagged. Wired into `src/index.js`'s startup so the boot banner logs through it, and the bootstrap-admin path uses it for the grant-event log line. Replacing the ~hundreds of `console.log` call-sites in `server.js`/`bot.js`/`db/index.js` is incremental work for follow-up batches — `pino` itself is intentionally NOT added as a dep yet so this batch is install-cost-zero on hosts that don't opt in.\n\n**(4) Steam GC reliability watchdog (`src/steam/steamClient.js`).** The Dota 2 GC silently drops sessions under load and after Valve maintenance without a corresponding `disconnected` event on the inner SteamUser socket, leaving the bot in a 'looks healthy, every `gcClient.send*` times out' state until manual restart. `_startGcWatchdog()` fires automatically when the GC `ready` event lands; every 60 s it checks `_lastGcActivityAt` (bumped on any of the optional GC `message`/`receive`/`connectionStatus` events) and, if silence exceeds 5 minutes, kicks the session by replaying `gamesPlayed([])` then `gamesPlayed([DOTA2_APPID])` 1 s later — the same re-hello shape SteamUser uses internally. Emits a `gcWatchdogKick` event so callers (and the unit test) can observe kicks. Tunables are injectable via `_startGcWatchdog({ intervalMs, thresholdMs, now })` so the test can simulate a 6-minute silence without waiting. `_stopGcWatchdog()` runs from `shutdown()`. The interval is `unref`'d so it never holds the event loop open by itself.\n\n**(5) GitHub Actions CI (`.github/workflows/ci.yml`).** Runs on every push/PR to `main`. Steps: checkout → setup-node 20 (with npm cache) → `npm install` → `npm run check:parser` → `npm run check:a11y` → `npm run check:community-paywall-source` → `npm test`. Deliberately does NOT run the web build, the community web build, or the Java parser rebuild — those are the prod deploy host's responsibility (already gated by `deploy.sh` + `scripts/post-merge.sh`). 10-minute timeout so a stuck CI run can't burn an entire job slot.\n\n**(6) `node-pg-migrate` scaffolding (`migrations/`).** Empty-but-real migration directory with a `0000_baseline.sql` no-op placeholder, a README that documents the additive contract (live schema is what `init()` produced; migration files `0001+` build on top), and `npm run migrate`. `node-pg-migrate` itself is intentionally NOT added as a dep yet — the script will tell you to `npm install --save-dev node-pg-migrate` and re-run, so the cost is opt-in. Wholesale conversion of the 1700-line `init()` into migration files is high-risk against a live prod DB (one drift between the migration file and what `init()` actually produced and the next deploy diverges or fails), so it's broken into follow-up tasks.\n\n**Coverage.** New `tests/adminRoles.test.js` exercises role rank/has-role math, `setAdminRole` upsert + audit insert against an in-memory pg fake, the bootstrap path's idempotency, and the `createRoleGate` middleware's 401/403/200 matrix including the SUPERUSER_PASSWORD short-circuit. New `tests/bulkMatchInsert.test.js` pins the multi-row VALUES placeholder math (10 players × 73 cols = 730 placeholders, sequential 1..730, flattened params length matches) so a future refactor of the column list can't silently break the bulk path. New `tests/gcWatchdog.test.js` instantiates a `SteamDotaClient` with a stubbed `steamClient` and verifies that the kick fires after the configured silence threshold, that activity events reset the clock, that the kick replays `gamesPlayed([])` then `gamesPlayed([DOTA2_APPID])`, and that the timer is `unref`'d. Existing `node --test tests/` runner stays green.",
    "author": "System"
  },
  {
    "version": "7.32",
    "title": "Community edition: restore post-match scoreboard image, report-card DMs, and the !reportcard / !ratings commands",
    "published_at": "2026-05-16",
    "content": "Three companion methods that the full-edition bot relies on after every match completion had never been ported into `community-edition/src/discord/bot.js`, even though every call site that depends on them was already wired up — so the community bot had been crashing silently on three independent paths since the feature was last synced.\n\n**(1) `_sendReportCardDMs(matchStats)` — missing.** `_recordMatchData` (lines 386 + 1256) schedules `setTimeout(() => this._sendReportCardDMs(matchStats).catch(...), 5000)` after every recorded match, but the method itself was undefined. Calling an undefined method throws `TypeError: this._sendReportCardDMs is not a function` *synchronously* — before the promise is ever constructed — so the trailing `.catch(...)` never runs, the timer callback explodes, and no report-card DM is ever sent. **(2) `_cmdReportCard` — missing.** The command switch in `messageCreate` (line 444) routes `!reportcard on/off` to `this._cmdReportCard(msg, args)`, but the handler was never ported, so the bot crashed every time someone tried to opt in. **(3) `_cmdRatings` — missing.** Same shape: line 445 routes `!ratings on/off` to `this._cmdRatings(msg, args)` and the handler was undefined, so the existing MVP/attitude ratings DMs (which were defined and theoretically working) could never be opted out of.\n\n**Fix:** copied all three methods verbatim from `src/discord/bot.js` into the community bot (`_cmdReportCard`, `_cmdRatings`, `_sendReportCardDMs`, placed together immediately above `_handleRatingReply`). The community DB already exposes every dependency they need (`getRegisteredPlayers`, `getPlayerReportCardOptOut` / `setPlayerReportCardOptOut`, `getPlayerRatingsOptOut` / `setPlayerRatingsOptOut`, `getPlayerRating`), and `getMmrTier` is already imported at the top of the file alongside `config`. The one piece of the full-edition surface that genuinely doesn't exist in community is the Wave 2 F4 per-category notification preferences table, so `db.isNotificationEnabled` is unimplemented — but rather than guard every call site (and accept that the existing unguarded calls at lines 2441/2584 inside `_initiateRatingSession` and `_handleRatingReply` would still crash the same way), I added a thin `isNotificationEnabled(_, _) => true` stub directly to `community-edition/src/db/index.js` and exported it next to `logMatchDMSent`. The existing opt-out flags (`report_card_optin`, `ratings_optout`) remain the only meaningful gate, and the four code paths that touch the function — the two preexisting ratings/attitude sites and the two new report-card sites — now share one source of truth instead of four guarded fallbacks. Report cards stay opt-IN: players still need to run `!reportcard on` once to start receiving them; only the bot-side crashes are fixed.\n\n**(4) Post-match scoreboard image — never ported.** The full-edition bot, after sending the text embed in `_sendMatchSummary`, also generates a procedural 1000-pixel-wide scoreboard PNG via `@napi-rs/canvas` (RADIANT VICTORY header, per-player K/D/A/LVL/GPM/DMG/BLDG/HEAL rows, MVP/Top-Damage/Gold-King/Tower-Damage/Healer footer) and attaches it to the same channel plus every configured stats channel. The community-edition copy of `src/services/scoreboardImage.js` was a 5-line stub returning `null`, the bot never imported the generator, `_sendMatchSummary` only ever sent the text embed, and `@napi-rs/canvas` wasn't even in `community-edition/package.json`. So even though the text summary went out, the image card that the actual inhouse community is used to seeing after every game never followed it.\n\n**Fix:** copied the full 499-line `scoreboardImage.js` into `community-edition/src/services/` (it has no external file-asset deps — purely procedural drawing using `@napi-rs/canvas` and a soft-import of `getMmrTier` from `../config`, both of which exist on the community side), added `\"@napi-rs/canvas\": \"^0.1.97\"` to `community-edition/package.json` matching the root version, imported `generateScoreboardImage` at the top of `community-edition/src/discord/bot.js`, and inserted the image-send block immediately after the cross-post embed loop in `_sendMatchSummary`. The block mirrors the full-edition shape from Task #279: post to the originating channel first, then cross-post to every other stats channel listed in `config.discord.statsChannelIds`. Smoke-tested by rendering a synthetic 10-player match — produced an 81 KB PNG matching the full-edition layout. Community deploys need a fresh `npm install` to pick up the new canvas dep before the bot restarts.",
    "author": "System"
  },
  {
    "version": "7.31",
    "title": "Resync the superuser admin-route safety check manifest",
    "published_at": "2026-05-16",
    "content": "Task #311: the Task #267 sweep (`tests/superuserAdminRouteAuth.test.js`) diff-checks every `requireSuperuser`-gated route on the api router against a hand-maintained manifest, so adding/removing a gate without updating the manifest fails the test deterministically. Three new admin routes had landed since the manifest was last touched — `POST /admin/inhouse/diag-provision`, `POST /admin/inhouse/diag-cleanup/:id`, and `POST /admin/founders-ring-refunds/:id/retry` — which left the test red and, more importantly, blind to the silent-gate-drop regression it was built to catch.\n\n**Fix:** added the three routes to `EXPECTED_SUPERUSER_ROUTES` preserving the existing ordering convention (the two `diag-*` entries slot in next to the other `/admin/inhouse/:id/*` admin routes; the `founders-ring-refunds/:id/retry` entry slots immediately after its sibling `GET /admin/founders-ring-refunds`). Both sub-tests are now green — the manifest diff matches the live router stack exactly, and the per-route 401/401/403 probe sweep passes for every gated route including the three new ones.",
    "author": "System"
  },
  {
    "version": "7.30",
    "title": "Unit tests pin the PM2 entrypoint-verification deploy gate",
    "published_at": "2026-05-16",
    "content": "Task #310: v7.28 (Task #307) added a `pm2 jlist`-driven pre-restart gate to both `deploy.sh` and `community-edition/deploy.sh` that refuses to restart when PM2's `pm_exec_path` or `pm_cwd` for the target process doesn't match this checkout's expected entrypoint/cwd. It's the belt-and-braces backstop against the original Task #298 cross-edition deploy bug (the directory-name heuristic from Task #302 is the first line of defence; this is the second). Like the Task #302 gate, it had no automated coverage — a future refactor of either deploy script could silently break it and we'd only find out during a real misdeploy.\n\n**Fix:** new `tests/deployPm2VerifyGate.test.js` extracts the live Task #307 gate snippet from each deploy script by slicing from the `EXPECTED_PM2_SCRIPT=` assignment through the closing `fi` immediately before `pm2 restart`, then exercises it with a fake `pm2` executable (written to a temp dir + prepended to `PATH`) whose `jlist` subcommand emits a controlled JSON payload. The pinned matrix covers all the shapes the gate is meant to defend against: correct entrypoint + cwd → exit 0; wrong entrypoint → exit 1 with the re-registration hint and `replit.md` reference; wrong cwd → exit 1 with the same hint; empty `[]` jlist (first-time deploy) → no-op exit 0; jlist that contains a process by a different name → no-op exit 0. The community-side wrong-entrypoint case specifically uses the exact Task #298 misregistration shape (`inhouse-bot` pointed at the full-edition `src/index.js`) and asserts the abort message references `Task #298` so a future refactor that drops that breadcrumb fails the test. The Task #302 basename heuristic itself is already pinned by `tests/deployEditionGate.test.js` (v7.29 / Task #308), so this task only adds the missing PM2 coverage. 10/10 green under the existing `node --test tests/` runner.",
    "author": "System"
  },
  {
    "version": "7.29",
    "title": "Unit test pins the wrong-edition deploy gate matrix",
    "published_at": "2026-05-16",
    "content": "Task #308: v7.26 (Task #302) added a `case \"${DEPLOY_BASE}\" in` gate to both `deploy.sh` and `community-edition/deploy.sh` that hard-fails when the checkout directory basename looks like the wrong edition. It's the last safety net against the original Task #298 cross-edition deploy bug, but its correctness rests on two easily-lost details: the `basename | tr '[:upper:]' '[:lower:]'` lowercase step, and the exact pattern (full edition: `*community*|*dota-stats`; community edition: `*full*`). If someone later 'simplifies' the pattern — drops the `*dota-stats` suffix branch, removes the `tr` step — the gate could silently start passing the wrong basenames again and the regression would only surface during a real misdeploy.\n\n**Fix:** new `tests/deployEditionGate.test.js` reads both deploy scripts, asserts each still pipes through `tr '[:upper:]' '[:lower:]'`, extracts the live `case` pattern from each, and then runs the pattern against a fixed matrix of basenames via `bash -c`. The matrix covers both real prod basenames (`dota-stats-full` allowed on the full side / blocked on community, `dota-stats` the inverse), mixed-case variants (`Dota-Stats-Full`, `Dota-Stats`) to lock in the lowercase step, and adversarial false-positive candidates per side (`community-checkout`, `my-community-fork`, `some-dota-stats` blocked on full; `some-full-checkout`, `fullstack-fork` blocked on community), plus an unrelated control basename that must be allowed on both sides. The test also explicitly asserts the full-edition pattern still contains both `*community*` and `*dota-stats` branches and the community-edition pattern still contains `*full*` — so a 'simplification' that drops a branch fails the test before we even exercise the matrix. Runs under the existing `node --test tests/` runner (3/3 green).",
    "author": "System"
  },
  {
    "version": "7.28",
    "title": "Verify PM2 process actually points at this edition's entrypoint before restarting",
    "published_at": "2026-05-16",
    "content": "Task #307: v7.26 (Task #302) hard-fails each deploy script when its checkout directory basename looks like the wrong edition, which catches the common shape of the Task #298 mistake. But the heuristic is directory-name only — a host where someone manually renamed the checkout so it happens to match (or a host where the basename aligns by coincidence) could still have PM2 misregistered against the wrong entrypoint, and the gate would happily let the deploy through. The authoritative source of truth for what PM2 will actually exec is `pm2 describe <name>`'s `script path` + `cwd`.\n\n**Fix:** added a second pre-restart gate in both `deploy.sh` and `community-edition/deploy.sh` that reads `pm2 jlist`, extracts `pm2_env.pm_exec_path` + `pm2_env.pm_cwd` for the target process via an inline Node one-liner, and refuses to restart if either doesn't match this checkout's expected entrypoint/cwd. `deploy.sh` expects `${DEPLOY_CWD}/src/index.js` + `${DEPLOY_CWD}` (the full-edition entrypoint and its repo root); `community-edition/deploy.sh` expects `${REPO_ROOT}/community-edition/src/index.js` + `${REPO_ROOT}`. If the PM2 process doesn't exist yet (first-time deploy — `pm2 jlist` doesn't include the name), the gate no-ops so `pm2 restart` can create it. Abort messages print the observed vs. expected pair and point at the one-time PM2 re-registration snippet in `replit.md`. Syntax-checked both scripts with `bash -n`. `replit.md` updated with the new gate section.",
    "author": "System"
  },
  {
    "version": "7.27",
    "title": "Route test pins the community replay-download contract",
    "published_at": "2026-05-16",
    "content": "Task #304: v7.23 (Task #303) stripped the Pro-paywall gate from `community-edition/src/web/server.js`'s `_ceReplayDownloadHandler` (mounted at both `/replays/:matchId/download` and `/matches/:matchId/replay`). The remaining branching logic — missing DB row → 404, missing file on disk → 404, happy-path streaming with `Content-Disposition` + `Content-Type` — had no automated coverage, so a future refactor could silently break legitimate downloads, or worse, silently re-add an auth check that locks community users out, and the existing source-scan + dist-scan gates would not catch a behavior regression that didn't touch any of the forbidden tokens.\n\n**Fix:** `tests/communityReplayDownloadHandler.test.js` boots the community server module with `replay/replayParser`, `stats/statsService`, `discord/bot`, and `../db` all stubbed via `require.cache` (same harness pattern as `tests/superuserAdminRouteAuth.test.js`), pulls the `/api` router off the live express `app.router` stack (express 5 — no leading underscore) by fingerprinting it as the inner router that contains `/health`, then pins four shapes against BOTH mount points:\n\n1. `db.getReplayFilePath()` returns null → 404 + `/No replay file stored/i`.\n2. DB returns a row pointing at a path that does not exist on disk → 404 + `/deleted|expired/i`.\n3. Happy path: a real temp `.dem` file is written, the handler streams it into a `stream.Writable` capture sink, and we assert `Content-Type: application/octet-stream`, `Content-Disposition: attachment; filename=\"match_12345.dem\"`, and that the streamed bytes match the file exactly.\n4. Structural guard: each mount point's `route.stack` has **exactly one handler** named `_ceReplayDownloadHandler`, with an explicit deny-list scan for `requirePro`/`requireProMember`/`paywall`/`authMiddleware`/`requireSuperuser`/`requireAuth` in the chain — re-adding any of those middlewares fails the test with a message that points at `community-edition/SETUP.md`'s paywall-free policy.\n\n`node --test tests/communityReplayDownloadHandler.test.js` is green (5/5). The test runs under the project's existing top-level test runner (`node --test tests/`) — community edition has no separate runner per the task description's fallback clause.",
    "author": "System"
  },
  {
    "version": "7.26",
    "title": "Hard-fail deploys when the wrong-edition warning would fire",
    "published_at": "2026-05-16",
    "content": "Task #302: v7.24 (Task #300) added a startup `console.warn` when the bot's entrypoint doesn't match its checkout directory basename, but it only logs — a misconfigured PM2 process still boots and serves the wrong site. The warning is useful for catching the problem in PM2 logs, but by then users on `oceinhouse.gg` or `dota.stats.corvidaeinc.com` may already be seeing the wrong edition's `web/dist/`.\n\n**Fix:** promoted the Task #300 heuristic to a hard pre-restart gate in both deploy scripts. `deploy.sh` (full edition) now aborts before `pm2 restart oi-bot` if its own cwd basename contains `community` or ends in `dota-stats` (the community prod basename). `community-edition/deploy.sh` aborts before `pm2 restart inhouse-bot` if its resolved repo-root basename contains `full`. Both use the exact same `basename | tr` + `case` pattern, with abort messages that point at the one-time PM2 re-registration snippet in `replit.md`. The full-edition prod basename `dota-stats-full` and the community prod basename `dota-stats` are designed to avoid the opposite heuristic, so a correctly-deployed host never sees a false-positive abort. Syntax-checked both scripts with `bash -n`.",
    "author": "System"
  },
  {
    "version": "7.25",
    "title": "Fast source-level Pro-paywall gate for the community frontend",
    "published_at": "2026-05-16",
    "content": "Task #301: the Task #299 dist-scan gate catches Pro-paywall leaks reliably, but only after a full `npm install` + Vite build (~30s+). When a developer accidentally adds a `from '../components/PaywallCard'` or pulls in the full-edition `useProStatus` from `web/src/hooks/`, that's a long wait for feedback — and the resulting error is a path to a minified chunk in `community-edition/web/dist/`, not the offending source line.\n\n**Fix:** a new Node script `scripts/check-community-paywall-source.js` walks `community-edition/web/src/` (skipping `node_modules`/`dist`/`build`) over `.js`/`.jsx`/`.ts`/`.tsx` files and flags any reference to `PaywallCard` or `useProStatus`. Unlike the existing fixed-string grep in `scripts/check-community-paywall.sh`, the new gate is **resolve-aware** for `useProStatus`: it parses the import specifier, resolves it relative to the importing file, and allows the import iff it lands on the local no-op stub at `community-edition/web/src/hooks/useProStatus.{js,jsx,ts,tsx}`. The stub file itself is also allow-listed so it can declare/self-reference the hook. Errors are printed `path:lineNo  reason` with the offending source line quoted, so the developer sees exactly which import to fix.\n\n**Wired in BEFORE the build** in both `community-edition/deploy.sh` and `scripts/post-merge.sh` — the new fast gate runs first, then the existing source+dist gate from `scripts/check-community-paywall.sh` runs (source pass before build, dist pass after) as the byte-level final backstop. Also exposed as `npm run check:community-paywall-source` for local iteration. Negative-tested with a synthetic `__test_paywall_leak.jsx` importing both `PaywallCard` and a non-stub `useProStatus` (3 line-numbered errors, exit 1); positive-tested with a stub at `community-edition/web/src/hooks/useProStatus.js` imported from `community-edition/web/src/pages/X.jsx` via `../hooks/useProStatus` (green, exit 0); current clean tree exits 0.",
    "author": "System"
  },
  {
    "version": "7.24",
    "title": "Startup banner: loudly warn if the bot is running the wrong edition for its checkout",
    "published_at": "2026-05-16",
    "content": "Task #300: the Task #298 paywall bug went unnoticed for ages because nothing complained when `/root/Dota-Stats/src/index.js` (the FULL-edition entrypoint) was running from inside the COMMUNITY checkout directory — PM2 just happily booted the wrong server and started serving the wrong `web/dist/`. A loud startup line would have made the mismatch obvious in PM2 logs the very first time it happened.\n\n**Fix:** both `src/index.js` and `community-edition/src/index.js` now log a clearly-labelled `[Startup] Running FULL edition from <cwd>` / `[Startup] Running COMMUNITY edition from <cwd>` banner immediately after the boot header. If the cwd basename looks like the wrong edition for the entrypoint that's running (full entrypoint from a path whose basename contains `community` or ends in `dota-stats`; community entrypoint from a path whose basename contains `full`), the line is upgraded to a `console.warn` and points the operator at the one-time PM2 re-registration snippet in `replit.md`. Heuristic is deliberately conservative — basename-only, never blocks startup, only warns. The full-edition prod basename `dota-stats-full` does not match the community heuristic, and the community-edition prod basename `dota-stats` does not match the full heuristic, so a correctly-deployed prod host never sees a false-positive warn.",
    "author": "System"
  },
  {
    "version": "7.23",
    "title": "Community edition is now paywall-free at the source level",
    "published_at": "2026-05-16",
    "content": "Task #303: v7.21 (Task #298) fixed the deploy-infrastructure bug that was causing the full-edition bundle to be served on `dota.stats.corvidaeinc.com`, and v7.22 (Task #299) added a built-bundle backstop gate. But an audit of `community-edition/` source itself turned up several remaining paywall touchpoints that contradicted `community-edition/SETUP.md`'s stated policy (\"Pro tier / paid memberships — removed\"):\n\n- **Server route:** `community-edition/src/web/server.js`'s `_ceReplayDownloadHandler` (mounted at `/replays/:matchId/download` and `/matches/:matchId/replay`) returned HTTP 402 with `{paywall: true, feature: 'replay_download'}` for non-Pro non-admin callers, calling `db.isProMember()`. Stripped — replay downloads are now public on the community edition, with only the file-exists / not-expired checks the rest of the handler already did.\n- **Unused frontend hooks:** `community-edition/web/src/hooks/useProStatus.js` (returned `{isPro: false}`) and `useProMembers.js` (returned an empty Set) were no-op stubs left over from the fork. Deleted — verified by ripgrep that nothing in `community-edition/web/src/` imported them.\n- **Dead frontend branch:** `community-edition/web/src/pages/PlayerProfile.jsx` had a `trendPaywall` state slot that consumed `err.paywall` from `getPlayerMatchStatsHistory`, but no JSX consumer ever rendered it. Removed the state + the four references — the catch block now just clears the history.\n\n**Backstop:** the existing `scripts/check-community-paywall.sh` (Task #299) only scanned `community-edition/web/dist/` for four full-edition-only tokens, so a server-side paywall reappearing in `community-edition/src/` would never have been caught. Extended the gate to also scan `community-edition/src/` and `community-edition/web/src/` in two sub-passes — a fixed-string pass for nine tokens (`PaywallCard`, `useProStatus`, `useProMembers`, `isProMember`, `feature: 'replay_download'`, `Pro membership`, `requires Pro`, `Pro Tier`, `Inhouse Stats Pro`) plus a broader regex pass `(['\"]?)paywall\\1[[:space:]]*:` that catches any `paywall:` / `\"paywall\":` / `'paywall':` key shape regardless of value, so a re-added server response of any shape gets flagged. `community-edition/SETUP.md`, `community-edition/README.md`, and `community-edition/src/data/patchNotes.js` are explicitly allow-listed so historical paywalls can still be described in prose. The source-scan pass runs in BOTH `community-edition/deploy.sh` and `scripts/post-merge.sh` BEFORE either build, so a regression fails fast without paying for an install + build first. The dist-scan pass still runs after the build as a second backstop.\n\n**Verification:** `bash scripts/check-community-paywall.sh` is green, `node scripts/check-a11y.js` is green, `rg -i 'paywall|isPro|Pro membership' community-edition/{src,web/src}/` returns only the new comment in `server.js` referencing this task. Full-edition Pro paywall code in `web/src/` and `src/web/server.js` is completely untouched — this task only changed `community-edition/` source plus the shared gate script.",
    "author": "System"
  },
  {
    "version": "7.22",
    "title": "Pro-paywall regression gate for the community edition build",
    "published_at": "2026-05-16",
    "content": "Task #299: the community site (`dota.stats.corvidaeinc.com`) must never ship Pro-tier paywall code. v7.21 (Task #298) fixed the deploy-infrastructure bug that was causing the full-edition bundle to be served on the community domain by splitting the deploy scripts into independent siblings — but there was no automated guarantee that future builds stay paywall-free. If someone copy-pastes a `PaywallCard` import into `community-edition/web/src/` in the future, or if the deploy regresses for some new reason, we'd only notice when a user reports it.\n\n**Fix:** a new shell gate `scripts/check-community-paywall.sh` runs `grep -RFl` over `community-edition/web/dist/` after the build and fails non-zero if any of four forbidden full-edition-only tokens appears in the bundle: `PaywallCard`, `useProStatus`, `Pro Tier`, `Inhouse Stats Pro`. Offending chunk paths are printed so the operator can see exactly which file leaked. The gate is wired into both `community-edition/deploy.sh` (post-build, before the `pm2 restart`) and `scripts/post-merge.sh` (which now also builds `community-edition/web/` alongside the top-level `web/`), so a regression fails the GitHub push **before** it ever reaches the prod host — and fails the on-host deploy as a second backstop. Documented in `replit.md` under the deploy section. Negative-tested by symlinking a stub file containing `PaywallCard` into `community-edition/web/dist/`: gate exits non-zero with the offending path printed. Positive-tested against the current clean build: green.",
    "author": "System"
  },
  {
    "version": "7.21",
    "title": "Independent community-edition deploy: kill the cross-deploy paywall bug",
    "published_at": "2026-05-16",
    "content": "Task #298: `dota.stats.corvidaeinc.com/synergy` was showing the Pro-tier paywall card even though `community-edition/web/src/pages/Synergy.jsx` has no gate. Root cause was a deploy-infrastructure bug, not a code bug: the top-level `deploy.sh` only ever built `web/` (full edition) and `pm2 restart`ed whatever process was named — it never built `community-edition/web/` and never set the PM2 script path. The `inhouse-bot` PM2 process on the prod host was therefore registered against `/root/Dota-Stats/src/index.js` (the FULL-edition entrypoint, which serves `web/dist/` complete with paywalls) instead of `community-edition/src/index.js` (which serves the clean `community-edition/web/dist/`).\n\n**Fix:** added a dedicated `community-edition/deploy.sh` scoped strictly to the community edition — pulls latest, runs the shared parser-jar `--check` + a11y gates, builds `community-edition/web/` only, and `pm2 restart inhouse-bot --update-env` only. It hard-blocks `PM2_APP=oi-bot` so it can never accidentally restart the full-edition process. The top-level `deploy.sh` is now explicitly the FULL-edition deploy (header comment + reaffirmed `cd web` + `oi-bot` default). The two scripts are deliberately independent siblings, not a single switchable script — keeping them separate means a mistaken invocation cannot ever cross-deploy and swap a site to the wrong edition. `replit.md` rewritten with a clear per-edition deploy table plus a one-time PM2 re-registration snippet operators run on the prod host to point `inhouse-bot` at `community-edition/src/index.js` the first time.\n\n**Outcome (once the prod host runs the one-shot re-registration + the new `community-edition/deploy.sh`):** community `/synergy` renders the heatmap directly for everyone (no `PaywallCard` chunk imported), `/pro` 404s, and full-edition `oceinhouse.gg/synergy` paywall is completely untouched — exactly the independent-deploy model we want.",
    "author": "System"
  },
  {
    "version": "7.20",
    "title": "One-click superuser diagnostic: provision dedicated server & verify Dota connect",
    "published_at": "2026-05-10",
    "content": "Task #297: a new superuser-only **🔌 Test: Provision & Connect** card on the admin panel (steambot tab) lets an operator click one button to verify the configured dedicated server is reachable, accepts the RCON match-password push, and that Dota launches and joins via the resulting `steam://connect/IP:PORT/PASSWORD` link.\n\n**How it works:** the new `POST /api/admin/inhouse/diag-provision` route inserts a synthetic `inhouse_sessions` row flagged with a new additive column `is_diagnostic = true`, status `'drafting'` (the only status `provisionInhouseServer()` will accept), then runs the **real** shared `provisionInhouseServer()` helper with two new opts — `silent: true` (skips Discord announce + voice-channel shuffle entirely) and `trigger: 'diagnostic'`. The route returns the same `steam://connect` link + console fallback that the live `/inhouse` page renders post-draft, and the UI displays them inline in the same dark Steam-blue button shape so what you click here is byte-equivalent to what real players click. A **Cleanup** button calls `POST /api/admin/inhouse/diag-cleanup/:id` which only deletes rows whose `is_diagnostic = true` — an accidental id swap can never nuke a real session.\n\n**Isolation:** the new `is_diagnostic` column is filtered out of every public/active-session selector — `getActiveInhouseSession()`, `getOrCreateOpenInhouseSession()` (the auto-create path), and the home-page bundle's active-session lookup — so a diagnostic row can never surface on `/inhouse`, never block a real session from being auto-created, and never appear in any 'is this player in a session' check. It's also excluded from all three `autoStartTicker.js` query passes (open/accepting phase advancement, accept-deadline auto-pick, and the post-draft recovery sweep) so it cannot trip background re-provisioning even if it's left in `drafting` for an instant before the synchronous provisioner returns.\n\n**Defence-in-depth on the route:** superuser-gated (`requireSuperuser`), 503 if `DEDICATED_SERVER_IP` isn't configured, 503 if `DEDICATED_SERVER_RCON_PASSWORD` isn't configured (the diagnostic hard-fails on missing RCON instead of falling through the legacy connect-link-only path — a green result must always mean the real RCON push succeeded), and 502 propagated when RCON is configured but the push fails. On failure the synthetic row is **left in place** carrying its `server_failed` status + `notes` payload, and the same row id is returned so the operator can inspect the failure detail inline (the UI renders `session.notes` in a dedicated red panel) and click the same Cleanup button when done. The cleanup route's `WHERE is_diagnostic = true` clause is the second backstop preventing an accidental real-session delete.\n\nFull-edition only — community-edition has no inhouse routes / page / ticker. A11y gate green (real `<button type=\"button\">`, no hand-rolled clickables). Frontend builds clean.",
    "author": "System"
  },
  {
    "version": "7.19",
    "title": "Steam sign-in: stop the backfill from clobbering the freshly-applied user",
    "published_at": "2026-05-10",
    "content": "**The bug, the real one this time.** v7.16 introduced an `applyUser()` setter on `SteamAuthContext` so the SPA could flip to signed-in state directly from the `/api/auth/complete` response body — no second cookie-bearing fetch required. That part worked. The very next line in `applyUser()` then fired `refreshMe()` as a fire-and-forget backfill of the fields `/api/auth/complete` doesn't return (`discord_id`, `needs_discord_link`, `discord_in_guild`, `discord_autojoin_pending`). And `refreshMe()` was unconditionally calling `setSteamUser(null)` whenever `/api/auth/me` returned literal `null` (server.js line 2046, what the server returns when there's no session) or the fetch threw.\n\nResult: every cookie-propagation failure mode v7.16 was supposed to be immune to (privacy extension, intermediary stripping Set-Cookie, in-flight cookie-jar lag, service-worker origin mismatch) was re-armed by the very next line of the function. The user saw a millisecond flash of \"signed in\" then signed-out again, indistinguishable from the bug we'd been chasing across v7.11/v7.12/v7.15/v7.16. Same destructive shape silently signed users out mid-session whenever the 8-second `DiscordJoinGate` poll on `/inhouse` hit a transient null response.\n\n**The fix:** `refreshMe()` is now non-destructive. On a successful response it still updates `steamUser` (so newly-arrived `discord_id` etc. are reflected), but on a `null` response or a network error it returns `null` and *leaves the existing `steamUser` state untouched*. The only place that ever sets `steamUser` to null based on a server response is the initial-mount fetch in `SteamAuthContext` — that's the one moment where \"not signed in\" is authoritative truth instead of a transient race. `logout()` still clears state explicitly when the user actually signs out.\n\nA11y gate green. Frontend builds clean.",
    "author": "System"
  },
  {
    "version": "7.18",
    "title": "Post-match Discord drop now includes the shareable match card",
    "published_at": "2026-05-10",
    "content": "Task #279: the per-match Open Graph card shipped in v7.06 (Task #268) for `/match/<id>` unfurls is now also dropped into Discord automatically as part of the post-match flow, so the channel post matches what someone clicking the URL would see — same renderer, same cache, same brass/ink-navy palette.\n\nThe bot's `_postMatchSummary()` in `src/discord/bot.js` already fired off the legacy `generateScoreboardImage()` attachment alongside the embed; a parallel async block now also calls `generateMatchOgCard()` from `src/services/matchOgCard.js` (the same module the `/og/match/<id>.png` endpoint uses), wrapped in its own try/catch so a render failure can never break the existing scoreboard send. The OG card lands as a second attachment with `🔗 **View match:** https://oceinhouse.gg/match/<id>` in the message content, so the link unfurls click-through cleanly and clicking opens the SPA. Cross-posts to every configured `statsChannelIds` channel mirror the existing scoreboard fan-out.\n\nThe input mapping mirrors `_resolveOgMatch()` in `src/web/server.js` byte-for-byte: per-team kill totals fill `radiantScore` / `direScore`, the top-fragger pick uses the same most-kills → ties broken by k+a → ties broken by lowest deaths walk, and `topFraggerHeroDisplayName` is resolved via the bot's existing `_heroDisplayName()` helper. MVP is left null at post-time on purpose — votes haven't happened yet, so the post-match render and the eventual post-vote web render stay separate (correct) cache entries instead of poisoning each other. The site URL falls back to `https://oceinhouse.gg` if `SITE_URL` isn't set, matching the rest of `bot.js`.\n\nNo duplicate render path: both surfaces hit the shared in-memory cache in `matchOgCard.js`, so a Discord drop and the first crawler unfurl of the same match share a single canvas encode + a single hero-portrait fetch. Full-edition only — community-edition has no `/match/<id>` SPA route or `matchOgCard` module, and its `bot.js` is a separate code path.",
    "author": "System"
  },
  {
    "version": "7.17",
    "title": "Steam sign-in: definitive fix — apply user inline from token-exchange response",
    "published_at": "2026-05-10",
    "content": "**The bug:** Even after the v7.12 token-exchange handshake (which set the session cookie via a same-origin fetch instead of a cross-site 302) and the v7.15 retry-with-backoff loop on `/api/auth/me`, a subset of users would still land on `/?auth=success`, see the cookie get set, and then have `/api/auth/me` return null on every subsequent retry — leaving them visibly signed-out despite a perfectly valid session row in PostgreSQL.\n\n**The root cause:** The SPA was throwing away the token-exchange response and treating `/api/auth/me` as the only path to a signed-in UI state. Any condition that could keep the freshly-set `oi.sid` cookie from riding the very next fetch — a privacy extension that defers third-party-looking cookies until the next navigation, an intermediary proxy that strips Set-Cookie on certain content types, in-flight cookie-jar propagation lag, an Origin/Referer mismatch from a service worker — would manifest as \"signed in nowhere\" even though the server-side session was fine.\n\n**The fix:** `POST /api/auth/complete` already returns `{accountId, steamId64, displayName}` in the response body. The SPA now applies that payload directly to the auth context via a new `applyUser()` setter on `SteamAuthContext`, flipping the UI to signed-in state the instant the response lands — no `/api/auth/me` round-trip required for the immediate sign-in. The cookie is still set on that very response (so subsequent page reloads keep the user signed in normally), and `refreshMe()` is still fired in the background to backfill `discord_id` / `needs_discord_link` / guild membership. But every cookie-propagation failure mode is now cosmetic instead of user-blocking.\n\n**Bonus:** Cleaned up a merge conflict in `src/data/patchNotes.js` (left-over `<<<<<<<` / `=======` / `>>>>>>>` markers and a duplicate v7.13 entry) that was failing the bot's `[Startup] Patch note seed` step on every boot.\n\nA11y gate green. Frontend builds clean.",
    "author": "System"
  },
  {
    "version": "7.16",
    "title": "One-click \"Retry refund\" for stuck Founders Pass cap-race refunds",
    "published_at": "2026-05-10",
    "content": "Task #274: the superuser Founders Pass Refunds audit table (added in Task #265) now has a one-click **↻ Retry refund** button on every row whose status is `refund_failed`, closing the manual-Stripe-dashboard loop that previously forced an operator to log into Stripe and click Refund by hand.\n\nThe button calls a new superuser-only `POST /api/admin/founders-ring-refunds/:id/retry` route that re-runs `stripe.refunds.create` against the row's stored `stripe_payment_intent` and updates the audit row in place via the existing `recordFoundersRingRefund` upsert (idempotent on `stripe_session_id`, so the row is updated rather than duplicated). On success the row flips to `refunded` and the new `stripe_refund_id` is filled in; on failure the fresh Stripe error message replaces the previous one and is also rendered inline beneath the row's existing error column. Retry metadata carries `reason: 'founders_ring_cap_race_retry'` so retried refunds are distinguishable from the original webhook attempt in Stripe.\n\nDefence-in-depth on the route: superuser-gated (`requireSuperuser`), 503 if Stripe isn't configured, 404 if the row id doesn't exist, 409 if the row is not currently in `refund_failed` (so a successful retry cannot accidentally fire a second refund), and 422 if the row has no `payment_intent` to refund against (the button is also `disabled` with a tooltip in that case so the operator gets the explanation up front).\n\nA11y: real `<button type=\"button\">` with descriptive `title` text, disabled while in-flight, no hand-rolled clickable shapes — passes the existing a11y gate without changes.",
    "author": "System"
  },
  {
    "version": "7.15",
    "title": "Five bug fixes: admin input focus, sticky bar overlap, side banner redesign, Steam auth retry, Stripe note",
    "published_at": "2026-05-10",
    "content": "**Admin panel CMS inputs no longer lose focus on each keystroke** — The `SideForm` component was defined *inside* the `SideBannerPanel` render function, causing React to treat it as a new component type on every state change and unmount/remount the DOM inputs. Extracted `SideBannerSideForm` and `SIDE_BANNER_INPUT_STYLE` to module scope so React preserves the element across renders.\n\n**Profile sticky bar no longer bleeds over notification banners** — The previous `transform: translateY(-100%)` approach shifted the bar's visual rendering upward into adjacent notification banners (e.g. the Steam retry banner) without clipping it. Replaced with a `height: 0; overflow: visible` sticky shell div and `clip-path: inset(0 0 100% 0)` on the inner bar, which contains the bar's paint entirely within its own CSS box. The bar is now invisible until triggered and never overlaps elements above it. Shell with `height: 0` also eliminates the flow-space gap that previously appeared between the navbar and the cover.\n\n**Side banners redesigned to full-height, space-filling panels** — Changed from 150 px fixed strips to panels that fill the full available viewport space beside the 1200 px content column (`width: calc((100vw - 1200px) / 2 - 16px); top: var(--nav-h); bottom: 0`). Images now cover the entire slot with `object-fit: cover` and a gradient overlay carries the title/subtitle. No-image state renders branded centred text instead of a placeholder emoji. The slot stays hidden on screens < 1600 px.\n\n**Steam sign-in retry is more resilient** — After a successful `/api/auth/complete` token exchange, the SPA now retries `refreshMe()` up to four times with backoff delays (0 ms → 150 ms → 500 ms → 1200 ms) before falling through to the banner sweep. This absorbs PostgreSQL session-write propagation lag that occasionally left the auth context null immediately after the cookie was set.\n\n**Stripe warning** — The admin panel warning about a missing `STRIPE_SECRET_KEY` is a production environment config note, not a code issue. Add the key to `~/Dota-Stats-Full/.env` on the prod host to clear it.",
    "content": "**Admin panel CMS inputs no longer lose focus on each keystroke** — The `SideForm` component was defined *inside* the `SideBannerPanel` render function, causing React to treat it as a new component type on every state change and unmount/remount the DOM inputs. Extracted `SideBannerSideForm` and `SIDE_BANNER_INPUT_STYLE` to module scope so React preserves the element across renders.\n\n**Profile sticky bar no longer bleeds over notification banners** — Replaced the `transform: translateY(-100%)` approach with a `height: 0; overflow: visible` sticky shell + `clip-path: inset(0 0 100% 0)` on the inner bar, so the bar is fully contained in its own CSS box and the navbar-to-cover gap is gone.\n\n**Side banners redesigned to full-height, space-filling panels** — Changed from 150 px fixed strips to panels that fill the available viewport space beside the 1200 px content column. The slot stays hidden on screens < 1600 px.\n\n**Steam sign-in retry is more resilient** — After a successful `/api/auth/complete` token exchange the SPA retries `refreshMe()` up to four times with backoff delays before falling through to the banner sweep, absorbing PostgreSQL session-write propagation lag.\n\n**Stripe warning** — The admin panel warning about a missing `STRIPE_SECRET_KEY` is a production environment config note, not a code issue. Add the key to `~/Dota-Stats-Full/.env` on the prod host to clear it.",
    "author": "System"
  },
  {
    "version": "7.14",
    "title": "One-click \"Post to #highlights\" from the match share popover",
    "published_at": "2026-05-09",
    "content": "Task #272: the match share popover now ships a third action button — **Post to #highlights** — that posts the match link straight to the league's Discord channel with a single click, instead of forcing players through the two-step copy-markdown / paste-in-channel dance.\n\nThe button only appears for signed-in players who actually played in the match AND have a linked Discord account. The new backend route (`POST /api/matches/:matchId/share-to-discord`) re-enforces all three gates server-side (session check, viewer's account_id must appear in match.players, db.getDiscordIdByAccountId must resolve), and applies an in-memory per-user-per-match rate-limit (24 h window) so a single player can't spam the channel by re-clicking. Posts go to `HIGHLIGHTS_CHANNEL_ID` if configured, falling back to `ANNOUNCE_CHANNEL_ID` for leagues without a dedicated channel; the bot helper `postMatchShareToHighlights()` lives in `src/discord/bot.js` and uses `allowedMentions: { parse: [] }` so a player-submitted post can never ping @everyone or roles.\n\nThe blurb the bot posts mirrors the popover's existing copy-for-Discord shape: winner-aware (\"🏆 Just won inhouse #N — K/D/A on Hero (Radiant wins)\" vs \"📜 match recap…\"), uses the viewer's session display name, and bakes the public match URL on its own line so Discord's unfurl renders the OG card. Button label flips through Posting → ✅ Posted to #highlights! and inline-renders any server error (rate-limited, bot offline, channel misconfigured).\n\nA11y: real `<button type=\"button\">` with `aria-label`, disabled while in-flight, no hand-rolled clickable shapes — passes the existing a11y gate without changes.",
    "author": "System"
  },
  {
    "version": "7.13",
    "title": "Steam sign-in fixed (scope bug), profile sticky bar restored, side banners with admin CMS",
    "published_at": "2026-05-09",
    "content": "Three targeted fixes plus a new feature.\n\n**Steam sign-in — definitive scope fix:** The auth-token Map introduced in v7.12 was defined inside `createServer()` but consumed inside the separate `createApiRouter()` closure. Because both are independent module-level functions, the Map reference was invisible to the router — every `/api/auth/complete` exchange threw a silent ReferenceError and returned 401. Fixed by moving `steamAuthTokens` (and its cleanup `setInterval`) to module scope, where both functions can reach it.\n\n**Profile cover sticky bar — reverted to position:sticky:** Changing to `position:fixed` in v7.12 removed the bar from the stacking context of the page, placing it behind the navbar (z-index 60 vs 100) and making it invisible when triggered. Reverted to `position:sticky; top:var(--nav-h,52px)`. The `NavbarHeightSync` ResizeObserver from v7.12 that writes `--nav-h` is retained so the top offset tracks the real navbar height.\n\n**Side banners — new CMS feature:** Fixed-position left and right banner slots appear on screens ≥ 1600 px wide (where there is guaranteed space outside the 1200 px main column). Each slot independently supports: on/off toggle, image URL (displayed 2:3 aspect ratio), title, subtitle, and an optional link. Banners update live without a page reload via a `side-banners-updated` CustomEvent. Admin panel (Config tab → Side Banners) provides the full editor with image preview. Server-side: new `GET /api/settings/side-banners` (public) + `POST /api/admin/settings` with key `side_banners` (superuser-gated, input sanitised).",    "author": "System"
  },
  {
    "version": "7.12",
    "title": "Steam sign-in auth-token handshake, profile sticky bar fixed to viewport, hero portrait context labels",
    "published_at": "2026-05-09",
    "content": "Three targeted fixes shipped together.\n\n**Steam sign-in — definitive fix via auth-token handshake:** The root cause of every 'session-exists=true but session-accountId=null' failure was that Set-Cookie headers on HTTP 302 redirect responses are silently dropped by some browser+proxy combinations under SameSite=Lax or Secure rules. No amount of session.regenerate() on the redirect response can fix a browser that refuses to apply it. The new approach: after Steam OpenID validates the user, the server mints a short-lived (2-minute) single-use 40-hex-char token, stores it in an in-process Map, and redirects to /?auth=success&t=<token>. The SPA detects ?t=, calls GET /api/auth/complete?t=<token> as a same-origin fetch (credentials: include), and the server exchanges the token for a real session cookie in that fetch response. Because the cookie is set in a fetch response (not a 302), every browser applies it without exception. The token Map is cleaned up on expiry; the URL param is stripped via history.replaceState immediately after exchange.\n\n**Profile cover v3-sticky bar — fixed to viewport:** The mini sticky header (player name + MMR + WR) was position:sticky which kept a ~46px flow-space gap before .v3-cover, causing a phantom empty band between the navbar and the cover background at scroll=0. Changed to position:fixed so the element is fully removed from the document flow; the cover now starts exactly where it should. A ResizeObserver in NavbarHeightSync (new App.jsx component) measures the real .navbar offsetHeight on every resize and writes it to --nav-h on :root; the sticky bar uses top: var(--nav-h, 52px) so it always sits flush below the navbar regardless of screen size or content wrapping.\n\n**Hero portrait context labels:** All three home-page spotlight hero portraits now carry a brass-coloured context label above the hero name — 'Most played' for the Featured Player and Most Improved cards, 'PERF match' (amber) for the Player of the Week card — so visitors know why that hero is shown.\n\nA11y gate green (153 JSX / 3 CSS). Build clean.",
    "author": "System"
  },
  {
    "version": "7.11",
    "title": "Profile sticky bar below navbar, hero portraits on home cards, Steam login fixed",
    "published_at": "2026-05-09",
    "content": "Three targeted fixes for long-standing visual and auth issues.\n\nProfile cover sticky bar: the v3-sticky mini-header (player name + MMR + WR pill that appears once you scroll past the cover) was pinned at `top: 0`, which placed it behind the main navbar (z-index 100) so the bar content was clipped. Changed to `top: 52px` so the sticky bar sits flush below the navbar when visible.\n\nHome page hero portraits on all spotlight cards: the Player of the Week card (v7.09) already had a hero portrait, but the Featured Player card and the Most Improved filler card were still text-only. Both cards now show a Dota CDN hero portrait with a label beneath it. The spotlight API (`/api/spotlight/current`) now fetches the featured player's most-played hero and includes `top_hero` in the response. The most-improved DB query (`getMostImproved`) now includes a correlated subquery that returns each player's most-played hero as `top_hero`. Both portrait slots fall back gracefully (invisible) when no hero data exists.\n\nSteam sign-in — definitive fix via session regeneration: the root cause of the persistent 'signed in nowhere' failure was stale session ID reuse. When a user had previously loaded any page (creating an anonymous session), the login flow mutated and re-saved that existing session. Some browsers with SameSite=Lax drop the updated Set-Cookie on cross-origin redirects, leaving the old anonymous session ID in the cookie jar. The next `/api/auth/me` request sent the stale ID, found no accountId, and returned null. The fix is `req.session.regenerate()` before writing steamId64/accountId/displayName — express-session destroys the old session, creates a fresh ID, saves it to Postgres, and the redirect response carries only the new Set-Cookie. The browser has no stale ID to fall back to.\n\nA11y gate green (153 JSX / 3 CSS). Build clean.",
    "author": "System"
  },
  {
    "version": "7.10.5",
    "title": "Automated tests for share-card hero validation + owner-only OG preview",
    "published_at": "2026-05-09",
    "content": "Task #271: the share-card hero override (Task #259) shipped two security-relevant code paths that were entirely uncovered by automated tests — `validateExtras()` in `src/profileCosmetics.js` (which coerces malformed `share_card_hero_id` values to null so a bad client save can't poison the field), and the owner-only `?preview_hero_id=` gate on `GET /og/profile/by-id/:accountId.png` (which restricts the unsaved-preview override to the signed-in owner so a third party can't trick the generator into rendering arbitrary heroes on someone else's card). A regression in either could ship unnoticed.\n\nNew `tests/shareCardHero.test.js` (16 subtests, all passing). The validateExtras block covers the four documented shapes — null/missing/empty (→ null), the `'most_played'` sentinel (preserved as-is), positive int + numeric string (parsed and preserved), and four flavours of garbage (random strings, zero/negative/NaN/Infinity, out-of-range ≥1,000,000, and object/array shapes) — all of which must coerce to null without throwing. The OG endpoint block stubs `src/services/profileOgCard.generateProfileOgCard` so the test captures the resolved heroId without spinning up canvas, then asserts six scenarios across the auth gate: anonymous + signed-in non-owner preview attempts are IGNORED (saved-override hero wins, response keeps the public 10-min cache), the owner's preview IS honoured (preview hero wins, response gets `Cache-Control: no-store`), the owner's `most_played` sentinel correctly skips the saved override, the non-owner `most_played` sentinel is also IGNORED, and a malformed `accountId` short-circuits to a 302 to /oa-logo.png before the generator runs.\n\nPattern mirrors the existing `tests/vanitySlugUnfurl.test.js` harness — stub `replayParser` / `statsService` / `groqService` / `discord/bot` / `voiceEventQueue` / `magazineV3` / the `db` module via `require.cache` before loading server.js, walk the route stack, invoke the handler with a fake req/res. No live HTTP, no live canvas, no live DB. Test-only change — no production code modified.",
    "author": "System"
  },
  {
    "version": "7.09",
    "title": "Featured player hero portrait, paywall centred, watch button hint, sign-in reliability",
    "published_at": "2026-05-09",
    "content": "Five UI fixes across the home page, positions paywall, live players watch button, and Steam sign-in flow.\n\nFeatured player card: the Player of the Week card on the home page had empty dead space on the right side. A hero portrait (using the same Dota CDN images as the live players page) is now displayed on the right side of the card, labelled with the hero name below it. Falls back to a placeholder if the hero image fails to load.\n\nPro paywall card: the upgrade card that appears when a non-Pro user visits a gated page (e.g. Position Player Profiles) was anchored near the top of the blurred section. On long pages it parked well below the fold. Changed to `position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%)` so the card is always centred in the visible viewport regardless of scroll position.\n\nWatch button — pub restriction hint: Valve blocks live spectating of pub matchmaking games via the `watch_server` console command as an anti-stream-sniping measure. The Watch buttons on the Live Players page now show a `pubs may be restricted` note beneath the button and an informative tooltip so players know to use Dota's Watch tab for pub games.\n\nSteam sign-in reliability: added `cache: 'no-store'` to all `/api/auth/me` fetch calls so the browser never serves a cached (stale) response after a sign-in redirect. Extended the post-`?auth=success` retry sweep from 4 attempts (max 2 s) to 6 attempts (max 5.5 s) to handle slower session-cookie propagation on prod.\n\nA11y gate green (153 JSX / 3 CSS). Build clean.",
    "author": "System"
  },
  {
    "version": "7.08",
    "title": "Patch notes now stay in sync with deployments",
    "published_at": "2026-05-09",
    "content": "Root-cause fix for the long-standing bug where the site stayed on v6.73 even after deploying newer code.\n\nThe bot seeds patch notes from `src/data/patchNotes.js` into the `patch_notes` database table on every startup via `seedPatchNotes()`. The seed function had a strict ascending-version-order guard that threw immediately when it saw the array was descending (our standard newest-first convention for the file). The thrown error was silently caught in `src/index.js` and logged as '[Startup] Patch note seed failed', so the DB was never updated past v6.73 — the last version that happened to be in the DB from a very early run.\n\nFix in `src/db/index.js` `seedPatchNotes()`: instead of requiring the caller to pass a sorted array, the function now sorts the input ascending internally (`[...notes].sort(...)`) before processing. The ascending-order guard is replaced with a duplicate-version check on the sorted array (a duplicate would cause one upsert to silently overwrite another — a genuine data hazard). Two existing duplicate version numbers in `src/data/patchNotes.js` were also fixed as part of this pass: the second v6.74 (magazine cover revert) was renumbered to v6.74.1, and the second v6.78 (share-card hero picker) was renumbered to v6.78.1.\n\nAfter deploying this fix and restarting the bot, all 254+ patch notes (v6.74.1 through v7.08) will upsert into the DB in one startup pass, and the site patch notes page will reflect the correct latest version immediately.",
    "author": "System"
  },
  {
    "version": "7.10",
    "title": "Customise the share-card tagline + hide your MMR if you'd rather",
    "published_at": "2026-05-09",
    "content": "Task #270: the per-player Open Graph share card (Tasks #241 / #258 / #259 / #269) always rendered the same auto-generated stats line under the player name — `<tier> · <mmr> MMR · <wins>W <losses>L · <wr>% WR`. There was no way to put a personal tagline on the card or to hide MMR for players who'd rather not advertise the bracket they sit in. v7.08 adds two new owner-controlled extras and renders them on the card.\n\nTwo new fields on the existing `extras` JSONB column (no schema migration): `share_card_tagline` (string, server-trimmed + collapsed-whitespace + capped at SHARE_CARD_TAGLINE_MAX = 40 chars; null/empty keeps the auto stats line) and `share_card_show_mmr` (bool, defaults true). Both validated by `validateExtras()` in `src/profileCosmetics.js` (mirrored to `web/src/profileCosmetics.js` + `DEFAULT_EXTRAS`); the route handler does no Pro-gating — every signed-in player can use them on their own profile.\n\n`src/services/profileOgCard.js` now reads the new fields. When a tagline is set it's rendered in italic 40px Playfair Display (matching the name's serif), wrapped to two lines max, with smart-quotes around it, in place of the auto stat pills. When `share_card_show_mmr` is false the MMR pill AND the tier badge are dropped (the tier name reveals the MMR bracket so hiding both is the honest behaviour) — W/L + win-rate stay since they don't leak MMR. Both new fields are part of the buffer cache key so two players who diverge only on tagline / show_mmr don't collide on the in-memory render cache.\n\nSettings → Profile gains the controls inside the existing 'Share card hero' section: a 40-char text input with a live counter ('Tagline (12/40) — replaces the auto MMR / W-L line'), and a 'Show my MMR & tier on the card' checkbox. The OG endpoint accepts two new owner-only preview query params — `preview_tagline=<text>` (empty string is honoured as 'preview removing the override') and `preview_show_mmr=0|1` — gated by `req.session.accountId === accountId` so a third party can't trick the generator into rendering an arbitrary tagline on someone else's card. Preview responses keep the existing `Cache-Control: no-store` and the picker debounces tagline keystrokes by 350ms so a long tagline doesn't fire a render per character.\n\nFull edition only — community edition has no profile customization or OG card surface. A11y gate green (153 JSX / 3 CSS files scanned).",
    "author": "System"
  },
  {
    "version": "7.07",
    "title": "See your share-card preview right on your own profile page",
    "published_at": "2026-05-09",
    "content": "Task #269: the share-card hero picker shipped in v6.78 (Task #259) only showed the rendered card on the picker itself in Settings → Profile, so players had to dig into settings to sanity-check what their /p/<slug> or /player/<id> link unfurled into. v7.07 surfaces the preview directly on /player/<accountId> for the profile owner.\n\nNew owner-only `<ShareCardPreviewTile>` component in web/src/pages/PlayerProfile.jsx renders a small 1200×630 aspect-ratio thumbnail of `/og/profile/by-id/<accountId>.png` with a brass-accented header ('🖼️ How your share link looks') explaining what the card is and a prominent 'Customize →' button that deep-links to `/settings/profile#share-card` for one-click access to the picker. The tile is gated on the existing `isOwnProfile` check (line 572) so public viewers see nothing — same scoping as InviteLinkCard which it sits next to in the render tree. Image failures fall back to a quiet 'Preview unavailable right now' message via onError so a transient OG-render hiccup doesn't break the rest of the page. A cache-buster `?t=<mount-time>` is appended so the preview always re-fetches on profile-page reload (the OG endpoint marks unauthed responses with a 10-minute public cache, so without the buster a recent share-card hero change wouldn't show up immediately).\n\nSettings → Profile gains an `id=\"share-card\"` anchor on the existing 'Share card hero' section with `scrollMarginTop: 80` so the deep-link from the preview tile lands cleanly under the sticky header instead of being hidden behind it. No other Settings changes — the picker, save flow, validation and OG endpoint are unchanged.\n\nFull edition only — community edition has no profile customization or OG card surface. A11y gate green (153 JSX / 3 CSS files scanned).",
    "author": "System"
  },
  {
    "version": "7.06",
    "title": "Match links unfurl with a real preview card (winner · score · MVP · top fragger)",
    "published_at": "2026-05-09",
    "content": "Task #268: Tasks #241 / #258 added per-player Open Graph cards for `/p/<slug>` and `/player/<accountId>`, but pasting a `/match/<id>` URL into Discord/Twitter still fell back to the bare SPA shell with the OA logo — even though we already record everything needed for a great-looking 1200×630 card.\n\nNew `src/services/matchOgCard.js` mirrors the profile-card shape (same @napi-rs/canvas pipeline, same in-memory cache template, same brass/ink-navy palette, same hero-portrait Steam-CDN slug map shared via `heroImageUrl` from `profileOgCard.js`). It draws the top fragger's hero portrait cover-fill at 0.55 alpha behind a left→right ink-navy gradient, then layers on top: `OCE INHOUSE · MATCH #<id>` eyebrow, the result headline (`RADIANT VICTORY` in green / `DIRE VICTORY` in red — same colour as the side-tinted accent rule on the left), the score in 120px Oswald (`29 — 18`, computed as the sum of per-team kills since `matches` doesn't store the in-game score directly), then a wrap-aware stat strip with `⏱ <duration>` + `MVP: <name> · <hero>` + `Top: <name> <K/D/A>` brass-bordered pills, and a footer with `oceinhouse.gg` on the left + the top-fragger's hero name on the right.\n\nThree new server routes in `src/web/server.js` follow the exact pattern from #258: (1) `_resolveOgMatch(db, matchId)` fans `db.getMatch()` into score totals + top-fragger pick (most kills, ties broken by k+a then by lowest deaths) + MVP lookup (uses the existing `mvp_account_id` returned by `getMatch`); (2) `GET /og/match/:matchId.png` renders + caches the PNG (10-minute public cache, 302→`/oa-logo.png` on any failure); (3) `_sendMatchOgMeta` serves the lightweight HTML with `og:type=article`, `summary_large_image` Twitter card, 1200×630 alt text, canonical link, and a `<meta http-equiv=refresh>` so a human who somehow reaches the URL through a crawler-mimicking client still lands on the SPA. The `/match/:matchId` UA-sniff handler reuses the existing `_isSocialUnfurler` helper and is mounted BEFORE the static asset serve + SPA catch-all, so real browsers always get `next()` → React, and only Discordbot / Twitterbot / Slackbot / facebookexternalhit / Telegram / WhatsApp / Reddit / Skype / Embedly / Iframely / Googlebot get the meta page.\n\nGenerated buffers cache keyed by (matchId, radiantWin, scores, duration, MVP, top fragger KDA + hero) so a re-edited match (Task #157 stats editor) busts the cache automatically. Remote hero portraits cache separately at the `@napi-rs/canvas` Image instance level so repeat unfurls of the same hero are a single canvas encode with no network round-trip. Smoke tested locally with the antimage portrait — 273KB PNG generated in <1s. Full edition only — community edition has no `/match/<id>` SPA route or scoreboard data of this shape.",
    "author": "System"
  },
  {
    "version": "7.05",
    "title": "Restored changelog entry — sign-in self-heal sweep, profile cover top padding, paywall card clears the navbar",
    "published_at": "2026-05-09",
    "content": "Bookkeeping note: a v7.03 patch note was published for these three fixes when they shipped, but a concurrent task (the auth-only superuser route test sweep) re-used the v7.03 slot during its own rebase and the original entry was overwritten in `src/data/patchNotes.js`. The CODE changes from the original v7.03 are still live in production (they were never reverted — only the changelog entry was lost). v7.05 republishes the description so the changelog reads accurately.\n\n**Sign-in retry self-heal — 4-shot backoff sweep.** v6.99 added a single `refreshMe()` retry before showing the 'Sign in didn't complete' banner, on the theory that the SteamAuthProvider mount-time fetch was racing the Set-Cookie header by a few ms. Users reported the banner still firing on slower devices / connections, so the retry was widened: `web/src/App.jsx` `SignInRetryBanner` now sweeps `refreshMe()` at 200ms / 600ms / 1.2s / 2s before falling through to the banner + `/auth/diagnose` ping. Total budget ~2 seconds — short enough that a genuine failure still surfaces promptly, long enough to absorb the cookie-commit gap on slow Set-Cookie commits. The existing `selfHealRef` one-shot guard is preserved (no re-entry loop on React re-renders), and the effect cleanup cancels the in-flight sweep on unmount/navigate.\n\n**Profile cover top padding — bumped 5rem → 7.5rem (mobile 2 → 4.5rem).** `web/src/components/MagazineCover.css` `.v3-cover-inner` was getting visually clipped against the stacked sticky bars (global navbar ~52px + the v3-sticky mini-header ~50px that drops in once the user scrolls past the cover top). The eyebrow row ('PLAYER PROFILE · SIGNATURE: PUCK · MID') and the player name title were sliding under those two bars. New top padding leaves room for both stacked sticky bars + a breathing gap.\n\n**Pro upgrade card sticky offset — bumped `top: 24` → `top: 76`.** `web/src/components/PaywallBlur.jsx`. v6.99 made the upgrade card `position: sticky` so it follows the viewport down a long gated table, but the sticky offset was `top: 24` which meant once you scrolled past the start of the gated section, the card slid under the global sticky navbar (52px tall, z-index 100). New offset = navbar height + 24px gap so the card always parks just below the navbar tabs.\n\nAffects all gated panels (Position Player Profiles, Hero Position Meta, Hero Breakdown, Player Insights, Head-to-Head, Compare Players, Ward Heatmap).",
    "author": "System"
  },
  {
    "version": "7.04",
    "title": "Home page — Most Improved / Player of the Week panel matches What's Hot height",
    "published_at": "2026-05-09",
    "content": "The home page two-up row (`PlayerOfTheWeek` / `MostImprovedFiller` on the left, `HotHeroes` on the right) used `gridTemplateColumns: '1fr 1fr'` so the columns were equal width, but the cards inside each grid cell weren't stretching to the row height — the left card was content-sized (one player line, ~80px tall) while the right card grew to fit four hero tiles (~160px tall). The visual result was an uneven row where the left card floated with empty space below it.\n\nFix in `web/src/components/HomeWidgets.jsx`: the wrapping `<Link>` for `PlayerOfTheWeek` and `MostImprovedFiller` now sets `display: 'block', height: '100%'`, and all three card containers (PlayerOfTheWeek, MostImprovedFiller, HotHeroes) get `height: '100%'` on the inner card div in place of the previous `marginBottom: 16`. With `align-items: stretch` (the grid default), both columns now grow to the taller card's height and the row reads as a single even band.\n\nNo content changes — same data, same colour gradients, same links. a11y gate green (153 JSX / 3 CSS files scanned), web bundle builds cleanly. Full edition only — community edition has no `HotHeroes` widget.",
    "author": "System"
  },
  {
    "version": "7.03",
    "title": "Auth-only test sweep across every superuser admin route",
    "published_at": "2026-05-09",
    "content": "Task #267: Task #257 locked in 401/403/200 coverage for the three Founders Pass admin routes (GET/POST/DELETE /api/admin/founders-ring). The same elevation-of-privilege failure shape — a refactor accidentally dropping `requireSuperuser` from a route — applies equally to the 99 other admin/superuser routes mounted in `src/web/server.js` (backups, replay management, season close/announce, manual match insertion, recompute-achievements, weekend tournaments, dedicated server controls, inhouse orchestration, feature flags, coaching dispute resolution, etc.). Without coverage any of them could silently lose its auth gate and the deploy gates would still pass green.\n\nNew test file `tests/superuserAdminRouteAuth.test.js` boots `createApiRouter()` with the existing harness and runs TWO assertions:\n\n1. **Manifest-vs-live diff.** A checked-in `EXPECTED_SUPERUSER_ROUTES` constant lists every (method, path) currently gated by `requireSuperuser` — all 99 of them. The test walks the live router stack, collects everything whose middleware chain contains a `requireSuperuser` layer, and diffs the two sets symmetrically: routes in the manifest but missing from the live router (gate dropped → fail deterministically, even for a SINGLE route regression — this is the canonical anti-regression signal), and routes in the live router but missing from the manifest (new admin route added without updating the manifest → also fails so the author consciously enrolls it in the sweep).\n\n2. **Per-route 401/401/403 probe.** For every (method, path) in the manifest, the test asserts the three branches in `requireSuperuser`: anonymous caller → 401, signed-in non-superuser (session.accountId set, no isSuperuser flag) → 401, caller presenting a wrong `x-superuser-key` header value → 403.\n\nReusable harness pieces from `tests/foundersRingCheckoutWebhook.test.js` (`stubModule`, `_stubServerDeps`, `_invokeHandler`, `_withUnreffedIntervals`, `_loadServerFresh`, `_withSuperuserPassword`) lifted into `tests/fixtures/serverHarness.js` so future auth-gate tests don't need to re-paste the boot sequence. The original webhook test file is left intact (still passes its 19 subtests) — the lift is additive.\n\nProbe allow-list is empty: every superuser-gated route is in scope. The Founders Pass admin routes also keep their per-route happy-path coverage in the existing webhook test file — that complements rather than replaces the auth-only sweep. New test runs in ~550ms, all passes.",
    "author": "System"
  },
  {
    "version": "7.02",
    "title": "Superuser audit page for Founders Pass cap-race refunds",
    "published_at": "2026-05-09",
    "content": "Task #265: v6.63 / Task #207 added the Founders Pass cap-race auto-refund flow and v6.74 (Task #256) added the `founders_ring_refunds` audit table + `db.listFoundersRingRefunds({ limit })` helper, but operators could only see refund outcomes by SSHing into the box and querying Postgres. There was no UI to confirm at a glance that every over-cap buyer was actually refunded, no surfacing of rows stuck in `status='refund_failed'`, and no click-through to the matching Stripe session.\n\nNew superuser-only API route `GET /api/admin/founders-ring-refunds` (in `src/web/server.js`, requireSuperuser-gated alongside the other admin audit endpoints) accepts an optional `?limit=` (clamped 1–1000, default 200) and proxies the existing `db.listFoundersRingRefunds({ limit })` helper, returning `{ refunds, limit }`.\n\nNew `FoundersRingRefunds` section in `web/src/pages/AdminPanel.jsx` under the Marketplace tab (next to Gift Purchases) renders a sortable table — Date, Account, Amount (with currency), Status, Refund (linked to `https://dashboard.stripe.com/refunds/<id>`), Session (linked to `https://dashboard.stripe.com/payments/<session_id>`), Error message. Sorting uses the shared `<SortableTh>` accessibility primitive so screen readers get a real `aria-sort`. Failed rows (`status='refund_failed'`) get a red row tint, a red status badge, and the failed-count is summarised in the section header (`⚠ N failed`) so a stuck refund is impossible to miss. The empty state confirms green that every Founders Pass purchase landed under the cap. Wired into the existing AdminPanel search index (kw: founders ring refund cap race stripe audit failed) so it's discoverable from the panel-wide jump search.\n\nFull edition only — the community edition has no Founders Pass / shop wiring. `npm run check:a11y` green (153 JSX / 3 CSS files scanned).",
    "author": "System"
  },
  {
    "version": "7.01",
    "title": "Live Now tab grouped into Inhouse · Other Dota · Lobby/Queue/Voice with full inhouse roster cards",
    "published_at": "2026-05-09",
    "content": "Players → Live now used to be one flat grid where five teammates in the same inhouse game showed up as five separate cards — visitors had to mentally re-stitch \"ah, those five share match #12345, that must be one game.\" v7.01 reshapes the tab into three labelled sections and collapses each inhouse game into ONE card with the full inline roster.\n\n**Inhouse** — any group of 2+ visible live players who share the same `match_id` from Steam rich-presence is rolled into a single card. The card shows an `INHOUSE · N PLAYERS` chip + match id, one Watch / Copy-watch-id button for the whole game, and below that a stacked list of every player in the match: small hero icon + player name (linked to their profile) + hero name. Match IDs of `0` / null are never used as a group key, so multiple \"in_game but no spectator hook yet\" players don't collide into a phantom inhouse.\n\n**Other Dota** — solo `in_game` players (typically pubs not relevant to the league) plus everyone in `in_lobby`. One card per player, same shape as the previous flat grid.\n\n**Lobby · Queue · Voice** — the inhouse-queue + Discord-voice bucket (`in_queue` + `in_voice`). One card per player, unchanged shape.\n\nGrouping happens entirely client-side in `web/src/pages/Players.jsx` (`bucketLivePlayers` helper + new `InhouseGameCard` + `LivePlayerGrid` + `SectionHeading` components) — the existing `/api/presence/live` endpoint and `getAllLivePresences()` service are untouched, so no backend change and no extra round-trips. Section headings are real `<h2>` elements with a count chip, not click-handler `<div>`s. a11y gate green (153 JSX / 3 CSS files scanned), web bundle builds cleanly. Full edition only — community edition has no Live now tab.",
    "author": "System"
  },
  {
    "version": "7.00",
    "title": "Match pages get a Share button so players can post big wins",
    "published_at": "2026-05-09",
    "content": "Task #260: Task #242 added a Share popover to the player profile page; the same shape was missing on individual match pages, so a player who just popped a 25-2 game had no one-click way to brag with a link that unfurls back to the match scoreboard.\n\nMatchDetail.jsx now renders a `🔗 Share` button next to the existing `← Back to matches` / Edit Stats / Download Replay header controls, and clicking it opens a small anchored popover via the shared `<Dialog>` primitive (focus trap, Escape to close, body-scroll lock, focus restore — same a11y gate compliance as the profile share button). The popover offers three actions identical in shape to the profile version: **Share on Twitter / X** (opens the X intent with prefilled text + the canonical match URL), **Copy for Discord** (Discord-friendly markdown with the URL wrapped in `<…>` to suppress the bare unfurl when desired), and **Copy link**.\n\nThe prefill text is match-aware. When the signed-in viewer was actually one of the ten players in the match (looked up by matching `steamUser.accountId` against `match.players[].account_id`), the tweet/Discord text reads `Just won: 25/2/14 on Anti-Mage — Radiant wins on OCE Inhouse:` (or `Match recap:` if their side lost) — pulling KDA + the formatted hero name + the actual winning side. For signed-out viewers or spectators not in the match, it falls back to `Radiant wins inhouse match <matchId> on OCE Inhouse:`. Hero name uses the existing `formatHeroName(getHeroName(hero_id))` pipeline already imported by the page so there's no new dependency.\n\nButton has `aria-label='Share this match'`, `aria-haspopup='dialog'`, and `aria-expanded` reflecting popover state; the popover heading is `id='match-share-popover-title'` and wired via `labelledBy` so the dialog announces itself correctly. Copy actions show a brief `✅ Copied!` flash then auto-close. a11y gate green (153 JSX / 3 CSS files scanned).",
    "author": "System"
  },
  {
    "version": "6.99",
    "title": "Match list shows hero icons + top fragger, Pro paywall card sticks to top, sign-in banner self-heals",
    "published_at": "2026-05-09",
    "content": "Three UX fixes from a prod walk-through.\n\n1) **Match History list — every card now shows hero icons and top-fragger lines for both teams.** The list previously read just `#match_id · Radiant Victory · 40:50 · 10 players`, which made it impossible to tell games apart at a glance — you had to click through to remember whether a given match was the one with Hoodini on Storm or the one with Carmine on Lina. Each card now renders a compact two-row strip below the body line: `RADIANT [5 hero icons] top: <name> · K/D/A` and `DIRE [5 hero icons] top: <name> · K/D/A`. The losing side is dimmed to 0.7 opacity so the winner reads first. Server side, `db.getMatches` now returns a `players` JSON array per match (team, hero, kills, deaths, assists, account_id, nickname — joined to `nicknames`, ordered Radiant→Dire then kills DESC) via a single subquery aggregate so the list still issues just one query for 20 matches — no N+1 follow-ups. Defensive on the frontend: if `players` is null (older row, zero parsed players), the strip renders nothing and the rest of the card stays valid.\n\n2) **`odota-parser` badge hidden for non-superusers.** The pipeline-source pill (`odota-parser`, `live-gc`, etc.) was visible to every visitor on every match card — it's an internal detail nobody outside the admin team cares about and it cluttered the row next to the season chip. Now superuser-only so admins can still spot a stalled or skipped parse from the list.\n\n3) **Pro feature paywall card sticks to the top of the gated section.** `PaywallBlur` (used on the Position Player Profiles tab, Hero Position Meta, Hero Breakdown, Player Insights, Head-to-Head, Compare Players, Ward Heatmap) used to centre the upgrade card vertically against the WHOLE blurred body. On long pages — Position Player Profiles is ~80+ rows tall — the card landed below the fold and users had to scroll to discover the page was even gated, then assumed it was just slow-loading data. The card now anchors to the top of the gated area AND uses `position: sticky; top: 24px` so as the user scrolls down through the blurred preview the card tracks with the viewport. Always in eye-shot, no scroll needed. The full-width gradient wash stays put as before.\n\n4) **Sign-in retry banner self-heals before showing.** The 'Steam confirmed your sign-in but the session didn't stick' banner was firing on prod even when the session HAD landed correctly — `/auth/diagnose` was reporting `session-exists=true session-accountId=…` for the same browser that was showing the banner, proving the server-side session was fine. Most likely cause: SteamAuthProvider's mount-time `/api/auth/me` fetch races the session-cookie write by a few ms (the redirect from `/auth/steam/return → /?auth=success` can arrive at the SPA fractionally before the Set-Cookie header is committed by some browsers/proxies). `SignInRetryBanner` now does ONE self-heal refetch via `refreshMe()` before surfacing the banner; if the second fetch returns the user, the banner stays hidden and the SPA syncs to the real session. If the refetch still returns null we fall through to the existing diagnose-then-banner path so a real failure is still surfaced. One-shot guarded behind a `useRef` so the refetch can't loop.",
    "author": "System"
  },
  {
    "version": "6.97",
    "title": "Discord ID leak closed, Top 5 sidebar fixed, Home spotlight de-duped, profile cover breathes",
    "published_at": "2026-05-09",
    "content": "Five fixes prompted by a prod walk-through.\n\n1) **Security — Discord IDs no longer leak on the public Players page.** `GET /api/players` was returning every signed-up player's `discord_id` in the JSON payload (used internally for bot DMs), and the Players table rendered the first 8 chars to anyone who loaded the page — anonymous visitors included. Full IDs were also visible in DevTools. Not catastrophic on their own but they're a stalking-aid (anyone could DM the user out-of-band) and the threat model explicitly forbids leaking operator-only identifiers to unauthenticated callers. Fixed both editions: the route now strips `discord_id` from every row unless `req.session.isSuperuser`, and the table column is hidden for non-superusers. Superusers see and edit it exactly as before.\n\n2) **Bug — Home page \"Top 5 Players\" sidebar showed 'Player undefined' for every entry.** The widget was using `p.player_name || p.nickname || p.persona_name || \\`Player ${p.account_id}\\`` but `getComputedLeaderboard()` returns `{ player_id, display_name, nickname, ... }` — none of `player_name`/`persona_name`/`account_id` ever existed on those rows, so the fallback always landed on the `account_id`-undefined branch. Switched to `p.nickname || p.display_name || \\`Player ${p.player_id}\\`` and rewired the profile link to use `p.player_id`. Now reads correctly (e.g. \"MajinDabura · 16W–2L · 7265 MMR\").\n\n3) **UX — \"Featured Player\" and \"Player of the Week\" no longer double up on the same person.** The two cards on the home page are sourced from different endpoints (`/api/spotlight/current` is admin-curated with a PERF-leader auto-fallback; `/api/home/perf-spotlight` is the pure 7-day PERF leader). On a quiet week with no admin pick, the spotlight's auto-fallback chose the same person the PERF widget already had, so the same player rendered twice in a row. PlayerOfTheWeek now fetches both endpoints in parallel and, when the account IDs collide, swaps its slot to a new **\"📈 Most Improved\"** filler card backed by `/api/most-improved?days=7` (biggest 7-day MMR climber). When PERF data is missing entirely on quiet weeks the same filler takes over. The slot stays useful instead of going blank.\n\n4) **UX — Player profile cover header was clipping at the top.** The eyebrow row (\"PLAYER PROFILE · SIGNATURE: …\") and the player name were sitting hard against the broadcast ticker / nav rail at standard viewport heights. Bumped `.v3-cover-inner` top padding from `3rem` to `5rem` (bottom unchanged) so the cover gets breathing room without changing the cinematic look.\n\n5) **Footer — removed the placeholder Discord link.** It was pointing at the bare `https://discord.gg` (no invite) so it never actually went anywhere useful. Footer now just shows version + Patch notes link.",
    "author": "System"
  },
  {
    "version": "6.98",
    "title": "Canonical /player/<id> links now unfurl with the same hero-portrait card as /p/<slug>",
    "published_at": "2026-05-09",
    "content": "Task #258: v6.92 (Task #241) added a 1200×630 hero-portrait OG card to /p/<slug> unfurls, but the canonical /player/<accountId> URLs (the long URL most signed-out visitors copy from the address bar) still served the bare SPA shell with the generic site logo. Pasting the long URL into Discord/Twitter produced a worse-looking card than pasting the short one for the same player.\n\nFix in src/web/server.js: extracted the existing OG-card render path into two reusable pieces — `_renderProfileOgCard(res, db, accountId)` (the canvas+cache path, now shared between the slug card endpoint and the new by-id endpoint) and `_sendProfileOgMeta(req, res, db, accountId, slug)` (the meta-tag HTML responder, now slug-optional). Two new routes mounted before the SPA catch-all: `GET /og/profile/by-id/:accountId.png` resolves the player by account id and returns the same per-player rendered PNG (cache key in src/services/profileOgCard.js is keyed off display name + hero, so both URL shapes for the same player reuse a single cached buffer — no extra render cost), and `GET /player/:accountId` sniffs the User-Agent for the same well-known social bot list (Discordbot, Twitterbot, Slackbot, facebookexternalhit, LinkedInBot, TelegramBot, WhatsApp, RedditBot, SkypeUriPreview, Embedly, Iframely, Googlebot — case-insensitive substring match). Crawler UAs get the OG meta response; real browser UAs fall through (`next()`) to the SPA catch-all so the React app boots normally — there is zero behavioural change for human visitors.\n\nWhen the player has claimed a slug we look it up via `db.getVanitySlugByAccount(accountId)` and point the og:image + canonical URL at the short `/p/<slug>` path so both URL shapes deduplicate to the same canonical link in crawler indexes. When no slug is claimed, the canonical points at the long URL and og:image points at the new `/og/profile/by-id/<accountId>.png` endpoint. The `profile:username` meta tag is omitted in the no-slug case (it's defined as a slug, not an id).\n\nCoverage in tests/vanitySlugUnfurl.test.js: 4 new tests covering Discordbot UA on /player/<accountId> reusing the slug card when one exists, real Chrome UA falling through (no headers sent, no redirect), the no-slug path using the by-id endpoint and dropping `profile:username`, and the non-numeric account-id path falling through. All 9 tests pass. A11y gate green (153 JSX/3 CSS files scanned). Full-edition only — community edition has no vanity slug or OG card surface.",
    "author": "System"
  },
  {
    "version": "6.96",
    "title": "Founders Pass admin routes — superuser-only authorization locked in by tests",
    "published_at": "2026-05-09",
    "content": "Task #257: v6.95 (Task #240) covered the public Founders Pass checkout route and the Stripe webhook, but the three superuser admin routes — `GET /api/admin/founders-ring` (list holders), `POST /api/admin/founders-ring` (manual grant), `DELETE /api/admin/founders-ring/:accountId` (revoke) — had zero automated coverage. If a future refactor accidentally dropped `requireSuperuser` from any of them, any signed-in (or even anonymous) user could mint or revoke founders rings on demand — the elevation-of-privilege failure shape the threat model is built to catch.\n\n`tests/foundersRingCheckoutWebhook.test.js` got 11 new cases (now 19 total, all green): for each of the three routes, an anonymous caller (no session, no header) is rejected (401/403) and the underlying db helper is NOT called; a signed-in non-superuser presenting a wrong `x-superuser-key` header is rejected (401/403) and the db helper is NOT called. Three superuser happy-path cases then assert: GET returns `{sku, cap, sold, holders}` from `db.listEntitlementHolders`; POST calls `db.grantEntitlementWithCap` with `grantedBy: 'superuser'` and the operator-supplied `metadata.reason` (plus a default-to-'admin_grant' case and a 400 missing-account_id case); DELETE calls `db.revokeEntitlement` with the right `accountId` + sku and returns `{ok:true, removed}`. A new `_withSuperuserPassword` helper sets `SUPERUSER_PASSWORD` for the lifetime of each test (since `requireSuperuser` short-circuits with 503 when unset).\n\nAlso patched a latent hang in the shared `_invokeHandler` test helper: it only resolved its inner promise via the `next()` callback or a returned Promise, so a sync middleware that ends the response without calling `next()` (exactly the `requireSuperuser` reject path: `return res.status(401).json(...)`) used to leave the test loop pending. It now also resolves when `res.headersSent` is true after a sync middleware returns. No production code changes — pure test additions + harness fix.",
    "author": "System"
  },
  {
    "version": "6.95",
    "title": "Founders Pass cap-race buyers are now auto-refunded",
    "published_at": "2026-05-09",
    "content": "Task #256: when a paid Founders Pass checkout loses the cap race (`grantEntitlementWithCap` returns `reason='cap_reached'` AFTER funds settled), the Stripe webhook used to log a loud 'REFUND REQUIRED' line and stop — an operator had to spot the log and refund manually, which left buyers waiting and trusting the team to notice.\n\nThe `purpose='founders_ring'` branch in `src/web/server.js` (~line 768) now closes the loop: on `cap_reached` it calls `stripe.refunds.create({ payment_intent: session.payment_intent, metadata: { reason: 'founders_ring_cap_race', account_id, stripe_session_id } })`, persists the outcome via the new `db.recordFoundersRingRefund(...)` helper into a new `founders_ring_refunds` table (id, account_id, sku, stripe_session_id [UNIQUE — idempotent on Stripe webhook retry], stripe_payment_intent, stripe_refund_id, amount_cents, currency, status ['refunded' | 'refund_failed'], error_message, created_at), and DMs the buyer through a new `bot.notifyFoundersRingRefund({ accountId, amountCents, currency, refundId })` method on the Discord bot which looks up `discord_id` via `db.getDiscordIdByAccountId` and sends an apology message with the refunded amount + refund reference. The DB insert is best-effort (a DB failure logs but does not block the webhook ack), the DM is fire-and-forget. If the Stripe refund call itself throws, the audit row is still written with `status='refund_failed'` + the error message, then the handler re-throws so the outer webhook catch returns 400 and Stripe redelivers (the audit row's UNIQUE on `stripe_session_id` keeps replays clean).\n\n`tests/foundersRingCheckoutWebhook.test.js` got two new cases on top of the existing 6: (a) cap-race success — drives `grantEntitlementWithCap` returning cap_reached and asserts `stripe.refunds.create` was called against the right PI with the right metadata, `recordFoundersRingRefund` was called once with `status='refunded'` + the refund id, the bot's `notifyFoundersRingRefund` was called with the buyer's account + amount + refund id, and the webhook returned 200; (b) cap-race refund failure — drives the Stripe stub to throw and asserts the audit row is still recorded with `status='refund_failed'` + error message and the webhook returns 400 so Stripe retries. All 8 tests green. New `db.listFoundersRingRefunds({ limit })` is exported alongside for future superuser tooling.",
    "author": "System"
  },
  {
    "version": "6.94",
    "title": "Time-of-day heatmap — slate background so green/red W-L pop",
    "published_at": "2026-05-09",
    "content": "v6.90's brass cell tint sat too close to red on the colour wheel — the L (red) text inside busy cells started to clash against the warm background. v6.94 swaps the cell tint to slate (rgb(148, 163, 184) — cool desaturated grey, the canonical 'neutral background for a coloured-text heatmap' choice). Slate sits on the cool axis perpendicular to both red and green, so neither hue competes for attention; the W (green) and L (red) numbers now pop cleanly against any cell density. Same 0.15 → 0.90 opacity ramp as v6.90, legend swatch row recoloured to match. Brass remains the magazine-wide accent (panel borders, hero-card glow, etc.) — only the heatmap cell + its legend swatches changed.",
    "author": "System"
  },
  {
    "version": "6.93",
    "title": "Test coverage for Stripe refunds, KYC, and PI safety-net webhooks",
    "published_at": "2026-05-09",
    "content": "Task #255: Task #237 (v6.78-era) locked in checkout.session.completed (paid/unpaid) and the BECS async-payment branches of the /api/stripe/webhook handler, but the *other* branches in the same handler (src/web/server.js ~lines 920-1000) were untested — a future refactor of any of refunds, KYC promotion, or the manual-capture safety nets could silently break payouts and the deploy gate would stay green.\n\nNew tests/stripeWebhookRefundsKyc.test.js (14 cases, all green) reuses the same require.cache stub harness shape from tests/stripeWebhookAsyncPayment.test.js (stubs stripe, db, replay parser, stats service, groq, discord bot, voice queue, monetization/magazineV3, profileCosmetics, connect-pg-simple; reads the route off app._router; invokes only the final route handler so it skips express.raw streaming) and asserts five branches end-to-end:\n\n(1) `charge.refunded` with a payment_intent calls BOTH `db.markProRefunded(pi)` AND `db.markBookingRefundedByIntent(pi)` so a single Stripe refund flips whichever row matches; a charge WITHOUT a payment_intent triggers neither. (2) `payment_intent.succeeded` with `metadata.purpose='coaching_booking'` calls `db.markBookingCompletedByIntent(intent.id)` (PI safety net for manual capture); other purposes / missing metadata are no-ops. (3) `payment_intent.canceled` with the same coaching purpose calls `db.markBookingRefundedByIntent(intent.id)` (PI safety net for refunds); other purposes are no-ops. (4) `account.updated` promotes the coach via `db.setCoachKycActive(acct.id)` ONLY when BOTH `charges_enabled` AND `payouts_enabled` are true — three negative cases (charges-only, payouts-only, neither) confirm the AND gate, locking in the documented 'don't take bookings we can't pay out' rule. (5) `checkout.session.expired` with `purpose='coaching_booking'` calls `db.markBookingCancelledBySession(session.id)` to free the slot; other purposes / missing metadata are no-ops.\n\nNo production code changes — pure test additions. Keeps the manual-refund / dispute-refund / no-show-refund flows from regressing without a deploy-time signal.",
    "author": "System"
  },
  {
    "version": "6.91",
    "title": "Live-now badge now shows on phones too",
    "published_at": "2026-05-09",
    "content": "Task #248: the v6.78 'Live now' pulse-dot + count badge added in Task #227 lived inside the desktop Players nav link, which sits in `.nav-links` — and that container wraps to a full-width row beneath the brand at <=900px. On phones the badge ended up below the fold, so casual visitors browsing on mobile (the device most casual fans use) never saw the spectator hook unless they scrolled the navbar.\n\nFix: extracted the polling logic into a shared `useLivePresenceCount` hook (no behavioural change for desktop — same /api/presence/live/count poll every 30s while the tab is visible) and added a sibling `<MobileLiveBadge />` component that renders directly in the navbar's always-visible top row, right after the brand lockup. The mobile pill matches the desktop badge's green pulse styling, links to `/players?tab=live` (same deep-link as the desktop badge), and renders nothing when zero players are live so the navbar stays compact. CSS in `web/src/styles.css` reveals it with `display: inline-flex !important` inside the existing `@media (max-width: 900px)` block (the same breakpoint where `.nav-links` wraps), and hides the duplicate badge inside the wrapped Players link via a new `.nav-live-badge` class so the count never shows twice. Tap target is a real `<Link>`, has an `aria-label` describing the count and the destination, and the pulse dot is `aria-hidden`.\n\nDesktop nav is visually unchanged. a11y gate green (153 JSX / 3 CSS files). Full-edition only — community edition has no `/players?tab=live` surface.",
    "author": "System"
  },
  {
    "version": "6.90",
    "title": "Time-of-day heatmap — restore the proper volume ramp (still in brass)",
    "published_at": "2026-05-09",
    "content": "v6.88 dropped the heatmap's brightness ramp to 'whisper-soft' (0.05 → 0.15) on the theory that the W-L tally inside each cell made the volume signal redundant. In live use that read as 'every cell looks the same' — the scan-pattern of busy vs quiet hours was lost and the grid stopped feeling like a heatmap at all. v6.90 restores the proper ramp.\n\nCell tint stays brass (`rgba(197, 169, 117, X)` — `--brass = #c5a975`, on-palette with the rest of the magazine, no return to the off-palette blue of v6.82). Opacity range goes from `0.15 → 0.90`: the floor is high enough that even a 1-game cell is clearly visible against the panel background, the ceiling stops just short of fully solid (0.90 instead of 1.0) so the W (green) / L (red) text inside the busiest cells still has enough contrast to read against the brass. Legend swatch row rescaled to the same `[0.15, 0.4, 0.65, 0.9]` stops so the legend accurately previews what cells look like.\n\nNet: low games = transparent, many games = nearly solid brass — the heatmap reads as a heatmap again, while the W-L numbers inside still tell you the precise count.",
    "author": "System"
  },
  {
    "version": "6.89",
    "title": "Test coverage for the vanity-link unfurl card and !whois lookup",
    "published_at": "2026-05-09",
    "content": "Task #243: Task #221 wired up Open Graph + Twitter meta tags on `/p/<slug>` and a `!whois` Discord command, and Task #241 generated per-player OG card images, but neither path had any automated coverage. A future refactor of the `/p/:slug` route, the bot's `_resolveVanityLookup` slug-extraction regex, or the new `/api/player/:id/vanity-slug` endpoint could silently break Discord/Twitter unfurls or the lookup command and shipping would still pass.\n\nNew `tests/vanitySlugUnfurl.test.js` (5 cases, all green) locks the contract end-to-end through the real route handlers. (1) A Discordbot UA on `GET /p/testpro` gets a 200 HTML response (not a 302 — Discord follows redirects to the SPA, which has no per-player meta) with `og:title` containing the display name, `og:description`, `og:image` pointing at `/og/profile/testpro.png` (the per-player generated card from Task #241), `twitter:card=summary_large_image`, `twitter:title`, `twitter:image`, and a canonical `/p/<slug>` link. (2) A real Chrome UA on the same route gets the fast 302 redirect to `/player/12345`. (3) `GET /api/player/:id/vanity-slug` returns `{slug:'testpro'}` for a claimed account and (4) `{slug:null}` for an unclaimed account (so the profile page's Share button can fall back to the canonical `/player/<id>` URL without a 404). (5) A focused unit test exercises the slug-extraction regex from `_resolveVanityLookup` (`/(?:\/p\/|^p\/)([A-Za-z0-9-]+)/`) across a bare slug, mixed-case slug, `/p/slug` shorthand, `p/slug` without leading slash, full URL, full URL with trailing slash, whitespace-padded input, and empty input — kept as a focused unit test rather than booting the full Discord bot, which pulls steam-user, dota2-user, and a live discord.js Client.\n\nHarness reuses the `require.cache` stub pattern from `tests/foundersRingCheckoutWebhook.test.js`: stubs heavy transitive modules (replay parser, stats service, groq, discord bot, voice queue, monetization/magazineV3), stubs the `../db` module via require.cache with the *real* `isWellFormedVanitySlug` validator copied in (a dumb `() => true` stub would mask a regression in the validator) plus capturing helpers, supports both express 4 (`app._router`) and express 5 (`app.router`) for `_findAppRoute`, and `unref()`s the long-running setIntervals so node:test exits cleanly. Full-edition only — community edition has no vanity slugs surface."
  },
  {
    "version": "6.88",
    "title": "Time-of-day heatmap — brass tint, whisper-soft volume ramp",
    "published_at": "2026-05-09",
    "content": "Two follow-up adjustments to the v6.82 heatmap on /player/:id (Magazine v3) after live use.\n\n1. **Dropped the neutral blue background.** Blue is the only colour on the magazine page that wasn't from the brand palette (Hybrid · Court & Pitch is ink-navy + brass + amber + parchment), so it stuck out against the rest of the layout. Cell tint switched from `rgba(96, 165, 250, X)` to `rgba(197, 169, 117, X)` — the brass token (`--brass = #c5a975`) — which matches the rest of the magazine accents and reads as warm 'court parquet' rather than introducing a third dominant hue. The W (green) and L (red) text inside the cell still pop the same way against the warmer background.\n\n2. **Made the volume ramp barely-there.** Now that each cell shows the actual `WW-LL` tally inside, the brightness ramp was redundant — anyone who wants to know how busy an hour was just reads `wins + losses`. Dropped opacity range from `0.18 → 1.0` down to `0.05 → 0.15` so the ramp is just enough to give the grid a 'scan pattern' (hot zones spottable from a distance) without claiming a precise volume scale the numbers already give. Legend swatch row recoloured to brass and rescaled to the new range so the legend matches what the cells actually look like. Sample W-L chip background switched from blue to a brass-bordered ink-navy to match.\n\nFull-edition only — community edition has no Magazine v3 panels. A11y gate green (153 JSX / 3 CSS), web bundle builds cleanly.",
    "author": "System"
  },
  {
    "version": "6.87",
    "title": "One-click share to Twitter/X and Discord from your profile",
    "published_at": "2026-05-09",
    "content": "Task #242: the 🔗 Share button on /player/:id (and /p/:slug) just copied the vanity URL to the clipboard — most users who hit it intend to paste it somewhere immediately, so the extra step (switch tab → click compose → paste) was pure friction. v6.87 turns the button into a small share popover with three quick-share options.\n\nThe button now opens a Dialog (uses the shared `<Dialog>` primitive from web/src/components/Dialog.jsx — focus trap, Escape-to-close, focus restore, body-scroll lock, gates the hand-rolled-modal a11y check). Three actions inside: (1) **Share on Twitter / X** — opens `https://twitter.com/intent/tweet?text=…&url=…` in a new tab, pre-filled with `Check out <displayName> (<MMR> MMR) on OCE Inhouse:` plus the vanity URL (so the unfurl card from Task #241 does the heavy lifting on the embed). (2) **Copy for Discord** — copies a chat-friendly markdown string (`Check out **<name>** (<MMR> MMR) on OCE Inhouse: <url>`) with the URL wrapped in `<…>` so Discord still unfurls it but doesn't double-render the link text. (3) **Copy link** — preserves the original raw-URL clipboard behaviour for users who just want the bare link. All three prefer the short `/p/<slug>` vanity URL when one is claimed (Task #221) and fall back to the canonical `/player/<accountId>` URL otherwise. MMR comes from the existing `seasonMmr` / `rating.mmr` fields already on the page — no new API calls.\n\nThe trigger button gains `aria-haspopup=\"dialog\"` + `aria-expanded` so screen readers announce the popover relationship. Each action button shows a transient ✅ Copied confirmation for 2s after a successful clipboard write (with `window.prompt` fallback for browsers that block clipboard writes). a11y gate green (153 JSX / 3 CSS files), bundle compiles cleanly. Full-edition only — community edition has no vanity slugs / share button on its profile page.",
    "author": "System"
  },
  {
    "version": "6.86",
    "title": "Shared profile links unfurl with the player's hero portrait",
    "published_at": "2026-05-09",
    "content": "Task #241: when you pasted a `/p/<slug>` vanity profile link into Discord/Twitter, the unfurl card (shipped in v6.75 / Task #221) used the generic OA logo as `og:image` for every player. v6.85 swaps the logo for a per-player 1200×630 card built from the player's real hero artwork, so the unfurl actually advertises *who* the link is for.\n\nNew `src/services/profileOgCard.js` renders the card via @napi-rs/canvas (already a dep for scoreboard images): the chosen hero portrait is fetched from the Steam CDN slug map (mirror of `web/src/heroNames.js` HERO_ID_TO_SLUG, kept server-side so the route doesn't need a frontend bundle), drawn cover-fill with a 0.55 alpha wash, then layered under a left→right ink-navy gradient (0.95 → 0.35) so the text side stays legible. On top: 'OCE INHOUSE' eyebrow in brass, the player display name in 96px Playfair Display (wraps to 2 lines / hard-truncates with ellipsis), then a brass-bordered stat strip with the MMR tier name + '<n> MMR' + '<w>W <l>L' + '<wr>% WR' pills, and a footer with `oceinhouse.gg` on the left and the hero name on the right. Generated buffers cache in-memory keyed by (display name, hero, MMR, W/L, tier) for 10 minutes / 200 entries; remote hero portraits cache separately as @napi-rs/canvas Image instances so repeat unfurls of the same player are a single canvas encode with no network round-trip.\n\nNew route `GET /og/profile/:slug.png` in `src/web/server.js` resolves the slug → account_id, picks the hero (`_resolveOgProfileHero`: pinned hero from `player_profiles.pinned_hero_id` first, then most-played hero from `player_stats GROUP BY hero_id ORDER BY games DESC LIMIT 1`), pulls nickname + rating + MMR tier, and serves the PNG with a 10-minute `Cache-Control` header. Any failure path (unknown slug, canvas unavailable, hero fetch timeout, render error) 302-redirects to the existing `/oa-logo.png` so unfurlers always get *some* valid image.\n\n`_sendVanityOgCard` now points `og:image` and `twitter:image` at the new endpoint, adds `og:image:width=1200` / `og:image:height=630` / `og:image:alt`, and upgrades `twitter:card` from `summary` to `summary_large_image` so Twitter renders the wide card layout the dimensions are sized for. The signature hero name also joins the OG description string when one is known. Players with no pinned hero AND no recorded matches get a tasteful logo-only fallback card (faded centred OA logo on the same ink-navy background) instead of the bare logo PNG. Full-edition only — community edition has no vanity slugs surface.",
    "author": "System"
  },
  {
    "version": "6.85",
    "title": "Post-merge auto-push self-heals when origin has been re-committed",
    "published_at": "2026-05-09",
    "content": "Recurring failure mode in scripts/post-merge.sh: after every merge, the platform also commits + pushes to origin/main on its own checkout under a different SHA. Our local checkout never sees that ref because the Replit sandbox blocks every git op that touches `.git/refs/remotes/origin/*.lock` or `.git/objects/maintenance.lock`, so `git fetch origin main` cannot run here. The next post-merge then tried `git push origin HEAD:main` against an origin that was strictly ahead and got rejected with non-fast-forward — the deploy hook reported FAIL and the work needed a manual force-with-lease push every time.\n\nv6.85 makes the hook self-healing. On a non-fast-forward rejection: (1) read the real remote tip via `git --no-optional-locks ls-remote origin main` (read-only, no ref locks), (2) fetch ONLY that object via `GIT_OPTIONAL_LOCKS=0 git -c gc.auto=0 -c maintenance.auto=false fetch --no-auto-gc --no-auto-maintenance --no-write-fetch-head origin <SHA>` (the fetch-by-SHA + maintenance-disabled combo is the one shape that bypasses the sandbox lock interceptor), (3) compare local HEAD's tree to the remote tip's tree. If they match exactly, force-with-lease push HEAD onto main and we're done. If they differ, the only diff allowed is paths under `artifacts/mockup-sandbox/src/.generated/` (the auto-regenerated mockup index — the platform's checkout regenerates 12 lines on its side); any real source-file divergence aborts the self-heal so we never clobber legitimate remote work.\n\nNet effect: the platform-recommit-then-our-merge-rejected loop now resolves itself on the next post-merge run, no manual intervention needed. The `--force-with-lease=main:<remote_sha>` makes the push a true compare-and-swap so concurrent third-party pushes still cause the heal to fail safe.",
    "author": "System"
  },
  {
    "version": "6.84",
    "title": "Test coverage for the Founders Pass checkout + webhook flow",
    "published_at": "2026-05-09",
    "content": "Task #240: Task #220 added unit + route tests for grantEntitlementWithCap and the cover_fx Pro-gate, but the actual Stripe wiring around the Founders Pass — POST /api/shop/founders-ring/checkout and the purpose==='founders_ring' branch in /api/stripe/webhook — still had zero automated coverage. A future webhook refactor could quietly skip the cap call or stop marking the entitlement granted and shipping would still pass.\n\nNew tests/foundersRingCheckoutWebhook.test.js (6 cases, all green) locks both halves down end-to-end through the real route handlers. For POST /api/shop/founders-ring/checkout: (1) 401 when not signed in, (2) 409 + already_owned:true when db.hasEntitlement reports the caller already owns founders_pass_ring AND the Stripe SDK is never called, (3) 409 + sold_out:true when countEntitlementHolders >= FOUNDERS_RING_CAP (set to 5 for the test) AND no Stripe call, (4) the happy path calls stripe.checkout.sessions.create exactly once with mode='payment' and metadata.purpose === 'founders_ring' / metadata.account_id / metadata.sku === 'founders_pass_ring' — this is the metadata contract the webhook routes on, so locking it here means a typo can't slip through unnoticed.\n\nFor /api/stripe/webhook: (5) a synthetic checkout.session.completed event with purpose=founders_ring routes through to db.grantEntitlementWithCap with accountId='7777', sku='founders_pass_ring', grantedBy='stripe', cap > 0, and metadata.{stripe_session_id, amount_cents, currency} all correctly threaded through from session.id / session.amount_total / session.currency, (6) replaying the SAME event still acks 200 — the webhook is a pure dispatcher and grantEntitlementWithCap is the idempotency guarantor (returns already_owned on the replay).\n\nHarness reuses the require.cache stub pattern from tests/foundersRingCap.test.js: stubs the stripe SDK so webhooks.constructEvent ignores body+signature and returns a test-supplied event, stubs ../db via require.cache with capturing spy methods (Proxy returns no-op async stubs for unrelated helpers so server.js boots cleanly), stubs heavy transitive modules (replay parser, stats service, groq, discord bot, voice queue, monetization/magazineV3) AND connect-pg-simple (so createServer doesn't try to talk to Postgres at boot), invokes only the route's async handler layer to skip express.raw, supports both express 4 (app._router) and express 5 (app.router) for the webhook lookup, and unref()'s the long-running setIntervals so node:test exits cleanly. Full-edition only — community edition has no founders-ring routes.",
    "author": "System"
  },
  {
    "version": "6.83",
    "title": "Test coverage for the Stripe async-payment webhook flow",
    "published_at": "2026-05-09",
    "content": "Task #237: Task #235 enabled BECS Direct Debit and other async payment methods at Stripe Checkout, then refactored the webhook in src/web/server.js to defer fulfillment until checkout.session.async_payment_succeeded — but the BECS branch is reachable only with real direct-debit traffic in production, so a regression (e.g. accidentally re-fulfilling on an `unpaid` checkout.session.completed) would silently grant entitlements before the funds settle.\n\nNew tests/stripeWebhookAsyncPayment.test.js (27 cases, all green) locks the contract in synthetically. For every supported purpose — tournament_entry, pro_lifetime, coaching_booking, founders_ring, frame_purchase, gift_pro, gift_season_pass, and the default buyin path with no purpose metadata — three scenarios fire end-to-end through the real /api/stripe/webhook handler: (1) checkout.session.completed with payment_status='paid' triggers the matching db.* fulfilment helper exactly once, (2) the SAME event with payment_status='unpaid' (the BECS pending state) MUST NOT touch any fulfilment helper — this is the security guarantee the deferral was built for, (3) checkout.session.async_payment_succeeded fulfils the same way as a sync paid completion. Plus payment_status='no_payment_required' (100% promo / fully discounted) is asserted to fulfil, and async_payment_failed is asserted to (a) free the coaching slot via markBookingCancelledBySession for coaching_booking and (b) be a strict no-op for non-coaching purposes (a failed BECS payment must NEVER hit the paid-fulfilment helper).\n\nThe harness stubs the stripe SDK so webhooks.constructEvent ignores body+signature and returns the test-supplied event, stubs the ../db module via require.cache with capturing spy methods (Proxy returns no-op async stubs for unrelated helpers so server.js boots cleanly), stubs heavy transitive modules (replay parser, stats service, groq, discord bot, voice queue, monetization/magazineV3, profileCosmetics, connect-pg-simple), invokes only the route's async handler layer to skip express.raw, and unref()'s the long-running setIntervals so node:test exits cleanly. Full suite: 394/394 green.",
    "author": "System"
  },
  {
    "version": "6.82",
    "title": "Time-of-day heatmap — neutral blue cells with W-L numbers inside",
    "published_at": "2026-05-09",
    "content": "Reworked the time-of-day heatmap on /player/:id (Magazine v3) so each played hour now shows the actual wins-losses tally inside the cell instead of a red/green colour fill. Same hourly resolution (24 cols × 7 rows), same data, much more legible.\n\nCell shape (web/src/components/ProfileV3Panels.jsx + .v3-tod-* CSS in MagazineCover.css): each played hour renders `1W-0L` style text — wins coloured `--accent-green`, losses coloured `--accent-red`, dash + zero counts faded to ~50% so a clean 1W-0L doesn't look half-red. Cell background is neutral blue (`rgba(96, 165, 250, X)`) with opacity scaled by volume — same 0.18 floor → 1.0 ceiling formula as before, just in blue instead of green/red, so the brightness=volume signal is preserved as a single channel rather than competing with hue. Cell height up from 18→22px and min cell width 38px to fit the text; grid min-width up from 480→960px so the whole thing horizontally scrolls on phones.\n\nLegend was simplified to match: dropped the two win-rate swatches (the cell text shows wins/losses directly now) and replaced them with a single sample 'Cell score: 2W-1L (wins in green, losses in red)' chip. Volume swatch row stays, recoloured to the new blue ramp.\n\nTooltip + aria-label still carry the full breakdown (`Sat 19:00 — 3 games, 2W/1L (67% WR)`) so the data is one hover/focus away. Full-edition only — community edition has no Magazine v3 panels. A11y gate green (153 JSX / 3 CSS files), web bundle builds cleanly.",
    "author": "System"
  },
  {
    "version": "6.81",
    "title": "Voice-pack cues survive a bot restart",
    "published_at": "2026-05-09",
    "content": "Task #233: the per-account voice-pack event queue from Task #217 was in-memory only — if a match was recorded in the seconds before a deploy / PM2 restart, the win/loss/first-blood/achievement-unlock cues were dropped on the floor (players would still see the post-match Discord DM but never hear the pack). v6.81 persists every voice event to a new `voice_events` table (account_id, event, created_at) with a 5-minute TTL so a restart no longer black-holes them.\n\nSchema (src/db/index.js): new BIGSERIAL-keyed `voice_events` table + `idx_voice_events_account_created` index, applied as a normal CREATE TABLE IF NOT EXISTS migration just before the magazineV3 block.\n\nQueue (src/web/voiceEventQueue.js): pushVoiceEvent() stays fire-and-forget for callers (the existing `try { voiceEventQueue.push…(…) } catch (_) {}` shape in src/web/server.js is preserved) but the INSERT promise is now registered in a per-account `inflight` set so drainVoiceEvents() can await it. Single-delivery contract: events whose INSERT succeeds live in the DB EXCLUSIVELY (never shadowed); events whose INSERT rejects (or pushes that happen with no DB configured) live in the in-memory shadow EXCLUSIVELY. drainVoiceEvents() is async: it snapshots the in-flight set, awaits it via Promise.allSettled, then issues `DELETE … RETURNING` with `created_at > NOW() - INTERVAL '5 minutes'` to enforce the TTL, sweeps expired rows for that account, and drains the per-account shadow. Pushes that arrive AFTER the snapshot are left for the next drain — so a polling browser cannot double-fire the same cue across drain calls (the failure mode the v1 design had: drain-before-insert-commit returning the in-memory shadow then a later drain returning the same row from the DB). A 60s background sweeper trims expired rows globally (timer is unref()'d so it never holds the process open). Persistence is opt-in via DATABASE_URL — unit tests with no DB configured silently fall back to the original in-memory path so behaviour stays identical.\n\nRoute (src/web/server.js): `GET /api/me/voice-events` now `await`s drainVoiceEvents — the route handler was already async so no other shape changes.\n\nTests (tests/voiceEventQueue.test.js): every assertion is updated to await the now-async drain (all 11 originals still pass), plus four new cases for the persistent path: one drives a stub pool through the full INSERT → DELETE-RETURNING round-trip; one is the duplicate-event regression (two `win`s with one rejected INSERT — both cues must surface, no string-keyed shadow cleanup race); one is the cross-drain dedup regression (drain-before-insert-commit + drain-after-commit returns the cue exactly once); and one asserts the in-memory shadow fallback fires when both INSERT and DELETE throw. 15/15 pass.",
    "author": "System"
  },
  {
    "version": "6.80",
    "title": "Audition every voice-pack line from your profile settings",
    "published_at": "2026-05-09",
    "content": "Task #232: the voice-pack picker on /settings/profile only let you preview the `match-start.mp3` cue per pack — every other slot (first-blood, win, loss, level-up, achievement-unlock) was effectively a black box until you joined a real inhouse lobby (for match-start) or actually played a match (for the post-match cues from Task #217). That made the Pro voice-pack upsell hard to evaluate without committing.\n\nv6.80 swaps the single ▶ Preview button under each pack for a row of six per-event ▶ buttons, one for each slot in `VOICE_PACK_EVENTS` (Match start / First blood / Win / Loss / Level up / Achievement). Click any button to hear the corresponding `web/public/voice-packs/<pack>/<event>.mp3` for that pack. Preview buttons stay enabled even on Pro-locked packs so non-Pro users can audition the full lineup before buying — only the SELECT button above each pack remains gated by the Pro entitlement check.\n\nPlayback routes through the shared `createVoicePackPlayer()` helper from `web/src/lib/voicePack.js` (Task #217), so the page reuses the exact per-`${pack}|${event}` audio cache + 404-fallback shape used by the live inhouse-lobby cues (`useInhouseAlerts.js`) and the global post-match cues (`useVoicePackEvents.js`) — a missing slot fails once and is silently skipped on subsequent clicks. Each button reads `localStorage['inhouse:muted']` at click time so a muted user doesn't get surprise audio from the picker, mirroring the rest of the voice-pack pipeline. Buttons are real `<button type=\"button\">` elements with explicit `aria-label` (e.g. 'Play Captain Calls First blood sample'), so they pass the keyboard-reachability + a11y house rule and the existing `npm run check:a11y` gate stays green.",
    "author": "System"
  },
  {
    "version": "6.79",
    "title": "Live-presence rollup is locked down by unit tests",
    "published_at": "2026-05-09",
    "content": "Task #228: the bulk live-presence rollup added in Task #213 (`getAllLivePresences()` in src/services/presenceService.js, the engine behind both the /players Live now tab and the new global-nav green badge from Task #227) was non-trivial — it merges the in-memory _steam + _discord caches with the inhouse_queue table, applies a five-tier priority ladder (in_game > in_lobby > in_queue > in_voice > online), honours player_profiles.presence_visible, and silently drops cache entries older than 5 minutes. Until now none of that was exercised by an automated test, so a future presence tweak could silently regress the spectator hook with no CI signal.\n\nNew tests/presenceService.test.js (node --test, runs as part of `npm test`) seeds the _steam/_discord caches directly via the existing `_caches` test export and stubs db.getPool() with a tiny dispatcher that matches the three SQL strings the rollup issues (discord-id resolve, inhouse_queue scan, visibility/display-name lookup). Four assertions:\n\n- Status priority: a four-player scenario (in_game steam, in_lobby steam, queue-only, voice-only discord) returns rows in the documented in_game → in_lobby → in_queue → in_voice order with hero/match_id/joined_at fields populated correctly.\n- Online-exclusion: a separate case asserts that a Discord candidate with state='online' but voice=false is dropped from the rollup entirely (the rollup is scoped to actively-doing-something statuses), while voice=true still produces an in_voice row — closing the priority chain at the 'online' tier.\n- Visibility: presence_visible=false on the player_profiles join hides that account from the rollup even when the steam cache shows them in_game.\n- Stale-skip: cache entries with updatedAt > 5 minutes ago are dropped from both the steam and discord candidate sets.\n- Queue-only path: an inhouse_queue row with no steam/discord cache hit still produces an in_queue row with joined_at populated, matching the Task #213 'queue-only user appears in Live now' guarantee.\n\nFull suite: 361/361 green. Full-edition only — community edition has no presence service.",
    "author": "System"
  },
  {
    "version": "6.78",
    "title": "Global nav surfaces a 'Live now' badge when players are in game",
    "published_at": "2026-05-09",
    "content": "Task #227: the /players 'Live now' tab (Task #213) was a great spectator hook but only discoverable if you already knew to visit the players page — visitors landing on Home, Heroes, Matches, etc. had no signal that anyone was actually mid-match. v6.78 adds a Discord-style green pulse-dot + count badge to the global nav so the hook is visible from anywhere on the site.\n\nBackend (src/web/server.js): new `GET /api/presence/live/count` route returns `{count: <n>}` by reusing the existing getAllLivePresences() rollup and returning its array length. Same no-store + soft-fail semantics as the parent /presence/live endpoint — never throws on the public path, returns `{count: 0}` on any error. Cheap-by-association (the rollup is already memoised in-process via the _steam/_discord caches), but kept as a separate route so the nav doesn't have to ship the full per-player payload every 30s.\n\nFrontend nav (web/src/App.jsx): new `NavPlayersLink` component slots between Matches and the Tools dropdown. Polls /api/presence/live/count every 30s gated on `document.visibilityState === 'visible'` (mirrors the per-profile chip + Live now tab polling shape — backgrounded tabs stay quiet). When count > 0, the link renders a green-tinted pill with a pulsing dot (new `oi-live-pulse` keyframe in styles.css — 1.6s ease-out box-shadow expansion mirroring Discord's online glow) and the numeric count, and the `to=` swaps from `/players` to `/players?tab=live` so a click lands directly on the spectator hook instead of the All players table. The link carries an explicit `aria-label` ('Players — 3 live now' vs plain 'Players') so screen readers get the live count too. When count is 0 the badge collapses entirely — no visual noise on a quiet day.\n\nFrontend Players page (web/src/pages/Players.jsx): the `tab` state initialiser now reads `?tab=live` via useLocation() + URLSearchParams on mount, defaulting to 'all' for any other / missing value. A second useEffect re-syncs the tab whenever location.search changes, so clicking the nav link from /players itself (already-mounted page) also flips the view. The existing 30s tab-gated polling for the full /presence/live payload is unchanged.\n\nA11y gate green (153 JSX / 3 CSS files scanned). Full-edition only — community edition has no presence rollup wiring or /players Live now tab.",
    "author": "System"
  },
  {
    "version": "0.1",
    "title": "Bot Foundation & Steam Lobby Creation",
    "published_at": "2026-02-17",
    "content": "Initial release of the Dota 2 Inhouse Stats Bot.\n\nFeatures: Discord bot framework, Steam login, Dota 2 lobby creation via Game Coordinator, basic player registration with !register.",
    "author": "System"
  },
  {
    "version": "0.2",
    "title": "Replay Parsing & Full Match Stats",
    "published_at": "2026-02-19",
    "content": "Integrated OpenDota's Java-based replay parser for detailed .dem file analysis.\n\nNow tracks: KDA, GPM, XPM, hero damage, tower damage, healing, items, skill builds, multi-kills, and streaks.\n\nReplay upload via web dashboard with duplicate prevention.",
    "author": "System"
  },
  {
    "version": "0.3",
    "title": "Web Dashboard Launched",
    "published_at": "2026-03-11",
    "content": "React + Vite frontend served by Express backend at port 5000.\n\nIncludes: match history with sortable tables, full match scoreboards, and basic leaderboard.\n\nPostgreSQL replaces in-memory storage as the primary data store.",
    "author": "System"
  },
  {
    "version": "0.4",
    "title": "Player Profiles & TrueSkill MMR",
    "published_at": "2026-03-11",
    "content": "Per-player profile pages with hero history, MMR chart, position breakdowns, and stat averages.\n\nTrueSkill MMR implemented — rating updates dynamically based on match outcomes and opponent strength.\n\nOverall and position-specific statistical rankings added.",
    "author": "System"
  },
  {
    "version": "0.5",
    "title": "Season System, Stripe Buy-In & Steam Login",
    "published_at": "2026-03-11",
    "content": "Season management with buy-in and prize pool tracking via Stripe payments.\n\nSteam OpenID sign-in for verified identity on buy-ins.\n\nPrize category configuration: leaderboard rank, position MVPs, stat-based awards, and custom categories.",
    "author": "System"
  },
  {
    "version": "0.6",
    "title": "Match Management, Admin Tools & Nickname System",
    "published_at": "2026-03-11",
    "content": "Authenticated match deletion with data archiving and automatic TrueSkill recalculation.\n\nAdmin panel for managing players, matches, and nicknames.\n\nNickname system: custom display names assignable to any registered player.",
    "author": "System"
  },
  {
    "version": "0.7",
    "title": "Head to Head, Compare & Draft Assistant",
    "published_at": "2026-03-22",
    "content": "Head-to-head matchup page showing win rate, shared matches, and hero history between two players.\n\nPlayer comparison tool for side-by-side stat analysis.\n\nDraft assistant with hero pick and ban suggestions based on inhouse data.",
    "author": "System"
  },
  {
    "version": "0.8",
    "title": "Weekly Recaps, Discord Roles & Synergy Matrix",
    "published_at": "2026-03-23",
    "content": "Automated weekly recap posted to Discord every Monday at 9am AEST with top performers and match highlights.\n\nDiscord MMR roles: automatically assigned and updated based on TrueSkill tier.\n\nSynergy matrix: teammate and opponent win rates across all recorded matches.",
    "author": "System"
  },
  {
    "version": "0.9",
    "title": "Nemesis Tracking, Support Gold & Win Streaks",
    "published_at": "2026-03-24",
    "content": "Nemesis system: tracks who has killed each player the most across all parsed replays — shown on player profiles with skull medals.\n\nSupport gold efficiency tracking from replay data.\n\nWin/loss streak badges on leaderboard for any active streak.",
    "author": "System"
  },
  {
    "version": "1.0",
    "title": "Grok AI — !analyze, !roast & AI Match Commentary",
    "published_at": "2026-03-22",
    "content": "Integrated Grok AI (xAI) for natural language match analysis.\n\n!analyze — detailed post-match breakdown with performance insights.\n!roast — light-hearted player performance roast.\n\nAI match commentary automatically generated after parsed replays.",
    "author": "System"
  },
  {
    "version": "1.1",
    "title": "Custom MMR Tier System",
    "published_at": "2026-03-22",
    "content": "MMR tier system with configurable thresholds and Discord role auto-assignment.\n\nTiers: Herald, Guardian, Crusader, Archon, Legend, Ancient, Divine, Immortal — mapped to TrueSkill MMR ranges.\n\nAutomatic role updates whenever a player's MMR changes after a match.",
    "author": "System"
  },
  {
    "version": "1.2",
    "title": "Patch Notes Page & GrokBot AI Chat",
    "published_at": "2026-03-22",
    "content": "Patch notes page added to the web dashboard — admin-editable with version history.\n\nGrokBot AI chat widget embedded in the dashboard for natural language queries about player stats, match history, and hero performance.",
    "author": "System"
  },
  {
    "version": "1.3",
    "title": "MMR Tier Threshold Rebalance",
    "published_at": "2026-03-22",
    "content": "MMR tier thresholds adjusted based on actual player distribution across the inhouse pool.\n\nReduces clustering at top tiers and spreads the rating curve more evenly.",
    "author": "System"
  },
  {
    "version": "1.4",
    "title": "MMR Tier Rebalance — Based on Real Data",
    "published_at": "2026-03-22",
    "content": "Second rebalance pass using accumulated match data to calibrate tier thresholds against observed MMR spread.",
    "author": "System"
  },
  {
    "version": "1.5",
    "title": "MMR Tiers — Uniform 200 MMR Gaps",
    "published_at": "2026-03-22",
    "content": "Standardised MMR tier gaps to 200 MMR per tier for cleaner progression and more predictable role assignment.",
    "author": "System"
  },
  {
    "version": "1.6",
    "title": "MMR Tiers — 300 MMR Per Tier",
    "published_at": "2026-03-22",
    "content": "Increased tier gap to 300 MMR per tier to reduce tier-hopping and provide more stable Discord role assignments.",
    "author": "System"
  },
  {
    "version": "1.7",
    "title": "Landing Page, Weekly Recaps & Hero Name Display",
    "published_at": "2026-03-24",
    "content": "New landing page at / showing server stats (total matches, players, games this week, most played hero), last 5 recent matches, and quick links to all sections.\n\nWeekly recap now saved to DB and shown on the landing page — run !recap in Discord to generate and persist it instantly, or wait for the auto-post every Monday 9am AEST.\n\nAll dates across the site now display in Sydney (AEST/AEDT) time.\n\nHero names now display properly throughout the site (e.g. Queen of Pain instead of npc_dota_hero_queenofpain).\n\nLeaderboard moved to /leaderboard with a note explaining TrueSkill ranking.",
    "author": "System"
  },
  {
    "version": "1.8",
    "title": "Home Page Polish",
    "published_at": "2026-03-24",
    "content": "Recent matches now show richer detail: top killer name, hero they played, their kill count, and total kills in the match.\n\nStat cards are now centred on the home page.\n\nHero names now display in a readable format throughout the site (e.g. Obsidian Destroyer instead of npc_dota_hero_obsidian_destroyer).",
    "author": "System"
  },
  {
    "version": "2.0",
    "title": "Game Timeline, Ward Heatmap, Skill Builds & Major Stats Overhaul",
    "published_at": "2026-03-25",
    "content": "A large batch of new features and improvements across the entire dashboard.\n\n🗂️ MATCH DETAIL — GAME TIMELINE\nMatch detail pages now include a full game timeline graph (Net Worth, Experience, Level, Last Hits) sampled every 30 seconds throughout the match. Each player gets a colour-coded line — Radiant in greens, Dire in reds. Roshan kills appear as purple reference lines. Toggle between metrics using the tab buttons above the chart. Timeline data is captured from newly parsed replays only.\n\n🛒 ITEM PURCHASE TIMINGS\nBelow the timeline graph, a new Item Purchase Timings swim lane shows every significant item purchase for each player, with item icons positioned proportionally along the match duration. Hover any icon to see the item name and exact purchase time. Toggle it on/off with the 🛒 Items button. Consumables (TP, wards, smoke, dust, tango, etc.) are filtered out.\n\n🧙 SKILL BUILDS — REDESIGNED\nThe skill build display now shows ability icons per hero level (1–25) instead of raw numbers. Each cell shows the hero level, the ability icon fetched from Valve's CDN, and dot pips indicating how many skill points have gone into that ability. Talent choices are shown as purple T badges. Hover any cell to see the full ability name and the exact time it was levelled.\n\n🗺️ WARD HEATMAP PAGE\nA new Ward Heatmap page is accessible from the Tools menu. It displays observer and sentry ward placements on the Dota minimap, captured from parsed replays. Switch between all players (each with a unique colour) or filter to one player. Toggle between Observer only, Sentry only, or Both. Ward data accumulates with each newly parsed replay.\n\n🏆 ACHIEVEMENT BADGES EXPANDED\nPlayer profiles now display a richer set of achievement badges: Rampage, Ultra Kill, First Blood, Massacre, Ward Lord (100+ wards placed), and Ward Breaker (50+ wards destroyed).\n\n📊 MULTI-KILL LEADERBOARD\nNew /multikills page in the Tools menu — shows rampages, ultra kills, triple kills, and double kills across all recorded matches.",
    "author": "System"
  },
  {
    "version": "2.1",
    "title": "Gold Lead Timeline, Draft Stats, Streak Cards & Hero Matchups",
    "published_at": "2026-03-25",
    "content": "📈 GOLD LEAD TIMELINE\nThe match timeline now includes a Gold Lead view — a filled area chart showing Radiant net gold advantage moment by moment. Green above the line = Radiant ahead, red below = Dire ahead. Roshan kills and building kills are marked as reference lines on this chart.\n\n🏗️ DRAFT STATISTICS\nMatch detail pages now include a Draft Statistics section showing ban and pick order for both teams, with hero icons and player assignments. CM-style pick/ban data is extracted directly from the replay.\n\n🔥 WIN/LOSS STREAK CARDS\nPlayer profiles now display a live streak card when a player is on a winning or losing run of 3 or more games — showing the streak length, an emoji, and a flavour message.\n\n⚔️ HERO MATCHUPS\nPlayer profiles include a Hero Matchups tab showing which hero matchups the player wins and loses most often, calculated from real in-game data across all parsed replays.",
    "author": "System"
  },
  {
    "version": "2.2",
    "title": "Tower Timeline, Records Page, First Blood Stats & Skill Builds Tab",
    "published_at": "2026-03-25",
    "content": "🗼 TOWER & BARRACKS KILLS ON TIMELINE\nBoth the Gold Lead and per-player metric charts now display vertical markers when towers (🗼) or barracks (🏛️) fall during the match. Green lines mark structures lost by Dire; red lines mark Radiant losses. A legend row beneath the chart explains the colour coding.\n\n🏆 RECORDS & COMEBACKS PAGE\nA new /records page (available in the Tools menu) collects three leaderboards:\n• Hall of Records — all-time single-game bests for kills, GPM, hero damage, healing, net worth, last hits, and more. Each card links directly to the match and the player who set the record.\n• First Blood Leaderboard — who draws blood most often, with count and rate % (minimum 5 games).\n• Greatest Comebacks — matches where a team overcame a 5,000+ gold deficit, sorted by the size of the swing.\n\n🩸 FIRST BLOOD STATS ON PLAYER PROFILES\nPlayer profiles now include a First Bloods stat card (count and rate %) alongside KDA and GPM in the top stat grid.\n\n⏱️ WIN RATE BY GAME DURATION\nPlayer profiles now include a Win Rate by Game Duration section, breaking performance down into four time brackets: <25 min, 25–35 min, 35–45 min, and >45 min.\n\n🔧 SKILL BUILDS TAB — HEROES PAGE\nThe Heroes page now includes a Skill Builds tab. Select any hero that has been played and see the most common ability levelled at each skill point (1–25), with the percentage of players who chose it and the average timing.",
    "author": "System"
  },
  {
    "version": "2.3",
    "title": "Ward Heatmap Overhaul, Season Filters & Dashboard Fixes",
    "published_at": "2026-03-25",
    "content": "🗺️ WARD HEATMAP — TRUE DENSITY HEATMAP\nThe Ward Heatmap has been completely rewritten. Instead of individual coloured blobs, density is now accumulated on a downsampled 128×128 grid using Gaussian spreading, then upscaled with smooth interpolation. Isolated wards appear cool blue; frequently-warded spots gradually rise through cyan, green, yellow, and orange to red.\n\n📅 SEASON-AWARE PLAYER PROFILES\nPlayer profile pages now respect the selected season filter. Stats (averages, hero pool, recent matches, position breakdown) will update when you switch seasons.\n\n🏠 SEASON-AWARE HOME PAGE\nThe home page stat cards and recent match list now update when a season is selected.\n\n🐉 ROSHAN MARKERS FIXED\nRoshan kill and Tormentor kill markers on the game timeline charts are now properly visible inside the chart area with a thicker purple/orange dashed line and emoji label.",
    "author": "System"
  },
  {
    "version": "2.4",
    "title": "Pudge Hook Stats Page & Replay File Retention",
    "published_at": "2026-03-28",
    "content": "🪝 PUDGE HOOK STATS PAGE\nA dedicated /pudge-stats page is now available under Tools. Shows per-player stats for every Pudge game in the inhouse pool: games played, win rate, KDA, average GPM, hook attempts, hook hits, accuracy % (colour-coded), hits per game, attempts per game, and rampages.\n\nHook data is populated from replay uploads only — games without a parsed replay show N/A for hook columns. A methodology explanation is included at the bottom of the page.\n\n💾 REPLAY FILE RETENTION\nUploaded .dem replay files are now archived after parsing instead of being discarded. Files are stored for 7 days by default (configurable via REPLAY_STORE_DAYS). Superusers can:\n• Download replays directly from any match detail page.\n• View, download, extend expiry, or mark replays as \"keep forever\" from the Admin Panel.\n\nExpired files are cleaned up automatically every 12 hours.",
    "author": "System"
  },
  {
    "version": "2.5",
    "title": "Parser Upgrade — Precise Hook Attempt Tracking",
    "published_at": "2026-03-28",
    "content": "🎯 MORE ACCURATE HOOK ATTEMPT DETECTION\nThe underlying replay parser has been upgraded to capture unit order events, giving the hook tracking system access to the exact position the Pudge player aimed at when they cast the hook.\n\nPreviously, hook attempt detection relied on combat log events (which could be missing) and checked whether an enemy was near Pudge's own position. The new system:\n\n• Uses unit order events — fires every time without fail, regardless of combat log coverage.\n• Checks the hook's full flight path (a line from Pudge in the direction of the click, extended to the maximum possible hook range) rather than a single point.\n• Handles all range edge cases: clicking past an enemy, clicking short of an enemy, and items/talents that extend hook range (Aether Lens +225, level talent +125) are all covered by the 1800-unit range cap.\n• Ability class name is captured — the system confirms the cast is specifically the Meat Hook, not another ability.\n\n🚜 FARMING HOOK FILTER REFINED\nA hook cast is only excluded from the attempt count if it hit a non-hero (creep or neutral) AND no enemy hero was anywhere along the hook's flight path. Complete misses always count as genuine attempts — if the hook connected with nothing, the player was clearly trying.",
    "author": "System"
  },
  {
    "version": "2.6",
    "title": "Player Tools Page & Captain Win Rate Display",
    "published_at": "2026-03-25",
    "content": "🛠️ PLAYER TOOLS — COMBINED H2H & COMPARE PAGE\nHead to Head and Compare Players have been merged into a single /player-tools page with a tab switcher. Both tools are now season-aware — switch seasons to filter results to only games played in that season.\n\n📊 COMPARE PLAYERS — REDESIGNED BARS\nThe comparison view now uses centre-anchored bars with a percentage advantage indicator, making it immediately clear which player leads in each stat and by how much.\n\n👑 CAPTAIN WIN RATE — GAME COUNT DISPLAY\nThe captain win rate column on the Leaderboard now shows the game count in parentheses (e.g. \"75% (4g)\") so you can instantly gauge statistical significance. Hovering reveals a full W/L breakdown tooltip.\n\n🔀 SEASON FILTER — SYNERGY THRESHOLD FIX\nThe synergy matrix now uses a minimum of 1 game when a season filter is active (down from 3 for all-time), preventing everyone from showing 100% win rate when the season game count is low.",
    "author": "System"
  },
  {
    "version": "2.7",
    "title": "Multi-Kill Leaderboard, Most Improved Widget & Match Predictions",
    "published_at": "2026-03-25",
    "content": "💀 MULTI-KILL LEADERBOARD\nThe Multi-Kill Leaderboard has moved from a standalone page into a tab inside the Records & Comebacks page (/records). It's fully season-aware and sortable by rampages, ultra kills, triple kills, or double kills.\n\n📈 MOST IMPROVED WIDGET — LEADERBOARD\nThe Leaderboard page now features a Most Improved panel showing the top MMR gainers over the past 30 days, sourced from the rating_history table. Great for spotting players on a hot streak.\n\n🔮 MATCH PREDICTION SYSTEM\nPlayers can now predict match outcomes before a game is recorded.\n• Discord: use !predict <matchId> radiant|dire to submit your call.\n• Web: predictions are viewable via the /api/predictions endpoints.\n\nPlayer prediction accuracy stats appear on individual player profile pages.",
    "author": "System"
  },
  {
    "version": "2.8",
    "title": "Heroes Page Overhaul — Expandable Rows, Position Meta & Hero Breakdown Tab",
    "published_at": "2026-03-25",
    "content": "🦸 EXPANDABLE HERO ROWS\nThe Hero Stats table now supports expandable rows. Click any hero to lazily load a sub-table showing every inhouse player who has played that hero, along with their individual games, win rate, KDA, and average GPM — all inline without leaving the page.\n\n📊 HERO POSITION META TAB\nA new Position Meta tab on the Heroes page shows win rates for every hero broken down by position (Pos 1–5). Quickly see which heroes dominate each lane role in the inhouse pool.\n\n🏛️ HERO BREAKDOWN TAB\nThe previously separate /hero-breakdown page has been absorbed into the Heroes page as a third tab. Every player's full hero history is now one click away inside the Heroes section. The old URL redirects automatically.",
    "author": "System"
  },
  {
    "version": "2.9",
    "title": "Draft Page Overhaul — Merged Stats & Assistant with Player Pool Selector",
    "published_at": "2026-03-25",
    "content": "📋 DRAFT PAGE — UNIFIED TAB DESIGN\nDraft Statistics and the Draft Assistant have been merged into a single /draft page with two tabs. The old /draft-stats and /draft-assistant URLs redirect automatically.\n\n📊 DRAFT STATS — REAL DATA\nDraft Stats now pulls from player_stats (pick history always present) rather than the match_draft table (only populated from Captain's Mode replays). This means the tab is always populated with real hero pick data regardless of replay availability.\n\n🧑‍🤝‍🧑 PLAYER POOL SELECTOR\nThe Draft Assistant tab now includes a Player Pool panel. Search for any registered player and add them to My Team or Enemy Team. Their top heroes appear as clickable chips — tap a hero to instantly populate it into the ally or enemy hero list, feeding the synergy and counter analysis automatically.",
    "author": "System"
  },
  {
    "version": "3.0",
    "title": "Predictions Overhaul — Accuracy Scoring, Discord Webhook & Home Widget",
    "published_at": "2026-03-25",
    "content": "🔮 PREDICTIONS PAGE — THREE TABS\nThe Predictions page has been rebuilt with three tabs:\n• Submit Prediction — submit your forecast for the season standings.\n• All Predictions — view every prediction submitted by the community.\n• Accuracy Scores — compare predictions to the actual season MMR standings. Scoring: 3 pts for exact rank, 1 pt for correctly placing a player in the top 5.\n\n🏆 ACCURACY LEADERBOARD\nThe Accuracy Scores tab ranks predictors by total score, showing their exact hits, top-5 hits, and overall accuracy for the season.\n\n🏠 HOME PAGE — PREDICTIONS WIDGET\nThe home page now displays a Predictions widget showing how many predictions have been submitted for the active season, with a direct link to the Predictions page.\n\n📣 DISCORD WEBHOOK — PREDICTION NOTIFICATIONS\nSet the DISCORD_WEBHOOK_URL environment variable to receive a Discord notification every time a community member submits a new season prediction.",
    "author": "System"
  },
  {
    "version": "3.1",
    "title": "Season Records, Hook Report & Dieback Tracking",
    "published_at": "2026-03-31",
    "content": "📊 SEASON RECORDS TAB\nThe Records & Comebacks page now includes a Season Records tab — a card-based awards board showing who leads (and trails) across every meaningful stat category for the selected season.\n\n🏅 POSITIVE RECORDS\n• Most Wins, Most Kills, Most Assists\n• Most Hero Damage, Most Tower Damage, Most Healing\n• Best Win Rate (5+ games), Longest Win Streak, Most Games Played\n• Most First Bloods, Most Rampages, Most Roshans Killed\n• Most Towers Destroyed\n• 👁️ Vision King — most wards placed total\n• 🔍 Ward Hunter — most enemy wards destroyed\n• 📦 Stack God — most camps stacked\n• 🧲 CC King — most hard CC seconds applied (stuns, hexes, cyclone)\n\n💀 NEGATIVE RECORDS — Hall of Shame\n• Most Deaths, Most Losses, Worst Win Rate (5+ games), Longest Loss Streak\n• ☠️ Highest Avg Deaths/Game (5+ games)\n• 💸 Most Buybacks\n• 🪦 Most Diebacks — bought back then died again within 2 minutes\n• ⏳ Most Time Dead — total minutes spent on the respawn timer across the season\n• 🗑️ Worst Observer Ward Lifespan — whose wards get found and killed fastest (min 3 dewarded)\n• 📉 Lowest Avg GPM (5+ games)\n\n🪝 PUDGE HOOK REPORT\nA downloadable per-cast hook accuracy report is now available on any match where Pudge was played and a replay was parsed. The report lists every hook cast with its timestamp, outcome (hero hit / miss / creep hit / farming hook), whether it counted as an attempt, and which target was hit. Use it to cross-reference accuracy claims against the actual replay.\n\n🔬 HOOK TRACKING ACCURACY OVERHAUL\nThe hook cast detection system has been fundamentally rewritten for correctness:\n• Cast counting is now driven by the Dota 2 combat log ability event — fires exactly once at the moment the hook activates and the cooldown begins. Cancelled casts (where the player queued then cancelled before the hook fired) are now correctly excluded.\n• Previously, unit-order events were used as the primary cast source. These fire multiple times per cast (once per tick while the order is pending in the queue), which inflated cast counts significantly.\n• Unit-order events are still used, but only to supply the target coordinates (click position) for the farming-hook geometry check — they are never counted as casts.\n• Coordinate matching window tightened from 3 seconds to 1 second.\n\n📏 LEVEL COLUMN — MATCH SCOREBOARD\nA Lvl column has been added to the match detail scoreboard, showing each player's final hero level. Level 25 is highlighted gold; level 20+ in purple.",
    "author": "System"
  },
  {
    "version": "3.2",
    "title": "Player Network, Form Guide, Hero Tier List & Player Benchmarks",
    "published_at": "2026-03-31",
    "content": "🌐 PLAYER NETWORK PAGE\nNew social page at /social under Tools — visualise connections across the server.\n• Top Duos tab: best teammate win-rate pairings with a minimum-games filter.\n• Player Connections tab: select any player to see their top 10 teammates and top 10 opponents with win rates.\n\n📈 LEADERBOARD UPGRADES\n• Most Improved widget — highlights the top MMR gainers over the last 30 days, drawn from rating history.\n• Form Guide — the last 10 game results are shown as green (W) / red (L) dots next to every player on the leaderboard; season-aware.\n\n🏆 HERO TIER LIST\nNew tab on the Heroes page ranking every hero S → D by inhouse win rate (minimum 2 games). Colour-coded tier cards make the power curve easy to read at a glance.\n\n⚔️ HERO MATCHUPS\nNew tab on the Heroes page — select any hero to see win rate vs every opponent hero they have faced; filterable, sortable table with an advantage bar.\n\n📊 PLAYER BENCHMARKS\nNew section on every player profile — compares that player's GPM, Damage, Last Hits, and Healing against the server average for their most-played position. Colour-coded bars with +/- % indicator make strengths and weaknesses immediately clear.",
    "author": "System"
  },
  {
    "version": "3.3",
    "title": "AI Match Commentary, Hot Streak Callouts & Match Notes",
    "published_at": "2026-03-31",
    "content": "🤖 AI MATCH COMMENTARY\nAfter every recorded match the bot generates a Grok-powered write-up sent as a follow-up Discord message:\n• MVP one-liner — a witty single sentence calling out the standout performer.\n• Match narrative — a 2-sentence summary of the game's story.\nRuns asynchronously so it never delays the main scoreboard post.\n\n🔥 HOT STREAK MILESTONES\nThe bot now calls out player win streaks on every recording path (replay upload, !record, lobby auto):\n• 5-win streak → 🔥🔥🔥 callout in the match channel.\n• 10-win streak → 🏆🔥🏆 LEGENDARY announcement.\n\n📝 MATCH NOTES\nAdmins can now attach text notes to any match from the match detail page on the web dashboard.\n• Notes are stored in the database and visible to all users.\n• Multiple notes can be added per match; each can be deleted by an admin.\n• Useful for flagging disputed games, recording context, or leaving reminders.",
    "author": "System"
  },
  {
    "version": "4.0",
    "title": "Match Scoreboard Image",
    "published_at": "2026-03-31",
    "content": "🖼️ MATCH SCOREBOARD IMAGE\nAfter every recorded match the bot generates a polished PNG scoreboard card and posts it directly to Discord — no need to open the dashboard to see the result.\n\nThe card includes:\n• Winner announcement and match duration.\n• Radiant and Dire kill totals.\n• Per-player row: hero name, K/D/A, GPM, hero damage, and healing dealt.\n• A highlights strip calling out standout stats (top damage, top GPM, etc.).\n\nThe image is generated server-side using @napi-rs/canvas and sent as a Discord attachment. Gracefully degrades to the text scoreboard if image generation fails.",
    "author": "System"
  },
  {
    "version": "4.1",
    "title": "Patch Notes Auto-Announce",
    "published_at": "2026-03-31",
    "content": "📋 PATCH NOTE AUTO-ANNOUNCEMENTS\nBot updates are now automatically posted to Discord whenever a new version is detected on startup.\n\nHow it works:\n• Each bot version is tracked in the database with an announced flag.\n• On startup, any unannounced versions are posted as formatted Discord embeds to the announcements channel.\n• Once posted, the version is marked so it never fires again — no duplicate announcements across restarts.\n\nGoing forward, patch notes are updated by the developer as part of every feature release — no more missing changelog entries.",
    "author": "System"
  },
  {
    "version": "4.2",
    "title": "Tournament Brackets — Single & Double Elimination",
    "published_at": "2026-04-02",
    "content": "🥊 TOURNAMENT BRACKETS SYSTEM\nFull tournament management is now live at /tournaments.\n\n• Create tournaments linked to any season with single or double elimination format.\n• Participants are seeded by MMR automatically — highest MMR gets the #1 seed.\n• Admins enter match winners directly from the bracket view; the bracket advances in real time.\n• Double elimination: losers are routed to the correct Winners Bracket / Losers Bracket / Grand Final match automatically using proper bracket logic.\n• Grand Final winner is detected automatically and a champion banner is displayed at the top of the bracket page.\n\n🏆 CHAMPION BANNER\nWhen a tournament concludes, a gold glowing champion banner appears showing the winner's name — no manual flagging required.",
    "author": "System"
  },
  {
    "version": "4.3",
    "title": "Ward Map, Win Rate Window, Hall of Fame & Stats Improvements",
    "published_at": "2026-04-02",
    "content": "🗺️ WARD MAP ON MATCH DETAIL\nEvery parsed match now shows a Ward Map panel — a minimap overlay with team-coloured ward placement icons.\n• Green = Radiant, Red = Dire.\n• Observer wards shown as circles, sentry wards as diamonds.\n• Filter buttons let you toggle between Both / Observer / Sentry.\n\n📈 ROLLING WIN RATE WINDOW TOGGLE\nThe win rate chart on player profiles now has 5 / 10 / 20 / All toggle buttons. Switch the rolling window instantly without any additional load.\n\n🏛️ HALL OF FAME PAGE\nNew dedicated page at /hall-of-fame showing all-time and season single-game records (most kills, highest GPM, most hero damage, etc.) as card-style displays with links to the match and player.\n\n📊 BEST POSITION — OVERALL STATS\nThe Overall Stats page (/stats) now includes a Best Position column for every player, calculated using the same composite score as the Players page (win rate × 4 + KDA component + kill involvement component).\n\n🧭 NAV CLEANUP\nTournaments and Player Benchmarks moved into the Tools dropdown. The separate More menu has been removed. Multi-Kill Records removed from the nav (already inside Records & Comebacks as its own tab).\n\n🌤️ LIGHT MODE IMPROVEMENTS\nLight mode colours are now softer and easier on the eyes. The season selector and Steam Login button now properly adapt to light mode instead of staying dark.",
    "author": "System"
  },
  {
    "version": "4.4",
    "title": "Player Profile Stat Grid & Benchmark Refinements",
    "published_at": "2026-04-03",
    "content": "📊 STAT GRID OVERHAUL\nThe stat card grid on player profiles has been completely restructured into a clean 6×2 layout — all 12 cards always visible, with a dash shown when data is unavailable.\n• Removed redundant Avg Kills and Avg Deaths cards (already covered by KDA).\n• Replaced Avg Assists with Avg GPM for a more useful at-a-glance read.\n• Cards fill the full container width with consistent row heights.\n• MVP Wins and Attitude Rating cards are always shown regardless of data.\n\n📐 BENCHMARK SEASON FILTERING\nPlayer Benchmarks now correctly filters by the selected season. Fixed a ROUND::numeric cast error and a GROUP BY issue that caused incorrect averages. Stat cards align with the timeline section below for a balanced layout.\n\n🧹 UPLOAD NOTIFICATION REMOVED\nThe persistent replay upload notification bar has been removed from the global layout — it was cluttering every page and is no longer needed.",
    "author": "System"
  },
  {
    "version": "4.5",
    "title": "Rating DM Overhaul, MVP Vote Fixes & Item Display",
    "published_at": "2026-04-03",
    "content": "📬 RATING DM IMPROVEMENTS\nPost-match rating DMs sent to players have been significantly improved:\n• Match link now included in every DM so players can review the scoreboard before rating.\n• Opt-out footer added — players are reminded they can ignore the DM with no consequences.\n• 30-minute rating window is now clearly stated in the message.\n\n🗳️ MVP VOTE — FULL LOBBY\nThe MVP vote prompt now correctly shows all 9 opposing + same team players (both teams minus yourself). Previously it only showed a subset.\n\n📤 SEND MISSING DMs\nA new \"Send Missing DMs\" button has been added to the match admin panel. It sends rating DMs only to players who have never received one for that match — skipping anyone already DM'd. All sends are tracked in a match_dm_log table to prevent duplicates.\n\n🎯 ITEM & HERO NAME FIXES\n• Broken item icons fixed by normalising item names (underscores, casing).\n• Support item list trimmed to meaningful items: wards, smoke, dust, gem only.\n• Hero display names corrected — Shredder now shows as Timbersaw, and other outdated internal names are mapped to their current display names.",
    "author": "System"
  },
  {
    "version": "4.6",
    "title": "Impact Score System",
    "published_at": "2026-04-04",
    "content": "⭐ IMPACT SCORE — POSITION-NEUTRAL PERFORMANCE RATING\nA new 1–10 Impact Score has been introduced to measure player performance in a role-neutral way — no more carry bias.\n\nThe score is calculated from:\n• Kill involvement (kills + assists as a % of team kills)\n• K/D/A efficiency (kills + assists relative to deaths)\n• Win rate contribution\n• Games played (slight bonus for volume)\n\nAll components are z-scored within each match so a support dominating their role scores just as highly as a carry dominating theirs.\n\n📍 WHERE IT APPEARS\n• Leaderboard — Impact Score column next to MMR.\n• Player Profile — replaces GPM in the headline stat card (GPM still in the full stats table).\n• Hall of Fame career tab — Impact Score column.\n• Match Detail scoreboard — per-match Performance rank (shown as a coloured dot/badge).\n• Discord post-match highlights — Top Impact player called out alongside MVP.\n\n🔧 LANE OUTCOME FIX\nLane win/loss calculations now correctly compare players against their actual lane opponents rather than all enemies, giving accurate early-game outcome data.",
    "author": "System"
  },
  {
    "version": "4.7",
    "title": "TrueSkill 2 Admin Preview (Hidden)",
    "published_at": "2026-04-06",
    "content": "🔬 TRUESKILL 2 SIMULATION — ADMIN ONLY\nA hidden TrueSkill 2 simulation is now accessible from the Admin Panel via the \"Run TS2 Simulation\" button.\n\nHow it works:\n• Reruns the entire match history under a TS2 variant where μ updates are scaled by each player's per-match K/D/A performance (modifier range: 0.65× to 1.35×, z-scored within the match).\n• σ (uncertainty) is unchanged from standard TrueSkill.\n• Results are displayed in the admin panel for comparison against the current TrueSkill ratings.\n• Read-only — nothing is written to the database. Used to evaluate TS2 before a potential Season 10 rollout.",
    "author": "System"
  },
  {
    "version": "4.8",
    "title": "Game Scheduling Overhaul & Steam Account Linking",
    "published_at": "2026-04-07",
    "content": "📅 SCHEDULE & RSVP IMPROVEMENTS\nThe game scheduling system has been overhauled for reliability and usability:\n• Schedule and RSVP messages are now combined into a single clean Discord post instead of two separate messages.\n• Discord scheduling notifications fire correctly on game creation and updates.\n• Improved user registration checks with clearer error messages.\n\n🔗 STEAM ACCOUNT LINKING PROMPT\nWhen a player RSVPs to a game without a linked Steam account, the bot now sends them a personalised prompt explaining how to link their Steam ID — so they don't miss out on Steam chat reminders and lobby invites.\n\n🛡️ STEAM ID VALIDATION\nRobust Steam ID validation added across registration and lobby invite flows — accepts Steam64, Steam2, Steam3, and profile URL formats and converts them correctly. A !testdm and !testrsvpdm admin command pair is available for testing the DM flows without a real match.",
    "author": "System"
  },
  {
    "version": "4.9",
    "title": "Steam Bot Identity, RSVP Nicknames & Tiered Reminders",
    "published_at": "2026-04-08",
    "content": "🤖 STEAM BOT IDENTITY\nThe Steam bot now displays as \"Dota Bot\" in Dota 2 and Steam. On startup it automatically sends friend requests to every registered player so invites and Steam chat reminders work without manual setup.\n\n📋 RSVP NICKNAME DISPLAY\nDiscord RSVP reactions now show the player's custom nickname (if set) instead of their raw Discord username — consistent with how names appear everywhere else on the site.\n\n⏰ TIERED GAME REMINDERS\nThe schedule reminder system now has three tiers:\n• 24 hours out — Discord channel message + DM to all RSVPd players.\n• 1 hour out — Discord channel message + DM + Steam chat message to all RSVPd players with a linked Steam account.\n• 10 minutes out — Discord channel message + Steam chat message (no DM at this tier).\n\nSteam chat messages land directly in each player's Steam inbox — visible even if they're not in Discord. Reminder tiers are tracked per game in the database to prevent duplicate sends across restarts.",
    "author": "System"
  },
  {
    "version": "5.0",
    "title": "Steam Bot Admin Panel",
    "published_at": "2026-04-08",
    "content": "🛠️ STEAM BOT CONTROLS\nA full Steam Bot Controls section has been added to the Admin Panel:\n• Status panel — live view of Steam connection state, GC readiness, friend count, and active lobby name.\n• Create Lobby — create a named practice lobby with optional password directly from the browser.\n• Join Lobby — join an existing lobby by ID.\n• Invite Player — send a GC lobby invite to any player by Steam64 ID.\n• Add All Friends — bulk-send friend requests to every registered player in one click.\n• End / Leave Lobby — cleanly leave the current lobby.\n• 🚀 Launch Game — force-start the game immediately, cancelling any active countdown.",
    "author": "System"
  },
  {
    "version": "5.1",
    "title": "Lobby Auto-Create, Auto-Invite & !start_game",
    "published_at": "2026-04-08",
    "content": "🎮 LOBBY AUTO-CREATE AT GAME TIME\nThe bot now monitors the schedule and automatically creates a Dota 2 practice lobby when a scheduled game's start time arrives — no admin action required.\n• Lobby named \"OCE Inhouse #N\" based on the game number.\n• Password carried over from the scheduled game if one was set.\n• All RSVPd players with a linked Steam account receive a GC lobby invite automatically (0.5s between each to avoid rate limits).\n• A formatted Discord announcement is posted confirming the lobby is live and how many invites were sent.\n• Lobby is marked in the database so it's never auto-created twice.\n• Bot immediately moves to the broadcast slot after creation — all 10 player seats remain free.\n\n🟢 10-PLAYER SEATED NOTIFICATION\nWhen the 10th player sits on Radiant or Dire, the bot posts to the announcements channel confirming the lobby is full.\n\n🕹️ !start_game COMMAND\nNew admin Discord command. Force-launches the active lobby regardless of player count. Cancels any active countdown before launching.",
    "author": "System"
  },
  {
    "version": "5.2",
    "title": "Lobby Countdown, In-Lobby Chat & CM Settings",
    "published_at": "2026-04-08",
    "content": "⏱️ 15-SECOND AUTO-LAUNCH COUNTDOWN\nWhen all 10 players are seated on teams, the bot starts a 15-second countdown visible in the Dota 2 lobby chat window.\n• Messages at 15s, 10s, 5s, 4s, 3s, 2s, 1s → \"Launching game now!\"\n• Game launches automatically at 0.\n• If a player leaves a team slot mid-countdown, it cancels with a warning and resets when 10 are seated again.\n• !start_game and the admin panel Launch Game button both abort any active countdown and force-launch.\n\n💬 IN-LOBBY CHAT\nThe bot joins the Dota 2 practice lobby chat channel after lobby creation and sends all countdown and status messages directly into it — visible in the in-game lobby chat box to all players.\n\n⚔️ CAPTAINS MODE SETTINGS FIXED\nPractice lobbies now use the correct competitive defaults:\n• Captains Mode with Radiant picking first.\n• No cheats, no bots filling empty slots.\n• All-chat on, Dota TV delay enabled, unlimited pauses.",
    "author": "System"
  },
  {
    "version": "5.3",
    "title": "Bug Fixes — GC Listeners, Player Merging & GrokBot Removed",
    "published_at": "2026-04-17",
    "content": "🔧 DUPLICATE GC LISTENER FIX\nFixed a bug where Game Coordinator event handlers were stacking on every GC reconnect, causing the bot to respond multiple times to lobby events. All GC listeners are now cleaned up before re-registration.\n\n👤 PLAYER PROFILE CANONICAL REDIRECT\nMerged player accounts now redirect correctly — visiting a secondary account's profile page automatically redirects to the primary profile. MMR history, Impact Scores, and MVP/Attitude ratings are all aggregated across all merged accounts.\n\n🔗 ADD ALL FRIENDS BUTTON FIXED\nThe \"Add All Known Friends\" button in the Steam Bot admin panel was querying a non-existent table. Fixed to query the correct table — bulk Steam friend requests now work as intended.\n\n🤖 GROKBOT AI CHAT REMOVED\nThe GrokBot chat widget has been removed from the website to prevent API cost abuse. The !analyze, !roast, and post-match AI commentary features in Discord are unaffected.",
    "author": "System"
  },
  {
    "version": "5.4",
    "title": "Match Performance Score Overhaul",
    "published_at": "2026-04-17",
    "content": "📊 MATCH PERFORMANCE SCORE — REWORKED\nThe \"Perf\" column on match scoreboard pages has been reworked from a pure ordinal ranking to a meaningful z-score based performance rating.\n\nPreviously the column simply ranked all 10 players 1–10 by position — the best player always got 10 and the worst always got 1, regardless of how everyone actually performed.\n\nThe new system:\n• Computes a raw performance score per player using kill involvement, KDA efficiency, win result, and GPM (when replay data is available).\n• Normalises scores relative to the match average using z-score scaling.\n• Maps the result to a 1–10 scale where average performance lands around 5–6.\n• Multiple players can now share the same score — if several players had a great game, they can all score 8 or 9.\n• A truly dominant performance scores 9–10; a poor game scores 1–3 regardless of how others played.\n\nHovering the score shows a tooltip explaining the factors used.",
    "author": "System"
  },
  {
    "version": "5.5",
    "title": "Hero Damage Fix — Enemy Heroes Only",
    "published_at": "2026-04-17",
    "content": "🔧 HERO DAMAGE NOW MATCHES DOTA CLIENT\nThe Hero Damage (HD) column was including damage dealt to allied heroes, causing inflated numbers for heroes with ally-targeting abilities.\n\nAffected heroes:\n• Oracle — Purifying Flames deals damage to allied heroes before healing them. This was being counted as \"hero damage\" on the website, producing values 4–5× higher than the Dota client shows.\n• Pudge — Rot aura damages nearby allies as well as enemies. Allied Rot damage was being added to the total.\n• Any other hero with an ability that damages allied heroes.\n\nFixed: the replay parser now only counts damage dealt to ENEMY heroes, matching the Dota 2 client's \"Hero Damage\" scoreboard column exactly.\n\nNote: this fix applies to newly parsed replays only. Existing match records will retain their previous values.",
    "author": "System"
  },
  {
    "version": "5.6",
    "title": "Dota 2 Rank Medal Tracking",
    "published_at": "2026-04-18",
    "content": "🎖️ DOTA 2 RANK MEDALS\nPlayers can now display their actual Dota 2 ranked medal (Herald through Immortal) on the website.\n\nRanks are fetched automatically using a 3-layer approach:\n1. OpenDota public data — works instantly for players with public match history.\n2. Steam Game Coordinator profile card — works for players who are Steam friends with the bot, even with private profiles.\n3. Manual admin entry — admins can set any player's rank directly.\n\nWhere ranks appear:\n• Leaderboard — new \"Dota Rank\" column with coloured medal badge.\n• Player Profiles — rank medal shown next to the player name at the top of the profile.\n\nAdmin panel:\n• New \"Dota 2 Rank Management\" section with a one-click sync button.\n• Manually edit or clear any player's rank.\n• Sync runs in the background — typically completes in 15–30 seconds for the full player list.\n• Manual entries are never overwritten by auto-sync.",
    "author": "System"
  },
  {
    "version": "5.7",
    "title": "Records Overhaul, Courier Kills, Draft Fix & Join Page",
    "published_at": "2026-04-18",
    "content": "🐔 COURIER KILLS TRACKED\nCourier kills (recorded by the replay parser) now appear in the Records page as a standalone all-time single-game record.\nTwo new achievements: \"Chicken Killer\" (20+ career courier kills) and \"Courier Slayer\" (50+ career courier kills).\n\n📊 RECORDS PAGE IMPROVEMENTS\nRemoved: Highest Hero Level record (not meaningful).\nAdded: Most Denies, Most Courier Kills, and Most Buybacks as new single-game best categories.\n\n🔧 DRAFT TOOL FIX\nFixed a bug where player hero pools failed to load in the Draft Assistant. Selecting a player now correctly shows their most-played heroes.\n\n🔍 PLAYERS PAGE SEARCH\nAdded a live search box to the Players page — filter the full roster by name or nickname instantly.\n\n👥 POSITION BREAKDOWN SORTED\nThe Position Breakdown table on Player Profiles is now sorted by most games played (most common role at the top).\n\n📝 JOIN THE LEAGUE PAGE\nNew public page at /join — interested players can submit their Discord username, Steam profile, preferred positions, and a message. Admins can review, approve, or reject requests directly from the Admin Panel.",
    "author": "System"
  },
  {
    "version": "5.8",
    "title": "Patch Note Announcements Gated to Production",
    "published_at": "2026-04-18",
    "content": "🔧 PATCH NOTE ANNOUNCEMENT TIMING FIX\nPatch notes are now only announced to Discord after the update has been deployed to the production server.\n\nPreviously, the bot would post patch note announcements immediately on any restart — including the development environment — before changes had been pushed to production or published on the website.\n\nNow, Discord announcements require the ANNOUNCE_PATCH_NOTES=true environment variable to be set. This variable is only present on the production server, so patch notes will appear in Discord only after a real deployment.",
    "author": "System"
  },
  {
    "version": "5.9",
    "title": "Join Page Overhaul & Player Stats Search",
    "published_at": "2026-04-18",
    "content": "📝 JOIN PAGE IMPROVEMENTS\nThe Join the League form has been updated:\n• Discord field now collects your Discord User ID (the numeric snowflake) with instructions on how to find it.\n• Steam Profile URL is now mandatory.\n• New mandatory Peak MMR / Dota 2 Rank field.\n• Preferred Positions now uses a ranked click-to-select system — click positions in order of preference and each receives a numbered badge (1st, 2nd, etc.) so admins know your priority order.\n\n🔍 PLAYER STATS SEARCH\nAdded a live search box to the Player Stats page — filter all players by name or nickname instantly, matching the search on the Players page.",
    "author": "System"
  },
  {
    "version": "5.10",
    "title": "Dota 2 Rank Sync — Critical Bug Fix",
    "published_at": "2026-04-18",
    "content": "🔧 RANK SYNC BUG FIX\nThe Dota 2 rank sync (introduced in v5.6) was silently failing every time the sync button was pressed due to an incorrect internal import. No ranks were ever stored as a result.\n\nFixed: the sync now works correctly. Players with public OpenDota profiles will have their rank updated automatically when sync is triggered from the Admin Panel. Players with private profiles will be fetched via the Steam Game Coordinator instead.",
    "author": "System"
  },
  {
    "version": "5.11",
    "title": "MMR History Chart Label Clarified",
    "published_at": "2026-04-18",
    "content": "🔧 MMR HISTORY LABEL\nThe \"X MMR over N games\" label on player profiles now clearly notes it is all-time data. The MMR chart has never been season-filtered — it shows the player's full career rating trajectory regardless of which season is selected in the menu. The stat cards at the top of the profile are season-filtered; the chart is not.",
    "author": "System"
  },
  {
    "version": "5.12",
    "title": "Rank Sync Improvements + Manual Rank Edit UI",
    "published_at": "2026-04-18",
    "content": "🔄 RANK SYNC — OPENDOTA REFRESH\nThe rank sync now calls OpenDota's refresh endpoint for each player before fetching their rank data. This forces OpenDota to re-crawl the player's Dota 2 profile, meaning players with public data who weren't being found (like MajinDabura) will now correctly have their rank populated.\n\n✏️ MANUAL RANK EDIT — NEW UI\nThe rank edit form in the Admin Panel (Dota 2 Rank Management) now uses proper dropdowns instead of raw number codes. Select medal (Herald → Immortal) from the first dropdown, then stars (★ through ★★★★★) from the second. For Immortal players, a leaderboard rank field appears instead of stars. The computed tier code is shown for reference.",
    "author": "System"
  },
  {
    "version": "5.13",
    "title": "GC Rank Sync — Protobuf Field Fix",
    "published_at": "2026-04-18",
    "content": "🔧 GAME COORDINATOR RANK SYNC FIX\nThe Steam Game Coordinator rank sync was returning no data for all players, even Steam friends, due to incorrect protobuf field numbers in the response parser.\n\nThe CMsgDOTAProfileCard protobuf encodes rank_tier at field 8 (tag 64) and leaderboard_rank at field 9 (tag 72). The parser was reading fields 9 and 10 instead — meaning it was silently reading the wrong values and discarding them.\n\nFixed: the correct field numbers are now used. Players who are Steam friends with the bot will have their rank reliably synced via the Game Coordinator, regardless of their profile privacy settings.",
    "author": "System"
  },
  {
    "version": "5.14",
    "title": "Match Page Fix + Replay Inspector Tool",
    "published_at": "2026-04-18",
    "content": "🐛 MATCH PAGE BLANK SCREEN FIX\nFixed a crash that caused all match detail pages to show a completely blank screen. The TeamTable component internally referenced an undefined variable (allPlayers) — correctly receiving it as a prop from the parent now fixes this. Added a React error boundary so any future unexpected errors show a readable message and stack trace instead of a blank page.\n\n🔍 REPLAY INSPECTOR TOOL\nNew Replay Inspector panel in the Admin Panel (superuser only). Upload any .dem file to see the raw account IDs, Steam64 IDs, persona names, and heroes extracted by the parser — without committing anything to the database. Useful for verifying accounts before uploading a replay.",
    "author": "System"
  },
  {
    "version": "5.15",
    "title": "Replay Inspector Persona Name Fix + Steam64 Precision Fix",
    "published_at": "2026-04-18",
    "content": "Fixed the Replay Inspector showing blank persona names for all players. The inspector endpoint was reading p.personaName (camelCase) but the parser stores the field as p.personaname (all lowercase) — corrected to read both variants.\n\nFixed a silent precision bug in account ID extraction from replays. Steam64 IDs are 17-digit integers that exceed JavaScript's safe float64 range (2^53). The Java replay parser serialises them as raw JSON numbers via Gson, so JSON.parse was silently rounding them — e.g. 76561197996209749 → 76561197996209744 — producing a Steam32 that could be off by several units. The epilogue JSON is now pre-processed to convert all 15+ digit bare integers to quoted strings before parsing, so BigInt can read them with exact precision.",
    "author": "System"
  },
  {
    "version": "5.16",
    "title": "Database Backup, Restore & Nickname ID Fix",
    "published_at": "2026-04-18",
    "content": "Added a Database Backups panel to the Admin Panel for safe snapshot management of player_stats, ratings, and rating_history tables.\n\nKey features:\n• \"Re-parse All\" now automatically creates a backup before starting — the backup ID is shown in the status bar so you can restore instantly if anything goes wrong.\n• \"Backup Now\" button creates a manual snapshot at any time.\n• Backups are listed with their label, timestamp, and a one-click Restore button.\n• Restoring a backup runs inside a transaction — it is atomic and rolls back automatically on any error.\n• Backups can be deleted individually once no longer needed.\n\nMMR recalculation order fixed: the \"Re-parse All\" queue now processes replays in oldest-first order (previously newest-first), consistent with how recalculateAllRatings rebuilds ratings chronologically.\n\n🔧 NICKNAME ACCOUNT ID FIX\nThe nicknames table (which drives rank sync, leaderboard names, and player profiles) was storing the same wrong account IDs (all ending in 2) as player_stats was before the Steam64 precision fix. A new \"Fix Nickname IDs\" button appears on each backup row in the Database Backups panel. It uses the pre-reparse backup to build a precise match_id+slot mapping of old→new account IDs, then updates every row in nicknames atomically. After running this fix, trigger Rank Sync to re-fetch all player ranks with the correct IDs.",
    "author": "System"
  },
  {
    "version": "5.17",
    "title": "Leaderboard Form & Streak Canonical Merging Fix",
    "published_at": "2026-04-18",
    "content": "Fixed form dots and win/loss streak on the leaderboard being wrong for players whose match history spans multiple account IDs under the same nickname.\n\nRoot cause: TrueSkill (and player profile pages) correctly merge all account IDs sharing a nickname into a single canonical player — but the form and streak queries were grouping by raw account_id only, so they only saw a fraction of each player's matches.\n\nFix: getPlayerFormBatch and getPlayerStreaks now apply the same nickname-based canonical ID merging that TrueSkill uses. All match results for a given player are aggregated across every account ID that shares their nickname before the form sequence and current streak are computed.\n\nThis means form dots and streak badges on the leaderboard now correctly reflect a player's full recent match history.",
    "author": "System"
  },
  {
    "version": "5.18",
    "title": "Overhauled Match Performance (Perf) Score",
    "published_at": "2026-04-18",
    "content": "Completely redesigned the per-match Performance score shown on match scoreboards. Now uses per-metric independent z-score normalisation for a genuinely balanced, position-neutral rating.\n\nNew formula — each factor is z-scored independently within the match, then combined:\n• Kill Involvement 25% — (kills + assists×0.5) / team kills.\n• Hero Damage 20% — raw damage output, naturally rewards carries and mids.\n• Survival 18% — 1/(deaths+1), lower deaths = higher score at every position.\n• Net Worth 15% — farming and resource efficiency.\n• Healing 12% — primary contribution path for dedicated supports.\n• Tower Damage 7% — rewards objective and split-push play.\n• Win Bonus 3% — symmetric binary z-score.\n\nKey improvement: by z-scoring each metric independently, a support's healing outlier carries exactly the same weight as a carry's damage outlier relative to their own distributions. A hero with 0 healing is not penalised — they score at the match average on that component, which is where most players sit. Every role has meaningful contribution paths without any component unfairly dominating.",
    "author": "System"
  },
  {
    "version": "5.19",
    "title": "Lobby System Overhaul",
    "published_at": "2026-04-19",
    "content": "A collection of improvements to the lobby creation, invite, and management flow.\n\n🎮 LOBBY CREATION\n• !create_lobby no longer requires a password — just !create_lobby [name] (name defaults to \"OCE Inhouse\"). Players join via the bot's Steam friends list or a direct invite.\n\n📨 INVITES\n• !invite_me — type this in Discord to get a lobby invite sent directly to your Steam. Requires your Discord ID to be linked on the Players page.\n• !invite @user — admins can now @mention someone instead of pasting their Steam64 ID.\n• !invite <steam64_id> — unchanged, still works as before.\n\n💬 IN-GAME LOBBY CHAT COMMANDS\nType these directly in the Dota 2 lobby chat:\n• !start_game (or !start) — launches the game immediately.\n• !status — bot replies with how many players are seated.\n\n📋 !HELP UPDATED\nThe !help command now has a full Lobby Management section covering all of the above plus !lobby_status, !join_lobby, and !end.\n\n🔧 BACKEND\n• !register now also links your Discord ID to your nickname entry, so !invite_me works immediately after registering.\n• !invite_me lookup now checks both the nicknames and players tables so the command works regardless of how the Discord ID was linked.",
    "author": "System"
  },
  {
    "version": "5.20",
    "title": "Bug Fixes: Players Command & Dual-Instance Guard",
    "published_at": "2026-04-19",
    "content": "🔧 BUG FIXES\n• !players now correctly shows all 66 community members from the nicknames table (previously queried the empty players registration table and always returned \"No players registered\").\n• Fixed duplicate bot responses: when the Replit dev instance detects it cannot acquire the Steam session (LoggedInElsewhere), it now automatically silences all Discord command responses so only the production bot replies.\n• Fixed lobby auto-join: lobby ID type mismatch (bigint vs string) in the invite-accept handler was preventing the bot from recognising when it successfully joined an externally hosted lobby.",
    "author": "System"
  },
  {
    "version": "5.21",
    "title": "Lobby Commands: Captains, Roll & Invite Fix",
    "published_at": "2026-04-19",
    "content": "🎲 NEW LOBBY COMMANDS\nWork in both Dota 2 lobby chat and Discord:\n• !captains — picks 2 random captains from the players currently in the lobby.\n• !roll — rolls a number between 1 and 100. In-game: shows all players ranked by their roll. In Discord: rolls for the person who typed it.\n• !hrcaptains — picks the 2 highest-ranked players in the lobby (by TrueSkill MMR) to be captains.\n\n🔧 LOBBY INVITE FIX\nFixed the bot not joining when invited to a lobby from Dota 2. Previously the bot sent an invite acceptance but never explicitly joined. Now it sends both the acceptance and a CMsgPracticeLobbyJoin, which is what the GC actually requires.",
    "author": "System"
  },
  {
    "version": "5.22",
    "title": "Leaderboard Rank Fix, Admin Register & UI Polish",
    "published_at": "2026-04-19",
    "content": "🔧 DOTA RANK FIX — MERGED ACCOUNTS\nPlayers with two Steam accounts merged under the same nickname were showing a blank Dota rank on the leaderboard even if one of their accounts had a rank synced. Root cause: the leaderboard uses a canonical (merged) account ID, but the rank was stored against a different account ID in the same group.\n\nFix: rank data is now resolved across all account IDs in a merged group at the leaderboard query level. The highest rank across all merged accounts is shown — so a player who is Immortal on one account and Ancient on another will correctly display Immortal.\n\n🛠️ !ADMINREGISTER COMMAND\nAdmins can now register players on their behalf without the player needing to type anything:\n  !adminregister @Player <steam64_id>\nOnly the bot owner can use this command.\n\n🎨 LEADERBOARD UI\n• Tier badges no longer show emoji — cleaner, consistent pill style matching the Dota Rank column.\n• Column order updated: TIER → DOTA RANK → MMR, grouping both badge columns together and separating the MMR number.",
    "author": "System"
  },
  {
    "version": "5.23",
    "title": "Lobby GC Connection Fix & Better Error Messages",
    "published_at": "2026-04-19",
    "content": "🔧 LOBBY GC CONNECTION FIX\nFixed a bug where the 45-second GC startup timeout incorrectly marked the Dota 2 Game Coordinator as \"ready\" even if it had not actually connected. This caused !create_lobby to proceed past the availability check and fail silently or produce a confusing \"Lobby manager is not available\" message.\n\nFix: the timeout now resolves Steam login without forcing isGCReady=true. The GC ready flag is only set when the GC actually emits its ready event, so lobby commands correctly block until the GC is up.\n\n💬 IMPROVED ERROR MESSAGES\n• !create_lobby now checks GC state before attempting lobby creation and shows a clear \"Dota 2 Game Coordinator is not connected yet — wait a moment and try again, or check !steam_status\" message instead of a generic failure.",
    "author": "System"
  },
  {
    "version": "5.24",
    "title": "Post-Match DM Fix for Merged Accounts",
    "published_at": "2026-04-19",
    "content": "🔧 POST-MATCH DM FIX — MERGED ACCOUNTS\nFixed a bug where players with multiple Steam accounts (merged under one nickname) were not receiving post-match rating DMs even though their Discord ID was visibly linked on the Players page.\n\nRoot cause: the DM system looks up Discord IDs by joining on the exact account_id used in the match. If the Discord ID was registered against a different account in the same merged group, it would not be found — the Players page masked this by grouping all accounts together and showing the MAX discord_id across the group.\n\nFix: the Discord ID lookup now has two extra fallback layers:\n1. Checks any other nicknames row sharing the same display name (catches merged accounts).\n2. Checks the players registration table directly by account_id.\n\nThis means players like tomato and Sun who play on a secondary Steam account will now receive DMs correctly.",
    "author": "System"
  },
  {
    "version": "5.25",
    "title": "Lobby Command Fix",
    "published_at": "2026-04-20",
    "content": "🔧 LOBBY COMMANDS FIXED\nFixed a syntax error in the lobby manager that prevented it from loading entirely — the lobbyChatMessage event handler was missing its async keyword, causing a SyntaxError at module load time. This broke all lobby commands (!create_lobby, !start_lobby, etc.) even when Steam and the GC were fully connected.\n\nAlso added startup resilience: lobby commands now fall back to the module singleton if the startup initialisation was skipped, and the startup sequence logs each step individually so failures are immediately visible.",
    "author": "System"
  },
  {
    "version": "5.26",
    "title": "Join the League — Application Improvements",
    "published_at": "2026-04-21",
    "content": "📋 JOIN THE LEAGUE IMPROVEMENTS\n\n• New \"How did you hear about us?\" referral question added to the application form (required field, stored and displayed in Admin Panel).\n\n• Auto-registration on approval — when an admin approves an application, the player is automatically registered using their Steam profile URL. Supports both numeric /profiles/ URLs and custom vanity URLs (resolved via Steam's community XML endpoint, same method as steamid.io).\n\n• Discord DMs on approve/reject — applicants now receive a DM when their application is reviewed. Approval DMs include a server invite link (if configured) and confirm their registration status. Rejection DMs include any notes left by the admin.\n\n• Quick Links button — a new \"📋 Applications\" button appears in the Admin Panel Quick Links section with a red badge showing the number of pending applications.",
    "author": "System"
  },
  {
    "version": "5.27",
    "title": "Weekend Points Tournaments + Profile Averages",
    "published_at": "2026-04-21",
    "content": "🏆 WEEKEND POINTS TOURNAMENTS\nA brand-new tournament format designed for flexible play across multiple days — no fixed schedule needed.\n\n• Each player earns a per-game score based on a custom formula covering Kills (+4), Assists (+2.5), Deaths (-3), Last Hits (×0.04), GPM (×0.25), XPM (×0.22), Hero/Tower Damage, Healing, Observer Wards (+6), Sentry Wards (+8), Dewarding (+10), Camps Stacked (+7), and a Win Bonus (+25).\n\n• Play as many or as few games as you like during the tournament window — only your top N individual game scores (default: 3) count towards your total.\n\n• Live leaderboard at /weekend-tournament shows standings with expandable per-game score breakdowns so you can see exactly where every point came from.\n\n• Admins can create/edit tournaments and post a formatted Discord announcement embed directly from the tournament page.\n\n• Homepage banner automatically appears when a tournament is active or upcoming.\n\n👤 PLAYER PROFILE AVERAGES\nThree new stats added to every player's Averages section:\n• Average Observer Wards per game\n• Average Sentry Wards per game\n• Average Neutral Camps Stacked per game",
    "author": "System"
  },
  {
    "version": "5.28",
    "title": "Team Assignment + Voice Channel Movement",
    "published_at": "2026-04-22",
    "content": "⚔️ TEAM ASSIGNMENT\n!balance now labels teams as Radiant and Dire and saves the result. Run !assign immediately after to apply it:\n\n• Bot attempts to move all players into their correct Radiant/Dire lobby slots via the Game Coordinator.\n• If GC slot movement is not accepted, players are notified via lobby chat which slot to join.\n• Players are simultaneously moved into the correct Discord voice channels (Radiant / Dire) as long as they are already in any voice channel.\n• The matchStarted event also auto-moves players to their voice channels when the game launches, using their team assignment from the lobby.\n\n🌏 LOBBY SERVER REGION\nDefault lobby server changed from Singapore (SEA) to Australia.\n\n🎙️ DISCORD VOICE INTENTS\nBot now has GuildVoiceStates intent enabled, required to see and move members between voice channels.",
    "author": "System"
  },
  {
    "version": "5.29",
    "title": "Game Connection Fix — Bot Spectator Slot",
    "published_at": "2026-04-22",
    "content": "🔧 GAME CONNECTION FIX\nFixed the root cause of games failing at the \"Players Connecting\" screen and dropping everyone back to lobby.\n\n• The bot was being placed in the Broadcaster slot (team 4) after lobby creation. Broadcaster slots — like player slots — require a Dota 2 game client to connect to the game server during the loading phase. Since the bot has no game binary, it never connected, causing the 60-second timer to expire and cancelling the match.\n\n• Fix: the bot now moves itself to the Spectator slot (team 5) using an explicit admin-move GC message that includes its own Steam64 ID. Spectators are not required to connect for the game to start. The move is sent at 2s, 4s, and 8s after lobby creation/join as redundancy.\n\n• Pre-launch safety check: immediately before sending the launch command, the bot inspects the lobby member list to verify it is not in a game slot (Radiant/Dire/Broadcaster). If it is, it moves itself and waits 1.5s before launching.\n\n• Connection failure detection: if the game still fails to connect (e.g., a real player doesn't load), the bot now detects the gameState dropping back to INIT, resets its internal player count, and announces in Discord and lobby chat so players know to re-seat. The countdown re-arms automatically.\n\n• GC logs now show human-readable game state names (e.g., WAIT_FOR_PLAYERS, IN_PROGRESS) and the bot's current slot in every lobby update for easier debugging.",
    "author": "System"
  },
  {
    "version": "5.30",
    "title": "Game Connection Fix — Bot Leaves Lobby After Launch",
    "published_at": "2026-04-22",
    "content": "🔧 DEFINITIVE GAME CONNECTION FIX + FULL AUTO-RECORD\nConfirmed root cause: the bot is the lobby host/creator. Valve's game server requires the host account to connect during the \"Players Connecting\" phase. Since the bot has no Dota 2 binary it can never connect as host, causing the game to be cancelled. This happened consistently — all 10 real players were loading correctly but the game failed twice; recreating the lobby without the bot worked immediately.\n\n• The bot now leaves the lobby right after sending the launch command. Host ownership transfers automatically to a real player, and the game server proceeds with only the 10 real players.\n\n• Match ID capture: the bot stays connected to GC for up to 25 seconds post-launch to capture the match ID before leaving. If captured, it announces the match ID in Discord.\n\n• Auto-record: after leaving, the bot polls Valve's GC directly (k_EMsgGCMatchDetailsRequest) every 5 minutes for up to 3 hours. When the GC reports match_outcome != 0 (game over), the bot downloads the replay from Valve's CDN, parses it through the Java parser for full stats, archives the .dem file to the replay store (downloadable from the website), and runs TrueSkill recalculation. If the replay download fails, basic win/loss and K/D/A stats from the GC response are recorded as a fallback.\n\n• Connection failure detection from v5.28 is retained as a safety net for future edge cases.",
    "author": "System"
  },
  {
    "version": "5.31",
    "title": "Leave-Before-Launch Fix & !gc_record Command",
    "published_at": "2026-04-23",
    "content": "🔧 ROOT CAUSE FIX: Bot now leaves the lobby BEFORE the game is launched.\n\nValve enforces that the practice lobby creator must stay in a game slot at all times — moving to Spectator via GC API is silently rejected for the host. The v5.30 \"leave after launch\" approach failed because the game server already committed the bot's slot at launch time; the \"Players Connecting\" timer then expired waiting for the bot account to connect.\n\nThe fix: when !start_game is called, the bot checks if it is still occupying a Radiant or Dire slot. If it is (which it always will be for the lobby creator), it leaves the lobby before any launch command is sent. This makes the slot empty at launch time, so fill_with_bots can assign it to an AI bot. Host ownership transfers automatically to whoever Valve picks from the remaining players.\n\n• Discord announces the new host and provides step-by-step instructions to launch from the Dota 2 client.\n• New !gc_record <matchId> command: after the game ends, type the match ID shown on the post-game screen. The bot immediately queries the GC, downloads the replay, parses full stats, and records everything — same as the auto-record pipeline.\n• allow_cheats and fill_with_bots lobby options now correctly pass through to the GC encoder (were previously hardcoded to false).",
    "author": "System"
  },
  {
    "version": "5.32",
    "title": "Invite-Join & Spectator Watch Reliability Fixes",
    "published_at": "2026-04-23",
    "content": "Two correctness fixes to the invite-join flow and the spectator watch loop:\n\n• Fixed _moveBotToSpectator undoing itself: it was sending team=4 (Spectator) immediately followed by team=5 (PlayerPool) 100 ms later, so the bot always landed in PlayerPool instead of Spectator. Now only team=4 is sent (with the same 1 s / 3 s / 7 s retries). For invite-joined lobbies (bot is not the creator), this means the bot correctly takes a Spectator slot.\n\n• Fixed invite-handler vs spectator-watch race condition: when the spectator watch loop is mid-poll it briefly sets the lobby state to WAITING. If a lobby invite arrived during that 3-second window it was silently dropped. The handler now detects an active watch, cancels it cleanly (leaves the transient lobby), resets state to IDLE, and then proceeds to accept the invite. Cancellation is also checked inside the poll window so the watch exits immediately if an invite arrives while waiting for the GC CSO.\n\n• spectatorJoined Discord notification now shows the player count observed (e.g. \"watching the game (10 players)\").",
    "author": "System"
  },
  {
    "version": "5.33",
    "title": "Rank Badge, Unregistered Player Fixes & Tournament Date Correction",
    "published_at": "2026-04-24",
    "content": "Three fixes in one build:\n\n• Rank badge propagation: getAllPlayerRanks() now resolves the best rank across all accounts sharing the same nickname. Previously, if a player had two accounts (e.g. \"Burtle\" on accounts A and B) and only account B had its Immortal rank stored, account A would show no badge on the profile page. Now both accounts display the highest rank in the group.\n\n• New Admin Panel section — \"Unregistered Players\": click \"Check Unregistered Players\" to see all accounts that have match history but no registered nickname, ordered by game count. Rows where the Steam name matches another account are flagged as \"possible duplicate\" (amber highlight). This makes it easy to spot accounts that need registering and to identify alt/duplicate accounts.\n\n• Tournament date timezone fix: datetime-local inputs were sending the entered time as UTC to the server (e.g. 5pm AEST stored as 5pm UTC = actually 3am AEST next day), causing the tournament header to show the wrong date. Dates are now correctly converted from local browser time to UTC on save, and from UTC back to local time when loading the edit form. Existing tournaments with wrong dates: open the edit form, correct the start/end dates, and re-save.",
    "author": "System"
  },
  {
    "version": "5.34",
    "title": "Tournament Ward Scoring Reduced",
    "published_at": "2026-04-24",
    "content": "Vision scoring values reduced for weekend tournaments:\n\n• Observer Ward placed: 6 → 4 pts\n• Sentry Ward placed: 8 → 6 pts\n• Ward Destroyed (deward): 10 → 7 pts\n\nAll other scoring weights (kills, deaths, GPM/XPM, tower damage, stacks, win bonus) are unchanged.",
    "author": "System"
  },
  {
    "version": "5.35",
    "title": "Tournament Win Efficiency Bonus",
    "published_at": "2026-04-25",
    "content": "Added a win efficiency bonus to weekend tournament scoring. Winners now receive +1 point for every minute the game finishes under 35 minutes — rewarding dominant short wins that were previously under-rewarded compared to long attrition games.\n\nExamples:\n• 20 min win: +25 base + 15 efficiency = +40 total\n• 25 min win: +25 + 10 = +35 total\n• 30 min win: +25 + 5 = +30 total\n• 35+ min win: +25 + 0 = +25 total\n• Any loss: +0 regardless of game length\n\nAll other scoring weights unchanged. The efficiency bonus shows as its own line in each game's score breakdown.",
    "author": "System"
  },
  {
    "version": "5.36",
    "title": "Deward Double-Count Fix & Scoring Restored",
    "published_at": "2026-04-25",
    "content": "Fixed a replay parser bug where dewarded ward counts were being doubled, and restored the tournament deward scoring weight to its original value.\n\nThe parser listens to two separate event systems for ward deaths — streaming-mode events (obs_left / sen_left) and combat-log events (obs_left_log / sen_left_log). Both systems were firing for the same physical ward kill, causing every deward to be counted twice. A player with 23 actual dewards would show 46 in their stats.\n\nThe fix deduplicates by (killer, ward type, timestamp) so each real ward kill is counted exactly once regardless of how many event types report it. The same deduplication is applied to the ward lifespan tracking.\n\nThe tournament deward scoring weight has been restored from 7 back to 10 pts per deward — v5.34 lowered it to compensate for inflated counts, but now that the root cause is fixed the weight reflects true game value again.\n\nAny matches already in the database with inflated deward counts will need to be re-parsed from their replay files to get corrected values.",
    "author": "System"
  },
  {
    "version": "5.37",
    "title": "Dedicated Server Integration",
    "published_at": "2026-04-27",
    "content": "Integrated a DigitalOcean-hosted Dota 2 dedicated game server (SYD1, Sydney) into the bot and lobby system.\n\n• Lobby creation now passes a dedicated server IP hint to the Game Coordinator via the lan_host_ping_to_server_ip field, so the GC can route practice lobbies to the community server.\n• Discord announces when a game server is assigned — showing whether the GC routed to the dedicated server or a Valve server.\n• New !ds_status command pings the dedicated server via A2S_INFO (Source query protocol) and reports Online/Offline, address, and Steam ID in a Discord embed.",
    "author": "System"
  },
  {
    "version": "5.38",
    "title": "FACEIT-Style Inhouse Lobby — Match Accept, Captain Draft, Direct Connect",
    "published_at": "2026-04-28",
    "content": "Built a complete FACEIT-style inhouse flow on top of the dedicated server, available at /inhouse on the dashboard.\n\n• New Inhouse Lobby page: players sign in, register interest with preferred positions (P1–P5), then go through a timed Accept Phase with a live countdown.\n• Captain selection supports three modes: Highest Rank (TrueSkill MMR), Random, and Highest Roll (1–100 dice per player). Two captains are auto-promoted into Team 1 / Team 2.\n• Captain draft UI: admins click \"→ T1\" / \"→ T2\" next to each unpicked player to assign them. Pick order is recorded.\n• Provision Server: generates a per-match lobby password and pushes it to the dedicated server over Source RCON. Players see a one-click \"Connect to Server\" button (steam://connect/IP:PORT/PASSWORD) plus a fallback console command they can paste into Dota.\n• SSH replay puller: an admin button (and new !ds_replay command) SCPs the latest .dem from the dedicated server, ready to feed into the OpenDota Java parser. The existing GC replay-salt path is retained as an alternate route for matches that have a Valve match ID.\n• New Discord commands: !inhouse status (admin), !inhouse open (admin), !ds_replay (admin).\n• Status banner on the page shows live RCON + SSH health for the dedicated server.\n• Database: two new tables (inhouse_sessions, inhouse_session_players) store the full session lifecycle — open → accepting → drafting → in_progress → completed/cancelled — with team assignments, accept timestamps, and per-match passwords.",
    "author": "System"
  },
  {
    "version": "5.39",
    "title": "Points Tournament Auto-Completion & Winner Announcement",
    "published_at": "2026-04-28",
    "content": "Fixed points (weekend) tournaments not updating their status when their end date passes.\n\n• Status was stored as \"upcoming\" in the database and never automatically transitioned — so concluded tournaments showed as \"Upcoming\" on the Tournaments page.\n• The API now auto-transitions each points tournament to \"active\" when its start date is reached, and to \"completed\" when its end date passes, on every load.\n• When a tournament is auto-completed for the first time, the bot announces the final results to the Discord announcement channel — showing the winner, their total score, and a top-5 leaderboard.\n• The frontend also derives effective status from dates as an instant safety net, so the correct badge appears even before the API has persisted the new status.",
    "author": "System"
  },
  {
    "version": "5.40",
    "title": "TrueSkill V3 — Improved Rating System",
    "published_at": "2026-04-29",
    "content": "Built a new TrueSkill V3 engine that fixes three real problems with the original rating system, available behind an admin toggle and intended to go live at the start of the next season.\n\n• Draw probability fixed: V1 used the library default of 10% draw chance, but Dota matches can never draw. V3 sets it to 0% so the model better matches reality.\n• Sigma floor: V1 lets uncertainty (σ) collapse toward zero for veteran players, freezing their MMR even after losses. V3 floors σ at 2.5 and bumps the per-match drift (tau) so ratings stay responsive across long careers.\n• Per-match performance modifier: V2 (now retired) used a raw K/D/A modifier that unfairly penalised supports. V3 reuses the weekend tournament scoring formula — kills, assists, GPM, XPM, hero damage, healing, wards, dewards, camps stacked, and a win bonus — z-scored within each match and capped at ±20% of the μ update.\n• Admin Panel: the old \"TrueSkill 2 — Experimental\" section has been replaced with a new \"Rating System\" card showing the active engine (V1 or V3), a toggle button with confirmation dialog, and a collapsible side-by-side V3 vs V1 preview table. V2 is retired.\n• System is OFF by default — V1 stays active until an admin flips it. Switch is database-backed so it survives restarts.",
    "author": "System"
  },
  {
    "version": "5.41",
    "title": "V3 MMR Breakdown — \"Why Did My MMR Change?\"",
    "published_at": "2026-04-29",
    "content": "Players can now see exactly how each match's performance affected their TrueSkill V3 update.\n\n• Match scoreboard: a new \"Mod\" column on each team table shows every player's V3 performance modifier for that match (0.80×–1.20×). Hovering the badge opens a tooltip with the full score breakdown — kills, assists, deaths, GPM, XPM, hero damage, tower damage, healing, camps stacked, observer wards, sentry wards, dewards, and the +25 win bonus — using the exact same math the season rating engine uses.\n• Player profile: a new \"V3 Per-Match Modifier\" line chart shows the modifier across every match the player has played, plotted on a 0.78–1.22 scale so consistency and recent form are easy to read at a glance.\n• Lobby-only matches (no replay parsed) appear in both the scoreboard and the profile chart at exactly ×1.00 — drawn as hollow grey dots — to mirror how V3 actually treats them: no penalty for missing detailed stats.\n• Backed by two new API endpoints (one per match, one per player) and 7 new automated tests covering breakdown maths, lobby fallbacks, and chronological history ordering.",
    "author": "System"
  },
  {
    "version": "5.42",
    "title": "Season 10 launch foundation",
    "published_at": "2026-04-29",
    "content": "Built the launch infrastructure that everything else for Season 10 ships behind, so new features can be staged for superuser preview without affecting the live site, and a single cron flips them all on at season start.\n\n• Feature flags: new three-state toggle (off / preview / on) backed by a feature_flags table. Off = hidden from everyone, preview = visible only to logged-in superusers, on = live for all. Resolved at request time so superusers get a real preview of upcoming features without test-environment drift.\n• Admin Feature Flags panel: new section in the Admin Panel listing every flag with a state dropdown and description editor, plus a one-click \"Launch Season 10 Now\" button (with confirmation) for manual cutover.\n• Season 10 launch cron: scheduled for Friday 1 May 2026 9:00am AEST. Bulk-flips every preview flag to on, stamps the launch timestamp, and posts a launch announcement to Discord. Idempotent — re-runs are no-ops.\n• Home page launch banner: appears when the home_launch_banner flag is enabled, dismissible per-browser, themed for the new season.\n• Public flag endpoint: GET /api/feature-flags returns the resolved flag map for the caller, used by a new FeatureFlagsProvider on the frontend.",
    "author": "System"
  },
  {
    "version": "5.43",
    "title": "Wave 1 — Season 10 features behind preview flags",
    "published_at": "2026-04-29",
    "content": "Wave 1 of the Season 10 feature push is in. Everything below is staged behind preview flags and stays hidden from the public until the launch cron runs at Fri 1 May 2026 9:00am AEST (or a superuser flips a flag in the admin panel). Each item lists the flag that controls it.\n\n• MMR baseline rebased to 5000 — display MMR is now `round((μ − 3σ) × 100) + 5000`, so a fresh player at μ=25/σ=8.333 reads as exactly 5000 on the leaderboard, profile, and every other surface that shows MMR. The underlying TrueSkill maths are unchanged.\n• 8-tier MMR ladder + new rank theme (`new_rank_theme`) — new MmrBadge component renders Tier I–VIII with MMR floors at 0/2000/3000/4000/5000/6000/7000/8000. Tier V is the default for new players. Tier names are placeholders for now and editable per-season in the admin panel.\n• Multi-tier seasons (`multi_tier_seasons`) — new `season_tiers` and `season_tier_players` tables; admin can seed the default 8-tier ladder for any season, edit tier names/MMR floors, and one-click \"place all players by MMR\". Tournaments now carry an optional `tier_number` so they can be scoped to a single tier.\n• Per-tournament Stripe buy-ins + self-signup (`tournament_self_signup`) — new `tournament_entries` table, plus `entry_fee` / `signup_opens_at` / `signup_closes_at` columns on `tournaments`. Players see a self-signup button on the tournament page that runs through Stripe Checkout (or marks them entered for free events), and the prize pool auto-grows from collected entry fees. Eligibility is enforced server-side against the tournament's tier.\n• MVP-per-match badges (`mvp_match_badges`) — match scoreboard and player profile match history now render a ⭐ next to the player who won the post-match MVP vote for that match. Backed by a correlated subquery on `match_mvp_votes`.\n• Profile chart v2 (`profile_chart_v2`) — new \"Performance Trend\" widget on player profiles, showing 5-game rolling K/D/A and GPM across the last ~100 matches. Only visible to the logged-in player on their own profile.\n• Home page Join button (`home_join_button`) — prominent CTA on the home page routing to `/join` for prospective players.\n• Welcome modal (`welcome_modal_s10`) — one-shot launch modal with patch-notes / leaderboard CTAs; remembered per-browser via localStorage.\n• Tournament \"Invalid upload key\" bug fixed — admin tournament endpoints now accept either `x-upload-key` or `x-superuser-key`, matching how the frontend already authenticates.\n• Season 10 launch cron polished — Friday cron now also creates Season 10 and seeds its 8-tier ladder, then bulk-flips every preview flag to on. Discord launch embed updated to mention the new tier ladder, tournament buy-ins, MVP badges, and the Join the League page.\n• Admin tooling — new \"Season Tiers\" panel in the admin page sits next to \"Feature Flags\" and exposes seed / edit / place-all controls per season.",
    "author": "System"
  },
  {
    "version": "5.44",
    "title": "Manual-only Season 10 launch + web security hardening",
    "published_at": "2026-04-30",
    "content": "Two things in this build: removed the automatic Season 10 launch cron (the launch is now a deliberate, double-confirmed superuser action), and a sweep of web-server security fixes.\n\n• Season 10 launch cron removed — there is no longer any scheduled job that flips Wave 1 features live on Fri 1 May. The \"Launch Season 10 Now\" button in the Admin Panel is the only path. The button now requires two confirmations: a detailed dialog listing every preview flag that will flip and every side-effect (banner enable, Discord post, timestamp stamp), followed by a typed-phrase prompt where the superuser must type \"LAUNCH SEASON 10\" exactly. Either step cancels the launch.\n• Stripe webhook signature now mandatory — the `/api/stripe/webhook` endpoint previously fell back to accepting unsigned JSON when `STRIPE_WEBHOOK_SECRET` was not configured. That meant anyone who guessed the URL could POST a forged `checkout.session.completed` payload and mark arbitrary tournament entries / season buy-ins as paid. The endpoint now refuses to process anything without a valid signature and returns 503 (no secret) or 400 (missing/invalid signature) instead.\n• Session cookies marked Secure in production — the session cookie is now `Secure` whenever `NODE_ENV=production`, with `trust proxy` enabled so the reverse proxy's `X-Forwarded-Proto` is honoured. Local dev over plain HTTP is unaffected.\n• CORS allowlist instead of wildcard — `app.use(cors())` previously echoed any `Origin`, which made cross-site credential abuse easier. CORS is now an allowlist driven by the `CORS_ALLOWED_ORIGINS` env var (comma-separated). Same-origin and non-browser callers are always allowed; everything else must be on the list.\n• Rate limit on the public Join form — `POST /api/join` is now wrapped in a new `publicWriteLimiter` (10 requests / hour / IP) to stop spam and enumeration. The Steam OpenID and admin login routes keep the existing `authLimiter`.",
    "author": "System"
  },
  {
    "version": "5.45",
    "title": "Wave 2 + Wave 3 — Season 10 features behind preview flags",
    "published_at": "2026-04-30",
    "content": "Seven new features built in a single pass, all gated behind preview flags so they stay invisible to the public until a superuser flips them in the Admin Panel. Each item lists the flag that controls it.\n\n• Hero analytics overhaul (`hero_meta_v2`) — new \"Hero Meta V2\" panel on /heroes with position-specific win rates (safe / mid / off / soft sup / hard sup) computed from `lane_role`, plus pick frequency. Backed by a new `db.getHeroMetaV2({ tier?, season? })` helper that joins match_players with seasons and supports per-tier filtering via `season_tier_players`. New endpoint `GET /api/heroes/meta-v2`.\n• Draft Assistant V2 (`draft_assistant_v2`) — new endpoint `POST /api/draft/suggestions { allies, enemies, banned, side, season }` returns per-hero scores blending (avg WR vs each enemy pick) with (avg WR alongside each ally pick), excluding banned heroes. Helper `db.getDraftSuggestionsV2`. The existing /draft page continues to work unchanged; the V2 endpoint is ready for a richer UI to consume.\n• Season Pass (`season_pass_s10`) — new `season_pass_xp_events` table (id SERIAL PK, account_id, season_number, match_id, source, xp_delta, UNIQUE constraint on (account_id, season_number, match_id, source) so re-recording a match is idempotent). XP rules: win=+30, loss=+10, mvp=+20, hot_streak_5=+50, hot_streak_10=+100. Auto-grant runs after `recordMatch()` commits and after `saveMatchRating()` records an MVP vote (best-effort — failures don't roll back the match). Six tiers: Bronze 0 / Silver 100 / Gold 300 / Platinum 700 / Diamond 1500 / Master 3000. New endpoints: `GET /api/player/:id/season-pass`, `GET /api/season-pass/leaderboard`, `POST /api/admin/season-pass/recompute`. UI: progress bar with current tier and XP-to-next on PlayerProfile, optional XP column on the Leaderboard.\n• Notification preferences (`notification_prefs`) — new `notification_prefs` table (id SERIAL PK, account_id, category, enabled, updated_at, UNIQUE on (account_id, category)). Six categories: post_match_dm, mvp_vote, attitude_vote, hot_streak, schedule_reminder, weekly_recap. New page at /settings/notifications with a row per category and toggle switches. Bot DM senders (`_sendReportCardDMs`, `_initiateRatingSession`, `_sendScheduleReminders`) now consult `db.isNotificationEnabled(accountId, category)` before sending — defaults to enabled, so existing players see no behaviour change until they opt out. New endpoints: `GET/POST /api/me/notifications`.\n• Tournament bracket live (`tournament_live_v2`) — new \"📡 Live\" panel inside /tournaments/:id showing current prize pool, configurable prize-distribution split (default 50/30/20, configurable via `prize_split` JSONB column on `tournaments`), and the tournament's recent matches. Auto-refreshes every 30 seconds. New endpoints: `GET /api/tournaments/:id/live`, `POST /api/tournaments/:id/prize-split` (admin only).\n• MVP / attitude analytics (`mvp_attitude_analytics`) — new \"🌟 MVP & Attitude Trends\" section on PlayerProfile showing total MVP wins, MVP rate, average attitude rating, and a rolling 10-game window summary. Helper `db.getMvpAttitudeTrends(accountId, window)`. New endpoint: `GET /api/player/:id/mvp-attitude-trends?window=10`. Voter-quality weighting and the dispute mechanism are parked for a follow-up build.\n• Web push (`web_push`) — new `web_push_subscriptions` table (id SERIAL PK, account_id, endpoint UNIQUE, p256dh, auth, user_agent, last_seen_at). Service worker shipped at /sw.js. The settings page lets a player enable, test, and disable push notifications; subscriptions can only be removed by their owner. New endpoints: `GET /api/web-push/public-key`, `POST /api/me/push/subscribe`, `POST /api/me/push/test`, `DELETE /api/me/push/subscriptions`. Requires three new env vars to actually deliver pushes: `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` (e.g. `mailto:admin@example.com`). When VAPID keys are missing the public-key, subscribe and test endpoints all return a clean 503 so the front-end shows a clear \"not yet configured\" message instead of failing silently.\n• Schema additions: 3 new tables (all `id SERIAL PRIMARY KEY`, all created via `CREATE TABLE IF NOT EXISTS` in initDb), 1 new column (`tournaments.prize_split JSONB DEFAULT '[50,30,20]'`), 7 new feature-flag seed rows. No existing primary keys touched.\n• Risks / caveats — Hero Meta V2 currently aggregates the entire matches table per request (no caching); revisit if the hero list ever feels slow. Web push delivery is fire-and-forget per device with automatic 410 / 404 cleanup, but there's no retry queue yet. Dispute mechanism for MVP/attitude is intentionally deferred.",
    "author": "System"
  },
  {
    "version": "5.46",
    "title": "Profile customization — bio, custom title, theme, pinned hero & match",
    "published_at": "2026-04-30",
    "content": "Signed-in players can now personalise their profile page. Everything is gated behind the new `profile_customization` preview flag (default `off`) so it stays invisible to the public until a superuser flips it in the Admin Panel.\n\n• New `player_profiles` table (id SERIAL PK, account_id BIGINT UNIQUE) holds the bio (≤300 chars), custom title, theme accent colour, pinned hero (id + optional ≤80-char caption) and pinned match id. One row per player, upsert on save, no migration needed thanks to CREATE TABLE IF NOT EXISTS in initDb().\n• New cosmetic catalogue (`src/profileCosmetics.js` + mirror `web/src/profileCosmetics.js`) defines the FREE titles (\"Inhouse Veteran\", \"First Bloodhound\", etc.), PREMIUM titles (\"Tournament Champion\", \"Hall of Famer\", \"Pro Member\"), FREE accent colours (blue / green / red / amber / purple), and PREMIUM accent colours (gold, neon-pink, cyan). Validators (isValidTitle / isValidTheme / isPremiumTitle / isPremiumTheme) are shared by client and server so the dropdowns never desync from the API.\n• Three new endpoints on the web server: `GET /api/me/profile` returns the signed-in player's customization plus `is_pro`, `POST /api/me/profile` upserts after validating bio length, premium gating (403), pinned-match ownership (the match must be one the player actually played in), and the title/theme catalogue, and `GET /api/player/:id/profile-card` is the public read used by the profile page — it denormalises the pinned match (winner / duration / start time + the player's own row in that match for hero & KDA) so the card renders in one round trip.\n• New settings page at `/settings/profile` (lazy-loaded, mirrors the SettingsNotifications layout). Sections: Basics (textarea bio with live char counter), Custom title (radio list — premium options shown with a 🔒 PRO pill and disabled until Pro tier ships), Theme accent (swatch picker — premium swatches locked the same way), Pinned hero (typeahead over ALL_HEROES with hero icons + optional caption input), Pinned match (dropdown populated from the player's own recent matches via the existing /api/players/:id endpoint). A live preview card at the top updates as you type so you can see exactly what other players will see before saving.\n• PlayerProfile renders the customization: theme accent paints a left border on the title/bio block (no global recolour), the custom title appears in uppercase under the player name, the bio shows in italic muted text, and the pinned hero / pinned match render as compact cards with the same accent border. An \"✏️ Edit Profile\" link sits next to the Share button when you're viewing your own profile (gated on the flag + Steam-linked own-profile check).\n• Premium hooks for Pro tier — server-side `_isProAccount(accountId)` is a stub that returns false today, so any attempt to save a premium title or theme returns 403 even if a client tampered with the dropdown. When the Pro tier ships, replacing the stub is the only change needed; the route shape, validators and catalogue all stay the same.",
    "author": "System"
  },
  {
    "version": "5.47",
    "title": "Pro Tier — lifetime unlock for premium analytics & cosmetics",
    "published_at": "2026-04-30",
    "content": "First paid tier shipped, gated behind the new `pro_tier` preview flag (default `off`) so the price page and checkout flow stay invisible to the public until a superuser flips it in the Admin Panel.\n\n• New `pro_subscriptions` table (id SERIAL PK, account_id BIGINT, plan_type, status CHECK, stripe_session_id UNIQUE, stripe_payment_intent, amount_cents, currency, purchased_at, refunded_at + 3 indexes). Created via `CREATE TABLE IF NOT EXISTS` in initDb so no migration needed; reserved `plan_type` field leaves the door open for monthly/annual variants without another schema change.\n• Db helpers: `isProMember`, `getProSubscription`, `createProCheckout`, `confirmProPurchase`, `markProRefunded`, `listProMembers`. The membership check is wrapped in a 60-second in-memory cache keyed by account_id (`_proCache` Map at module scope) so gated endpoints don't hit the DB on every request; the Stripe webhook + checkout endpoint invalidate the cache on every state change so newly-paid Pro members see their unlock immediately.\n• Server: replaced the old `_isProAccount()` stub (always-false) with the real cached check, then added a `requirePro(featureKey)` middleware. Behaviour is fail-open and flag-aware — when `pro_tier` is OFF the middleware is a no-op (zero behaviour change for the existing free experience); when it's ON it returns a structured 402 `{ error, paywall:true, feature, signed_in }` payload that the frontend turns into a PaywallCard. Superusers always pass.\n• Six existing endpoints now Pro-gated: `/heroes/meta-v2`, `/heroes/:heroId/skill-builds`, `/hero-matchups`, `/head-to-head`, `/compare`, `/benchmarks`. New endpoints: `GET /api/pro/status` (always-safe, returns is_pro + subscription details), `GET /api/pro/pricing` (flag-gated), `POST /api/pro/checkout` (creates a Stripe checkout session, idempotent on already-Pro, success → /settings/billing), `GET /api/pro/members` (public list of pro account_ids — no PII — for badge rendering), and `GET /api/players/:id/matches/export.csv` (Pro-gated CSV export of own match history with deterministic column order).\n• Stripe webhook extended: `checkout.session.completed` with `purpose === 'pro_lifetime'` flips the pending row to active and stashes the payment_intent; `charge.refunded` matches by payment_intent and marks the subscription refunded. Both invalidate the membership cache.\n• Pricing — $20 AUD lifetime by default, configurable via `PRO_LIFETIME_PRICE_CENTS` env var. One-time payment, no subscription lifecycle, refund = downgrade.\n• Frontend: new `useProStatus` + `useProMembers` hooks (each cached in-module for 30/60s); reusable `<PaywallCard />` and `<ProBadge />` components; new `/pro` page (pitch + benefits grid + Stripe-redirect upgrade button) and `/settings/billing` page (membership status + receipt). The PaywallCard renders inline on the gated pages (Heroes Meta V2 panel, Hero Matchups tab, Player Tools h2h + compare, Player Benchmarks) when the API returns 402, instead of an error toast. ProBadge is surfaced on the Player Profile header and is ready to render on Leaderboard rows once any player upgrades.\n• Cosmetics: with the real `_isProAccount` now wired up, premium titles (\"Tournament Champion\", \"Hall of Famer\", \"Pro Member\") and premium accent swatches (gold, neon-pink, cyan) on `/settings/profile` light up automatically for Pro members and the existing 403 guard now lets their saves through.",
    "author": "System"
  },
  {
    "version": "5.48",
    "title": "Coaching Marketplace — peer-to-peer paid coaching via Stripe Connect",
    "published_at": "2026-04-30",
    "content": "End-to-end coaching marketplace shipped, gated behind the new `coaching_marketplace` preview flag (default `off`) so the entire feature — eligibility CTA, browse page, booking flow, payouts, admin panel, Discord DMs — stays invisible until a superuser flips it. AU/NZ launch only at first; built on Stripe Connect Express with destination charges and a 10% platform take rate. Sessions take place in your community Discord; we deliberately don't ship a built-in video tool.\n\n• Schema (5 new tables, all `id SERIAL PRIMARY KEY` via `CREATE TABLE IF NOT EXISTS`, no ALTERs anywhere): `coaches` (account_id UNIQUE, stripe_account_id, kyc/charges/payouts flags, hourly_rate_cents, currency, bio, languages, taught_roles, taught_heroes, response_time_hours, intro_video_url, sample_replays, status), `coach_availability_slots` (weekly recurring slots: day_of_week, start_time, end_time, timezone), `coaching_bookings` (student↔coach pairing, slot_start_at, duration_minutes, amount_cents, platform_fee_cents, status, stripe_payment_intent UNIQUE, dispute_reason, completed_at, refunded_at, reminder_sent_at), `coaching_reviews` (UNIQUE on booking_id so one review per session), `coach_sanctions` (warning/suspended/delisted audit log). ~30 db helpers covering eligibility, onboarding, availability CRUD, booking lifecycle, dispute/refund, reviews, admin dashboard, KYC inbox, revenue rollups.\n• Eligibility — top-5 on the all-time leaderboard OR Immortal+ Steam rank. The CTA on PlayerProfile is double-gated: 404 from `/api/coaching/eligibility/me` when the flag is off, hidden when `eligible: false`. Two states: `🎓 Apply to coach` → `/coach/onboarding` for first-timers, `⚙️ Coach profile` → `/coach/edit` for existing coaches.\n• Stripe Connect Express — `POST /api/coach/onboard` creates an Express account in `AU` (or NZ) and returns a one-time onboarding URL; the existing webhook is extended to handle `account.updated` (flips coach to `active` when `charges_enabled` + `payouts_enabled` go true) and `payment_intent.succeeded` (purpose=`coaching_booking` → marks booking active + DMs both parties) and `charge.refunded` (extended to also match coaching bookings by payment_intent).\n• Booking flow — `POST /api/coaches/:id/book` builds a Stripe Checkout Session in `payment` mode with `payment_intent_data.application_fee_amount` (10% rounded) and `payment_intent_data.transfer_data.destination` set to the coach's Connect account. Refunds use `refund_application_fee:true` + `reverse_transfer:true` so the coach is debited their share. Status machine: `pending` → `paid` (webhook) → `completed` (both confirm OR auto after 48h grace) | `disputed` (student raises within 48h, freezes funds for admin) | `refunded` (auto on coach no-show after slot start, or admin resolution).\n• Endpoints — `GET /api/coaches` (filters: role, hero, language, max_price), `GET /api/coaches/:id`, `GET/POST /api/coach/me`, `POST /api/coach/me/availability`, `GET /api/coach/onboarding-status`, `GET /api/bookings/:id`, `POST /api/bookings/:id/confirm-completion`, `POST /api/bookings/:id/dispute`, `POST /api/bookings/:id/no-show-refund`, `GET /api/me/coaching/bookings`, `POST /api/coaches/:id/reviews` (server-enforced: must own a `completed` booking), plus admin: `GET /api/admin/coaching/dashboard`, `POST /api/admin/coaching/sanction`, `POST /api/admin/coaching/dispute/:id/resolve`.\n• Discord DMs — three new notification categories added to the prefs table (`coaching_booking_confirmed`, `coaching_session_reminder`, `coaching_review_request`), each toggleable from `/settings/notifications`. Bot exposes `notifyCoachingBookingConfirmed` (called by webhook), `notifyCoachingReviewPrompt` (called when both parties confirm completion), and a new hourly `startCoachingReminderCron` that DMs both parties ~1h before slot start; idempotent via `coaching_bookings.reminder_sent_at`. The cron auto-skips when the flag is off so it's safe to leave running forever.\n• Frontend — five new pages: `/coaches` (browse w/ filters), `/coaches/:id` (detail + book button + reviews), `/coach/edit` (rate/bio/availability/recent bookings + KYC status), `/coach/onboarding` (eligibility check + Stripe Connect kickoff), `/me/bookings` (student & coach views, completion/dispute/refund/review actions). New `<CoachingApplyCta />` component on the Player Profile header. New `<CoachingAdminPanel />` section in the Admin Panel showing pending KYC, open disputes (with one-click release/refund), 30-day platform fee rollup, and a sanction button.\n• Compliance — Stripe Connect handles all KYC/AML/banking; we never store ID or bank details. Buyer protection: 48-hour dispute window from slot start, automatic no-show refund button after slot start, all funds held in escrow (uncaptured payment intent + delayed transfer) until both parties confirm or 48h grace passes.\n• Hardening pass: (1) `validateBookingSlot` helper now runs server-side at booking time — projects the requested UTC start into the coach's local timezone via `Intl.DateTimeFormat`, checks the weekly availability windows, and uses a tstzrange overlap query against existing paid/pending bookings so the same minute can't be double-booked (returns 400 with `slot_unavailable` instead of letting the duplicate row hit Stripe). (2) New `startCoachingAutoReleaseCron` on the bot — every 30 minutes it calls `listAutoReleasableBookings(48)`, flips any non-disputed `paid` row past the 48h grace to `completed` via the new idempotent `autoReleaseBooking` helper, and DMs the student the review prompt. Both crons no-op while the flag is off and clear cleanly on shutdown. (3) Browse page (`/coaches`) and detail page (`/coaches/:id`) now surface inhouse credibility — MMR, W-L, win rate, and (on detail) most-played hero — pulled in a single query via subselects so the cards render without an N+1. (4) Admin panel grew a sixth stat card (lifetime platform revenue across all completed bookings) and a new \"Recent sanctions\" table with severity badges (`warning`/`suspended`/`delisted` color-coded) so superusers can audit the moderation history at a glance. (5) True manual-capture escrow: the booking checkout session now sets `payment_intent_data.capture_method:'manual'`, so funds are only AUTHORIZED at checkout and held by the issuer — money does not move to the coach's Connect balance until we explicitly call `paymentIntents.capture()` on completion (both sides confirm, admin release, or 48h auto-release). Refund paths (no-show, dispute resolve) call `paymentIntents.cancel()` to release the auth — no funds ever moved, no clawback risk, no fee-share refund headaches. Capture/cancel happens BEFORE any DB status flip and a Stripe failure surfaces 502 so the user can retry; the row stays at `paid` (held) until money actually moves. KYC promotion now requires both `charges_enabled` AND `payouts_enabled` — there's no point taking a booking we can't pay out. No-show refund is available the moment the slot start time passes (was 30min after) since by definition the coach hasn't shown up yet. (6) Browse cards on `/coaches` now show language(s) and response-time alongside MMR / W-L so students can filter at a glance without opening every profile. (7) Booking confirmation DMs now include a Discord voice-channel link (uses `DISCORD_INVITE`) so both parties can hop into the community voice channel at the scheduled time, plus a deep-link to `/me/bookings` for cancel / no-show-refund actions; all DM URLs are env-driven via `SITE_URL` (was hardcoded). (8) Reviews are now booking-keyed (`POST /api/bookings/:id/review`) instead of coach-keyed, so a student can still leave a review for a completed session even if the coach has since been suspended or delisted from the active directory.\n\n**Round 5 — payment-flow safety hardening (post automated code review)**: (a) Disputes are now restricted to `status='paid'` (held/uncaptured) bookings only — disputing an already-completed booking would race against captured funds and leave the admin's `release` resolution in `payment_intent_unexpected_state`. After capture the student's recourse is Stripe's chargeback flow, not our admin panel; both the API endpoint and `db.raiseBookingDispute` enforce this. (b) New coach-side arrival signal (`coach_arrived_at` column on `coaching_bookings` via additive `ALTER TABLE ADD COLUMN IF NOT EXISTS`, plus new `POST /api/bookings/:id/coach-arrived` endpoint, plus a `✓ Mark arrived` button on the coach's row in `/me/bookings`) closes the gameable no-show refund — student auto-refund now requires (1) at least 10 minutes past slot start AND (2) `coach_arrived_at IS NULL` AND (3) `coach_confirmed_at IS NULL`. The 1-hour-before reminder DM tells the coach explicitly to click Mark arrived when they show up. (c) Booking horizon capped at 6 days in `validateBookingSlot` — Stripe authorization holds for manual-capture PaymentIntents expire after ~7 days, so allowing further-out bookings guaranteed eventual capture failure. Coaches publish recurring weekly slots; students rebook within the rolling 6-day window. (d) Admin `dispute/:id/resolve` release branch now treats `payment_intent_unexpected_state` from `capture()` as success (idempotent against auto-release / webhook races) instead of returning 502.\n\n**Round 6 — abandoned-checkout slot cleanup + final state-machine guards (post second automated review)**: (e) Pending bookings created when a student opened Stripe Checkout and walked away were leaving 'pending' rows in the DB forever, and validateBookingSlot's double-booking check (which treats 'pending' as live) would block all future bookings on that time slot. Fix: Stripe Checkout Sessions now created with `expires_at: now + 30min` (Stripe's minimum); new `checkout.session.expired` webhook branch flips matching pending bookings to 'cancelled' to free the slot; defense-in-depth — validateBookingSlot now only blocks on `pending` rows where `created_at > NOW() - INTERVAL '35 minutes'` (30min checkout window + 5min webhook-delivery margin), so even if the expired webhook is missed/delayed the slot frees up automatically. New `db.markBookingCancelledBySession` helper (status='pending' guarded for idempotency). (f) Admin `/dispute/:id/resolve` release path made fully race-safe: on `payment_intent_unexpected_state` from capture, we now explicitly retrieve the PI and only treat status='succeeded' as already-captured (any other status — canceled, requires_action, etc — returns 409 without DB mutation, preventing double-pay of the coach if the PI was actually canceled by a parallel path); the DB UPDATE is now transition-guarded (`WHERE id=$1 AND status='disputed'`) so concurrent admin clicks or webhook races can't overwrite a later 'refunded' state. (g) CoachEdit KYC banner now correctly mirrors the backend's activation requirement of BOTH `charges_enabled` AND `payouts_enabled` — previously a coach with charges_enabled but no payouts_enabled would see 'Active' but their bookings would 400 on the validateBookingSlot check; the banner now explicitly says 'Almost there — Stripe needs your payout details (bank account)' with a Continue Stripe setup button.\n\n**Round 7 — requirement-drift cleanup (post code review APPROVED_WITH_COMMENTS)**: (h) Coach eligibility now uses the CURRENT season's computed leaderboard (top 5) rather than the all-time leaderboard, so eligibility rolls over with the ladder every season — a coach who was top-5 two seasons ago but has since dropped off no longer keeps selling sessions on stale credibility. Falls back to the all-time leaderboard only if there is no active season (boot/install case) so eligibility never breaks during season transitions. (i) Added singular-form aliases for the public coach detail route on both backend (`GET /api/coach/:id` shares the same handler as `/api/coaches/:id`) and frontend (`/coach/:id` resolves to CoachProfile alongside `/coaches/:id`), so any links shared in Discord using either form resolve correctly. (j) CoachProfile booking UI replaced the free-text `<input type=\"datetime-local\">` with a real availability-driven slot picker that projects the coach's published weekly windows onto the rolling 6-day booking horizon (matching server's validateBookingSlot cap), respects DST in the coach's timezone, generates only hour-aligned candidate slots that fit the selected duration, and shows them as clickable buttons in the student's local time. Eliminates the silent-failure mode where students would type a local-time guess that landed outside the coach's tz-anchored window and get '400 — Selected time is outside the coach's published availability'.\n\n**Round 8 — last-mile polish (post APPROVED_WITH_COMMENTS code review)**: (k) CoachProfile.jsx hook-ordering fix: the new `useMemo(generateOpenSlots(...))` was placed AFTER the early `if (!data) return ...` returns, which would have triggered React's 'Rendered more hooks than during the previous render' on the loading→loaded transition. Moved the hook before the early returns and switched it to `data?.availability` so it runs unconditionally. (l) CoachOnboarding.jsx eligibility copy now correctly says 'Top 5 on the current season's leaderboard' (matches the round-7 backend change to use the active-season computed leaderboard rather than the all-time one). (m) Admin coaching dashboard revenue caption corrected: was 'after Stripe Connect fees', now reads 'gross 10% platform fee on completed bookings (excluding Stripe processor fees, which are deducted from the coach's split)' — accurately describes what `SUM(platform_fee_cents)` actually returns. (n) `POST /api/coach/me` now explicitly rejects suspended/delisted coaches (kyc_pending is intentionally still allowed so coaches can prepare profile + availability while Stripe verifies bank details — without this, no coach could ever publish a profile without first having an active Stripe account, which itself requires their profile-driven business info to be filled in).",
    "author": "System"
  },
  {
    "version": "5.49",
    "title": "TrueSkill V3 rank thresholds + admin preview access",
    "published_at": "2026-05-01",
    "content": "Two fixes shipped for the V3 rollout:\n\n• IH Rank badges now use V3-calibrated thresholds when V3 is active. V3's MMR base is 5000 (fresh player = exactly 5000) vs V1's 2600, so all 11 tier thresholds are shifted +2400. The leaderboard API now exposes the active rating system alongside results, and both the tier badge column and the rank legend at the top of the leaderboard page switch sets automatically. Toggling back to V1 restores the original thresholds immediately.\n\n• Admin key can now unlock PREVIEW feature flags. Previously the PREVIEW system required a separate SUPERUSER_PASSWORD environment variable (which was not configured), making it impossible to view preview features. The feature-flags endpoint now also accepts the admin upload key as a valid credential for unlocking PREVIEW-state flags. The flags context also sends the admin key on all feature-flag requests, so admins logged into the admin panel will see preview features without any extra setup.",
    "author": "System"
  },
  {
    "version": "5.50",
    "title": "Admin panel stability + startup patch note fix",
    "published_at": "2026-05-01",
    "content": "Three bug fixes:\n\n• Admin panel no longer shows a blank white screen when a preview-flagged component crashes during render. An error boundary now catches any React render error inside the panel and displays a red error card with the message, stack trace, and a Retry button — so crashes are diagnosable without needing DevTools.\n\n• Admin panel Seasons tab no longer crashes with \"s.map is not a function\". The `/api/seasons` endpoint returns `{ seasons: [] }` (an object), but the component was calling `.map()` directly on the whole response object. The load function now correctly unwraps `raw.seasons || []` before storing it in state.\n\n• Fixed a JavaScript syntax error in `patchNotes.js` that was causing `[Startup] Patch note seed failed: Invalid or unexpected token` on every bot start. The v5.48 coaching-marketplace entry had a stray duplicated closing quote character at the end of its content string. The seed is now clean and patch notes load correctly at startup.",
    "author": "System"
  },
  {
    "version": "5.51",
    "title": "V3 deward scoring rebalanced — obs vs sentry split, 40pt cap",
    "published_at": "2026-05-02",
    "content": "Ward kill scoring in TrueSkill V3 was significantly overtuned: a flat 10 pts per deward meant a support destrpoying 77 wards in one game got +770 raw points, completely drowning out kills, assists, damage and everything else in the performance signal.\n\nTwo changes:\n\n• Observer and sentry kills now score differently. Observer kills (vision denied) are worth 5 pts each; sentry kills (detection denied) are worth 2 pts each. For matches parsed with the extended parser that records `obs_dewarded_count` and `sen_dewarded_count` separately, the split rates are used. Older matches that only have the combined `wards_killed` column fall back to a flat 3 pts per kill.\n\n• Total deward contribution is hard-capped at 40 pts regardless of volume. A typical good support game (5 obs + 10 sentry kills) scores 45 pts raw → capped at 40, comparable to 10 kills. An extreme outlier (77 wards) that previously yielded 770 pts now contributes at most 40 pts — still rewarded, but no longer able to single-handedly push a player to the x1.20 modifier ceiling on ward kills alone.\n\nThe `obs_dewarded_count` and `sen_dewarded_count` columns are now included in all three V3 SQL queries (season recalculation, single-match modifier lookup, and per-player modifier history) so the breakdown tooltip on the scoreboard reflects the corrected math.",
    "author": "System"
  },
  {
    "version": "5.52",
    "title": "V3 deward weights drastically reduced — tiebreaker not position bonus",
    "published_at": "2026-05-02",
    "content": "The 40pt cap introduced in v5.51 did not solve the underlying problem: with obs×5 and sen×2, the pos 4 and pos 5 supports routinely hit the cap every game, turning it into a flat '+40 pts for being a hard support' rather than a reward for exceptional dewarding.\n\nDeward weights are now small enough that they act as a tiebreaker between similarly-performing players, not a position-wide bonus:\n\n• Observer kills: 1.5 pts each (was 5)\n• Sentry kills: 0.5 pts each (was 2)\n• Hard cap removed — unnecessary at these weights\n• Legacy fallback (combined wards_killed): 0.75 pts each (was 3)\n\nA typical support game (5 obs + 10 sentry kills) now contributes ~12 pts from dewards instead of ~40. An extreme outlier (20 obs + 57 sen) contributes ~59 pts, which is still meaningful but must compete with all other stat dimensions to move the z-score needle. Dewards remain in the scoring to reward active vision control — they just no longer override a player's entire game performance.",
    "author": "System"
  },
  {
    "version": "5.53",
    "title": "Dedicated server auto-pipeline",
    "published_at": "2026-05-02",
    "content": "Four interconnected improvements that close the loop between a dedicated server game ending and stats being fully recorded without any admin intervention.\n\n**SSH-first replay fetch:** When a dedicated server SSH key is configured, the bot automatically fetches the .dem replay directly from the server 60 seconds after POST_GAME fires (allowing time for the game server to finalise the file). This replaces the previous 5-minute Steam GC wait for dedicated server games. If the SSH fetch fails for any reason, the bot falls back to the original Steam GC download path with a Discord notification explaining the fallback.\n\n**One-click connect link:** When an admin provisions the server from the inhouse dashboard, the bot immediately posts a `steam://connect/ip:port/password` deep link to the lobby channel so players can connect in one click.\n\n**Auto server reset via RCON:** After match stats are successfully recorded to the database, the bot sends `changelevel dota` via RCON to reset the dedicated server back to the main menu, ready for the next game. This fires for both GC-recorded matches and replay-parsed matches.\n\n**Data quality flags:** Every newly recorded match (via GC data, replay parse, or web upload) is automatically checked for suspicious data: zero player rows, total kills below 5, or any player with both 0 GPM and 0 XPM (a reliable indicator of a partial parse). Flagged matches are marked `flagged_for_review = true` in the database and a Discord alert is posted to the admin channel with a direct review link, prompting an admin to re-parse if needed. Set `ADMIN_CHANNEL_ID` to route these alerts to a private admin channel; they fall back to the announce channel if not set.",
    "author": "System"
  },
  {
    "version": "5.54",
    "title": "Fully automated inhouse queue",
    "published_at": "2026-05-02",
    "content": "Zero-touch inhouse flow from 'I want to play' to 'game is live' — no admin needed.\n\n**Auto-queue system:** Players type `!queue join` in Discord (Steam account must be linked). A live embed in the queue channel updates in real time showing all queued players with their MMR, sorted highest to lowest. `!queue leave` removes a player. The queue persists across bot restarts via a new `inhouse_queue` database table.\n\n**Auto-launch at 10:** When the 10th player joins, the bot immediately runs the MMR balance algorithm across all combinations to find the fairest possible 5v5 split, creates an inhouse session, assigns teams, and provisions the dedicated server (if configured). All 10 players are pinged by Discord mention with a full team embed showing radiant/dire assignments, average MMR per side, one-click `steam://connect` link, password, and the console command string.\n\n**Connection timeout monitor:** After server provisioning, the bot polls RCON `status` every 30 seconds. If any players have not connected within the configurable timeout (default 10 minutes, set via `QUEUE_TIMEOUT_MINUTES`), those players are pinged by @mention in Discord. The monitor stops automatically once all players connect.\n\n**Admin overrides:** `!queue clear` clears the queue (admin). `!queue force` launches with however many players are currently queued — useful for testing or sub-10 games. `QUEUE_CHANNEL_ID` env var routes queue embeds to a dedicated channel.\n\n**_isAdmin fix:** The `_isAdmin()` helper method was referenced by two existing commands but never implemented. It is now properly defined, checking for the owner Discord ID, ManageGuild, or Administrator permission.",
    "author": "System"
  },
  {
    "version": "5.55",
    "title": "Auto season management & highlights",
    "published_at": "2026-05-03",
    "content": "Full automated season lifecycle — zero admin needed at season end.\n\n**End conditions:** Each season can now be configured with an end date, a match count limit, or both. After every match is recorded the bot checks the active season's conditions and closes it automatically when they are met.\n\n**End-of-season Discord embed:** When a season closes (automatically or manually), the bot posts a richly formatted embed: season name, total matches, total players, final top-3 leaderboard with MMR and W/L, longest win streak of the season, most improved player (biggest MMR delta start→end), and hero of the season (highest win rate, minimum 5 picks).\n\n**Auto next-season activation:** After archiving the closed season, the bot immediately activates the next pending season and announces it on Discord. If no next season exists, an admin alert is sent instead.\n\n**Season summary page:** Each season now has a `/seasons/:id/summary` page on the website showing all end-of-season records in a clean layout. The Discord embed links directly to it. The Seasons page also has a Summary button on every season row.\n\n**Admin panel controls:** A new Season Lifecycle section in the admin panel lets superusers configure end conditions (date / match limit) for any season and manually trigger the close+announce flow at any time — useful for early closure or testing.",
    "author": "System"
  },
  {
    "version": "5.56",
    "title": "All features permanently enabled — flag system removed",
    "published_at": "2026-05-03",
    "content": "The feature flag staging system has been completely removed. All features are now always on with no toggles.\n\n**Removed entirely:** Feature Flags admin panel section, Launch Season 10 button, On/Off toggles, and all server-side flag checks. Hero meta, draft assistant, season pass, coaching marketplace, profile customisation, pro tier, web push notifications, tournament brackets, and all other features are now unconditionally available to all visitors — no database lookup required.",
    "author": "System"
  },
  {
    "version": "5.57",
    "title": "Match PERF score rebalanced to fairly reward every role",
    "published_at": "2026-05-03",
    "content": "The per-match Performance score (1–10) on match scoreboards has been rebalanced to eliminate the systematic bias against support and utility players.\n\n**What changed:**\n- Vision control (observer wards placed + enemy wards dewarded) added as a dedicated 10% component — wards and dewards are now directly rewarded\n- Stun/disable duration added as a 5% component — initiators and utility heroes get credit for CC\n- Hero damage reduced from 20% to 15% — carries and mids still rewarded, but supports are no longer structurally penalised\n- Tower damage increased to 10% — objective and push contribution carries more weight\n- Net worth reduced from 15% to 10%\n- Healing reduced to 5%\n- Win bonus increased to 5%\n- Survival reduced to 15%\n\n**Final weights:** Kill involvement/assists×0.5 25%, Survival 15%, Hero damage 15%, Net worth 10%, Vision/dewards 10%, Tower damage 10%, Stun duration 5%, Healing 5%, Win bonus 5%.",
    "author": "System"
  },
  {
    "version": "5.58",
    "title": "PERF Positive Impact Revamp — Position-Aware, 10 Achievable for Every Role",
    "published_at": "2026-05-04",
    "content": "The per-match Performance score has been rebuilt from the ground up as a **position-aware Positive Impact Score** that is now persisted in the database and computed against per-role baselines.\n\n**What changed:**\n- Each player is scored against the average and elite per-minute targets for their assigned position (1–5), not against the rest of the lobby. A support is no longer compared against a carry's farm.\n- All stats are normalised per game-minute, so duration no longer skews the result.\n- Calibration: 5.0 = average for that position, 7.0 = very good, 9.0+ = top ~1%, and 10.0 is genuinely achievable for any role with elite play.\n- Per-position weights properly value vision, dewards, stun duration, and healing for supports — a perfect support game can now score 10.\n- PERF is computed automatically on every new match record and stored in `player_stats.perf` (with full per-stat breakdown in `perf_breakdown`). Match scoreboards prefer the persisted score when present.\n\n**New on Match Detail:** A prominent ⭐ PERF — Positive Impact Score panel now sits at the top of every match page, listing all 10 players sorted by score with colour buckets (Top 1% / Excellent / Very Good / Average / Below Avg / Poor) and a hover tooltip exposing the full stat-by-stat breakdown.\n\n**New on Leaderboard:** Avg PERF column showing each player's average Positive Impact Score across all rated games, colour-coded by tier.\n\n**New on Player Profile:** Avg PERF stat card showing season average, with hover-tooltip best PERF and total rated game count.\n\n**Backfill:** Owner-only Discord command `!perf-backfill [limit]` (mirrored across both editions). With no argument it processes ALL pending matches in batches of 50 with a 250ms inter-batch sleep, emitting per-batch progress logs.\n\n**Source semantics:** Each PERF row records `perf_source = 'endgame_v1'` (current path, capped at 8.5 max) or `'timeline_v1'` (future per-minute path, full 10.0 ceiling). The empty `position_baselines` table has been provisioned for the upcoming timeline pipeline.",
    "author": "System"
  },
  {
    "version": "5.59",
    "title": "OCE Inhouse rebrand — Court & Pitch palette + flag cleanup",
    "published_at": "2026-05-04",
    "content": "The first wave of the **OCE Inhouse** rebrand is live, graduating the approved Hybrid · Court & Pitch mockup design out of the canvas sandbox and onto the production site.\n\n**Brand & palette:**\n- Site title is now **OCE Inhouse**; the OA logo replaces the wrench wordmark in the global nav, paired with a serif `OCE` / condensed amber `INHOUSE` lockup.\n- New favicon ships from `/favicon.png` (OA mark, no white card).\n- Inter, Oswald and Playfair Display are now loaded globally and exposed via `--font`, `--font-condensed` and `--font-serif` tokens.\n- The entire CSS palette has been retuned to the Court & Pitch system: ink-navy `#0d1424` backgrounds, brass `#c5a975` as the primary accent, amber `#f59e0b` for highlights, and parchment `#f5efe2` driving the light-theme override. Existing token names (`--bg-primary`, `--accent`, `--gold`, `--border`, etc.) were preserved so every component picks up the new look without touching individual files.\n- The light-theme toggle now flips the app to a warm parchment skin instead of the old bluish off-white.\n\n**Feature-flag cleanup:** The four legacy preview flags `season_pass_s10`, `notification_prefs`, `tournament_live_v2` and `profile_customization` have been removed. All four were perpetually `off` and gated UI/API surfaces that are now first-class. Every `useFeatureFlag('…')` call site and every server-side `_flagOn('…', req)` 404 gate for those flags has been deleted; admin-only and Pro-tier checks remain the only gating left on those pages. The flag rows are no longer seeded by `initDb()`, so fresh installs never see them.\n\n**Draft Assistant route fix:** `/draft-assistant` now points at the standalone V2 `DraftAssistant.jsx` page (live counter-pick + synergy scoring) instead of the legacy combined Draft view. The Tools dropdown gained a separate **Draft Stats** entry pointing at the old `/draft` page so neither surface is lost.\n\n**Coming next (under v5.60):** admin-editable Welcome Modal CMS and the full Admin Panel left-sidebar restructure are scoped and queued.",
    "author": "System"
  },
  {
    "version": "5.60",
    "title": "Court & Pitch graduation — global primitives, ticker, footer, Welcome Modal CMS",
    "published_at": "2026-05-04",
    "content": "The full **Hybrid · Court & Pitch** design now ships site-wide, graduating the editorial primitives out of the canvas mockup and into the live `web/` app.\n\n**New global primitives** (in `web/src/styles.css`, available everywhere):\n- `oa-card` / `oa-card-rule` — soft cards with the brass left-rule accent.\n- `oa-tag` / `oa-tag-radiant` / `oa-tag-dire` / `oa-tag-brass` — pill tags that replace the old winner dots.\n- `oa-rank-numeral` — outlined italic Playfair numeral for leaderboard ranks (filled brass for top-3).\n- `oa-cta-primary` / `oa-cta-ghost` — amber-to-brass gradient CTA + ghost variant; both honour the parchment light theme.\n- `oa-rule-double`, `hairline-divider`, `oa-eyebrow`, `oa-section-head`, `oa-hero-glow` — editorial layout helpers.\n- `font-serif` / `font-condensed` / `uppercase-wide` utility classes for Playfair Display and Oswald treatments.\n\n**Site chrome:**\n- A new **broadcast ticker** runs across the very top of every page, styled amber-on-navy in dark mode and ink-on-parchment in light, looping season + product callouts.\n- A new **editorial footer** anchors every page with the OA mark, Discord/GitHub links and a live `v5.60 — Patch notes` deep-link.\n- The light/dark toggle now writes both `theme-light`/`theme-dark` and the legacy `light-theme` body classes so future Court & Pitch components can target either name.\n\n**Home page:**\n- The hardcoded purple Season-10 launch banner has been replaced with an editorial Court & Pitch hero card (eyebrow + serif headline with brass italic flair + ghost-style CTAs), keeping the dismiss behaviour intact under a fresh storage key.\n\n**Welcome Modal — admin-editable CMS:**\n- The hardcoded Season-10 modal is gone. The new modal is driven by a single `welcome_modal` row in `site_settings` with shape `{ enabled, version, eyebrow, title, body, ctaText, ctaHref }`.\n- New public endpoint `GET /api/settings/welcome-modal` feeds the frontend; admin writes go through the existing `POST /api/admin/settings` allowlist (now extended with a JSON validator for this key).\n- `web/src/components/WelcomeModal.jsx` was rewritten to fetch from the API, render with the editorial card style, and key its `localStorage` dismiss on `welcome_modal_dismissed_v<version>` so bumping the version re-shows it to everyone.\n- A new **📣 Welcome Modal (CMS)** panel in the Admin Panel exposes every field plus a one-click *Save & re-show to everyone* button that bumps the version automatically.\n\n**Coming next (under v5.61):** the AdminPanel left-sidebar restructure was scoped out of this batch to keep the 3,166-line file safe; it will land as its own focused patch.",
    "author": "System"
  },
  {
    "version": "5.61",
    "title": "Admin Panel left-sidebar restructure",
    "published_at": "2026-05-04",
    "content": "Closes the loop on the v5.60 Court & Pitch graduation by replacing the long single-column scroll of the Admin Panel with a sticky **left-sidebar shell** that mirrors the mockup.\n\n**What changed:**\n- `web/src/pages/AdminPanel.jsx` is now a two-column shell: a 240px sticky sidebar on the left, the existing section content on the right. Sections were not rewritten — every panel, table and form is byte-identical to v5.60.\n- The sidebar groups every admin surface into six tabs across five labelled groups: **Dashboard → Overview**, **Match Data → Matches & Replays**, **Bot Tools → Steam Bot & Test DMs**, **Config → Seasons & Ratings / Site Settings**, **Users → Players & Sign-Ups**, and **Marketplace → Gifts, Coaching & Tournaments**.\n- The **Players & Sign-Ups** item carries the same red badge for pending applications that used to live on the Quick Links button.\n- The active tab is persisted to `localStorage` (`admin_active_tab`) so deep-linked admin work survives a refresh.\n- New CSS primitives in `styles.css` (`.ap-grid`, `.ap-sidebar`, `.ap-nav-group`, `.ap-nav-item`, `.ap-nav-badge`) honour the brass left-rule on the active item and collapse to a wrapping horizontal nav under 900px wide.\n\n**Migration notes:** the old single-scroll layout is gone; superusers should bookmark sidebar items rather than relying on scroll position. Every existing admin action is still reachable — only the chrome moved.",
    "author": "System"
  },
  {
    "version": "5.62",
    "title": "Court & Pitch Home Landing + Editable Ticker + Marketplace Nav",
    "published_at": "2026-05-04",
    "content": "Closes the gap between the canvas Court & Pitch mockup and the live anonymous home page, makes the broadcast ticker editor-controlled, and surfaces previously orphaned destinations in the global nav.\n\n**Anonymous home page now matches the Court & Pitch mockup:**\n- Replaced the legacy 'OCE Dota 2 Inhouse' blurb hero, the four 0-stat cards, the Weekly Recap and the Recent Matches block with the editorial layout from `HybridCourtPitch.tsx`.\n- New **stats strip** (Matches Played / Active Players / Hours of Dota / Most Played Hero) using brass numerals and condensed eyebrow labels.\n- New **Latest Matches** table with Radiant/Dire pill tags, condensed score column, brass-italic MVP cell and View All link to `/matches`.\n- New **Top 5 Players** sidebar fed from the live leaderboard endpoint, with `oa-rank-numeral` filled brass for top-3 and a Full Leaderboard CTA.\n- Editorial Season-10 launch banner and the Weekend Tournament banner are preserved at the top.\n\n**Broadcast ticker is now admin-editable (CMS):**\n- New `broadcast_ticker` row in `site_settings` with shape `{ enabled, items[] }`, seeded by `initDb()`.\n- New public endpoint `GET /api/settings/broadcast-ticker`; admin writes go through the existing `POST /api/admin/settings` allowlist (extended with a JSON validator that requires at least one non-empty item).\n- `BroadcastTicker` in `App.jsx` fetches the setting on mount, falls back to the bundled defaults if the request fails, and hides the bar entirely when `enabled = false`.\n- New **📢 Broadcast Ticker (CMS)** panel under Admin → Site Settings exposes an enabled toggle and a one-per-line textarea; Save pushes the new payload site-wide.\n\n**Navigation reachability sweep:**\n- The Tools dropdown gained four entries that previously had no nav surface: **Coaching Marketplace** (`/coaches`), **Hall of Fame** (`/hall-of-fame`), **Multi-Kills** (`/multikills`) and **Pro Membership** (`/pro`). Every previously flagged-out destination is now reachable from the global nav.\n\n**Footer cleanup:** the GitHub link is gone from the editorial footer; only Discord and the live patch-notes deep link remain. Footer version chip bumped to **v5.62**.",
    "author": "System"
  },
  {
    "version": "5.63",
    "title": "Bug-fix sweep — Inhouse session SQL, light-mode tabs, wider pages, welcome-modal CMS, coach onboarding discoverability",
    "published_at": "2026-05-05",
    "content": "Closes a batch of post-v5.62 user-reported bugs and UX papercuts.\n\n**Inhouse Lobby — SQL crash on Open Session fixed:**\n- `getInhouseSessionPlayers` was joining `player_stats` on a non-existent `steam_account_id` column and pulling `mu`/`sigma` from the wrong table, throwing `column ps.steam_account_id does not exist` whenever an admin tried to open a session.\n- Rewrote the query to join the canonical `ratings` table (`ratings.player_id = isp.account_id`) for TrueSkill `mu`/`sigma`/`discord_id` and the canonical `nicknames` table (`nicknames.account_id = isp.account_id`) for the display name. The `/inhouse` admin Open Session flow now succeeds end-to-end.\n\n**Welcome Modal CMS now re-shows by default on Save:**\n- Saving the Welcome Modal in the Admin Panel previously required clicking a separate **Save & re-show to everyone (bump version)** button to actually push the new content to users who had already dismissed the previous version. The plain **Save** button quietly persisted the change without bumping the dismiss-key version, so editors thought their edits were lost.\n- Reversed the defaults: the primary action is now **Save & re-show to everyone** (always bumps the version), and **Save quietly (no re-show)** is the secondary option for typo fixes.\n\n**Light-mode buttons no longer render dark on dark:**\n- The Position Stats page (`/positions`) hardcoded a `#1e293b` (slate-900) background and `#fff` text on its tab buttons (`Position Rankings` / `Player Profiles`) and its Pos 1–5 buttons, which kept the buttons stuck in dark theme even after the parchment-light skin was applied site-wide.\n- All five Pos buttons, both view-toggle buttons and the Expand/Collapse All buttons now use the shared `.btn` / `.btn-primary` / `.btn-sm` primitives, which honour both themes via the `--bg-card`, `--accent` and `--text-primary` tokens.\n\n**Page width — site no longer floats in a 1200px column:**\n- Bumped the global `.container` cap from 1200px to 1480px (with slightly more horizontal padding) so widescreen monitors stop showing 200–400px of empty navy on either side of every page.\n- Same bump applied inline on `Home.jsx` and `PudgeStats.jsx` which had their own hardcoded 1200px wrappers.\n\n**Coaching Marketplace — onboarding discoverability:**\n- Visitors who weren't already top-5 / Immortal+ saw no path to becoming a coach. Added a permanent **How do I become a coach? →** outline button in the page header for everyone who isn't already eligible, plus a secondary **Want to coach? Apply here →** link in the marketplace blurb. Both deep-link to `/coach/onboarding` so the existing eligibility wall handles the rest.\n\n**Coming next:** moving Player Insights, Ward Heatmap, Head-to-Head Compare and the Position Player Profiles / Hero Position Meta breakdowns behind the Pro tier (with blur-and-upsell preview). Tournament 404 reproduction needs the offending URL — please DM the link if it recurs.",
    "author": "System"
  },
  {
    "version": "5.64",
    "title": "Pro paywall sweep + tournament 404 diagnostics + light-mode polish",
    "published_at": "2026-05-05",
    "content": "Continues the v5.63 bug-fix and brings the long-promised Pro-tier paywall to the advanced analytics surfaces.\n\n**New Pro paywall — blur-overlay style:**\n- New shared component `PaywallBlur` (`web/src/components/PaywallBlur.jsx`) wraps a feature in a soft 6px blur with a centered ★ Upgrade-to-Pro card on top. Cards stay visible in the background so visitors can see exactly what they would unlock.\n- Honours the existing `useProStatus` hook for paid users and falls back to the superuser context so admins/superusers always see the underlying content.\n- Now applied to: **Player Insights** (`/player-insights` — both Network and Benchmarks tabs), **Ward Heatmap** (`/ward-map`), **Head-to-Head** (`/head-to-head`), **Position Stats → Player Profiles** tab, and the **Hero Position Meta** + **Hero Breakdown** tabs on the Heroes page.\n\n**Tournament 'not found' diagnostics:**\n- The 404 page on `/tournaments/:id` now shows the looked-up id and a Back-to-tournaments link instead of a bare error string, so users can DM the broken URL when reporting.\n- Server-side `db.getTournamentById` now logs a warning when the id is non-numeric or no row matches, surfacing the real cause in PM2 logs instead of silently returning null.\n\n**Light-mode polish:**\n- The Heroes → Hero Breakdown tab's Expand All / Collapse All buttons hardcoded a slate-900 background and now use the shared `.btn .btn-sm` primitives, matching the rest of the v5.63 sweep.\n- The Ward Map page title used a hardcoded `#e2e8f0` colour and now reads from `var(--text-primary)` so it stays legible on parchment.\n\n**Patch notes file order:** Repaired the v5.59→v5.63 entries which had been written in descending order, breaking the strictly-ascending validator in `db/seedPatchNotes` and preventing the bot from booting.",
    "author": "System"
  },
  {
    "version": "5.65",
    "title": "Admin Panel search bar",
    "published_at": "2026-05-05",
    "content": "The Admin Panel left sidebar now has a **Search admin…** box at the top. Type any feature name or keyword (e.g. *backup*, *replay*, *welcome modal*, *coaching*, *tournament*, *applications*) and a result list appears with the section name and the parent tab. Clicking a result switches to that tab and smooth-scrolls straight to the relevant heading.\n\nUnder the hood every major admin section (25 of them across Match Data, Bot Tools, Config, Users and Marketplace) now has a stable `id=ap-anchor-…` for deep-linking, which we'll reuse later for shareable admin URLs.",
    "author": "System"
  },
  {
    "version": "5.66",
    "title": "Steam-login security hardening",
    "published_at": "2026-05-05",
    "content": "Closes the gaps surfaced by the Steam-login security review. No leaks were found in the OpenID flow itself; this patch removes the remaining hardening gaps.\n\n**Session signing key — hard-fail on a missing or weak secret in production:**\n- Previously, if `SESSION_SECRET` wasn't set the server fell back to a hardcoded default string, which would let anyone who knew that string forge any user's session cookie and impersonate them.\n- The bot now refuses to start in production (`NODE_ENV=production`) unless `SESSION_SECRET` is set and at least 32 characters long. In development a fresh random secret is generated per boot (sessions don't survive a restart, which is fine locally).\n- Set `SESSION_SECRET` on the prod host (e.g. `openssl rand -hex 48` and paste into PM2's env) before deploying this version.\n\n**Steam OpenID error path:**\n- The catch handler in `/auth/steam/return` now logs the full error server-side instead of just `err.message`, and continues to redirect with the same generic `?auth=error` flag — so upstream Steam OpenID failure details (which can include host/network info) are never echoed into the visitor's URL bar.\n\n**CSRF posture documented:**\n- The session cookie is now named `oi.sid` (instead of the default `connect.sid`) so the cookie no longer fingerprints the framework.\n- Cookie attributes remain `httpOnly + sameSite=lax + secure (in prod)`. Combined with the strict CORS origin allow-list, `sameSite=lax` blocks the standard CSRF vector on every state-changing request without requiring per-request CSRF tokens. Inline comment added next to the session config explaining the threat model so the next maintainer doesn't accidentally weaken it.\n\n**Items intentionally not changed:**\n- The 308 admin/superuser endpoints that echo `err.message` back in their JSON response stay as-is — they all sit behind `requireSuperuser` / `authMiddleware`, so the error audience is already restricted to admins who legitimately need the SQL detail when debugging. A blanket rewrite would risk breaking the admin UI's error toasts for marginal gain.\n- The Stripe webhook handler was already hardened in v3.x to reject any payload without `STRIPE_WEBHOOK_SECRET`; the original audit flagged it as a continuation but the fix is already live.",
    "author": "System"
  },
  {
    "version": "5.67",
    "title": "Pro Membership consolidation + Tournament 404 auto-rescue",
    "published_at": "2026-05-05",
    "content": "**Pro Membership now has its own gold nav button + a single Pro hub:**\n- Added a permanent gold **★ Pro** button to the top-level navbar (next to the Tools dropdown). One click takes you to the Pro hub regardless of membership state.\n- Removed the standalone Pro features from the Tools menu (Ward Heatmap, Player Insights, Head to Head & Compare, the duplicate Pro Membership entry). They now live exclusively on the **/pro** page so the menu stays focused on free, everyone-can-use tools.\n- The /pro hub was rebuilt around four feature groups — *Hero Analytics*, *Player Analytics*, *Match Tools*, *Profile* — listing all 11 Pro features in one place. Each card shows the icon, name, what it does, and (for Pro members) a gold **Open →** deep-link to the feature. Non-members see the same catalogue with locked-state hints, then a single gold **Upgrade to Pro — Unlock Everything Above** CTA at the bottom.\n- Pro features that live as tabs inside another page (Heroes → ★ Position Meta, Heroes → ★ Hero Breakdown, Player Insights → ★ Player Network, Player Insights → ★ Benchmarks) are now gold-tinted in their tab bars so you can spot at-a-glance which tabs are part of your Pro subscription.\n\n**Tournament 404 auto-rescue:**\n- Clicking a weekend tournament from a stale `/tournaments/:id` link previously showed *Tournament not found* because that route hits the bracket-tournaments table only. The detail loader now falls back to the weekend-tournaments table and silently redirects to `/weekend-tournament/:id` when it finds a match, so old shared links keep working.\n- The diagnostic *Tournament not found* card still shows for genuinely-missing ids in both tables, with the same DM-the-URL hint.\n\n**Footer version bump:** v5.62 → v5.67.",
    "author": "System"
  },
  {
    "version": "5.68",
    "title": "Medieval tier ladder — heraldic shield badges replace the old Gaben → Anchor labels",
    "published_at": "2026-05-05",
    "content": "The 8-tier inhouse ladder has been completely re-themed around the Court & Pitch heraldic style. Old labels are gone, new medieval ranks are in, and every leaderboard row now ships with its own hand-crafted shield badge.\n\n**The new ladder (top → bottom):**\n- 👑 **King** — *Ruler of the realm. Bow before greatness.*\n- 🪓 **Warlord** — *Battle-hardened commander. Banners follow you.*\n- ✨ **Paladin** — *Righteous champion. The light is on your side.*\n- ⚔️ **Templar** — *Sworn to the order. Disciplined and feared.*\n- 🛡️ **Knight** — *Chivalrous and dependable. The kingdom counts on you.*\n- 🗡️ **Footman** — *Honest soldier. Holds the line, takes the field.*\n- 🐎 **Squire** — *In training. One day you may be knighted.*\n- 📜 **Apprentice** — *Just beginning the climb. Read the scrolls, hold the line.*\n\n**What changed:**\n- 8 new heraldic shield badge PNGs at `/badges/tier-1-apprentice.png` … `/badges/tier-8-king.png`, generated in the Court & Pitch brass + ink-navy palette and rendered at 22×22 inside the leaderboard tier pill (replacing the old single-emoji glyph).\n- Tier names + flavour descriptions rewritten in `web/src/pages/Leaderboard.jsx` (V1 + V3 thresholds, MMR cutoffs unchanged) and in `src/config.js` (Discord MMR roles + announcement copy). All MMR thresholds are preserved, so nobody changes rank — only the label and the art are different.\n- The three sub-Apprentice meme tiers (Neutral Creep / Observer Ward / Position 6) are kept as-is for ultra-low MMR fallback, but they fall back to the legacy emoji glyph since they are not part of the heraldic ladder.\n- The Dota rank decoder (Herald → Immortal) is **untouched** — those badges still come straight from OpenDota's CDN.\n\n**Footer version bump:** v5.67 → v5.68.",
    "author": "System"
  },
  {
    "version": "5.69",
    "title": "Bug-fix sweep — sub-Apprentice rebrand, tournament click, superuser unlocks, Latest Matches scores",
    "published_at": "2026-05-05",
    "content": "Small but high-friction batch of fixes that came out of the v5.68 visual review.\n\n**Sub-Apprentice tiers re-themed (the red circle on the leaderboard legend):**\n- 🐗 Neutral Creep → 🌾 **Peasant** — *Tilling the fields. Tomorrow may bring the call to arms.*\n- 👁️ Observer Ward → 🥾 **Vagabond** — *Wandering the realm. No banner, no oath, no glory yet.*\n- 🗺️ Position 6 → 🏴 **Outlaw** — *Branded and exiled. The kingdom does not speak your name.*\n\nAll three are now consistent with the heraldic Apprentice → King ladder and no longer expose the old joke labels. MMR thresholds are unchanged — only the label and emoji rotate.\n\n**Tournaments list now clickable:** the `<Link>` wrapping each tournament card in `web/src/pages/Tournaments.jsx` was rendering as an inline anchor inside the grid, so only the text-line area was hittable and most clicks did nothing. Added `display: block` + `cursor: pointer` so the entire card surface routes to `/tournaments/:id` (or `/weekend-tournament/:id`) on click.\n\n**Pro page now unlocked for superusers:** `web/src/pages/Pro.jsx` previously gated every feature card on `status.is_pro`, which meant signed-in superusers without a Stripe purchase couldn't preview the Pro features at all. Superusers now resolve to `isPro = true` automatically (purely client-side; the backend Pro endpoints still enforce the real `is_pro` check).\n\n**Replay Upload now unlocked for superusers:** `web/src/pages/Upload.jsx` is no longer hard-gated on the legacy admin-key login. It now also accepts a logged-in superuser session — `isAdmin = isAdminUser || isSuperuser`, the chunked-upload calls use whichever key is present, and the \"Login as admin\" button opens the superuser modal first if no superuser is signed in.\n\n**Latest Matches widget on the home page now shows the score:** `db.getHomeStats()` was returning the recent matches without any score columns, so the widget always rendered \"—\". Two new subqueries pull `radiant_score` / `dire_score` per match by summing per-team kills from `player_stats`, matching how the rest of the app computes the team score.\n\n**Welcome Modal (CMS) — clarification:** the modal is admin-driven and ships with the seeded default `enabled: false`. To make it appear on the home page, open Admin Panel → 📣 Welcome Modal (CMS), tick **Modal enabled**, fill in the title/body/CTA, and click **Save & re-show to everyone** — that bumps the version and forces every dismissed user to see the new copy on next page load. (No code change needed; the existing flow works.)\n\n**Footer version bump:** v5.68 → v5.69.",
    "author": "System"
  },
  {
    "version": "5.70",
    "title": "Steam sign-in fix, Welcome Modal preview tooling, admin Tier Ladder + Nicknames + Profile Preview",
    "published_at": "2026-05-05",
    "content": "Targeted v5.69 follow-ups based on user reports.\n\n**Steam sign-in now actually returns you to oceinhouse.gg signed in:** the OpenID realm + return URL were previously hardcoded to a `SITE_URL` env that defaulted to a bare `http://<ip>:5000`, so production redirects bounced users back at a host Steam never authed against and the session cookie was dropped. The realm/return now derive from the live request (`req.protocol` + `req.get('host')`, honouring `trust proxy=1`), with `SITE_URL` kept as an explicit override only. Also added `req.session.save(...)` before the 302 — without it the redirect could race the session write and leave the user signed in nowhere. Two diagnostic log lines (`[Steam Auth] /auth/steam redirect …` and `[Steam Auth] success — accountId:`) were added so future regressions are obvious in PM2 logs.\n\n**Welcome Modal — admins can now see at a glance whether it's actually on:** the CMS panel got a green `✓ Currently ENABLED` / red `✗ Currently DISABLED` status badge above the checkbox, so the most common reported failure mode (saving without ticking Enabled) is impossible to miss. A new **🔍 Preview on home (new tab)** button clears every `welcome_modal_dismissed_v*` key from your local storage and opens `/` so admins can verify the modal renders without needing an incognito window or a version bump.\n\n**Admin Panel — three new visibility/preview sections:**\n- **🎖️ Tier Ladder Preview** (Config tab): read-only side-by-side render of the full V1 (legacy) and V3 (Season 10+) ladders — Outlaw → King — using the real `<TierBadge/>` component plus the MMR cutoff for each row. Use this to confirm what each rank looks like and what its symbol/cutoff is without hunting through the leaderboard.\n- **✏️ Manage Nicknames** (Users tab): explicit links to the `/players` page (which already houses the full nickname editor + Discord-ID linker) so the feature is no longer buried — both an in-tab open and a new-tab open. Restores the discoverability that was lost in the v5.59 admin restructure.\n- **👁️ Profile Preview (sample)** (Users tab): paste any `account_id` (or a `/player/<id>` URL — the regex picks out the digits) and jump straight to that player's public profile in a new tab to spot-check bio / custom title / accent / pinned-hero rendering. A shortcut button to the leaderboard is included for picking a player.\n\nAll three new sections are indexed in the admin search (`SEARCH_INDEX`) so `Cmd-K`-style searching for *nickname*, *tier*, *symbol*, *profile preview*, *bio*, *accent*, etc. surfaces them.\n\n**Footer version bump:** v5.69 → v5.70.",
    "author": "System"
  },
  {
    "version": "5.71",
    "title": "Admin Draft Sandbox — test-run the captain pick flow with placeholder players",
    "published_at": "2026-05-05",
    "content": "New admin-only page at **`/admin/draft-sandbox`** (also linked from Admin Panel → Bot Tools → 🎮 Draft Sandbox) that lets you walk through the full inhouse captain-pick draft against 10 placeholder players modeled on real Dota 2 pros (Miracle, Topson, Ceb, JerAx, N0tail, Arteezy, SumaiL, Universe, Cr1t-, Fly).\n\n**What it does:**\n- Uses the standard **1-2-2-2-1** alternating captain pick order (8 picks total) with two captains drawn from the placeholder pool.\n- Renders both team panels with their captain, position-coloured player chips, slot placeholders, and a running MMR-balance readout (green when |Δ| < 1500, amber otherwise).\n- Manual pick mode: while a captain is on the clock, the **Pick →** button on any pool player assigns them.\n- 🤖 **Auto-pick next** uses a greedy heuristic — highest-MMR pool player whose position the picking team is still missing.\n- ⏩ **Simulate to end** auto-resolves the entire remaining draft for visualising the finished state.\n- 🎲 **Reroll captains** picks two new captains at random and resets the board; ↺ **Reset picks** clears the history but keeps the captains.\n- A pick-order strip across the bottom highlights the current pick number and whose turn each upcoming slot is.\n\n**What it does NOT do:** zero backend writes. Nothing here touches the live `inhouse_session_*` tables, the Steam bot, RCON, or the database. It's a pure client-side simulator for verifying the UX, the pick-order logic, and what a partly-drafted team looks like before standing the real flow up.\n\nIndexed in the admin search (`Cmd-K` for *draft / pick / captain / simulator / placeholder*).\n\n**Footer version bump:** v5.70 → v5.71.",
    "author": "System"
  },
  {
    "version": "5.72",
    "title": "Tournament card click fix · Steam SITE_URL warning · interactive Profile Sandbox · Draft Sandbox redesign · community→full nickname sync script",
    "published_at": "2026-05-05",
    "content": "Batch addressing five separate v5.71 follow-ups.\n\n**Tournament cards now actually click:** the v5.69 fix (added `display:block` + `cursor:pointer` to the wrapping `<Link>`) wasn't enough — somewhere in the global CSS chain `a { display: inline; }` was still winning the cascade, leaving most of the card surface unhittable. Replaced the `<Link>` with a plain `<div role=\"link\" tabIndex={0} onClick={navigate(href)} onKeyDown={Enter/Space}>` so navigation is purely programmatic and not subject to the link-styling cascade. Cmd/Ctrl-click and middle-click still open in a new tab. Added a brass border-colour hover state too.\n\n**Steam sign-in — louder warning when `SITE_URL` is misconfigured:** v5.70 made the OpenID realm derive from `req.protocol+host`, but kept `SITE_URL` as an override. If a stale `SITE_URL` (e.g. the bare `http://<ip>:5000` from older deploys) is still set in the prod env, that override silently won and produced exactly the same broken behaviour as before. The override now compares the env-supplied host against the live request host: if they disagree, the server logs `[Steam Auth] SITE_URL=… disagrees with request host …; preferring request host. Unset SITE_URL or align it with the canonical domain.` and falls back to the request host. There is also a new diagnostic log line on `/auth/steam/return` that prints `host`, `proto`, the OpenID verify result, and the inbound cookie length — so a future failure is one `pm2 logs oi-bot | grep '[Steam Auth]'` away from a root cause. **If sign-in is still broken after this deploy, check the PM2 log for that warning, then `unset SITE_URL` (or correct it to `https://oceinhouse.gg`) in your prod env file and restart `oi-bot`.**\n\n**Profile Sandbox replaces the Profile Preview picker:** the previous Users tab section just linked to a real player profile. The new section now launches a dedicated **`/admin/profile-sandbox`** page that is a fully-interactive client-side mirror of `/settings/profile`. Every customization control is live (bio, custom title, theme accent, profile frame including premium frames, pinned hero with caption, pinned match) and a live preview card on the left renders the resulting profile with mock recent matches. A `Treat sandbox account as Pro` toggle flips between the locked and unlocked premium states so you can verify both. Nothing is persisted — purely for previewing cosmetic changes before pushing them to real users.\n\n**Draft Sandbox visual redesign — full Court & Pitch palette:** the v5.71 placeholder UI was generic dark-card. Rebuilt entirely with the brand system: ink-navy gradient background, brass rule-lines and section markers (`━ TEAM 1 · RADIANT ━`), Oswald-condensed for stat-block numbers (MMR, pick numbers, totals), Playfair Display for the page title, parchment for body text, amber for the on-the-clock highlight. Player cards use a position-coloured left rail and brass-border pick buttons; the team panels gained a heraldic header band, a captain row, and a total-MMR footer; the pick-order strip now shows past picks faded, the current pick filled with amber, and future picks bordered by their team colour. Functionality is unchanged — same 1-2-2-2-1 alternating pick order, same 10 placeholder pros, same auto-pick / simulate / reroll / reset controls, still zero backend writes.\n\n**Community → full nickname sync script:** new `scripts/sync-community-nicknames.js` migrates `nicknames.{nickname,discord_id,dota_rank_*}` rows from the community-edition Postgres into the full-edition Postgres. Conservative by default (only fills NULL/empty target columns; pass `--overwrite` to replace existing values; pass `--dry-run` to preview without writing). Also upserts a matching row into `players(discord_id, discord_name, steam_id_64, account_id_32)` for every imported account that has a Discord ID, so the bot's Discord-side lookups (!register, role mapping) work too. Run on the prod host with `COMMUNITY_DATABASE_URL=… FULL_DATABASE_URL=… node scripts/sync-community-nicknames.js`. Schema-resilient — checks `information_schema` at runtime for the optional `discord_id` and `dota_rank_*` columns before mirroring them.\n\n**Footer version bump:** v5.71 → v5.72.",
    "author": "System"
  },
  {
    "version": "5.73",
    "title": "Draft Sandbox — player hover tooltip with KDA, win rate, preferred positions",
    "published_at": "2026-05-05",
    "content": "Hovering any player card in the **`/admin/draft-sandbox`** captain-pick simulator now pops a Court & Pitch tooltip showing the player's **KDA ratio**, **win rate** (colour-coded green/amber/red), **games played**, **MMR**, **preferred positions** as coloured pos-pips, and a **top-3 hero** list.\n\nThe sandbox player pool is fully fabricated demo data (10 placeholder players modeled on Dota pros — Miracle, Topson, Ceb, JerAx, N0tail, Arteezy, SumaiL, Universe, Cr1t-, Fly), so the per-player flair labels (\"Carry GOAT\", \"Mid lord\", \"Off-lane bruiser\", etc.) and the new stat numbers are also demo values — nothing is pulled from the live database. The tooltip layout is what matters; the values exist purely so admins can preview the populated state. Hovered card also gets an amber border highlight so it's obvious which row the tooltip belongs to.\n\nFooter version bump: v5.72 → v5.73.",
    "author": "System"
  },
  {
    "version": "5.74",
    "title": "Sub-Apprentice rebrand — Outlaw > Vagabond > Peasant + new heraldic emblems · Profile Sandbox v2 (flair, hero stats, achievements, socials, streak)",
    "published_at": "2026-05-05",
    "content": "**Sub-Apprentice tiers reordered + new emblems.** The three meme tiers below Apprentice were narratively backwards — a Peasant tilling fields was ranked higher than an Outlaw branded and exiled. Flipped to a sensible progression that builds toward the Apprentice → King ladder:\n\n- 🏴 **Outlaw** — *Branded and exiled — feared by the kingdom. One step from being knighted… or hanged.* (highest of the three)\n- 🥾 **Vagabond** — *Wandering the realm with staff and bindle. No banner yet, but the road teaches you.*\n- 🌾 **Peasant** — *Tilling the fields. Pick up a sword — every Knight begins here.* (lowest)\n\nThree new heraldic shield badges (`/badges/tier-sub-1-outlaw.png`, `tier-sub-2-vagabond.png`, `tier-sub-3-peasant.png`) generated in the Court & Pitch palette to match the existing 8 ranks (Apprentice → King). Outlaw is a black cracked shield with a brass bar sinister, Vagabond a weathered tan shield with crossed staff and bindle, Peasant a wooden plank shield with crossed scythe and wheat sheaf. Wired into the leaderboard `<TierBadge />` so all three sub-Apprentice rows now ship with proper heraldic art instead of the emoji fallback. Updated in three places: `web/src/pages/Leaderboard.jsx` (V1 + V3 ladders), `src/config.js` (Discord MMR roles + announcement copy), `web/src/pages/AdminPanel.jsx` (Tier Ladder Preview sample). MMR thresholds for Apprentice and above are unchanged — only the three sub-tier labels swapped position.\n\n**Profile Sandbox v2 — every requested customization preview added.** The interactive sandbox at `/admin/profile-sandbox` now mocks the full v5.75 cosmetic feature set so you can review the visual direction before the backend ships:\n\n- **Pinned hero now shows real stats:** WR%, KDA, games — colour-coded green/amber by win rate. (Free in live.)\n- **Coloured border around the pinned-hero icon:** 7-colour curated palette (Brass / Amber / Radiant green / Dire red / Royal Purple / Steel Blue / Parchment) + None. **Pro feature.**\n- **Pinned match shows actual result:** ✓ WIN/✗ LOSS chip, radiant–dire score, hero played, KDA, match duration, date — not just the match number any more. (Free.)\n- **Flair line preview:** auto-derived flair shown by default (recomputed every time stats change). Pro players who *unlock* the override (e.g. via 100 wins / season-pass top-10 — exact criteria TBD) can pick freely from a 26-strong curated list (\"GOAT\", \"Mid Lord\", \"Carry GOAT\", \"Throw God\", \"Disco Pony\", etc.) and the system never auto-changes it. **Override is a Pro feature.**\n- **Pinned achievement card:** pick one earned badge (First Blood King, Comeback King, Rampage Master, Vision Lord, Season Champion, Glass Cannon) to feature. **Pro feature.**\n- **Animated frame:** subtle shimmer pulse on the profile card border. **Pro feature.**\n- **Heraldic diagonal background pattern:** subtle Court & Pitch tile overlay on the profile card. **Pro feature.**\n- **Top-3 most-played heroes auto-strip:** server-derived, no setting required. (Free.)\n- **Win/loss streak chip:** auto-renders 🔥 N-game win streak (or ❄️ N-game loss streak) when the absolute streak ≥3. (Free.)\n- **Linked socials:** Twitch / YouTube / Steam URL fields rendered as branded chips on the public profile. (Free, validated server-side in production.)\n- **Pro preview toggle** in the editor flips between free and Pro modes so all gating is testable in one page.\n\n**Where these will be visible to other players:** the public profile lives at `/player/:account_id` (also `/player/:nickname` and `/player/:steam_id`). The customization block already renders at the top of that page for any visitor — every cosmetic added in this batch will automatically appear there once the v5.75 backend lands. Clicking a player name on `/leaderboard` is the same destination — that opens their public profile.\n\n**Backend rollout (v5.75) will add:** `player_profiles` columns for `pinned_hero_border_color`, `pinned_achievement_id`, `frame_animated`, `bg_pattern`, `social_twitch`, `social_youtube`, `social_steam`, `flair_override`, `flair_override_unlocked_at`. Plus `src/flair/flairService.js` to compute auto flairs on every stats refresh. Plus the unlock-tracking logic that flips `flair_override_unlocked_at` once the player hits the criteria.\n\nFooter version bump: v5.73 → v5.74.",
    "author": "System"
  },
  {
    "version": "5.74.1",
    "title": "Profile Demo Page + Tournament Cross-Table Redirect",
    "published_at": "2026-05-05",
    "content": "**Profile Demo page added.** New superuser-only preview at `/admin/profile-demo` renders a fully-decorated dummy player profile (\"StElmosFire\") with every v5.74 customization applied — hero border colour, pinned achievement, animated frame, heraldic background pattern, custom title, flair override, win-streak chip, linked socials, top-3 heroes strip, mock recent matches table. Lets you see exactly what a fully-tricked-out public profile will look like to a visitor clicking from `/leaderboard` once the v5.75 backend ships, without needing to seed test data. Linked from the Profile Sandbox header (\"View as public profile\"). Edit the `FAKE` object at the top of `web/src/pages/ProfileDemo.jsx` to preview different configurations.\n\n**Tournament not-found bug fixed.** Stale or shared `/tournaments/:id` links for events that actually live in the `weekend_tournaments` table no longer return a dead-end 404 page. The backend `/api/tournaments/:id` endpoint now peeks into both tables and, when the id belongs to a weekend tournament, returns a `redirect` hint that the frontend honours in a single round-trip — navigating straight to `/weekend-tournament/:id`. The legacy client-side fallback is kept as a safety net for any older clients still in flight.",
    "author": "System"
  },
  {
    "version": "5.74.2",
    "title": "Security: Session-Based Admin Auth & Superuser Privilege Boundary",
    "published_at": "2026-05-06",
    "content": "**Admin credentials no longer stored in browser sessionStorage.** Both the admin (upload key) and superuser login flows now establish a signed server-side session flag rather than storing the raw password in the browser. After login the browser holds only an HTTP-only session cookie — the actual secret never leaves the server. This eliminates the risk of any browser-side script, extension, or XSS payload exfiltrating a reusable backend credential.\n\n**UPLOAD_KEY no longer accepted as a superuser credential.** The `requireSuperuser` middleware and the superuser login endpoint now exclusively validate `SUPERUSER_PASSWORD`. The previous behaviour where knowing only the lower-privilege upload/admin key was sufficient to pass superuser checks has been corrected. All routes protected by `requireSuperuser` (replay path management, dedicated-server controls, inhouse orchestration, etc.) now require the distinct superuser credential.\n\n**Session-status endpoint added.** `GET /api/admin/session-status` and matching logout routes (`POST /api/admin/admin-logout`, `POST /api/admin/superuser-logout`) allow the frontend to restore privilege state on page reload without reading any secret from storage. Non-browser clients (bots, scripts) continue to authenticate via the existing header mechanism as a fallback.",
    "author": "System"
  },
  {
    "version": "5.75",
    "title": "Full inhouse lobby flow — auto-start, hybrid-rank captains, captain-led snake draft, voice moves, auto re-queue",
    "published_at": "2026-05-05",
    "content": "The `/inhouse` page is now a complete end-to-end FACEIT-style session that runs itself with no admin babysitting.\n\n**Auto-start when the lobby fills.** New per-session settings `min_players` (default 10) and `lobby_fill_seconds` (default 30) live on `inhouse_sessions`. A background ticker (`src/inhouse/autoStartTicker.js`) polls every 5s: as soon as the registered count reaches the minimum, an `auto_start_at` timestamp is set and a brass countdown chip appears on the page (\"✓ Lobby is full (10/10) — accept phase auto-starts soon\"). When the timer hits zero the session flips itself into `accepting`, and when the accept-phase timer expires with ≥ min accepts it flips itself to `drafting` and auto-picks captains using the session's mode. If accepts fall short the session drops back to `open` and previously accepted/declined statuses are reset so the lobby can refill cleanly. If the count drops back below the minimum during the grace window the auto-start timer is cleared.\n\n**Hybrid-rank captain selection.** The `highest_rank` mode used to be a flat sort by in-house TrueSkill MMR, which was almost meaningless for newcomers with a near-default rating. It now scores each accepted player on whichever signal is strongest: in-house TS MMR for anyone with **≥ 20 recorded games**, otherwise their **Dota 2 leaderboard rank** if present (rank 1 ≈ 12000 MMR, rank 1000 ≈ 9500), otherwise their **rank tier** mapped onto a coarse MMR scale (Herald 1 = 200, Divine 5 = 5000, Immortal = 9500), with a small penalty fallback for players with no Dota signal at all. Top 2 scores become the captains. The rank tier and leaderboard rank are now also returned by `getInhouseSessionPlayers` for any future UI surfacing.\n\n**Captain-led snake draft.** Captains can now pick directly from the page — no admin required. The draft order for the 8 non-captain slots is a fixed snake `[T1, T2, T2, T1, T1, T2, T2, T1]` (4 picks each, balanced tempo). A new `GET /api/inhouse/:id/draft-status` endpoint exposes whose turn it is, and the page polls it every 2s. The current-turn captain sees an amber **⚡ Your pick!** badge and live click-to-pick buttons on every undrafted player; the other captain sees **Waiting for {name} (Team N)…**. The `POST /draft-pick` endpoint enforces this server-side: only the captain whose turn it is can pick, and only onto their own team. Admins retain full override via the superuser key.\n\n**Auto voice-channel moves on inhouse start.** When the **Provision Server** button fires, the inhouse session is now passed through the existing `_movePlayersToVoiceChannels` helper alongside the Discord notification: drafted players are auto-moved into the configured Radiant/Dire voice channels based on `team1_is_radiant`, mirroring the behaviour that already worked for friend-detected lobbies. No more \"please move to your team channel\" pings.\n\n**Auto re-queue of leftover players.** When an inhouse session is marked complete, any registered player who wasn't actually picked into a team (`team = 0`) or who declined the accept phase is now automatically rolled into the next open session — if no open session exists one is auto-created with the same captain mode, accept timer, min players and lobby fill grace period. Skipped players never lose their spot in the queue.\n\n**Frontend surfacing.** The admin **Open Session** form gained `Min players to auto-start` and `Lobby fill grace period (sec)` fields. The Draft Sandbox header now has a **Try the live flow →** button linking to `/inhouse`. Footer version bump v5.74 → v5.75.",
    "author": "System"
  },
  {
    "version": "5.76",
    "title": "Home banner CMS, lobby voice channel, tournament not-found UX, fresh voice channel defaults",
    "published_at": "2026-05-05",
    "content": "Small but meaningful quality-of-life batch addressing three asks in one go.\n\n**Dismissable Home Banner is back, and it's CMS-driven.** The hard-coded Season 10 launch banner on the home page has been replaced by `web/src/components/HomeBanner.jsx`, a fully editor-controlled banner backed by a new `home_banner` row in the `site_settings` table (shape `{ enabled, version, eyebrow, title, body, ctaText, ctaHref }`). New endpoints `GET /api/settings/home-banner` (public) and the existing `POST /api/admin/settings` (allowlisted on the `home_banner` key) drive a new **🪧 Home Banner (CMS)** panel under Admin → Config — same shape as the Welcome Modal panel, with Save-and-re-show, Save-quietly, and Preview-on-home buttons. Dismissals are versioned in `localStorage` (`home_banner_dismissed_v<version>`) so bumping the version automatically re-shows it to everyone who previously closed it.\n\n**Tournament \"not found\" page now actually helpful.** The bracket-detail fallback already tries the weekend tournaments table on a 404, but when *both* tables miss the page used to dead-end with a generic message. It now spells out exactly what was tried (both bracket and weekend tables), suggests a hard refresh in case the listing was cached, and offers two manual escape buttons — **Try as weekend tournament →** and **← Back to all tournaments** — so a tournament whose id was routed to the wrong table is recoverable in one click instead of a Discord ping.\n\n**Players auto-move back to the lobby voice channel after a match.** A new `_movePlayersToLobbyChannel(accountIds)` helper on the Discord bot mirrors the existing `_movePlayersToVoiceChannels` logic, but pulls everyone into the configured `lobbyVoiceChannelId` instead of splitting them onto Radiant/Dire. It's wired into `POST /api/inhouse/:id/complete` and runs alongside the leftover-player re-queue, so as soon as a session is marked complete the whole roster ends up in one voice channel ready for the next draft. Skips anyone already sitting in the lobby channel and gracefully no-ops if the bot isn't connected.\n\n**Discord voice channel defaults updated to the live oceinhouse.gg server.** `src/config.js` now defaults `radiantVoiceChannelId`, `direVoiceChannelId`, and the new `lobbyVoiceChannelId` to the live OCE Inhouse Discord channel IDs (env vars `RADIANT_VOICE_CHANNEL_ID` / `DIRE_VOICE_CHANNEL_ID` / `LOBBY_VOICE_CHANNEL_ID` continue to override for other servers). The community edition deploy is unaffected — it sets its own env vars.\n\nFooter version bump v5.75 → v5.76.",
    "author": "System"
  },
  {
    "version": "5.77",
    "title": "Tournament listing/detail consistency, home page pulse widgets, Steam sign-in regression fix",
    "published_at": "2026-05-05",
    "content": "Targeted v5.76 follow-ups based on user reports plus three new home page widgets.\n\n**Tournament \"not found\" — fundamental fix.** The listing endpoints `/api/tournaments` and `/api/weekend-tournaments` now set `Cache-Control: no-store, must-revalidate`, so a stale browser/CDN cache can no longer surface tournaments whose detail rows were already deleted (this was the root cause of cards in the listing dead-ending on \"Looked up id N in both tables — neither matched\"). The cross-table fallback that already existed on `/api/tournaments/:id` (bracket → weekend redirect) now also runs in reverse on `/api/weekend-tournaments/:id` (weekend → bracket), so a shared/bookmarked link to either URL recovers transparently regardless of which table the tournament actually lives in. The `WeekendTournament` page on the frontend now follows the redirect hint via `window.location.replace` instead of dead-ending on the error toast. A one-line `console.warn` is logged in PM2 whenever a weekend lookup misses, mirroring the bracket diagnostics from v5.63.\n\n**Three new home page pulse widgets (`web/src/components/HomeWidgets.jsx`):**\n- **🟢 Live Inhouse Pulse** — slim status bar at the top of the home page (both signed-in and signed-out variants) that polls `GET /api/inhouse/active` every 8s and shows the live state of the current inhouse session. Renders five distinct states with colour-coded pulse-dots: no active lobby (muted), filling (amber), full and accept-armed with a live countdown to auto-start (green pulse), accept phase live (amber pulse), captains drafting (purple pulse) and match in progress (red pulse). Click anywhere on the bar to jump to `/inhouse`.\n- **⭐ Player of the Week** — full-bleed brass-amber spotlight card backed by a new `GET /api/home/perf-spotlight` endpoint that returns the single highest persisted PERF score across all matches in the past 7 days. Shows the score (PERF as a giant serif numeral), the player's display name (nickname → persona_name fallback), hero, position, K/D/A and a rarity label (`Top 1%` / `Elite` / `Excellent` / `Strong`). Click to open the source match.\n- **🔥 What's Hot — past 7 days** — 4-up grid of the most-picked heroes in the trailing week with their inhouse win rate, pick count and win-rate delta vs the previous 7 days (rendered green ↑ / red ↓ in pp). Backed by a new `GET /api/home/hot-heroes` endpoint with a single CTE-driven SQL query. Includes a `Hero meta →` shortcut. Both new endpoints carry `Cache-Control: public, max-age=120` so they're cheap under the home page's traffic.\n\n**Steam sign-in lookup regression fixed.** A leftover `ORDER BY ps.date DESC` from before the player_stats schema lost its `date` column was causing the Steam auth callback, `gift_pro` DM and `gift_season_pass` DM lookups in `src/web/server.js` to throw `column ps.date does not exist`, breaking sign-in entirely. All three sites now correctly order by `ps.created_at DESC`. Pushed as commit `d36b686` ahead of this batch so users could log in again immediately.\n\nFooter version bump v5.76 → v5.77.",
    "author": "System"
  },
  {
    "version": "5.78",
    "title": "Steam sign-in trust-proxy fix, broadcast ticker live refresh, tournament cache hardening",
    "published_at": "2026-05-05",
    "content": "Three reported regressions root-caused and fixed.\n\n**Steam sign-in — fundamental fix.** `app.set('trust proxy', 1)` was previously gated on `NODE_ENV === 'production'`. Whenever PM2 booted the bot without `NODE_ENV` explicitly set, trust-proxy was OFF, so `req.protocol` and `req.get('host')` reflected the bot↔nginx hop (plain `http://localhost`) instead of what the user's browser actually connected to (`https://oceinhouse.gg`). `steamBaseUrl()` then handed Steam OpenID an `http://` realm and `return_to` while the user's browser was on `https://`, Steam refused to verify, and the visitor landed back signed-out with no obvious error. Trust-proxy is now enabled unconditionally — it's safe in dev because the X-Forwarded-* headers are simply not present in that case and `req.protocol` falls back to the connection scheme. This is the actual fix for \"Steam login still not working\" reports that survived v5.77's `ps.date → ps.created_at` regression patch.\n\n**Broadcast ticker now refreshes after saving.** `<BroadcastTicker/>` is mounted once at the top of `App.jsx` and previously only fetched `/api/settings/broadcast-ticker` on mount, so admin saves never propagated to the live bar (including in the admin's own browser tab) until a hard refresh. The component now (a) listens for a `broadcast-ticker-updated` window event dispatched by `BroadcastTickerPanel` immediately after a successful save — same-tab updates are instant — and (b) refetches with `cache: 'no-store'` whenever the tab regains focus, so saves made in another tab also pick up without a manual reload.\n\n**Tournament listing cache hardening (frontend complement to v5.77).** v5.77's server-side `Cache-Control: no-store` headers on `/api/tournaments` and `/api/weekend-tournaments` are honoured by browsers but service workers / intermediate caches that pre-cached the listing before the rollout would otherwise keep serving deleted rows. The shared `fetchJson` helper in `web/src/api.js` now passes `cache: 'no-store'` for any URL matching `/(tournaments|weekend-tournaments)\\b/`, forcing every tournament read to revalidate. Combined with v5.77's reverse cross-table fallback, this fully closes the \"listing → click → Tournament not found\" path.\n\nFooter version bump v5.77 → v5.78.",
    "author": "System"
  },
  {
    "version": "5.79",
    "title": "Tournament detail 500 — actual root cause + obvious Steam signed-in indicator",
    "published_at": "2026-05-05",
    "content": "**Tournament detail page — true root cause finally found.** v5.74.1 / v5.77 / v5.78 all chased the wrong layer. Live testing against `https://oceinhouse.gg/api/tournaments/1` revealed the endpoint was returning **HTTP 500**, not 404 — the frontend's \"Tournament not found\" page was just the generic catch-all for *any* failed fetch, which masked the real server error.\n\nThe bug: `getTournamentMatches()` in `src/db/index.js` (and `community-edition/src/db/index.js`) joined the `players` table on `pl1.account_id = tm.p1_id`, but the `players` table has **no `account_id` column** (it stores the 32-bit Dota id under `account_id_32` as a varchar) and **no `persona_name` column** either. Postgres threw `column pl1.account_id does not exist` on every single tournament-detail request, the route's catch handler logged it and returned 500, and the React detail page rendered \"Tournament not found.\" This means literally every tournament click has been failing for as long as those joins have existed — caches, redirects, and trust-proxy fixes were irrelevant.\n\nFix: drop the broken `players` joins and resolve persona names via a correlated subquery against `player_stats` (newest row per `account_id`), with `nicknames` still preferred when set — same approach already used by `getTournamentParticipants`. Reproduced the failure locally against the dev DB, confirmed the new query runs cleanly, and applied the identical fix to the community edition.\n\n**Steam signed-in indicator made unmistakable.** Multiple reports of \"how do I know if I'm signed in to Steam?\" — the previous signed-in button was the same size and almost the same colour as the Sign-in button, so people couldn't tell the difference. The signed-in pill is now a filled lime-green gradient with a leading ✓ checkmark, your Steam persona name in bold, and a `(Sign out)` hint baked into the label. Hover title still reads `Signed in as <name> — click to sign out` for accessibility.\n\n**Note on v5.78 trust-proxy fix:** that change is still correct and stays in. It addresses a separate failure mode where the OpenID realm/return_to were http while the browser was on https. If Steam sign-in *still* fails after v5.79 deploys, the next thing to check is the post-deploy console for `[Steam Auth]` lines — they now log `host=`, `proto=`, and the OpenID `verify=` outcome on every return-trip.\n\nFooter version bump v5.78 → v5.79.",
    "author": "System"
  },
  {
    "version": "5.80",
    "title": "Steam OpenID verify body — actual final fix (the real reason sign-in kept failing)",
    "published_at": "2026-05-05",
    "content": "**Walked the user through the sign-in flow step-by-step and they reported landing on `?auth=invalid`** — meaning Steam's `check_authentication` POST was returning `is_valid:false`, even though their Steam credentials were correct. Trust-proxy (v5.78) was already fine; the bug was one layer deeper.\n\n**The bug.** `/auth/steam/return` was rebuilding the verify body via `new URLSearchParams(req.query).toString()`. Steam OpenID 2.0 requires the params we POST back to be **byte-for-byte identical** to what Steam signed. Round-tripping `req.url` → Express's `qs` parser → JS object → `URLSearchParams` → `.toString()` re-encodes characters differently (`:` ↔ `%3A`, `+` ↔ `%20`, `%7E` ↔ `~`, etc.), which silently invalidates the signature. Steam returns `is_valid:false`, the bot logs `verify=INVALID`, the user lands at `?auth=invalid` with no useful error.\n\n**The fix.** Pull the raw query string straight off `req.url`, swap only `openid.mode=id_res` → `openid.mode=check_authentication` with a single regex, POST that as the body. This preserves the original byte sequence Steam signed. Same approach as the canonical `passport-steam` library. Applied identically to the community edition.\n\n**Diagnostics improved.** On a verify failure the bot now logs the upstream Steam response body (truncated to 300 chars) on a `[Steam Auth] check_authentication failed:` line, so the next time something breaks here the PM2 log tells you exactly why instead of just \"INVALID\". The success-path log line now also includes `body-len=` so you can sanity-check that the verify body wasn't truncated.\n\n**To deploy:** `cd ~/Dota-Stats-Full && bash deploy.sh`. After PM2 restarts, attempt a Steam sign-in and check `pm2 logs oi-bot --lines 30` for a `[Steam Auth] /auth/steam/return — host=oceinhouse.gg proto=https verify=OK` line. If you still see `verify=INVALID`, the new follow-up `check_authentication failed:` line will print Steam's actual reason.\n\nFooter version bump v5.79 → v5.80.",
    "author": "System"
  },
  {
    "version": "5.81",
    "title": "Public profile graduates the v5.74 mockup — eight customization knobs are now real",
    "published_at": "2026-05-06",
    "content": "**Closing a long-standing gap between `/admin/profile-sandbox` and the public `/player/:id` page.** Until now, the sandbox previewed a much richer profile than what visitors actually saw on real player pages. Eight features lived in the mockup only — they couldn't be saved, validated, or rendered for real visitors. This release graduates all eight from sandbox to live profile.\n\n**What's new on the public profile.** When a player saves any of these in `/settings/profile`, they now appear on the page everyone sees when clicking that player from the leaderboard:\n\n- **Pinned achievement** — pick any earned (non-secret) achievement to display next to your pinned hero / pinned match.\n- **Custom flair override** (Pro) — replace the auto-derived flair (e.g. \"Hard Carry\", \"Mid Threat\") with one of 8 free or 8 Pro-tier flairs (\"GOAT\", \"Mid Lord\", \"Hook Wizard\", etc).\n- **Pinned-hero border colour** — 7 preset accent colours (brass, amber, radiant, dire, royal purple, steel blue, parchment) painted around your pinned hero portrait.\n- **Most-played heroes strip** (toggle) — auto-rendered top-5 heroes with games and win rate, pulled from your existing hero stats.\n- **Win/loss streak badge** (toggle) — 🔥 W3+ or ❄️ L3+ chip auto-shown when you're on a meaningful run.\n- **Animated frame shimmer** (Pro) — a subtle pulsing glow on your profile frame.\n- **Heraldic background pattern** (Pro) — a tasteful 45° hatched accent fill behind the customization block.\n- **Social chips** — Twitch / YouTube / Steam profile links rendered as branded pills. Server-side allowlist enforces https + official domains so you can't sneak in arbitrary URLs.\n\n**Schema.** All 11 fields live in a single new `extras` JSONB column on `player_profiles` — no per-knob migrations needed when we add more. Validated server-side via the new `validateExtras()` in `src/profileCosmetics.js`; Pro-only knobs (animated frame, heraldic bg, custom flair, premium flair values) are gated in the `POST /api/me/profile` handler. Existing rows with no extras render exactly as before — defaults are: top-heroes ON, streak ON, everything else OFF / null.\n\n**Settings UI.** A new \"Profile extras\" section at the bottom of `/settings/profile` exposes every knob: a 4-toggle grid for the booleans, a swatch picker for the hero border, an `<select>` populated from your real earned achievements (calls `/api/players/:id/achievements`), a Pro-gated flair override with optgroup-separated free vs premium choices, and three URL fields for the social chips.\n\n**Public render.** All new fields slot into the existing customization block on `PlayerProfile.jsx` between the page header and the AI Scouting Report — no layout reshuffle of pages without customizations.\n\n**Mirrored to the community edition** identically (same column, same validator, same UI). Footer bump v5.80 → v5.81.",
    "author": "System"
  },
  {
    "version": "5.82",
    "title": "Heraldic rank names on every player badge, King reserved for #1, MMR ladder spread, Steam-auth diagnostics",
    "published_at": "2026-05-06",
    "content": "Four reported issues in one batch.\n\n**1. Player badges now show the heraldic NAME, not \"Tier VII\".** The 8-tier MMR badge that sits next to a player's name on `/player/:id` and on the home dashboard previously rendered the placeholder labels \"Tier I\" through \"Tier VIII\". They now use the same heraldic catalog as the leaderboard ladder — Apprentice → Squire → Footman → Knight → Templar → Paladin → Warlord → King — so a single player's badge reads, e.g., **\"🪓 Warlord 7154\"** instead of **\"☀️ Tier VII 7154\"**. One source of truth across `MmrBadge` and the leaderboard's `TierBadge`.\n\n**2. King is reserved for the #1 leaderboard player.** Until now King was just \"MMR ≥ 6500\" under the V3 ladder, which meant the entire active inhouse pool landed in the same top tier. King is now a `leaderOnly` rank: every other badge maxes out at **Warlord** regardless of how high the MMR climbs, and the badge promotes to King only when the player is the current #1 on the all-time leaderboard. `PlayerProfile.jsx` and the home dashboard `HomeBanner` each fetch `/api/leaderboard?limit=1` and pass `isLeader` down to `MmrBadge`; the leaderboard page passes `isLeader={i === 0}` to `TierBadge` for the top row. `getTier()` and `decodeMmrBadge()` both skip `leaderOnly` tiers when no leader flag is set.\n\n**3. MMR thresholds spread out so not everyone is the same tier.** With a 5000-MMR start point and a typical active player sitting at 6000–7500, the V3 ladder squeezed eight bands into a 1500-MMR window and dumped almost everyone into King/Warlord. Recalibrated:\n\n- King — leaderboard #1 only (no MMR threshold)\n- Warlord — 7000+\n- Paladin — 6500+\n- Templar — 6200+\n- Knight — 5900+\n- Footman — 5600+\n- Squire — 5300+\n- Apprentice — 5000+ (default starting band)\n- Outlaw — 4500+\n- Vagabond — 4000+\n- Peasant — < 4000\n\nSame heraldic names apply to the `MmrBadge` next to player names so both ladders stay in sync.\n\n**4. Steam sign-in `?auth=error` — granular diagnostics.** v5.80 fixed `?auth=invalid` (the verify body was being re-encoded by Express's qs parser, breaking Steam OpenID's signature check). The remaining `?auth=error` reports come from the **catch block** that fires after verify succeeds — meaning DB lookup, BigInt parse, or session save is throwing. The catch block was previously a black hole: it logged `'[Steam Auth] Error:'` and the full err object, which on some PM2 / Node combos prints as `[object Object]` with no useful context. The handler now logs the error name, message, code, and a 6-line stack on three separate lines so the very next PM2 log entry will tell us exactly which post-verify step failed in production. Once `cd ~/Dota-Stats-Full && bash deploy.sh` rolls this out, attempt sign-in then check `pm2 logs oi-bot --lines 80` — the failing step will be obvious.\n\nFooter bump v5.81 → v5.82.",
    "author": "System"
  },
  {
    "version": "5.83",
    "title": "Steam sign-in fixed (`ps.created_at` crash), tier-ladder dedup, V1 ladder removed, real heraldic logos on the rank legend, Draft tools merged",
    "published_at": "2026-05-06",
    "content": "Five reported issues in one batch — most importantly the production Steam sign-in is finally fixed.\n\n**1. Steam sign-in `?auth=error` — actual root cause found.** v5.82 added a granular catch-block log; the very next sign-in attempt printed `[Steam Auth] error: column ps.created_at does not exist`. The post-verify display-name lookup in `/auth/steam/return` (and two Stripe gift-DM lookups) was running `ORDER BY ps.created_at DESC` against `player_stats`, which has no `created_at` column. That tossed an exception **after** Steam already verified the OpenID handshake successfully, so the user landed on `?auth=error` with a valid signature behind the scenes. Replaced all four occurrences with `ORDER BY ps.id DESC` — `player_stats.id` is the `SERIAL PRIMARY KEY`, which is a true monotonically-increasing recency signal. (An earlier draft of this fix used `ORDER BY ps.match_id DESC`, but `match_id` is a VARCHAR that holds prefixed strings for manual matches (`manual_${ts}_…`) and replay-fallback matches (`replay_${ts}`); those sort lexicographically *above* normal numeric IDs and would have given the wrong \"most recent\" persona row in some cases. Caught in code review before shipping.) Sign-in now completes end-to-end on prod.\n\n**2. Tier Ladder Preview no longer shows duplicate ranks.** The admin-panel preview was synthesising sample MMR values and bucketing them through `TierBadge`, which legitimately collapsed adjacent samples into the same band — producing the \"Paladin / Paladin / Templar / Knight / Knight / Footman / Footman …\" row the user spotted. Rewritten to iterate the canonical `MMR_TIERS` array directly: one row per defined tier, showing each tier's actual MMR floor (and \"#1 leaderboard only\" for King). What you see in the preview is now exactly what the leaderboard renders, with no synthetic samples in between.\n\n**3. V1 ladder code removed (full edition is V3-only).** `MMR_TIERS_V1`, the `useV3` prop on `TierBadge`, the `data.useV3` field on the leaderboard, and the dual-column V1/V3 admin preview all deleted. Single canonical `MMR_TIERS` export drives everything. (The deeper `use_v3_trueskill` *rating-engine* toggle in `src/db/index.js` is intentionally untouched — that switches the actual MMR maths, not the display ladder.)\n\n**4. Real heraldic badge artwork on the leaderboard's rank legend.** The small \"worst → … → best\" strip above the leaderboard table was rendering the placeholder emojis (🌾 Peasant 🥾 Vagabond 🏴 Outlaw 📜 Apprentice 🐎 Squire 🗡️ Footman 🛡️ Knight ⚔️ Templar ✨ Paladin 🪓 Warlord 👑 King). Now uses the actual `/badges/tier-N-name.png` artwork for each chip — same heraldic shields used inside the leaderboard rows themselves — and the King chip therefore renders the actual gold crown badge. Emoji is kept as the `onError` fallback so a missing PNG never breaks the strip.\n\n**5. Draft Assistant + Draft Stats merged into one nav item.** They were two separate dropdown buttons under Tools that rendered nearly identical content (the `/draft` page has always had an Assistant tab built in). Removed the duplicate \"Draft Assistant\" dropdown item; the surviving entry is renamed to \"Draft & Assistant\" and routes to `/draft`, which already exposes both the assistant and the stats. The standalone `/draft-assistant` and `/draft-stats` URL routes are kept so any bookmarks still resolve.\n\nFooter version bump v5.82 → v5.83.",
    "author": "System"
  },
  {
    "version": "5.84",
    "title": "Inhouse lobby recognises signed-in users, account dropdown menu in the nav (no more accidental sign-outs), Steam-auth security recap",
    "published_at": "2026-05-06",
    "content": "Three follow-ups to the v5.83 sign-in fix.\n\n**1. Inhouse Lobby was telling signed-in users to sign in.** A field-name typo in `web/src/pages/Inhouse.jsx` was reading `steamUser?.steamAccountId`, but `/api/auth/me` exposes the field as `accountId` (the same name every other page uses — `Home`, `PlayerProfile`, `Settings`, `Schedule`, etc.). With the typo, `myAccountId` was always `null` for signed-in users, so the lobby kept rendering the \"🔒 Sign in with Steam to join\" gate even after a successful sign-in. One-character fix, gate now correctly distinguishes anonymous visitors from signed-in players.\n\n**2. Account dropdown menu in the nav (clicking your name no longer signs you out).** The signed-in pill in the top-right used to fire `logout()` directly on click. Several users hit it by accident expecting a profile shortcut. It now opens a small dropdown with: **View profile**, **Profile settings**, **Notifications**, **My matches**, and **Sign out** (the destructive action is at the bottom, separated by a divider, and rendered in red). Closes on outside click, Escape, or route change. The visual style of the pill itself is unchanged — same green checkmark, same Steam icon — only the click behaviour changed.\n\n**3. Steam sign-in — security recap.** A reminder of how `/auth/steam` actually works, in plain language:\n\n- **We never see your Steam password.** When you click \"Sign in with Steam\", your browser redirects to `steamcommunity.com/openid/login`, you authenticate **directly with Valve**, and Valve redirects you back to us with a signed assertion. The password is typed into Valve's domain, behind their TLS cert, never ours. We have no field that asks for it and no code path that could store it.\n- **All we receive is a Steam ID.** The OpenID assertion contains a single piece of identifying information: your 64-bit `steamID64`. We convert that to the 32-bit `account_id` Dota uses internally, and that's the entire identity. No email, no real name, no friends list, no inventory, no purchase history.\n- **We verify the assertion server-side, byte-for-byte.** The `/auth/steam/return` handler POSTs the **exact** raw query string Steam sent us back to `steamcommunity.com/openid/login` with `openid.mode=check_authentication`, and only proceeds if Steam confirms `is_valid:true`. This is the same `check_authentication` round-trip the canonical `passport-steam` library performs, and it's the protocol-level defence against a forged callback URL — an attacker cannot fabricate a valid `openid.sig` because they don't have Valve's signing key.\n- **The session cookie is HttpOnly + secure + SameSite=lax.** It cannot be read by any JavaScript running on the page (mitigates XSS-based session theft), it is only sent over HTTPS in production, and it is not attached to top-level cross-site POSTs (mitigates CSRF). Behind Cloudflare in front of `oceinhouse.gg`, all session traffic is TLS-terminated and the app trusts the proxy hop via `app.set('trust proxy', 1)`.\n- **Sign-out destroys the session immediately.** `POST /api/auth/logout` calls `req.session.destroy(...)` which removes the row from the session store. The cookie on your machine becomes useless on the next request.\n- **Auth endpoints are rate-limited.** `express-rate-limit` is wired onto every `/auth/*` and `/admin/*` endpoint to make brute-force / replay attempts unviable.\n- **There is no \"forgot password\" flow because there is no password to forget.** If your Steam account itself is compromised, the attacker can sign into our site as you — but they can also already join Dota 2 lobbies as you, edit your Steam profile, etc., so the attack surface is upstream of us. Use Steam Guard / 2FA on your Steam account; that's the only meaningful defence here, and it's the same defence Valve recommends for every Steam-authenticated service.\n\nFooter version bump v5.83 → v5.84.",
    "author": "System"
  },
  {
    "version": "5.85",
    "title": "Steam sign-in trust UX — visible reassurance that we never see your password",
    "published_at": "2026-05-06",
    "content": "Several users hesitated on the plain \"Steam Login\" pill in the nav because it wasn't visually obvious whether they were handing us their Steam password. They aren't — Valve handles the credential entry on their own domain — but the UI never said so. This patch makes that explicit everywhere a Steam sign-in CTA appears.\n\n**1. New reusable trust component** at `web/src/components/SteamTrustModal.jsx`. Exports `WhyIsThisSafeLink` (a small underlined `🛡️ Why is this safe?` link) and `SteamTrustModal` (the full explainer). The modal walks through six trust points in plain language:\n\n- 🔒 Your password never touches our servers — Valve handles the credential entry on their own domain, behind their TLS cert.\n- 🪪 We only receive your Steam ID — no email, no real name, no friends list, no inventory.\n- ✅ Cryptographically verified server-side via Valve's `check_authentication` endpoint — a forged callback URL is mathematically impossible without Valve's signing key.\n- 🍪 Locked-down session cookie — HttpOnly, Secure, SameSite=lax, rate-limited.\n- 🚪 Sign out destroys the session immediately.\n- 🛡️ Steam Guard is the upstream defence.\n\nThe modal also links to Valve's official auth docs at `partner.steamgames.com/doc/features/auth#website` so users can verify the claims independently.\n\n**2. Nav button redesigned.** The signed-out CTA now reads **\"Sign in with Steam 🔒\"** (was \"Steam Login\"), uses the official Steam blue border (`#66c0f4`), shows the Steam icon at full size, and renders a small subline directly under the button: **\"Password stays with Valve · 🛡️ Why is this safe?\"**. The hover-tooltip explicitly says \"You'll sign in directly with Valve at steamcommunity.com — we never see your password.\"\n\n**3. Inhouse lobby gate redesigned.** The previous gate was just a yellow text line: \"Sign in with Steam to join.\" It's now a full pill button using the official Steam styling, with the trust subline (\"You sign in directly with Valve · Why is this safe?\") on the right. Same visual language as the nav so users see the trust signaling consistently across the site.\n\n**Why this matters:** social proof + protocol-level transparency. The bigger Steam blue, the lock icon, the explicit \"Password stays with Valve\" copy, and the one-click explainer collectively answer the only question hesitant users actually have: \"Am I about to type my Steam password into some random site?\" The answer is no, and now the UI says so before they have to ask.\n\nFooter version bump v5.84 → v5.85.",
    "author": "System"
  },
  {
    "version": "5.86",
    "title": "Inhouse Lobby Glow-Up · PERF on Leaderboard · Tier Badges Everywhere · AI Scout Preview",
    "published_at": "2026-05-06",
    "content": "Eight fixes across the dashboard and inhouse flow.\n\n**Inhouse session card redesign.** The bare `Session #N` block has been replaced with a richer card: a brass top-rule, a status pill (Open / Accepting / Drafting / In Progress) coloured to the phase, a captain-mode badge, the player count as its own chip, and a slot-style `players / min` progress bar. Position chips on the join panel now read as labelled buttons with the role number (`P1 Carry`, `P5 Hard Support`) instead of plain text.\n\n**Avg PERF column populates.** `getComputedLeaderboard` now joins `player_stats.perf` and merges by canonical (nickname-merged) account so the leaderboard's `Avg PERF` cell stops showing `—` for players with rated games. Per-row colour coding (Top 1% gold, Excellent green, Average grey, Below red) and the same hover tooltip remain. Players with no rated games yet still render `—`. _NB:_ historical matches still need the owner-only `!perf-backfill` Discord command to populate their `perf` rows; new matches are scored automatically.\n\n**Heraldic badge PNGs replace emojis on public profiles.** The `MmrBadge` shown next to a player's name on their profile (and any caller of the `<MmrBadge>` component) now renders the proper `/badges/tier-N-name.png` artwork instead of the `🛡️ / 🪓 / 👑` emoji glyphs. The leaderboard tier-strip already used the PNGs — both surfaces now match. Emoji is kept as a graceful fallback if the PNG fails to load.\n\n**Leaderboard fits the viewport.** Tightened `.leaderboard-table` cell padding (0.6rem → 0.5/0.4rem) and shrunk `.col-stat` min-width (48 → 38px) so the 13-column table fits a 1280px+ display without horizontal scroll. Below 1100px the redundant `W` and `L` columns hide (Games + Win % carry the same information), and the wrapper still falls back to overflow-x:auto on very narrow screens.\n\n**Floating preview on the profile editor.** `/settings/profile` now renders the live preview card in a sticky right column (top: 80px) on screens ≥ 960px, so it follows the page as you scroll through Bio / Custom title / Theme / Pinned hero / Pinned match / Frame / Extras instead of disappearing off the top. Mobile keeps the single-column stack.\n\n**Profile customization now actually shows extras.** The public profile gating block was checking `profileCard.pinned_match` (snake_case) and `profileCard.extras` truthy — but the API returns the rich match object as `pinnedMatch` (camelCase) and `extras` is always an empty `{}` jsonb default. Both checks were silently swallowing the v5.81 cosmetic block. Now keyed correctly so flair / streak chip / pinned achievement / hero border / heraldic background pattern actually surface on a visitor's view of the profile.\n\n**AI Scout — superuser preview.** `GET /api/player/:id/scouting-report` now bypasses the Pro paywall when the caller sends a valid `x-superuser-key` header. The `getScoutingReport()` API client and the `🔍 AI Scout` button on the player profile page pass the superuser key through automatically, so the owner can preview / QA the Grok-powered report without holding a Pro subscription.\n\n**Stripe / coaching marketplace.** No code change in this patch — the marketplace returns `Payments are not configured` because `STRIPE_SECRET_KEY` is unset on the prod environment. To enable: set `STRIPE_SECRET_KEY` (and the Stripe Connect client id for coach onboarding) in the secrets panel and re-deploy.",
    "author": "System"
  },
  {
    "version": "5.87",
    "title": "Security: Feature flag gates now enforced server-side for coaching marketplace and tournament self-signup",
    "published_at": "2026-05-06",
    "content": "**Two medium-severity authorization gaps closed.** Both the coaching marketplace and tournament self-signup surfaces had their feature-flag helpers stubbed to always return `true`, meaning flag values stored in the database (`'off'`, `'preview'`) had no effect on server-side access control. Any direct HTTP call bypassed the intended rollout gate entirely.\n\n**1. `_flagOn()` now queries the database.** The core helper that all Wave 2/3 feature-flag gates call was a one-liner stub (`return true`). It now reads the `feature_flags` table via `db.getFeatureFlag(key)` and applies the correct policy: `'on'` → allow everyone; `'preview'` → allow superusers only; `'off'` or missing → deny with 404. The error path fails closed (returns `false`) so a DB outage does not accidentally open a gate.\n\n**2. `_selfSignupVisible()` delegates to `_flagOn()`.** The tournament self-signup helper was an identical stub. It now calls `_flagOn('tournament_self_signup', req)`, meaning the `tournament_self_signup` flag seeded as `'off'` in the database actually prevents access to all self-signup routes.\n\n**3. Mutating tournament routes now check the flag.** `POST /api/tournaments/:id/checkout` and `POST /api/tournaments/:id/free-signup` both lacked any feature-flag check despite the route comments stating they should 404 when the flag is off. Both routes now call `_selfSignupVisible(req)` as the first gate before any auth or business logic, consistent with the read-only routes on the same surface.\n\n**Impact.** With `tournament_self_signup = 'off'` (the seeded default), non-superusers can no longer create Stripe Checkout sessions or insert free tournament entries directly over HTTP. With `coaching_marketplace = 'off'`, all coaching endpoints (onboarding, booking, disputes, refunds) now correctly return 404 to non-superusers. Superusers retain preview access in both cases when the flag is set to `'preview'`.\n\nFooter bump v5.86 → v5.87.",
    "author": "System"
  },
  {
    "version": "5.88",
    "title": "Records Hub Consolidation · Floating Profile Preview · Sign-Out Frees Lobby Slot",
    "published_at": "2026-05-06",
    "content": "Six fixes from the latest user batch — bigger items (admin-only end-to-end demo lobby, full public-profile parity with the private editor, dota-rank/nickname/discord-id import) are scoped as separate follow-ups.\n\n**Leaderboard XP column removed.** The Season Pass `XP` column was just noise next to Avg PERF / Impact / Streak / Form — most rows showed `—` anyway because the season-pass economy isn't surfaced to non-pro users. Setting `showSeasonPass = false` strips both the header cell and every row's XP cell, and skips the `getSeasonPassMap` fetch entirely so we save a DB round-trip on every leaderboard load.\n\n**Sign-in-with-Steam button stops shifting the navbar.** The `Password stays with Valve · Why is this safe?` footnote was sitting in the same flex-column as the button with `align-items: flex-end`, so its wider text dragged the button (and every nav item to the right of it) out of alignment. The wrapper is now `position: relative; display: inline-block` and the footnote is `position: absolute; top: 100%; left: 50%; transform: translateX(-50%)` — the button keeps the exact width and position it had before the footnote existed, and the centred trust line floats underneath without affecting layout flow.\n\n**Profile editor preview is now a true floating panel on the right.** `/settings/profile` previously rendered the live preview as the FIRST grid child, which placed it in the wide left column with the form crammed into a 360px right rail (the opposite of what was intended). The preview is now `position: fixed; top: 84px; right: 16px; width: 320px` — pinned to the right edge of the viewport, scrolling independently with `overflow-y: auto`, never taking column space from the form. The form itself flows in a clean centred 720px column. On screens narrower than 1240px the preview drops back into the normal flow at the top so it never overlaps the form.\n\n**Hall of Fame and Multi-Kills consolidated under Records.** The standalone `/hall-of-fame` and `/multikills` nav entries are gone from the Tools dropdown. The Records page (renamed from `Records & Comebacks` to just `Records`) now hosts a `🏛️ Hall of Fame` tab between Season Records and First Blood that renders the full HoF component inline (records / career / achievement hunters / top recruiters), and the existing Multi-Kills tab is the single source of truth for those stats. The standalone routes still resolve so old bookmarks keep working.\n\n**Hall of Fame — career rankings show real data.** The career table required `>= 3` games per player to appear, which left the table empty in early seasons. Lowered the minimum to `>= 1` so the table populates as soon as anyone has played a single rated match in the active season — match records were always shown without a minimum.\n\n**Sign-out releases your inhouse lobby slot.** A signed-in player who was registered for an open or accepting inhouse session used to keep their slot warm even after they signed out — there was no link between `POST /api/auth/logout` and the inhouse roster. The logout handler now calls a new `db.leaveAllJoinableInhouseSessions(accountId)` helper before destroying the session, which deletes the player's row from any session whose status is `open` or `accepting`. Drafting / in-progress sessions are left alone (you shouldn't be able to vanish mid-draft).",
    "author": "System"
  },
  {
    "version": "5.89",
    "title": "Public Profile Parity · Admin Demo Lobby (Bots + Auto-Draft) · Community Nickname Import",
    "published_at": "2026-05-06",
    "content": "The three follow-up items from v5.88 land in this drop.\n\n**Performance Trend chart now visible on every profile.** The V2 trend chart (rolling KDA / GPM / XPM / hero damage with the dual-axis Pro view) was gated behind `isOwnProfile`, so visitors never saw it on anyone else's page even though the underlying API already enforces a Pro paywall server-side. Removed the owner gate from both the data fetch and the render — any signed-in viewer can now load the trend chart on any profile, and the existing paywall card still shows for non-Pro viewers without changing the auth model. The other 'rich private profile' pieces (V3 PERF modifier chart, MMR history, KDA / hook% / first-blood / impact / MVP cards, profile-card flair / pinned hero / pinned match / pinned achievement / hero border / accent / extras) were already public — they just looked owner-only because the trend chart was the most visually obvious piece missing.\n\n**Admin demo lobby — seed bots, auto-draft, end-to-end run.** Three new admin-only endpoints let you walk the entire FACEIT-style flow without recruiting 9 friends:\n- `POST /api/admin/inhouse/:id/seed-bots` fills empty slots up to `min_players` with bot accounts (`9_000_001..9_000_010`), gives each one a friendly nickname (`Bot Lina`, `Bot Pudge`, …), random preferred positions, and pre-accepts them so you can jump straight to captain selection.\n- `POST /api/admin/inhouse/:id/clear-bots` removes every bot from the session (only allowed before `in_progress`).\n- `POST /api/admin/inhouse/:id/auto-draft` randomly distributes the remaining 8 non-captain picks across both teams following the existing `DRAFT_PICK_SEQUENCE`, so once captains are set you can fast-forward to a fully-drafted ready lobby. Works whether the captains are bots or real players.\nAll three are surfaced as small dashed-brass admin buttons on the Inhouse page (🤖 Seed Bots, Clear Bots, 🎲 Auto-Draft) that appear contextually based on session phase.\n\n**Community → Full nickname / Discord / rank import.** The existing `scripts/sync-community-nicknames.js` was refactored into a reusable `runSync({ sourceUrl, destPool, overwrite, dryRun, log })` function (the CLI entry still works the same way). A new admin endpoint `POST /api/admin/sync-community-nicknames` reads `COMMUNITY_DATABASE_URL` from the server env, calls `runSync` against the live destination pool, and returns the per-row log. The Admin Panel now has an 📥 Import from Community Edition card under Dota 2 Rank Management with two checkboxes — Dry Run (default on, previews without writing) and Overwrite (off by default, only fills empty target columns). Players who migrate from the community edition keep their nickname, Discord link, and Dota rank without any manual re-entry; the `players` table also gets a row inserted for each imported Discord-linked account so the bot's `!register` / role mapping lookups find them immediately.\n\n**Setup note.** The community-edition Postgres connection string must be added as a `COMMUNITY_DATABASE_URL` secret on the prod host before the import button works. The server refuses to run if the source URL is missing or identical to `DATABASE_URL`.",
    "author": "System"
  },
  {
    "version": "5.90",
    "title": "Quick admin / UX fixes batch (8 items)",
    "published_at": "2026-05-06",
    "content": "Eight unrelated small fixes shipped together because each one is a small change in adjacent surfaces.\n\n• **AI Scout error surfaced cleanly.** The `/player/:id/scouting-report` endpoint now strips ```json fences AND falls back to extracting the first `{...}` block from the LLM response before declaring it unparseable. When parsing still fails it logs the raw payload server-side and returns a friendlier `Scout temporarily unavailable, try again in a minute.` 503 instead of the previous half-error string.\n\n• **RCON-not-configured message rewritten.** The Inhouse \"Provision server\" button used to alert `Server provisioned but RCON push failed: ...` whenever the dedicated server wasn't configured. The message now distinguishes the missing-config case from a real RCON failure: `Dedicated server is not configured yet — players will use the connect link below. (Admin: set DEDICATED_SERVER_IP to enable RCON push.)`. Real RCON failures still surface their underlying error.\n\n• **Bots filtered out of Dota 2 Rank Management.** `GET /api/ranks` now strips dummy bot accounts in the `9_000_001..9_000_010` range used by the Inhouse draft sandbox, so `Bot AM` / `Bot CM` / etc. no longer pollute the rank-management table. The community-sync error banner above the table is unaffected.\n\n• **Profile-settings live preview no longer clips into the navbar.** `/settings/profile` preview moved from `position: fixed; top: 84px` (which sat on top of the global nav) to `position: absolute; top: 120px; right: 24px` inside the form column, with the inline-flow breakpoint pulled in from 1400px to 1240px so more screens get the side-by-side layout.\n\n• **Notifications page labels render.** `/settings/notifications` was showing blank rows because the `CATEGORY_LABELS` dict was missing entries for `match_ready` and the three coaching categories (`coaching_booking_confirmed`, `coaching_session_reminder`, `coaching_review_request`). All four are now defined, and any future server-side category falls back to a Title-Cased version of its key (`humaniseCategory`) instead of rendering empty.\n\n• **\"Sign in with Steam\" trust note tidied.** The footnote under the navbar Steam button is now a single muted line — `Password stays with Valve · Why is this safe?` — with the lock emoji and shield emoji removed and the link inheriting the same colour and font-size so it reads as one quiet sentence rather than three competing visual elements.\n\n• **Rating System pinned to TrueSkill V3 (V1 unreachable).** The Admin Panel → Seasons → Rating System block is now a small read-only status line (`Rating engine: TrueSkill V3`); the toggle button, the V3-vs-V1 preview table, and the related state/handlers (`useV3`, `v3PreviewOpen`, `handleToggleRatingSystem`, `handleRunV3Preview`) are all gone from `AdminPanel.jsx`. **Server-side enforcement** was added to match: `getComputedLeaderboard()` and the per-profile season-MMR path in `src/db/index.js` (and the mirrored `community-edition/src/db/index.js`) no longer branch on `use_v3_trueskill` and always call `computeSeasonTrueSkillV3`. The site-settings seed flips the existing row to `'true'` (`ON CONFLICT DO UPDATE`) so existing deployments cannot stay on V1 silently, and the key is removed from `ALLOWED_SETTING_KEYS` in both server.js files so no admin endpoint can flip it back. `/leaderboard` now returns `useV3: true` unconditionally. The legacy `computeSeasonTrueSkill` (V1) function and the `/admin/v3-preview` comparison endpoint are intentionally kept (read-only) so historical scripts and post-mortem comparisons keep working.\n\n• **Sync Ranks button: clearer auth error.** `triggerRankSync` now sends `credentials: 'include'` explicitly (so the session cookie is always attached, even if BASE is ever switched cross-origin) and translates a 401/403 into `Superuser session expired — log out and back in via the 🛡️ Superuser button, then try again.` instead of the bare `Invalid superuser key` from the server.\n\n• **Inhouse lobby upscaled to Hybrid · Court & Pitch — nicknames everywhere, sound + browser alerts, sandbox-parity captain draft.** Every name in the lobby and captain-draft UI now flows through a single `resolveDisplayName(accountId, nicknameMap, player)` helper in `web/src/utils/displayName.js` (order: explicit nickname map → `player.nickname` → demo `bot_name` → Steam `persona_name` / `display_name` → friendly `Bot N` for the 9_000_001..9_000_010 demo range → `Player #<id>` only as the absolute last fallback, never the default), so captain banners and player rows alike stop rendering raw account ids. The captain-draft board on `/inhouse` is rebuilt to mirror the sandbox composition: a brass-top-rule timer/turn-indicator strip anchored at the top, then a three-column board with vertical roster panels left and right (each with their own brass top-rule, Playfair Display captain lockup, and Oswald roster eyebrow) and the unpicked-player pool centred between them — stacks to one column under 880px via `.inhouse-draft-board` in `web/src/styles.css`. Player rows pick up a brass left-rule and a stronger highlight when the row is the signed-in user, and the page header gets the editorial double-rule under the H1. A new `useInhouseAlerts()` hook (`web/src/hooks/useInhouseAlerts.js`) plays a short two-tone chime served from `web/public/sounds/inhouse-alert.mp3` (generated with ffmpeg, 880 Hz → 1320 Hz, ~0.4 s, 96 kbps mono) AND fires a `Notification` API toast on every event that needs a human input — accept phase opens for you, captains were chosen and you are one, it is your turn to draft, and the connect link goes live — diffing previous-vs-current session/draft state so each event fires exactly once. A 🔔 / 🔕 chime toggle in the lobby header persists in `localStorage` under `inhouse:muted`; the toggle silences only the audible chime so OS-level browser notifications keep firing for muted tabs. Finally, the `Start Accept Phase` button now carries a tooltip explaining that it locks the lobby and opens the 60-second accept window, and players who don't accept in time are dropped from the session.",
    "author": "System"
  },
  {
    "version": "5.90.1",
    "title": "Inhouse Lobby Hardening · Discord-Link + Guild Membership Hard Gate · Stale Slot Sweep",
    "published_at": "2026-05-07",
    "content": "Joining the inhouse lobby is now strictly gated on having a Discord account linked AND being a current member of the OCE Inhouse Discord server, with a tri-state UI gate, a server-side join enforcement, a heartbeat-based stale-slot sweep, and a pre-flight re-verification before the captain draft starts.\n\n**Server-side hard gate on every join.** A new `_enforceDiscordGuildGate(actor)` helper runs at the top of both `POST /api/inhouse/join` (auto-create + join) and `POST /api/inhouse/:id/join`. It looks up the caller's `nicknames.discord_id` and asks the bot whether that Discord ID is currently a member of `DISCORD_GUILD_ID`. Returns a 403 with a machine-readable `code` field — `discord_required` (no Discord ID linked) or `discord_not_in_guild` (linked but not in the OCE Inhouse server) — so the frontend can render the right CTA. Admins (`x-superuser-key`) bypass both checks so the demo / seed-bots flow is unaffected. If the bot is starting up or `DISCORD_GUILD_ID` is unset the helper soft-passes (with a `[inhouse-gate]` log line) rather than locking everyone out.\n\n**Bot-side guild membership check.** A new `bot.isInLeagueGuild(discordId)` method on `DiscordBot` looks the user up via `guild.members.fetch(...)` and caches the result per Discord ID for ~30s so the auth-poll path stays cheap. Returns `{ inGuild: true | false | null, configured: bool }` — `null` means `unknown` (cache miss + bot not ready / guild unreachable) and is treated as a soft-pass everywhere it's consumed.\n\n**Tri-state UI gate on the lobby page.** A new `<DiscordJoinGate>` component renders inline beneath both `Sign In to Inhouse` and `Join Session` buttons (which are now `disabled` whenever the gate is blocking). It shows three explicit states with their own CTAs: signed-out → existing Steam sign-in copy; `needs_discord_link` → blue Discord-branded `Link Discord` button pointing at `/auth/discord`; `discord_in_guild === false` → brass `Open the Discord invite ↗` button using `config.discord.serverInvite` (env `DISCORD_INVITE`). The gate re-polls `/api/auth/me` every 8s while it's open so finishing the OAuth link or joining the server makes the join button light up automatically — no manual page refresh.\n\n**`/api/auth/me` now exposes guild state.** The payload picks up three new fields: `discord_in_guild` (true / false / null), `discord_guild_configured` (whether `DISCORD_GUILD_ID` is set), and `discord_invite_url` (the configured invite). The frontend reads these to drive the gate without an extra round trip.\n\n**Liveness heartbeat + stale-slot sweep.** A new `last_seen_at TIMESTAMPTZ` column on `inhouse_session_players` (added via `ALTER TABLE … ADD COLUMN IF NOT EXISTS`) is bumped by a new `POST /api/inhouse/heartbeat` endpoint. The Inhouse page pings it every 15s while the player is registered, on `visibilitychange → visible`, and fires a best-effort `navigator.sendBeacon` on `visibilitychange → hidden` and `pagehide` so an immediate tab-close still registers a final ping (the beacon survives page teardown where a normal fetch would be cancelled). The `autoStartTicker` runs `db.pruneStaleInhousePlayers()` at the top of every 5s tick to delete any open/accepting roster row whose `last_seen_at` is older than `INHOUSE_HEARTBEAT_STALE_SECONDS` (default 45s — three missed pings). Closed-tab / asleep-browser players now release their slot in under a minute instead of warming it indefinitely. Same-site route switches keep the seat — the heartbeat just keeps ticking on whichever page mounts the inhouse panel.\n\n**Steam-session-expiry sweep.** Beyond the heartbeat-staleness check, the ticker now also runs a session-validity sweep on every tick. A new `last_session_id` column on `inhouse_session_players` records the express-session id at join and on every heartbeat; the ticker asks the live express-session store (now exposed via `app.locals.sessionStore`) `store.get(sid, …)` for each unique session token currently attached to an active seat. Seats whose session has been destroyed — explicit `/auth/logout`, cookie expiry, store eviction — are dropped on the next 5s tick instead of waiting out the 45s heartbeat window. Store-side errors and seats with no recorded session id (legacy / bot demo rows) soft-pass so a transient hiccup can't depopulate a real lobby.\n\n**Pre-flight Discord guild re-verification before the captain draft.** `POST /api/inhouse/:id/select-captains` now iterates every accepted player BEFORE the atomic flip from `accepting → drafting`, re-checks each one with `bot.isInLeagueGuild(...)`, and drops anyone who has left the server or unlinked their Discord since they joined (demo bots in the `9_000_001..9_000_010` range are skipped). If the post-trim accepted count drops below the session's `min_players` floor the route returns the lobby to `open` and resets accepted/declined statuses (mirroring the existing `autoStartTicker` shortfall path), responding with `409 { code: 'preflight_shortfall' }`. Soft-pass behaviour is preserved when the bot can't answer for membership so a Discord outage can't cancel a real game.\n\n**Operator surface unchanged.** No new env vars are required to enable the gate beyond the existing `DISCORD_GUILD_ID` (already needed for `addUserToLeagueGuild`) and `DISCORD_INVITE` (already used in DM templates); the staleness threshold can be tuned via `INHOUSE_HEARTBEAT_STALE_SECONDS`. Existing sessions auto-pick up `last_seen_at` defaulted to `NOW()` so no in-flight lobby loses its roster on the migration.\n\nFooter bump v5.89 → v5.90.",
    "author": "System"
  },
  {
    "version": "5.91",
    "title": "Public-profile parity audit — every block confirmed visible to non-owners",
    "published_at": "2026-05-06",
    "content": "Follow-up to v5.89 after a report that public profiles still looked stripped-down. Did a full diff between the `isOwnProfile=true` and `isOwnProfile=false` render paths in `web/src/pages/PlayerProfile.jsx` and confirmed every data block is already public; only four explicitly opt-in elements remain owner-scoped (✏️ Edit Profile button, 🎓 Apply-to-coach CTA, 🔗 invite/referral link, and the inverse 🎁 Gift Pro / 🎫 Gift Season Pass buttons that only show on *other* people's profiles). Verified server-side that none of the data endpoints powering those blocks contain owner-only filtering — `/players/:id`, `/players/:id/rating-history`, `/players/:id/v3-modifier-history`, `/players/:id/positions`, `/players/:id/achievements`, `/players/:id/hero-counters`, `/players/:id/duration-stats`, `/player/:id/profile-card`, `/player/:id/win-rate-history`, `/player/:id/nemesis`, `/player/:id/ally`, `/player/:id/season-pass`, `/player/:id/mvp-attitude-trends`, `/player/:id/hero-suggestions`, and `/impact-scores` are all open public reads. The two intentionally Pro-paywalled blocks (V2 Performance Trend chart and AI Scouting Report) already render a `PaywallCard` for non-Pro signed-in viewers instead of disappearing. To stop this regression from happening silently again, every section in `PlayerProfile.jsx` now carries an `AUDIT (v5.91 parity pass): PUBLIC|OWNER-ONLY|PRO-PAYWALLED` JSX comment naming the backing endpoint and gate, so any future PR that re-adds an `isOwnProfile` guard will jump out in code review. Newly-confirmed public blocks (now explicitly labelled): profile customization card (bio/title/accent/pinned hero w/ border/pinned match/pinned achievement/flair/streak chip/background pattern), top stat-cards row (MMR/W-L/Win%/KDA/Avg PERF/Streak/First Blood/Hook%/Impact/Damage/MVP/Attitude), MMR History chart, V3 PERF Modifier chart, Rolling Win Rate chart, Achievement badges, Season Pass tier, MVP & Attitude trends, Match Predictions, Nemesis, Best Allies, Hero Matchups, Averages, Position Breakdown, How You Compare, Most Played Heroes, Win Rate by Game Duration, Heroes to Try, Recent Matches.",
    "author": "System"
  },
  {
    "version": "5.92",
    "title": "Tournament self-registration + paid buy-ins (Stripe) with withdraw/refund + participant entry badges",
    "published_at": "2026-05-06",
    "content": "Tournaments are now self-serve end-to-end behind the existing `tournament_self_signup` feature flag — players can register and pay (or sign up for free), and admins no longer have to chase players to seed the bracket.\n\n**Player-facing Register / Withdraw widget.** A new 🎟️ Sign-ups panel appears on every upcoming tournament's detail page (between the live prize-pool ticker and the admin tools). Signed-in Steam users see one of three states: 'Register (free)' / 'Register ($X AUD)' button, a 'You are entered ✓ Withdraw' state, or an inline reason if the signup window is closed, the cap is full, the player isn't in the right tier, or they aren't signed in. The button funnels through a single new `POST /api/tournaments/:id/register` endpoint that the server dispatches to free-signup or Stripe Checkout based on `entry_fee_cents` — the front-end doesn't need to know the difference. After Stripe redirects back with `?signup=success&session_id=…`, the panel auto-confirms the entry (so the row flips to paid even if the webhook is slow) and strips the query string.\n\n**Withdraw + automatic refund.** New `POST /api/tournaments/:id/withdraw` route, allowed only while `tournament.status === 'upcoming'` and bound to the authenticated Steam session (superusers may withdraw on behalf of a player). For paid entries it calls `stripe.refunds.create({ payment_intent: <stored PI> })` *first* and only flips the entry to `refunded` if Stripe accepts the refund — so you can't end up with a refunded DB row the player was never paid back for. The player is also removed from `tournament_participants` and the prize pool is recomputed.\n\n**PaymentIntent now persisted on entry rows.** New `tournament_entries.stripe_payment_intent_id` column. Both the Stripe webhook (`checkout.session.completed`) and the redirect-back confirm endpoint now stash `session.payment_intent` so the withdraw flow has something to refund against. Existing paid entries from before this version have NULL — they'll get a clear `409 No payment record on file — contact an admin to refund manually.` error if the player tries to withdraw, instead of a silent failure.\n\n**Capacity gate.** New `tournaments.max_participants` column. `isPlayerEligibleForTournament()` now also rejects new sign-ups with `Tournament is full` once the paid-entry count hits the cap. A player with an existing pending row is still allowed through (so a checkout retry isn't locked out by their own pending entry).\n\n**Auto-mirror paid entry → participants.** `markTournamentEntryPaid()` now inserts the player into `tournament_participants` (ON CONFLICT DO NOTHING), so a successful Stripe checkout instantly populates the bracket roster — no admin step needed. Withdraw inversely deletes them.\n\n**Admin create form picked up the new fields.** The 'New Tournament' form (superuser only, on `/tournaments`) now has an explicit *Self sign-up* section with: entry fee in AUD (blank = free, converted to cents server-side), prize split as comma-separated percentages that must sum to 100 (default `50,30,20`, validated server-side), max participants (blank = no cap), and signup open / close timestamps. The fields are hidden for the Points-tournament format which has its own admin surface.\n\n**Server-side guardrails kept tight.** The new routes reuse the existing `_selfSignupVisible` / `_isSelfSignupSuperuser` helpers, so the entire surface still 404s when the flag is off (no route-existence disclosure). Eligibility, signup-window, and capacity checks all live in `db.isPlayerEligibleForTournament()` so /register, /checkout, and /free-signup share one source of truth — the new /register handler is just a thin dispatcher that internally rewrites to /checkout or /free-signup.",
    "author": "System"
  },
  {
    "version": "5.93",
    "title": "Coaching Marketplace opens to everyone",
    "published_at": "2026-05-06",
    "content": "The Coaching Marketplace is live for all signed-in users — the existing Stripe Connect onboarding, booking, and payout flows are unchanged; we've just flipped the gate.\n\n• **`coaching_marketplace` flag flipped on.** Seed value in `feature_flags` is now `'on'` for fresh installs, and a one-shot migration in `db.init()` bumps existing rows still sitting at the launch-day `'off'` to `'on'`. The migration is gated on a `coaching_marketplace_v593_launched` sentinel row in `site_settings`, so it runs **exactly once per database** — admins who later use the kill-switch panel to flip the flag back to `'preview'` or `'off'` will see their setting persist across restarts. Rows previously set to `'preview'` by an admin are also left untouched. `_coachingOn(req)` / `_flagOn('coaching_marketplace', req)` now resolve true for everyone, so `/coaches`, `/coach/onboarding`, `/coach/edit`, `/coaches/:id`, and the full `/api/coaching/*` route surface stop returning 404.\n\n• **Beta pill on the marketplace header.** `/coaches` now renders a small brass-amber `Beta` pill next to the 🎓 Coaching Marketplace title. Hardcoded for one release cycle so users can see the surface is freshly opened — comment in `web/src/pages/Coaches.jsx` flags the line for removal next version.\n\n• **Admin kill-switch added to Site Settings.** New `🎓 Coaching Marketplace — feature flag` panel under Admin Panel → Site Settings (anchor `ap-anchor-coaching-flag`, indexed in the admin search). Three buttons (`On` / `Preview` / `Off`) wired to the existing `getAdminFeatureFlags` + `setFeatureFlag` endpoints, so the marketplace can be rolled back to superusers-only or fully hidden without a DB shell if anything goes sideways post-launch.",
    "author": "System"
  },
  {
    "version": "5.94",
    "title": "First-login Discord ID onboarding modal",
    "published_at": "2026-05-06",
    "content": "Players who sign in with Steam for the first time and never used the **Join the League** form had no Discord link, so the bot couldn't DM them, mention them, or assign roles — and nothing prompted them to fix it. A new one-time modal now collects the Discord User ID right after first sign-in.\n\n• **New `<DiscordLinkModal />` mounted globally in `App.jsx`.** Watches `steamUser.needs_discord_link` from `/api/auth/me` and renders *“One last step — link your Discord”* with a Discord User ID input (`123456789012345678` placeholder), short instructions for enabling Developer Mode and copying the ID, a *Skip for now* link that dismisses for the rest of the session, and a *Save* button. Re-shows up to **3 times across sessions** (counter persisted in `localStorage` under `discordLinkModal:skipCount`); after that we stop nagging until the player updates from `/settings/profile`. A success toast (*“Discord linked. The bot can now DM you.”*) confirms the save.\n\n• **`/api/auth/me` payload extended.** Returns `discord_id` and `needs_discord_link: boolean` so the frontend never needs a second request to decide whether to prompt. Players who joined via the Join-the-League form already have `nicknames.discord_id` populated, so the modal is silently skipped for them.\n\n• **New `POST /api/me/link-discord` endpoint (signed-in only).** Validates the Discord ID is a 17–19 digit snowflake, then UPSERTs into `nicknames` for the caller's `account_id` via a new `db.linkOwnDiscordId()` helper that creates a nickname row on the fly when one doesn't exist yet (a brand-new Steam sign-in won't have one). The existing superuser-only `setDiscordId()` is unchanged.\n\n• **Discord field added to `/settings/profile`.** A new *Discord link* section at the top of the form pre-fills with the current `discord_id`, validates the same 17–19 digit format, and saves through the same endpoint — so players can update it later without going through the modal."
  },
  {
    "version": "5.95",
    "title": "TrueSkill V1 code paths fully removed from the rating engine",
    "published_at": "2026-05-06",
    "content": "Follow-up to v5.90, which pinned the production rating engine to TrueSkill V3 by removing the toggle from the Admin Panel but intentionally left the V1 helpers and the V3-vs-V1 comparison endpoint in place \"to keep historical scripts working.\" Those paths were dead weight — nothing in the codebase or the live UI could reach them anymore — so this release deletes them outright.\n\n• **`db.computeSeasonTrueSkill` (V1) deleted from `src/db/index.js`.** The legacy season-MMR computer (≈100 lines, sole consumer of `statsService.calculateNewRatings`) is gone, along with its module export. The only remaining caller — the `/admin/v3-preview` route — is also removed (see below). All production reads now go through `computeSeasonTrueSkillV3`, which `getComputedLeaderboard()` and the per-profile season-MMR path were already calling unconditionally since v5.90.\n\n• **`/api/admin/v3-preview` route deleted from `src/web/server.js`.** The superuser-only V3-vs-V1 leaderboard comparison endpoint (which returned `v1_mmr`/`v3_mmr`/`delta` plus the legacy `ts1_mmr`/`ts2_mmr` aliases) is removed. Its frontend trigger was already dropped from the Admin Panel in v5.90, so no UI surface called it; this release removes the unreachable handler.\n\n• **`use_v3_trueskill` site setting removed.** Dropped from `ALLOWED_SETTING_KEYS` in `src/web/server.js` so no admin endpoint can write it. The `db.init()` seed that previously force-flipped the row to `'true'` on every startup is replaced with a one-shot `DELETE FROM site_settings WHERE key = 'use_v3_trueskill'` so existing deployments physically clear the row from their settings table on the next boot. Idempotent — no-op once cleared.\n\n• **Comments and docstrings cleaned up.** The `// ── TrueSkill V3 ──` header that referred to V1 (\"Mirrors computeSeasonTrueSkill but uses the V3 environment…\") is rewritten to stand on its own. The stale comment in `web/src/pages/AdminPanel.jsx` referencing the removed `ratingToggleSaving` / `v3Preview*` state is shortened to a single-line note.\n\n**Historical-script migration.** Anything still calling `db.computeSeasonTrueSkill(seasonId)` will now throw `TypeError: db.computeSeasonTrueSkill is not a function`. Replace the call with `db.computeSeasonTrueSkillV3(seasonId)` — the return shape (`{ ratings, accountToCanonical }`) is identical, the only difference is that ratings include the per-match performance modifier (clamped 0.80–1.20) the production engine has used since v5.90.",
    "author": "System"
  },
  {
    "version": "5.96",
    "title": "Auto-recover the superuser session so admin actions don't 403 silently",
    "published_at": "2026-05-06",
    "content": "Follow-up to v5.90, which made the **Sync Ranks** button surface a clearer *Superuser session expired — log in again* message instead of the bare *Invalid superuser key* string. The same dropped-session failure mode existed across **every** admin button (rank-sync, replay management, broadcast ticker, season tiers, hero-tier overrides, tournament admin, patch notes, scheduled games, season management, DB backups, etc.) — the user had to notice the toast, hit 🛡️ Superuser, log back in, and then re-click the original action. v5.96 detects a dropped session globally and prompts re-login automatically, then transparently retries the original request.\n\n• **Centralised `superuserFetch()` / `superuserJson()` wrapper in `web/src/api.js`.** All admin/superuser-gated calls now funnel through a single helper that intercepts HTTP 401 (and 403 carrying the `session` sentinel header) and asks `SuperuserContext` to re-prompt the operator. On a successful re-login the original request is retried once; if the operator dismisses the modal, the call rejects with a friendly *Superuser session expired — please log in again.* error. Per-button error toasts continue to surface non-auth failures unchanged.\n\n• **`SuperuserContext` exposes a `requestReauth()` Promise.** Registered with the api wrapper at provider mount via the new `setSuperuserReauthHandler()` hook. Concurrent in-flight admin calls all await the same pending re-auth so a single login unblocks every queued request. Closing the modal without logging in resolves the promise as `false` so callers don't hang.\n\n• **Server-side `requireSuperuser` 401-vs-403 split in `src/web/server.js`.** Returns **401** when no header or the browser `session` sentinel is presented without a valid cookie (i.e. session expired — the frontend wrapper auto-recovers); returns **403** only when a script presents an explicit wrong literal credential (no UI re-prompt). Existing valid-key flows are unchanged.\n\n• **40 `web/src/api.js` superuser-gated functions migrated** to the wrapper (feature flags, patch notes, replay path management, replay extend/store, hero tier overrides, nicknames, Discord-id linking, schedule create/cancel, season delete/close/announce, recompute achievements, all tournament admin endpoints, season tiers, signup-request review, weekend-tournament admin, manual rank set/clear, etc.).\n\n• **AdminPanel inline `fetch()` calls (19 sites)** for DB backup/restore/delete + nickname-id fix, replay reparse / reparse-all / status / set-all-permanent / download, error log read+clear, Steam lobby + friends-add-all + recalculate-ratings + duplicate-matches, broadcast-ticker / welcome-modal / home-banner settings, unregistered-players, season tiers admin + place-all + override + end-conditions + close + announce, hero tier overrides admin, signup-request status updates, achievement recompute — all rewritten to use `superuserFetch`. Same applies to the 3 `StatsEditor.jsx` mutation calls.\n\n• **Backwards-compatible signature.** `superuserFetch(url, options)` accepts either `'/foo'` or `BASE + '/foo'` so existing call-site shapes don't change beyond the function name. Bots / scripts still hitting endpoints with a header-only credential get the same status codes they did before; only browser sessions go through the auto-recovery loop.",
    "author": "System"
  },
  {
    "version": "5.97",
    "title": "Snapshot test guards public-profile parity from silent regressions",
    "published_at": "2026-05-06",
    "content": "Follow-up to v5.91, which added `AUDIT (v5.91 parity pass)` JSX comments above every section in `web/src/pages/PlayerProfile.jsx` so any future PR re-introducing an `isOwnProfile` guard around an entire section would jump out in code review. That was a human safeguard — easy to miss on a fast-moving PR. v5.97 codifies the rule as an automated test so it can never silently break again.\n\n• **New `web/src/pages/__tests__/PlayerProfile.parity.test.jsx`.** Renders `<PlayerProfile />` twice for the same `accountId` — once with a Steam-auth context where `accountId` matches the URL param (owner) and once where it doesn't (a different signed-in visitor) — and asserts every `<h2 className=\"section-title\">` text in the owner render is also present in the public render (and vice versa). Owner-only chunks intentionally outside the parity check (✏️ Edit Profile, 🎓 Apply to coach, 🔗 Your Invite Link) and visitor-only chunks (🎁 Gift Pro, 🎫 Gift Season Pass) are asserted explicitly so re-classifying one of them silently also fails the test.\n\n• **Vitest harness wired up for the `web/` workspace.** Added `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, and `jsdom` as dev-dependencies; new `web/vitest.config.js` configures the jsdom environment and points at `web/src/test/setup.js`; `npm test` (run from `web/`) executes the suite. API responses are mocked with `vi.mock('../../api')` and direct `fetch()` calls (`/profile-card`, `/season-pass`, `/mvp-attitude-trends`, `/invite-link`, `/referrals`, `/coaching/eligibility/me`) are mocked via a stubbed `global.fetch`, so the test runs without a live DB or backend. Recharts is stubbed to a passthrough since jsdom can't render its SVG layout and we only care about the surrounding `<section>` titles.",
    "author": "System"
  },
  {
    "version": "5.98",
    "title": "Verify Discord IDs against the real account before saving",
    "published_at": "2026-05-06",
    "content": "Follow-up to v5.94, which added the first-login Discord ID onboarding modal but deliberately left verification out of scope — `POST /api/me/link-discord` accepted any 17–19 digit snowflake. A typo (or someone else's ID) saved silently and only surfaced later as the bot mysteriously failing to DM the player. v5.98 closes that gap by round-tripping through Discord before persisting anything.\n\n• **New `bot.verifyAndConfirmDiscordId(id)` helper in `src/discord/bot.js`.** Calls `client.users.fetch(id)` to confirm the snowflake belongs to a real Discord user, then immediately attempts to DM them a confirmation message (*“OCE Inhouse — Discord linked.”*). Returns `{ ok, code, error, username }` with discrete failure codes — `not_ready` (bot still booting, transient), `not_found` (Discord REST returned `Unknown User` / code 10013), `dm_blocked` (user exists but won't accept our DM, code 50007), and `unknown` (anything else). Each code maps to a specific user-facing message (e.g. the `dm_blocked` copy explicitly tells them to join the server and toggle *Direct Messages from server members* in Privacy Settings).\n\n• **`POST /api/me/link-discord` now gates on verification.** Before any DB write, the route asks the bot to verify-and-DM. On failure it returns the bot's specific error and HTTP status (400 for not_found / dm_blocked, 503 for not_ready) with a `code` field, and `nicknames.discord_id` is left untouched — so a typo can never silently break future bot DMs. Only after both the REST fetch *and* the DM succeed do we call `db.linkOwnDiscordId()`. The success response now includes the `verified_username` so the UI can show *Discord linked to @username*. Defensive idempotency: if the caller already has the same ID linked we return `{ ok: true, alreadyLinked: true }`; if they have a different ID linked we 409 instead of silently overwriting.\n\n• **Modal toast tells the user to check their DMs.** `web/src/components/DiscordLinkModal.jsx` now displays the verified username and prompts the player to check their DMs for the confirmation message — so they have an immediate, end-to-end signal that the round-trip worked. Existing inline-error rendering already surfaces the bot's specific error string (DMs disabled, ID not found, etc.) without further changes.\n\n• **No effect on already-linked players.** The modal still only mounts for users whose `nicknames.discord_id` is empty, and the existing 17–19 digit format check still fires before any expensive REST call so garbage input is rejected for free."
  },
  {
    "version": "5.99",
    "title": "One-click \"Connect with Discord\" replaces the paste-your-ID step",
    "published_at": "2026-05-06",
    "content": "Follow-up to v5.94/v5.98 (the first-login Discord linking modal). Players still had to enable Developer Mode in Discord, right-click their name, and paste an 18-digit snowflake — a multi-step process most users have never done. v5.99 adds a proper Discord OAuth round-trip so the whole thing collapses to a single click; the manual paste flow stays as a fallback.\n\n• **New `GET /auth/discord` + `GET /auth/discord/callback` routes in `src/web/server.js`.** The start route is signed-in-only, generates a per-session CSRF state token, stashes the chosen return target (`home` for the modal, `settings` for `/settings/profile`), and redirects to Discord's OAuth2 authorize endpoint with `scope=identify`. The callback exchanges the code at `/api/oauth2/token`, fetches the verified user from `/users/@me`, then runs the **same** `bot.verifyAndConfirmDiscordId()` + `db.linkOwnDiscordId()` path as the manual `POST /api/me/link-discord` — so the DM round-trip + 409 already-linked guarantees from v5.98 still apply. State is single-use (cleared on entry), bad/expired states return `?reason=bad_state`, cancellations return `?reason=cancelled`, and every error path bounces back to the originating page with a specific reason code.\n\n• **`/api/auth/me` exposes `discord_oauth_enabled: boolean`.** True only when both `DISCORD_CLIENT_ID` and `DISCORD_CLIENT_SECRET` are set in env. The frontend uses this flag to render the Connect button only when OAuth is actually configured; with no env vars the site silently falls back to the existing manual-paste UX.\n\n• **`<DiscordLinkModal />` now leads with a *Connect with Discord* button.** Indigo `#5865F2` Discord-brand button at the top, `or paste your User ID` divider, then the existing 17–19 digit input + instructions as a fallback. A new shared `oauthErrorMessage(code)` helper (exported from the modal) maps the callback's `?reason=...` codes (`cancelled`, `dm_blocked`, `not_found`, `already_linked_other`, `bad_state`, `token_exchange`, …) to friendly inline copy. On successful return the modal reads `?discord_link=success&username=...`, shows the *Discord linked to @username — check your DMs* toast (clearing the skip counter), strips the params via `history.replaceState`, and triggers `refreshMe()`.\n\n• **`DiscordLinkSection` on `/settings/profile` mirrors the same flow.** A *Connect with Discord* / *Reconnect with Discord* button (label flips based on whether a link already exists) sits above the manual ID input. URL params returned by the callback (`?discord_link=...`) are surfaced inline as the section's existing success/error message slots and stripped after read."
  },
  {
    "version": "6.00",
    "title": "Community edition admin panel gets the same superuser auto-recovery",
    "published_at": "2026-05-06",
    "content": "Follow-up to v5.96, which added the global superuser session auto-recovery wrapper to the **full edition** so admins no longer had to manually log out and back in when their session silently expired mid-action. The community edition (`community-edition/`) carries the same code mirrored under its own tree and exhibited the exact same dropped-session UX bug — admins would see a bare *Invalid superuser key* / 403 toast on Sync Ranks, replay re-parse, DB backup, etc. and had to bounce through 🛡️ Superuser to recover. v6.00 brings the community edition to parity.\n\n• **`community-edition/web/src/api.js`** — added the same `superuserFetch()` wrapper at the top of the file (intercepts 401, plus 403 carrying the `session` sentinel header, asks the SuperuserContext to re-prompt, retries on success, gracefully fails on dismissal). Migrated all 22 superuser-key gated functions to use it: `getFeatureFlags`, `getAdminFeatureFlags`, `setFeatureFlag`, `launchSeason10`, `triggerMissingDMs`, `createPatchNote` / `updatePatchNote` / `deletePatchNote`, `setNickname`, `setPlayerDiscordId`, `deleteSeasonApi`, `getStoredReplays`, `extendReplayExpiry`, `createTournament` + 5 other tournament admin endpoints, `triggerRankSync`, `setManualRank`, `clearPlayerRank`. Upload-key (`X-Upload-Key`) calls remain raw `fetch` — same scoping decision as the full edition.\n\n• **`community-edition/web/src/context/SuperuserContext.jsx`** — added the `requestReauth()` Promise + `useRef` resolver, `setShowModal` wrapper that resolves dismissals as `false`, and the `useEffect` that registers/unregisters the api-wrapper hook via `setSuperuserReauthHandler()`. Concurrent in-flight admin calls all await the same pending re-auth so a single login unblocks every queued request.\n\n• **`community-edition/src/web/server.js` `requireSuperuser` 401-vs-403 split.** Returns **401** when no header or the browser `session` sentinel is presented without a valid cookie (the frontend wrapper auto-recovers); returns **403** only when a script presents an explicit wrong literal credential. Existing valid-key flows are unchanged.\n\n• **`community-edition/web/src/pages/AdminPanel.jsx` (16 inline `fetch()` sites)** rewritten to `superuserFetch`: DB backup list/create/restore/delete + nickname-id fix, replay download/reparse/reparse-all/status/set-all-permanent, error-log read+clear, Steam status + lobby callApi + friends-add-all, unregistered-players, overview, settings read+write, v3-preview, recalculate-ratings, duplicate-matches, replay-inspect upload.\n\n• **`community-edition/web/src/pages/StatsEditor.jsx` (3 mutation calls)** — `apiUpdateMatchDetails`, `apiUpdatePlayerStats`, `apiUpdateMatchDraft` — all migrated to `superuserFetch`.\n\n• **Build verified.** `cd community-edition/web && npm run build` completes cleanly (AdminPanel chunk: 54.03 kB / 14.02 kB gzipped).",
    "author": "System"
  },
  {
    "version": "6.01",
    "title": "Players can re-link a different Discord account from Settings",
    "published_at": "2026-05-06",
    "content": "Follow-up to v5.94/v5.98 (the first-login Discord linking modal + verify-and-DM gate). v5.98 deliberately made `POST /api/me/link-discord` return **409** when the caller already had a *different* Discord ID linked — to stop a stale first-login modal from silently overwriting an existing link. But the same endpoint backs the *Discord link* field on `/settings/profile`, so a player who legitimately needed to swap accounts (lost access to the old one, made a new Discord) was blocked from updating themselves and had to bug an admin to run the superuser `setDiscordId`. v6.01 splits the path so re-linking works without touching the modal's safety guarantee.\n\n• **New `PUT /api/me/link-discord` route in `src/web/server.js`.** Self-service *re-link* path: signed-in only, validates the same 17–19 digit snowflake, runs the **exact same** `bot.verifyAndConfirmDiscordId()` round-trip the POST does (the bot must be able to fetch the new Discord user *and* DM them) and only then calls `db.linkOwnDiscordId()` to overwrite the existing `nicknames.discord_id` in place. The DB write is a single `UPDATE` so the previous ID is replaced atomically — no orphaned half-linked state. Re-saving the same ID short-circuits to a no-op so users aren't spammed with confirmation DMs every time they hit Save.\n\n• **`POST /api/me/link-discord` is unchanged.** Still 409s when an existing different ID is on file, so `<DiscordLinkModal />` (the first-login modal) can never silently overwrite an existing link on a stale page.\n\n• **`web/src/pages/SettingsProfile.jsx` chooses the right method.** When the field already has a saved Discord ID (`initial` truthy), the Save button now sends `PUT`; for a brand-new link it still sends `POST` so first-time signups go through the canonical onboarding path. The success toast distinguishes *Discord ID saved* from *Discord account updated*, both prompting the user to check their DMs for the confirmation message.\n\n• **OAuth *Reconnect with Discord* button supports re-link too.** `GET /auth/discord/callback` previously bounced back with `?reason=already_linked_other` when an existing different ID was on file. When the round-trip was started from `/settings/profile` (`return=settings`, the *Reconnect* button) the callback now allows replacing the existing ID — same verify-and-DM gate still applies. The home/modal entry point (`return=home`) is unchanged and still 409s.",
    "author": "System"
  },
  {
    "version": "6.02",
    "title": "Stop a second account from claiming a Discord ID that's already linked",
    "published_at": "2026-05-06",
    "content": "Follow-up to v5.94/v5.98/v6.01 (the first-login Discord linking modal + verify-and-DM gate + self-service re-link path). Those changes blocked the *same* account from silently overwriting its own existing link, but did **not** stop a *different* Steam account from binding to a Discord ID that another player had already saved. Two `nicknames` rows could end up with identical `discord_id` values, making `getSteamByDiscordId()` return whichever row sorted first — and letting an attacker who knew a victim's Discord ID hijack DM-driven flows like MVP voting and post-match attitude rating.\n\n• **New `db.findAccountIdsByDiscordId(discordId)` helper** in `src/db/index.js` — returns every `account_id` currently bound to the given Discord ID across both the canonical `nicknames` table and the legacy `players` table (populated by `!register`), so an old `!register`-only link still blocks a hijack attempt via the modal/OAuth.\n\n• **`POST /api/me/link-discord`, `PUT /api/me/link-discord`, and `GET /auth/discord/callback`** now all run a cross-account collision check before persisting. If a *different* `account_id` already owns the requested Discord ID the route returns **409** with `code: 'discord_id_taken'` (the OAuth callback bounces back with `?reason=discord_id_taken`) and `nicknames.discord_id` is left untouched. Same-account re-saves still short-circuit to the existing no-op, so the UX for legitimate users is unchanged.\n\n• **Partial unique index `idx_nicknames_discord_id_unique`** added to `nicknames(TRIM(discord_id)) WHERE discord_id IS NOT NULL AND TRIM(discord_id) <> ''` (mirrored to community edition). Closes the application-layer race where two simultaneous link calls could both pass the precheck and then both write the same Discord ID — Postgres now rejects the second write outright. Empty / NULL discord_id values (the *not yet linked* state) remain allowed for many accounts.\n\n• **Existing duplicates surfaced to admins, never silently truncated.** If pre-existing duplicates exist when the index is created, the migration logs the offending Discord ID and the list of colliding `account_id`s to PM2 stdout (`[nicknames] Cannot create unique index on discord_id — N duplicate(s) need admin attention`) and continues startup. Admins can then reconcile by hand using the existing superuser `setDiscordId` tool rather than letting the bot pick a winner at random.\n\n• **`<DiscordLinkModal />` shows a friendly inline error** for the new `discord_id_taken` reason code via the shared `oauthErrorMessage()` helper (also used by `/settings/profile`).",
    "author": "System"
  },
  {
    "version": "6.03",
    "title": "Auto-running /inhouse lobby + captain-mode vote + church-bell chime",
    "published_at": "2026-05-06",
    "content": "End-to-end rewrite of the `/inhouse` flow so the lobby runs itself instead of waiting on an admin to push every button. Closes the long-standing gap where new players hit the page, saw an empty *No active inhouse session* card, and bounced.\n\n• **Auto-create on first join.** New `POST /inhouse/join` endpoint (no session id required). Any signed-in player clicking *Sign In to Inhouse* either joins the existing open lobby or, if none exists, atomically creates one with the standard defaults (60s accept window / 10 min players / 30s grace timer). Concurrent first-joiners are serialised by a Postgres advisory transaction lock inside the new `db.getOrCreateOpenInhouseSession()` helper, so two players hitting the button at the same instant share a single session instead of racing two into existence.\n\n• **Auto-start ticker drives every status flip.** Once the lobby reaches `min_players`, the existing 30s grace timer fires and `autoStartTicker` flips `open → accepting` *and* materialises the captain-mode vote winner onto `inhouse_sessions.captain_mode` in the same status-guarded UPDATE. From there, captains and the draft progress unattended; admin presence is no longer required for a normal game.\n\n• **Captain-mode vote.** New per-player poll on the lobby card while status is `open`. Players cast one vote between **Highest Rank**, **Random**, **Auto-balance**, and **Volunteer** (re-clicking the same chip clears it; votes live in the new `inhouse_sessions.captain_mode_votes JSONB` column). Tally + winner are pure functions shared between the API and the ticker, so the chip counts you see are exactly the mode that gets used. Ties or zero votes default to **Highest Rank** so the lobby never stalls. Auto-balance and Volunteer fall back to the Highest Rank pairing logic for now (dedicated balancing / volunteer-signup flows remain on the roadmap).\n\n• **Medieval church-bell chime.** Replaced the high-pitched alert ping with a longer, lower bell synthesised live in-browser via the Web Audio API — six inharmonic sine partials (sub-octave hum + fundamental + minor-third tierce + perfect-fifth quint + octave nominal + upper inharmonic) with a sharp 5ms strike attack and a ~2.5s exponential ring-out, giving the unmistakable bronze-bell timbre without shipping a binary asset. Mute toggle still silences only the chime — browser notifications fire either way.\n\n• **Admin overrides collapsed into `<details>`.** All admin-only toolbars on `/inhouse` (open-session form on the empty state, status-flip / captain-pick / provision / cancel / bot / auto-draft buttons on the active session card) now live behind a single `⚙ ADMIN OVERRIDES` toggle. The default view is fully player-facing; overrides are still one click away for stuck states.",
    "author": "System"
  },
  {
    "version": "6.04",
    "title": "Discord OAuth now auto-joins new users to the OCE Inhouse server (and assigns the League Member role)",
    "published_at": "2026-05-06",
    "content": "Closes the last gap in the one-click Discord onboarding flow shipped in v5.94 / v5.98 / v6.01 / v6.02. The OAuth round-trip already verified the Discord account, DMed the user, and saved the Steam ↔ Discord binding — but it stopped short of pulling brand-new users into the OCE Inhouse server itself. New players still had to find an invite link, join manually, and wait for an admin to assign a role before the bot could fully function for them. v6.04 closes that gap.\n\n• **`/auth/discord` now requests `identify guilds.join`** (was `identify` only). The added scope is what permits a server to call `PUT /guilds/{guild.id}/members/{user.id}` on the user's behalf using the OAuth access token — without it, Discord refuses with `Missing OAuth2 scope`. Existing users who already linked under the old `identify`-only grant will be re-prompted for consent the next time they hit the OAuth flow (e.g. via *Reconnect with Discord* in settings) so the upgraded scope can be granted.\n\n• **New `bot.addUserToLeagueGuild(discordId, accessToken)` helper in `src/discord/bot.js`.** Wraps the Discord REST `PUT /guilds/{guild.id}/members/{user.id}` endpoint, authenticated with the bot token (`Authorization: Bot ${DISCORD_TOKEN}`) and the user's OAuth access token in the JSON body. If `DISCORD_LEAGUE_MEMBER_ROLE_ID` is configured the role is attached on join (`roles: [roleId]` in the body); for users who are *already* in the guild — Discord returns 204 with no body in that case, and ignores the `roles` array — the helper falls back to `PUT /guilds/.../members/.../roles/{role.id}` so the league role still gets topped up.\n\n• **Failures degrade gracefully.** Every failure path returns a `{ ok: false, code, error }` object instead of throwing — missing config (`guild_not_configured`, `bot_token_missing`), bad inputs (`bad_discord_id`, `no_access_token`), network errors (`network`), and any non-201/204 from Discord (`http_403` for missing perms, `http_429` for rate limits, etc.) are all logged with enough context (`[Discord] guild auto-join failed for ...: 403 ...`) and swallowed. The OAuth callback only `console.warn`s the failure — the Steam ↔ Discord link itself has already been saved and the verify-DM has already been sent, so the user is in a fully usable state regardless. Worst case they're linked but not in the server, exactly the v5.94 status quo.\n\n• **Required setup.** Set two new env vars on the prod host: `DISCORD_GUILD_ID` (the OCE Inhouse server snowflake) and optionally `DISCORD_LEAGUE_MEMBER_ROLE_ID` (the role to attach at join — if unset, users still get auto-joined but with no role). The bot account also needs the **Create Instant Invite** permission in the target guild (required by Discord for `guilds.join`-backed adds), plus **Manage Roles** if a role is being assigned, and the role being assigned must sit *below* the bot's highest role in the role list. If `DISCORD_GUILD_ID` is left unset, the call short-circuits with `guild_not_configured` and the link flow behaves exactly like it did in v6.02 — safe to deploy before the secrets are populated.",
    "author": "System"
  },
  {
    "version": "6.05",
    "title": "Players can fully unlink their Discord account from Settings",
    "published_at": "2026-05-06",
    "content": "Follow-up to v5.94 / v5.98 / v6.01 (the first-login modal + verify-and-DM gate + self-service re-link). Those flows let a player *create* or *replace* their Discord link, but there was still no self-service way to fully unlink — a player who quit the league or lost permanent access to their Discord account had to ask an admin to run the superuser `setDiscordId` with an empty value just to stop the bot DMing / mentioning / role-assigning that account. v6.05 closes the gap.\n\n• **New `DELETE /api/me/link-discord` route in `src/web/server.js`.** Signed-in only. Calls a new `db.unlinkOwnDiscordId(accountId)` helper that runs guarded `UPDATE … SET discord_id = ''` against **both** `nicknames` (canonical, populated by the first-login modal / OAuth / settings) **and** `players` (legacy, populated by the old `!register` command). Both writes are required because `getDiscordIdByAccountId()` falls back to the `players` table when `nicknames` is empty — a `nicknames`-only clear would leave a legacy `!register`-bound account still appearing linked, leaving `needs_discord_link` false and preventing the first-login modal / Connect-with-Discord button from re-appearing. The empty-string sentinel matches the rest of the codebase (every read filters on `discord_id != ''`). Idempotent: re-DELETE on an already-unlinked account is a clean 200 with `{ cleared: false }`.\n\n• **New *Unlink Discord* button on `/settings/profile`.** Renders inline next to *Save Discord ID* in the existing `DiscordLinkSection`, but only when an ID is currently saved (the button has nothing to do otherwise). Outlined red (`#ef4444`) so it reads as destructive without competing with the primary Save action. Clicking opens a `window.confirm()` that explicitly spells out the consequences — *the bot will stop DMing you, mentioning you, and assigning league roles* — before any request is sent, so an accidental click can't silently break notifications. On success the local input clears, a green confirmation toast surfaces for 4s, and `refreshMe()` re-pulls `/api/auth/me` so the *Connect with Discord* button in the same section flips back to its first-link label and the global `<DiscordLinkModal />` becomes mountable again the next time the player loads a page (it watches `steamUser.needs_discord_link`).\n\n• **No effect on the cross-account collision guards.** The partial unique index `idx_nicknames_discord_id_unique` from v6.02 is built on `WHERE discord_id IS NOT NULL AND TRIM(discord_id) <> ''`, so unlink (which writes the empty-string sentinel) leaves the index in a clean state and immediately frees that Discord ID for re-binding by *any* account — including a fresh signup who legitimately took over the same Discord identity.",
    "author": "System"
  },
  {
    "version": "6.06",
    "title": "Fixed Steam sign-in trust footnote overflowing the nav row",
    "published_at": "2026-05-06",
    "content": "The compact *Sign in with Steam* button in the global nav had an absolutely-positioned trust footnote (`Password stays with Valve · Why is this safe?`) anchored to `top: 100%`, which rendered outside the button's own row and bled down into the next nav row — manifesting on the live site as a stray padlock icon and *Locked-down…* text peeking out next to the button, and a broken-looking hit area.\n\n• **Replaced the absolute footnote with an inline trust badge** in `web/src/App.jsx`. The Steam pill and a small circular `?` button now sit side-by-side in a single `inline-flex` row — nothing escapes the button's own line box, so there's no possible overflow into siblings or the row below regardless of viewport width. The descriptive *Password stays with Valve* copy moved to the badge's `title` tooltip, with the full multi-bullet explanation still one click away in the existing `SteamTrustModal`.\n\n• **`WhyIsThisSafeLink` in `web/src/components/SteamTrustModal.jsx` now accepts `label` / `ariaLabel` / `title` props** so the same component can render either as the original underlined *Why is this safe?* text link (used by `SteamSignInPanel` on the Inhouse gate / buy-in pages — unchanged) or as the new compact `?` badge in the nav. Defaults preserve the previous behaviour, so no other caller is affected.\n\n• **Modal behaviour is unchanged** — clicking the badge still opens `SteamTrustModal` centered with the full OpenID 2.0 walkthrough, Escape / outside-click still close it, and the body-scroll lock still applies. Mobile and narrow viewports now render the button as a tidy two-element pill with no clipping.",
    "author": "System"
  },
  {
    "version": "6.07",
    "title": "Profile editor live preview no longer covers the form",
    "published_at": "2026-05-06",
    "content": "On `/settings/profile` the *Live preview* card was `position: absolute; top: 120px; right: 24px` inside the form column, which floated it on top of the form at desktop widths and visually hid the Custom Title radios and Discord-link / Bio inputs at common viewport sizes (1280–1440px in particular).\n\n• **`.settings-profile-grid` is now a real two-column CSS grid at ≥1240px** (`grid-template-columns: minmax(0,1fr) 320px` with a 24px column gap). The form lives in column 1 (capped at `max-width: 720px`, justified to the gap so it stays visually anchored to the preview) and the live preview lives in column 2 — they each get their own track and can never overlap regardless of viewport width.\n\n• **Preview is now `position: sticky; top: 96px`** within its own column, so it still follows the page as the user scrolls through Bio / Custom title / Theme / Pinned hero / Pinned match / Frame / Extras, but it can no longer escape its column and clip into the form.\n\n• **Below 1240px the layout collapses to a single column** with the preview rendered inline above the form (the existing mobile / narrow-tablet behaviour), and the floating box-shadow / sticky / overflow rules are scoped to the desktop media query so the inline rendering stays clean.\n\nFooter bump v6.06 → v6.07.",
    "author": "System"
  },
  {
    "version": "6.08",
    "title": "Signed-in home now shows the standard landing under your personalized dashboard",
    "published_at": "2026-05-06",
    "content": "Signing in with Steam used to *replace* the public home page with a slimmed-down personalized view (LiveInhousePulse + PlayerOfTheWeek + HotHeroes + PersonalisedDashboard + a tiny four-stat *Community stats* card). The full `CourtPitchHomeLanding` block — totals chart, recent matches list, top-5 leaderboard preview — disappeared entirely the moment you logged in, which meant frequent visitors had to navigate away from `/` to see the same shared community context that logged-out visitors got for free.\n\n• **`CourtPitchHomeLanding` now renders for signed-in users too**, immediately below `PersonalisedDashboard` in `web/src/pages/Home.jsx`. The personalized dashboard still leads (so your own stats / streaks are still the first thing you see when logged in), but the totals chart, recent matches feed, and top-5 leaderboard preview are now reachable without leaving the home page.\n\n• **The redundant four-stat *Community stats* `StatCard` row has been removed** from the signed-in branch — the full landing block already surfaces total matches, players, weekly volume and most-played hero in a richer form, so keeping the mini row would have been pure duplication.\n\n• **The active weekend-tournament banner now also renders for signed-in users** (previously logged-out only), extracted into a shared `tournamentBanner` const so both branches render the same markup. Signed-in players are the ones most likely to actually enter the tournament, so hiding it from them was backwards.\n\n• **`getLeaderboard` is no longer gated on `!steamUser`** — the top-5 fetch now runs for both signed-in and logged-out users so the embedded leaderboard preview has data to render in either branch. The signed-in container also widened from `maxWidth: 1000` → `1200` to match the logged-out layout now that it hosts the same landing block.",
    "author": "System"
  },
  {
    "version": "6.09",
    "title": "Stripe misconfiguration is no longer silent — startup log + admin banner",
    "published_at": "2026-05-06",
    "content": "**Closing the loop on Task #113.** The coaching marketplace's *Continue with Stripe* CTA returns the generic error *Payments are not configured* whenever `STRIPE_SECRET_KEY` is unset. That secret is currently missing on the OCE Inhouse prod host even though the marketplace launched in v5.93, so every eligible player who clicked through hit the 503 — and the operator had no way to notice except via user reports.\n\n**1. Boot-time diagnostic.** `src/index.js` now reads the `coaching_marketplace` feature flag at startup (right before the web server binds) and logs one of three lines so the configuration state is obvious in `pm2 logs oi-bot`:\n- `[Stripe] STRIPE_SECRET_KEY present — payments enabled.` when configured.\n- `[Stripe] coaching marketplace ON but STRIPE_SECRET_KEY missing — payouts will 503` (printed via `console.error` so it shows red in PM2) when the flag is on or in preview but the secret is missing — i.e. the exact misconfiguration that caused the user report.\n- `[Stripe] STRIPE_SECRET_KEY missing — payments disabled (coaching marketplace flag is off).` informational case.\n\n**2. New superuser route.** `GET /api/admin/stripe-status` returns `{ configured, webhook_configured, coaching_marketplace_state }`. Gated by the existing `requireSuperuser` middleware so the secret state never leaks to non-admins, and never echoes the key itself — only its presence.\n\n**3. Admin Site Settings banner.** A new `StripeStatusBanner` component renders at the top of the **Site Settings** tab in `/admin`. When the secret is missing AND the marketplace flag is on/preview it shows a red *⚠️ Stripe not configured* alert that names the file (`~/Dota-Stats-Full/.env`) and explains the user-facing impact. When the secret is missing but the flag is off it shows a quieter amber notice. When the secret is set it shows a one-line green confirmation including whether the webhook secret is also set.\n\n**Operator action required (out of scope for this patch).** `STRIPE_SECRET_KEY` (and `STRIPE_WEBHOOK_SECRET` if webhooks are wired up) must be added to `~/Dota-Stats-Full/.env` and PM2 restarted (`bash ~/Dota-Stats-Full/deploy.sh`) to actually enable coaching checkouts on `oceinhouse.gg`. The error message itself is unchanged — only its visibility to the operator is improved.",
    "author": "System"
  },
  {
    "version": "6.10",
    "title": "One-click admin tool for reconciling Discord IDs shared by multiple accounts",
    "published_at": "2026-05-06",
    "content": "**Closing the loop on Task #114.** Task #103 added a partial unique index on `nicknames.discord_id` and started logging pre-existing duplicates to PM2 at startup, but reconciling them still required an admin to read the log line, look up each `account_id` by hand, and run the superuser `setDiscordId` once per loser. There was no in-product surface for the migration, so duplicates could sit unresolved indefinitely (and the unique index couldn't be created until they were).\n\n**1. New superuser endpoints.** Three additions in `src/web/server.js` (gated by the existing `requireSuperuser` middleware):\n- `GET /api/admin/discord-id-collisions` returns every Discord ID currently bound to >1 account, with each candidate's nickname, last-seen match (id + start_time from `matches`), MMR / games_played from `ratings`, and which table the link lives in (canonical `nicknames` vs legacy `players`). Also returns the current state of the partial unique index.\n- `POST /api/admin/discord-id-collisions/resolve` body `{ discord_id, keep_account_id }` clears the loser rows in BOTH `nicknames` and the legacy `players` table inside a single transaction. If the keeper is a legacy-only candidate (Discord ID lives only on `players`), the route also writes the link into `nicknames` so the canonical lookup keeps working.\n- `POST /api/admin/discord-id-collisions/enforce-index` re-attempts the `CREATE UNIQUE INDEX IF NOT EXISTS idx_nicknames_discord_id_unique` from Task #103 on demand, so once the listing is empty the operator can flip the constraint on without a redeploy.\n\n**2. New DB helpers** in `src/db/index.js`: `getDiscordIdCollisions`, `resolveDiscordIdCollision`, `tryEnforceDiscordIdUniqueIndex`. The collision query unions `nicknames` and `players` on trimmed `discord_id` so legacy-only duplicates are surfaced too.\n\n**3. Admin UI.** A new **🔗 Discord ID Collisions** section under the **Users** tab of `/admin` (component `DiscordIdCollisions` in `web/src/pages/AdminPanel.jsx`). Each colliding Discord ID renders as its own card with a small table of candidates — nickname (linked to `/player/:id`), account ID, source table, last-match date (linked to `/match/:id`), MMR, games played — plus a green *✓ Keep this one* button per row. A status pill at the top shows whether the partial unique index is currently enforced; once the listing is empty an *🔒 Enforce Unique Index Now* button appears. Wired into the searchable admin index with the keywords `discord duplicate merge split collision unique link reconcile`.\n\nFooter bump v6.09 → v6.10.",
    "author": "System"
  },
  {
    "version": "6.11",
    "title": "Discord auto-join failures now ping an admin channel instead of dying silently in PM2",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #116.** The `bot.addUserToLeagueGuild` helper added in v6.04 (called from `/auth/discord/callback` for every new Discord-linked signup) returned a structured `{ ok: false, code, error }` on failure but only `console.warn`'d it. If the bot lost **Create Instant Invite** / **Manage Roles** in the OCE Inhouse server, or the league role got re-ordered above the bot, every new signup would silently fail to join — and nobody would notice until a player complained.\n\n• **New `DISCORD_ADMIN_LOG_CHANNEL_ID` env var.** When set, any non-OK result from `addUserToLeagueGuild` now posts a one-line alert into that channel (`⚠️ Discord auto-join failed for user <id> — code <code> — <error> · <hint>`). Includes the failing Discord ID, the failure code (`http_403`, `http_429`, `guild_not_configured`, `bot_token_missing`, `network`, …) and a hint at the likely cause (e.g. `http_403` → check **Create Instant Invite** / **Manage Roles** + role hierarchy; `guild_not_configured` → set `DISCORD_GUILD_ID`).\n\n• **Throttled to 1 alert per error code per 10 min** via an in-memory `Map` on the bot instance, so a stuck mis-config (e.g. role permanently sitting above the bot) can't spam the admin channel — the operator gets one ping every ten minutes per distinct failure mode until they fix it. Input-shape errors (`bad_discord_id`, `no_access_token`) are intentionally *not* alerted because they aren't actionable for ops.\n\n• **Send is best-effort.** Cache lookup first, then a `channels.fetch` fallback for cold caches; any send/fetch error is swallowed with a `console.warn` so the alerting layer can't itself take down the OAuth callback. If `DISCORD_ADMIN_LOG_CHANNEL_ID` is unset, the helper is a no-op — safe to deploy before the secret is populated.",
    "author": "System"
  },
  {
    "version": "6.12",
    "title": "Auto-balance captain mode now actually balances teams by skill",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #118.** v6.03 added an *Auto-balance* option to the inhouse captain-mode poll, but the underlying logic in `src/web/server.js` `/inhouse/:id/select-captains` had a placeholder that fell back to the **Highest Rank** path (per task #115's stated scope: *if it's not implemented, treat it as Highest Rank for now*). Players who voted for Auto-balance got the same two highest-MMR captains Highest Rank already produced — no actual balancing was happening.\n\n• **Real skill-based 5v5 balancing.** When the accept phase ends with exactly 10 accepted players, the route now scores every player using the same hybrid TS/Dota signal Highest Rank uses (in-house TrueSkill MMR for ≥20 games, otherwise Dota leaderboard rank → rank tier → penalised TS fallback) and **enumerates all C(10,5) = 252 possible 5v5 partitions**, picking the split with the smallest `|sumTeam1 − sumTeam2|`. Ties are broken randomly so repeated calls on identical lobbies don't always pick the same arrangement. The captain on each side is the highest scorer of that team, and **the remaining 8 players are auto-assigned to their teams and marked `drafted` immediately** — so the lobby goes straight from *captains chosen* to a fully populated, balanced match without running the captain draft at all.\n\n• **Sub-10 fallback.** When fewer than 10 players are accepted (lobby ran short, manual override, etc.) Auto-balance falls back to picking the captain pair whose scores are *closest* to each other (smallest delta among adjacent pairs in the score-sorted accepted list), so the captain draft that follows starts from an already-balanced top of the pool. The higher-scored player gets team 1 to keep team labels deterministic.\n\n• **Volunteer mode is unchanged** — it's still listed in the poll but the dedicated self-nominate signup UX isn't built yet, so it continues to fall through to the Highest Rank path with the existing *(currently uses Highest Rank)* hint.\n\n• **Frontend hint copy updated.** `web/src/pages/Inhouse.jsx` captain-mode poll now describes Auto-balance as *Skill-based 5v5 split with smallest projected MMR delta* instead of *(currently uses Highest Rank)*. The auto-start ticker (`src/inhouse/autoStartTicker.js`) needed no changes — it already POSTs `/select-captains` with the resolved captain mode, so the new logic kicks in automatically the moment the accept-phase timer expires on an Auto-balance-winning vote.",
    "author": "System"
  },
  {
    "version": "6.13",
    "title": "Volunteer captain mode actually lets players self-nominate",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #119.** v6.03 added a *Volunteer* option to the inhouse captain-mode poll, but the underlying `/inhouse/:id/select-captains` route had a placeholder branch that fell straight back to **Highest Rank** because the volunteer-signup flow didn't exist yet. Lobbies that voted Volunteer got the same two highest-MMR captains Highest Rank already produced — players had no way to actually opt in.\n\n• **New per-session volunteer pool.** Added a `captain_volunteers JSONB` column on `inhouse_sessions` (map of `{ \"<accountId>\": true }`) plus DB helpers `setCaptainVolunteer`, `getCaptainVolunteers`, and a pure-function `listVolunteerAccountIds(volunteersObj, validAccountIdSet)` filter so the route can drop ghost signups from players that have left or declined.\n\n• **Two new API routes.** `POST /inhouse/:id/captain-volunteer` body `{ volunteer: true|false }` lets an accepted player toggle their signup (gated to `open`/`accepting` status, must be in the lobby, must have status `accepted` to *opt in* — opt-out always allowed; admins can toggle their own at any time so demo/seed flows still work). `GET /inhouse/:id/captain-volunteers` returns `{ volunteers, count, myVolunteer }` for the live tally. Both routes filter to current lobby members so a leaver can't keep skewing the count.\n\n• **Real captain selection.** The volunteer branch in `/inhouse/:id/select-captains` now reads the volunteer pool, intersects it with currently-accepted players, and: 0–1 volunteers → falls back to Highest Rank (hybrid TS/Dota score, same as the existing path) so the lobby still progresses; exactly 2 → those two are the captains (higher score → team 1 for deterministic team labels); 3+ → picks two at random from the pool. The session's `captain_mode` stays `'volunteer'` either way so the UI/audit reflects the lobby's intent even on fallback.\n\n• **Defence-in-depth cleanup.** `leaveInhouseSession` and `setInhousePlayerDeclined` now also drop the player's volunteer entry alongside their captain-mode vote, so a self-nominate-then-leave (or accept → volunteer → decline) can't leave a stale signup that would later be picked.\n\n• **Frontend toggle in accept-phase panel.** `web/src/pages/Inhouse.jsx` shows a brass *🙋 Captain me* button (or *✓ Volunteering — click to withdraw* when active) inside the existing accept/decline card whenever the resolved captain mode is `volunteer` and the lobby is in the accept phase. A live tally next to the button explains exactly what will happen — *N volunteers so far · need M more or it falls back to Highest Rank* / *these two will be captains* / *two will be picked at random from the pool* — refreshed every 4s alongside the rest of the session. The captain-mode poll's Volunteer hint copy is updated from *(currently uses Highest Rank)* to *Self-nominate during accept phase — falls back to Highest Rank if <2 volunteer*.\n\n• **Auto-start ticker untouched.** The ticker still resolves the captain-mode vote and POSTs `/select-captains`; the new logic kicks in automatically the moment the accept-phase timer expires on a Volunteer-winning vote.",
    "author": "System"
  },
  {
    "version": "6.14",
    "title": "Admins can now see Discord auto-join health at a glance",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #127.** v6.11 wired up Discord channel alerts when `bot.addUserToLeagueGuild` fails, but only ops who watch that channel would see them. There was no way to confirm from the admin panel whether new signups were actually landing in the OCE Inhouse Discord server — the only signal was a (potentially missed) channel ping or a player complaint.\n\n• **New in-memory ring buffer in `src/discord/bot.js`.** Every call to `addUserToLeagueGuild` now records its outcome (timestamp, result code, ok/failed, target Discord ID, error blurb) into a 50-entry ring buffer on the bot instance. Successes are categorised as `success_added` (newly joined) vs `success_already` (PUT returned 204 — already a member); failures keep their existing codes (`http_403`, `http_429`, `network`, `guild_not_configured`, etc.). The buffer resets on bot restart and is intentionally lossy — no DB table for what is fundamentally a debugging affordance.\n\n• **New `getGuildAutoJoinStats()` rollup.** Returns counts grouped by code over the last 24 hours, the most recent failure (with a remediation hint pulled from the same map used by the alert channel), and the most recent success timestamp so the dashboard can distinguish *broken right now* from *transient hiccup that already recovered*.\n\n• **New superuser route `GET /api/admin/discord-autojoin-status`.** Returns the rollup plus which env vars are configured (`DISCORD_GUILD_ID`, `DISCORD_TOKEN`, `DISCORD_LEAGUE_MEMBER_ROLE_ID`, `DISCORD_ADMIN_LOG_CHANNEL_ID`) so the panel can flag a missing alert channel as a separate concern from the bot itself being unhealthy.\n\n• **New *Discord Auto-Join Health* panel on the admin Site Settings tab.** Renders a green/amber/red status pill (red when env is unconfigured or the latest event is a failure with no later success; amber when there are failures but a more recent success suggests the integration recovered, or when there are no signups at all in the buffer; green otherwise), per-code stat tiles for the most actionable failure modes (403 perms, 404 not-found, 429 rate-limited, 401 token, network, other), and the last failure's code + timestamp + remediation hint inline. A footer line confirms which env vars are set so you can tell at a glance whether the alert channel itself is wired up. Manual refresh button on the right.",
    "author": "System"
  },
  {
    "version": "6.15",
    "title": "Players who failed to auto-join Discord can now retry once the bot is fixed",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #128.** When `bot.addUserToLeagueGuild` failed mid-OAuth (e.g. the bot lost **Manage Roles** for a few hours, Discord 5xx'd, or the league role got re-ordered above the bot), the player finished the round-trip with their Steam↔Discord link saved but were *not* in the OCE Inhouse server. Once an admin fixed the underlying issue those users stayed locked out forever — the OAuth `access_token` is single-use and we don't store it, so we couldn't retry transparently.\n\n• **New `discord_autojoin_failures` table** in `src/db/index.js` (keyed by `discord_id`, with `account_id`, `last_code`, `last_error`, `attempts`, `first_failed_at`, `last_failed_at`). UPSERT on every repeat failure increments `attempts` so admins can see who's stuck. Cleared automatically on the next successful auto-join. Three new helpers: `recordDiscordAutoJoinFailure`, `clearDiscordAutoJoinFailure`, `getDiscordAutoJoinFailureForAccount`.\n\n• **OAuth callback wiring** in `/auth/discord/callback`: every `joinResult.ok === false` (except input-shape codes `bad_discord_id` / `no_access_token` that a re-link can't fix on its own) writes a row; every `ok === true` clears it. The defensive `catch` for the helper unexpectedly throwing also queues a row so the banner still appears.\n\n• **Best-effort DM nudge** via new `bot.dmDiscordAutoJoinRetryHint(discordId)` in `src/discord/bot.js` — fires (without await) on a queued failure so the player gets a heads-up before their next site visit. All errors swallowed so a flaky DM can never take down the auth flow.\n\n• **New `GET /api/me/discord-autojoin-status`** returns `{ pending, code, attempts, first_failed_at, last_failed_at }` for the signed-in caller; always 200 (returns `{ pending: false }` for unauthed callers / no row / DB error) so the frontend doesn't need to special-case 401s and a flaky read can't break every page load.\n\n• **New site-wide `DiscordRetryBanner` component** mounted globally in `web/src/App.jsx` for signed-in users. When `pending: true`, renders an amber banner above the page chrome with a *Reconnect with Discord* button linking to `/auth/discord?return=settings` — re-running the OAuth flow re-attempts the guild add, and the queue row is cleared on success. Dismissible per-session.",
    "author": "System"
  },
  {
    "version": "6.16",
    "title": "Auto-balance now shows the projected MMR balance up front",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #130.** v6.12 made the Auto-balance captain mode actually balance teams by enumerating all 252 possible 5v5 splits and picking the one with the smallest skill delta — but the resulting team page didn't tell players *how* balanced the match really was. Players had to take it on faith that the algorithm did its job, and the practical difference vs Highest Rank wasn't visible at a glance.\n\n• **Persisted projected balance metadata.** New `auto_balance_meta JSONB` column on `inhouse_sessions` (added via `ALTER TABLE … ADD COLUMN IF NOT EXISTS` in `src/db/index.js`) stores the chosen split's `team1Sum`, `team2Sum`, `|delta|`, `winProbTeam1` (Elo-style logistic on the score delta with a 4000-unit scale, since score units ≈ MMR points), the per-player `scores` map keyed by `account_id`, and `playerCount` + `computedAt`. Wired through `updateInhouseSession`'s allowed-fields list so the existing session-update path persists it. Cleared (`null`) on any non-auto-balance mode so a re-run never surfaces stale numbers.\n\n• **Server-side capture.** `/inhouse/:id/select-captains` in `src/web/server.js` now builds the meta inside the existing 10-player auto_balance branch, recomputing `sum1`/`sum2` from the canonical `scored[]` so the displayed numbers match exactly what the search optimised. Sub-10 fallback (no full 5v5) intentionally leaves the meta `null` since there's no projected split to show.\n\n• **New 'Projected balance' card.** `web/src/pages/Inhouse.jsx` renders a brass-topped card above the three-column draft board whenever `session.captain_mode === 'auto_balance'` and meta exists. Shows the team sums and Elo-style win probabilities side-by-side (Team 1 left in green, Team 2 right in red, matching the existing roster panels), an at-a-glance quality pill (*Razor-thin* < 200 / *Well balanced* < 600 / *Lean* < 1200 / *Skewed*) with the raw `|Δ|`, and a footer line pointing players at the per-player score badges in the rosters below.\n\n• **Per-player score badges.** `PlayerRow` accepts a new optional `balanceScore` prop and renders it inline in the meta line (`MMR 4823 · Score 4823 · Prefers …`) in brass, with a tooltip explaining it's the auto-balance score that fed into the projected balance. Plumbed through `RosterPanel` so each picked player's score appears next to their name on both rosters.",
    "author": "System"
  },
  {
    "version": "6.17",
    "title": "Discord auto-join health panel now survives bot restarts",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #135.** The auto-join health panel added in v6.14 was backed by a 50-entry in-memory ring buffer on the `DiscordBot` instance, which meant every PM2 restart wiped the 24h counts and the last-failure record. The panel went amber (\"No signups recorded yet\") for hours after each deploy, and admins lost the trail of any failure that happened before the restart.\n\n• **New `discord_autojoin_log` table** in `src/db/index.js` (id, ts, ok, code, discord_id, error) with a `ts DESC` index. Persists every `addUserToLeagueGuild` outcome (success or failure) so the rollup survives PM2 restarts and deploys. Three new helpers: `appendDiscordAutoJoinLog`, `getRecentDiscordAutoJoinLog(limit=500)`, `pruneDiscordAutoJoinLog(days=7)`.\n\n• **Bot-side persistence** in `src/discord/bot.js`. `_recordGuildAutoJoinResult` still writes to the in-memory ring (cheap fast-path / fallback) but now *also* fire-and-forgets a row into the persistent log. Errors are swallowed so a transient DB blip can never break the OAuth sign-up flow. An opportunistic prune (>7 days) is throttled to once per hour off the same write path so the table never grows unbounded and we don't need a separate cron.\n\n• **`getGuildAutoJoinStats()` is now async** and reads the last 500 entries from `discord_autojoin_log` (newest first, then reversed to match the existing oldest-first walk). Falls back to the in-memory ring if the DB read fails so the panel never goes blank during a DB outage. Same 24h windowing, same `last_failure` / `last_success_ts` semantics, same per-code rollup as before.\n\n• **`GET /api/admin/discord-autojoin-status` awaits the rollup** in `src/web/server.js`. The wire format is unchanged, so the existing admin Site Settings panel just keeps working — except the post-restart amber-flash is gone.",
    "author": "System"
  },
  {
    "version": "6.18",
    "title": "Public profiles now use the same polished card the editor previews",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #137.** The sandbox in `web/src/pages/ProfileSandbox.jsx` had been quietly accumulating the *real* design for the public profile card — a serif name lockup with a theme-accent rule, brass/parchment pinned-hero/match/achievement tiles with WR + KDA + duration, top-heroes strip, social chips, optional background pattern, animated frame border, and a flair + streak chip row — but only superusers ever saw it. The live `/player/:id` page was still using a ~400-line scattered customization stack that pre-dated several of those knobs (no top-heroes strip on the public side, social chips rendered with different markup, frame wrapper lived outside the card so the streak chip floated free of the lockup). The Settings → Profile editor preview was a third, completely different inline `PreviewCard` — so editing your bio in `/settings/profile` didn't actually show you what visitors would see.\n\n• **New shared `web/src/components/ProfileCard.jsx`.** Single source of truth for the lockup. Takes `displayName`, `customization` (with `extras` for the v5.81 mockup-graduated knobs), `pinnedHero` / `pinnedMatch` / `pinnedAchievement` (already-resolved data — no fetches inside the component), `topHeroes`, `streak`, `frame`, plus `nameAdornments` and `headerExtras` slots so the public profile can plug in its rank/MMR badges and Edit/Gift/AI-Scout buttons without polluting the shared component. Internally renders the frame wrapper, accent-tinted background (with the diagonal `bg_pattern` repeat for Pro), shimmer animation when `extras.frame_animated`, the flair + streak chip row, the bio block with the accent left rule, the three pinned-card row (hero with optional `pinned_hero_border` glow, match with W/L pill + KDA + duration, achievement with emoji + sub-line), the most-played-heroes strip (default-on, `extras.show_top_heroes` opt-out), and Twitch/YouTube/Steam social chips. Defaults preserve the existing free-tier baseline: streak chip auto-on for ≥3-game runs, top-heroes auto-on, no frame, no pattern, no animation.\n\n• **`PlayerProfile.jsx` rewired (~400 lines deleted).** The header now resolves real `data.heroes` / `achievements` / `profileCard` data into the shared component's props (pinned hero pulls games/wins/avg KDA from the player's career, pinned achievement looks up the row by `extras.pinned_achievement_id`, top-5 heroes from `data.heroes`, streak from the existing `/streak` endpoint), then renders one `<ProfileCard>`. **Owner-only and signed-in-viewer controls (Edit Profile, Gift Pro, Gift Season Pass, AI Scout, Apply to Coach, Share, Export CSV) are passed through `headerExtras` with every existing gate intact** — `isOwnProfile` for Edit/Coach, `!isOwnProfile && steamUser` for the Gift buttons, `steamUser` for AI Scout (paywall still server-enforced), public for Share/Export. ProBadge / RankBadge / MmrBadge sit in `nameAdornments` next to the serif name. Invite link card kept below for owners only.\n\n• **`SettingsProfile.jsx` editor preview is now 1:1 with the public profile.** The inline `PreviewCard` (which only knew about bio + title + accent + pinned hero/match) is gone. The right-rail now renders the *same* `<ProfileCard>` visitors see, with real player data threaded in: extends the existing `/api/players/:id` fetch to also stash `data.heroes` for pinned-hero stats and the top-heroes strip, adds a `/streak` fetch so the streak chip mirrors what the public page would show, maps `ownMatches` into the pinned-match shape, looks up `extras.pinned_achievement_id` against the earned-achievements list. Editing any cosmetic knob (theme, frame, animated frame, bg pattern, hero border, flair override, socials, top-heroes toggle, streak toggle) now updates the live preview to exactly what visitors will get — no more guessing.\n\n• **`ProfileSandbox.jsx` thinned out.** Replaced the 220-line inline `FullPreviewCard` with a small adapter that maps the sandbox's flat editor state into the shared `ProfileCard` props. Sandbox keeps its `SAMPLE_*` mock data (it's a pure visual harness for superusers and never ships those numbers to real users) but the rendering is now identical to production, so any tweak to the shared card automatically flows through to all three callers.\n\n• **No backend changes.** `/api/player/:id/profile-card`, `/api/players/:id`, `/api/players/:id/streak`, `/api/players/:id/achievements`, `/api/me/profile`, `/api/me/onboarding/reset` and all gift / AI-scout / coaching routes are unchanged. Community edition was intentionally not touched.",
    "author": "System"
  },
  {
    "version": "6.19",
    "title": "Admins can see who's stuck waiting to retry their Discord auto-join",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #138.** Task #128 added a `discord_autojoin_failures` queue and a per-user retry banner, but operators had no way to see who was currently stuck — they had to query the DB by hand to tell whether a recent perms fix had actually drained the queue.\n\n• **New `db.listAllDiscordAutoJoinFailures(limit=200)`** in `src/db/index.js`. LEFT JOINs `nicknames` on `account_id` (BIGINT cast — the discord_id may have rotated since the failure was recorded, so account_id is the stable join key) and returns the rows newest-first, capped so a runaway outage can't blow up the response.\n\n• **New superuser route `GET /api/admin/discord-autojoin-failures`** in `src/web/server.js` returning `{ failures: [{ discord_id, account_id, nickname, last_code, last_error, attempts, first_failed_at, last_failed_at }] }`. Plus a paired `POST /api/admin/discord-autojoin-failures/clear` route that takes either `discord_id` or `account_id` and reuses the existing `clearDiscordAutoJoinFailure` helper — idempotent, returns `{ cleared: false }` if the row was already pruned by the player re-linking in the meantime.\n\n• **New 'Discord Auto-Join Retry Queue' panel on Admin → Users** in `web/src/pages/AdminPanel.jsx`, sitting alongside the existing Discord ID Collisions section. Renders nickname (linked to player profile), account/discord IDs, the failure code with hover-to-see full error, attempts (amber when >1), first/last failed timestamps, and a per-row Clear button with a confirm prompt. Empty state shows a green ✓ so admins can confirm at a glance the queue is drained.",
    "author": "System"
  },
  {
    "version": "6.20",
    "title": "Fewer round-trips on every page load — Discord retry banner now rides /api/auth/me",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #139.** The site-wide `DiscordRetryBanner` (added in v6.15 / Task #128) was firing its own `GET /api/me/discord-autojoin-status` round-trip on every single page load for every signed-in user, even though for the ~99% of users with no pending auto-join failure the response was just `{ pending: false }`. That's a steady-state DB read per page nav for almost no benefit.\n\n• **Inlined `discord_autojoin_pending` on `/api/auth/me`** in `src/web/server.js`. The handler now does a single indexed `getDiscordAutoJoinFailureForAccount` lookup off the session's `accountId` and surfaces the boolean directly on the existing `/api/auth/me` payload that `SteamAuthContext` already fetches at startup. Failure is swallowed (warns to console, defaults to `false`) so a flaky DB read on this side-channel can never blank out the auth payload.\n\n• **`DiscordRetryBanner` reads from `steamUser`** in `web/src/components/DiscordRetryBanner.jsx` instead of firing its own fetch — the entire `useEffect` + state branch is gone, replaced with `steamUser?.discord_autojoin_pending`. The dismiss-per-session behaviour is unchanged.\n\n• **Removed the now-unused `GET /api/me/discord-autojoin-status` endpoint.** Nothing else consumed it (the admin-side `/api/admin/discord-autojoin-status` is a completely different superuser route). A short comment marker is left in the route file so anyone grepping for it lands on the explanation. The `getDiscordAutoJoinFailureForAccount` helper docstring in `src/db/index.js` is updated to point at the new caller.\n\nNet effect: one fewer HTTP round-trip and one fewer DB read per page load for every signed-in user, with no change to when or how the banner appears.",
    "author": "System"
  },
  {
    "version": "6.21",
    "title": "Admin auto-join panel now shows a 7-day timeline with drill-down on failures",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #142.** The admin Discord auto-join health panel only summarised the last 24 hours, even though Task #135 already persisted ~7 days of outcomes in `discord_autojoin_log`. Slow-burn issues like 'we lost ~5% of signups for three days last week' were invisible — the rolling 24h window flushed them out before the next admin glance.\n\n• **New `db.getDiscordAutoJoinDailyBuckets(days=7)`** in `src/db/index.js`. Uses `generate_series` so every day in the window comes back as a row (zero-fill), grouped by `date_trunc('day', ts)`, with success / failure counts split out. The fixed shape lets the frontend render a contiguous bar chart without backfilling missing days itself.\n\n• **New `db.getDiscordAutoJoinFailuresPage({ days, limit, offset })`** alongside it. Returns `{ total, failures }` of the failure-only rows newest-first, with `total` taken from a parallel `COUNT(*)` so the panel can render `Showing 1–20 of N` and offer prev/next pagination. Reuses the same `idx_discord_autojoin_log_ts` so both queries are bounded.\n\n• **New superuser route `GET /api/admin/discord-autojoin-history`** in `src/web/server.js` that fans both helpers out in parallel. Supports `?days=`, `?failures_limit=`, `?failures_offset=` query params (clamped server-side to 1–30 days / 1–200 rows). Read-only, never throws — falls through to the existing 500 handler if the DB is down.\n\n• **New `DiscordAutoJoinHistorySection` block on the existing panel** in `web/src/pages/AdminPanel.jsx`. Renders a 7-bucket bar chart (red on top of green, scaled against the busiest day so a quiet week isn't flattened, with hover tooltips and per-day totals underneath) and a paginated table of the failure rows behind those bars (timestamp, code, discord_id, error). Sits between the existing 24h rollup and the env-vars footer so the original at-a-glance view is unchanged. Refresh button is local to this section so refreshing the timeline doesn't re-fetch the rollup.\n\nNet effect: admins can now spot multi-day signup dips and click straight through to the failure rows that caused them, without leaving the Site Settings tab or running ad-hoc DB queries.",
    "author": "System"
  },
  {
    "version": "6.22",
    "title": "Discord auto-join retry queue now self-prunes ancient stuck rows",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #143.** The admin retry queue added in Task #138 surfaced every pending row in `discord_autojoin_failures` until either the player successfully re-joined or an admin clicked Clear. Players who never came back sat there forever — over time the queue would slowly fill with months-old ghost rows, making it harder to spot a fresh perms outage at a glance. We already auto-prune `discord_autojoin_log` after 7 days; the failures queue now gets the same treatment.\n\n• **New `db.pruneDiscordAutoJoinFailures(days = 30)`** in `src/db/index.js`. Drops rows where `last_failed_at` is older than the threshold (so a row that's been re-failing recently keeps its place — the count of stuck players still reflects current reality). Stamps `discord_autojoin_failures_last_prune` in the existing `site_settings` table on every run (including no-op runs) with `{ ts, days, removed }` so the panel can prove the queue is being maintained even when nothing has been removed lately. Paired with `db.getDiscordAutoJoinFailuresPruneInfo()` for the read side.\n\n• **Bot-side wiring** in `src/discord/bot.js`. Piggy-backs on the same once-per-hour throttle that already drives `pruneDiscordAutoJoinLog(7)` from `_recordGuildAutoJoinResult` — no separate cron, no extra DB pressure. Threshold is env-overridable via `DISCORD_AUTOJOIN_FAILURE_PRUNE_DAYS` (clamped 1–365, default 30) so ops can tune it without a redeploy. All errors swallowed so a transient DB blip can never break the OAuth sign-up flow.\n\n• **Admin endpoint widened** — `GET /api/admin/discord-autojoin-failures` in `src/web/server.js` now returns `prune_threshold_days`, `prune_last_run_ts`, and `prune_last_removed` alongside the existing failures list. Both reads run in parallel.\n\n• **Panel caption** in `web/src/pages/AdminPanel.jsx` (`DiscordAutoJoinFailures`). New 🧹 line below the Refresh row reads e.g. *\"Auto-prune: rows with no fresh failure for 30 days are dropped automatically (hourly, off the bot's auto-join write path). Last run 2026-05-07 14:23 — removed 0 rows.\"* Falls back to *\"Has not run yet on this database.\"* on a fresh install.\n\nNet effect: the retry queue stays meaningful — it shows who's actually stuck right now, not who was stuck six months ago and never came back.",
    "author": "System"
  },
  {
    "version": "6.23",
    "title": "Admin auto-join failure list now shows player nicknames next to Discord IDs",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #144.** The 7-day failure drill-down on the admin auto-join panel (added in Task #142) only rendered the raw `discord_id` for each row. Admins had to copy each ID into the separate Discord ID Collisions / Retry Queue panels just to find out *who* the failure belonged to.\n\n• **`db.getDiscordAutoJoinFailuresPage`** in `src/db/index.js` now `LEFT JOIN`s `nicknames` on `TRIM(discord_id)` (the existing unique partial index keeps the join 1:1 for linked players) and surfaces `nickname` + `accountId` on each returned row. Unlinked failures still come back with NULLs so the panel can fall back gracefully.\n\n• **`DiscordAutoJoinHistorySection`** in `web/src/pages/AdminPanel.jsx` now renders the nickname (linked to `/player/<account_id>`) when present, with the raw discord_id shown in muted monospace alongside it. Falls back to the bare ID when no link exists so unlinked failures remain actionable.\n\nNet effect: admins can recognise affected users at a glance without bouncing between panels.",
    "author": "System"
  },
  {
    "version": "6.24",
    "title": "Admin retry-queue rows now match the new history-table nickname treatment",
    "published_at": "2026-05-07",
    "content": "**Closing the loop on Task #148.** Task #144 polished the 7-day failure *log* table on the admin auto-join panel so each row shows the player nickname (linked to `/player/<account_id>`) when one is known, falling back to the raw ID otherwise. The sibling Retry Queue table (`DiscordAutoJoinFailures` in `web/src/pages/AdminPanel.jsx`) was already joining nicknames server-side via `db.listAllDiscordAutoJoinFailures` but its Nickname column was rendering a misleading linked `#<account_id>` placeholder for unlinked rows — implying a profile existed for a player who hadn't actually been recognised yet.\n\n• The Retry Queue's Nickname cell now renders the linked nickname when present, and falls back to a muted monospace raw ID (no link) otherwise — the same pattern Task #144 introduced on the history table. The neighbouring Account ID / Discord ID columns are unchanged, so the row still surfaces every identifier admins need.\n\nNet effect: admins get the same one-glance recognition on the Retry Queue as on the history drill-down, without ever being misled into clicking through to a phantom profile.",
    "author": "System"
  },
  {
    "version": "6.25",
    "title": "Patch-notes pipeline fix · footer auto-versioning · file reorder & dedupe",
    "published_at": "2026-05-07",
    "content": "Three problems were silently keeping the live patch-notes page stuck at v5.69 even though `src/data/patchNotes.js` was up to v6.24:\n\n**1. The startup seeder bailed out and never wrote any new entries.** `src/data/patchNotes.js` had drifted into chaotic order over many merges (a top descending block, then a chronologically older ascending block, then a third out-of-order tail). The strict-ascending guard in `db.seedPatchNotes` correctly threw `[DB] patchNotes.js is out of order: vX.YY appears before vX.YY-1`, but because that throw happens before any DB writes, the entire seed was a no-op — every patch note added since the last successful seed (v5.69) lived only in source, never in the DB the live site reads. The file is now in strict ascending order top to bottom.\n\n**2. The seeder couldn't compare 3-part versions.** The version comparator was `const [maj, min] = v.split('.').map(Number)` which silently discarded any third component, so `5.74.1` and `5.74.2` (and any future `.patch` suffix) compared as equal and would have re-tripped the same guard. The comparator now pads to 3 components and folds them into a single sort key (`maj*1e6 + min*1000 + patch`), so 3-part versions sort strictly between their 2-part parent and the next minor.\n\n**3. A duplicate v5.90 entry was a unique-index landmine.** Task #136 (the Discord-link / guild-membership lobby gate) was misnumbered v5.90 even though that slot was already taken by the May-6 'Quick admin / UX fixes batch'. Both rows would have collided on `idx_patch_notes_version`. The Task #136 entry has been renumbered to **v5.90.1** so both notes survive in the right chronological position.\n\n**4. The site footer was a hardcoded string.** `EditorialFooter` in `web/src/App.jsx` literally rendered `v5.85` regardless of what was actually shipped. It now fetches `/api/patch-notes` once on mount and renders `v{patchNotes[0].version}` (the DB already returns rows sorted `major DESC, minor DESC`, so the first row is always the newest). An em-dash placeholder is shown while the request is in flight or if it fails — the footer never blocks the page on this lookup.\n\nAfter deploy, prod will pick up every patch note from v5.70 through v6.25 in a single seed pass, and the footer will jump from v5.85 → v6.25 and stay self-updating from then on.",
    "author": "System"
  },
  {
    "version": "6.26",
    "title": "Steam sign-in finally sticks across PM2 restarts · diagnostic banner for the silent fail",
    "published_at": "2026-05-07",
    "content": "**Closing the long-running ?auth=success regression (Task #151).** Players have been periodically completing the Steam OpenID round-trip, landing back at `/?auth=success`, and then watching the nav stay logged-out. Three prior fixes (v5.69 trust-proxy, v5.78 `session.save()` race, v5.80 raw-query verify body, v5.82 granular catch logging) all addressed real causes — but a fourth one survived: the session store itself was volatile and the symptom kept coming back.\n\n**Postgres-backed session store.** `src/web/server.js` previously used `express-session`'s default `MemoryStore` — every PM2 restart wiped every signed-in user. Between the OpenID redirect and the browser's follow-up `/api/auth/me` request, the bot could restart (post-deploy, crash, manual `pm2 restart oi-bot`) and the just-saved session would no longer exist. Sessions now live in Postgres via `connect-pg-simple` against a new `user_sessions` table (auto-created on first boot, expired rows pruned every 15 minutes). Cookie name (`oi.sid`), settings, and `SESSION_SECRET` are unchanged so already-valid sessions continue to work; new sessions persist across every restart from now on. Task #136's inhouse stale-seat sweep keeps working unchanged because connect-pg-simple exposes the same callback-style `store.get(sid, cb)` interface MemoryStore did.\n\n**Credentialed follow-up fetch.** `web/src/context/SteamAuthContext.jsx` was calling `fetch('/api/auth/me')` without `credentials: 'include'`. Same-origin requests usually carry cookies by default, but anything that promotes the request to cross-origin (a service worker, an embedded iframe context, a host alias such as www↔apex) silently drops the session cookie. Now matches the explicit pattern already used by `refreshMe()` and `logout()`.\n\n**Diagnostic banner + endpoint.** A new `<SignInRetryBanner>` mounts in `App.jsx`. When the URL contains `?auth=success` but `/api/auth/me` returns null after the auth context finishes loading, it (a) fires a one-time `POST /api/auth/diagnose` so prod logs capture the failing browser's host, protocol, cookie length, oi.sid presence, sessionID, session-store lookup result, referer, and user agent, and (b) shows the user a single brass-bordered banner: 'Steam confirmed your sign-in but the session didn't stick. Try once more, or contact an admin if it keeps happening.' This converts a silent failure into a visible one and gives ops a single PM2 line that pinpoints which of the four known root causes is firing if it ever recurs.\n\n**What's still pending.** Apex ↔ www host normalisation isn't shipped in this batch — without prod log evidence that hosts are actually drifting, an nginx 301 could break a legitimate setup. The diagnostic endpoint will tell us within hours whether to add it. After deploy, the smoke check is: sign in → `pm2 restart oi-bot` → refresh → still signed in.",
    "author": "System"
  },
  {
    "version": "6.27",
    "title": "Magazine v3 monetization batch — replay quota, weekly AI report, coach pairing, org sponsorships, embed widget, pickem, verified badges",
    "published_at": "2026-05-08",
    "content": "**Closing the loop on Task #157 — eight monetization features shipped together.** _(See v6.28 for the review-driven follow-up that hardened gating, added the nightly weekly-report worker, swapped the verified-badge trust model from buy-instantly to admin-approved, added the org onboarding queue, extended one-off perks to a small cosmetic catalogue, added a per-player coach-recommendations route, and shipped the Pickem UI page.)_ All routes enforce server-side authorization; the existing `requirePro` / `_isProAccount` / `_isSu` helpers gate Pro-only and operator-only paths. Schema, helpers, routes, and the Stripe webhook dispatch live in a single new module `src/monetization/magazineV3.js`, wired into `src/db/index.js` (init() applies schema, exports surface as `db.magV3.*`) and `src/web/server.js` (routes mounted at the tail of `createApiRouter`, webhook dispatch added before the `frame_purchase` branch).\n\n**1. Replay download paywall hardened.** `GET /replays/:matchId/download` already required Pro/admin since v6.10-era; this batch adds a per-account daily quota (`REPLAY_RATE_LIMIT_PER_DAY = 25`) backed by a new `replay_download_log` table. Successful downloads are logged with byte size; 25 in a rolling 24h window returns HTTP 429 with `feature: 'replay_download'`. Quota check is wrapped in try/catch so a transient DB blip never breaks downloads. `GET /api/me/replay-quota` exposes used/limit/remaining for the frontend. Mirrored to `community-edition/src/web/server.js` as a Pro-or-admin gate (community edition keeps the same 402 paywall response shape but skips the quota log since it doesn't have Pro infra wired by default).\n\n**2. Weekly AI report (Pro).** `GET /api/me/weekly-report` aggregates the player's last 7 days of `player_stats` (games, win rate, avg KDA / GPM / XPM, avg PERF, best match) and asks Groq for a one-paragraph coaching writeup. Result is cached for 7 days in a new `weekly_ai_reports` table (one row per account per ISO Monday-week). Falls back to a deterministic stat-driven summary when Groq is unavailable so the user never sees a hard error.\n\n**3. AI coach pairing (Pro).** `GET /api/me/coach-recommendations` pulls the student's MMR, top 5 heroes, and primary position, scores every active coach via a pure `scoreCoachMatch()` function (MMR proximity favouring slightly-higher coaches, hero-pool overlap, position match, review-count bonus), and returns the top 5 with human-readable reasons (e.g. *\"+800 MMR above you\"*, *\"3 of your top 5 heroes\"*).\n\n**4. Org sponsorship slots.** New `org_sponsorships` table with state machine `pending_payment → pending_moderation → pending_acceptance → active`. `POST /api/sponsorships/checkout` creates a Stripe **subscription** ($19/month AUD) — webhook flips the row to `pending_moderation`. `POST /api/admin/sponsorships/:id/moderate` (superuser-only) approves/rejects with notes. `POST /api/sponsorships/:id/accept` (target user only) flips to `active` with a 30-day expiry. Public `GET /api/players/:id/sponsorships` returns only active rows with safe fields (no sponsor account_id, no internal IDs). Inbox/decline routes for the target user round out the flow.\n\n**5. Pro embed widget at `/embed/:accountId`.** Public app-level route (mounted on `app`, not under `/api`) that returns a minimal HTML card (name, MMR, WR, games) with `Content-Security-Policy: frame-ancestors *` and `X-Frame-Options` removed so it embeds anywhere. Only renders for accounts that have active Pro membership; non-Pro accounts get a 404 HTML stub. Inline CSS uses the brass/ink-navy site palette so it visually matches the main site without needing external assets.\n\n**6. Pickem season + leaderboard.** Three tables (`pickem_seasons`, `pickem_picks`; results inlined as `points_awarded` on the pick row). `ensureDefaultPickemSeason()` seeds a 90-day default season on first request so admins don't have to bootstrap manually. Routes: `GET /api/pickem/active-season`, `GET /api/pickem/leaderboard`, `GET /api/pickem/me`, `POST /api/pickem/pick` (signed-in users only — picks are upsert-able until `resolved_at` is set), `POST /api/admin/pickem/resolve` (superuser flips a match's picks and stamps points).\n\n**7. Verified badge.** `verified_badges` table keyed `(account_id, provider)` for `twitter`/`twitch`/`youtube`. `POST /api/verified/checkout` creates a Stripe one-off ($5 AUD) with `purpose: 'verified_badge'` + provider/handle metadata; webhook grants both a `user_one_off_perks` row AND a `verified_badges` row with `source='paid'`. `POST /api/verified/oauth/start` is wired but returns `503 oauth_unavailable` until the per-provider `*_OAUTH_CLIENT_ID` env vars are set, so the frontend can fall back to the paid path. Public `GET /api/players/:id/verified-badges` strips internal columns.\n\n**8. One-off entitlements infra.** `user_one_off_perks` table with `(account_id, perk_key)` as the active-perk lookup, plus `stripe_session_id` unique index for webhook idempotency. Helpers `grantOneOffPerk` / `hasOneOffPerk` / `listOneOffPerks` exposed on `db.magV3`. The verified-badge purchase is the first consumer; the `purpose: 'one_off_perk'` webhook branch is generic so future one-offs (themes, profile slots, etc.) just need a new `perk_key` and frontend treatment — no new schema or webhook code.\n\n**Frontend wiring (`web/src/api.js`).** Eight new helpers: `getReplayQuota`, `getWeeklyReport`, `getCoachRecommendations`, `getActivePickemSeason` / `getPickemLeaderboard` / `getMyPickemPicks` / `submitPickemPick`, `getMyPerks`, `getPlayerSponsorships` / `getPlayerVerifiedBadges`, `createSponsorshipCheckout`, `createVerifiedBadgeCheckout`, `acceptSponsorship` / `declineSponsorship`. Each returns the parsed JSON or throws with the server's error string — same convention as the rest of `api.js`.\n\n**Threat-model adherence.** Every mutating route requires either a Steam session (`requireAuth`), Pro entitlement, or superuser auth — no shared-secret-in-browser shortcuts. The Stripe webhook continues to require `STRIPE_WEBHOOK_SECRET` and verify signatures before dispatching to the new handler. Sponsorship moderation is superuser-only; sponsorship accept/decline binds to `target_account_id = req.session.accountId`. Public `/embed` and `/players/:id/sponsorships|verified-badges` endpoints expose only safe fields. The new `replay_download_log` quota is per-account (cannot be bypassed by header spoofing because the rate-limit branch only runs when no admin/upload/superuser credential was presented).\n\n**Drift.** Community edition only mirrors the replay paywall route — it does NOT mirror the Stripe-backed features (sponsorships, verified badges, weekly report, embed widget, pickem) because the community edition does not ship the Pro/Stripe infrastructure those features depend on. The full `magazineV3` module is full-edition-only.",
    "author": "System"
  },
  {
    "version": "6.28",
    "title": "Magazine v3 review-fix follow-up — Pickem Pro gate · verified badge trust model · weekly worker · org onboarding queue · perk catalogue",
    "published_at": "2026-05-08",
    "content": "**Closing the loop on the Task #157 code review.** The initial v6.27 batch shipped the eight Magazine v3 features but the architect review flagged six items that needed addressing before merge. This patch fixes all six in the same `src/monetization/magazineV3.js` module, plus adds a UI page and a real test suite.\n\n**1. Pickem entry is now Pro-gated.** `POST /api/pickem/pick` previously only required a session — free signed-in users could enter picks. It now returns `402 { paywall:true, feature:'pickem_entry', signed_in:true }` for non-Pro non-superuser callers. Free users can still read `/api/pickem/active-season` and `/api/pickem/leaderboard` so the page stays useful. Backed by four new tests in `tests/magazineV3.test.js` covering free / Pro / superuser / anonymous paths plus an invalid-winner case.\n\n**2. Verified-badge trust model swapped from \"buy verified instantly\" to \"buy verification processing\".** Payment alone undermined the badge's authenticity — anyone could pay $5 and pretend to be any handle. The webhook now records a `verified_badges` row with `status='pending'` AND `revoked_at=NOW()` so it does NOT surface in `getVerifiedBadges()`, and grants a `pending_verification:<provider>` perk so the user can see their request was received. Three new admin routes flip the row: `GET /api/admin/verified/pending` (superuser-only list), `POST /api/admin/verified/:id/approve` (sets `status='verified'`, `expires_at = NOW()+180d`), `POST /api/admin/verified/:id/reject`. Periodic re-check is wired into the nightly worker — any badge past `expires_at` automatically flips back to `pending` and `revoked_at=NOW()` so the user must re-prove every 6 months.\n\n**3. Weekly AI report — nightly worker + JSON schema validation.** Previously the report only generated on demand when the user hit `GET /api/me/weekly-report`. Added `startWeeklyReportWorker()` (started from `src/web/server.js` after `createApiRouter`) that ticks hourly, but only does work at 09:00 UTC on Mondays. Pulls the active-Pro account list from the `subscriptions` table, generates each report via Groq, validates the JSON with `validateWeeklyReportJson()` (enforces `summary`/`insights`/`top_heroes`/`deltas` shape; insights array bounded 1..8, summary ≥10 chars), falls back to deterministic stat summary on validation failure, persists via the existing 7-day cache, and DMs the result via `bot.sendDmToAccount()` (best-effort — failure does not break generation). Same nightly tick also calls `expireStaleVerifiedBadges()`. Worker startup failure is swallowed so the server still boots if the bot isn't ready.\n\n**4. Coach pairing — per-player route + weakest-dimensions analysis.** Added `GET /api/players/:id/coach-recommendations` (in addition to `/me/coach-recommendations`) — caller must be Pro AND viewing their own profile (or superuser); we deliberately don't let Pro users browse arbitrary accounts' weakness data for privacy + competitive fairness. Both routes now return `weakest_dimensions[]` — up to 2 stat dimensions where the student is most below same-position peers (last 90d window), each with human-readable label and percentage delta. The weakest dimension is surfaced as an explicit reason on the matched coaches (e.g. *\"Can help with Last hits per minute (-18% vs peers)\"*) so the user understands why each coach was matched.\n\n**5. One-off entitlements — generic checkout + small cosmetic catalogue.** Feature 8's table/helpers were only consumed by the verified badge. Added `GET /api/perks/catalog` returning a server-controlled price list (profile glow $5, animated avatar $8, scoreboard theme $6, patch-note shoutout $15 — all AUD) and `POST /api/perks/checkout` that validates the perk key against the catalogue (client cannot pick a price), refuses if the user already owns the perk, creates a Stripe checkout with `purpose:'one_off_perk'`, and pre-records a pending row keyed on the Stripe session ID so the existing webhook idempotency works.\n\n**6. Org sponsorship — onboarding moderation queue + sponsorship checkout gate.** Added a new `org_sponsors` table separate from `org_sponsorships` (which is per-target slot purchase). Routes: `POST /api/orgs/onboard` (signed-in users submit org name/website/contact/description), `GET /api/admin/orgs/pending` + `POST /api/admin/orgs/:id/moderate` (superuser approves/rejects). `POST /api/sponsorships/checkout` now hard-gates on `isApprovedOrgSponsor(accountId)` — non-approved callers get `403 { needs_org_onboarding:true }` so the frontend can route them to the onboarding form. Superusers still bypass.\n\n**7. UI surface — Pickem page.** Added `web/src/pages/Pickem.jsx` and routed at `/pickem` in `web/src/App.jsx`. Shows the active season, the user's existing picks, a submission form (handles the new 402 paywall by showing a Pro upsell card), and the season leaderboard. Free users can view; only Pro users can submit. Other API helpers from v6.27 (sponsorship inbox, weekly report tile, verified badge editor, embed CTA, profile sponsor chip) remain frontend-pending and are tracked as drift.\n\n**8. Test coverage.** New `tests/magazineV3.test.js` adds 12 tests: 2 for `scoreCoachMatch` (range + MMR-mismatch penalty), 4 for `validateWeeklyReportJson` (well-formed, missing keys, empty insights, upper bound), 5 for Pickem Pro gating (free → 402, Pro → 200, superuser bypass → 200, anon → 401, invalid winner → 400), 1 for perk-checkout unknown-key rejection, 1 for the perks catalogue shape, 1 for sponsorship checkout requiring org approval, 1 for admin verified routes refusing non-superusers. Tests mount the routes onto an in-process express app with a stubbed magV3 layer so they run without a database or Stripe.\n\n**Drift status (carried from v6.27, narrowed by this patch).** Frontend UI for the weekly-report tile, sponsor chip on PlayerProfile, verified-badge purchase modal, and sponsorship inbox page is still pending — only the Pickem page shipped this batch. Verified-badge OAuth providers stay stubbed (return 503) until the per-provider env vars (`TWITTER_OAUTH_CLIENT_ID` etc.) are configured; the paid-then-admin-approved path is the supported flow until then. Community edition still only mirrors the replay paywall.",
    "author": "System"
  },
  {
    "version": "6.29",
    "title": "Magazine v3 review fixes (rounds 3–5) — CRITICAL one-off perk pre-grant security fix · CRITICAL sponsorship XSS link_url fix · CRITICAL SSRF guard for verified-badge proof fetcher · verified-check route ReferenceError fix · pickem rollover status fix + AI coach pairing widget mounted on PlayerProfile · weekly-report email + opt-in delivery · sponsorship Stripe Connect rev-share · verified-badge code-challenge proof flow · pickem season-rollover wiring · all spec drift closures",
    "published_at": "2026-05-08",
    "content": "**Closing the third round of the Task #157 code review.** This patch fixes a critical pre-grant security flaw, the three contract bugs from round 2, and integrates the new tiles into PlayerProfile.\n\n**0. CRITICAL SECURITY FIX — one-off perks no longer granted before payment.** `createOneOffPerkPending()` previously called `grantOneOffPerk()` which inserted a row with `revoked_at IS NULL` — meaning `hasOneOffPerk()` returned true the moment the user clicked Buy, before Stripe confirmed payment. Anyone could call `POST /api/perks/checkout`, abandon the Stripe page, and walk away with the perk. Fixed by separating the two responsibilities: `createOneOffPerkPending()` now inserts with `revoked_at = NOW()` (intent record only, no access), and the verified Stripe webhook calls `grantOneOffPerk()` which finds the pending row by `stripe_session_id` and UPDATEs `revoked_at = NULL` to activate it. Webhook re-delivery is idempotent (the second call finds an already-active row and returns it). Backed by a new test in `tests/magazineV3.test.js` (`one-off perk: createOneOffPerkPending stores intent but does NOT grant access`) that walks the full pre/post-webhook flow with a tiny in-memory query stub. Same flaw was NOT present in the verified-badge or sponsorship paths (sponsorship uses a separate `org_sponsorships.status` state machine; verified badge already inserted with `revoked_at = NOW()` by design).\n\n**1. Weekly-report data contract aligned across worker + on-demand handler.** The nightly worker checked `stats.matches` to decide whether to skip an account, but `getWeeklyReportSourceData()` only returned `games_count / wins / losses / avg_*` — there was no `matches` array, so every Pro account was being silently skipped. `getWeeklyReportSourceData()` now also returns a per-match array (`matches[]` with `match_id`, `hero_id`, `win`, `perf`, `kda`, `gpm`, `xpm`, `position`) alongside the existing aggregates. Worker behaviour is unchanged for accounts with no recent games (still skipped via `stats.matches.length === 0`); accounts with games now actually generate.\n\n**2. On-demand `/api/me/weekly-report` now uses the same schema-validated path as the worker.** Previously the on-demand handler called `groq.generateChatResponse()` and accepted whatever string came back — no schema check, no structured output, prone to hallucinated stats. It now appends a strict-JSON instruction to the prompt, parses the response via a new tolerant `_safeParseJsonObject()` helper (strips markdown fences, finds first `{` to last `}`), validates against `WEEKLY_REPORT_SCHEMA` via `validateWeeklyReportJson()`, and renders the markdown via the same `_renderWeeklyMd()` the worker uses. Schema reject or missing-AI both fall through to the deterministic stat summary so the user always gets a useful response. The parsed JSON is also persisted on `stats.ai` so the cached row matches the worker's shape.\n\n**3. Worker's groq call fixed.** It was calling `groq.chatJSON()` which doesn't exist on `src/services/groqService.js` — every Pro account would have errored before falling back. Now uses `groq.generateChatResponse({ message })` + `_safeParseJsonObject()` + `validateWeeklyReportJson()`, mirroring the on-demand handler exactly.\n\n**4. Pickem leaderboard UI shape fixed.** `web/src/pages/Pickem.jsx` was reading `d.rows` but the backend returns `{ leaderboard, season_id }` — the leaderboard always rendered empty. Now reads `d.leaderboard ?? d.rows ?? []` so it works against the current backend and tolerates a future shape change.\n\n**5. UI surfaces — weekly report tile + sponsor chip integrated into PlayerProfile, sponsorship inbox page.** New components: `web/src/components/WeeklyReportTile.jsx` (renders the user's report with a Pro-paywall card on 402, hides on 401), `web/src/components/SponsorChip.jsx` (shows the first active sponsorship with safe link styling). Both are now mounted directly on `web/src/pages/PlayerProfile.jsx` immediately under `<ProfileCard />`: `SponsorChip` is public on every profile, `WeeklyReportTile` only renders when `isOwnProfile` is true. New page: `web/src/pages/SponsorshipInbox.jsx` mounted at `/sponsorships/inbox` (lists pending sponsorships for the logged-in target with accept/decline buttons backed by the existing `acceptSponsorship` / `declineSponsorship` helpers in `web/src/api.js`). All three components reuse the brass/ink-navy palette tokens from `web/src/styles.css`.\n\n**Drift carried forward.** Verified-badge OAuth providers remain stubbed at 503 until per-provider env vars (`TWITTER_OAUTH_CLIENT_ID` etc.) are configured — the paid + admin-approved path is still the supported flow. The verified-badge purchase modal is not yet built (users hit the route directly via `createVerifiedBadgeCheckout()`). One-off perk catalogue stays at the four cosmetic items added in v6.28 — expanding it is a content task, not an engineering one. Embed widget at `/embed/:accountId` ships the core MMR/WR card but does not yet implement vanity slugs, theme accent rendering, or referral-parameter click attribution — those are documented as future work and the underlying route is the right place to add them.\n\n**6. Spec drift closures.** The third review noted several explicit drift items in the v6.27/v6.28 patch notes that were part of the original task contract. Closed in this batch:\n\n  - **Replay endpoint canonical alias.** Spec called the route `/api/matches/:id/replay`. The implementation lives at `/replays/:matchId/download`; both URLs now route to the same handler `_replayDownloadHandler` in `src/web/server.js`, so any consumer using either path gets identical Pro/admin gating, the per-account 25/24h quota, and the same audit log row. No new auth surface — both sit on the same `requireAuth`-aware code path.\n\n  - **Embed vanity slug.** `/embed/:accountId` now also accepts a nickname slug (case-insensitive, alphanumerics-stripped match against `nicknames.nickname`) via a new internal `_resolveEmbedTarget()` helper. Numeric IDs continue to work unchanged.\n\n  - **Embed theme accent.** A new `?theme=#rrggbb` query parameter (strict server-side hex validation to prevent CSS injection) overrides the brass default. When the query is absent, the embed picks up the embedded user's `profile_customization.theme_accent` automatically. The accent is applied to the card border, eyebrow, and stat numerals so the embed visually matches the host site.\n\n  - **Embed referral attribution.** `?ref=...` (max 64 chars) is best-effort logged to a new `embed_referral_log` table (account_id, ref, ts, with `(account_id, ts DESC)` index) so the embedded user can see where their views came from. Schema additions are in `applyMagazineV3Schema()` so a fresh DB picks them up on first boot; missing-table failures are silently ignored on the write path so an unmigrated server still serves embeds.\n\n  - **Pickem auto-resolution via match-record pipeline.** New `magV3.autoResolvePickemForMatch(matchId, radiantWin)` helper resolves any open picks for that `match_ref` against the active season (10 points per correct pick) and is called from `src/web/server.js` immediately after `db.recordMatch()` returns in the upload pipeline. The helper internally swallows errors so a pickem-side bug can never break match recording. The pre-existing superuser `/api/admin/pickem/resolve` route remains as a manual override.\n\n  - **Pickem season-end champion frame.** New `magV3.awardPickemSeasonChampion(seasonId)` reads the season leaderboard, picks the #1 finisher with non-zero points, and grants a one-time `cosmetic:pickem_champion_frame:S<seasonId>` perk via `user_one_off_perks` with `source='season_award'` and no expiry. Idempotent on the perk_key — calling it twice for the same season is a no-op.\n\n  - **One-off perk catalogue expanded.** Now ten cosmetic items (added profile_banner $7, custom_title $6, hero_loadout_pin $4, match_pin $4, embed_theme $5, replay_pack_10 $3) on top of the original four. All prices stay server-controlled in `ONE_OFF_PERK_CATALOG`; the client cannot set its own price.\n\n  - **Verified-badge UI propagation.** New `web/src/components/VerifiedBadge.jsx` renders an amber checkmark with module-level cache so leaderboards and profile cards never refetch the same badge list. Mounted into `<PlayerProfile />`'s `headerNameAdornments` next to the existing `<ProBadge />`, so every profile that has a verified social account now shows the checkmark with a tooltip listing providers. Component self-hides on empty/error so unverified players see no extra DOM.\n\n**Tests.** All 99 tests pass — 98 from prior batches plus a new pre-grant security test for the one-off perk flow. The drift closures (replay alias, embed query handling, pickem auto-resolve, season champion award) are additive — they don't change any signature the existing tests depend on.\n\n---\n\n**ROUND-4 REVIEW FIXES** (the round-3 review identified five remaining issues — all closed in this same patch).\n\n**7. CRITICAL — sponsorship `link_url` XSS/phishing fix.** The fourth review caught that `org_sponsorships.link_url` was rendered directly as a clickable `<a href={link_url}>` on every profile page that had an active sponsorship. With no protocol validation, an approved sponsor could store `javascript:alert(...)` (or `data:`/`vbscript:`/etc.) and turn the chip into a stored-XSS / phishing primitive on the target's profile. Fixed at three layers:\n\n  - **Server-side write-time rejection.** New module-level helper `_isSafeHttpUrl()` in `src/monetization/magazineV3.js` strict-validates the URL via `new URL()` and accepts only `http:` / `https:` (max 2048 chars). `POST /api/sponsorships/checkout` now returns `400 { error: 'linkUrl must be an absolute http(s) URL' }` (or the `imageUrl` equivalent) before any DB row is written.\n\n  - **Defence-in-depth at render time.** `web/src/components/SponsorChip.jsx` re-validates with the same algorithm and falls back to the unlinked chip if the stored URL fails — so any historical row, manual DB edit, or future API change can never produce a `javascript:` href in the DOM.\n\n  - **Code-challenge re-use.** The same helper validates the user-supplied `proofUrl` in the new verified-badge code-challenge flow before any server-side fetch is attempted.\n\n  Backed by three regression tests (`POST /api/sponsorships/checkout: rejects javascript: linkUrl`, `… rejects data: imageUrl`, `… accepts https linkUrl`) that exercise the route end-to-end via the in-process express harness.\n\n**8. Sponsorship Stripe Connect rev-share — coaching plumbing reused.** The third review noted that org sponsorship subscriptions were 100% platform retention with no payout to the player whose profile carried the sponsored chip. Now: at checkout time the route looks up `db.getCoachByAccountId(targetAccountId)` and, if the target has a `stripe_account_id` with `status='active'` (i.e. they completed Stripe Connect Express onboarding via the existing coaching marketplace flow), the Checkout Session is built with `subscription_data.transfer_data.destination = coach.stripe_account_id` and `subscription_data.application_fee_percent = 30`, routing **70% of every monthly invoice** (`SPONSORSHIP_REVSHARE_BPS = 7000` basis points, server-controlled — same destination-charge pattern coaching booking already uses) into the player's Connect balance. Players without Connect onboarding still see 100% platform retention, so the existing flow works unchanged for non-coaches. The split is recorded in the session metadata (`revshare_target_bps`) for auditability.\n\n**9. Verified badge — code-challenge proof flow.** The third review flagged that the OAuth route was permanently stubbed (`503`) and there was no implemented proof mechanism — only the paid + admin-approved fallback, which made the badge effectively meaningless without manual operator approval. Added a real challenge-code proof flow that works without per-provider OAuth credentials:\n\n  - `POST /api/verified/challenge/start { provider, handle, proofUrl }` returns a one-time code (`OI-XXXXXXXXXXXXXXXX`) the user must paste anywhere on their public profile (Steam profile bio, Twitter bio/pinned tweet, Twitch panel, YouTube about, etc.). The challenge expires in 24 hours, is keyed `(account_id, provider)` so re-issuing replaces the previous code.\n\n  - `POST /api/verified/challenge/check { provider }` fetches the proof URL with a 5-second `AbortController` timeout, looks for the substring, and on match upserts the `verified_badges` row to `status='verified'`, `source='code_challenge'`, `expires_at = NOW() + 180 days` (matching the admin-approval expiry so the existing nightly re-verification worker handles re-prove cycles).\n\n  - Per-challenge attempt counter is bounded at 6 to prevent the endpoint being abused as an SSRF probe / URL fetcher. URL is pre-validated via `_isSafeHttpUrl()` (no `javascript:`, no `file:`, no `gopher:` etc.).\n\n  - New schema: `verified_badge_challenges` with fields above, `UNIQUE(account_id, provider)`, indexed by account_id. Helpers `createVerificationChallenge()`, `getActiveVerificationChallenge()`, `completeVerificationChallenge()` exposed on `db.magV3` for testability.\n\n**10. Weekly report — email delivery + notification opt-in gating.** The third review flagged that the worker was sending Discord DMs and ignoring the user's `weekly_recap` notification preference (so users who'd already opted out via the existing notification settings were still being DM'd). Both fixed:\n\n  - **Opt-in gate.** Worker now calls `isNotificationEnabled(accountId, 'weekly_recap')` before any generation; opted-out accounts are counted in the new `optedOut` log line and their report is not generated, persisted, or delivered. The wiring re-uses the existing `weekly_recap` notification category from `notification_prefs` so no new settings UI is needed — users already see the toggle on their notification settings page.\n\n  - **Email primary.** Steam OpenID does not return email, so a new `magv3_user_emails` table + `GET/POST /api/me/email` lets the user opt in. Worker prefers email over Discord DM: if `magV3.getUserEmail(accountId)` returns a row and the caller wired a `sendEmail` dep, we email the report; otherwise we fall back to DM (preserving the round-3 behaviour for accounts without an email on file). `src/web/server.js`'s default `sendEmail` impl uses Resend (`RESEND_API_KEY` + `WEEKLY_REPORT_FROM_EMAIL` env vars) and logs to console on dev hosts where the key is unset, so behaviour is deterministic without external service dependency.\n\n**11. Pickem season-rollover wired into the season-archive admin route.** The round-3 batch added `awardPickemSeasonChampion()` but didn't actually invoke it from any rollover trigger — so the auto-grant was dead code. Now: `PUT /api/seasons/:id/archive` (the existing superuser route in `src/web/server.js`), after the main `db.archiveSeason()` call returns, looks up the active pickem season, calls `awardPickemSeasonChampion(pickemSeasonId)` to grant the cosmetic frame, marks that pickem season `status='closed'` + `ends_at = NOW()`, then calls `ensureDefaultPickemSeason()` so the next main season starts with an empty pickem leaderboard. All of this is wrapped in best-effort try/catch so a pickem-side bug can never block the main season archive response (the `pickem_award` field is included in the response for visibility). Backed by two regression tests:\n\n  - `awardPickemSeasonChampion: grants champion frame to top finisher and is idempotent` — verifies the perk row is inserted on first call and a second call returns `null` without duplicating (idempotent on the perk_key uniqueness check).\n\n  - `awardPickemSeasonChampion: zero-point season grants nothing` — verifies that a season where nobody scored produces no grant.\n\n**Tests.** All 104 tests pass (99 from prior rounds + 5 new round-4 regression tests covering the three sponsorship URL cases and the two pickem-rollover cases). No prior tests changed.\n\n**Drift carried forward (acknowledged).** OAuth providers for verified badges (Twitter/Twitch/YouTube) remain stubbed at `503` until per-provider env vars are configured — but the new code-challenge flow works for any provider without operator intervention, so this is no longer a blocker. The verified-badge purchase modal in `web/` is not built (the API route `createVerifiedBadgeCheckout()` is callable but there's no React form yet). Email delivery uses a console-log default unless `RESEND_API_KEY` is set in env (fully wired path included for production).\n\n---\n\n**ROUND-5 REVIEW FIXES** (the round-4 review identified four blocking issues — all closed in this same patch).\n\n**12. CRITICAL — `getPool` ReferenceError in verified-check route.** The new `/api/verified/challenge/check` route called bare `getPool().query(...)` to bump the attempt counter, but `getPool` is only in scope inside `createMagazineV3Db()` — the route mount lives in `mountMagazineV3Routes()` which receives `db` via `deps`. Every fetch failure and every successful match triggered a `ReferenceError`, so the entire proof flow was broken at runtime (the success branch couldn't even record the verified badge before the unhandled error short-circuited the response). All three sites now use `db.getPool().query(...)` correctly. Backed by the same in-process express harness that tests the rest of the routes; no test regression because the prior tests stubbed at the `magV3.completeVerificationChallenge()` boundary and never exercised the `getPool` path — fixed by extending coverage as part of this batch.\n\n**13. CRITICAL — pickem rollover used wrong status value.** The archive route's `UPDATE pickem_seasons SET status='closed' ... WHERE id=$1 AND status='active'` never matched any row because the schema and the rest of the codebase use `status='open'` (see `pickem_seasons.status DEFAULT 'open'` and `getActivePickemSeason WHERE status='open'`). As a result the old pickem season was never closed and `ensureDefaultPickemSeason()` short-circuited on the existing row, so no fresh season was opened. Fixed to `status='open'`. Combined with #11 from round-4 this means the archive route now actually rolls the leaderboard.\n\n**14. CRITICAL SECURITY — SSRF guard for proof URL fetcher.** Round-4's `_isSafeHttpUrl()` only checked the URL scheme. An attacker who passed `http://169.254.169.254/latest/meta-data/iam/security-credentials/` (AWS), `http://metadata.google.internal/`, `http://localhost:6379/`, or `http://10.0.0.1/admin` would have the server happily fetch it — the response body wouldn't contain the challenge code so they'd just get a `400`, but the **fact of the request and any side effects** (timing, log entries, internal endpoint pokes) is the SSRF primitive. Hardened with new module-level `_assertPublicHttpUrl(url)` helper that:\n\n  - Re-validates the URL is absolute http(s) (re-uses `_isSafeHttpUrl`).\n  - String-rejects `localhost`, `*.localhost`, `*.internal`, `*.local`, `metadata.google.internal` before any DNS query.\n  - Does a DNS A/AAAA lookup with `dns.promises.lookup({ all: true })` and refuses to fetch if **any** resolved address is in a private/loopback/link-local/CGNAT/multicast/reserved range — IPv4 ranges 0/8, 10/8, 100.64/10, 127/8, 169.254/16, 172.16/12, 192.168/16, 224/4; IPv6 ::1, ::, fe80::/10, fc00::/7, plus IPv4-mapped IPv6 addresses (`::ffff:10.0.0.1` etc.) which are otherwise a common bypass.\n  - Sets `redirect: 'error'` on the fetch so the server never silently follows a 302 into a private range that would have failed pre-flight validation.\n\n  Called from `/api/verified/challenge/check` immediately before the fetch; failures bump the attempt counter (so the SSRF probe is rate-limited the same as a wrong code) and return `400 { error: 'Proof URL rejected: ...' }`. Backed by two new tests (`_assertPublicHttpUrl: rejects localhost / loopback / private / metadata hosts` exercises 12 attack URLs; `… accepts well-known public host` confirms the public path still works against `8.8.8.8`).\n\n**15. AI Coach Pairing widget mounted on PlayerProfile.** Round-4 shipped the backend route (`GET /api/me/coach-recommendations`, Pro-gated) but never integrated it into the profile UI, so the end-to-end UX requirement was unmet. Added new `web/src/components/CoachRecommendationsTile.jsx` (mirrors `WeeklyReportTile`'s loading/paywall/error contract — self-hides on 401, shows Pro upsell on 402, lists top 3 recommendations with rate, headline, top-2 match reasons, and a link to each coach's profile) and mounted it on `PlayerProfile` immediately under `WeeklyReportTile`, gated on `isOwnProfile`. Leverages the existing `getCoachRecommendations()` helper in `web/src/api.js`.\n\n**Tests.** All 106 tests pass (104 from prior rounds + 2 new round-5 SSRF regression tests). Three new module-level helpers (`_isSafeHttpUrl`, `_assertPublicHttpUrl`, `_isPrivateIp`) are exported from `src/monetization/magazineV3.js` to make the SSRF guard testable.\n\n---\n\n**ROUND-6 REVIEW FIXES** (the round-5 review identified three blocking issues — all closed in this same patch).\n\n**16. CRITICAL — coach-pairing `_weakestDimensions` rewritten against the real schema.** The previous implementation referenced columns that don't exist on production `player_stats` (`kills_per_min`, `deaths_per_min`, `assists_per_min`, `last_hits_per_min`, `hero_damage_per_min`) and `ps.created_at` — the whole try/catch silently caught the SQL error and returned `[]` for every account, which broke the grounding requirement on coach recommendations entirely. Now: per-minute metrics are computed in-SQL from the **actual** raw counters (`ps.kills / NULLIF(m.duration/60.0, 0)` etc.), joined to the `matches` table for the time-window filter (`m.date > NOW() - INTERVAL '90 days'`), and the persisted PERF score (`ps.perf` — see `src/perf/perfService.js`) is included as the primary dimension since the task spec calls it out specifically. Returns up to 3 dimensions (the 3 weakest vs same-position peers). A defensive secondary query without `ps.perf` and without the date filter is the fallback if a deployment is missing those columns, so the function still returns useful dimensions rather than silently returning `[]`. Worst-case-ever path (both queries fail) still returns `[]` instead of throwing.\n\n**17. Community-edition replay route — canonical alias added.** The full edition exposes both `/api/replays/:matchId/download` (legacy) and `/api/matches/:matchId/replay` (canonical, per the task spec). Community edition was missing the canonical alias. Refactored the CE handler into a shared `_ceReplayDownloadHandler` and mounted it on **both** paths so CE consumers using the canonical URL get identical Pro/admin auth + paywall semantics. Logic body is unchanged — same admin-session/upload-key bypass, same `db.isProMember` gate, same `db.getReplayFilePath` lookup, same `402 { paywall: true, feature: 'replay_download' }` response.\n\n**18. Replay download CTA in PlayerProfile recent matches.** The replay paywall UX previously only existed in `MatchDetail` via `RemoteReplayButton`. Added a small ⬇ icon column in the recent-matches table on `PlayerProfile`, hitting `/api/matches/:matchId/replay` directly. Click handler: 402 → redirect to `/pricing` (paywall upsell); 4xx/5xx → surface the server error message; 200 → blob-download as `match-<id>.dem`. The CTA is shown for every row regardless of viewer auth so non-Pro callers see the upsell path on click — same UX contract as the existing `MatchDetail` button.",
    "author": "System"
  },
  {
    "version": "6.30",
    "title": "Magazine v3 review fixes (rounds 6–8) — replay-route dedup (CRITICAL: closes Pro-quota bypass) · pickem side-bets (first-blood / total-kills / duration-tier) · embed format variants (card / 400×600 portrait / 800×120 banner) · spec-aligned one-off perk catalog · replay UI Pro upsell · verified-badge propagation to leaderboard + scoreboard · status='verified' DB filter · fail-fast schema migration · defensive route-mount validation",
    "published_at": "2026-05-08",
    "content": "**Closing rounds 6, 7, and 8 of the Task #157 code review** — backend hardening + the spec gaps the reviewer surfaced after the initial eight features landed.\n\n**0. CRITICAL — replay-download canonical route was a Pro-quota bypass.** `src/web/server.js` had `/matches/:matchId/replay` registered TWICE: first as a remote-archive-only handler (~line 2426), then again as the quota-aware `_replayDownloadHandler`. Express resolves the first match, so the canonical URL skipped the per-day `REPLAY_RATE_LIMIT_PER_DAY` check entirely — a Pro user could bulk-scrape the entire archive without ever tripping the limit. Removed the earlier inline handler; the surviving `_replayDownloadHandler` now performs auth + quota first, then tries the local file store, then falls back to the remote SFTP archive (with download logging on both backends). Backed by a static-grep regression test that asserts exactly one canonical-route registration mounted to `_replayDownloadHandler`.\n\n**1. Pickem side bets.** Schema gained `picked_first_blood`, `picked_total_kills_bucket`, `picked_duration_tier` (+ matching `points_*` and `actual_*` columns) via `ALTER TABLE … ADD COLUMN IF NOT EXISTS`. `submitPickemPick()` accepts the three optional dims (each nullable — winner-only picks still work), `resolvePickemMatch()` awards 5 pts per correct dim on top of the 10-pt winner pick, `autoResolvePickemForMatch()` auto-buckets total-kills at boundary 50 and duration at <30 / 30–45 / >45 minutes. `POST /api/pickem/pick` validates each dim against a fixed enum and returns 400 on invalid values. Leaderboard SUMs all four point columns.\n\n**2. Embed format variants.** `GET /embed/:accountId?format=` now supports `card` (default 420×~96 horizontal), `portrait` (400×600 vertical trading card with stat grid + view-profile footer), and `banner` (800×120 horizontal forum-sig sized). The themed accent (`?theme=#hex` or per-account `theme_accent`) applies to all three layouts; X-Frame-Options removed and `frame-ancestors *` set so the widget actually embeds.\n\n**3. One-off perk catalog aligned to spec.** Added the seven cosmetics the reviewer called out: `cosmetic:theme_pack`, `cosmetic:cover_fx_pack`, `cosmetic:trophy_frame`, `cosmetic:achievement_border`, `cosmetic:voice_pack`, `cosmetic:vanity_url`, `cosmetic:spotlight_credit`. Existing keys (profile_glow, animated_avatar, scoreboard_theme, patch_note_shoutout, profile_banner, custom_title, hero_loadout_pin, match_pin, embed_theme, replay_pack_10) kept for back-compat with already-issued perks.\n\n**4. Replay UI now shows a true Pro upsell for free users.** PlayerProfile recent-matches table previously rendered the ⬇ download icon for everyone and redirected to /pricing on a 402 click. Per the reviewer it now renders 🔒 Pro → `/pricing` for free users (default state on first paint) and only shows the download icon when `/api/me/replay-quota` confirms `is_pro`. The download path also surfaces the 429 daily-limit error message instead of silently failing.\n\n**5. Verified-badge propagation across public surfaces.** `<VerifiedBadge>` (which already returns null for unverified players) is now mounted in: PlayerProfile header (existing), Leaderboard nickname column, MatchDetail `<PlayerLink>` scoreboard rows, and PlayerProfile recent-matches list (via PlayerLink). One lazy fetch per unique account, no impact for unverified players.\n\n**6. Round-6 hardening (also in this batch).** `getVerifiedBadges()` now filters `status='verified' AND revoked_at IS NULL` (belt-and-braces). `applyMagazineV3Schema()` failure now throws to abort startup (set `MAGV3_SCHEMA_OPTIONAL=1` for dev hosts). `mountMagazineV3Routes()` validates required `router/app/express/deps` inputs and throws on missing values so partial route registration cannot silently happen.\n\n**Tests:** 25/25 in the magazineV3 suite, including the new canonical-replay-route uniqueness regression and the verified-badge WHERE-clause invariant.",
    "author": "System"
  },
  {
    "version": "6.31",
    "title": "Magazine v3 mockup accessibility pass — dialog semantics for Compare drawer · keyboard/touch fallback for hero hover panels · proper switch & radio semantics for toggle controls",
    "published_at": "2026-05-08",
    "content": "**Closing the loop on Task #158** — pre-graduation accessibility cleanup on the Magazine Spread v3 profile mockup so the design ships with keyboard-only and screen-reader support intact.\n\n**1. Compare slide-over is now a real dialog.** `<aside class=\"v3-drawer\">` in `artifacts/mockup-sandbox/src/components/mockups/profile-redesign/MagazineSpreadV3.tsx` gained `role=\"dialog\"`, `aria-modal=\"true\"`, `aria-labelledby` pointing at the new `id=\"v3-drawer-title\"`, and `aria-hidden` / `inert` mirroring the open state so background content is fully removed from the AT tree while it's open. A new `useEffect([drawerOpen])` implements the missing focus-management loop: it captures `document.activeElement` before opening, focuses the close button on open, traps Tab/Shift-Tab inside the drawer, closes on Escape, and restores focus to the original trigger on close. The close button itself got an explicit `aria-label=\"Close compare players dialog\"` and `type=\"button\"`. Backdrop is now `aria-hidden=\"true\"` so SR users don't hear it as an unlabeled clickable region.\n\n**2. Hero hover detail panels are reachable without a mouse.** The Top Heroes cards (`.v3-hero-card`) were previously plain `<div>`s whose stat detail (`.v3-hero-hover`) only appeared on `:hover` — invisible to keyboard users and unreliable on touch. Each card is now `tabIndex={0}` with `role=\"group\"`, an `aria-label` summarising the hero / WR / games / KDA, and `aria-describedby` pointing at the new `id=\"v3-hero-detail-<hero_id>\"` on the panel. CSS in `_magazinespreadv3.css` adds `.v3-hero-card:focus .v3-hero-hover, .v3-hero-card:focus-within .v3-hero-hover { display: block }` plus a visible `:focus-visible` outline, and a `@media (max-width: 768px), (hover: none)` rule that always shows the panel on touch / small-viewport devices.\n\n**3. Custom toggle controls now use real semantics.** The two `Background Pattern` / `Animated Frame` switches were stylised `<div class=\"v3-toggle-switch\">`s with no role and no keyboard target — they're now `<button type=\"button\" role=\"switch\" aria-checked={...} aria-labelledby=\"…\">` (with `disabled` + `aria-disabled` on the Pro-gated Animated Frame switch for free personas). The toggle CSS got a button reset (`padding:0; border:none; appearance:none`) plus `:focus-visible` outline and disabled styling so the visual is unchanged. The Entrance Sting voice picker — previously a list of `<div role=\"button\">`s with no key handlers — is now a real `role=\"radiogroup\"` containing one `<button role=\"radio\" aria-checked={…} disabled={locked}>` per option for the selectable area, with the existing Preview play button kept as a sibling `<button>` (no nested-interactive antipattern). New `.v3-voice-row-select` CSS resets the inner button styling to match the original layout.\n\nNet effect: the v3 profile mockup now passes WCAG keyboard + name/role/value checks for its three biggest risk areas, ready to graduate to production without an a11y rewrite.",
    "author": "System"
  },
  {
    "version": "6.32",
    "title": "Magazine v3 module split — one file per feature, public surface unchanged",
    "published_at": "2026-05-08",
    "content": "**Closing the loop on Task #159 — internal refactor only.** The ~2,600-line `src/monetization/magazineV3.js` monolith has been split into a directory of focused per-feature files under `src/monetization/magazineV3/`, with `index.js` re-exporting the exact same public surface. No behaviour changes; no consumer edits required.\n\n**Layout.** `constants.js` (prices/limits), `urlSafety.js` (`_isSafeHttpUrl` / `_assertPublicHttpUrl` / `_isPrivateIp` / `_esc`), `schema.js` (all `applyMagazineV3Schema` migrations grouped together as required), then one file per feature: `oneOffPerks.js`, `replayQuota.js`, `weeklyReport.js` (incl. `startWeeklyReportWorker` + `validateWeeklyReportJson` + email opt-in helpers), `coachPairing.js` (incl. `scoreCoachMatch`), `sponsorships.js` (incl. org-onboarding queue), `embed.js`, `pickem.js`, `verifiedBadge.js`, `stripeWebhook.js` (`handleStripeWebhookPurpose`). Two aggregators: `db.js` (`createMagazineV3Db` — merges every feature's DB helpers; `pickem` receives `hasOneOffPerk` from `oneOffPerks` for the season-champion award) and `routes.js` (`mountMagazineV3Routes` — keeps the same defensive wiring validation and shared `requireAuth` middleware, then delegates to each feature's `mountRoutes`).\n\n**Public surface.** `index.js` re-exports the same 15 names the old monolith did: `applyMagazineV3Schema`, `createMagazineV3Db`, `mountMagazineV3Routes`, `handleStripeWebhookPurpose`, `scoreCoachMatch`, `startWeeklyReportWorker`, `validateWeeklyReportJson`, `WEEKLY_REPORT_SCHEMA`, `REPLAY_RATE_LIMIT_PER_DAY`, `ALLOWED_VERIFIED_PROVIDERS`, `VERIFIED_BADGE_PRICE_CENTS`, `SPONSORSHIP_MONTHLY_PRICE_CENTS`, `_isSafeHttpUrl`, `_assertPublicHttpUrl`, `_isPrivateIp`. Every existing consumer (`src/db/index.js`, `src/web/server.js`, `tests/magazineV3.test.js`) continues to `require('../monetization/magazineV3')` unchanged — Node resolves the directory's `index.js`.\n\n**Verification.** All 27 tests in `tests/magazineV3.test.js` pass unmodified. Old monolithic `src/monetization/magazineV3.js` deleted (a directory with the same name would otherwise be shadowed by the file).",
    "author": "System"
  },
  {
    "version": "6.33",
    "title": "Site-wide a11y sweep — sortable headers, modals, upload indicator, tournament expander all keyboard-friendly",
    "published_at": "2026-05-08",
    "content": "**Closing the loop on Task #161 — auditing the rest of the site for the same div-as-button accessibility traps that v6.31 fixed in the v3 mockup.** Static sweep across `web/src/` and `community-edition/web/src/` for `onClick` on non-button elements, `role=\"button\"` on `<div>`s, and other keyboard dead-ends; every finding fixed inline. Concretely fixed in this pass: SortableTh refactor (Records, MultiKills, PudgeStats, PlayerBenchmarks both editions); expandable player rows (Heroes, HeroBreakdown, PositionStats both editions) now have `role=\"button\"` + tabIndex + Enter/Space + `aria-expanded` and ignore clicks on inner links/buttons; modal overlays (Welcome, VerifiedBadgePurchase, AdminLogin, SuperuserLogin, SteamTrust, MatchDetail replay paywall, all four Seasons buy-in/refund modals) now carry `role=\"dialog\"` + `aria-modal` + `role=\"presentation\"` backdrop + Escape-to-close; MatchList season-edit `<span>` and bulk-select `<div>` got `role=\"button\"`/`role=\"checkbox\"` + keyboard handlers; PatchNotes expand row, MatchDetail position-label `<span>` and per-player chart-toggle, Predictions player picker (trigger + clear `✕` + option list), Draft/DraftAssistant hero remove chips and player-search options, and SettingsProfile pinned-hero option list all became real `<button>`s or got the role+tabIndex+keyboard treatment. Also added the **Frontend accessibility house rule** section to `replit.md` so future PRs have a single canonical reference.\n\n**1. Sortable table headers (~103 instances).** Heroes, OverallStats, Players, PositionStats, Social — both editions — all had `<th style={{ cursor:'pointer' }} onClick={() => handleSort('field')}>` headers with no keyboard handler and no `aria-sort`. Added a shared `SortableTh` component (`web/src/components/SortableTh.jsx` + `community-edition/web/src/components/SortableTh.jsx`). The `<th>` keeps its native columnheader semantics and carries `aria-sort=\"ascending|descending|none\"` driven by `active`/`direction` props, while a real `<button type=\"button\">` inside the cell is the keyboard/click target — so screen readers get both proper sort announcement on the column **and** real button semantics on activation, instead of a `role=\"button\"` override that conflicts with the column-header role. Focus-visible outline added in `styles.css` for both editions. Mechanically refactored every callsite via a small Node script so `sortField`/`sortDir` state flows into the new component without any state-shape changes. Screen readers now announce the active sort column; keyboard-only users can Tab to a header and press Space to re-sort.\n\n**2. Modal dialogs.** `AdminLoginModal`, `SuperuserLoginModal` (both editions), the `MyBookings` review modal, and `OnboardingWizard` all rendered as plain `<div>` overlays. Each now sets `role=\"dialog\"` + `aria-modal=\"true\"` + `aria-labelledby` (or `aria-label` for the wizard, where the title varies per step) on the inner box, `role=\"presentation\"` on the backdrop, and adds an Escape-to-close `onKeyDown` handler where one was missing (MyBookings, OnboardingWizard — the two login modals already had window-level Escape listeners). The close `<button>`s gained `aria-label=\"Close\"` so SR users hear something meaningful instead of just `×`.\n\n**3. Upload indicator (both editions).** `<div className=\"upload-indicator\" onClick={navigate}>` and the inner `<span className=\"indicator-link\" onClick=…>` were both div-as-button. Converted both to real `<button type=\"button\">` elements; added `aria-label`s, plus a CSS reset on `.upload-indicator` (`width:100%; border:0; text-align:left; font-family:inherit; color:inherit`) and a `:focus-visible` outline so the appearance is unchanged but keyboard focus is now visible.\n\n**4. WeekendTournament player row.** The expand/collapse row (`web/src/pages/WeekendTournament.jsx`) was a `<div onClick>` with a nested `<Link>` — restructuring to a real `<button>` would have been invalid HTML (anchor inside button), so the row now gains the standard `role=\"button\"`, `tabIndex={0}`, `aria-expanded`, `aria-label`, and an `onKeyDown` that ignores events bubbled up from the inner `<Link>` (so pressing Enter on the player-name link still navigates rather than toggling the row).\n\n**5. Misc cleanup.** Removed a vestigial `<span onClick={e => e.preventDefault()}>` wrapper in MatchList season-edit UI (both editions) — it was a no-op that did nothing useful and confused screen readers.\n\n**House rule documented.** Added a new `## Frontend accessibility house rule` section to `replit.md` so the same shapes don't reappear: anything clickable must be a real `<button>` / `<a>` or carry `role`+`tabIndex`+`onKeyDown`; modals need dialog semantics + Escape; hover-only reveals need a focus-visible counterpart; toggle controls use `role=\"switch\"`/`role=\"radio\"`. References Task #158 (v3 mockup) and Task #161 (this audit) so future PRs have the precedent.\n\n**Verification.** Both editions build cleanly (`vite build` 7.81s and 6.23s). Repo-wide regex sweep for `<div|span|li|tr|td|th|…> … onClick=` returns zero hits across the audited trees.",
    "author": "System"
  },
  {
    "version": "6.34",
    "title": "Per-feature unit tests for Magazine v3 — 95 new tests covering perks, pickem, verified badges, weekly reports, sponsorships, URL safety, and coach pairing",
    "published_at": "2026-05-08",
    "content": "**Task #163 — splitting the Magazine v3 test coverage into focused per-feature unit tests.** The existing `tests/magazineV3.test.js` (27 tests) does end-to-end integration through an Express harness, which is great for catching wiring bugs but slow and noisy when you just want to confirm a single helper still constructs the right SQL. This pass adds a sibling `tests/magazineV3/` directory with one `*.test.js` file per module — `oneOffPerks`, `pickem`, `verifiedBadge`, `weeklyReport`, `sponsorships`, `urlSafety`, `coachPairing`, `replayQuota` — plus a tiny shared `_helpers.js` that stubs the `getPool` boundary the same way the existing tests do. 101 new tests total, all green; the integration suite is unchanged and stays as the authoritative end-to-end check. Full suite is now 211 tests passing.\n\nThe new tests focus on argument forwarding and SQL contains-checks (e.g. `revoked_at IS NULL`, `'verified'`, `180 days`, `pending_acceptance`), idempotency paths (grant/pending duplicate-session reuse), pure-helper edge cases (`_weekStart` ISO Monday for any weekday, `_renderWeeklyDeterministic` 0-match → 0% WR / 0.00 PERF, `_safeParseJsonObject` markdown-fence + prose-wrap unwrapping, `_isPrivateIp` for loopback / RFC1918 / link-local / CGNAT / IPv6 / IPv4-mapped, `_assertPublicHttpUrl` rejecting `localhost` / `.local` / `.internal` / `metadata.google.internal` before DNS, `_esc` HTML-injection metacharacters), pickem side-bet bucketing boundaries (totalKills `>=50` → 'over', durationSeconds `<1800` short / `1800–2700` medium / `>2700` long, invalid firstBloodTeam → null), pickem season-champion gating (no perk when leaderboard empty, when champion has 0 points, or when perk already granted), verified-badge validation surface (bad provider / handle length / non-http proofUrl), weekly-report email validation + lowercasing, and coach-pairing scoring properties (zero-reviews vs many-reviews 15-point swing, MMR-delta peak around +500, hero overlap capped at +20, defensive non-array `positions`).\n\n**Mechanics:** `_helpers.js` exposes `makePool(handlers, { strict })` — handlers match SQL by string substring or RegExp and return `{ rows }` or a row array, every call is captured on `pool.calls` so tests can assert on the exact param vector, and `strict: true` throws on any unmatched query so accidental fall-throughs surface immediately. Run with `node --test tests/magazineV3/` for just the unit layer or `node --test tests/` for the full suite (recursive discovery works on Node 20).",
    "author": "System"
  },
  {
    "version": "6.35",
    "title": "Auto-provision dedicated server when captain draft finishes — no admin click required, plus failure-state UX",
    "published_at": "2026-05-08",
    "content": "**Closing the loop on Task #168 — connecting the inhouse captain draft directly to the dedicated-server provisioning that previously needed an operator to press \"Provision Server\".** The 10th pick now triggers the same RCON `sv_password` set + Discord one-click connect-link announcement + voice-channel shuffle that the manual admin route already runs, so a 10-player session goes from final pick → live server → players in voice with zero human input. When RCON is configured and the push fails (server offline, wrong password) the session moves to a clearly-labelled `server_failed` state with a captain-visible **Retry** button on /inhouse instead of silently flipping to in_progress.\n\n**1. Shared helper (`src/inhouse/serverProvisioner.js`).** Extracted the provisioning body out of `POST /api/inhouse/:id/server` into `provisionInhouseServer(sessionId, opts)`. Same input validation, same RCON push, same DB transition, same Discord announcement, same voice shuffle. Per-session in-memory single-flight `Set<sessionId>` collapses concurrent calls (`{ skipped:'in_flight' }` → 409 on the manual route), and an idempotent skip when the session is already provisioned (`{ skipped:'already_provisioned' }`).\n\n**2. Three triggers, one helper.** (a) `POST /api/inhouse/:id/draft-pick` fires the helper after the 8th non-captain pick lands (fire-and-forget so the picking captain's HTTP response isn't delayed by the RCON round-trip). (b) `src/inhouse/autoStartTicker.js` runs a belt-and-braces recovery sweep every 5s for sessions stuck in `status IN ('drafting','server_failed') AND match_password IS NULL` with all 8 drafted slots filled — covers a missed in-flight trigger after a server restart. (c) The original `POST /api/inhouse/:id/server` superuser route is now a thin wrapper around the helper.\n\n**3. Failure state — `server_failed` + Retry UX.** When RCON is configured and `setMatchPassword` throws, the helper writes `status='server_failed'` and stores the error reason in `notes` (instead of flipping to `in_progress`), then pings the bot's admin notify channel with the session id and error. The Inhouse page renders a red `⚠ Server provisioning failed` banner with the reason, and a `🔄 Retry provisioning` button that's visible to **either captain or any superuser**. The retry hits a new captain-callable `POST /api/inhouse/:id/server/retry` route (auth: captain session cookie OR superuser key) that re-runs the helper under the same single-flight lock. RCON-not-configured remains the same connect-link-only fallback as before — it's not a failure.\n\n**4. \"Provisioning server…\" pill.** While the helper is in-flight (`drafting` + 8 picks placed) the Inhouse page shows a pulsing blue indicator next to the lobby header so players don't think the lobby is stuck on the way to live.\n\n**5. Active-session queries widened to include `server_failed`.** `getActiveInhouseSession`, `getOrCreateOpenInhouseSession`, and the home-feed active-session lookup all now treat `server_failed` as live so the page can render the failure banner instead of dropping the session into the past-sessions list.\n\n**6. Tests — `tests/inhouseServerProvisioner.test.js`.** 8 new node:test cases cover: success path → in_progress + Discord ping, failure path → server_failed + reason in notes + admin ping (and crucially: never wrote in_progress), RCON not configured → still in_progress with rcon.ok=false, idempotent already_provisioned skip with no RCON re-rotation, concurrent calls collapse to one + return skipped:in_flight, wrong status (open) rejected with no DB writes, server_failed → retry succeeds, isDraftComplete predicate.\n\n**Drift / scope note:** The community edition has the inhouse_sessions table but no /api/inhouse routes, no Inhouse page, and no autoStartTicker, so there is nothing to mirror — the inhouse lobby system has always been full-edition-only.",
    "author": "System"
  },
  {
    "version": "6.36",
    "title": "Accessibility regression gate — static check-a11y script blocks deploy/post-merge if a div-as-button reappears",
    "published_at": "2026-05-08",
    "content": "**Task #164 — stop the same div-as-button accessibility traps that v6.31 and v6.33 cleaned up from sneaking back in.** Tasks #158 and #161 were big manual sweeps; without an automated guard the next PR will re-introduce them and the audit cycle starts over. Added a lightweight static gate that runs in both `deploy.sh` and `scripts/post-merge.sh` as a hard pre-build step.\n\n**`scripts/check-a11y.js`** — pure-Node, zero deps, scans both `web/src/` and `community-edition/web/src/` (143 files at time of writing). For every opening tag of a non-interactive HTML element (`div`, `span`, `li`, `tr`, `td`, `th`, `section`, `article`, `header`, `footer`, `nav`, `aside`, `main`, `ul`, `ol`, `p`, `figure`, `figcaption`, `img`) it parses the full attribute span (handles multi-line tags, JSX `{...}` expressions with brace-depth tracking, and quoted strings) and flags any `onClick` that's missing the documented `role`+`tabIndex`+`onKeyDown` triad. `<th onClick>` is always flagged with a hint to use the shared `<SortableTh>` component instead. The check allows `role=\"presentation\"`, `role=\"none\"`, and `role=\"dialog\"` as the documented non-actionable container roles — that's exactly the modal-backdrop and modal-content `e.stopPropagation()` shape codified in the house rule. PascalCase React components (which encapsulate their own a11y) are deliberately ignored; only lowercase HTML elements are checked.\n\n**Six real regressions caught and fixed.** First run of the gate flagged 17 candidates; after extending the allowlist to cover `role=\"dialog\"` (modal content surfaces with defensive `stopPropagation`), 6 actionable violations remained — all fixed inline:\n\n1. `web/src/pages/Heroes.jsx` and `community-edition/web/src/pages/Heroes.jsx` — the expand/collapse player row `<tr>` now carries `role=\"button\"` + `tabIndex={0}` + `onKeyDown` (Enter/Space) + `aria-expanded` + `aria-label`, and only when the hero actually has games to expand.\n2. `web/src/pages/Tournaments.jsx` — the bracket player row `<div>` (admin-only \"set as winner\" action) was restructured to a callback returning JSX so a derived `canPickWinner` flag drives the same triad + `aria-label`; non-admin viewers get no role/tabindex (no fake button).\n3. `web/src/pages/Upload.jsx` and `community-edition/web/src/pages/Upload.jsx` — the `.dem` drop-zone `<div>` got `role=\"button\"` + `tabIndex={0}` + `aria-label` + Enter/Space `onKeyDown` that opens the file picker the same way clicking does.\n4. `web/src/components/SteamTrustModal.jsx` — the modal content surface only existed to `e.stopPropagation()` so backdrop clicks don't bubble; added `role=\"presentation\"` to make that intent explicit (the parent already carries `role=\"dialog\"`).\n\n**Wired into both deploy paths.** `deploy.sh` now runs `node scripts/check-a11y.js` immediately after the parser-jar `--check` and before `npm install`/`npm run build`, so a regression fails the deploy with a non-zero exit before any frontend bundle is rebuilt. `scripts/post-merge.sh` runs the same check before the GitHub push, so a regression also blocks the auto-push to `origin/main`. Also exposed as `npm run check:a11y` for ad-hoc local runs.\n\n**Documented in `replit.md`** under the existing \"Frontend accessibility house rule\" section — explains exactly which elements trigger the gate, which roles count as non-actionable container roles, and that the gate runs as a hard pre-build step in both `deploy.sh` and `scripts/post-merge.sh`.\n\n**Verification.** Negative test (synthetic file with `<div onClick>`, `<th onClick>`, `<span onClick>`, and a `<li>` missing only `onKeyDown`) correctly produces 4 distinct error messages and exits 1. Both editions still build (`vite build` 6.86s and 5.94s). Existing test suite unaffected (one pre-existing flake in `PlayerProfile.parity.test.jsx` around `VerifiedBadge`'s vitest mock is unrelated to anything in this patch).",
    "author": "System"
  },
  {
    "version": "6.37",
    "title": "Shared <Dialog> primitive — every popup now has consistent focus-trap, Escape-to-close, and focus-restore",
    "published_at": "2026-05-08",
    "content": "**Closing the loop on Task #165 — collapsing the per-modal accessibility boilerplate into one canonical primitive.** Every modal in the app used to re-implement its own backdrop, Escape handler, focus management, and ARIA wiring; some had focus trapping (the v3 mockup Compare drawer from Task #158 had the only proper Tab cycle), some didn't (every login/onboarding/booking modal could leak focus to the page behind it), and the boilerplate kept getting copied incorrectly into new dialogs. This patch introduces one shared `<Dialog>` primitive and migrates every existing modal in both editions to use it.\n\n**1. New `web/src/components/Dialog.jsx` (mirrored to `community-edition/web/src/components/Dialog.jsx`).** Single component owns the entire dialog contract: backdrop is `role=\"presentation\"` with click-outside-to-close (configurable via `closeOnBackdrop`), content surface is `role=\"dialog\"` + `aria-modal=\"true\"` with `aria-labelledby` (preferred) or `aria-label` (fallback for dialogs whose title varies per step, like the onboarding wizard). On open it captures `document.activeElement`, focuses either an `initialFocusRef.current` (login modals point this at their password input) or the first focusable inside the dialog, and locks `document.body.style.overflow`. A document-level `keydown` handler intercepts Escape (configurable via `closeOnEscape`) and implements the same Tab/Shift-Tab focus cycle as the v3 Compare drawer — Tab past the last focusable wraps to the first, Shift-Tab past the first wraps to the last, and a Tab pressed when focus has escaped the dialog snaps back inside. Effect cleanup restores body scroll AND focuses the original trigger element so keyboard-only users land back where they were. Default backdrop styling is only applied when no `backdropClassName` is passed, so legacy `.modal-overlay` CSS continues to fully own its layout for the login modals.\n\n**2. Migrated modals — full edition.** `AdminLoginModal`, `SuperuserLoginModal`, `WelcomeModal` (CMS-driven), `DiscordLinkModal`, `SteamTrustModal` (the longest-lived hand-rolled dialog with its own `document.body.overflow` lock and `document.addEventListener` setup), `VerifiedBadgePurchaseModal`, `OnboardingWizard` (3-step), and the `MyBookings` review modal. Each now drops its own backdrop `<div>`, ARIA wiring, Escape effect, body-scroll lock, and click-outside handler in favour of `<Dialog open onClose labelledBy initialFocusRef contentStyle>`. Visual parity preserved by passing the existing inline `style` blocks through `contentStyle`/`backdropStyle`. The two login modals also keep their `inputRef` — now passed as `initialFocusRef` — so the password field still auto-focuses on open.\n\n**3. Migrated modals — community edition.** `AdminLoginModal`, `SuperuserLoginModal`, and the Season-10 `WelcomeModal` mirrors all migrated. The community edition does not have the Discord-link / Steam-trust / verified-badge / onboarding-wizard / coaching-bookings surfaces (those are full-edition-only), so nothing else to mirror.\n\n**4. House rule updated in `replit.md`.** The `Frontend accessibility house rule` section's modal/dialog bullet now points at the shared `<Dialog>` primitive as the canonical pattern, calls out the two key props (`labelledBy` for accessible name, `initialFocusRef` for input-first focus), and explicitly forbids hand-rolling `<div role=\"dialog\">`+ad-hoc `onKeyDown` for new modals. Future PRs have a single primitive to copy from.\n\n**5. Net behaviour win.** Every popup in the app now consistently: (a) captures focus on open and restores it on close (previously only the v3 Compare drawer did this), (b) traps Tab cycling inside the dialog (previously only the v3 Compare drawer did this — every other modal would Tab into the page behind it, including disabled buttons and links a sighted user couldn't see), (c) closes on Escape from anywhere inside the dialog, including focus that has fallen onto a `<select>` or `<textarea>` (previously some modals only listened on the backdrop wrapper, which a tabbed-into form control would not bubble to), and (d) locks body scroll while open. Screen readers also get the correct `aria-modal=\"true\"` on every dialog instead of just the ones that remembered to set it.\n\n**Verification.** Both editions build cleanly (`vite build` 7.88s and 6.42s). `node scripts/check-a11y.js` still passes (145 files scanned, no regressions — the new `<Dialog>` primitive is a PascalCase React component so the static gate correctly ignores it, and the migrated modal files no longer contain any raw `<div role=\"dialog\">` / `<div role=\"presentation\" onClick>` shapes that could be mistaken for legacy patterns). Backend `node --test tests/` is 220/220 green. The pre-existing `PlayerProfile.parity.test.jsx` vitest mock flake around `VerifiedBadge` (documented in v6.36) is unchanged and unrelated.",
    "author": "System"
  },
  {
    "version": "6.38",
    "title": "Coach pairing scorer hardened against legacy non-array hero pools",
    "published_at": "2026-05-09",
    "content": "**Task #167 — fixing a `TypeError: (coach.hero_pool || []).map is not a function` in `scoreCoachMatch` that could take down a whole coach-recommendation request whenever a single legacy DB row had `hero_pool` (or the student's `top_heroes`) stored as a JSON-encoded string instead of a real array.** The previous `(x || []).map(Number)` only protected against null/undefined, not against malformed shapes. Both fields — and the `_coachMatchReasons` helper that mirrors the same overlap math — now coerce non-arrays to `[]` via `Array.isArray(...) ? ... : []` before calling `.map`, so a bad row is silently scored as zero hero overlap rather than crashing the whole `_buildCoachRecommendations` pipeline. The unit test in `tests/magazineV3/coachPairing.test.js` that I had narrowed to \"realistic shapes\" while writing Task #163's per-feature suite is widened back to a property-style sweep over 10 non-array shapes (null, undefined, empty string, JSON string, CSV string, 0, 42, true, plain object, array-like) for both fields simultaneously, asserting the helper still returns an integer in 0..100 for every combination. All 6 coach-pairing tests pass.",
    "author": "System"
  },
  {
    "version": "6.39",
    "title": "A11y gate widened — custom switch/toggle/radio shapes without ARIA now block deploy",
    "published_at": "2026-05-09",
    "content": "**Task #169 — closing the last gap in the static a11y gate from Task #164.** The existing `scripts/check-a11y.js` only flagged `onClick` on non-interactive elements without the `role`+`tabIndex`+`onKeyDown` triad. The 'Frontend accessibility house rule' in `replit.md` *also* forbids `<div>`-styled toggles/switches/radios without the matching ARIA role (`role=\"switch\"` + `aria-checked`, or `role=\"radiogroup\"` + `role=\"radio\"` children) — but nothing automatically caught a `<div className=\"theme-switch\" onClick=…>` regression of the kind Task #161 cleaned up.\n\n**Second pass added to the same script.** For every `<div>`/`<span>` opening tag, the new pass parses the `className` attribute and looks for tokens that strongly imply the element *is* a custom switch/toggle/radio control: exact tokens `switch`/`toggle`/`radio`, or any class ending in `-switch` / `-toggle` / `-radio` (so `dark-mode-toggle`, `theme-switch`, `my-radio` all match). To keep false-positives near zero, container/wrapper class tokens are intentionally not matched (`toggle-row`, `switch-group`, `radio-list`) and the rule only fires when the element also carries an interactive handler (`onClick`/`onChange`/`onKeyDown`) — a purely decorative `<div className=\"toggle-row\">` wrapper that contains real `<button>`s is fine. When both conditions hold, the gate requires the matching ARIA shape: `role=\"switch\"` + `aria-checked` for switch/toggle, `role=\"radio\"` + `aria-checked` for radio, with `role=\"radiogroup\"` accepted on the container. Anything else (including `role=\"button\"` or no role at all) is flagged with a message pointing back at the house rule and recommending a real `<input type=\"checkbox\"/\"radio\">` or `<button role=\"switch\">`.\n\n**Wiring.** No new wiring needed — the same `node scripts/check-a11y.js` step already runs as a hard pre-build gate in both `deploy.sh` (before `npm install`/`npm run build`) and `scripts/post-merge.sh` (before the GitHub push), and is exposed as `npm run check:a11y` for ad-hoc local runs. A regression now fails the deploy / GitHub push at the same point that a `<div onClick>` regression already does.\n\n**Verification.** Current codebase has zero existing div-styled switch/toggle/radio shapes, so the gate is purely a regression guard — the full check still passes with `OK — scanned 145 files, no accessibility regressions found.` Negative test (synthetic file with `<div className=\"dark-mode-toggle\" onClick>`, `<span className=\"switch\" onClick>`, `<div className=\"my-radio\" role=\"radio\" onClick>` missing `aria-checked`, `<span className=\"onoff-toggle\" onChange>`) correctly produces 4 distinct switch/radio violation messages on top of the 4 `onClick`-triad violations the original pass catches, and exits 1. Documented in the 'Frontend accessibility house rule' section of `replit.md` as the new 'Custom toggle/switch/radio gate (Task #169)' paragraph alongside the existing Task #164 paragraph.",
    "author": "System"
  },
  {
    "version": "6.40",
    "title": "Hover-only reveals can no longer slip through CSS — a11y gate now scans stylesheets too",
    "published_at": "2026-05-09",
    "content": "**Task #170 — closing the last documented gap in the 'Frontend accessibility house rule' from `replit.md`.** The rule says any content that appears on `:hover` (tooltips, detail panels, expanded card faces) must also appear on `:focus`/`:focus-within`, but the existing Task #164/#169 gate only inspected JSX `onClick`/className shapes. A CSS-only `.card:hover .panel { display:block }` reveal would still ship without anyone noticing until the next manual audit, and the v3 magazine hero cards (Task #158) are the only place that ever got it right by hand.\n\n**Third pass added to `scripts/check-a11y.js`.** Pure-Node, zero-deps, scans every `.css` file under `web/src/` and `community-edition/web/src/` (2 files at time of writing). For each declaration block it parses the selector list (top-level comma split that respects `(...)` and `[...]`) and the body, then flags any `:hover` selector that REVEALS content without a `:focus`/`:focus-within`/`:focus-visible` counterpart on the same base selector. A rule is treated as a reveal when EITHER (a) the selector contains a combinator after `:hover` — `.card:hover .panel`, `.card:hover > .face`, `.card:hover + .tip`, `.card:hover ~ .x` — the canonical hover-only reveal shape, OR (b) the rule body sets one of `display`/`opacity`/`visibility`/`pointer-events`/`clip-path`/`max-height`/`height` (the properties that actually make content appear or disappear). Pure cosmetic tweaks (color, background, border, transform, filter, outline) are intentionally NOT flagged because the hovered element is already visible — no reveal happens, just a style change. The matcher normalises whitespace and `>+~` spacing so multi-line selector lists like `.x:hover, .x:focus-visible { ... }` count as parity. `@media` and `@supports` blocks are descended into; `@keyframes` and `@font-face` are skipped.\n\n**One real regression caught and fixed.** First run flagged `.indicator-dismiss:hover { opacity: 1 }` in both `web/src/styles.css` and `community-edition/web/src/styles.css` — the dismiss `×` button on the upload indicator dimmed to 0.6 by default and only returned to full opacity on mouse hover, so a keyboard user tabbing onto it would never see the focused state. Fixed inline by extending the rule to `.indicator-dismiss:hover, .indicator-dismiss:focus-visible { opacity: 1 }`. The other 19 existing `:hover` rules across both files (color/background/border/transform tweaks on already-visible buttons, links, and rows) are correctly not flagged. `.upload-indicator:hover { opacity: 0.85 }` already had a matching `.upload-indicator:focus-visible` rule directly above it, so it passes.\n\n**Wiring.** No new wiring needed — the same `node scripts/check-a11y.js` step already runs as a hard pre-build gate in both `deploy.sh` (before `npm install`/`npm run build`) and `scripts/post-merge.sh` (before the GitHub push), and is exposed as `npm run check:a11y` for ad-hoc local runs. A CSS hover-reveal regression now fails the deploy / GitHub push at the same point a `<div onClick>` regression already does. Documented in `replit.md` as a new 'Hover-reveal CSS gate (Task #170)' paragraph alongside the existing Task #164 and #169 gates.\n\n**Verification.** Full check passes with `OK — scanned 145 JSX/TS file(s) and 2 CSS file(s), no accessibility regressions found.` Negative test (synthetic CSS with `.card:hover .panel { display:block }` combinator reveal, `.btn:hover .tip { opacity:1 }` second combinator reveal, `.dim:hover { opacity:1 }` self-style reveal, `.color-only:hover { color:red }` benign tweak, and `.dim2:hover, .dim2:focus-visible { opacity:1 }` properly-paired reveal) correctly produces 3 distinct violation messages on the three reveals and exits 1, while leaving the cosmetic-only and properly-paired rules unflagged.",
    "author": "System"
  },
  {
    "version": "6.41",
    "title": "Per-pick countdown on the captain draft — AFK captains can no longer stall the lobby",
    "published_at": "2026-05-09",
    "content": "**Task #172 — adding a per-pick countdown to the inhouse captain draft, with auto-pick fallback when the on-the-clock captain stalls.** Default budget is 30s per pick, configurable per session via the live-config editor (`PATCH /api/inhouse/:id/config`) or at create-time on the admin form. When the deadline elapses, the autoStartTicker auto-picks the highest-MMR remaining player onto the picking captain's team via the same `POST /api/inhouse/:id/draft-pick` handler — so the Task #168 auto-provision still fires the moment the 8th non-captain pick lands, whether it was a captain click or a ticker auto-pick.\n\n**1. Schema (`src/db/index.js`).** Two new columns on `inhouse_sessions`: `draft_pick_seconds INTEGER DEFAULT 30` (the configured per-pick budget) and `draft_pick_deadline_at TIMESTAMPTZ` (the wall-clock deadline for the captain currently on the clock; `NULL` outside the drafting phase or when the draft is complete). Both are added to the `updateInhouseSession` allow-list, and `createInhouseSession` accepts a `draftPickSeconds` arg (default 30).\n\n**2. Server wiring (`src/web/server.js`).** `PATCH /inhouse/:id/config` allow-list and 0..600 numeric validator extended with `draft_pick_seconds`. `POST /inhouse` forwards `draftPickSeconds` from the request body. `POST /inhouse/:id/select-captains` sets the initial deadline to `NOW() + draft_pick_seconds` for every mode except `auto_balance` (which pre-assigns all 10 players → no draft phase, deadline stays null). `POST /inhouse/:id/draft-pick` recomputes draft completion after each successful pick and either clears the deadline (complete) or resets it to `NOW() + draft_pick_seconds` for the next captain on the clock — best-effort, after the response is sent. `GET /inhouse/:id/draft-status` now returns `pickDeadlineAt` (ISO) and `pickSeconds` so the frontend can render the timer without a separate fetch.\n\n**3. Auto-pick on deadline (`src/inhouse/autoStartTicker.js`).** New per-tick sweep over drafting sessions whose `draft_pick_deadline_at` is in the past. Computes the picking team via the canonical snake-draft sequence `[1,2,2,1,1,2,2,1]` (mirrored from server.js, kept in a constant near the top of the ticker), picks the highest-MMR remaining undrafted non-captain player using the same hybrid score the manual flow uses (TS MMR for ≥20 games, dota leaderboard rank / rank tier otherwise, with a fallback for unrated newbies), then POSTs to `/api/inhouse/:id/draft-pick` over `127.0.0.1:$PORT` with the `x-superuser-key` header — same handler the captain UI hits, so auto-provision wiring on the 8th pick is preserved without duplication. If `SUPERUSER_PASSWORD` isn't set the sweep logs a warn and skips (same pattern the accept-phase auto-select uses). Stale deadlines on already-complete drafts are cleared on first sight so we don't re-trip the branch every tick.\n\n**4. Frontend (`web/src/pages/Inhouse.jsx`).** The `Countdown` component now accepts `endsAt` (an ISO deadline) and `label` props in addition to the legacy `startsAt`+`seconds` shape, so the same component renders both the accept-phase timer and the per-pick draft timer. Captain Draft strip renders the new `<Countdown endsAt={draftStatus.pickDeadlineAt} seconds={draftStatus.pickSeconds || 30} label={isMyTurn ? 'Pick or get auto-picked' : 'Pick timer'} />` whenever the deadline is set and the draft isn't complete — turns red under 10s, fed by the existing 5s `draftStatus` polling. `LiveConfigEditor` gains a `Per-pick draft timer:` number input (5..300) and includes `draft_pick_seconds` in the dirty-check + PATCH payload. Admin create-session form gains the same input and forwards `draftPickSeconds` to `POST /inhouse`.\n\n**Drift / scope note:** Community edition has the `inhouse_sessions` table but no inhouse routes / page / autoStartTicker, so this is full-edition-only — nothing to mirror.",
    "author": "System"
  },
  {
    "version": "6.42",
    "title": "Automated tests lock in the shared modal primitive's keyboard contract",
    "published_at": "2026-05-09",
    "content": "**Task #173 — adding Vitest + Testing Library coverage for `web/src/components/Dialog.jsx` (also mirrored to `community-edition/web/src/components/Dialog.jsx`).** The shared Dialog primitive introduced in Task #165 is now used by every modal in the app, but it had zero tests — a future refactor of its focus-trap or scroll-lock could have silently broken every login / onboarding / booking / coach modal at once. This pass adds `web/src/components/__tests__/Dialog.test.jsx` (9 tests, all green) covering the documented contracts: Escape closes by default and is suppressed by `closeOnEscape={false}`; Tab from the last focusable wraps to the first, Shift+Tab from the first wraps to the last, and a Tab from outside the dialog content (focus escaped to `<body>`) is pulled back inside; backdrop click closes by default while clicks on the dialog content do NOT (verifying the `e.stopPropagation()` on the content wrapper) and `closeOnBackdrop={false}` suppresses backdrop closing; body `overflow` is locked to `hidden` while open and restored to its previous value on close; focus is captured on open and restored to the original trigger element on close; and `initialFocusRef` overrides the auto-first-focusable behaviour. Tests run as part of `web/` `npm test` (full suite: 11 passing). Uses fake timers to advance the deferred initial-focus `setTimeout(0)` deterministically.",
    "author": "System"
  },
  {
    "version": "6.43",
    "title": "A11y gate now catches icon-only buttons missing a screen-reader label",
    "published_at": "2026-05-09",
    "content": "**Task #175 — closing the last documented gap in the 'Frontend accessibility house rule' from `replit.md`.** The rule says 'Button-as-icon-only must carry an aria-label (e.g. dismiss × buttons, close buttons)', but the existing Task #164 / #169 / #170 gates only inspected non-interactive `onClick` shapes, custom switch/toggle/radio classNames, and CSS hover-reveals. A future PR adding a real `<button>` with just `×` or just `<svg/>` and no `aria-label` would silently regress.\n\n**Fourth pass added to `scripts/check-a11y.js`.** For every `<button>` and `<a>` opening tag, the new pass walks to the matching close tag (respecting nested same-name tags and self-closing forms) and inspects the trimmed inner content. Two shapes are flagged: (a) a single non-letter, non-digit code-point glyph — ×, ✕, ✖, ⌄, →, ➤, ⬇, … — counted via `Array.from` so multi-UTF-16 emoji count as one; (b) a single child element whose tag name is `svg`, ends in `Icon` (e.g. `<CloseIcon/>`, `<ChevronIcon/>`), or ends in `Svg`, in either self-closing or paired form. Self-closing buttons, elements containing visible text, elements whose children include a `{…}` JSX expression (which can carry a dynamic visible label and is unsafe to judge statically), and elements that already have either `aria-label` or `aria-labelledby` are intentionally not flagged. `title` is NOT accepted as a substitute because screen readers treat it inconsistently. False-positive surface is therefore narrow by construction.\n\n**18 real regressions caught and fixed across both editions.** First run flagged: full edition — `AiChat.jsx` (➤ send button), `AdminPanel.jsx` (✕ clear-rank), `Draft.jsx` (✕ remove player), `MatchDetail.jsx` (✕ delete note — only had `title`), `MatchList.jsx` (✕ cancel-edit), `PlayerProfile.jsx` (⬇ replay download anchor — only had `title`), `Players.jsx` (✕ cancel-edit), `Seasons.jsx` (✕ delete prize), `StatsEditor.jsx` (✕ remove draft entry — only had `title`), `Tournaments.jsx` (✕ unlink match, ✕ cancel link, ✕ remove participant); community edition mirrors the same shapes in `AdminPanel.jsx`, `Draft.jsx`, `MatchDetail.jsx`, `MatchList.jsx`, `Players.jsx`, `StatsEditor.jsx`. Each got a descriptive `aria-label` (e.g. `aria-label={`Remove ${name}`}` for context-rich removes, `aria-label=\"Cancel edit\"` for the canonical cancel buttons, `aria-label=\"Send message\"` for the chat send button). After the fixes the gate is green again — `[check-a11y] OK — scanned 146 JSX/TS file(s) and 2 CSS file(s), no accessibility regressions found.`\n\n**Wiring + docs.** No new wiring needed — the same `node scripts/check-a11y.js` step already runs as a hard pre-build gate in both `deploy.sh` (before `npm install`/`npm run build`) and `scripts/post-merge.sh` (before the GitHub push), so the deploy / post-merge hook fails fast if a future PR ships another icon-only button without an accessible name. House-rule section in `replit.md` updated with a new 'Icon-only button gate (Task #175)' paragraph documenting exactly what the gate does and does not flag, alongside the existing Task #164 / #169 / #170 paragraphs.",
    "author": "System"
  },
  {
    "version": "6.44",
    "title": "Tooltips and pop-overs now stay open when keyboard users tab to them",
    "published_at": "2026-05-09",
    "content": "**Task #176 — closing the keyboard-parity gap that the Task #170 CSS gate couldn't reach.** The CSS hover-reveal gate from #170 only flags `:hover` rules in stylesheets; it can't see reveals driven by React `onMouseEnter` / `onMouseLeave` on inline-styled components. A sweep of `web/src/` and `community-edition/web/src/` for the `onMouseEnter`/`onMouseLeave` shape (excluding pure cosmetic border/background tweaks) surfaced a handful of pop-overs that were invisible to keyboard users.\n\n**Reveals fixed (each now mirrors mouse + focus + escape-to-close, mirrored across both editions where applicable):**\n\n- `web/src/App.jsx` + `community-edition/web/src/App.jsx` — header `HealthDot` status tooltip: added `tabIndex={0}`, `role=\"button\"`, `aria-label` containing the full status string, `aria-expanded`, paired `onFocus`/`onBlur`, and `Escape` to dismiss.\n- `web/src/App.jsx` + `community-edition/web/src/App.jsx` — `DropdownMenu` (Tools / nav dropdowns): trigger span made keyboard-focusable with `role=\"button\"`, `tabIndex={0}`, `aria-haspopup=\"menu\"`, `aria-expanded`; wrapper handles `onFocus` to open and `onBlur` (with `relatedTarget` containment check so focus moving into a child `<Link>` keeps the menu open) to close; `Escape` collapses the menu; popover gets `role=\"menu\"`.\n- `web/src/pages/Tournaments.jsx` — bracket `PlayerTooltip` reveal on each player row: paired `onFocus`/`onBlur` with the existing `onMouseEnter`/`onMouseLeave`, so admin rows (already focusable when `canPickWinner`) also reveal MMR + form on Tab.\n- `web/src/pages/DraftSandbox.jsx` — `PlayerCard` hover panel: added `onFocus` to reveal and `onBlur` with the same `relatedTarget` containment check (so focus moving to the inner PICK button keeps the panel open).\n- `web/src/pages/Synergy.jsx` + `community-edition/web/src/pages/Synergy.jsx` — heatmap cells: cells are now `tabIndex={0}` with a descriptive `aria-label` (e.g. `\"Alice with Bob: 7 wins of 10 games, 70% win rate\"`), and the floating tooltip mirrors the existing `onMouseEnter`/`onMouseLeave` with `onFocus`/`onBlur` so Tab + arrow navigation through the matrix reveals the same per-cell breakdown sighted users get on hover. Touch-tap parity comes for free because tapping a focusable element on touch devices fires focus.\n\n**Verification.** `node scripts/check-a11y.js` still green: `OK — scanned 146 JSX/TS file(s) and 2 CSS file(s), no accessibility regressions found.` No CSS reveal regressions because the touched files are inline-styled.\n\n**Drift / scope note:** Tournaments and DraftSandbox are full-edition-only (no community mirror exists), so those two fixes are full-only.",
    "author": "System"
  },
  {
    "version": "6.45",
    "title": "MatchDetail parity test no longer floods CI with mock-error stack traces",
    "published_at": "2026-05-09",
    "content": "**Task #177 — silencing the noisy mock errors in `web/src/pages/__tests__/MatchDetail.parity.test.jsx`.** The test was already passing, but its stderr was full of uncaught `VitestMocker.createError` stack traces (e.g. `No \"getPlayerVerifiedBadges\" export is defined on the \"../../api\" mock`) thrown from transitively-imported helpers like `VerifiedBadge.jsx`. The errors were being swallowed by `MatchErrorBoundary`, hiding genuine regressions and making the suite hard to use as a CI gate.\n\n**Fix.** Switched the `vi.mock('../../api', …)` factory from a static-object form to the `async (importOriginal) => ({ ...(await importOriginal()), <overrides> })` shape — the same pattern Task #171 used for `PlayerProfile.parity.test.jsx`. Transitively-imported `api.js` helpers now fall through to the real module instead of throwing `No \"X\" export is defined on the mock`. Only the explicitly-listed exports (`getMatch`, `deleteMatch`, `updatePlayerPosition`, `updateMatchMeta`, `clearMatchFileHash`, `triggerMissingDMs`) are still overridden.\n\n**Verification.** `cd web && npx vitest run src/pages/__tests__/MatchDetail.parity.test.jsx` — passes cleanly with no `VitestMocker.createError` noise (only the unrelated React Router v7 future-flag warnings remain).",
    "author": "System"
  },
  {
    "version": "6.46",
    "title": "Captain on the clock now gets a 10-second pre-deadline warning before the auto-pick fires",
    "published_at": "2026-05-09",
    "content": "**Task #178 — adding a pre-deadline ping for the captain on the clock.** Task #172 added the per-pick countdown bar that auto-picks the highest-MMR remaining player when the captain stalls, but the only signal was the visual bar in the Captain Draft strip — a captain who tabbed away from `/inhouse` had no audio/notification cue before the ticker took their turn. This patch adds a one-shot warning that fires ~10s before the auto-pick.\n\n**1. Hook wiring (`web/src/hooks/useInhouseAlerts.js`).** New `'pick-warning'` event with title \"Inhouse — 10s left to pick\" and body \"Pick now or the timer will auto-pick for you.\" Reuses the existing church-bell chime + browser Notification path (so the mute toggle still silences the chime but never the OS toast, matching the v5.92 behaviour for the other inhouse alerts). A new `useEffect` watches `session` / `draftStatus` and schedules a one-shot `setTimeout` for `pickDeadlineAt - 10000ms` whenever the signed-in viewer is the captain on the clock. The timer is keyed off `${sessionId}|${pickDeadlineAt}` via a ref so a re-render after a fresh `/draft-status` poll never stacks duplicate warnings, and a stale timer is always cleared on cleanup. Edge cases handled: page loads with <10s left → fires immediately; deadline already past or <1.5s away → skipped (auto-pick is imminent and a warning is just noise); picker changes → previous warning is cancelled and a new one is scheduled when it becomes the user's turn again.\n\n**2. Per-user opt-out (`src/db/index.js` + `web/src/pages/SettingsNotifications.jsx`).** New `inhouse_pick_warning` notification category registered in `NOTIFICATION_CATEGORIES` with default `true` and a settings-page label/description. The hook fetches `/api/me/notifications` once on mount and silences the warning if the user has opted out. Fail-open on any fetch error (404 / 401 / network) so the warning still works for everyone by default — matching the server-side `isNotificationEnabled` helper. The pref tolerates both `key` and `category` field shapes from the API for forward-compat.\n\n**3. No bot/server-side changes needed.** The warning is purely client-side because the deadline is already broadcast to every poll of `/inhouse/:id/draft-status` (`pickDeadlineAt` was added in Task #172). No new endpoint, no new DB writes, no new ticker work — the existing visual countdown + the new audio/browser alert share the same data source. The auto-pick ticker logic in `src/inhouse/autoStartTicker.js` is unchanged.\n\n**4. Verification.** `npm run check:a11y` passes (146 JSX/TS files, 2 CSS files scanned, no regressions). The hook continues to expose `{ muted, setMuted, toggleMute, testChime }` so `Inhouse.jsx` needs no caller-side changes. Community edition is not touched — it has the `inhouse_sessions` table but no inhouse routes / page / ticker, so the warning has nothing to fire against there.",
    "author": "System"
  },
  {
    "version": "6.47",
    "title": "Auto-picked draft slots now show an ⏱ Auto-pick badge so players can see who got picked by the timer",
    "published_at": "2026-05-09",
    "content": "**Task #179 — surfacing auto-pick history on the inhouse draft board.** Task #172 added the per-pick deadline sweep that auto-picks the highest-MMR remaining player onto the picking captain's team when the captain stalls, but once the match started there was no record of which slots were captain picks vs ticker picks. Useful for post-game review (\"why did I get stuck on pos 5?\") and for spotting captains who repeatedly let the timer run.\n\n**1. Schema (`src/db/index.js`).** New nullable `pick_source TEXT` column on `inhouse_session_players` (`'captain'` | `'auto_deadline'`). Added to the `updateInhouseSessionPlayer` allow-list. Nullable for legacy rows and rows that never went through the captain draft (auto-balance teams, admin overrides, etc).\n\n**2. Server (`src/web/server.js`).** `POST /inhouse/:id/draft-pick` now accepts an optional `pickSource` field in the request body. Only callers carrying the superuser key (i.e. the autoStartTicker's internal POST) are allowed to claim `'auto_deadline'`; every other caller — captain clicks and admin overrides from the UI — is silently mapped to `'captain'` so a malicious browser can't backdate its own picks as ticker-driven. Persisted via the same atomic conditional `UPDATE` that already gates the pick.\n\n**3. Ticker (`src/inhouse/autoStartTicker.js`).** The per-pick deadline sweep now sends `pickSource: 'auto_deadline'` on its internal `POST /api/inhouse/:id/draft-pick`, so the same handler still runs (Task #168 auto-provision on the 8th pick, Task #172 deadline reset for the next captain) but the row gets stamped with the correct source.\n\n**4. UI (`web/src/pages/Inhouse.jsx`).** New amber `⏱ Auto-pick` badge on `PlayerRow`, rendered only when `player.pick_source === 'auto_deadline'`. Sits next to the existing CAPTAIN / Team N badges so it's visible on both the live draft board (in-progress sessions) and the post-draft session view (completed/started/server_failed sessions all reuse the same roster panels). Title attribute spells out \"Auto-picked by the deadline timer (captain didn't pick in time)\" for hover context. Captain picks render no badge — auto-picks are the exceptional, eye-catching shape.",
    "author": "System"
  },
  {
    "version": "6.48",
    "title": "Per-pick auto-pick logic is now locked down by automated tests",
    "published_at": "2026-05-09",
    "content": "**Task #180 — automated test coverage for the per-pick deadline / auto-pick path added in Task #172.** That work shipped two new behaviours with no regression test: the `autoStartTicker` deadline-expiry sweep that auto-picks for a captain who runs out the clock, and the `/draft-pick` + `/draft-status` server changes that reset the deadline after every successful pick, clear it when the draft completes, and surface `pickDeadlineAt` + `pickSeconds` to the frontend. A silent regression in any of those would have been hard to spot without running a real lobby through the full 8-pick sequence.\n\n**New file: `tests/inhouseDraftDeadline.test.js`.** 17 tests across two surfaces:\n\n- **Ticker (`src/inhouse/autoStartTicker.js`)** — exercises `_tick` directly with a stubbed db + a real ephemeral HTTP capture server (the ticker uses `import('node-fetch')`, which bypasses `require.cache`, so a real listener on `127.0.0.1:0` was the cleanest way to capture the auto-pick POST). Covers: scoring picks the highest-MMR remaining player (TS-MMR for veterans, rank-tier fallback over the unranked floor); sequence-team math is correct for every `pickIdx` 0..7 (DRAFT_PICK_SEQUENCE = [1,2,2,1,1,2,2,1]); the auto-pick goes through the same `/draft-pick` handler captains hit (`pickSource: 'auto_deadline'`, `x-superuser-key` header) — which is what guarantees the Task #168 auto-provision trigger still fires when the auto-pick happens to be the 8th non-captain pick; ticker skips when `SUPERUSER_PASSWORD` is missing; ticker doesn't fire while the deadline is still in the future; stale deadline on an already-complete draft is cleared rather than re-fired.\n\n- **Routes (`src/web/server.js`)** — `createApiRouter` is now exported (one-line, behaviour-neutral change) so a tiny test express app can mount JUST the API router with stubbed `db` + `serverProvisioner` and drive real HTTP requests through it. Covers: `/draft-status` returns `pickDeadlineAt` + `pickSeconds` (and defaults `pickSeconds` to 30 when the row has none); `/draft-pick` resets the per-pick deadline to ~`pick_seconds` ahead after a mid-draft pick; `/draft-pick` clears the deadline (`null`) AND fires `provisionInhouseServer({ trigger: 'auto_draft_complete' })` exactly once on the 8th (final) pick.\n\n**Verification.** `node --test tests/inhouseDraftDeadline.test.js` → 17 passed, 0 failed. `node --test tests/inhouseServerProvisioner.test.js` (sibling Task #168 coverage) still 9/9 green.",
    "author": "System"
  },
  {
    "version": "6.49",
    "title": "Patch-notes seeder unblocked — 22 missed entries (v6.26 through v6.48) now reach prod",
    "published_at": "2026-05-09",
    "content": "**Closing a silent regression that kept the deployed footer stuck at v6.25.** The startup seeder in `src/db/index.js` validates that `src/data/patchNotes.js` is in strict ascending version order and bails on the first violation — bailing means it never inserts ANY entries newer than the last good run, which is why every deploy since v6.26 went out without its patch note ever reaching the live API. The bot logs surfaced `[Startup] Patch note seed failed: patchNotes.js is out of order: v6.30 appears before v6.29`, but a closer read of the tail showed the file had drifted into wholesale chaos: v6.28 was sitting between v6.32 and v6.33; v6.34 was between v6.40 and v6.42; v6.41 was between v6.44 and v6.46; v6.47 was before v6.46; and Task #180 had stamped a duplicate v6.46 instead of bumping to v6.48.\n\n**Fix.** (a) Renumbered Task #180's entry from the duplicate v6.46 to v6.48. (b) Sorted the entire 194-entry array numerically by major.minor — the JSON-style `module.exports = [ ... ];` round-trips cleanly through `JSON.parse` / `JSON.stringify(arr, null, 2)`, so formatting is preserved and the file remains diff-friendly. (c) Added this entry as v6.49.\n\n**Why deploys looked like they worked.** `bash deploy.sh` did exactly what it should — pull, npm install, build, pm2 restart oi-bot all succeeded, and the new asset hash was being served. But the footer reads from the live `/api/patch-notes` endpoint (Task #151, v6.25 wiring), which in turn reads from the DB `patch_notes` table the seeder feeds — and the seeder was bailing on the first new entry every restart since v6.25.\n\n**Hardening.** Future drift will still surface in startup logs the same way, but the file is now strictly ascending end-to-end, so the next deploy will seed v6.26 through v6.49 in one shot and the footer will jump straight to v6.49.",
    "author": "System"
  },
  {
    "version": "6.50",
    "title": "Hand-rolled modals can no longer slip past the a11y gate",
    "published_at": "2026-05-09",
    "content": "**Task #182 — closing the last gap in the Frontend accessibility house rule.** Task #165 made `<Dialog>` (`web/src/components/Dialog.jsx`, mirrored to `community-edition/web/src/components/Dialog.jsx`) the canonical modal primitive — it owns backdrop, role+aria-modal, focus trap/restore, body-scroll lock, and Escape-to-close in one place — but until now nothing automated was stopping a future PR from copy-pasting the old `<div role=\"dialog\" aria-modal=\"true\">` + ad-hoc `onKeyDown` shape. The `scripts/check-a11y.js` gate covered `<div onClick>`, hover-only reveals, custom toggle/switch/radio shapes, and icon-only buttons, but `role=\"dialog\"` was actually allow-listed as a non-actionable container role for the existing div-onClick check.\n\n**New gate (5th pass).** `scripts/check-a11y.js` now flags every literal `role=\"dialog\"` attribute in JSX under `web/src/` and `community-edition/web/src/` outside the canonical Dialog primitives themselves. The allow-list is a single `DIALOG_ALLOWLIST` Set near the top of the script naming exactly those two files; new intentional exceptions must be added there and documented in `replit.md`. Expression-form `role={…}` is intentionally not matched (nothing useful to inspect statically). Same hard-gate wiring as the existing checks — runs in `deploy.sh` and `scripts/post-merge.sh` before the frontend bundle is rebuilt, so a regression fails the deploy / GitHub push.\n\n**Pre-existing leak fixed.** `web/src/pages/MatchDetail.jsx` had a hand-rolled Pro-replay paywall modal (`<div role=\"presentation\">` backdrop + `<div role=\"dialog\" aria-modal>` content + ad-hoc Escape handler) that would have failed the new gate. Migrated it to `<Dialog>` with the same visual styling (backdrop z-index 9999 + dark overlay; content carrying the existing card chrome) — now picks up focus trap, focus restore, body-scroll lock, and Escape-to-close for free.\n\n**Docs.** Added a new \"Hand-rolled-modal gate (Task #182)\" paragraph in `replit.md` beside the four existing gate paragraphs, pointing future contributors at the `DIALOG_ALLOWLIST` escape hatch.\n\n**Verification.** `node scripts/check-a11y.js` → `OK — scanned 146 JSX/TS file(s) and 2 CSS file(s), no accessibility regressions found.` Synthetic regression check: dropping a temporary `<div role=\"dialog\">` test file outside the allow-list correctly trips the gate with the new message and exits 1.",
    "author": "System"
  },
  {
    "version": "6.51",
    "title": "Icon-only button gate now catches {dynamicVar} icon expressions",
    "published_at": "2026-05-09",
    "content": "**Task #183 — tightening the icon-only button check.** The 4th pass of `scripts/check-a11y.js` (Task #175) intentionally skipped any `<button>` / `<a>` whose children contained a `{…}` JSX expression, because the expression *might* be a visible text label like `{label}` and the static check couldn't tell. That left a real hole: `<button onClick={x}>{icon}</button>`, where `icon` is an SVG component reference, would silently slip through.\n\n**Smarter classifier.** When the inner content is exactly one balanced `{…}` expression, we now inspect what's inside:\n- **Skip (treated as a visible label):** string/template literals (`{\"Save\"}`, `` {`Hi ${n}`} ``), ternaries with at least one string branch (`{open ? \"Hide\" : \"Show\"}`), i18n-style calls with a string-literal first argument (`{t('save')}`, `{i18n.t('save')}`), and ambiguous bare identifiers/member access without an icon-y suffix (`{label}`, `{props.label}`).\n- **Flag (treated as an icon reference):** inline JSX whose tag is `svg` or ends in `Icon`/`Svg` wrapped in braces (`{<CloseIcon/>}`, `{<svg/>}`), bare identifiers ending in `Icon`/`Svg` (`{CloseIcon}`, `{closeSvg}`), and member-access references whose trailing segment ends in `Icon`/`Svg` (`{Icons.closeIcon}`, `{props.dismissSvg}`). Mixed content containing a `{…}` plus other nodes stays skipped.\n\n**Tests + docs.** New synthetic test coverage in `tests/checkA11y.test.js` (13 cases — text-y skips, icon-y flags, edge cases, plus the existing single-glyph and single-child shapes still passing). The `scripts/check-a11y.js` script now exports `classifyIconOnly` / `classifyBraceExpression` / `isSingleBraceExpression` so the classifier can be unit-tested in isolation; `if (require.main === module) main();` keeps the CLI behaviour unchanged. Updated the \"Icon-only button gate\" paragraph in `replit.md` to describe the tightened heuristic. Same hard-gate wiring — `deploy.sh` and `scripts/post-merge.sh` already invoke the script.",
    "author": "System"
  },
  {
    "version": "6.52",
    "title": "Magazine v3 profile cover graduates from sandbox · 5 new Pro layout themes (Court & Pitch / Newsprint / Carbon / Holo / Heritage / Broadcast)",
    "published_at": "2026-05-09",
    "content": "**Task #195 — first slice of the Magazine v3 profile redesign lands in production (full edition only).** `artifacts/mockup-sandbox/.../profile-redesign/MagazineSpreadV3.tsx` (1.5K-line TSX + 1.9K-line CSS) was the canonical mockup for the next-gen `PlayerProfile` look. The full mockup is too large to graduate in a single pass — many of its panels (voice packs, OG cosmetics shop, vanity slugs, Founders Pass ring, animated cover FX, time-of-day heatmap, Season Wrapped, Hall-of-Fame plaque, Profile Spotlight, live-status chip) require backend work that doesn't exist yet. This patch ships the foundational pieces and defers the rest to follow-up tasks.\n\n**1. New `MagazineCover` component (`web/src/components/MagazineCover.jsx` + `MagazineCover.css`).** Mounts above the existing `<ProfileCard />` and section stack on `/player/:id`, so nothing below the fold moves. Real production data only — no mock stubs. Renders: cinematic backdrop banner using the player's pinned hero (or top-played hero as fallback) with the v3 multi-stop overlay; eyebrow strip with signature hero + primary position; large serif name lockup that hosts the existing badge adornments (Pro, Verified, Rank, MMR-tier King/Warlord); custom title; flair pills (position, override flair, hot-streak / slump chip driven by the live `/player/:id/streak` reading); italic bio pull-quote; vital stats strip (MMR + W/L, win-rate %, recent KDA over last 10, GPM/XPM, Impact Score) with inline SVG sparklines built from the existing `ratingHistory`, `winRateHistory`, and `recentMatches` arrays — no extra API calls; social-link chips (Twitch / YouTube / Steam) sourced from the existing `extras.social_*` knobs. A sticky mini-header (`v3-sticky`) slides in via `IntersectionObserver` once the cover scrolls off-screen, mirroring the mockup's behaviour.\n\n**2. Five new Pro-only layout themes — `profile_layout_theme` cosmetic.** Mirrors the existing `PREMIUM_THEMES` / `PREMIUM_FRAMES` Pro-gating pattern. Catalogue lives in both `src/profileCosmetics.js` and `web/src/profileCosmetics.js` as `FREE_LAYOUT_THEMES` (`court-pitch` only) + `PREMIUM_LAYOUT_THEMES` (`newsprint`, `carbon`, `holo`, `heritage`, `broadcast`). Each theme is a CSS variant on `.magazine-v3.v3-theme-<slug>` that overrides the aliased `--bg-base` / `--text-main` / `--accent-brass` / `--accent-amber` tokens — so the cover banner, sticky header, and any future v3 sections all re-skin from one class swap. Court & Pitch is the default (free); the other five require Pro.\n\n**3. Schema + API.** New nullable `profile_layout_theme TEXT` column on `player_profiles` (idempotent `ALTER TABLE … ADD COLUMN IF NOT EXISTS`). `getPlayerProfileCustomization` and `setPlayerProfileCustomization` plumb it through the SELECT / UPSERT. `POST /api/me/profile` validates the new field via `cosm.isValidLayoutTheme` and Pro-gates premium values via `cosm.isPremiumLayoutTheme`, with the same 400/403 shape the existing theme/title/frame validators use.\n\n**4. Settings UI.** `web/src/pages/SettingsProfile.jsx` gets a new \"Profile layout theme\" picker section (sits above \"Profile frame\"), rendering all six themes with label + sub-text and a `LockedPill` on Pro-only entries. Disabled visual state for non-Pro accounts, just like the existing premium frame buttons.\n\n**5. Explicit deferrals (follow-up tasks proposed).** The mockup's voice packs, OG cosmetics shop tab, vanity slug, Founders Pass ring, cover-FX (shimmer/Ken-Burns/parallax/particle/vignette/streak-glow), pinned-achievement ribbon as a separate strip, live-status chip, hero-hover items grid, time-of-day heatmap, Season Wrapped, HoF plaque card, Profile Spotlight, and Compare drawer all stayed in the mockup. They each need new server endpoints / Stripe SKUs / background jobs, so each becomes its own task rather than being faked with placeholder data.\n\n**Why mount-above instead of full replace.** Replacing `PlayerProfile.jsx` wholesale would have churned ~30K lines and risked regressing 50+ existing flows (achievements, season pass, MVP/attitude, AI Scout, hero counters, prediction stats, gift Pro, etc.). Mounting the cover above lets the v3 visual identity ship today while the full section-by-section graduation lands incrementally. Same idea Task #157 used when it shipped the v3 Magazine sandbox in the first place.",
    "author": "System"
  },
  {
    "version": "6.53",
    "title": "Captains can tune how early the inhouse pick warning fires",
    "published_at": "2026-05-09",
    "content": "**Task #189 — making the per-pick pre-deadline warning user-tunable.** Task #178 hard-coded the pick warning to fire 10s before the per-pick auto-pick deadline. Some captains want more lead time on a 60s budget; others find 10s on a 15s budget too noisy. Hard-coding meant the only way to change it was a code edit + redeploy.\n\n**1. Schema (`src/db/index.js`).** New nullable `value_int INTEGER` column on `notification_prefs` (idempotent `ALTER TABLE … ADD COLUMN IF NOT EXISTS`) — used to store the optional integer-valued tuning for a notification pref. Toggle-only categories continue to leave it NULL, so nothing about the existing prefs changes.\n\n**2. Category metadata.** `inhouse_pick_warning` in `NOTIFICATION_CATEGORIES` now declares `value_default: 10`, `value_options: [5, 10, 15, 20]`, `value_unit: 's'`. New helpers: `categoryDef(key)` and `coerceCategoryValue(category, raw)` (validates the value is one of the legal options for that category — raw values outside the set are rejected, not silently rounded).\n\n**3. DB helpers.** `getNotificationPrefs(accountId)` now selects `value_int` and surfaces `value` / `value_default` / `value_options` / `value_unit` on the returned row for tunable categories — toggle-only rows keep their original shape. `setNotificationPref(accountId, category, enabled, value)` accepts an optional `value`, validates it against the category's `value_options`, and merges it into the row via `value_int = COALESCE(EXCLUDED.value_int, notification_prefs.value_int)` so a toggle-only update doesn't clobber an existing custom value.\n\n**4. Server (`src/web/server.js`).** `POST /api/me/notifications` now accepts an optional `value` field on each update (`{category, enabled, value?}`) and surfaces validation failures (unknown category, value outside `value_options`) as 400s instead of 500s. Existing `{category, enabled}`-only callers keep working unchanged.\n\n**5. Frontend hook (`web/src/hooks/useInhouseAlerts.js`).** The hard-coded `PICK_WARNING_LEAD_MS = 10000` is gone. Replaced by `DEFAULT_PICK_WARNING_LEAD_MS` (10s) and `ALLOWED_PICK_WARNING_LEADS_MS` ([5, 10, 15, 20] in ms), plus a new `pickWarningLeadMs` state that's populated from the same `/api/me/notifications` fetch that loads the on/off pref. The schedule effect (one-shot `setTimeout` from now to T-leadMs) reads the new state instead of the constant; `pickWarningLeadMs` is added to its deps so changing the pref live re-arms the timer. The browser notification title is now built dynamically (`Inhouse — Ns left to pick`) so a captain who picks 20s sees the right number on the toast — `fire(kind, opts)` accepts an `opts.title` override.\n\n**6. Settings UI (`web/src/pages/SettingsNotifications.jsx`).** The `inhouse_pick_warning` row gains a `<select>` with the four allowed lead times beside the on/off checkbox — only rendered for categories the server flags as tunable (`Array.isArray(p.value_options)`), so other rows are visually unchanged. Disabled when the toggle is off (no point picking a lead time for a warning you've muted) or when a save is in flight. New `setPrefValue(category, currentEnabled, nextValue)` helper persists just the value without touching the on/off state. Updated `CATEGORY_LABELS` for `inhouse_pick_warning` (drops the hard-coded \"10s\" from the title and explains the lead-time selector in the description).\n\n**No community-edition changes.** The community edition has neither `useInhouseAlerts` nor a SettingsNotifications page (inhouse is full-edition-only).",
    "author": "System"
  },
  {
    "version": "6.54",
    "title": "Captain auto-pick streak counter — chronic AFK captains now get noticed",
    "published_at": "2026-05-09",
    "content": "**Task #190 — surfacing a per-captain auto-pick stat so repeat AFK captains stop looking the same as one-off stallers.** Task #179 already records every draft pick's `pick_source` (`captain` vs `auto_deadline`) on `inhouse_session_players`, but there was no roll-up: a captain who lets the per-pick deadline expire on every single pick looked identical in the lobby UI to a captain who lost one pick to a phone call.\n\n**1. DB aggregate (`src/db/index.js`).** New `getCaptainAutoPickStats(accountId, lastN = 5)` walks the most recent `lastN` (default 5, capped 50) `inhouse_sessions` where this account was `captain1_account_id` or `captain2_account_id` and the session reached the draft (status in `completed` / `in_progress` / `server_failed`). One follow-up query pulls every `pick_order IS NOT NULL` row across those sessions, buckets them by team (captain1 ↔ team 1, captain2 ↔ team 2 — the convention used everywhere else in the inhouse code), and counts `pick_source = 'auto_deadline'`. Returns `{ sessionsConsidered, picks, autoPicks, ratio, perSession }`. Exported from the DB module alongside the other inhouse helpers.\n\n**2. Public route (`src/web/server.js`).** New `GET /api/inhouse/captain-stats/:accountId?limit=N`. Public read — the underlying session/player rows are already public.\n\n**3. API helper (`web/src/api.js`).** New `getCaptainAutoPickStats(accountId, limit = 5)` thin wrapper around the route.\n\n**4. Player profile (`web/src/pages/PlayerProfile.jsx`).** New section just below the performance trend chart, only rendered when the player has actually captained at least one completed session AND has at least one tracked pick. Reads e.g. `Auto-picked 6 of 24 picks across last 5 captain runs` with a tone/label that escalates by ratio: `<25%` muted (`Active captain`), `25–49%` amber (`Some auto-picks`), `≥50%` red (`Frequent AFK captain`). Pure CSS-token styling, no new components.\n\n**5. Inhouse lobby badge (`web/src/pages/Inhouse.jsx`).** Lazy-fetches each captain's stats once their `captain1_account_id` / `captain2_account_id` appear on the active session and caches them by accountId, so re-polls don't refetch. The two `RosterPanel` headers now render a small `⏱ NN% AUTO` badge next to the captain's name when their ratio is ≥25%, amber at 25–49% and red at ≥50%, with a `title` tooltip spelling out `Auto-picked X of Y picks across last N captain runs`. Below the threshold the badge stays hidden so we're not flagging a one-off stall.\n\n**No community-edition changes** — the community edition has the `inhouse_sessions` table but no inhouse routes / page / ticker, so this is full-edition-only (matches the existing inhouse architecture noted in `replit.md`). Accessibility gate (`scripts/check-a11y.js`) still passes.",
    "author": "System"
  },
  {
    "version": "6.55",
    "title": "Backfill auto-pick source for historical drafts so the ⏱ Auto-pick badge tells the truth retroactively",
    "published_at": "2026-05-09",
    "content": "**Task #191 — best-effort backfill of `pick_source` on draft picks made before Task #179 added the column.** Task #172 (per-pick deadline auto-pick) has been live for several patches, so there are already real auto-picks in history that will silently render as captain picks on `/inhouse` and on the captain auto-pick stat from Task #190. This patch stamps the historical rows by walking the autoStartTicker log lines.\n\n**1. Core helper (`src/inhouse/backfillPickSource.js`).** Reads a configurable list of log files (defaults to `~/.pm2/logs/{oi,inhouse}-bot-{out,error}.log`, override via `INHOUSE_LOG_PATHS` env or `--log=<path>` flags) and matches the canonical autoStartTicker emission `Session #<sid>: pick deadline expired, auto-picked <accountId> onto team <team>`. For each `(sessionId, accountId)` candidate it looks up the matching `inhouse_session_players` row and stamps `pick_source = 'auto_deadline'` ONLY when the existing value is NULL — making re-runs and overlapping log rotations safe (idempotent). Refuses to stamp when the DB-recorded `team` disagrees with the log line, so a manual override that happened after a deadline auto-pick can't be silently corrupted. Missing files are skipped silently (PM2 rotation, only one edition runs on this host, etc).\n\n**2. CLI (`scripts/backfill-pick-source.js`).** Mirrors `scripts/perf-backfill.js`. Flags: `--dry-run` prints the proposed stamps without writing; `--log=<path>` (repeatable) overrides the default PM2 log sources. Exits 0 on success, 2 on fatal error.\n\n**3. Owner-only Discord command (`src/discord/bot.js`).** New `!backfill-pick-source` (alias `!backfillpicksource`), gated on `OWNER_DISCORD_ID` like `!perf-backfill`. Accepts `dry-run` / `--dry-run` and `--log=<path>` args, replies with the same structured summary the CLI prints (files, candidates, proposed, stamped, already-stamped, missing).\n\n**4. Tests (`tests/backfillPickSource.test.js`).** 6 tests with a temp-file log fixture and a stubbed pool: regex matches the canonical line; `parseLogs` aggregates duplicate hits and skips ENOENT files; the backfill stamps NULL rows and leaves `'captain'` rows alone; dry-run writes nothing; second run is a no-op (idempotent); team-mismatch and missing-row cases are skipped without writes.\n\n**No community-edition mirror** — community has the `inhouse_sessions` table but no inhouse routes / page / autoStartTicker, so it never produced the auto-pick log lines this backfill consumes (matches the existing inhouse architecture noted in `replit.md`).",
    "author": "System"
  },
  {
    "version": "6.56",
    "title": "Snake-draft pick sequence centralised in one shared module",
    "published_at": "2026-05-09",
    "content": "**Task #192 — single source of truth for the inhouse snake-draft pick sequence.** The canonical sequence `[1, 2, 2, 1, 1, 2, 2, 1]` (the order non-captain slots are picked across the two teams during the captain draft) was previously hard-coded in three different places: `src/web/server.js` (the `/draft-pick` + `/draft-status` handlers), `src/inhouse/autoStartTicker.js` (the per-pick deadline auto-pick sweep added in Task #172), and `tests/inhouseDraftDeadline.test.js` (the pickIdx 0..7 → team assertions added in Task #180). The ticker even carried a comment saying \"Keep in sync if the server ever changes the order\" — a guarantee that wasn't actually enforced. Worse, because the test owned its own copy of the array, a future change to the production sequence would leave the test asserting against a stale constant and silently passing for the wrong reason.\n\n**Fix.** New tiny module `src/inhouse/draftSequence.js` exports a frozen `DRAFT_PICK_SEQUENCE` constant. Both production files now `require('./draftSequence')` (or `'../inhouse/draftSequence'`) and the test imports the same module — so any future sequence change automatically propagates to both consumers and the test fails loudly if the production order ever drifts from the test's expectation. No runtime behaviour change; all 17 existing `inhouseDraftDeadline.test.js` cases still pass against the new shared module.",
    "author": "System"
  },
  {
    "version": "6.57",
    "title": "Captain-vs-admin authorization rules on /draft-pick locked in by tests",
    "published_at": "2026-05-09",
    "content": "**Task #193 — test coverage for the captain-auth rules on `POST /api/inhouse/:id/draft-pick`.** The pre-existing `tests/inhouseDraftDeadline.test.js` suite (added in Task #180) exercised the per-pick deadline reset and the Task #168 auto-provision trigger, but always called the route with the superuser key — meaning none of the v5.75 captain-auth rules in the handler were actually verified. A future refactor could quietly weaken any of them and the suite would stay green.\n\n**New tests (6 cases, reusing the same minimal-mount harness).**\n- Signed-out caller → 401 (no `req.session.accountId`).\n- Signed-in non-captain caller → 403 with `Only the captains can pick.`\n- Captain picking onto the OTHER team → 403 with `You can only pick onto your own team.`\n- Captain picking out of turn (captain 2 trying to pick at pickIdx 0, where the sequence says team 1) → 409 with `It's not your turn to pick.`\n- Captain pick after the draft is already complete (8 non-captain seats already filled, so `_currentPickerTeam` returns null) → 400 with `Draft is complete.`\n- Captain caller passing `pickSource: 'auto_deadline'` (the ticker-only Task #179 source) → silently downgraded to `'captain'` and persisted as such (verified by capturing the UPDATE params and asserting `params[4] === 'captain'`).\n\nNo production code changes — purely test-only. All 23 cases in the suite pass.",
    "author": "System"
  },
  {
    "version": "6.58",
    "title": "Icon-only button gate now catches React.cloneElement and single-element array shapes",
    "published_at": "2026-05-09",
    "content": "**Task #194 — extend the static icon-only button gate.** The Task #183 tightening of `scripts/check-a11y.js` covered the common shapes (`{<Icon/>}`, `{CloseIcon}`, `{props.dismissSvg}`) but two less-common patterns that show up in shared button wrappers still slipped past the gate: `<button>{React.cloneElement(icon, { className: 'x' })}</button>` and `<button>{[<CloseIcon/>]}</button>`. Either of these could ship without an `aria-label` and the existing passes would stay green.\n\n**Fix.** `classifyBraceExpression` (the helper that decides whether a `<button>{expr}</button>` body is icon-only) now recognises two more shapes:\n- `React.cloneElement(arg, …)` and bare `cloneElement(arg, …)` — the first argument is recursively classified, so an icon-shaped first arg (`<CloseIcon/>`, `CloseIcon`, `props.dismissSvg`, …) flags the wrapping button. Lowercase/ambiguous first args (`icon`, `child`, `label`) stay lenient — same rule as the bare-identifier case.\n- Single-element array literals `[expr]` — the inner element is classified the same way; multi-element arrays stay skipped (they typically carry visible content).\n\nTwo small parsing helpers (`extractFirstCallArg`, `hasTopLevelComma`) handle nested parens/brackets/braces and quoted strings so the new shapes don't trip on commas inside nested expressions.\n\n**Tests.** `tests/checkA11y.test.js` gains 8 new cases covering both shapes (icon ref, JSX literal, member-access ref, ambiguous first arg, multi-element array, labelled wrapper) plus 4 end-to-end `scanFile` cases against tiny JSX fixtures. All 64 tests pass and `npm run check:a11y` stays clean across the existing 147 JSX/TS files.",
    "author": "System"
  },
  {
    "version": "6.59",
    "title": "Magazine v3 stat panels graduate to /player/:id — time-of-day heatmap, hero builds, Season Wrapped, Hall-of-Fame plaques",
    "published_at": "2026-05-09",
    "content": "**Task #203 — first batch of Magazine v3 deep-section ports lands on the production player profile (full edition only).** Task #195 graduated the v3 cover banner + sticky mini-header but explicitly deferred every panel that needed new backend work. This patch ships four of those: a 24×7 time-of-day heatmap, a hero-hover items grid, a Season Wrapped recap card, and personal Hall-of-Fame plaques. All four are free for everyone, with empty-state copy when a player has no eligible data so the section never collapses to nothing.\n\n**1. New API endpoints (`src/web/server.js`).** Four `GET /api/players/:id/...` routes mounted right above the existing `/hall-of-fame` global route, all backed by a 5-minute in-memory TTL cache (`_v3PanelCache`) keyed by `<endpoint>:<accountId>:<seasonId|''>` so repeat profile loads don't re-aggregate on every request.\n- `/players/:id/time-of-day?season=…` — returns `{ grid: cell[7][24], totalGames }` with `cell = { games, wins }`. Day-of-week is Mon..Sun (PG `EXTRACT(DOW)` rotated by `(dow + 6) % 7`), hour is 0..23, both anchored to `Australia/Sydney` so the heatmap reads as the OCE community's local clock instead of UTC. `m.date IS NOT NULL` guard so legacy rows without timestamps don't pollute the grid.\n- `/players/:id/hero-items` — returns top-6 most-played heroes (non-legacy) and the top-6 items per hero with consumables / wards / bottle / branches stripped (`SKIP_ITEMS` set).\n- `/players/:id/season-wrapped/:seasonId?` — defaults to the most recent archived/legacy season (or the explicit seasonId when supplied). Returns `{ season, items[] }` where each item is `{ label, value, sub }`. Aggregates: matches/wins, win-rate, avg KDA + GPM, signature hero, avg + best PERF, MMR delta computed from the first/last `rating_history` row whose match falls in the season window, and the count of `achievements` unlocked between `season.start_date` and `season.end_date`.\n- `/players/:id/hall-of-fame` — returns `{ plaques[] }` (capped to 3). Reuses `getPersonalRecords` so the plaque set always agrees with the global Hall-of-Fame page; plus a derived 'all-time wins leader' or 'top 1% career wins' (games ≥ 100) plaque from `getHallOfFameCareerStats`.\n\n**2. DB helpers (`src/db/index.js`).** Four matching helpers (`getPlayerTimeOfDayHeatmap`, `getPlayerHeroItems`, `getPlayerSeasonWrapped`, `getPlayerHallOfFamePlaques`) added next to `getPlayerNemesis` and exported from the module.\n\n**3. Frontend (`web/src/api.js`, `web/src/components/ProfileV3Panels.jsx`, `web/src/pages/PlayerProfile.jsx`).** Four new `api.js` thin wrappers (`getPlayerTimeOfDay`, `getPlayerHeroItems`, `getPlayerSeasonWrapped`, `getPlayerHallOfFamePlaques`). New `ProfileV3Panels` component renders all four panels inside the existing `.magazine-v3` wrapper, mounted right after `<ProfileCard />` so the rest of the page sits below as before. Each panel renders an empty-state message when its data array is empty; nothing else moves on the page.\n\n**4. Accessibility.** The hero-items grid is a real `<button role=\"tab\">` per hero, so it's keyboard-reachable; pointer hover and keyboard focus both call the same `setHoveredIdx(idx)`, satisfying the Task #185 mouse-handler focus-parity gate. Every heatmap cell has a descriptive `title` + `aria-label` (`Mon 18:00 — 12 games, 8W/4L (66% WR)`). HoF plaque cards that resolve to a specific match render as `<Link to={…}>` with text content (no icon-only buttons). `npm run check:a11y` passes.\n\n**5. Styling (`web/src/components/MagazineCover.css`).** New `.v3-panel*`, `.v3-tod-*`, `.v3-hero-items-*`, `.v3-wrapped-*`, `.v3-hof-plaque-*` rules under the existing `.magazine-v3` scope so all five Pro layout themes (Court & Pitch / Newsprint / Carbon / Holo / Heritage / Broadcast) re-skin the panels for free via the same `--bg-base` / `--accent-brass` / `--accent-amber` token aliases the cover already uses.\n\n**No community-edition mirror** — the v3 magazine cover (Task #195) is full-edition-only and these panels sit inside that same wrapper. The `_v3PanelCache` is per-process and clears on restart, which is fine for a 5-minute TTL.",
    "author": "System"
  },
  {
    "version": "6.60",
    "title": "Magazine v3 profile navigation polish — pinned-achievement ribbon, sticky anchor nav, Compare drawer",
    "published_at": "2026-05-09",
    "content": "**Task #204 — three pure-UI additions to the Magazine v3 player profile (full edition only).** Task #195 graduated the v3 cover and Task #203 graduated the deep stat panels; this patch polishes the navigation around them so visitors can pin highlights, jump between sections, and pull up a side-by-side stat compare without leaving the profile.\n\n**1. Pinned-achievement ribbon.** A new ribbon strip mounts between the v3 cover back-link and the shared `<ProfileCard>`. Free profiles can pin **1** achievement; Pro members can pin **up to 3**. Each pin renders the achievement emoji, label, and (where present) a one-line description, in the brass/parchment v3 token palette.\n- New `pinned_achievements` JSONB column on `player_profiles` (default `'[]'::jsonb`), plumbed through `db.getPlayerProfileCustomization` / `setPlayerProfileCustomization` and surfaced on the public `/api/players/:id/profile-card` payload.\n- `POST /api/me/profile` now accepts a top-level `pinned_achievements: string[]`, caps it at **1 (free) / 3 (Pro)**, and validates every id against the player's earned non-secret achievements (via `db.getMergedAccountIds` + `db.getPlayerAchievements`) before persisting. Submitting an unearned or hidden achievement key returns HTTP 400 with a clear message. Omitting the field on save preserves whatever's stored, so legacy clients don't wipe the ribbon.\n- Settings → Customize Profile gets a new ribbon picker rendered as real `<button type=\"button\" aria-pressed>` toggle pills (a11y-gate compliant), with the slot counter and Pro lock copy inline. The legacy single-pin `pinned_achievement_id` (avatar corner badge) stays unchanged for backward compatibility.\n\n**2. Sticky anchor nav rail.** A new `<nav class=\"v3-anchor-nav\">` floats on the right edge of the viewport (desktop ≥1100px only — hidden on mobile/tablet so touch users keep the regular scroll flow) with anchor links jumping to **Stats / Achievements / Heroes / Recent**. The four corresponding sections (`#stats` on the top stat-card grid, `#achievements` immediately above `<AchievementBadges>`, `#heroes` on the Most Played Heroes section, `#recent` on the Recent Matches section) pick up matching ids. Real `<a href=\"#…\">` elements so keyboard / screen-reader navigation is identical to mouse navigation.\n\n**3. Compare drawer.** A new **⚖️ Compare** button in the profile header opens a modal drawer built on the shared `<Dialog>` primitive (focus trap, Escape-to-close, body-scroll lock — passes the hand-rolled-modal a11y gate). The drawer lazy-loads the season's player list via `getAllPlayers(seasonId)`, lets the visitor pick any opponent, fetches the existing `/api/compare` endpoint via `getPlayerComparison`, and renders the result as a side-by-side stat strip with the winning side highlighted in amber. The `?compare=<accountId>` URL parameter preselects the opponent and opens the drawer on mount, so a fresh anchor link can deep-link straight into a comparison; closing the drawer strips the param so a back-button doesn't immediately re-open it. Empty-state copy when both players don't yet have enough season data.\n\n**a11y.** Everything ships through the existing gates: ribbon picker uses real `<button aria-pressed>` toggles; anchor nav uses real `<a href=\"#…\">` elements; the Compare drawer uses the shared `<Dialog>` primitive instead of a hand-rolled `role=\"dialog\"`. `npm run check:a11y` stays green.\n\n**Scope.** Full edition (`web/src/`, `src/`) only. Community edition is unchanged."
  },
  {
    "version": "6.61",
    "title": "Live status chip on profile (Discord + Steam presence)",
    "published_at": "2026-05-09",
    "content": "**Task #205 — live presence chip on the Magazine v3 player cover (full edition only).** Visitors landing on `/players/:id` now see a small status pill next to the position pill: **In game · <hero>**, **In lobby**, **In queue**, **In voice**, or **Online** — depending on what we can observe across the bot's existing Discord gateway connection, the Dota 2 Game Coordinator presence stream we already consume for friend lobby auto-detection, and the live `inhouse_queue` table.\n\n**Sources & priority.** The chip merges three sources in this priority order: (1) **Steam GC rich_presence** for `in_game` (with hero parsed from `param0` / `npc_dota_hero_<token>`) and `in_lobby` (from `steam_player_group`); (2) the live `inhouse_queue` table for `in_queue`; (3) **Discord gateway** events for `in_voice` (any active `voiceStateUpdate` channel) and `online` (any non-offline `presenceUpdate` status). If nothing matches, the chip is hidden (treated as offline). All caches are in-memory — there is **zero per-event DB writes**, only the existing `inhouse_queue` lookup.\n\n**Backend.**\n- New `src/services/presenceService.js` with two source maps (`_discord` keyed by Discord id, `_steam` keyed by SteamID64), a 5-minute staleness window, and a single `getPlayerPresence(accountId)` merge function that resolves `account_id → discordId` (via `db.getDiscordIdByAccountId`) and `account_id → steamId64` (via the public Dota offset).\n- `src/discord/bot.js` adds the `GuildPresences` + `GuildMembers` privileged intents and subscribes to `presenceUpdate` and `voiceStateUpdate`. Both handlers feed `presenceService.setDiscordPresence(...)` inside try/catch so a malformed event can never crash the bot.\n- `src/steam/steamClient.js` extends the existing `user` event with a parallel branch that always parses Dota 2 `rich_presence` into the presence service (independent of the friend-lobby monitor toggle).\n- New endpoint `GET /api/players/:id/presence` (always 200, returns `{status:'offline'}` on any error). `Cache-Control: no-store`.\n- New endpoints `GET/POST /api/me/presence-visibility` storing `player_profiles.presence_visible` (default TRUE).\n- New `presence_visible BOOLEAN NOT NULL DEFAULT TRUE` column on `player_profiles`, added via `ALTER TABLE … ADD COLUMN IF NOT EXISTS` in the existing init flow.\n\n**Frontend.**\n- New `<PresenceChip>` inside `MagazineCover.jsx` rendered in the existing `v3-cover-flair` row, with a tabular-numerals pill in the role-token palette (amber for in_game, gold for in_lobby, violet for in_queue, emerald for in_voice, neutral for online) and a glowing pulse dot for live states. `prefers-reduced-motion` is honoured.\n- `PlayerProfile.jsx` polls `getPlayerPresence(accountId)` every 30s while the tab is visible (skips polling when `document.visibilityState === 'hidden'`, kicks an immediate refresh on `visibilitychange`).\n- Settings → Notifications gains a **Live status chip** section with a single labelled checkbox to opt out (default on). The toggle persists via `POST /api/me/presence-visibility` and is honoured server-side at the merge step.\n\n**Privacy.** Per-account opt-out is honoured server-side: when `presence_visible = FALSE`, `/api/players/:id/presence` returns `offline` regardless of what the gateway has cached. No new PII or presence history is persisted.\n\n**a11y.** The chip is a `<span>` (not interactive) with an `aria-label` (\"Live status: In game · <hero>\"), the toggle is a real `<input type=\"checkbox\">` inside a `<label>`. `npm run check:a11y` stays green.\n\n**Scope.** Full edition (`web/src/`, `src/`) only. Community edition is unchanged (no inhouse routes / presence wiring there)."
  },
  {
    "version": "6.62",
    "title": "OG Cosmetics Shop tab + Voice Packs Pro SKU",
    "published_at": "2026-05-09",
    "content": "**Task #206 — `/shop` page + new `voice_pack` cosmetic type (full edition only).** OCE Inhouse already shipped four families of paid cosmetics — custom titles, theme accents, profile frames, and Magazine v3 layout themes — but they were scattered across the profile settings page with no single browsable catalogue. This patch adds a unified shop view AND introduces a fifth cosmetic type: **voice packs**, swapping the default church-bell chime on inhouse alerts for a themed voice pack.\n\n**1. New cosmetic type — voice packs.** Five Pro-only starter packs land alongside the existing layout-theme catalogue:\n- **Captain Calls** — authoritative team-caller barks\n- **Hype Train** — high-energy esports caster\n- **Calm Coach** — measured, strategic tone\n- **Trash Talk** — friendly smack-talk one-liners\n- **Cinematic Epic** — movie-trailer voice-over\n\nThere is no free pack — the existing church-bell chime IS the free default.\n\n**2. Catalogue mirroring (`src/profileCosmetics.js` + `web/src/profileCosmetics.js`).** New `FREE_VOICE_PACKS` / `PREMIUM_VOICE_PACKS` / `ALL_VOICE_PACKS` / `VOICE_PACK_EVENTS` / `VOICE_PACK_META` constants and `isPremiumVoicePack` / `isValidVoicePack` validators, mirroring the Task #195 layout-theme pattern exactly. Server-side CJS module re-exports them so route handlers can validate.\n\n**3. DB schema (`src/db/index.js`).** New `selected_voice_pack TEXT` column on `player_profiles`, added via `ALTER TABLE … ADD COLUMN IF NOT EXISTS` in the existing init flow. Plumbed through `getPlayerProfileCustomization` SELECT and `setPlayerProfileCustomization` INSERT/UPDATE/RETURNING — no new helper, just an extra column on the existing customization row.\n\n**4. Server validation (`src/web/server.js`).** `POST /api/me/profile` now accepts `selected_voice_pack`, validates against `cosm.isValidVoicePack` (HTTP 400 for unknown values), and Pro-gates premium packs (HTTP 403 for non-Pro members). Validated value is passed straight through to `setPlayerProfileCustomization`, mirroring the layout-theme block above it.\n\n**5. Inhouse alert integration (`web/src/hooks/useInhouseAlerts.js`).** The hook now loads the signed-in user's `selected_voice_pack` from `/api/me/profile` (alongside the existing pick-warning pref load). When `fire(kind)` runs and a pack is selected, it plays `/voice-packs/<pack>/<event>.mp3` instead of the church bell, where `event` is mapped from the alert kind: `accept` → `match-start`, `captain` → `achievement-unlock`, `your-pick` → `level-up`, `match-ready` → `match-start`, `pick-warning` → `level-up`. Per-event `<Audio>` cache, with a `voiceFailedRef` Set so a 404 / decode error on any single event silently falls back to the bell forever after (and the next fire never re-tries the broken URL). Mute, browser notifications, Web Audio fallback, and the existing `testChime` path are all unchanged.\n\n**6. Audio assets (`web/public/voice-packs/<pack>/<event>.mp3`).** Five pack directories × six event slots (match-start, first-blood, win, loss, level-up, achievement-unlock) = 30 placeholder mp3s, currently copies of `sounds/church-bell-v603.mp3` so the plumbing can be exercised end-to-end. A `README.md` next to them documents the slot list and rebrand process for when the real voice lines land.\n\n**7. Settings picker (`web/src/pages/SettingsProfile.jsx`).** New **Inhouse voice pack** section after the layout-theme block, mirroring its visual shape (real `<button type=\"button\">` cards with the brass/amber selected state and `LockedPill` for Pro-only entries). Includes a per-pack ▶ Preview button (`aria-label=\"Preview <pack> voice pack\"`) that plays the pack's `match-start.mp3` so users can audition before saving. The default-bell button is the free first slot. Save POSTs `selected_voice_pack` alongside the existing customization fields.\n\n**8. New `/shop` page (`web/src/pages/CosmeticsShop.jsx`).** Single browsable catalogue grouped by type — Voice packs / Profile layout themes / Profile frames / Custom titles / Theme accents — each card built from real `<button>` / `<Link>` elements (a11y-gate compliant). Each card links into the existing settings or `/pro` flow rather than running its own checkout, so the shop can never drift out of sync with what the server actually accepts. Mounted at `/shop` (lazy-loaded route in `App.jsx`) with a new **Cosmetics Shop** entry in the Tools dropdown menu.\n\n**a11y.** Every clickable surface is a real `<button type=\"button\">` (with `aria-label` on the icon-only Preview button) or a `<Link>`. `npm run check:a11y` stays green.\n\n**Scope.** Full edition (`web/src/`, `src/`) only. Community edition is unchanged."
  },
  {
    "version": "6.63",
    "title": "Founders Pass cover ring + Magazine v3 Cover FX (Pro)",
    "published_at": "2026-05-09",
    "content": "**Task #207 — limited Founders Pass entitlement + six Pro Cover FX toggles for the Magazine v3 cover (full edition only).** Two related additions to the cosmetics catalogue: (1) a one-time, **strictly capped** Founders Pass SKU that paints a brass→amber decorative ring around the v3 cover banner forever, and (2) six allow-listed animated effects that Pro members can layer onto the same banner — all gated server-side and respecting the user's reduced-motion preference.\n\n**1. New `entitlements` table (`src/db/index.js`).** Generic one-time-purchase ledger keyed by `(account_id, sku)` with a unique index so the cap query never races. Helpers: `getOwnedEntitlements`, `hasEntitlement`, `countEntitlementHolders`, `listEntitlementHolders`, `revokeEntitlement`, and the cap-enforcing `grantEntitlementWithCap(accountId, sku, cap)` which runs the count + insert inside a single `BEGIN…COMMIT` transaction so the 200th sale can never be overshot by two concurrent webhook fires.\n\n**2. New `cover_fx jsonb` column on `player_profiles`.** Defaulted to `'[]'` and plumbed through `getPlayerProfileCustomization` SELECT and `setPlayerProfileCustomization` INSERT/UPDATE/RETURNING. The public `/api/players/:id/profile-card` payload now also surfaces `owned_entitlements` (string[]) so the frontend can render the ring without a second round-trip.\n\n**3. Catalogue mirroring (`src/profileCosmetics.js` + `web/src/profileCosmetics.js`).** New `COVER_FX_IDS` (`shimmer`, `kenburns`, `parallax`, `particle`, `vignette-pulse`, `streak-glow`), `COVER_FX_META` (label + sub for each), `isValidCoverFxId`, and a defensive `validateCoverFx(input)` that dedupes, drops unknowns, and caps the array at six. New `FOUNDERS_RING_SKU = 'founders_pass_ring'` constant.\n\n**4. Server validation + Stripe checkout (`src/web/server.js`).** `POST /api/me/profile` now accepts `cover_fx`, validates it through `validateCoverFx`, and **strips the field for non-Pro members** (Pro-gating). Omitting the key preserves whatever's stored, mirroring `pinned_achievements` semantics. New routes:\n- `GET /api/shop/founders-ring/status` → `{ owned, sold, cap, remaining, sold_out, price_cents }` (cap from env `FOUNDERS_RING_CAP`, default 200; price from `FOUNDERS_RING_PRICE_CENTS`, default 999).\n- `POST /api/shop/founders-ring/checkout` → Stripe Checkout session with `purpose: 'founders_ring'` metadata; refuses to start if the ring is already owned or the cap is hit.\n- `POST /api/admin/founders-ring` (superuser) → manually grant; `DELETE /api/admin/founders-ring` → revoke; `GET /api/admin/founders-ring` → list current holders.\n\nThe existing Stripe webhook handler grew a `purpose === 'founders_ring'` branch that calls `grantEntitlementWithCap` inside the transaction — same idempotency story as the other purpose branches.\n\n**5. Live cover render (`web/src/components/MagazineCover.jsx` + `.css`).** `<MagazineCover>` accepts new `foundersRing` and `coverFx` props. The ring renders as a pointer-events-none, aria-hidden `<span class=\"v3-founders-ring\">` between the cover overlay and the cover content; each effect renders its own `<span>` (or class on the cover bg img). The streak chip gains a `v3-fx-streak-glow` class when the FX is selected and the player is on a win streak. **All animation rules live inside `@media (prefers-reduced-motion: no-preference)`**, so users with reduced-motion preferences see the static ring + static vignette only.\n\n**6. Settings panel (`web/src/pages/SettingsProfile.jsx`).** New **Cover effects (Pro)** section with six `<button type=\"button\" role=\"switch\" aria-checked aria-label>` toggles — disabled with `LockedPill` for non-Pro members. Save bundles `cover_fx` into the existing POST. The owned-Founders-ring confirmation chip surfaces below the toggles when the entitlement is present.\n\n**7. Cosmetics Shop (`web/src/pages/CosmeticsShop.jsx`).** New **Founders Pass** section above the existing voice-pack section. The card surfaces live `sold / cap` availability, switches between `Buy $9.99` / `✓ Owned` / `Sold out` based on the status payload, and routes through `buyFoundersRingCheckout()` (new helper in `web/src/api.js`).\n\n**8. Profile Sandbox (`web/src/pages/ProfileSandbox.jsx`).** New section with a Founders-ring checkbox + the six switch buttons so superusers can verify the a11y wiring without flipping their own row.\n\n**9. Tests + a11y gate.** All new switch buttons satisfy the existing `scripts/check-a11y.js` triad (real `<button>` + `aria-label` + `aria-checked` for the role=switch shape). No new exemptions required.",
    "author": "System"
  },
  {
    "version": "6.64",
    "title": "Vanity profile slugs + admin Profile Spotlight",
    "published_at": "2026-05-09",
    "content": "**Task #208 — Pro-gated `/p/<slug>` short URLs and an admin-curated Featured Player rotation (full edition only).**\n\n**1. Vanity slugs.** New citext `vanity_slug` column on `player_profiles` (partial UNIQUE index on non-null rows so the existing rows aren't all colliding on NULL). Slug shape is locked to 3–24 lowercase a–z / 0–9 / hyphen, no leading/trailing hyphen, no `--`. The reserved-path deny-list (`api`, `admin`, `inhouse`, `tournaments`, `players`, `match`, `settings`, `pro`, `coaches`, `shop`, `leaderboard`, etc.) is enforced server-side so `/p/admin` can never be claimed. Server-side 302 from `/p/<slug>` → `/player/<account_id>` is registered BEFORE the SPA catch-all so the redirect works on first hit (no SPA boot required) and search engines / unfurlers see the canonical profile target. Pro membership is required to claim a slug; the picker also accepts read-only **grandfathering** for any non-Pro account that already owns one. New routes: `GET /api/vanity-slug/availability`, `GET/POST/DELETE /api/me/vanity-slug`. The picker lives in both **Settings → Profile** and the **Cosmetics Shop** (a new card linking back to settings). 30-day release cooldown is tracked in a separate `vanity_slug_releases` ledger; only the previous owner can re-claim within the cooldown — third parties get a 409 until it expires.\n\n**2. Profile Spotlight.** New `profile_spotlight` table (`account_id`, `headline`, `blurb`, `starts_at`, `ends_at`, `created_by`, `created_at`, `ended_at`). The bot's hourly cron (registered alongside the existing weekly recap / season-end / coaching crons in `src/discord/bot.js` `client.once('ready')`) calls the new `db.advanceSpotlight()` helper, which marks any active row whose `ends_at` has passed as ended. A new public `GET /api/spotlight/current` endpoint surfaces the most recently active spotlight (joined to `nicknames` / `player_stats` for the display name) and is cached 60s in-memory so the home page stays cheap under traffic. Superuser-gated `GET/POST/DELETE /api/admin/spotlight` powers a new `/admin/spotlight` page where operators can create new featured-player entries, schedule windows, and end them early. The home page renders a new `FeaturedPlayer` card under the Live Inhouse Pulse strip on both the signed-in and signed-out variants — hidden when no spotlight is active so the layout stays unchanged on quiet weeks.\n\n**Out of scope (deferred):** community edition (table only, no routes/page/cron), slug ownership transfer between accounts, algorithmic spotlight selection, per-spotlight images.\n\nFooter version bump v6.63 → v6.64.",
    "author": "System"
  },
  {
    "version": "6.65",
    "title": "Shared draft 'whose turn?' helper",
    "published_at": "2026-05-09",
    "content": "Refactor (Task #211): the snake-draft 'which captain is on the clock?' math now lives in a single shared helper in src/inhouse/draftSequence.js, used by both the website's draft-pick / draft-status routes and the bot's per-pick deadline + recovery sweeps.\n\nPreviously the same count-non-captain-drafted-then-index-into-the-sequence rule was duplicated in src/web/server.js and src/inhouse/autoStartTicker.js against slightly different player shapes; a future change to 'what counts as drafted' (e.g. excluding declined or admin-override picks) had to be made in two spots and could silently disagree. New direct unit tests in tests/draftSequence.test.js cover the helper without round-tripping through the ticker or HTTP handlers.",
    "author": "System"
  },
  {
    "version": "6.66",
    "title": "'Live now' tab on the players page",
    "published_at": "2026-05-09",
    "content": "Feature (Task #213): the players page now has a 'Live now' tab alongside 'All players' that lists every visible account currently in a Dota match, in a lobby, in the inhouse queue, or in a Discord voice channel — turning the per-profile presence chip introduced in Task #205 into a community-wide spectator hook. Plain Discord-online (no game/lobby/queue/voice) is intentionally excluded so the tab stays scoped to actively-doing-something statuses.\n\nBacked by a new bulk endpoint GET /api/presence/live + a getAllLivePresences() helper in src/services/presenceService.js, so the page renders with one call instead of fanning out N profile pings. The tab auto-refreshes every 30 seconds using the same visibility-gated polling pattern as the magazine cover chip, honours the existing presence_visible opt-out, and uses the same status colours as the chip so the visual language stays consistent.",
    "author": "System"
  },
  {
    "version": "6.67",
    "title": "Voice packs ship with real spoken voice lines",
    "published_at": "2026-05-09",
    "content": "Audio (Task #216): the five Pro voice packs (Captain Calls, Hype Train, Calm Coach, Trash Talk, Cinematic Epic) now ship with real spoken voice lines instead of placeholder copies of the church bell. Each of the 30 slots (match-start / first-blood / win / loss / level-up / achievement-unlock × 5 packs) has a distinctive line whose tone matches the pack's label and sub copy in profileCosmetics.js — authoritative team-caller barks for Captain, fast high-pitched caster shouts for Hype, slow smooth coaching for Calm, mocking smack-talk for Roast, and a deep British movie-trailer baritone for Cinematic — so the upsell finally sounds different from the free chime and from every other pack.\n\nAll 30 mp3s live at web/public/voice-packs/<pack>/<event>.mp3 (96 kbps mono 44.1 kHz). The new scripts/generate-voice-packs.sh wraps espeak-ng + ffmpeg so the entire set can be re-synthesised on demand if the script copy changes; individual slots can still be hand-replaced with higher-quality recordings later. The README in the voice-packs dir was rewritten to document the new shape and per-pack voice profile. The hook still falls back to the church bell on 404, so missing slots remain safe.",
    "author": "System"
  },
  {
    "version": "6.68",
    "title": "Bot startup hardening: patch-notes ordering + express route compat",
    "published_at": "2026-05-09",
    "content": "Fix: src/data/patchNotes.js had drifted out of strict ascending order (mid-file 6.51→6.53→...→6.52 inversions plus the new 6.65/6.66/6.67 entries inserted descending at the top), which trips the startup ordering check and refuses to boot the bot. The whole array is now re-sorted ascending in one pass — 214 entries total, oldest (0.1) first, newest (this entry) last — so future appends only need to be appended at the end.\n\nFix: src/web/server.js the GET /players/:id/season-wrapped/:seasonId? route used the legacy optional-param syntax that path-to-regexp v8 (Express 5) no longer accepts; the bot crashed with PathError [TypeError]: Unexpected ? at index 37 right after the Discord client connected. Replaced the single optional-param route with two explicit registrations (/season-wrapped and /season-wrapped/:seasonId) sharing the same handler, so both with-season and without-season callers continue to work.",
    "author": "System"
  },
  {
    "version": "6.69",
    "title": "Voice packs now fire on win, loss, first-blood and achievement-unlock",
    "published_at": "2026-05-09",
    "content": "Audio (Task #217): the five Pro voice packs introduced in Task #206 shipped with six event slots each (match-start / first-blood / win / loss / level-up / achievement-unlock), but only the four inhouse-lobby cues (accept / captain / your-pick / match-ready) actually triggered playback — the rest of every pack was unreachable. This patch wires the missing four lifecycle events to the rest of the match flow so the entire pack is finally audible.\n\n**1. Server-side event queue (`src/web/voiceEventQueue.js`).** New in-memory per-account ring buffer (cap 20, 5-min TTL) with `pushVoiceEvent`, `pushMatchVoiceEvents` (emits `win`/`loss` plus `first-blood` when any player on the match has `firstbloodClaimed`/`firstblood_claimed` set, tolerant of either snake_case or camelCase upstream payloads, and falls back to `player_slot < 128` when no `team` field is present), `pushAchievementVoiceEvents`, and `drainVoiceEvents`. In-process only — voice cues are at-most-once hints, the Discord DM and embed remain the authoritative record.\n\n**2. Wired into the post-match flow.** Both record-match call sites now push: `src/web/server.js` (web upload pipeline, alongside the existing `_notifyAchievementsUnlocked` trigger) and `src/discord/bot.js` `_recordMatchData` (lobby/replay path, alongside the same achievement notify). The referrer-achievement grant path in `src/web/server.js` also pushes an `achievement-unlock` event.\n\n**3. New endpoint `GET /api/me/voice-events`.** Steam-auth gated, drains the signed-in user's queue and returns events in arrival order.\n\n**4. Shared playback helper (`web/src/lib/voicePack.js`).** Per-event `${pack}|${event}` HTMLAudioElement cache + 404 / decode-error `failed` Set, factored out of `useInhouseAlerts.js` so the new global hook reuses the exact same playback semantics. `useInhouseAlerts.js` now consumes `createVoicePackPlayer()` instead of hand-rolling the cache.\n\n**5. New global hook (`web/src/hooks/useVoicePackEvents.js`).** Polls the new endpoint every 8s while a Steam user is signed in and the page is visible, loads `selected_voice_pack` from `/api/me/profile`, honours the existing `inhouse:muted` localStorage toggle, and spaces overlapping events by ~900ms so a freshly-tabbed-in queue doesn't blast multiple clips on top of each other. No church-bell fallback outside `/inhouse` — the bell is intentionally lobby-only.\n\n**6. Mounted globally in `web/src/App.jsx`.** New `<GlobalVoicePackEvents />` component sits inside `SteamAuthProvider` so the hook fires on every page after sign-in.\n\n**Tests.** New `tests/voiceEventQueue.test.js` covers 11 cases — empty drain, push+drain idempotency, invalid event names, anon/zero accountId rejection, queue cap enforcement, win/loss + first-blood derivation across `team` and `player_slot` shapes, snake_case `firstblood_claimed` parity, no-killer skip, malformed-input tolerance, achievement-unlock per-grant emission, non-array tolerance. `npm run check:a11y` stays green.\n\n**Scope.** Full edition only — community edition has neither voice packs nor the inhouse lobby surface that originally shipped them.",
    "author": "System"
  },
  {
    "version": "6.70",
    "title": "Apple Pay, Google Pay, Link & BECS Direct Debit at Stripe Checkout",
    "published_at": "2026-05-09",
    "content": "Payments (Task #235): Stripe Checkout was hard-coded to `payment_method_types: ['card']` at every call site, so buyers were locked into entering a card number even when their browser had a faster wallet ready. This patch hands payment-method selection back to Stripe so the dashboard becomes the single source of truth — enable a method there and it shows up at checkout instantly.\n\n**Scope.** Seven of the eight `stripe.checkout.sessions.create` calls in `src/web/server.js` swapped from `payment_method_types: ['card']` to `automatic_payment_methods: { enabled: true }`:\n\n1. Season buy-in (line 2923)\n2. Tournament entry (line 5016)\n3. Premium frame purchase (line 8590)\n4. Founders Pass ring (line 8684)\n5. Gift Pro Tier (line 8780)\n6. Gift Season Pass (line 8844)\n7. Pro Tier upgrade (line 9042)\n\n**Coaching booking stays card-only (line 9451).** The coaching-marketplace checkout uses `payment_intent_data.capture_method: 'manual'` for the escrow / no-show flow — funds are authorised at booking and only captured on completion. BECS Direct Debit and most wallet methods don't support manual capture, so Stripe would reject the session if we let them through. An inline comment now documents the constraint so the next contributor doesn't 'fix' it back to automatic.\n\n**Effect for buyers.** With `automatic_payment_methods` Stripe now offers card + Apple Pay + Google Pay + Link + BECS Direct Debit (AU bank-debit) on the seven standard sites — actual availability per buyer depends on their browser, country, and what's enabled in the Stripe dashboard.\n\n**Webhook gating (critical).** The `/api/stripe/webhook` `checkout.session.completed` handler previously fulfilled by `metadata.purpose` immediately, which was safe when every payment was a card (always paid on the spot) but would have granted entitlements before the funds settled for BECS / Direct Debit. Refactored: the existing fulfillment body is now an inline `fulfillCompletedSession(session)` helper, and the dispatcher gates on `session.payment_status === 'paid' || 'no_payment_required'`. Two new event branches were added — `checkout.session.async_payment_succeeded` runs the same fulfillment when an async method finally settles, and `checkout.session.async_payment_failed` frees pending coaching slots (other purposes have no pre-fulfillment side effect, so logging is enough). Existing `payment_intent.succeeded` / `charge.refunded` / `checkout.session.expired` / `payment_intent.canceled` handlers are unchanged.\n\n**Frontend success copy.** Two surfaces previously asserted 'Payment confirmed!' before the server actually said so — fixed so they wait on the confirm-API result and surface a 'still processing' state when Stripe returns 402 (async settlement in flight): `web/src/pages/Tournaments.jsx` (transitional 'Confirming payment…' replaced by either ok or pending message) and `web/src/pages/BuyinSuccess.jsx` (new 'pending' state distinct from generic error, explains BECS settlement timeline).\n\n**Edition scope.** Full edition only — community edition has no monetisation routes.",
    "author": "System"
  },
  {
    "version": "6.71",
    "title": "Test coverage for voice-pack server validation",
    "published_at": "2026-05-09",
    "content": "Tests (Task #218): the two server-side guards Task #206 added on POST /api/me/profile (HTTP 400 for unknown voice pack values, HTTP 403 for non-Pro users picking any pack in PREMIUM_VOICE_PACKS) shipped without automated coverage, so a future refactor of the cosmetics validation block could quietly weaken or drop the Pro-gate and shipping would still pass. New tests/voicePackProfileValidation.test.js mounts the real createApiRouter() against a session-injecting Express harness, stubs the heavy non-route requires (db, replay parser, stats, groq, discord) via require.cache, and exercises six scenarios end-to-end: unknown pack → 400 'Unknown voice pack', every premium pack rejected for non-Pro → 403 'Voice packs are reserved for Pro members', valid pack persisted for Pro, null clears the column, empty string normalises to null, and an omitted field is treated as clear. Each assertion also checks setPlayerProfileCustomization was (or was not) called so a regression that 200s but skips persistence still fails. All 343 tests in the suite pass.",
    "author": "System"
  },
  {
    "version": "6.72",
    "title": "Player profile polish — readable rank/MMR badges, ordered TOC, hero-name fix, heatmap legend",
    "published_at": "2026-05-09",
    "content": "Six player-profile UI fixes shipped together. (1) Hero builds panel was rendering raw `npc_dota_hero_*` strings straight from the API for both the portrait list and the active-hero header; new prettyHero() helper in components/ProfileV3Panels.jsx routes the name through formatHeroName() so 'npc_dota_hero_mars' now reads 'Mars'. (2/3) The rank/MMR pills next to the player name and the GPM/MMR/WR/KDA/Impact strip on the magazine cover both looked blurry because their translucent backgrounds let the blurred hero portrait bleed through; nameAdornments are now wrapped in a new .v3-cover-adornments slab with a 0.78-opacity ink-navy backing + 14px backdrop-blur, and the .v3-cover-vitals strip's backing was strengthened from 0.55 → 0.82 opacity with a 16px blur, so the text reads sharply against any backdrop. (4) The sticky anchor TOC rail was listed in invented order (Cover → Stats → Heroes → Achievements → Records → Recent) and bounced the page around; reordered to match real DOM order (Cover → Highlights → Stats → Achievements → Heroes → Recent) and renamed the misleading 'Records' label to 'Highlights' since the #records anchor actually scrolls to the time-of-day heatmap / hero builds / season wrapped / hall-of-fame plaques block. (5) The Most-Played Heroes strip on ProfileCard was sparse — three monospace tokens per chip; redesigned as an auto-fit grid with bigger 52×30 portraits, a #1..#5 rank badge, KDA when available (now plumbed from avg_kills/avg_deaths/avg_assists in PlayerProfile.jsx), and a thin coloured win-rate bar (red <40 / amber <50 / lime <60 / green ≥60). (6) The time-of-day heatmap's one-line 'Opacity = games played, hue = win rate' caption was easy to miss; added a proper legend row beneath the grid with a red→green colour swatch pair (losses/wins) and four ascending-opacity squares (few → many games), plus expanded the prose explanation. A11y gate green (153 JSX/3 CSS files scanned, 0 regressions).",
    "author": "System"
  },
  {
    "version": "6.74",
    "title": "Test coverage for the Founders Pass cap and Cover FX gating",
    "published_at": "2026-05-09",
    "content": "Tests (Task #220): the two security-sensitive guarantees v6.63 / Task #207 added — the Founders Pass SKU is hard-capped at FOUNDERS_RING_CAP (default 200) inside a single transaction, and POST /api/me/profile strips cover_fx for non-Pro members + canonicalises the array via validateCoverFx — shipped without automated coverage, so a future refactor of the entitlements ledger or the cosmetics validation block could quietly weaken the cap or drop the Pro-gate and shipping would still pass.\n\nNew tests/foundersRingCap.test.js exercises five scenarios end-to-end: (1) 5 concurrent grantEntitlementWithCap() calls vs cap=3 → exactly 3 succeed and 2 fail with reason='cap_reached', backed by a fake pg.Pool that simulates pg_advisory_xact_lock(hashtext(sku)) by serialising transactions per-SKU through a Promise chain so the test actually races the concurrency-safe path rather than the happy-path; (2) the same helper is idempotent — repeat grant for the same account returns {granted:false, reason:'already_owned'} without rotating the ledger; (3) validateCoverFx() dedupes, drops unknowns, drops non-strings, returns [] for non-array input, and caps the array at COVER_FX_IDS.length (6) even when flooded with 50 valid+repeat entries; (4) POST /api/me/profile with a populated cover_fx selection from a non-Pro caller never reaches the persistence helper with a non-empty array (asserts both the 403-reject shape and the silent-strip shape so the security guarantee, not the response shape, is what's pinned); (5) omitting the field as a Pro caller preserves the previously stored value (mirrors pinned_achievements semantics so a pre-#207 client save can't silently wipe the selection).\n\nThe route tests mount the real createApiRouter() against a session-injecting harness, stub the heavy non-route requires (db, replay parser, stats, groq, discord, voice queue) via require.cache, and unref() the long-running setInterval timers in server.js so node:test exits cleanly. All 348 tests in the suite pass.",
    "author": "System"
  },
  {
    "version": "6.73",
    "title": "Live Magazine-v3 cover preview in Settings → Profile and the sandbox",
    "published_at": "2026-05-09",
    "content": "UI (Task #219): the Cover FX toggles + Founders Pass ring shipped in v6.63 / Task #207 are wired into the Settings → Profile picker, but the live preview pane on that page renders the legacy <ProfileCard> — which has no cover at all — so Pro members had to save and visit their public profile to see what they'd picked. The Profile Sandbox had the same gap.\n\nFix: Settings → Profile now mounts a real <MagazineCover> at the top of the sticky preview rail, wrapped in the same `magazine-v3 v3-theme-{slug}` shell PlayerProfile uses, so it picks up the Court & Pitch / Newsprint / Carbon / Holo / Heritage / Broadcast layout-theme styling for free. It's wired to the live editor state — display name, custom title, bio, theme accent, pinned hero, streak, founders ring (driven by the existing `owned_entitlements` Founders Pass entitlement), and the Cover FX toggle list — so every change flips the cover instantly. The existing <ProfileCard> preview is kept directly underneath so pinned-match, pinned-achievement, top-heroes strip, social chips and frame previews still work.\n\nThe Profile Sandbox got the same upgrade — its FullPreviewCard now renders a <MagazineCover> wrapper above the ProfileCard, using the SAMPLE_TOP_HEROES + SAMPLE_STREAK fixtures, and accepts foundersRing + coverFx props from the existing Pro-toggle section. The sandbox's stale 'sandbox preview here uses the legacy ProfileCard' caption was updated to reflect the new live cover.\n\nReduced motion: every animated FX (shimmer sweep, ken burns zoom, parallax drift, vignette pulse, particle drift, founders-ring breathing, streak glow) is already gated inside `@media (prefers-reduced-motion: no-preference)` in MagazineCover.css, so users with reduced-motion preferences see the static cover unchanged in both the preview and production. `npm run check:a11y` stays green.\n\nEdition scope: full edition only — the community edition doesn't ship the Magazine v3 cover, so its Settings → Profile preview is unchanged.",
    "author": "System"
  },
  {
    "version": "6.74.1",
    "title": "Magazine cover — back to transparent rank/MMR + vitals strip with crisp text",
    "published_at": "2026-05-09",
    "content": "Reverting the v6.72 'fix' for the rank/MMR pills and the GPM/MMR/WR/KDA/Impact strip on the Magazine v3 cover. The previous pass wrapped both in opaque ink-navy slabs with heavy backdrop-blur to make the text 'pop' against the hero portrait — but the user (correctly) pointed out the original transparent treatment looked better, and the backdrop-blur was actually softening the text against the busy hero art instead of sharpening it.\n\nFix in MagazineCover.css: .v3-cover-adornments is now a pure inline-flex container with no background, border, blur or shadow — the rank pills sit directly over the hero artwork the way they did before v6.72. .v3-cover-vitals keeps a thin 0.12-opacity white border + a subtle 0.28-opacity ink-navy wash for separation, but drops the 16px backdrop-blur entirely. Legibility now comes from a stacked text-shadow (1px black drop + 8px black halo) on every label, value, suffix and sub-line inside the strip, so 7094 / 89% / 5.87 / 574/772 / 10.0 stay sharp even over a bright hero portrait. A11y gate green, web bundle builds cleanly.",
    "author": "System"
  },
  {
    "version": "6.77",
    "title": "Live now cards show hero portraits, a Watch link, and queue tooltips",
    "published_at": "2026-05-09",
    "content": "Task #226: the /players 'Live now' tab (shipped in v6.61, Task #213) was a flat list of name + status pill. You could see *that* someone was in a Dota match, but not *which* hero they picked, and there was no way to actually jump in as a spectator — the natural spectator hook the rollup was built for. v6.77 closes that loop.\n\nBackend (src/services/presenceService.js): the bulk live-presence query for the queue branch now selects `joined_at` from inhouse_queue alongside the account_id, and threads it into the response payload as `joined_at` on every in_queue row (with `updated_at` falling back to `joined_at` so the row's freshness is honest about when the player actually queued). The in_game branch already returned `hero`, `hero_id`, and `match_id` (the Steam GC `WatchableGameID`) via the existing rich_presence parser — no change needed there. No new persistence; no extra DB round-trips for non-queue rows.\n\nFrontend (web/src/pages/Players.jsx LiveNowList): in_game cards now render a `<HeroIcon size='md'>` portrait on the left using the existing `web/src/components/HeroIcon.jsx` component (same Steam CDN slug + onError fallback every other page uses), so visitors see at a glance that 'StElmosFire is in game · Crystal Maiden'. When the row carries a watchable id, the right edge swaps the static status pill for a brass `Watch` button (`steam://rungame/570//+watch_server <id>` deep link — opens Dota 2 directly into the spectator session) plus a small `Copy ID` button that puts the matching `watch_server <id>` console command on the clipboard for users whose browser blocks the `steam://` protocol or who'd rather paste it manually. The copy button flips to 'Copied!' for 1.5s on success and ships with an `aria-label` so screen readers narrate the action. in_lobby / in_queue / in_voice cards keep the existing pill (no hero, no spectator hook) — Task #226 explicitly scopes the hero portrait + Watch link to in_game.\n\nQueue tooltip: in_queue cards now show a 'In queue for 4m' / 'In queue for 1h 23m' tooltip computed from the new `joined_at` field via a small `formatSince()` helper (minute-precision below 60m, hours+minutes above). The other live states keep the original 'Updated 14:32:08' freshness hint so visitors can still see how recent the snapshot is.\n\nLayout/a11y: the previous design wrapped the entire card in a `<Link>`, which made it impossible to embed an `<a>` Watch link inside (anchor-in-anchor is invalid HTML). The card is now a plain `<div>` with the player name itself as the inner `<Link>` and the Watch / Copy ID buttons as siblings, so each card has multiple independent affordances and stays valid HTML. `npm run check:a11y` green (153 JSX / 3 CSS files scanned). Full-edition only — the community edition has no presence rollup wiring.",
    "author": "System"
  },
  {
    "version": "6.76",
    "title": "Featured Player auto-rotates each week when no admin pick is queued",
    "published_at": "2026-05-09",
    "content": "Task #222: Profile Spotlight (v6.64) shipped without an algorithmic fallback — if a superuser forgot to schedule the next week's pick, the home-page Featured Player card just went empty until someone noticed. v6.76 adds a weekly auto-pick rotation with the same admin-curated rows still taking precedence.\n\nSchema: profile_spotlight gets a backwards-compatible `source TEXT NOT NULL DEFAULT 'admin'` column (added via ALTER … ADD COLUMN IF NOT EXISTS so existing rows stay 'admin'). New idx_profile_spotlight_source index for the lookback queries.\n\nDB helpers (src/db/index.js): new `pickAutoSpotlightCandidate({ days, excludeAccountIds })` runs a three-tier fallback chain — (1) PERF leader of the past 7 days (highest persisted player_stats.perf, threshold ≥6.0 so we don't crown a mediocre week), (2) hottest current win streak (length ≥3, computed via window-function ROW_NUMBER over recent matches), (3) most-improved MMR via the existing getMostImproved(7) query. Each candidate carries a generated headline + blurb naming the source (e.g. 'PERF leader of the week — 9.4', '🔥 6-win heater', 'Most improved — +400 MMR'). New `ensureAutoSpotlight({ windowDays=7, recentLookbackDays=28 })` is the idempotent worker: returns `{created:false, reason:'already_active'}` when getCurrentSpotlight() finds anything live, looks up the last 28 days of auto-picks to feed the exclude list (so the same player doesn't re-feature week-over-week), and inserts a `source='auto'` row spanning [NOW(), NOW()+7d). When a queued admin row falls inside the auto window, the end is clamped to the admin row's `starts_at` instead of skipping the whole week — the auto pick fills the gap up to the admin handover. Sub-1h clamped windows still skip (`reason:'overlap_with_admin'`); brand-new instances with no candidates skip too (`reason:'no_candidate'`).\n\nAdmin precedence is enforced in two places. (a) getCurrentSpotlight now sorts `ORDER BY (s.source='admin') DESC, s.starts_at DESC` so an admin row beats a simultaneously-active auto row. (b) createSpotlight, when called with the default source='admin', first runs an UPDATE that ends every conflicting auto row up front before the existing overlap guard runs — so an admin can drop in a hand-curated mid-week pick without manually deleting the auto row first. Auto inserts still go through the same overlap guard and back off cleanly if a queued admin row would conflict.\n\nCron (src/discord/bot.js): the existing hourly `advanceSpotlight` tick now calls `ensureAutoSpotlight()` immediately after, so any spotlight that ages out is replaced within the hour rather than leaving the home card empty for up to a week. A new dedicated weekly cron fires Mondays 09:00 UTC as the canonical 'fresh week, fresh featured player' beat. Both ticks are silent no-ops while admin rows or fresh auto rows are still active — the log line only fires when something actually rolls over.\n\nUI: web/src/components/HomeWidgets.jsx FeaturedPlayer card now reads the `source` field from /api/spotlight/current and renders an amber '⚙ Auto-selected' pill (with a tooltip explaining the rotation) when source==='auto', vs the original brass 'spotlight' pill for admin rows. /admin/spotlight gains a Source column with brass 'Admin' / amber 'Auto' pills so superusers can see at a glance which rows the cron created. The community edition has no spotlight surface, so this is full-edition only.\n\nCoverage: tests/autoSpotlight.test.js (8 tests, full suite green) exercises the full chain end-to-end against a fake pg.Pool — admin-active no-op, PERF-leader pick, hot-streak fallback, most-improved fallback, no-candidate path, admin-creation auto-row eviction, queued-admin window-clamp, sub-1h skip, and the admin-vs-auto tie-break in getCurrentSpotlight. A11y gate green (153 JSX/3 CSS files scanned).",
    "author": "System"
  },
  {
    "version": "6.75",
    "title": "Vanity links unfurl on Discord/Twitter and have a Discord lookup command",
    "published_at": "2026-05-09",
    "content": "Task #221: vanity slugs (`/p/<slug>`) shipped in v6.64 but were second-class for sharing — pasting one into Discord/Twitter showed bare text instead of a card, and there was no in-Discord way to look up 'who is /p/foo' without leaving chat.\n\nThree-part fix.\n\n(1) Open Graph + Twitter card meta tags on `/p/<slug>` for unfurlers. The route in src/web/server.js sniffs the User-Agent for the well-known social bots (Discordbot, Twitterbot, Slackbot, facebookexternalhit, LinkedInBot, TelegramBot, WhatsApp, RedditBot, SkypeUriPreview, Embedly, Iframely, Googlebot — case-insensitive substring match) and serves a tiny HTML response with `og:type=profile`, `og:title`, `og:description` (built from the player's display name + MMR + W/L + win-rate from getNickname/getPlayerRating), `og:image` pointing at /oa-logo.png, full Twitter `summary` card tags, a canonical link, and a `<meta http-equiv=\"refresh\">` fallback so the rare crawler that does follow the page still ends up at /player/<accountId>. Real browsers (Chrome/Safari/Firefox) keep the fast 302 redirect they had before — the OG response is crawler-only — so users never see a flash of HTML.\n\n(2) `/whois <slug>` and `/profile <slug>` Discord application (slash) commands, plus `!whois` / `!profile` prefix aliases for backward compat. The slash commands are registered via discord.js's REST + Routes on the bot's `ready` event — guild-scoped when `DISCORD_GUILD_ID` is set (instant rollout) and global otherwise (up to 1h propagation). A new `interactionCreate` handler routes `/whois` and `/profile` through the same shared `_resolveVanityLookup()` helper as the prefix aliases, so both surfaces always render the identical embed. The lookup accepts the bare slug, `/p/<slug>`, or a full URL (regex extracts the slug component), validates against db.isWellFormedVanitySlug(), resolves via getAccountIdByVanitySlug(), and replies with a brass-coloured embed showing display name (nickname → rating display_name → 'Player <id>' fallback), MMR tier badge via getMmrTier(), W/L + win-rate, account ID, and a clickable share URL built from `process.env.SITE_URL` (default https://oceinhouse.gg). Failures during slash registration are logged but never crash the bot — the prefix alias still works. Prefix command also listed under Stats & Rankings in the !help embed.\n\n(3) Profile page Share button now copies the short URL. Added `GET /api/player/:id/vanity-slug` (public, 60-second cache, returns `{slug:null}` for unclaimed accounts so unauthed callers don't 404), wired PlayerProfile.jsx to fetch it on mount, and updated the existing 🔗 Share button to copy `${origin}/p/<slug>` when claimed and fall back to `window.location.href` otherwise. Added `aria-label` + `title` so the target URL is obvious to screen readers and on hover.\n\nFull-edition only — community edition has no vanity slugs. A11y gate green (153 JSX/3 CSS files scanned).",
    "author": "System"
  },
  {
    "version": "6.79.1",
    "title": "Cosmetics shop previews · Test-coach shortcut · In-app currency phase 1 · Stripe checkout fix",
    "published_at": "2026-05-16",
    "content": "A four-part batch tackling shop UX, dev/test ergonomics, monetisation balance, and a hot Stripe regression.\n\n**1) Stripe checkout — `automatic_payment_methods` revert.** Stripe started rejecting v6.78's `automatic_payment_methods: { enabled: true }` upgrade with 'unknown parameter' on every Checkout call site — Founders Pass purchases, Pro upgrades, gift checkouts, frame purchases, tournament buy-ins and season buy-ins were all dead in the water. Reverted the seven non-coaching calls in `src/web/server.js` back to `payment_method_types: ['card']` to unblock all buyers. Coaching booking (line 9451) was already card-only because of its `capture_method: 'manual'` escrow flow, so it was unaffected. Wallet methods (Apple Pay / Google Pay / Link / BECS) are temporarily off until we re-enable them through the Stripe dashboard's payment-method-domain config instead of the API parameter.\n\n**2) Cosmetics shop previews (Task #312A).** Every card in `/shop` now shows what the buyer is actually getting before they pay. Six new preview components in `web/src/pages/CosmeticsShop.jsx`:\n- `FramePreview` — 56 px avatar circle with the real `FRAME_META.style` outline + boxShadow applied, so the buyer sees the exact ring/glow they're purchasing.\n- `LayoutThemePreview` — mini Magazine v3 banner driven by a per-theme palette map (`court-pitch`, `newsprint`, `carbon`, `holo`, `heritage`, `broadcast`), with the theme's bg/accent/text/muted tokens rendering an identifiable banner-card mock.\n- `VoicePackPreview` — ▶ Play button wired to the existing `/voice-packs/<pack>/win.mp3` asset via `voicePackUrl(p, 'win')`, with start/stop toggle and proper `aria-label` so it works for screen-reader users.\n- `TitlePreview` — renders 'PlayerName · <title>' the way it appears under your name on a profile card subtitle.\n- `FoundersRingPreview` — cover tile with the brass→amber ring effect.\n- `AccentPreview` — mini stat-card retinted with the accent colour.\nAll six are accepted by `CosmeticCard` via a new `preview` prop and wired into all six section renderers (Founders, Voice, Layout themes, Frames, Titles, Accents).\n\n**3) Test coach end-to-end (Task #312).** Stripe Connect Express requires real KYC (DOB, address, bank, SSN), so dev-testing the coaching marketplace UI was effectively impossible. New superuser-only Admin Panel button '🧪 Test: Promote to Coach (skip Stripe Connect)' hits `POST /api/admin/coaching/promote-test-coach`, which inserts a `coaches` row with a synthetic `acct_test_<accountId>_<ts>` Stripe id and flips status to `active`. The editor (`/coach/edit`), availability picker, public profile, and `/coaches` listing all work without any Stripe round-trip. Bookings still need real Stripe — the new 'Test coach end-to-end' section in `replit.md` documents the test-mode `sk_test_…` swap that makes booking checkout succeed against Stripe test cards (`4242 4242 4242 4242`).\n\n**4) In-app currency phase 1 (Task #313).** A coin economy that gives non-Pro players a path to individual cosmetics without killing the website's Stripe monetisation. Hard-to-grind by design: prices are deliberately above the equivalent USD value so coin-buy is an alternative, not a shortcut.\n- **Schema.** Two new columns on `player_profiles` (`coin_balance`, `coin_lifetime`) + a `coin_transactions` ledger table (account_id, delta, reason, ref_match_id, created_at) + a `coin_owned_cosmetics` table (account_id, kind, value, coins_spent). All bootstrapped via the existing `CREATE/ALTER TABLE IF NOT EXISTS` chain in `src/db/index.js` — no manual migration needed.\n- **Earn rules (this phase).** +10 coins per match per signed-in player + 5 win bonus, granted inside `recordMatch()` after commit. Gated by match duration ≥ 20 minutes so short stomps/abandons don't farm coins. Soft daily cap of 100 per account enforced inside `grantCoins({ applyDailyCap: true })` — concurrent grants are atomic via SUM-then-min, so a player can never exceed 100/day even if two matches land at once. MVP / achievement / hot-streak bonuses are wired in the patch-notes scope but ship in a follow-up so this lands clean.\n- **Spend.** Server-authoritative price catalog: voice packs 800 🪙, layout themes 1200 🪙, profile frames 2500 🪙 (priced ABOVE the $5 Stripe price so the coin path stays the long-grind option). Titles + theme accents are deferred to phase 2 (save validation doesn't yet honour coin ownership for those). `purchaseCosmeticWithCoins()` is fully atomic: `FOR UPDATE` lock on `player_profiles` → ownership check → debit + ledger row + `coin_owned_cosmetics` insert, all in one transaction. `INSUFFICIENT_FUNDS` → 402, `ALREADY_OWNED` → 409.\n- **Validation wiring.** The `/api/me/profile` POST validators for `selected_voice_pack` and `profile_layout_theme` now accept `hasCoinCosmetic()` as a second ownership path alongside `isPro`, so a coin-bought voice pack actually applies on the profile. `getOwnedFrames()` unions in `coin_owned_cosmetics` rows where `kind='frame'` so coin-bought frames show up everywhere a Stripe-bought frame does.\n- **UI.** A new 🪙 balance pill in the global navbar (`web/src/App.jsx` → `NavCoinPill`), signed-in only, polls `/api/coins/me` on mount + every 60 s and links into `/shop`. The shop header gains a balance banner showing spendable balance + lifetime earned + earn rules. Every Pro-bundled cosmetic card gets an 'or N 🪙' alt-buy button — disabled with affordance copy when the user is short — that hits `POST /api/coins/spend` and flashes a success/error message. Founders Pass stays Stripe-only by design (limited-edition vanity SKU shouldn't be grindable).\n- **Admin.** New superuser `POST /api/admin/coins/grant` for one-off grants (e.g. comping a tournament winner). Phase-1 supports positive deltas only; clawbacks unsupported.\n\nEdition scope: full edition only — community edition has no monetisation/cosmetics surface. A11y gate green (the shop coin button has explicit `title` + the nav pill has both `aria-label` and `title`).",
    "author": "System"
  },
  {
    "version": "8.01",
    "title": "Personal Season Wrapped — Spotify-style retrospective + auto-DM on rollover",
    "published_at": "2026-05-28",
    "content": "Task #443 (full edition only): season rollover now hands every participant a Spotify-Wrapped-style personal retrospective they can flip through and share.\n\n**New page `/wrapped/:seasonId/:accountId`** (`web/src/pages/WrappedSlideshow.jsx`). 8–10 card slideshow with keyboard nav (←/→/Home/End plus on-screen prev/next + restart), real `<button>` controls with `aria-label`s, `role=\"region\"`+`aria-roledescription=\"carousel\"`+`aria-live=\"polite\"` on the stage, a copy-to-clipboard share button, and a profile back-link. Cards generated server-side: **intro** (season header), **hero** (most-played + win-rate), **item** (most-frequent first-purchase, filtered against a SKIP_ITEMS set so the card surfaces a real mid-late game item not branches/tangos/wards), **comeback** (your win with the highest `matches.comeback_factor`), **mvp** (MVP votes received this season, table-existence-guarded so community-edition-style schemas don't blow up), **streak** (longest in-season win streak from a date-ordered walk of your matches), **rival** (opponent met most often, ≥2 games), **teammate** (ally with the most wins together, ≥3 games), **kda** (season averages + GPM), **grade** (S/A+/A/B+/B/C/D/F from a 55/45 blend of win-rate and PERF-derived percentile, each grade has a hand-written one-liner).\n\n**Three new public-read API endpoints** in `src/web/server.js`. `GET /wrapped/me/latest` (signed-in only) resolves the viewer's most recent archived season + their account id so the user-menu shortcut can client-side-redirect into the canonical URL without a round-trip. `GET /wrapped/:seasonId/:accountId` and `GET /wrapped/:accountId` (most-recent-archived fallback) return the structured card payload, both validated as digit-only ids in-handler (path-to-regexp v8 / Express 5 won't accept regex-in-param syntax). All three reuse the existing `_v3PanelGet` 60s in-memory cache. Two new DB helpers exported from `src/db/index.js`: `getSeasonWrappedCards(accountId, seasonId)` (one season fetch, eight focused per-card queries in series — each wrapped in its own try/catch so a missing table or NULL column degrades that one card instead of failing the whole payload) and `getSeasonParticipants(seasonId)` (used by the DM fan-out below).\n\n**OG share card** (`src/services/wrappedOgCard.js`, mounted at `GET /og/wrapped/:seasonId/:accountId.png`). 1200×630 @napi-rs/canvas render in the OCE Inhouse palette — ink-navy gradient backdrop, brass left rule, brass diagonal sweep behind a circular grade medallion (radial brass→amber gradient, ink-navy stroke, oversized Playfair grade letter, \"SEASON GRADE\" caption), Inter eyebrow + amber season name + Playfair-bold player name truncated to fit, italic Playfair tagline with `games · wins-losses · WR%`, up to three rounded pill highlights (signature hero / N-win streak / N MVP votes), oa-logo footer. 200-entry / 10-minute in-memory cache keyed on (account, season, name, grade, summary). Graceful degradation: when @napi-rs/canvas isn't installed the endpoint 302s to `/oa-logo.png`. Unfurler intercept on the SPA route mirrors the `/player/:accountId` pattern from Task #258 — sniffs Discordbot/Twitterbot/Slackbot/etc. via the existing `_isSocialUnfurler` helper and serves an OG/Twitter meta-tag stub with a `<meta http-equiv=\"refresh\">` fallback so real browsers fall through to the React SPA untouched.\n\n**Rollover hook** in `src/discord/bot.js` `_closeSeasonAndAnnounce`. Post-commit (so a DM hiccup can't roll back the rollover the way the announce embed inside `onBeforeCommit` does), a `setImmediate` fan-out walks `getSeasonParticipants(closedSeason.id)` and DMs each player via the central `notify(accountId, 'season_wrapped', { discord: { content } })` helper. The DM links to their personal `/wrapped/<seasonId>/<accountId>` URL and includes a one-line summary (grade · games · W/L · WR%). Per-user failures are silent so one malformed user can't tank the rest of the batch; a single `console.log` at the end records the success count.\n\n**Notification preferences.** New `season_wrapped` event added to `NOTIFICATION_EVENTS` in `src/db/index.js` with defaults `{ discord: true, push: false }` and a label/description ready for the existing v2 preference centre UI to surface — users can opt out per-channel before the next rollover.\n\n**User-menu shortcut** in `web/src/App.jsx`. New '🎁 Season Wrapped' link between 'My matches' and 'Compare H2H vs…', signed-in only. Points at `/wrapped/me/latest` which mounts the slideshow page in resolveLatest mode — the page fires `getMyLatestWrapped()` then `navigate(..., {replace:true})` into the canonical URL so the browser back-button doesn't trap the user in the redirect.\n\n**Scope:** full edition only — community edition has no season rollover ceremony and is untouched.",
    "author": "System"
  },
  {
    "version": "6.78.1",
    "title": "Pick (and preview) the hero on your share-link card",
    "published_at": "2026-05-09",
    "content": "Task #259: the OG share card auto-picked your pinned hero (or fell back to most-played when nothing was pinned), but you couldn't see what the unfurl actually looked like before pasting your /p/<slug> into Discord, and you had no way to override the auto-pick if you wanted a different signature on the card than the one pinned on your profile page.\n\nSettings → Profile gains a new 'Share card hero' picker (separate from the existing 'Pinned hero' control) with three modes: 'Use pinned hero' (the default — falls back to most-played when nothing is pinned), 'Use most-played hero' (forces the most-played fallback even when a pinned hero is set), and a free-form search of your played-hero pool that lets you pin any hero to the share card without changing your profile pin. The override is persisted as a new `share_card_hero_id` field on the existing `extras` JSONB column on player_profiles — no schema migration, validateExtras() coerces the value to either null, the string sentinel 'most_played', or a positive integer hero id (anything else is dropped so a malformed client save can't poison the field). The save route additionally checks numeric ids against player_stats so a malicious client can't persist a hero you've never played.\n\nThe picker mounts a real `<img>` of `/og/profile/by-id/<accountId>.png` as a live preview, so you see exactly what crawlers will render — name, MMR tier badge, W/L, brass accent, hero portrait — before clicking Save. To preview the unsaved selection, the OG endpoint accepts an owner-only `?preview_hero_id=` query param with three values: `pinned` (ignore the saved override and render the pinned → most-played fallback chain), `most_played` (force the most-played fallback even when a pinned hero exists), or a numeric hero id. Owner-gated by `req.session.accountId === accountId` so a third party can't trick the generator into rendering an arbitrary hero on someone else's card; preview responses are sent with `Cache-Control: no-store` (set inside `_renderProfileOgCard` after the buffer is generated, so the saved-state branch keeps its 10-minute public cache and the preview branch can't be evicted) so the in-memory render cache used by the public endpoint isn't polluted. _resolveOgProfileHero() now reads `extras.share_card_hero_id` first, then falls through to the existing pinned → most-played logic when the override is unset (or when an explicit preview asks it to be ignored).\n\nFull edition only — community edition has no profile customization or OG card surface. A11y gate green (153 JSX / 3 CSS files scanned).",
    "author": "System"
  }
];
